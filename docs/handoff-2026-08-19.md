# Handoff — 2026-08-19. READ FIRST.

**LANGUAGE RULE: ASD-STE100 Simplified Technical English.** See `CLAUDE.md`.

Supersedes `docs/archive/handoff-2026-08-18b.md` (consumed).
★ Planning doc: `docs/master-worklist-2026-08-17.md`.

**★ #380 SESSION BUDGET MODE stands.** Sessions are scoped SMALL. One Opus
review round, only when C#/money/data paths changed. Smoke as BOOKENDS + ONE
batched mutation-proof run. No agent fan-outs. If an item balloons: STOP at a
triage doc + plan, hand off, do not build.

---

## 1. State — verify, do not trust

```
git --no-optional-locks log --oneline -3
git --no-optional-locks status --porcelain
```

The **#381–#384 batch** (N12 + N40 + the F5 verdict) is committed by Damir on
top of `26f29133`. Verify the chain reaches `26f29133` → `400468f6` →
`646ef4e9`. If it does not match, STOP and say so.

**Smoke baseline: 1971 / the 4 KNOWN pre-existers (#136 · #149③ · M5 · B3).**

| Item | State |
|---|---|
| **N12** | **BUILT + F5 PASS** (5/5 legs). A restored account opens the tail on "Join the community"; the 30-day reminder clock is seeded at the restore |
| **N40** | **BUILT + F5 PASS** (9/9 legs) — ⚠ **but see §2, the verdict has a caveat** |
| **N13** | CLOSED (works on device) |
| Docs | DECISIONS #381–#384 · `docs/n40-triage-connecting.md` · security gate §#383 · worklist rows N63–N70 |
| ⚠ | `Config.cs` was temporarily lowered to `spixi-0.9.0` for the F5 and is **reverted** in the batch. Confirm it reads `spixi-0.9.22` before committing |

## 2. ★ The N40 caveat — read before claiming D-21 is closed

The served update file advertises **0.9.22 — the same number as the shipping
build**. So on an unmodified build the compare is 0, the update arm never wins,
and **M1 cannot be what Damir originally observed.** The #383 fix is still
correct and necessary — it fires for every user the day 0.9.23 ships — but the
original "Connecting… stops showing after long use" is **NOT proven fixed**.

`docs/n40-triage-connecting.md` §3–§4 stays live, and **N69(a) below is now the
leading candidate**: the app can sit in a genuine no-client state for a long
time, which the indicator reports truthfully.

## 3. NEXT SESSION — the bug round (Damir's call: fix the crucial ones)

Six findings came out of the #383 F5. Take them in this order; **pick ONE or
TWO**, budget mode still applies.

1. **N65 🔴 — the Windows language pick is a silent, total no-op.** Highest
   value: it is user-visible, it is reproducible on the desktop you develop on,
   and the suspect is already named — `SettingsPage.xaml.cs:469-497` puts the
   preference, the `setLocale` push, `reloadShell()` and the live-chat reloads
   ALL inside `if (SpixiLocalization.loadLanguage(lang))`, so a `false` return
   does nothing and reports nothing. **Triage first (#215):** log what
   `loadLanguage("pt-br")` actually returns on Windows before touching code. If
   it returns true, the bug is the FOUR-way state split (hub dictionary · row
   value · checkmark · home shell) — enumerate them separately. Either way the
   handler must stop failing silently.
2. **N66 🔴 — only the OS-FOLLOW theme path is broken.** Explicit Dark/Light
   themes everything correctly; System leaves the Android Account screen light
   and makes the Windows panes disagree. **Diff the two paths** — do not invent
   a third. Prime suspect: the parked Account WebView (#315) is missing from the
   flip fan-out, the same "a collection was missed" class as #251 and #284.
3. **N69 🔴 — first connect after account creation never completes until a
   restart**, and a contact request sent in that window reports success and is
   lost. Split it: **(b) the silent loss is FE/C# and fixable on its own** — an
   outgoing request accepted while disconnected must queue-and-say-pending or
   refuse honestly. **(a) is C#/Core** and may also explain the original D-21.
4. **N67 🔴 — ONE "Delete Spixi account" action** (Damir's product call).
   Destructive path: reproduce the delete→restore error FIRST, write a
   security-gate row, and keep the W14/#348 live-wallet guard intact.
5. **N68** — the fatal-exception dialog on failed-restore→create. **logcat
   before code.**
6. **N70** — the update notice does not appear when the app started offline
   (re-arm the hourly check on the offline→online edge). Small; rides N64.

**Also open from the same F5, not urgent:** **N63** (10 keys untranslated in all
7 older locales — the "Show older messages" pill) · **N64** (the update-notice
design round: scope, missing CTA, warning-vs-info tone, copy).

## 4. Do not touch

N57 stays Core-side — Damir's protocol run is still pending, and it is still his
top build-unblocking action. The §5 deferred pile and the archived 17g §6 list
stand. N10 · N15 · N39 keep sliding; they are lower value than the six above.

## 5. How to work — unchanged

Cloud twin recipe: clone/tar → checkout the batch commit → `npm install jsdom
--no-save` → **bundle BEFORE shells** → smoke green at **1971/4** (state the
number when it moves). String-change pipeline: extract → build-locales →
build-strings-iife → (bundle → shells) → verify-locales + i18n-lint + pseudo +
**i18n-overflow-audit**. Standing set: verify plans against code (#215) · #294 ·
mutation-honest pins, PROVEN · Damir commits · DECISIONS rows at decision time ·
the #46 loop on Opus (#352) when C#/money/data paths change · the security
handover gate while building · tarball delivery · `git --no-optional-locks`
always (a plain `git status` strands a lock the mount cannot delete).

**★ New standing gotcha (#383): a green smoke run does NOT prove the GENERATORS
work.** `build-demo-bundle.mjs` had been dead in the tree for an unknown number
of sessions (8 component files are CRLF; the import stripper is LF-shaped). If a
batch touches components, run the bundle build EARLY and read its output.

Ixian-Core reference: `097341a`. Deploy — Android:
`dotnet build Spixi\Spixi.csproj -f net10.0-android -c Release -t:Run`.
Windows two-step: `dotnet build Spixi\Spixi.csproj -f
net10.0-windows10.0.19041.0 -c Debug`, then run the exe (⚠ `-t:Run` does NOT
chain the build on Windows). ⚠ Any C# change → wipe `Spixi\obj`, `Spixi\bin`.
