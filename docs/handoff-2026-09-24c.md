# Handoff — 2026-09-24 (c) → the OFFICE iPhone day

**Read this first (#215).** It replaces `handoff-2026-09-23d.md` and `handoff-2026-09-24b.md` as the entry
point (both moved to `docs/archive/`; their office plan is folded in below). The walk sheet is
`docs/walk-artifact-ios-office.html` — **39 rows** now (§6 added: the AE–AF changes on the iPhone).

## 0 · State of the tree you inherit

* **Session AF is committed and pushed** (Damir, GitHub Desktop, 2026-09-24): #944–#970, the two AF walks
  (AF 16 · 0 · 7 · AF2 15 · 0 · 2), and the Core patch record. Damir pulls on the office Mac.
* Suite: **4883 / the 2 KNOWN** (#136 · B3), container, snapshot, Core sibling @ `097341a`. Four `--check`
  gates green. `CHAT_KB_CEIL` 660 (headroom ~1.4 KB).
* **Ixian-Core is patched LOCALLY on the PC only** (#962 — the late-accept null guard, uncommitted in that
  repo). The office Mac's Ixian-Core is the clean `097341a` — so on the Mac a Debug session CAN still break on
  that NRE (a caught first-chance exception; Continue resumes). This is expected, not a regression. The BE
  engineer adopts the patch (`docs/core-patches/962-accept-null-guard.patch`).
* The iOS-only code has **never compiled**: `Spixi-PushService/` (`SpixiPushGate.cs` · `NotificationService.cs`),
  `Platforms/iOS/SPushPrefsShare.cs`, `SPushService.cs`'s iOS changes, #912 ② (`NSURLIsExcludedFromBackupKey`),
  #490's iOS line. The Apple portal is DONE (#932; iO.1 = P).

## 1 · The office day, in order

1. **Pull**, then install the two development profiles (Xcode → Accounts, or double-click).
2. **Wipe `obj`/`bin`** → build **Debug for the iPhone**. ⚠ First compile of the files in §0. **A build error is
   a Session AC/AD bug — paste it whole into the chat; do not guess.** First suspects: the extension's
   `SupportedOSPlatformVersion`; the App Group id string must be IDENTICAL in both Entitlements;
   `OneSignal_app_groups_key` in both Info.plist (`docs/ios-nse-spec.md`, DECISIONS #919/#921).
3. **§1 push parity is the reason for the day.** ★ **iO.7 (the mute test)** is the one question no document
   answers: a muted chat's push shows nothing, or a blank row? (`UNNotificationServiceExtension` can only
   MUTATE, never cancel; Suppress = rewrite to EMPTY.) The note IS the answer.
4. §2 iOS-43/44/55 (built #905, never walked) · §3 every screen · §4 landscape (the bar stays, the rail
   refuses on iOS) · **AE.6b** rides along (the fade through the keyboard slide) · **§6 the AE–AF rows**
   (You:, the tall language sheet with its visible scrollbar, the lower pill, the top-aligned select circle,
   no row lines, the stacked admin buttons, the second-tip refusal, no Cancel request).
5. §5: APP-1 restore (optional) · **route B (#920) LAST** and only if the iPhone walk is done — the first Mac
   run is the `dlopen` test the July stamp never reached.
6. Anything that reads wrong on the device is a walk **NOTE** first (row, what you saw, a screenshot), a fix
   second (#935). Fixes found today are a short follow-up round, not built mid-walk.

## 2 · Damir's open decisions (ask, do not assume)

* **#970** — a declined contact request comes back (no reject message in the protocol). Options: a BE
  "declined" message (with RC1/Q18), or an app-side ignore list + an un-block path.
* **Reply-to** — verify on device whether the protocol carries a reply id (~1 h, #232).
* The colon per locale in the "Name: text" prefix (French " :") — dial left open by #969.

## 3 · The code review between sessions (Damir's question, 2026-09-24)

**Answer given: yes to a review, but not before the iPhone walk, and read-only first.** Reasons:
* The office build is the first compile of the iOS code. Any refactor landed before it adds untested
  changes to the one walk that has not happened for ten sessions — a regression becomes ambiguous
  (the same rule `audit-refactor-plan.md` §0 states).
* The review is already in the plan: the **sweep** (`docs/audit-refactor-plan.md` phases 1–4) sits between
  the freeze and the strip (#916/#933). Phase 1 (architecture map) and phase 2 (findings) are READ-ONLY and
  can run any time; phases 3–4 (characterize, refactor one issue at a time) wait for the freeze and
  `git tag audit-baseline` on a clean, walked tree.
* Suggested order: office day → its fix round → **sweep phases 1–2 (read-only report: dead code, duplicate
  logic, over-large shells, the 123 naive comment-strip regexes, perf hot paths already measured)** →
  Damir picks → freeze → phases 3–4 → strip → gate re-run → merge → TestFlight.

## 4 · Then, unchanged (from 09-23d §4–§5)

* **Endgame** (#916 as shaped by #933): L6 (own mini-session, security-gated) → freeze (`maxLogCount 5 → 1`;
  retire `[CDPERF]`, `[SCROLL]`, `[PAINTDIAG]`, `[EXCERPTDIAG]`, the `chats-after-close` probe) → sweep → the
  strip (ONE branch, three comment tiers, workshop docs to a private `spixi-workshop` repo, DECISIONS rewritten
  by area, a `pre-strip` tag) → gate re-run → merge → TestFlight (the extension's DISTRIBUTION profile then).
* Own sessions: batch 4's medium rows (CI6 · C22 · A8 · FC1 · C12 · NT1(b) · PV1) · the Android 30-minute
  memory kill · the translator pass · Windows/MacCatalyst mini-app storage partition test.
* Crash logs owed before any fix: AND-25 · AND-27 · iOS-67 (`release-readiness.md` §1).
* BE engineer: the security walkthrough first (`docs/security-review-for-be-engineer.md`), the #232/#523
  money-path review, the inherited MAJORs, #962 adopt, CORE-9…13. Counsel on the privacy policy.
* Still open, do not re-derive: AND-47 · AND-48 · `docs/diagnostic-session-aa-findings.md` (armed) · S16 · the
  #930 dials · the #940 residuals · AF2 R4 (thumb drag, not walked).

## 5 · Workshop notes

* The suite runs in the container on a SNAPSHOT with the Core sibling (~5 min). Compare deltas in ONE
  environment (#895).
* Mutation roots: `cp -al` the tree, then REPLACE (rm + write) the mutated file — writing in place through a
  hard link mutates the original. Assert the pin under test printed; a stale splice range reads as a kill.
* Landing over the bridge: tar + `sha256sum -c`; `git --no-optional-locks`; never `git add -A` (CRLF churn).
* The #945/#946 lesson held all session: run the FULL suite before calling a batch done.

## 6 · Rules this round paid for

#215 · #294 · #660 · #663 · #771 · #772 · #798 · #811 · #895 · #906 · #935 · #943 · #953/#959 (claim only
what a pin observes). Chat replies to Damir in ASD-STE100 (#931).
