> ⚠ **SUPERSEDED 2026-08-17 by `docs/handoff-2026-08-17d.md`** (the #361/#362 F5 ran: 16/17 PASS, A4 → N36b deferred; the batch is committed). Archived.

# Handoff — 2026-08-17c. READ FIRST.

**LANGUAGE RULE: ASD-STE100 Simplified Technical English.** See `CLAUDE.md`.

Supersedes `docs/handoff-2026-08-17b.md` for state (banner it + archive it with this
commit). Written by the cheap-batch session (cloud twin on `bdf24360`).

★ Planning doc unchanged: `docs/master-worklist-2026-08-17.md` (N-ids).

---

## 1. State — verify it, do not trust it

```
git --no-optional-locks log --oneline -3
git --no-optional-locks status --porcelain
```

HEAD should be `bdf24360` (#356–#360) until Damir commits this batch. The batch
arrives as a tarball (extract at repo root, `tar xzf spixi-361.tar.gz`, NO
`--overwrite` flag) + a DELETE list (tar cannot delete — see the checklist).

| Item | Value |
|---|---|
| Batch | **#361 (all ten §3 items) + loop #362** — UNCOMMITTED, on Damir's disk after extract |
| Smoke | **BASELINE OK — 1757 pass / the 4 KNOWN pre-existers** (was 1703; +54 net, all mutation-proven) |
| Locales | `verify-locales` ALL CLEAN · i18n-lint ✓ · pseudo 9/9 · **zero em-dashes in every built dictionary** |
| Compiler | ⚠ **The C# is UNCOMPILED** (cloud has no .NET): SingleChatPage (N22) · HomePage + AppDetailsPage (N24). Assume broken until Damir builds |
| F5 | OWED — `docs/f5-checklist-361.md` (12 checks + commit hygiene incl. the SVG delete list) |
| Loop | 3 Opus auditors + fresh break-my-verdict → **PASS** (1 MAJOR found+fixed in-loop). Record: DECISIONS #361 + #362 |

## 2. What the batch is

Every item from 17b §3, none needed a dial: N5 (danger screen wraps + the stale
`--sd-master-w` clamp) · N22 (group topbar "N members", C# latched push) · N24
(apps selected row = `selectApp` push at PRESENT time, aria-current on `__open`,
full press-family CSS) · N32 (zero balance → `0.00`, `zeroAmount` in money.js) ·
N36 (select mode: pressable bail + pointer-dead in-bubble controls) · N38
(wallet-receive Share hidden on desktop; the Account half already landed in
#348-W9) · N2a (real `layout-grid` glyph, derived from your `apps.svg` — swap
with a Figma export at will) · N3a (em-dash sweep: en at source, 209 draft
values, 8 legacy lang values that feed redesigned surfaces; +copy-quality fixes
from the loop, ru/sl/fr) · N45+N14a (PNG art everywhere a PNG exists; rating
nudge carries `rate.png`).

## 3. Next sessions

1. **The fix session (Damir present):** D-19b + D-17b per 17b §2 — dials LOCKED
   there; do not rebuild from here. Then F5 re-check + legs 29–30 if testable.
2. After that the plan resumes: **D-5 → D-9 → D1**, then the worklist rounds
   (R1 identity first).

## 4. Flagged for Damir (one-line dials, from this batch's loop — no build owed)

- SR-only aria-label joiners (`'Copy — Your address'` class, ~14 sites) still
  use the em-dash. Spoken punctuation, not prose. Want them swept too?
- de-de carries ~40 spaced EN-dashes (–, correct German Gedankenstrich) that
  mirror the old dashy voice; sl-si has 2. Sweep at the R2 copy/locale round?
- Group count has no plural form ("1 members") — pre-existing bot class, R2.
- Select-mode dead controls are still TABBABLE (Enter toggles the row — an
  affordance lie for SR users; an `inert` pass is a behavior change, so parked).
- `selectChat` (chats pane) still pushes at the call site — the same
  fire-and-forget class the loop fixed for `selectApp` (loop A-1). Future round.

## 5. How to work — unchanged

Cloud twin recipe as in 15c §7 (archive). Standing set: verify plans against
code · #294 · #215 · smoke green at **1757/4** (state the new number when it
moves) · mutation-honest pins, proven · Damir commits · DECISIONS rows at
decision time · the #46 loop on anything substantial · **Opus for every
adversarial pass (#352)** · the security handover gate while building (this
batch: no new verb, no storage key, no sink, no fetch — `selectApp` is one new
display-only C#→JS push).

## 6. Do not re-attempt

All of 17b §6, plus:
- Do not stamp selection state on the `.c-app-item` WRAPPER — `__open` is the
  paint box and the focusable control; the wrapper must stay attribute-free
  (base.css `:not(.c-app-item)` guards; loop #362 B-MAJOR-1).
- Do not push `selectApp`/pane-coupled UI state from a `pushPageLoaded` call
  site — staged pages can be dropped before present; push from
  `onOverlayPresented` (loop A-1).
- Do not "fix" the em-dash sweep by editing `src/strings/*.json` — they are
  GENERATED. English lives in the component/shell fallbacks (+ the capx table
  in `extract-strings.mjs`), translations in `src/strings/draft/*.json`, and
  three N3a smoke gates now fail the run on any new em-dash.

## 7. Open device items

| ID | Finding | State |
|---|---|---|
| D-19b · D-17b | The two reworks | 🔴 OWED — fix session (17b §2, dials locked) |
| #361 batch | This batch | F5 OWED — `docs/f5-checklist-361.md` |
| N40 | "Connecting…" starving on a LIVE document | OPEN — triage |
| Compact-balance · flick-cancel | A52 legs 29–30 | OPEN — untestable last round |
