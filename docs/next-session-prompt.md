Spixi frontend redesign. Repo: C:\Users\Damir\Claude\Projects\Spixi Rework Of Frontend\Spixi
Branch redesign/frontend, Session J committed. Ixian-Core is a SIBLING clone at
..\Ixian-Core, frozen at 097341a.
READ docs/handoff-2026-09-03.md FIRST (it supersedes every earlier handoff), then
docs/fable-build-brief-premium-density.md §10 + §12 (the picks ledger — every value has its
reversal in the token comment), then DECISIONS #748–#758. The Opus verdict
(docs/opus-review-verdict-session-h.md) still has the three MAJOR lessons — read them
before touching the slide, the gates, or anything animated.

VERIFY THE BASELINE FIRST — clean clone, Linux container (npm i jsdom tree-sitter
tree-sitter-c-sharp), Ixian-Core sibling at 097341a. If any number differs, say so and STOP:
bundle 319 · shells 18 · smoke BASELINE OK 4003 / the 3 known (#136 · M5 · B3) WITH the
sibling (4002 without it — the M1 hold-out gate, #748)
locales ALL CLEAN 786 · i18n-lint OK (6 dev exemptions, 2 sites) · pseudo 9/9
cs-syntax 141 clean + 1 known gap · extract-strings --check OK · build-shells --check OK
build-legal-docs --check OK (terms baked · privacy HELD)
Mutate in FULL tar copies, never cp -al. Bundle BEFORE shells, always. Measure the closing
number AFTER the last suite edit. Render before I rebuild (the harness is handoff §3) — my
eye rules every dial. Measure on device before any fix (#215) — the [KBTRAY]/[WEBVIEW]
capture recipe in docs/f5-checklist-session-j.md is the one that works.

Then, in order (handoff §2):
① My A5 re-walk result (keyboard ↔ ⊕ tray). PASS → retire [KBTRAY] + [CDPERF] together.
   FAIL → read the [KBTRAY] order I paste, then and only then touch #721.
② The logged rows, each with its mechanism in the handoff: the received-sticker reaction
   pill · the identical group avatar colours · the Apps ⋯ menu as an anchored dropdown ·
   the Add-app open stutter (measure first) · the two Windows/WebView2 ghosts (one stamp
   pair, then the WebView2 background colour from C#) · the icon-only button without a label.
③ Walk fallout from my eye on the rest (reversals in the token comments).
④ The TG-order chat-info rebuild — render round, I pick.
⑤ My pending rulings as I give them (URL previews · privacy wording · "left the group").
⑥ The iOS rows (Session G walk 15–27, 37) when I say I am on the Mac.
Commit is mine, in GitHub Desktop; write the message file, the handoff, and the walk
artifact (clickable, P/F/N per row, copyable results) — plus the PowerShell build blocks.
Archive consumed handoffs/checklists into docs/archive; docs/ ends with ONE handoff and ONE
checklist.
