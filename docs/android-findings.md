# Android findings — first device run (2026-08-11, Samsung R5CRB1MWPPZ, Debug via Mac)

First-ever Android run of the redesign. Same rules as the iOS punch list
(ios-sim-findings.md): platform bugs only, fix small, log in DECISIONS.

| # | Finding | Root cause / read | Status |
|---|---------|-------------------|--------|
| AND-1 | **CRASH + wedged onboarding on fresh install**: fatal NRE dialog over the intro carousel, re-thrown 1 Hz (TransactionInclusion.onUpdate), language taps blocked | **ROOT-CAUSED from logcat (#329):** Android fires `RequestedThemeChanged` during boot → App.xaml.cs:127 `reloadAllPages()` → `HomePage.Instance()` — which CONSTRUCTS the singleton → its ctor `Node.start()` on a WALLET-LESS install → `IxianHandler.getWalletStorage` NRE (PresenceList.init) + the 1 Hz re-throw. Latent on EVERY platform (any pre-login `Instance()` touch); Android's boot-time theme event was simply the first trigger to fire. | **FIXED (#329)** — `HomePage.InstanceOrNull()` (read-only, never constructs) + all 7 read-only sites switched (reloadAllPages · Node UI tick · Node.shutdown · Android push-intent · iOS push-tap · 2× Utils detail lookups); `Instance()` construction remains ONLY for the App-start wallet-present path. Device re-run owed |
| AND-2 | Slow past splash (30s+) + language-change hang/ANR — SURVIVED the AND-1 fix | **ROOT-CAUSED (#330): the Android-only `localizeHtml(Stream)` overload built the page via `lines += line` — QUADRATIC over a ~22,000-line / ~1.3MB shell (gigabytes of copying per page load, on the UI thread); the language pick re-runs generatePage → same loop again. Legacy pages were small enough to hide it; the file overload (all other platforms) always streamed | **FIXED (#330)** — StringBuilder (linear, byte-identical output). Device re-run owed |
| AND-3 | Edge-swipe back (#325) vs Android system back gesture — who owns the left edge? | Gesture-nav Android reserves screen edges for system back; the in-chat shell swipe may never see those touches (system wins) — that is acceptable (system back routes through OnBackButtonPressed) but must be VERIFIED | OPEN — observe on device |
| AND-4 | Full Android walk owed once AND-1 verifies: language pick → account create → contact → chat → keyboard (native resize, no double-lift) → send-log share (R5 leg) → header physics (#327) → sl-si dates everywhere (iOS-55/#328 surfaces) | The S6 sweep leg, now unblocked on real hardware | OPEN — the walk |
| AND-5 | Onboarding "Back up now" chosen → flow just continues past it (no backup surface) | **= the known L3 launch row, now device-confirmed:** the onboarding tail has NO backup hand-off — no `ixian:backup` route on OnboardPage, so the tail completes regardless of the choice. Cross-platform (iOS has the same gap, untested there); already in the cheap-C# basket (ios-launch-gaps C: L1/L2/L5/L7 family — promote L3 into round 1) | OPEN — cheap-C# round 1 |
| AND-6 | Status bar renders a wrong BLUEISH color | Android chrome theming — almost certainly MAUI's default Material colorPrimary showing through (the Android twin of iOS-1/4: native chrome not following the app theme). Fix family: Android styles/colors + status-bar color follow the app theme incl. dark mode | OPEN — Android chrome batch |
| AND-7 | Welcome/onboarding screens not full-bleed (strips at system bars) | The #282 edge-to-edge class on Android: MainActivity has an InsetsListener but the launch surfaces don't bleed under the system bars. Same fix family as AND-6 — one Android-chrome round: edge-to-edge + themed bars + safe-insets, mirroring what #282 did on iOS | OPEN — Android chrome batch |
| AND-8 | After performing a backup, the Backup page still shows "not backed up yet" | The #283/#314 interim stamp (`spixi.backup.last`, localStorage, written on share LAUNCH) isn't being read back. Two suspects, in order: **(a) the C-9 storage class on ANDROID** — Android's generatePage loads shells via `HtmlWebViewSource` + assets BaseUrl (NOT the file:// path the other platforms use), and localStorage persistence for that origin is UNPROVEN on Android WebView — if dead, this is way bigger than backup (drafts · pins · mentions-seen · chat prefs · scan grant all dead on Android); (b) the stamp-write path simply not firing on the Android share leg. QUICK SELF-TEST that separates them: type a draft in a chat, kill the app, reopen — draft gone = storage dead app-wide (escalate to an Android-storage row, C# preference push per the #308 posture, "log it big, don't hack it small"); draft survives = (b), a small share-leg fix | OPEN — self-test decides the class |
| AND-9 | **localStorage DEAD on Android** — one root behind FOUR device symptoms: rating prompt on EVERY chat exit (snooze never persists) · drafts reset on chat exit · Backup page "not backed up yet" + Account badge "action needed" after a backup (stamp unreadable) · Account exit lands on the STALE tab, not the tapped one (the #314 landtab fix reads storage — the iOS bug's Android twin). AND-8's self-test answered | **ROOT-CAUSED + FIXED (#331):** `WebViewRenderer.cs:367` explicitly set `DomStorageEnabled = false` (legacy never used localStorage; Android WebView also defaults it off — every other platform ships it on). → `true`; DatabaseEnabled (WebSQL) stays off. Expected also healed: pins · mentions-seen · chat prefs/textscale · scan grant · media-loaded · settings stash. Device retest owed: rating snooze holds · draft survives · backup badge clears · Account exit lands on the TAPPED tab | ✅ built · 🟡 retest |
| AND-10 | Chat entry less snappy than iOS; chat details flickers + slower | Perf watch — re-judge AFTER AND-9 (storage churn removed) and on a RELEASE build (the iOS-32 rule: never judge perf on Debug). Also verify the #177/#225 warm-page machinery actually runs on Android (generatePage re-localizes ~1.8MB per cold entry via HtmlWebViewSource — iOS keeps warm WebViews) | WATCH — measure post-AND-9, Release |
| AND-11 | Answering the FIRST call → mic permission prompt; granting effectively DECLINES the call | Runtime RECORD_AUDIO requested at answer time; the permission flow eats the answer. Fix shape: pre-request at incoming-call UI (ring time) — or at first app-run like messenger peers — and/or re-drive the pending answer after grant instead of dropping | OPEN — call-flow round |
| AND-12 | Splash screen background BLACK (iOS shows the blue) | MAUI splash color/theme divergence on Android | OPEN — chrome round (with AND-6/7) |
| AND-13 | Notification-tray icon = bare Spixi mark, no background circle — reads broken next to other apps' notifications | Android notification SMALL ICON needs a proper white-silhouette asset + accent color (Material notification spec); currently falling back to the raw app icon | OPEN — chrome round |
| AND-14 | System-default dialogs (gray Material, pink OK) — off-brand vs the in-app dialog surface | MAUI DisplayAlert renders the platform default theme; fix = theme the MaterialAlertDialog palette (colors.xml/theme) or route alerts through the in-app dialog surface | OPEN — chrome round |
| AND-15 | Notifications CONTENT (x-plat, Damir: "we should discuss"): payment received shows "new message"; incoming CALL notification reads like a message → missed calls | Per-type notification composition: calls want ring-priority presentation (Android CallStyle/heads-up + caller name; iOS equivalent), payments their own copy. Composition is push-payload/BE-adjacent — needs the #232 privacy posture check (what may the payload disclose?) → DISCUSSION row before build, then its own batch | OPEN — Damir + BE discussion |
| AND-3 | ~~edge-swipe vs system back~~ | **VERIFIED (Damir 2026-08-11): BOTH work** — the system back arrow and the in-chat shell swipe coexist on this device. Best outcome; no action | ✅ CLOSED |
| AND-16 | Android keyboard: composer lifts correctly but OVERLAYS the last messages — the log never re-pins | **ROOT (#332):** Android's adjustResize path shrinks the layout viewport; neither #303/#324 inset writer fires (correct — delta≈0) so nothing re-pinned the log after the shrink. **FIXED:** window-resize listener re-pins to bottom while near it (chat.html, keyboardViewport IIFE); iOS structurally inert (constant innerHeight under keyboard, #294 measurement); desktop unreachable (IIFE guard) | ✅ built · 🟡 retest |
| AND-17 | Spixi-bot GROUP chat: sending contact requests to members "mostly didn't work" — only 1 of several actually sent | Unreproduced/unlogged — needs a repro WITH logcat attached (`adb logcat -c` → attempt several requests → dump). Suspects to check at repro: member-sheet request verb per-tap wiring vs a one-shot latch (#268 request-flow states) · C#-side request throttle · bot relay behavior (IXICore, #215-unverifiable in-tree) | OPEN — repro + logcat next round |
| AND-18 | Entering Account leaves the PREVIOUS tab slightly highlighted ("hover-ish") on the home nav | Sticky hover/focus after touch — Android WebView keeps :hover/:focus styling applied post-tap (iOS taps don't focus buttons). Fix shape: blur-after-tap in the bottomnav handler, or audit the nav styles to :focus-visible-only (the #43 hover-guard sweep's Android twin) | OPEN — small FE |
| AND-19 | Going to/from Account always FLICKERS (Android; iOS re-presents warm + clean) | Overlay present/close flash on Android — suspects: WebView default background (white flash class, the #229b family on a different engine) · the #315 park machinery's hide/show ordering on Android · cold first-present. Needs eyes-on with the Inspector equivalent (chrome://inspect) in its own round | OPEN — Android polish round |
| AND-20 | Keyboard open/close is CLUNKY + flickers (even when functional) | Android applies the whole IME inset in ONE jump (MainActivity InsetsListener pads the root view; no WindowInsetsAnimation sync) → the WebView relayouts a 22k-line document in one frame. Design direction, the iOS #303 route: STOP padding for IME natively; push the inset into the page (an insets-animation callback → `__setKbInset`-style per-frame or settled push) and let the composer's 280ms ride (#324) do the motion — viewport stays stable, no full-document relayout | OPEN — Android keyboard v2 round |
| AND-16b | Retest r1 FAILED ("still covers messages") — but the build was INCREMENTAL | Suspect: the iOS stale-Raw-html law's ANDROID TWIN — assets not repacked on incremental build. Retest r2 after a full obj/bin wipe decides: still-covers = mechanism wrong (then probe innerHeight through the transition, the #324 kb-probe Android edition); works = the wipe law extends to Android device builds (write it into the build recipe) | **FALSIFIED — full obj/bin wipe did NOT change the outcome (still covers)** → the resize-re-pin mechanism is wrong for Android's IME path, REVERTED per the #294 discipline (chat.html back to the #328-verified bytes). Next attempt lives in the AND-20 keyboard-v2 round and starts with a MEASUREMENT (the kb-probe Android edition: innerHeight/vv/insets sampled through the IME transition via chrome://inspect), never another guess |
