# Runbook — landing #346 + #347 (2026-08-14)

**LANGUAGE RULE: ASD-STE100 Simplified Technical English.** See `CLAUDE.md`.

## ⚠ STEP 0 — DO NOT RUN THE GENERATORS

Do **not** run `build-demo-bundle.mjs`. Do **not** run `build-shells.mjs`.

Your tree is mixed. The files I shipped are LF; every file I did not touch is still CRLF
(`file src/components/typed-bubbles.js` → "with CRLF line terminators").
`build-demo-bundle.mjs:145` strips imports with a single-line regex, so it fails on a
CRLF component — the blocker from `handoff-341-next.md`.

**You do not need them.** The bundle and all 22 shells are already built and shipped in
the batch, from the cloud twin, in the correct order. Running the generators would only
re-introduce CRLF into the built output and add noise to the diff.

The only script to run is the smoke test.

---

## STEP 1 — confirm the batch (2 minutes)

```
node scripts/smoke-test.mjs
```

**Expect exactly:**

```
BASELINE OK — 1540 pass / the 4 KNOWN pre-existers (#136 · #149③ · M5 · B3)
```

If the number is not 1540, stop and tell me before you build.

---

## STEP 2 — commit `.gitattributes` ALONE

It is presentation-only and deserves its own line in the history. It is also the change
you may want to show the lead developer on its own.

```
git add .gitattributes
git commit -m "label generated and vendored files (.gitattributes)"
```

What it does: of 341,084 tracked text lines, 180,108 are now marked generated or vendored
and 160,976 read as authored work. It also stops the 238 checked-in RocksDB C headers
making the repository report itself as largely a C project.

---

## STEP 3 — the cleanup commit

`ad2c7ca` committed 32 MB of transfer scratch by accident. All of it is tracked at HEAD.

```
git rm -r --quiet _auth343.tgz _gen343.tgz _r344.tgz _land-343.patch _gitlock_trash
git rm --quiet _to_delete_HEAD.lock _to_delete_index.lock _to_delete_index.lock2 _to_delete_maintenance.lock
```

Then stop it happening again — append to `.gitignore`:

```
# transfer scratch and git-lock debris (see DECISIONS #346)
_*.tgz
_*.patch
_gitlock_trash/
_to_delete/
_to_delete_*
```

Then:

```
git add .gitignore
git commit -m "remove 32 MB of committed transfer scratch, and ignore it"
```

**Note:** this does NOT shrink the pack. The bytes stay in git history. If a smaller
clone matters for the lead-developer conversation, that needs a history rewrite — decide
now rather than later, because every week adds commits on top.

Finally delete the two untracked folders yourself (I cannot — the bridge forbids unlink):
`_to_delete/` and `_gitlock_trash/`.

---

## STEP 4 — build and test

Build **`net10.0-windows`**. **Do NOT Rebuild Solution** — that trips the pre-existing
Android RocksDbSharp errors.

C# changed in 4 files: `SettingsPage.xaml.cs` (one line plus comments) and three files
where only `PerfTrace` call-site comments changed. No new file.

Then walk the checklist in STEP 5.

---

## STEP 5 — the test checklist

### A. The three regressions that were shipped and are now fixed

| # | Test | Expect |
|---|---|---|
| 1 | Open a chat | A visible spinner in the log while it loads. It was a blank rectangle |
| 2 | Account → change something → Save | The button width ANIMATES as it morphs to the success state. It was snapping |
| 3 | Switch bottom-nav tabs | The icon ink fades together with the pill. The ink was snapping while the pill animated |

### B. Press feedback — the part I broke and fixed mid-session

| # | Test | Expect |
|---|---|---|
| 4 | Tap a chat row, a chip, a settings row, the FAB | The tint appears INSTANTLY and fades out. No ramp in, on any of them |
| 5 | **Flick the chat list, let it settle, then tap a row once** | The tap tints. Before the fix the FIRST tap after any scroll was dead for up to 1.2 s and only worked on the second try |
| 6 | Flick the list and watch mid-scroll | No trail of lit rows |
| 7 | Press and hold a chat row, then drag off it | The tint clears |

### C. The money surface

| # | Test | Expect |
|---|---|---|
| 8 | In a GROUP, long-press a message from someone with NO nickname → Tip | The sheet shows that member's truncated address, NOT the group's name. The photo was already correct |
| 9 | Desktop Account → Share address | Still copies and toasts. Confirms nothing regressed |

### D. I2 — the desktop add-contact pane (#347)

| # | Test | Expect |
|---|---|---|
| 10 | Desktop: `+` → Add contact | The form sits in a 640px column, centred in the pane. The TITLE BAR still spans the full pane. Send request fills the 640 column, not the pane |
| 11 | Compare with `+` → Add app in the same column | Both are capped. **Add app is 560, add contact is 640.** 🟡 Pick one — a one-line change in either file makes them match |
| 12 | Mobile: Add contact | Unchanged, full width |

### E. Nothing should be broken

| # | Test | Expect |
|---|---|---|
| 13 | Open every tab and the Account pane | No red "Spixi could not load" panel anywhere |
| 14 | ⚠ Delete wallet, on a THROWAWAY account | The flow completes as before. The fix removes a plaintext preference that used to survive — nothing visible |

**What NOT to expect:** no layout, colour or spacing change anywhere, and no change to
chat entry speed. This batch is correctness, not latency. The #345 win is untouched.

**If a red panel appears:** it now names the file that failed, in load order, so the
FIRST name is the root cause.

---

## STEP 6 — commit the batch

```
git add -A
git commit -m "#346 the owed #46 loop over #342/#343/#345, and #347 the desktop add-contact pane"
```

---

## STEP 7 — the Release measurement (this is the one that unblocks a decision)

Everything measured so far is a **Debug** build on a worn Galaxy A52 5G. Debug on Android
means no AOT and no trimming.

1. Build **Release** for Android.
2. Open a chat and capture the `PERF` lines, the same way as before.
3. Record the numbers in `DECISIONS.md`.

**This decides whether any further performance work is worth doing at all.** Release on
decent hardware could halve the numbers again and change which lever is worth pulling.

`PerfTrace` is still in the build for exactly this. It has **no `#if DEBUG` guard**.
Delete `Spixi/Utils/PerfTrace.cs` and its 5 call sites once you have the numbers.

---

## What is parked, and why

- **iOS** — you have no iPhone access for now. Agreed: this session and the next are the
  punch list plus finalizing Android and Windows. iOS comes after.
- **The `+` in a wallet password** — real, but self-consistent inside the app, and
  "fixing" it would lock out existing users. Documented in
  `docs/security-review-for-be-engineer.md` L2, which previously claimed a lockout that
  does not happen. One test only you can run is recorded there.
- **Punch list I1 and E3** — not started. E3 needs a device log first; the failure is a
  swallowed exception and its identity is the whole blocker.
