# Handoff — 2026-09-23 → Session AD: everything that is OURS, before the office

**Read this first (#215).** Then `DECISIONS.md` #916–#927 (#927 is this session's work order). The tree is
CLEAN: Session AC and its follow-up are compiled, deployed, walked (19 P · 0 F · 1 N/A) and COMMITTED as
`b333142c`. HEAD is the landed tree — rebase nothing.

## 0 · State of the tree you inherit

* HEAD `b333142c` (Session AC #916–#926). Working tree clean apart from `_to_delete/` (scratch, Damir's).
* Baseline for the suite: **4817 / the 2 KNOWN** in the container WITH the `Ixian-Core` sibling @ `097341a`
  (compare deltas in ONE environment only, #895). Four `--check` gates green on the VM; `cs-syntax-check`
  183 + 3 known gaps. Logcat tag for Release lines is `DOTNET`.
* Everything C# on the tree has compiled on Windows and Android EXCEPT the iOS-only code (#912 ②, #919's
  extension, #920's Catalyst vendoring) — those wait for the Mac. Nothing here compiles C#: Damir's F5 is
  the compile; a build error in this session's files is this session's bug.
* Damir's plan: **this session (the "ours" round) → the Apple portal steps with guidance (#915) → the
  office (iPhone build · `docs/walk-artifact-ios-office.html` · the mute test iO.7 · optional route B) →
  the endgame (#916: freeze → sweep → strip → gate re-run → merge → TestFlight).**

## 1 · Why this session exists (#927)

Damir asked which of the open cutover rows are waiting on the BE engineer and which only LOOK like it. The
answer, from `docs/be-cutover-brief.md` re-read row by row: a verb lives in THIS repo's C# (an `ixian:`
handler in a page, or a `sendUiCommand` push) — we have added many at the cutover batches (#216, #219,
#242, #248, #267, #213, #801). Only rows whose fix is in Ixian-Core's storage or wire protocol are his.
About half of what was filed as "waiting for BE" is ours. This session closes the ours list; nothing on
the his list is touched; the decisions list is Damir's and is not built.

## 2 · The work order — OURS (app-side C# + shells, no Ixian-Core change)

Verify each row against the tree before building it (#660 — the brief is the ledger, not the truth).
ONE session for all of it (batches 1 → 3 → 4, W11 from batch 2), one #46 loop on Opus over the whole delta at
the end (a mid-point loop after batch 1 if it touched more than five C# files), mutation-kill every new pin,
the full suite BEFORE and AFTER, ONE walk sheet (Damir walks once).

**Batch 1 — kills complexity (do first; each deletes a workaround):**
1. **CH6 excerpt kind.** `addChat` pushes a message KIND with the row → delete the locale-carrier
   reverse-mapping in `home.html` (`excerptFromRaw` + the `#sl-carriers` spans, #192/#268). The biggest
   single simplification on the list.
2. **C1 / C2 payments.** `addPaymentRequest` pushes a status ENUM + request KIND (+ fiat, insufficient)
   instead of localized `title`/`status`/`statusIcon` strings the shell interprets (#187).
3. **Land-on-tapped-tab (S11's store).** One push replaces the `spixi.landtab` localStorage handshake
   (#238) — delete the store, the storage-event leg and the 15 s staleness guard.
4. **Settings view stash (#274).** Stop reloading `settings.html` on a language pick (re-localize in
   place, the mechanism #257/#285 already has) → delete `spixi.settings.view`.
5. **C16 remote delete persisted.** ★ It is OURS: `Spixi/Network/StreamProcessor.cs` `msgDelete` →
   `UIHelpers.deleteMessage` pushes UI only; call `friend.deleteMessage(...)` + recompute
   `metaData.lastMessage` (mirror `SingleChatPage.xaml.cs` local delete). Field-confirmed cross-client.
   THEN the Q12 `spixi.exdel` hint (#253/#254) can go — check its premise (local-only latch) first.
   ⚠ CORE-10 (no authorship check) stays his; this row only persists what the app already shows.

**Batch 2 — AMENDED the same day (Damir: "drop the wallet password work for now; finalize the C# verbs, all in
one session"): only W11 is built this round. L6 stays ours and named for its own mini-session. L2 and L8 are
OUT — not built, not designed. The original list, for the record:**
6. **W11** — `requestFundsResponse` dropped when the chat page is not open (the mutation sits inside
   `if (chat_page != null)`). Small. Money path: review.
7. **L6** — restore DESTROYS the current account before `verifyWallet`. Wallet path: full loop, the
   #565 heal ordering, the §security-handover-gate row updated.
8. **L2 / MAJOR #8** — `HttpUtility.UrlDecode` turns `+` into a space in the wallet password. ⚠ The naive
   fix LOCKS OUT anyone whose password has a `+`: design the migration first (try decoded, then raw;
   never silently rewrite), THEN code. Damir decides before code.
9. **L8** — wallet password → `SecureStorage`. Damir's ruling: BE signs off. PREPARE the patch (verify-
   then-remove migration, retry-screen-only fallback, the two `waletpass` typo lines, Windows unverified)
   for his read; do NOT enable it. `docs/security-review-for-be-engineer.md` L8 has the scope.

**Batch 3 — small verbs and pushes (one session covers most):**
10. S2 backup status push (replace the shell's `spixi.backup.last` stamp).
11. S9 the developer verb (`ixian:dev`) — un-gate the hub row.
12. A3 verified flag · A4 fetch-failure push · A5b installed casing.
13. C6 tip token: the amount, not the txid (`"tip:" + txForTip.id` at both sites).
14. C17 pending-contact state flag · CO1 roster flags (`addContact` 6th arg).
15. CO3 add-contact failure push · CO4 `onValidAddress` carries the address · CO5 dual nickname.
16. C21 group typing with the sender (`showUserTyping` + arg).
17. CH8 reaction excerpt: the empty-nick fallback (`updateChatReaction` sends `nick = ""`).
18. CI2 activity type as an enum (`ContactDetails` pushes localized text + `"true"/"error"`).
19. C4 "Tap to call back" shown while the call is still ACTIVE.
20. i18n-C#: `displaySpixiAlert("No recipient selected", …)` (HomePage) · the hardcoded `"Cancel"`
    (LockPage) → localized.
21. CH4: the unread total mute-aware; favorites persisted app-side (Core has no field — a preference).
22. CH3: mark-read persistence (Core's read flags via the app) + the media purge on chat delete (files
    are ours).

**Batch 4 — medium, ours:**
23. C10 fulfilled request = two cards → push the linkage (`updateRequestFundsStatus` exists).
24. CI6 shared-media feed in chat info (scan history for files → push a list).
25. C22 return-to-call route from the call bar.
26. A8 media flag on `addFile` + the reveal verb.
27. FC1 file-transfer cancel (app-side `TransferManager`; the peer-side half waits on C16 = batch 1).
28. C12 paste-to-send image on desktop (`docs/paste-image-spec.md`).
29. NT1 (b) notification grouping · PV1 privacy toggles — the open halves.
30. **Sweep items that are ours and cheap:** retire `[STARTDIAG]` once the phone's Release split is in a
    row (it is: #925) · delete `getChatPages`'s dead #284 branch · `#490 clearRemoteNotifications` dead code
    · the two open chat paths / two transports stay until #925's lever is measured (NOT now).

## 3 · NOT ours — do not touch (his, or a decision)

* **Ixian-Core / protocol:** CORE-1…CORE-11 (real deletion, authorship check, encryption at rest, empty
  kick/ban, expiry channel, start/unwind), C8 emoji reactions, reply/edit (new message type), resend
  (Core's queue), RC1 cancel family, CI7 group rename, GJ1 "added to a group", the two escalated Core
  blockers, N-BADGE / N-LOCALTAP, S12 (would extend the password-over-URL pattern — forbidden by CLAUDE.md).
* **Decisions, Damir's:** W7 share address format · #82 remote-media posture · C14 link previews
  (security-flagged) · C20 mini-app session UX · CI4 bot destructive actions · L2's migration shape ·
  AC.11 / AC.18 dials · the Z.10 chooser copy · the #890 dials.
* **Waiting on a measurement:** the startup lever (#925: the first WebView in parallel with the node —
  design with BE) · the two chat-open paths / two transports · the belts that never fired.

## 4 · Still open, unchanged (do not re-derive)

* AC.3's clean-launch line (`copyResources: 0 copied, 51 unchanged`, Windows) — ask Damir once.
* AND-47 `Arg_PlatformNotSupported` on Android Release logging — verify-first in Core's `Logging.cs`
  (console-colour APIs), likely a one-line guard in the APP's log options, not Core.
* AND-48 side-inset residuals — one RENDER session before any padding (915×412, `AndroidInsetRight = 48`).
* The Android 30-minute memory kill (511 MB, Release): `dumpsys meminfo` footer, `SpixiContentPage.Dispose()`
  stack guard, `CHAT_SPARE_ENABLED = false` as the free discriminator.
* `docs/diagnostic-session-aa-findings.md` still armed (AA.8 did not reproduce).
* The 123 naive comment-strip regexes + 26 local strippers (Session R).
* The translator pass (M13) · counsel on the privacy policy (#914) · the ASD-STE100 rule unconfirmed.
* The office day stays as `docs/handoff-2026-09-22b.md` §3 describes (archived; the walk sheet
  `docs/walk-artifact-ios-office.html` is LIVE, 31 rows) — after this session and the portal steps.

## 5 · The workshop

* `device_bash` works; `git --no-optional-locks status` only; never plain `git status` on the VM.
* The suite runs in the container on a SNAPSHOT copy with the Core sibling beside it (~5 min). Tar
  `git ls-files` minus the native libs; `npm i --no-save jsdom eslint globals playwright tree-sitter
  tree-sitter-c-sharp` TOGETHER (a single-package install prunes the rest).
* Landing: tar → `/mnt/user-data/outputs` → `device_commit_files` into `_to_delete/` → `tar --overwrite`
  on the VM → `sha256sum -c`. A file edited DIRECTLY on the VM must be staged back before the next tar.
  ⚠ If another session is running on the same folder, land NEW files only and hand it an addendum
  (2026-09-22's lesson: a tar that refused to overwrite was the safety).
* Scratch runners `scripts/_*.tmp.mjs` are gitignored; never land them. Mutation harness = the REAL pin
  text, green baseline in its own root first (#906).
* Housekeeping Damir owes locally: `git rm --cached crash-logcat.txt mem-*.txt` + gitignore; delete
  `_to_delete/`.

## 6 · Rules this session inherits

#215 · #294 · #660 · #663 · #771 · #798 · #811 · #882 ③ · #895 · #906 · #911 · #912 ·
★ #927: a row filed as "waiting for BE" is only his if the fix is in Core's storage or wire — a verb is
  ours; read the anchor before believing the owner column ·
★ #926: self-review plus mutation is not a review; a RESIDUAL row is a promise to re-read it when its
  premise changes.

## 7 · Files

`DECISIONS.md` (#927) · this handoff · `docs/next-session-prompt.md` (rewritten for the ours round) ·
`docs/walk-artifact-ios-office.html` (LIVE, untouched).
Consumed → `docs/archive/`: `handoff-2026-09-22b.md` · `commit-message-session-ac.txt` ·
`f5-checklist-session-ac.md` · `walk-artifact-session-ac.html`.
