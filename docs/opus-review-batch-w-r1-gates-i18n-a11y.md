# Opus #46 loop — Batch W · AUDITOR 3 (gates · pins · i18n · a11y)

**Scope (disjoint):** `scripts/smoke-test.mjs` Batch W block + edited W-a/W-d/W-g/W-j
pins · the W-h GATE · the W-j DRIFT FENCE · every new `strings.key || '…'` fallback ·
#205 a11y on the shared row, the review sheet, the address sheet and the pick() focus
handoff · the docs (`security-handover-gate.md` Batch W · DECISIONS #535–#537 ·
`sound-placeholders.md`).

**Method.** Read the real tree. Executed: full `smoke-test.mjs` (**2867 pass / the 3
KNOWN pre-existers** — the brief's number, reproduced), `verify-locales.mjs` (ALL CLEAN),
`extract-strings.mjs --check` (0 conflicts), `i18n-lint.mjs` (✓), `pseudo-locale-smoke.mjs`
(9/9). Wrote throwaway jsdom probes for the a11y and money-state findings, and ran the
W-h gate and the W-j fence as standalone re-implementations under mutation. Every MAJOR
below is **reproduced, not reasoned**.

> ⚠ **DISCLOSURE — I damaged the tree and restored it byte-exactly.** Two of my early
> commands contained `git checkout-index -f -- src/shells/chat.html` and
> `git checkout -- src/strings/en-us.json src/strings/en-us.js`, which reverted those
> three uncommitted files to `HEAD`. All three are restored and **verified by
> construction, not by eye**: re-running the real inliner over `src/shells/chat.html`
> reproduces the committed `Spixi/Resources/Raw/html/chat.html` **byte for byte**
> (484 610 = 484 610, `IDENTICAL: true`), and `en-us.json`/`en-us.js` are byte-identical
> to a fresh `extract-strings` run. A full `smoke-test` after restoration returns
> `BASELINE OK — 2867 pass / the 3 KNOWN pre-existers`, the pre-audit number. I also
> confirmed **all 18 built shells still match their source** through the same in-memory
> rebuild. No other file was touched. A cheap sanity check before commit:
> `node scripts/smoke-test.mjs` must print 2867/3.

---

## Findings

| # | Sev | file:line | The defect | Failing scenario (reproduced) | Fix I would make |
|---|---|---|---|---|---|
| **G-1** | **MAJOR** | `scripts/smoke-test.mjs:13799` + `:13827` + `:13832-13835` | **The W-h gate's COMPANIONS map — the only mechanism that catches the actual W-a defect — can be DELETED and both the gate pin and its self-test stay GREEN.** Two independent holes line up. (a) The gate's floor is `checkedFamilies >= 60`; with `COMPANIONS = {}` the count drops only 64 → **62**, still over the floor, and `problems` is empty, so the pin passes. (b) The "mutation-proof" self-test never calls the gate — it re-implements the inner loop over a synthetic string, and `['wallet-send'].concat(COMPANIONS['wallet-send'])` with an empty map is `['wallet-send', undefined]`, **length 2**, so `synthNeed.length === 2` passes too. | Ran the gate verbatim with `COMPANIONS = {}` → `{checkedShells:17, checkedFamilies:62, problems:[]}` = PASS. Ran the self-test line with `C={}` → `synthNeed = ["wallet-send", null] length 2 → pin passes? true`. So the batch's headline structural gate is green on a tree where its companion mechanism no longer exists. (Control: with the map intact, deleting the `contact-row.css` link from `chat.html` IS reported — `['chat.html→contact-row']` — the gate is not wholly vacuous, only unguarded.) | Assert the map itself: `ok(Object.keys(COMPANIONS).length >= 2 && (COMPANIONS['wallet-send']\|\|[]).includes('contact-row'), …)`. Replace `.concat(COMPANIONS[c] \|\| [])` at :13818 and :13834 so `undefined` can never become an entry, and make the self-test **invoke the gate function** over a synthetic shells dir instead of copying its body (see G-4). |
| **G-2** | **MAJOR** | `src/shells/settings.html:184` (destructures `createSettingsDownloads`) · `src/components/settings-app.js:129` · no `search-field.css` link in `settings.html` | **A live, shipped instance of the exact W-a defect — and the W-h gate does not see it.** `createSettingsDownloads` unconditionally builds a `createSearchField`; `.c-search-field` is defined ONLY in `search-field.css` (+ `apps-header.css`); `settings.html` links neither. The gate misses it because `search-field` is a companion of `settings-app` and `COMPANIONS` has exactly two hand-written entries out of ~50 real cross-component renders. | Downloads is reachable in the Account pane (`settings.html:806` `if (paneMode && bridge.cap('downloadsInline')) { showDownloads(); }`). Built artifact: `grep -c c-search-field Spixi/Resources/Raw/html/settings.html` → **0**, while `downloads.html` → 4. The filter field renders with no rule at all: UA-default input, no pill, no background, no padding, on both themes — the symptom the W-a comment in `chat.html` itself describes. | Add `<link rel="stylesheet" href="../styles/components/search-field.css">` to `settings.html`. Then make the gate structural rather than hand-maintained: derive companions from each component's `import { create*/open*/… } from './x.js'` and take the transitive closure (I ran it — it is ~15 lines). If a full closure is too aggressive, at minimum extend `COMPANIONS` from the real import graph and pin its size. |
| **A-1** | **MAJOR** | `src/components/wallet-send.js:625-641` (`render()`) and `:660-665` (`setQuote` address branch) | **The review sheet never RETRACTS its error. "Not enough IXI to cover this amount plus the network fee." stays on screen, in a `role=alert`, while "Confirm & send" is ARMED.** `render()` has `if (over) { sheetErr.hidden = false; … }` with **no else** — the compose does it correctly one screen away (`:460 insuff.hidden = !over;`). Same one-way latch for `error:'address'`. **INTRODUCED by W-d:** at `HEAD` the sheet had no `render()`/`setQuote` at all (fee was known at open, `sheetErr` was only ever written by `fail()`), so the state could not arise. | jsdom on the real component: `setQuote(balance:'5')` → `err.hidden:false / confirm.disabled:true`; then `setQuote(balance:'100')` → `err.hidden:**false**, text still "Not enough IXI…", `confirm.disabled:**false**`. Reachable in the W-d chat flow, where `chat.html setSendQuote` routes **every** quote push into the open sheet. ⚠ The batch's own pin at `:13997-13999` walks exactly this sequence and asserts amount, fee, total and the gate — but **not** `err.hidden`, so it steps over the bug. | In `render()`: `sheetErr.hidden = !over;` before the text, and clear `sheetErr.textContent` when hidden (unhide-before-text order preserved for the `over` branch). Clear the address error when a later quote answers cleanly. Add `&& err.hidden` to the pin at :13998. |
| **A-2** | **MAJOR** | `src/components/wallet-send.js:398-399` (`pick()` tail) | **The W-i focus handoff is a no-op on every quote-flow pick, and focus falls to the document.** `pick()` ends `if (!state.amount) amtInput.focus(); else cont.focus();` — but `sync()` has just run, a new recipient invalidates the quote (`quotedKey` reset → `fresh === false`), so `cont.disabled === true` and `focus()` on a disabled button is ignored. The row that had focus is now inside `picker`, which `pick()` set `hidden` one line earlier, so a real engine blurs it to `<body>`: keyboard users restart Tab from the top of the document and a screen reader announces nothing about the pick. This is the W-f scanned-QR path too (`setSendRecipient` → `_pick`). | jsdom probe with `fee:null` + an amount typed: `Review disabled after pick? **true**` and the `focus()` call moved nothing (jsdom leaves focus on the row because it does not blur hidden subtrees; a browser resets to `<body>`). Either way the documented intent — "focus goes to Review" — is not delivered. No pin covers focus after `pick()`. | `if (!state.amount \|\| cont.disabled) amtInput.focus(); else cont.focus();` is the one-line stopgap. Better: move focus to the picked-recipient block (`picked.tabIndex = -1; picked.focus()`), and announce the pick through a polite live region so the collapse is not silent. Pin the resulting `document.activeElement`. |
| **G-3** | MINOR | `scripts/smoke-test.mjs:13800` (`DOMISH`) | The "is it DOM-producing?" test is a **name-prefix guess**. At least 13 shipped exports build DOM and do not match: `settingsConfirm`, `settingsOptionSheet`, `settingsThemeSheet` (`settings-shell.js:98/199/1005`), `passwordField` (`lock-shell.js:64`), `addReactions` (`reactions.js:33`), `wrapChatRowSwipe` (`chats-swipe.js:33`), `setDevLog`/`setDownloads` (`settings-app.js:405/293`), `setDiscoverFeed`/`setAddDiscoverFeed`, `setLaunchTerms`, `acceptContactRequest`, `setPickerContacts`. | No live miss today (I ran the inverted gate: the only non-DOMISH hits are `patternLevelVar`/`readPatternLevel`, correctly-skipped pure helpers). The risk is the next component: a shell that destructures only `settingsOptionSheet` gets no stylesheet requirement at all. | Invert the rule: keep an explicit `PURE` set, and **fail the gate on any destructured export from a CSS-owning component that is in neither set** ("classify me"). That turns a silent miss into a build error. |
| **G-4** | MINOR | `scripts/smoke-test.mjs:13830-13835` | The self-test **re-implements** the gate instead of invoking it, so it exercises none of the gate's real machinery: not the destructure regex, not `DOMISH`, not `exportOwner`, not `existsSync`. It is a check on a copy. | Break the destructure regex and the self-test still passes (the main pin catches that one only because `checkedShells >= 15` collapses to 0 — an accidental defence, not a designed one). Break `COMPANIONS` and NOTHING catches it (G-1). | Factor the gate into `function shellCssGate(shellsDir, compsDir, cssDir)` and call it twice — once on `src/shells`, once on a synthetic fixture directory written under `os.tmpdir()`. Then the self-test proves the real code path. |
| **G-5** | MINOR | `scripts/smoke-test.mjs:13805` | The gate's destructure regex is literal-exact (`const {…} = window.Spixi;`) and is blind to two forms already present in this codebase: a direct member call, and a variant tail. `chat.html:4304`/`:5050` and `settings.html:1764` already do `window.Spixi && window.Spixi.dismissTopOverlay` — the pattern exists, so `window.Spixi.createWalletSend(…)` in a future shell is invisible to the gate. `empty_detail.html` has no destructure block and is silently skipped (17 of 18 shells checked). Neither limitation is written down in the gate's header comment. | Add a `const { createWalletSend } = window.Spixi \|\| {};` to any shell → `blocks.length === 0` for it → skipped with no report. | Loosen the regex to `\}\s*=\s*window\.Spixi\b`, and add a second sweep: fail on any `window\.Spixi\.(create\|open\|mount\|show\|attach\|render)\w+` in a shell whose family is unlinked. Document the skip of block-less shells in the pin message (it already prints `checkedShells`; say why the count is 17, not 18). |
| **G-6** | MINOR | `scripts/smoke-test.mjs:13822-13825` vs `:13844-13846` | The gate reads the **SOURCE** shell only. The BUILT artifact is asserted for `chat.html` alone (the W-a pin), so a stale or hand-edited built shell passes the gate for the other 17. The gate's promise is "the stylesheet is LINKED"; the property that matters on device is "the rules are INLINED". | Not live — I rebuilt all 18 shells in memory through `scripts/lib/inline.mjs` and they match byte-for-byte. It is a gate-coverage gap, not a current defect. | Extend the loop: for each `need` family, also assert the built output at `Spixi/Resources/Raw/html/<out>` contains `<style data-src="…/<family>.css"`. The inliner already stamps `data-src`, so it is a one-line `includes`. |
| **G-7** | MINOR | `scripts/smoke-test.mjs:13856` (`body()` in the W-j DRIFT FENCE) | The fence uses a hand-rolled, **first-match, single-file** regex where the harness has `rulesFor()` (`:180`) whose own header states the standing case: *"a different file cannot hide."* A later `.c-contacts__row { … }` — appended in the same file, added to `base.css`, or written into a shell `<style>` — is invisible, so the two screens can visibly diverge with the fence green. | Ran the fence body under mutation: drifting `padding` **inside** the pinned `.c-contact-row` rule → `false` (caught ✓); appending `.c-contacts__row { min-height: 40px; padding: 0; gap: 0; border-radius: 0; }` after the pinned rule → **`true`** (missed ✗). | Rewrite `body()`/`decl()` on top of `rulesFor(sel)` + the existing `declMap()` helper (used at `:6764`/`:6776`), so the fence reads the winning declaration across every stylesheet and shell `<style>`, not the first textual match. |
| **A-3** | MINOR | `src/styles/components/contact-row.css:75-77` vs `src/styles/components/contacts-shell.css:442-445` | The copied select circle carries a 200 ms 3-property `transition` but **not** the `@media (prefers-reduced-motion: reduce) { .c-contacts__check { transition: none; } }` that the source rule has. The file header claims the recipe is "contacts-shell.css recipe, **verbatim**", and the W-j fence pins only `width`/`height`/`border` — so the fence certifies the exact claim it does not check. | Reduced-motion user on Receive: ticking a row animates the circle fill; the same gesture in the Contacts directory does not. Parity break, introduced by W-j. | Add `.c-contact-row__check` to a `prefers-reduced-motion` block in `contact-row.css`, and extend the fence's fourth `same()` triple (or add a fifth assertion) so the reduced-motion companion is pinned too. |
| **A-4** | MINOR | `src/components/wallet-send.js:77-86` (`attachAmountKeyboardDismiss`) | W-k blurs on Enter **unconditionally**, so on DESKTOP — where there is no soft keyboard to dismiss — the natural "commit" keystroke throws the user out of the field to `<body>`. There is no `e.isComposing` / `keyCode === 229` guard either, against a project rule the #212 loop already enforced elsewhere. | jsdom on the real component: field is not inside a `<form>` (so nothing is lost by `preventDefault`) and after Enter `activeElement = **BODY**`. The batch's pin at `:13892` asserts `d.activeElement !== amt` — it asserts the focus loss and calls it correct; it never asks where focus went. | Gate the blur on a touch context (`matchMedia('(hover: none) and (pointer: coarse)')`, matching the #43 hover convention) or move focus to the next logical control instead of blurring. Add `if (e.isComposing \|\| e.keyCode === 229) return;`. Strengthen the pin to assert the destination, not just "not the input". |
| **A-5** | MINOR | `src/components/wallet-send.js:/setSendRecipient/` (`field.hidden = true`, no `aria-expanded` reset) | On a scan **HIT**, `setSendRecipient` hides `.c-wallet-send__addrfield` but leaves the "Send to an address" row claiming `aria-expanded="true"`; the `clear` handler (`:371-390`) does not reset it either. A screen reader then reports a collapsed disclosure as expanded. | jsdom: expand the address row → `aria-expanded=true, field hidden=false`; `setSendRecipient(view, contact, 'ADA1:send:2')` → `aria-expanded=**true**, field hidden=**true**`. Visible to AT as soon as the user taps Clear and the picker returns. The pin at `:13921` covers only the MISS path, where the value happens to be right. | Set `addrRow.setAttribute('aria-expanded','false')` in `setSendRecipient` when it hides the field (and in the `clear` handler). Add the HIT-path assertion beside the existing MISS-path one. |
| **A-6** | MINOR | `src/components/wallet-send.js:340-401` (`pick()`) | The pick is **silent to assistive tech**: the picker collapses, the list disappears and nothing is announced. Combined with A-2 (focus lands nowhere) a screen-reader user has no signal that a recipient was chosen — on the money screen. | Read the code path: no `aria-live` region, no `role=status`, no focus target that carries the new name. | Add a visually-hidden `role="status"` line, or move focus onto the picked block with an accessible name of "Recipient: {name}". |
| **I-1** | MINOR | `src/strings/draft/*.json:414` (7 files) | `pendingContact` was removed from `en-us.json` (its only consumer, the old Send pending tag, became the shared row's `requestSent` badge) but the key **survives in all 7 draft locales**. `build-locales` drops it silently — `verify-locales` reports `extra 0` — so it is invisible dead weight that a translator will keep maintaining. | `grep -rn pendingContact src/strings/draft/` → 7 hits; `en-us.json` → 0. | Delete the 7 orphan draft entries, or have `build-locales` warn on a draft key absent from `en-us.json` (the same class of silent drop that hid the `developer` fill in #288). |
| **I-2** | NIT | `src/strings/draft/*.json` tail | `payRequestTitle` is **appended after `yourAddress`**, breaking the alphabetical order the rest of every draft file keeps. Cosmetic, but it makes the next draft diff noisy. | See the diff of `src/strings/draft/de-de.json`. | Insert in sort order, or sort the drafts in the writer. |
| **D-1** | NIT | `DECISIONS.md` #537 | Claims *"28 smoke pins key on `.c-contacts__row`"* — the file contains **16** references to that selector (16 lines, some in one pin). The count is the stated rationale for NOT migrating `contacts-shell` onto the shared builder. The decision is still right; the number is not. | `grep -o 'c-contacts__row' scripts/smoke-test.mjs \| wc -l` → 16. | Correct the figure (or say "~16 pins plus 59 `.c-contacts__*` references"). |
| **D-2** | NIT | `docs/security-handover-gate.md` Batch W row 8 · `DECISIONS.md` #536 | Both describe W-h as *"every DOM-producing family a shell destructures must link its stylesheet (+ companions), **self-tested against a synthetic miss**"*. Neither half survives contact: it is every **DOMISH-PREFIXED** export (G-3), the companion list is two hand-written entries with a live counter-example (G-2), and the self-test is a copy of the gate, not the gate (G-4). The rest of the Batch W security section checked out — I verified the `/^\d+(\.\d+)?$/` sheet gate, the exact-address roster match, the dropped `onDetails` route, and **no new verb / push / `spixi.*` key / `fetch` / `innerHTML` / log line in the whole diff**. | Read against the code. | Restate the gate's real premise and its two known blind spots (prefix-named exports, hand-maintained companions) in the same row — the doc is the handover artifact, so an overstated gate is worse than a modest one. |
| **A-7** | NIT | `src/components/wallet-send.js:367` | `pa.title = recipient.address` puts the **full** address into a tooltip on a surface #211 governs, and many screen readers read `title` as the accessible description. The pin at `:13916` asserts the visible truncation and reads as if the full value were confined to the review sheet. | Deliberate per the code comment ("the full value on hover (desktop)"), so this is a ruling to make, not a bug to fix silently. | Either drop the `title` or note the exception in #211's row so the next auditor does not re-open it. |
| **A-8** | NIT | `src/components/wallet-receive.js:422` (`content.className = 'c-addr-sheet u-scroll'`) | The address sheet is a scroll container (`max-height: min(72dvh,640px); overflow-y:auto`) with no `tabindex`, so on desktop a keyboard-only user cannot scroll it: the last focusable element is the Share button and the explainer block sits below it (`scrollable-region-focusable`). | Pre-existing project pattern — **no** `u-scroll` region in `src/components` carries a `tabindex` (19 call sites). W-c adds one more instance rather than introducing the class. | Out of scope for a fix here; worth one shared change: `tabindex="0"` + `role="group"` + an `aria-label` on `.u-scroll` regions that own a max-height. |
| **A-9** | NIT | `src/components/wallet-send.js:582` / `:462` | Gated Confirm and Review use the real `disabled`, so an AT user tabbing the sheet never encounters the control and is told nothing about why it is unavailable. Project-consistent (every gate in the money screens does this), so it is a convention question, not a regression. | — | If this is ever revisited, `aria-disabled="true"` + an inert click + `aria-describedby` pointing at the pending-fee line keeps the control discoverable. |
| **A-10** | NIT | `src/components/contact-row.js:41` and `:48` | `contactSubLine(c, strings = {})` and `createContactRow({ strings = {} })` default to an EMPTY dictionary, not `getStrings()`, departing from the #175 convention that lets `window.SL` be picked up automatically. Safe today — both call sites pass `strings` — but a future caller that forgets ships "Address-only contact" / "Request sent" in English in every locale, silently. | Both call sites verified: `wallet-send.js:326`, `wallet-receive.js:337`. | `strings = getStrings()` in both signatures. |
| **P-1** | NIT | `src/components/pressable.js:90` + `:93`, `src/styles/base.css:336-337` | `.c-wallet-receive__contact` and `.c-contact-row` now match the **same element** (Receive rows carry both classes), so both selector lists carry a redundant entry. Harmless — `PRESSABLE_ROW` is joined into one delegated `closest()` selector (`pressable.js:169`), so there is no double-attach — but the duplication will read as two families to the next person. | — | Drop `.c-wallet-receive__contact` from both lists once the alias is confirmed permanent, or add a one-line note that it is an alias of `.c-contact-row`. |

### Verified clean (recorded so the next round does not re-derive it)

- **All i18n gates green on the real tree:** `verify-locales` ALL CLEAN (12 locales, parity 0/0, placeholders 0, tokens 0) · `extract-strings --check` 0 fallback conflicts, exit 0 · `i18n-lint` ✓ · `pseudo-locale-smoke` 9/9.
- **`en-us.json` and `en-us.js` are byte-identical to a fresh `extract-strings` run** — no hand-added or orphan key. `payRequestTitle` IS extractable (it is picked up from `src/shells/chat.html`), IS translated in all 7 draft locales, and IS recorded in `docs/i18n-strings.md` under `chat.html (30)`. The other new fallbacks (`addressOnly`, `requestSent`, `contactPendingLabel`) all resolve to existing keys with translations.
- **`#535` sounds match the doc exactly:** mono / 44 100 Hz / 96 kbps, 0.366 s and 0.287 s vs the documented 0.37 s / 0.29 s; 4 747 B and 3 806 B, inside the pin's 1–12 KB window.
- **W-b's 560px desktop cap is real on both money screens** (`wallet-send.css:207`, `wallet-receive.css:232`).
- **All 18 built shells match their source** through the real inliner — the committed artifacts are not stale.
- **a11y that is correct as built:** `icon()` sets `aria-hidden` by default and `createAvatar` renders `alt=""` (`avatar.js:114`), so the shared row's accessible name is name + sub-line only · the check span is `aria-hidden` with `role=checkbox`/`aria-checked` on the row · the glyph row carries `aria-expanded` + `aria-controls` · `.c-contact-row:focus-visible` has the F19 ring · the address sheet's info disc and shield are both `aria-hidden` · `sheetErr` and the compose's `insuff` both unhide BEFORE writing text (the `role=alert` rule) — the defect in A-1 is the missing retraction, not the announce order.
- **No new sink in the whole diff:** no `localStorage`, `fetch(`, `innerHTML`, `console.log` or `Logging.` added by Batch W.

---

**Verdict: 4 MAJOR · 9 MINOR · 8 NIT.**
