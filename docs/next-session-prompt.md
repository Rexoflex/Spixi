Next session — Session Y: the X walk verdict, then the contact-details premium batch

0 · Before anything else (#215) Read `docs/handoff-2026-09-17.md`. Check whether `device_bash` works (it did in Session X — node 22 / git / npm on the VM; #860 was a state, not a law; a detached `nohup` run still dies with the call). Run `node scripts/generate-icons.mjs --check` and `node scripts/build-shells.mjs --check` on the real tree before believing any statement about it, including this one. CHECK DECISIONS before accepting any "owed" row from this prompt (#660 — Session X found two stale OPEN rows in the release ledger).

1 · ★★ Ask first
* The last suite line after the X paste — expected BASELINE OK / the 2 KNOWN, +9 assertions. Verbatim if anything is red. A `parser missing` red = the `npm i --no-save jsdom eslint globals` line did not run (all three together — one alone PRUNES the rest).
* The X walk (`docs/f5-checklist-session-x.md` §2): X.1–X.10. Ticks are colour-only now (#877) — same weight, same size, both surfaces.
* Did the commit land (one batch, `docs/commit-message-session-x.txt`)?
* S1 timed? (#872 ②) — still undecided; #864 and the spare stay as they are until a number exists.

2 · Build — THE CONTACT-DETAILS PREMIUM BATCH, fully decided
`docs/contact-details-premium-proposal.md` — every fork is answered: **contact details ONLY** (the Account hub keeps its discs; #618 amended to one grammar per screen family) · **max FOUR rounded-rectangle neutral tiles** on `--surface-card` / radius 12, glyph on the single accent, label inside — chat header = Call · Pay · Mute, directory = Message · Call · Pay · Mute; **Request leaves the row** (it lives in the ⊕ attach sheet) · **address under the name** (tap = the address/QR sheet; the row leaves group 1) · **inset-grouped sections, monochrome glyphs, no elevation, labels outside the cards** · **1:1 destructive group LAST; group/bot destructive rows ABOVE the roster** (#873) · Remove/Leave = `--text-error` label, no fill · group avatars keep their gradient (identity, #34).
Shape: FE only (`chat-info.js` + `chat-info.css`); the desktop pane (#247) is the same component and takes the same CSS. ★ #221 untouched — no verb, no push, no key. Render BOTH themes on the built `contact_details.html` through the C# wire BEFORE any pin (harness recipe: handoff §3; contact_details pushes: `setContext · setAddress · setNickname · showIndicator · showCallButton · setContactNotifications · setSharedGroups · clearRecentActivity · addPaymentActivity`; a group needs `setGroupInfo · clearMembers · addMember`). Pins as PROPERTIES (no gradient on a row glyph; the danger group's position by kind; the error label is a role; the tile count ≤ 4). Contrast owed at build (#769): `--text-error` on `--surface-card` both themes; `--icon-neutral-02` glyphs ≥ 3:1 on the card in dark. Then the #46 loop — on Opus.

3 · Then
* The #46 Opus loop over Session X (#869–#877) — adversary the undeclared-identifier gate's `shellGlobals` heuristic (regex over `window.X =` and classic top-level declarations — a name made global another way is a FALSE RED, loud not silent; prove it).
* #864 after S1 · the settings swatch `110px 191px` mask (#866) · AND-40 (needs a run that kills) · AND-36/39 (repro first) · the freeze (#825) last · the BE cutover (`be-cutover-brief.md`, `security-review-for-be-engineer.md` first — §1a is CLOSED now, #874).
* Bottom bleed is DROPPED (#875). Do not reopen it on "Android requires it" — it does not.

Rules #215 · #294 · #663 (F5, never dotnet build on Windows) · #771 (strip comments before a negative sweep) · #798 (a sweep from the author's list is not a sweep) · #828 (harnesses from the suite's own header) · #861 (both halves) · #865 (SVGs by node export; the bridge stamps PNGs too) · ★ #869: a `catch (_) {}` around a one-line write is a feature that silently never runs · ★ Session X: run the suite's EXISTING gates on your change, not only your own pins (the reachability gate caught what my harness did not).

Files: `DECISIONS.md` (540 rows, #869–#877) · `docs/handoff-2026-09-17.md` · `docs/f5-checklist-session-x.md` · `docs/contact-details-premium-proposal.md` · `docs/sheets/session-x/` · `docs/release-readiness.md` (re-synced 09-17 for #234 + Privacy) · `docs/security-handover-gate.md` (Session X section).
