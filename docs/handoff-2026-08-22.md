# Handoff — the 2026-08-21 batch (#464–#471). READ FIRST.

**LANGUAGE RULE: ASD-STE100.** See `CLAUDE.md`.

Supersedes `docs/handoff-2026-08-21.md` (archive it at the commit).
★ F5 sheet: **`docs/f5-checklist-2026-08-21-notif.md`**.
★ Device truth for the PREVIOUS round stays `docs/f5-verdict-2026-08-21.md`.

---

## 0. What this batch is

Damir's pick, in his order: the four one-liners, then the notifications block, with the
sound-effects block riding the same wiring gate. **The lock is LOG-ONLY** — his explicit
call, and it is enforced by a smoke gate, not by a promise.

| Row | What |
|---|---|
| #464 | **F4 + F5** — and neither was one line. F5 would have shipped an empty grey box |
| #465 | **NOTIF-1** — the private-group mute. One line, plus a badge question raised not inherited |
| #466 | **NOTIF-2** — a settings screen that has shipped DARK for months, and a previews row that would have been a dead control |
| #467 | **NOTIF-3 + NOTIF-4** — a polled global and a per-message id. Neither symptom was where it looked |
| #468 | **SND-1/2/3** — plumbing, switch, and NOT ONE invented asset |
| #469 | ★ **The lock: LOG-ONLY.** The instrumentation's own blind spots were the two best findings |
| #470 | **F6 + F7** — measurements. A pre-existing pin caught a real bug in my own probe |
| #471 | **N80 needed no work.** It shipped in `f10ff1ca`; the handoff was stale |

## 1. State

**UNCOMMITTED on your disk. 89 files, three of them NEW.**

★ **`git add Spixi/Meta/SNotificationPrefs.cs Spixi/Meta/SSounds.cs Spixi/Meta/SLockDiag.cs`**
before you build, or it compiles for nobody (the #421 `theme-runtime.js` failure mode).

**Smoke: 2280 → 2339 pass, the same 4 known pre-existers** (#136 · #149③ · M5 · B3).
Shells 18. Bundle 273 exports. i18n gates green. NUL sweep clean. C# gate 140 files.

⚠ **C# changed in 19 files** → wipe `Spixi\obj` and `Spixi\bin` (#387).
★ **Ixian-Core is UNTOUCHED at `097341a`**, working tree clean. The hold-out gate passes;
the `reply` capability is still not declared.

⚠ `verify-locales.mjs` fails on ja-jp / `chainScanNoteUnknown`. **PRE-EXISTING** — verified
identical on a clean clone. Not this batch, and not in the smoke KNOWN list because it is a
different script.

## 2. ★ What the next F5 is FOR

Two thirds of this batch is ordinary feature work you can pass or fail on sight. The lock
third is not: **it ships no fix and is meant to fail exactly as before.** What it ships is a
log that says WHY, so the round after this one is a repair rather than a fourth guess.

The single most valuable artefact you can send back is **the Dev-screen log after one
background and one launch with the lock on.** §1 of the F5 sheet says what to search for.

## 3. What is now KNOWN that was not before

1. ★ **The verdict's first F1 candidate is false at source.** `MainTheme`'s
   `windowBackground` is `@layout/splash_screen` — `#144576`, splash **blue**, not light
   (`styles.xml:37`) — and the LockPage constructor already paints page, content and WebView
   `#13171b` **before** the modal push. Neither native layer can make a white frame. The
   surviving suspect is the Android WebView's own renderer. The same fact is the likeliest
   explanation of F2's splash-blue bar.
2. ★ **`IPlatformUtils` is DEAD CODE.** Nothing implements or consumes it; the real binding
   is a per-platform static `SPlatformUtils`. The brief's "add one verb to the interface"
   would have compiled and done nothing.
3. ★ **Muting a group already zeroed its badge** — Ixian-Core `Friend.getUnreadMessageCount()`
   (:513-520) — **while still firing notifications.** Backwards, pre-existing, and now
   consistent. Whether mute *should* zero the badge is your call (§6a of the F5 sheet).
4. ⚠ **Core will UN-MUTE a group.** `GroupChat.cs:103` re-creates `botInfo` with
   `sendNotification: true` on every received `createGroup`. Not ours; matters more now.
5. **The notification carried no sender name and no message text** (AND-15 #334), so the
   built "Show message previews" row had nothing to hide. Wired as labelled it would have
   been a switch that changes nothing.
6. **`Address.getInputBytes()` returns the PUBLIC KEY** when one is populated
   (`Address.cs:426-437`) — not a stable chat identity. `addressNoChecksum` is.

## 4. What I could NOT verify

1. **No .NET toolchain.** `cs-syntax-check.mjs` parses all 140 files, but it is not a type
   checker. **Your build is still the compile gate**, and this batch touches 19 C# files.
2. **No device.** Nothing was seen on a phone. Every lock finding is still open by design.
3. **iOS, MacCatalyst and Windows.** Unverified for eight batches. This batch touches all
   four platforms' `SPlatformUtils` and `SPushService`; only Android is under test.
4. **The sounds have never been heard** — no assets ship. What is verified is that the app
   stays silent and does not get slower.

## 5. The #46 loop

Three disjoint read-only auditors (C# notification/sound · shells and components · the
LOG-ONLY guarantee). **5 MAJOR and ~14 MINOR, all fixed**, and the two best ones were the
instrumentation failing to answer its own question:

* F2 was blind to the `setEdgeToEdge` call the lock actually takes, and would have printed a
  **false** "never repainted" verdict — the exact shape that gave F3 two wrong fixes.
* F1 started a clock only on the Android pause path, so the **launch** leg would have logged
  `+-1ms` on every line.
* The sound block sat outside every guard the notification respects.
* A chat message REPLACED that contact's missed-call notification.
* The notifications screen could never re-render, so an echo could not correct a switch.

★ **My own LOG-ONLY smoke gate was a tautology** and the audit caught it. The hardened gate
strips the diagnostic lines and asserts what remains is the shipped method statement for
statement; it was then proven by injecting a real `return` that mentions no diagnostic.

★ **Mutation, batched:** two revert runs put **21 pins** red and left the 4 known
pre-existers exactly where they were.

## 6. Next, in order

1. **`git add` the three new files**, build, F5 per the sheet, commit.
2. **Send back the Dev log.** F3/F1/F2 become one repair round instead of a guess.
3. Your five calls in §6 of the F5 sheet — the badge dial and the sound assets are the two
   that block further work.
4. Then, per `docs/master-worklist-2026-08-21.md`: **N67 / F8** (one account, wipe means
   wipe) with F9 riding along — ⚠ destructive path, reproduce and NAME the delete→restore
   error first (#215/#294), and it needs a security-gate row.
5. Then **CALL-1..4**. ⚠ The Android in-call strip has still never been exercised on
   hardware; it needs a real two-device call before any of it is judged.
6. **Wallet-send (WAL-1) stays LAST.** It is the money path. ⚠ And the #255 backlog item is
   its prerequisite: `createWalletSend` still consumes the UNFILTERED roster, i.e. groups as
   money recipients.

## 7. Standing rules this round re-earned

* ★ **A pin that passes vacuously is worse than none** — mine was, and so was the
  pre-existing #453 DOM pin, which asserted the very behaviour F5 removed.
* ★ **Mutate before believing.** 21 pins proven; three of my own were wrong first time.
* ★ **A probe that lies is worse than no probe.** My F6 probe subtracted two `ulong`s in the
  underflow direction — misleading in exactly the state F6 is suspected to involve. A
  PRE-EXISTING pin (#453) caught it; nothing I wrote would have.
* ★ **Verify the right question** (#215). "Add a verb to `IPlatformUtils`" was answerable
  only by asking whether anything implements it. One grep.
* ★ **A grep that does not contain the term proves nothing.** I briefly concluded N80 was
  dead code because my own pattern omitted the call site.
* ★ **The one-liner that is not a one-liner is the normal case.** F5's "remove one line"
  would have left an empty grey tile.
* Verify at source · never build past a missing repro (#294) · bundle BEFORE shells ·
  DECISIONS rows at decision time · `git --no-optional-locks` always · #387 wipe.

## 8. How to work

```
git clone --branch redesign/frontend https://github.com/Rexoflex/Spixi.git
git clone https://github.com/ixian-platform/Ixian-Core.git      # SIBLING folder — REQUIRED
npm install --no-save jsdom tree-sitter tree-sitter-c-sharp     # ONE call: --no-save replaces node_modules
```
⚠ Install them in a **single** `npm install` — `--no-save` wipes `node_modules` each time, so
separate calls leave you with only the last package.
Ixian-Core reference: `097341a`, **unchanged** — the hold-out gate checks it.
