# Handoff — 2026-08-17g (post-#375/#376). READ FIRST.

**LANGUAGE RULE: ASD-STE100 Simplified Technical English.** See `CLAUDE.md`.

Supersedes `handoff-2026-08-17f.md` (consumed → archive it with the commit).
★ Planning doc: `docs/master-worklist-2026-08-17.md` (N51–N59 + N36b struck;
N57 = triaged, verdict pending Damir's repro).

---

## 1. State — verify, do not trust

```
git --no-optional-locks log --oneline -3     ← af10a004 at HEAD when written
git --no-optional-locks status --porcelain   ← the #375/#376 batch is the tarball delta, UNCOMMITTED
```

| Item | Value |
|---|---|
| Batch #375 | The whole 17f §2 fix list BUILT: ① N51+AND-37 (chatoverlay/chatBack — the N50 grammar applied to chat + settings sheet arm) · ② N54/N53/N52 (typing gate · badge fed · pulse visible + window 50) · ③ N55 (3 sites)/N56/N58/N59 · ④ N36b · ⑤ N57 TRIAGE ONLY |
| Loop #376 | #46 on Opus: 3 auditors → fixes → break-my-verdict reviewer → **PASS**. 3 MAJOR found+fixed pre-F5 (A-1 back WEDGE via a dead select handle · B-1 the badge counted re-flushed HISTORY · B-2 the contact_new toast was FALSE on alert-and-stay paths) |
| Smoke | **BASELINE OK 1903 / the 4 KNOWN pre-existers** (was 1858/4) · lint ✓ · pseudo 9/9 · locales ALL CLEAN · NUL clean |
| Mutation | 47/50 batched ✗ + 3 individual/pre-existing proofs (all honest) |
| N57 | `docs/n57-triage-group-visibility.md` — FE + C# push path exonerated at source; 3 Core-side suspects; Damir runs the 4-leg protocol; be-cutover `[N57?]` row updated |
| F5 | `docs/f5-checklist-2026-08-17-fix2.md` (18 legs + the N57 protocol + the commit message) |

## 2. Damir — before the next build session

1. **Build + F5 the checklist → commit ONE batch** (message in the checklist).
   Android first: legs 1–7 (back grammar) and 18 (N36b) are Android-only.
2. **Send the BE engineer** be-cutover `[Q1-ESC]` + `[NT1]` (unchanged ask from
   17f §3) — and after the N57 repro, its verdict row.
3. **A52 re-measure** (leg 12): N52 doubled the chat-entry row count; the #349
   baseline was 234 ms at 25. If entry feels slow, bring the number.
4. **Dials still open (log answers, no build until given):** N56 wash values
   (9% light / 6% dark shipped — eyeball) · contact_new post-pop feedback
   (C# push or storage handshake — or accept the pop as enough) · blind-bubble
   avatar photos (#372) · N48 chip on non-blind heros (#370) · ru/lt legacy
   dashes (#371) · de requestSent retune (#371 — note: the de
   `chat-request-sent-title` compound was FIXED at source this batch, B-6) ·
   N3 voice-sweep target list · the A2 fallback (17f §3 wording stands).

## 3. NEXT SESSION — N4 locale expansion (its own session, per 17f)

The last un-started R2 item and the only (L)-size one left: audit existing
translations, pick the launch 15–20 languages, add the FE dictionaries
(⚠ it/id/lt/cn/ja have C# strings but NO FE dictionary — the #360 residual;
dictionary + the Utils.cs culture gate move TOGETHER), per-language
button-overflow audit. After N4, the remaining plate is the worklist §C
fix-first block (N13 top — data-loss class · N40 · N10 · N15 · N39) and the
rounds (R3 art → R4 onboarding → R7 wallet → R6 → R8 → R5 → R9, per §F).

## 4. How to work — unchanged from 17f §4

Cloud twin recipe (clone → `af10a004`+batch → `npm install jsdom --no-save` →
bundle BEFORE shells → smoke green at **1903/4**, state the number when it
moves) · verify against code (#215) · #294 · mutation-honest pins, PROVEN ·
Damir commits · DECISIONS rows at decision time · the #46 loop on Opus (#352) ·
the security handover gate while building (the #375 section is the template) ·
tarball delivery, plain `tar xzf` in PowerShell, the mount cannot unlink ·
`git --no-optional-locks` always. Ixian-Core reference: `097341a`.

## 5. THE DEFERRED PILE — unchanged from 17f §5

N13 (🔴 top) · D-17b · N40 · N10 · N46 (★ remind near completion) · N47 ·
legs 29–30 · the #368/#367/#372/#373 log-onlys. Plus from #376, fold into
related rounds: the retained chat-info takeover is a THIRD off-stack back
surface (mirror it if ever revived, A-3) · the ~400ms stale-TRUE close window
(A-4, inherited, one wasted press) · a >5s re-flush + >250ms stall can still
miscount the badge (B-1 residual, fails loud) · one false chat-sheet toast on
a mid-session relation change (r2 NIT-6, narrow).

## 6. Do not re-attempt — 17f §6 stands verbatim, plus:

- No optimistic toast on a surface whose C# can alert-and-stay without
  sending (#376 B-2) — feedback must come from a state the shell can SEE.
- `bursting` alone is NEVER a complete re-flush gate on the paths without
  `onChatScreenLoaded` (#376 B-1) — pair it with the quiet window.
- A select-mode handle is live ONLY while `box.dataset.selecting` exists
  (#376 A-1) — never trust the variable alone.
- The N56 wash is a THIRD state-tinted row family: any new press/hover rule
  on chat rows needs its `[data-pinned]` arm (the [aria-current] pair grammar).
