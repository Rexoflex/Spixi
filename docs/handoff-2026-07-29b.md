# HANDOFF 2026-07-29b (post #286/#287) -> next session

**Phase:** B — platform bring-up (`docs/handoff-post-freeze.md` §1B unchanged: platform bugs only,
fix small, log in DECISIONS.md).

This session (Cowork/cloud + device bridge, Windows PC, Damir F5 live) closed the 07-22 PC
handoff's **F5 residue** (#273/#276/#277/#278/#279 all PASS) and the **iOS-8 Windows cross-check**
(badges CLEAR → iOS-31 de-escalated from [XPLAT]; ios-sim-findings rows updated), then landed
**#286** (narrow-pane wallet: status badge painted over the fiat → meta overflow + badge
shrink/ellipsis · "Missing a transaction?" clipped to nothing → data-wrap second escalation +
tools max-height 160→220) and **#287** (#275 waiting-strip token-exact rebuild + pending medallion
· revealAmounts drafted ×6 locales). Full pipeline ran IN-SESSION (bundle → locales → iife → 17
shells → lint/NUL); both F5-VERIFIED same-session. Everything commits as ONE batch (incl. docs).

## THE PLATE — next

1. **Opus #46 loop over #284–#287 — OWED** (batch closed this session). Paste-ready prompt in
   §Review prompt below. #282/#283 were covered 2026-07-27 (`docs/opus-review-macsession-282-283.md`)
   — re-read them only where later work touched the same code. Highest-risk rows: #284 (composer-lock
   latch re-entry · getChatPages detail-content branch) · #285 (reloadShell + one-shot
   suppressNextTabOverlayExit · WalletSentPage setHideBalance ordering · WinUI flush belt) ·
   #286 (RO fit() double-measure) · #287 (shell-only, low).
2. **Device pass — verify-gated rows (Mac + iPhone).** Exec sheet:
   **`docs/device-pass-runsheet-2026-07-28.md`** (B3 Release-build gate FIRST — free — then B1
   OneSignal starvation · B2 double tap-handling · C-9 file:// localStorage probe · iOS-31 ·
   iOS-29 keyboard). ★ UPDATE: iOS-31's Windows cross-check does NOT reproduce (2026-07-29,
   badges clear) → treat as iOS-specific/timing; live sim inspection BEFORE any code (#215) — the
   prepped three-site fix stays parked. iOS-8 fix: Windows leg VERIFIED, iOS leg still owed.
3. **Mini-app WebView MAJOR #6** (`docs/security-review-for-be-engineer.md`): scope the iOS-10
   external-link handoff to trusted host shells (MiniAppPage → no silent `Browser.OpenAsync`; add
   the `TargetFrame.MainFrame` gate) + restore the mini-app safe-area inset (MiniAppPage never sets
   `loadedHtmlFileName` → zero-padding branch). Classify MiniAppPage's trust tier in the handler
   layer; prescriptions in the entry.
4. **Android bring-up** (folded from the archived 07-22 PC handoff — its only live remainder):
   `docs/archive/handoff-2026-07-20-pc-next.md` steps 3–6 (restore unblocked since the RocksDB
   nupkg) · emulator + `chrome://inspect` · Stage-4 checklist from `docs/mac-bringup-checklist.md`.
5. Carried polish/gaps: toast / pending-strip / request pane / encpass footer safe-area bottoms;
   landscape side insets · `fitTile` geometry · backup-stamp cross-WebView localStorage (folds into
   plate 2's C-9 probe) · translator todo: 21 keys ×6 locales (the about*/howTo* tail — a
   machine-draft pass was offered and stands).

## BE asks (accumulated, unchanged)
- FriendState enum + non-destructive "resend request" recovery for locked legacy states (A2).
- Can a GROUP ever carry a non-Approved state? (#275 guard: latent vs live).
- nuget.config hardening (packageSourceMapping + sha512 in local-nuget/README).
- A6: state-guard `onSend`/`onSendFile` C#-side (⑪ invariant is FE-only).
- MAJOR #6 trust-tier call (plate 3) once the FE fix is scoped.

## Carried forward (unchanged)
- Device pass iOS-26..32 (`docs/ios-sim-findings.md`; iOS-27 premise = marshalling abort first).
- Mac remaining: lock flow · Safari pass over `src/demo/*.html` · Stage-5 residual (untranslated-
  by-choice words + date-format check on device).
- Gated: wallet-send LAST (#232) · reply-to (BE carrier) · #234 (BE) · BE cutover docs.

## Review prompt (plate 1) — paste into the review session

    Spixi redesign, repo at C:\Users\Damir\Claude\Projects\Spixi Rework Of Frontend\Spixi
    (branch redesign/frontend, tip = the #286/#287 batch commit). Run the standing #46
    adversarial review loop over DECISIONS #284–#287. #282/#283 were covered by
    docs/opus-review-macsession-282-283.md — re-examine only where #284+ touched the same code.
    Read first: CLAUDE.md (orientation; session log tail) · DECISIONS.md rows 284–287 ·
    docs/opus-review-273-281.md (protocol + report format precedent).
    Protocol: 3 DISJOINT read-only auditors —
    A: C# pages/bridge (SingleChatPage, HomePage, SettingsPage, WalletSentPage, Utils; the #285
       reloadShell / suppressNextTabOverlayExit / WinUI flush-belt machinery; #284 composer-lock
       latch + getChatPages detail branch);
    B: shells/components (chat.html composer lock + pending strip #287 · wallet_sent.html
       hide/reveal cycle · wallet filters data-wrap + badge shrink #286 · the #285 one-shot
       suppress echo in home);
    C: pipeline + outputs (drafts/locales/iife vs built shells · smoke assertion honesty ·
       i18n-lint coverage · generated-artifact drift).
    Then: verify every finding against the tree (file:line, no speculation) → rank MAJOR/MINOR →
    fix small in place (DECISIONS rows) → adversarial re-review of your OWN fixes
    (break-my-verdict pass) → write docs/opus-review-284-287.md.
    Constraints: #215 verify-first (device-gated rows in the runsheet stay parked — do NOT build
    around them) · §1B fix-small · git --no-optional-locks always · component change = FULL
    pipeline (bundle BEFORE shells) · smoke = Damir local · I push via GitHub Desktop.

## Gotchas (CURRENT)
- Bridge git: **always `git --no-optional-locks`** for status/diff (plain status strands a 0-byte
  `.git/index.lock` the mount can't unlink). Mount can't delete — `mv` into `_to_delete/`; Damir
  trashes `_to_delete/` + `_to_delete_index.lock*` whenever.
- Device VM: generators are FINE (★ #287 ran the FULL pipeline in-VM: bundle → locales → iife →
  shells → lint + NUL sweep); long smoke runs get killed → smoke = Damir local. Cloud-container
  jsdom unrestricted (harness recipe: DECISIONS #285).
- `device_stage_files` can serve STALE bytes for a re-staged path — `cp` to a NEW on-device name
  to bust. Python-over-`device_bash` byte-replace (assert count==1, CRLF-aware) is the proven
  in-place edit path for big repo files (used for DECISIONS/handoffs/chat.html this session).
- `build-shells.mjs all` overwrites LEGACY `apps.html` + `wallet_send.html` — default (17) or
  `launch`. Its export guard is trustworthy. `createButton` icon = ELEMENT; shells use
  `window.SpixiIcons.icon(...)` (the bundle does NOT export `icon()`).
- The #46 loop: after this batch it owes #284–#287 (plate 1). Following span starts at #288.
- Consumed → `docs/archive/`: `handoff-2026-07-27-postreview.md` · `handoff-2026-07-29.md` ·
  `handoff-2026-07-22-pc.md` (Android remainder folded into plate 4 above).

## Next-session kickoff prompt (paste this)

    Spixi redesign, repo at C:\Users\Damir\Claude\Projects\Spixi Rework Of Frontend\Spixi
    (branch redesign/frontend). Read in order before doing anything:
    1. CLAUDE.md — orientation + session log (last rows = 2026-07-29: #285, then #286/#287).
    2. docs/handoff-2026-07-29b.md — the live plate.
    Context: #286 (narrow-pane wallet fixes) + #287 (waiting-strip token pass + revealAmounts ×6)
    committed & F5-verified; Windows F5 residue CLOSED; iOS-8 verified on Windows; iOS-31
    de-escalated (does not reproduce on Windows — sim inspection first). THE PLATE:
    1) Opus #46 review loop over #284–#287 (paste-ready prompt in the handoff §Review prompt) ·
    2) device pass per docs/device-pass-runsheet-2026-07-28.md (Mac + iPhone; guide me step by
    step) · 3) mini-app MAJOR #6 (security-review doc) · 4) Android bring-up. Workflow:
    Cowork/cloud via the device bridge; the handoff's Gotchas section is CURRENT. I push via
    GitHub Desktop; smoke runs locally on my machine.
