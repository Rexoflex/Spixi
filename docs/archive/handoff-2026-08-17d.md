# Handoff — 2026-08-17d. READ FIRST.

**LANGUAGE RULE: ASD-STE100 Simplified Technical English.** See `CLAUDE.md`.

Supersedes `docs/archive/handoff-2026-08-17c.md`. Read dates in headers, never
file names (`docs/archive/handoff-2026-08-17.md` is an OLD 08-13 file).

★ Planning doc: `docs/master-worklist-2026-08-17.md` (N-ids + rounds).
★ **Damir's course setting (2026-08-17, after the #361 F5): KEEP BUILDING
BATCHES. Bugs and reworks accumulate in §4 and get one later fix/triage pass.
Do not spend a build session on the §4 pile.**

---

## 1. State — verify it, do not trust it

```
git --no-optional-locks log --oneline -3
git --no-optional-locks status --porcelain
```

Damir commits the #361–#363 batch himself (title + body were handed to him —
the commit should be at HEAD when you read this; VERIFY).

| Item | Value |
|---|---|
| Batch #361/#362 | **F5 PASSED 16/17** (Windows all-pass · Android all-pass except A4 → N36b, §4). Committed with the doc round (#363) |
| Smoke | **BASELINE OK — 1757 pass / the 4 KNOWN pre-existers (#136 · #149③ · M5 · B3)** |
| Locales | verify-locales ALL CLEAN · i18n-lint ✓ · pseudo 9/9 · zero em-dashes in all 8 built dictionaries (3 smoke gates hold it) |
| Compiler | ✅ Damir built + ran Android (A52, Release) AND Windows for the F5 — the #361 C# risk is retired. It returns for every future batch |

## 2. NEXT SESSION — the next batch (build, no Damir input needed to start)

Per the worklist sequencing (§F) with the fix items deferred (Damir's call):

**R1 — the identity round** (+ the one cheap Table-A C# row). Verify each item
against code first (#215); pins + the #46 loop as always; anything needing a
dial → log it, skip it.

| Item | What (dial already given) | Size |
|---|---|---|
| **N1** | Avatar system rework: a distinct GROUP glyph (never the person icon) · white glyph/initials on DARKER, more saturated gradients · wider hue spread (fix the four-similar-greens — `hashHue` distribution). Touches avatar.js/.css + every consumer; check dark mode both ways | M |
| **N34** | Owner/Admin chip, top-right of group bubbles. Owner data exists (#248 `users.getOwner()` push); blind groups never get it | S–M |
| **N26** | Bot/community member sheet: show **Add contact** when the member is not in contacts; consider request-payment there. Folds in the D-19 dead-end-sheet residual | M |
| **N27** | "Cannot remove contact — in a group": NAME the group(s), offer the path out | S–M |
| **D-5** | "Add to contacts" offered for someone already in your contacts — C# knows the answer where it builds the push. Small C# here, NOT a BE item (15c table A row 6) | S |

D-9 (delete-account device story) and D1 (reply-to; design locked, §10 of
`docs/archive/handoff-2026-08-15c.md`; **two-device id round-trip verify
FIRST**) need Damir present — they run in a Damir session, not this one.

## 3. How to work — unchanged

Cloud twin recipe as in 15c §7 (archive): clone, checkout the branch tip,
`npm install jsdom --no-save`, bundle → shells → smoke; tarball delivery
(⚠ the mount cannot unlink — extract with GNU tar `--overwrite` from the
bridge, or Damir runs plain `tar xzf` in PowerShell; never plan a delete on
the mount, Move-Item/mv works). Standing set: verify plans against code ·
#294 · #215 · smoke green at **1757/4** (state the new number when it moves) ·
mutation-honest pins, PROVEN · Damir commits · DECISIONS rows at decision
time · the #46 loop on anything substantial · **Opus for every adversarial
pass (#352)** · the security handover gate while building. Ixian-Core
reference: clone + checkout `097341a` in the twin.

## 4. THE DEFERRED PILE — fix/triage later, in one pass (do NOT build now)

| ID | Item | State + pointer |
|---|---|---|
| **N13** | Onboarding "Back up now" does nothing at account creation | 🔴 TOP of the pile — data-loss class. Triage first when the pile runs |
| **D-19b** | NO "Hidden member" in bot rooms | Dial LOCKED — §5 below, verbatim |
| **D-17b** | Apps search bar ALWAYS visible | Dial LOCKED — §5 below, verbatim |
| **N36b** | A4 FAIL: select-mode tap on Android flashes a lighter tint before the selected tint | Evidence PRE-GATHERED (DECISIONS #363): prime suspect = the native WebView tap-highlight — N36 made the ROW the tap target and `.c-bubble-row` (message-bubble.css:43) sets no `-webkit-tap-highlight-color`; the pressable fill is pinned out, the menu long-press paints nothing. Candidate = one scoped line; **on-device repro first (#215)** |
| **N40** | "Connecting…" stops on a LIVE document after long offline use | Suspect on file: update-branch starvation (#357 loop notes) |
| **N10** | App-invite Cancel in chat does nothing | Triage (C# here + maybe BE) |
| Legs 29–30 | Compact-balance · flick-cancel (A52) | Untestable two rounds running |
| Dials | SR aria-dash joiners · de-de/sl-si en-dashes (→ R2 round) · "1 members" plural (→ R2) · select-mode dead controls still tabbable · `selectChat` call-site push (the A-1 class, #362) | One-liners, batched with related rounds |

## 5. THE TWO LOCKED REWORKS — dials carried VERBATIM from 17b §2 (archive)

### D-19b — NO "Hidden member" in bot rooms (`docs/f5-findings-2026-08-15.md`)

Damir: a bot group is ALWAYS nickname and/or address. The dial:

1. Label = nickname when known · truncated address when known · **NOTHING when
   neither** (legacy parity). Delete the "Hidden member" placeholder from every
   NON-BLIND surface (bubble label, copy path). Blind chats keep it.
2. **Reverse-resolve** the address for named-but-address-less rows: the roster
   (`friend.users`) still maps address → nick, so C# `insertMessage` (and/or the
   shell via `groupNicks`) looks up the nick and passes the address — **ONLY on
   an exact SINGLE-nick match**. A collision would hand out a copyable address /
   tip target for the wrong person: money-safety, so ambiguous = no address.
   This restores the member sheet, copy, and tip for every roster-known sender.
3. Keep from #356: no group-name impersonation · no group avatar on strangers'
   rows · nick-keyed bubble runs · the NRE guard · the empty-slot C# contract.
4. Retire/replace the affected pins honestly (placeholder pins die,
   reverse-resolve pins are born; count the delta).

### D-17b — the apps search bar is ALWAYS visible

The watchdog-vs-gate flash fired on BOTH platforms. Damir's dial supersedes the
whole mechanism: **the search + layout row never hides** — remove
`data-unknown`, the watchdog, AND the `data-empty` row-hiding
(`setAppsHeaderEmpty` keeps only whatever non-row duties it has; check before
deleting). Nothing can flicker if nothing ever hides. Retire the #359 pins
honestly.

## 6. Do not re-attempt

All of 17b §6 (archive), plus the #362 items:
- No hide/show mechanism for the apps header row, ever — the flicker class dies
  by subtraction (three attempts have now failed).
- No reverse-resolve of a nick to an address on a MULTI-match (money path).
- No selection state on the `.c-app-item` WRAPPER — `__open` is the paint box
  and the focusable control (base.css `:not(.c-app-item)` guards).
- No pane-coupled UI push from a `pushPageLoaded` call site — staged pages can
  be dropped; push from `onOverlayPresented`.
- No hand-edits to `src/strings/*.json` — GENERATED. English lives in the
  component/shell fallbacks (+ the capx table in `extract-strings.mjs`),
  translations in `src/strings/draft/*.json`; three N3a gates fail the run on
  any new em-dash.
