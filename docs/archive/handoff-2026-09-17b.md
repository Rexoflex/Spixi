# Handoff — 2026-09-17 (evening), after Session Y

Records: `DECISIONS.md` **#878–#882** (545 rows). Paste + walk: `docs/f5-checklist-session-y.md` · tick sheet `docs/walk-artifact-session-y.html`.
Commit message: `docs/commit-message-session-y.txt` — commit is Damir's, ONE batch, GitHub Desktop.
Consumed → `docs/archive/`: `handoff-2026-09-17.md` (X) · `f5-checklist-session-x.md` · `commit-message-session-x.txt`.

## What changed about the WORKSHOP (read this first)

★★ **THE FULL SMOKE SUITE RUNS IN THE CONTAINER** (#882 ①). Recipe: on the VM
`tar cf $HOME/tree.tar --exclude=.git --exclude=node_modules --exclude='Spixi/bin' --exclude='Spixi/obj'
--exclude='Spixi/Platforms/Android/lib' --exclude='Spixi/Platforms/iOS/NativeLibraries' --exclude='docs/sheets'
--exclude='docs/reference-screens' --exclude='docs/archive' --exclude='*.png' src scripts Spixi Spixi-PushService
Spixi-UnitTests local-nuget nuget.config SPIXI.sln *.md docs` (4 s, 32 MB; gzip -1 → 15 MB), a second tar of
`src/demo/images Spixi/Resources/Raw/html/images Spixi/Resources/Raw/html/img` (3 MB — the N45/reachability/L15
pins need them), `cp` both into `_to_delete/`, stage, untar in the container, `npm i --no-save jsdom eslint globals`,
`node scripts/smoke-test.mjs` (~5 min, no bridge limit). The container's `build-demo-bundle` is byte-identical to the
VM's (hash-verified), so a component edit can be bundled and suite-run there too. "The full run is Damir's,
predicted" (#860/#872) is over: the count below is a RUN, not a prediction.

⚠ Three traps, each with a one-line cure (#882 ②–④): `git status` on the VM leaves an undeletable `.git/index.lock`
(GitHub Desktop then refuses to commit) — use `git --no-optional-locks status`; after a bridge reconnect a
`device_commit_files` "written" was NOT on disk — re-read the hash before calling anything landed (#865); Playwright's
first context in a fresh Chromium drops its first synthetic input — open a throwaway context first.

`device_bash` worked all session (node 22 / git / npm). The delete permission does NOT survive a reconnect —
ask again if `rm` says "Operation not permitted".

## §1 answers (Damir)

| ask | answer |
|---|---|
| suite line after the X paste | **BASELINE OK / the 2 KNOWN, +9** |
| the X walk | **X.1–X.10 all P** |
| the X commit | landed as ONE batch, `896958b4` (verified on the tree before anything moved) |
| S1 | **not timed** → #864 stays gated, the spare stays |
| the address affordance (mid-turn) | **V2 — action ink** (of four rendered variants, #879) |
| bots (mid-turn) | "bot group needs to have address as well" — rendered: it does (#878 ⑤) |
| the reviewer model (mid-turn) | the #46 loop ran on **Opus**, pinned explicitly (Fable built, Opus reviewed — #880/#881) |

## What landed (uncommitted; every file re-read after write, hashes matched)

| file | what | verified how |
|---|---|---|
| `src/components/chat-info.js` | #878: `groupCard` / `rowGlyph` atoms, the tile row (≤ 4, `QA_MAX` log+trim), the Mute tile, the hero address, danger by kind; `infoDisc`/`discGrad` gone | 42 executed properties, 19 mutations killed; rendered both themes through the wire |
| `src/styles/components/chat-info.css` | #878: grouped cards, `::after` hairlines (base.css owns `::before`), tiles, hero address (action ink), error role, focus ring inside the card; switch/addr-row/card-per-row rules deleted | computed cascade in Chromium (#880 ①②), contrast computed in the suite |
| `src/styles/tokens.css` | `--surface-card-hover/-pressed` both blocks (light 200/300 · dark 600/500) | resolved-ramp pin: ≥ 1.11 from the card in BOTH themes |
| `scripts/smoke-test.mjs` | +42 assertions (the Session Y block + fixture) · 23 pins re-based in BOTH halves (#861) · the undeclared-identifier gate as `lintShellDir` with the comment scanner, attribute `src`, seeded externals, file lines, per-shell vacuity, a fixture (#881) · #877's clause re-based to the positive property · `CHAT_KB_CEIL` 682 → 684 (delta stated) | full suite in the container: **BASELINE OK — 4714 pass / the 2 KNOWN** on the final tree (count below) |
| `src/demo/spixi.iife.js` · `Spixi/Resources/Raw/html/*` | rebuilt on the VM (bundle 317 · 18 shells; `build-shells --check` ✓) | hash-matched container ↔ VM |
| `DECISIONS.md` #878–#882 · this handoff · `docs/f5-checklist-session-y.md` · `docs/commit-message-session-y.txt` · `docs/sheets/session-y/` (8 PNG, each with the bridge `caBX` chunk, #872 ⑤) · `CLAUDE.md` status line · `docs/security-handover-gate.md` Session Y section · `docs/next-session-prompt.md` | — | — |

`_to_delete/` holds the session's harnesses (`y-cd-props` · `y-fast` · `y-gate` · `y-877` · `y-probe`), the batch
diff and the DECISIONS rows file — `Remove-Item` in the paste.

## §2 · The decisions, one line each

#878 the premium pass built (three token spellings moved on measurements; contrast computed; the suite ran whole) ·
#879 the address line = action ink on the button form only (V2 of four renders; bots carry it) · #880 the Opus loop on
the batch (2 MAJOR in the real cascade: `::before` collision, focus ring clipped — fixed; Request-from-directory is
Damir's) · #881 the Opus loop on Session X (false REDs loud as claimed; ONE false GREEN — `<!--` in a script comment —
fixed with a scanner + fixture; #877's pin re-based, 6/6 killed) · #882 the workshop (suite in the container; the
lock; the phantom write) + four dials.

## §3 · The render harness (container) — unchanged from X §3, plus

`/tmp/y/render/harness.mjs` (this session's copy): `playwright` is the GLOBAL package here (`import … from
'<npm root -g>/playwright/index.mjs'`), Chromium at `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`. Pushes used:
contact `setContext · setAddress · setNickname · showIndicator · showCallButton · setCaps('composeSend,composeRequest')
· setContactNotifications('1'|'0') · setSharedGroups(addr, name, addr…) · clearRecentActivity · addPaymentActivity(txid,
type, epochSecs, amount, 'true'|'false')`; group `setGroupInfo(count, blind, admin, notifications, owner, 'group'|'bot',
amOwner) · clearMembers · addMember(addr, nick, avatar, role, relation)`. A bot = `setGroupInfo(…,'bot',…)` + `setAddress`.
The `ixian:` console errors are the C# wire missing — expected.

## §4 · Open, in order

1. **Damir**: the Y paste (checklist §1) · the walk (§2, 12 rows, both themes, phone + desktop pane) · commit ·
   **S1 timed** (still).
2. **The four dials (#882)** — each a one-line word: (a) rows-inside-cards hover/pressed invisible on both themes,
   every card-hosted family — the new card pair fixes it per family (tokens.css 5c-iii says ask first); (b) the
   Account hub's in-card hairlines invisible in dark (-01 = 800 on 800) — move to -03 like this screen?; (c) `chat.html`'s
   dead chat-info inline (~30 KB CSS + the #249-retired takeover call) — remove the link + the call (one session, the
   honest #345 saving, ≈ 2.4 ms per chat boot); (d) Request from the directory (#880 ⑤) — leave as decided, or return
   the tile on the directory arm, or retire `openRequestForPeer` + the `composeRequest` cap term together (a gate
   inventory row: `ixian:sendrequest` emit site on ContactDetails).
3. The #46 loop's latent gate holes (#881, recorded not fixed): literals harvested into `shellGlobals`;
   browser-global masking of a deleted local named `name`/`status`/`event`…; the inert seeding clauses.
4. Then, unchanged: #864 after S1 · the settings swatch `110px 191px` mask (#866) · AND-40 (needs a run that kills) ·
   AND-36/39 (repro first) · the freeze (#825) last · the BE cutover (`be-cutover-brief.md`,
   `security-review-for-be-engineer.md` first — §1a CLOSED, #874).
5. ~~Bottom bleed~~ — DROPPED (#875). Do not reopen it on "Android requires it".

## §5 · Tester-readiness — unchanged from X §6

The gate for a closed tester group is still: the #232/#523 money-path review (or a one-line cap gate on
`composeSend`/`composeRequest` for the tester build) · `maxLogCount` 5 → 1 · retiring the probe sets · an iOS walk if
testers get iPhones.

## Rules that earned their place (this session)

#215 (the X commit, the checks and the ledger were read off the tree before one line moved; three "measured"
numbers in my own comments were wrong until the reviewer computed them) · #294 (three token spellings moved because
the ground they land on was measured, not because the proposal named them) · #798 (the payments hairline was
missing in a screenshot I had looked at — a computed pseudo-element caught what my eye and my harness did not) ·
#861 (23 pins re-based in BOTH halves) · #865/#882 ③ (a "written" that was not on disk) · ★ run the suite's
EXISTING gates on the change — now possible in full, in the container · ★ #880 ⑪: a `throw` in a render path the
shell calls from a `setTimeout` is a boot spinner, not a loud error — log and degrade · ★ #881: the loud/silent
question is the right one to ask of every gate: a false red is a cost, a false green is the defect the gate exists
to catch.
