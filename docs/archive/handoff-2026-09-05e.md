# HANDOFF — AFTER SESSION O (2026-09-05): the loop is CLEAN, cancel-first is app-wide, the pre-warm's BEFORE instrument shipped

★★ **This file SUPERSEDES `docs/handoff-2026-09-05d.md`** (archived as
`docs/archive/handoff-2026-09-05d-session-n-close.md`). Read this whole file, then **DECISIONS
#798–#799**, then the verdict in `docs/opus-review-brief-sessions-m-n.md` §Verdict, then
`docs/prewarm-chat-spec.md` (the build) and `docs/cdperf-2026-09-05-session-n-capture.md` (its
before-number). #771 · #772 · #773 · #797 still stand.

## 0 · State — UNCOMMITTED, the commit is Damir's

```
HEAD cba8fc18  After Session N: handoff 05d, the next-session prompt, consumed docs archived
Working tree: 88 files (22 C# · scripts · components · shells · 25 locale files · built artifacts · docs)
Commit message: docs/commit-message-session-o.txt   Checklist: docs/f5-checklist-session-o.md
Walk: docs/walk-artifact-session-o.html            Renders: docs/sheets/session-o/
Ixian-Core sibling at 097341a, untouched.
```

Baseline the next session must reproduce (clean clone AFTER Damir commits, Linux container,
`npm i jsdom tree-sitter tree-sitter-c-sharp playwright-core`, Ixian-Core sibling PRESENT):

```
bundle 320 · shells 18 · smoke BASELINE OK 4229 / the 3 known (#136 · M5 · B3)  ← WITH the sibling
                        (one fewer without it — the M1 hold-out gate's second pin, #748)
locales ALL CLEAN 784 · i18n-lint ✓ (6 dev exemptions, 2 sites) · pseudo 9/9
cs-syntax 138 clean + 1 known gap
extract-strings --check ✓ · build-shells --check ✓ · build-legal-docs --check ✓ (privacy HELD 🟡)
strip-release --check ✓ (gate 1) · smoke-packaged ✓ (gate 2, 4228 without the sibling)
```

## 1 · What the session did (one paragraph each; the rows have the rest)

**#798 — the #46 loop over Session M + Session N's C# and #797, CLEAN in five rounds.** Three
auditors → two verifiers → three fixers → a fresh break-my-verdict reviewer per round (r1 6 MINOR
· r2 5 MINOR · r3 3 MINOR · r4 2 prose · r5 CLEAN). **Two MAJORs, neither in Session M's own
delta:** (1) the #797 rule held on ONE page — nine `onNavigating` handlers cancelled only at the
tail and six more kept a permissive `else`, and on `LockPage`/`LaunchPage`/`EncryptionPassword` the
URL Android would load as a page carries a plaintext password; **all 19 handlers now cancel after
the decode and re-allow only `file:`**, and the pin WALKS every handler (no list). (2) The dark
Chat-appearance screen shipped an empty card. Also: `leaveGroup` returns the truth at all four
sites; `ixian:painted` gated on `hasGeneratedContent`; `LockPage`/`ScanPage`/`HomePage` verb
parses anchored; the strip masker no longer throws on correct CSS; `PATTERN_LEVELS` retired
(786 → 784 keys); the settings `renderLayout` negative pin matched NOTHING on HEAD; four Session O
pins were found vacuous by the next reviewer (#771 in its own loop, four times). +95 pins.

**#799 — the pre-warm's BEFORE instrument.** `HomePage.onOverlayClosed`'s SingleChatPage branch
starts the existing `CdperfFrameProbe` (600 ms, Android) with tag `chats-after-close`, only when no
conversation remains — the line where `warm()` will be scheduled. TEMPORARY. The spare itself was
NOT built (Tier 3, no compiler, its before-number needs the phone). +2 pins → 4229.

## 2 · Still open — the whole list

- **A15 (the ONE row that decides a code change):** Windows, app lock ON, delete the `html` folder
  beside `Spixi.exe`, boot. The `file:`-only tail on 19 handlers vs #768's fallback ladder
  (`HtmlWebViewSource`, no `file:` URL): Android is immune by construction (`LoadHtml` →
  `LoadDataWithBaseURL`, no `SendNavigatingCanceled`); WinUI/iOS may report `about:blank`. A
  blank lock = one `about:blank` clause in the shared tail (19 places). The lead's W0 evidence
  was BROKEN by the r2 reviewer (the walk-L row does not name the page).
- **F1 — the BEFORE capture:** 8 chat closes on the Motorola → paste the eight
  `[CDPERF] chats-after-close frames n= drop= max=` lines. The pre-warm is judged against them.
- **D1–D4 (Damir, one word each):** doodle SVGO land/keep · lossy PNGs render/no ·
  `contacts-es.svg` convert/keep · the four Session M dials (`docs/walk-session-m-results.md`).
- **BE row:** a bot that is a group member cannot be left (Core `removeFriend` refuses
  `isFriendInGroup`); it now reads as an honest error (`settings-deleted-error-text`), not
  "Contact removed". Needs a route or a string.
- **The #788 `[CDPERF] present <page> by=paint|timer` line** — still owed, phone-only.
- `spixi.base.css` joins the strip allowlist only after it has a CONTENT pin (gate 2's
  transform-independent post-condition is one pin thin on the allowlisted file).
- `HomePage`'s `ixian:qrresult:` branch is dead code (only `scan.html` emits it, routed to
  `ScanPage`); anchored, not deleted — deletion needs a #789-style proof.
- The M queue's tail (unchanged): RAM (#764/#778 BE row) · the group-avatar device fact · the
  TG-order chat-info rebuild · URL previews / privacy wording / "X left the group" · iOS rows ·
  release hardening LAST (retire every `[CDPERF]` line incl. the Session N pair AND
  `chats-after-close`, `[LANDTAB]` · `[EXCERPTDIAG]` · `[SCROLL]` · `[KBTRAY]` · `[WV2]` ·
  `[WEBVIEW]` · the console mirror · `maxLogCount=5` · `SpixiDevCoexist` · the keystore).

## 3 · The next session, in order (also `docs/next-session-prompt.md`)

★ **Damir's ruling (2026-09-05, after the loop closed): BUILD BOTH LEVERS in the next session** —
the pre-warm AND the batch transport, one build, one #46 loop after both. The walk below may not
have run yet; the next session does not wait for A15 or F1. Damir captures the BEFORE numbers on
the Session O build (8 opens + 8 `chats-after-close` lines) before the next batch lands on the
phone, then the AFTER on the next build. The prompt file carries the order; items 1–2 below are
read as one session.

0. **Damir's walk first** (`docs/f5-checklist-session-o.md`): A15 decides whether one clause is
   added before anything else is built; F1 is the pre-warm's before-number.
1. **THE PRE-WARM BUILD** per `docs/prewarm-chat-spec.md` §3 — with the phone in the room and
   F1 in hand: the seven pins (§5), the measurement plan (§6: 8 opens with the spare READY + 3
   fallback, the SAME `chats-after-close` probe after, memory at rest), a #46 loop AFTER.
   Before-numbers = #796 (open median ~315) + F1 (list frames). ⛔ #779 stays parked.
2. **THE BATCH TRANSPORT** (#298, `docs/chat-transport-spec.md` §1–§4): `addMessages` +
   `messagesDone` — the eval queue's measured 50–70 ms. Small C#; the shell stays idempotent on
   both transports. AFTER the pre-warm's after-number, so the two levers stay attributed.
3. Damir's D rulings whenever given (D1 land = `cp` the candidate over
   `src/assets/images/chat-bg-doodles.svg` → `generate-chat-pattern` → `build-shells`).
4. Gate 2 recorded per release; `spixi.base.css` after its content pin. JS stays OFF the list.
5. The M tail; release hardening LAST.

## 4 · The render harness (this session's shape)

`/tmp/render/appearance.html` — a page that takes the BUILT `settings.html`'s `<head>` (its
inlined component styles, linked to the built `spixi.tokens.css`/`spixi.base.css`), loads
`spixi.icons.js` → `spixi.strings.js` → `spixi.bundle.js`, and mounts one component
(`window.Spixi.createChatAppearance({…})`) under `data-theme`. Playwright over
`/opt/pw-browsers/chromium-1194/chrome-linux/chrome` with `--allow-file-access-from-files`,
432×900 @2.5, `colorScheme` per theme. `page.on('pageerror')`. The Session N harness (the full
chat shell through the C# wire) is described in the archived 05d §4; rebuild either from these
notes — the container is gone.

## 5 · Rules and workflow

Everything in the archived 05d §5, plus what this loop paid for:
- **A refusal is documented from the cases you did not think of, never from the author's list**
  (the r1 reviewer): both fixes that WIDENED a refusal shipped a comment claiming the refused
  cases were enumerated, and each left one out.
- **A pin that asserts a LINE is not a pin that asserts a PROPERTY** — `if \(` with a space, a
  200-char window a `!` satisfied, an `ex.Message` ban that `+ ex` walked past, a length bound
  called a charset gate. Four times, in the loop that was closing #771 findings.
- **A verdict must exist before a row cites it** (#660 in reverse): #798 cited a §Verdict that
  did not exist yet; r4 caught it.
- `smoke-test.mjs` resolves its root from its own path, so a run started from any cwd reads the
  tree the script lives in — a mutation copy must be run with ITS scripts, never the real tree's.

Bridge: `device_commit_files` for deliveries (LF, NUL-swept first); `git --no-optional-locks`
on his tree; the bridge cannot `rm` — `mv` to `_to_delete/`; never `git add -A`; the commit is
Damir's in GitHub Desktop; nothing pushes from the container. PowerShell for Damir: ONE command
per block.

## 6 · The one thing only Damir can do

A15 on Windows · F1 on the phone · the D rulings · the walk.
