# HANDOFF 2026-07-28 (post #273–#281 review loop) -> next session

**Phase:** B — platform bring-up (`docs/handoff-post-freeze.md` §1B unchanged: platform bugs only,
fix small, log in DECISIONS.md).

This session (Cowork/cloud + device bridge, no C# build) ran the **owed Opus #46 loop over
#273–#281** (`17dc04c3..c0ceae1e`) and landed the two outstanding #283 plate fixes. Full findings +
fix inventory: **`docs/opus-review-273-281.md`** (read it first). DECISIONS #284. All edits are on
disk UNCOMMITTED (plus the in-session shell rebuilds); working tree also carries the usual CRLF
churn (`git diff --ignore-cr-at-eol` shows the real delta).

## THE PLATE — next

> **[2026-07-29] Item 1 DONE** — smoke + F5 run (all 4 points; wallet-hide failure → fixed as **#285** incl. desktop language propagation + de-de fill, F5-verified). Item 2's exec sheet: **docs/device-pass-runsheet-2026-07-28.md**. See CLAUDE.md last row.

1. **Damir: local smoke + build + F5 + commit** (checklist at the bottom of the review doc):
   `node scripts/smoke-test.mjs` locally (device VM killed long node runs; expect the #276
   assertion label change) → build net10.0-windows (NOT Rebuild; changed: SingleChatPage,
   SettingsPage, Utils, iOSWebViewHandler, Spixi.csproj + 3 shells) → F5 the 5-point list
   (#274 language pane · #275 lock 1:1-vs-group · delete-history immediacy both legs · wallet
   hide/address · launch illo on sim) → commit as ONE batch "Opus #46 review #273–#281 + fixes
   (#284)". Hygiene: `git rm --cached _to_delete_index.lock _to_delete_index.lock2` · delete
   `_to_delete/review-273-281/` (session scratch).
2. **Device pass verify-gated rows (#215 rule, from the review):** B1 OneSignal delegate starvation
   (foreground REMOTE push → does `handleNotificationReceived` fire + offline fetch run?) · B2
   double tap-handling · C-9 `file://` localStorage persistence probe (60 s, clears ~8 mechanisms)
   · B3 = build `-c Release -p:RuntimeIdentifier=ios-arm64` before any device ship.
3. **iOS-31 — push literal `0`** from loadMessages' unread-clear (requestAdd never markable-read).
   Second leg: HomePage contactStatusCache struct-copy + ts=0 drop. Inspect live (#215).
4. **iOS-29 — keyboard handler.** Baseline delta against boot-captured layout height, or pin
   `body.height = vv.height` while composer focused; gate on `vv.offsetTop`. Confirm with Safari
   Web Inspector whether `innerHeight` tracks `vv.height` on device.
5. **Mini-app WebView regressions (security, #282/#283 review).** Global `iOSWebViewHandler` hands
   mini-app `LinkActivated` out to Safari (no confirm, no TargetFrame check); mini-app pages lost
   their safe-area inset. Scope the handoff to trusted host shells; classify MiniAppPage.
   **Log in `security-review-for-be-engineer.md`.**
6. Safe-area gaps: toast / pending-strip / request pane / encpass footer bottoms; landscape
   left/right insets. `fitTile` geometry. Backup-stamp cross-WebView localStorage (folds into
   plate item 2's C-9 probe).

## BE asks accumulated this loop (be-cutover / BE chat)
- FriendState enum + a non-destructive "resend request" recovery for locked legacy states (A2).
- Can a GROUP ever carry a non-Approved state? (decides whether the #275 group guard is latent or
  live protection).
- nuget.config hardening call (packageSourceMapping + sha512 in local-nuget/README) — Damir/BE.
- A6 recommendation: state-guard `onSend`/`onSendFile` C#-side (the ⑪ invariant is FE-only today).

## Carried forward (unchanged)
- Device pass iOS-26..32 (`docs/ios-sim-findings.md` — triage paragraph + iOS-27 premise CORRECTED
  this session: hunt a marshalling/lookup abort first, the #280 guard cannot log that class).
- PC track: `docs/handoff-2026-07-22-pc.md` (⚠ its "lock files are untracked" gotcha is WRONG — see
  review C7) · Android bring-up.
- Mac remaining: Deutsch locale/dates · lock flow · Safari pass over `src/demo/*.html` · Stage-5.
- Gated: wallet-send LAST (#232; prerequisite: createWalletSend unfiltered-roster fix #255 + the
  isContact component fix from this review) · reply-to (BE carrier) · #234 (BE) · BE cutover docs.

## Gotchas (this environment)
- Mount can't unlink (`rm`/`git checkout` fail) — overwrite via `git show HEAD:f > f`; edit big
  files via node fs (CRLF-adapt!); new files OK. Long node runs (smoke, jsdom import) get KILLED on
  the device VM this session — generators run fine, smoke = Damir local.
- **`build-shells.mjs all` overwrites the two still-LEGACY drop-ins `apps.html` +
  `wallet_send.html` (money page). Use `node scripts/build-shells.mjs launch` for launch; default
  (no arg) for the 17 bridge-wired shells.** (Bit this session; restored from HEAD.)
- Consumed → `docs/archive/`: `handoff-2026-07-27-postreview.md`.
