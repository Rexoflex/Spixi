# Opus #46 loop — Batch B + C + D, ROUND 2 VERIFIER

Date 2026-08-24 · repo `/root/Spixi` @ `42e72109` + working tree · Ixian-Core frozen at
`097341a`, read-only. Language: ASD-STE100.

Scope: the 9 numbered checks in the r2 mandate — verify that the round-1 fixes hold and that
they broke nothing.

**No file under `/root/Spixi` was changed except this report.** Every measurement ran read-only
or in `/tmp/.../scratchpad`. Gates re-run here:

| Gate | Result |
|---|---|
| `node scripts/smoke-test.mjs` | **BASELINE OK — 2995 pass / 3 KNOWN**, exit 0 ✓ |
| `node scripts/i18n-overflow-audit.mjs` | NO BREAKERS (7 report-only topbar clips) ✓ |
| `node scripts/i18n-overflow-audit.mjs --all` | one near miss: de-de `revokeRequestConfirm` 82/88px (r1 n-6, unchanged) |

New tool used this round: **headless Chrome 148** (`/home/claude/.cache/puppeteer`, driven by
the puppeteer bundled with `mermaid-cli`). jsdom has no layout engine, so r1 could only reason
about the modal geometry. This round it was measured.

---

## FINDINGS

| # | Rank | Item | Where | What is wrong |
|---|---|---|---|---|
| **R2-1** | **MAJOR** | 7 — the M-1 fix is **INERT**. `min-width: 0` cancels `flex-wrap: wrap`; the pair NEVER wraps, and the confirm label now spills out of its own button instead of out of the modal | `src/styles/components/overlay.css:124-139` | The two lines fight each other. Flexbox collects items into lines by each item's **outer hypothetical main size** = flex base size clamped by the used min/max main size. `flex: 1 1 0` makes the flex base size `0`; the new `min-width: 0` makes the used minimum `0`; so the hypothetical size is **0** and both buttons always fit on one line. `flex-wrap: wrap` can never fire. Before the fix, `min-width: auto` resolved to min-content (the `white-space: nowrap` text), which is exactly what a wrap needs. **Measured in Chrome 148** (viewport 360, `.c-modal` padding 24): <br>· BEFORE — `wrapped=no`, btnW `[92.9, 278.1]` = 379px of buttons in a 312px content box.<br>· **AS SHIPPED (wrap + min-width:0)** — `wrapped=no`, btnW `[152, 152]`, fr-fr `deleteAccountConfirm` `scrollWidth=195` in a 152px button → **43px of destructive label spills past the button edge**, `documentElement.scrollWidth` **379 > 360** (the text leaves the viewport). Same for es-co (190/152, docScrollW 374) and ru-ru `cancelInviteConfirm` (158/152).<br>· **`flex-wrap: wrap` WITHOUT `min-width: 0`** — `wrapped=YES`, each button 312px full width, row height 96px, `scrollWidth == width` (no overflow), docScrollW 360. ✓<br>**Fix: delete `min-width: 0` from `overlay.css:139`.** One token. The comment on the line ("min-width 0 so flex can shrink before wrap") states the inverse of the spec — shrinking is what removes the need to wrap, so it removes the wrap. |
| **R2-2** | **MAJOR** | 7 — the shipped confirm labels were **NOT** shortened, so R2-1 is load-bearing | `src/strings/fr-fr.json` · `es-co` · `pt-br` · `Spixi/Resources/Raw/html/spixi.strings.js` | The mandate assumed shortened fr/es/pt/de/ru confirms. The **drafts** were shortened (`draft/fr-fr.json` `deleteAccountConfirm` = "Supprimer", `cancelInviteConfirm` = "Annuler"; same shape in es-co/pt-br/ru-ru/de-de/sl-si/sr-sp). The **shipped** locales were not, and the built runtime carries the long text: `spixi.strings.js` holds fr-fr "Supprimer définitivement le compte", es-co "Eliminar cuenta permanentemente", pt-br "Excluir conta permanentemente", ru-ru "Отменить приглашение", ja-jp "アカウントを完全に削除". The drafts do not ship (`build-locales.mjs` iterates `src/strings/*.json`), so the shortening is invisible to the app. Either land the short forms in the shipped files, or rely on R2-1's wrap — but today **neither** is in force. |
| **R2-3** | MINOR | 8 — `ixian:tab:tab1` does **not** flush the chats list, and it is **not** side-effect-free | `src/shells/home.html:3376` · `HomePage.xaml.cs:852-895` · `:4131-4149` | The C# `ixian:tab:` branch sets `currentTab`, runs the #240 overlay-exit sweep, calls `repaintOwnSystemBars()`, then branches **only** for `tab2` (`loadTransactions`) and `tab3` (`loadApps`). **`tab1` runs no loader at all.** So the shell comment "the tab verb re-runs loadChats without any other side effect" is wrong twice: (a) **m-2 is still open** — `onUndoRequestFor` still sets `UIHelpers.shouldRefreshContacts = true` inside the success branch only (`:4134`), so a refused revoke schedules no flush and the row stays gone; (b) the emit **does** have side effects — `suppressNextTabOverlayExit` is false on a normal run, so the `else` at `:868` fires `requestSettingsOverlayExit()`, `closeContactDetailsOverlays()`, `closeFormPaneOverlays()` and `closeTxDetailOverlays()`. On desktop/rail the chats list stays interactive beside an open Account or chat-info pane, so a **refused** revoke silently tears those panes down. No edit is lost (the Account exit is routed through the shell), but the teardown is unrequested. **Fix: set `UIHelpers.shouldRefreshContacts = true;` in `onUndoRequestFor`'s fail path (before the `undoRequestResult` push) and delete the `ixian:tab:tab1` emit.** |
| **R2-4** | MINOR | 6 — `warmPending` has **no clear** on any non-park exit of `pushPageLoaded`; a leak turns a later Account tap into a no-op | `SpixiContentPage.cs:1894` · `:1943-1955` · `:1973-1979` · `:2021-2033` | `warmPending = true` is set at `:1894`. The **only** clear is `:2026`, inside `if (op.parkOnLoad)`, and `op.parkOnLoad = parkOnLoad && overlayMode` (`:2021`). Four exits leave it TRUE: the `modalOverlayOp` drop (`:1943`), the `preloadPending‖activePreload` drop (`:1948`), the no-host plain-push fallback (`:1973`), and — the reachable one — **`overlayMode == false` in the staging turn**. `warmParkedOverlay` tests `overlayModeHolds` under the lock at `:1886`; `pushPageLoaded` re-derives `overlayMode` one dispatcher turn later at `:1966`. A legacy `PushAsync` landing above HomePage in that gap makes them disagree: `op.parkOnLoad` becomes false, `warmPending` is never cleared, **and the warm Account page is then PRESENTED unbidden** by the `else if (op.overlayMode)` fall-through. Because `accountWarmed` latches per HomePage, nothing ever resets the flag afterwards. Harm needs a second event: any later push sets `preloadPending = true` for one turn; an Account tap in that turn makes `claimWarmingOverlay` take the `preloadPending && warmPending` branch (`:1921`), **return true** ("a present is coming") and set `warmClaimRequested`. That push is not a warm settings op, so its staging never consumes either flag — **the tap does nothing and no present arrives.** Both windows are one dispatcher turn wide, so this is narrow, not impossible. **Exact fix — move the clear out of the conditional:**<br>`lock (preloadLock) { warmPending = false; if (op.parkOnLoad && warmClaimRequested) { warmClaimRequested = false; op.parkOnLoad = false; } }`<br>placed unconditionally at `:2022`, plus `warmPending = false; warmClaimRequested = false;` in the two early-drop lock blocks (`:1945`, `:1953`) and in `:1976`. As a belt, stamp the flag with a time bound the way `lockPreloadPending` already is (the file's own idiom, `:657-661`). |
| **R2-5** | MINOR | 9 — the optimistic "Invite canceled" toast breaks the codebase's **own** rule, and B1's twin proves it | `src/shells/chat.html:1776-1781` vs `:4042-4047` · `home.html:3365-3378` · `contact_new.html:184-191` | My view: **the success toast should wait for `'ok'`.** The precedent the comment cites does not cover this case. N55 is explicit that it applies to a **fire-and-forget verb with no ack**: "the verb is fire-and-forget (no ack push) → optimistic toast" (`chat.html:2421`, `:2534`). `cancelInvite` **has** an ack — `cancelInviteResult(id, status)` with an `ok`/`fail` contract. The codebase already ruled on that exact shape and ranked it MAJOR: `contact_new.html:184` — "**N55 → #376 loop B-2 (MAJOR): NO optimistic toast on THIS surface** … a green 'sent' toast fired exactly when it was false and stayed on screen beside the alert." And B1, the twin this batch says it copies, toasts success **only** in the `'ok'` branch (`home.html:3368-3370`). Today a `fail` answer paints a green "Invite canceled" and then a red "The invite could not be canceled." — two toasts that contradict each other on a destructive action. Moving the toast into `cancelInviteResult`'s `ok` branch also keeps r1 m-3's a11y intent (the announcement still lands after the opener is re-rendered away) and costs one dispatcher hop, because the answer is local, not networked. |
| **R2-6** | MINOR | 7 — both overflow-harvester blind spots are still open, so the gate still cannot see either label | `scripts/i18n-overflow-audit.mjs:54` · `:88-92` | `git diff --stat` on the script: **no change**. `keyRe = /strings\.([A-Za-z_$][\w$]*)/g` still matches `strings.KEY` only, so `chat.html`'s `s.cancelInviteConfirm` is invisible; the `settingsConfirm({` window still cannot follow `confirmAction(buildOpts())`, so `deleteAccountConfirm` is invisible. Proof by execution: `--all` lists `revokeRequestConfirm` (harvested via `createModal` in `home.html` with the `strings.` form) and **neither** of the two keys r1 flagged. The gate reporting "NO BREAKERS" is therefore not evidence about R2-1/R2-2. |
| **R2-7** | MINOR | 2 — MINOR-1 is **mitigated, not closed**; the eval still has no receipt | `SettingsPage.xaml.cs:1089` · `:1050` · `SpixiContentPage.cs:361-375` · `:982-1008` | Moving `wipeLocalState` to step 0 is strictly better and never worse, but it does not change the ordering that r1 questioned. `wipeEverything` runs **on** the main thread, and `evaluateJavascript` wraps the call in `MainThread.BeginInvokeOnMainThread` — so the eval is queued to turn T+1 while every remaining step **and** `goToWelcome()` (the `finally`, `:1050`) still run in turn T. What step 0 buys is real but narrow: the push now happens before any teardown can null `_webView` (which would silently divert it to `messageQueue`), and it is queued ahead of `disposeParkedOverlay`'s own `BeginInvokeOnMainThread` (`:994`). FIFO is still the only thing that carries it, and `EvaluateJavaScriptAsync` only submits the script. r1's proposed receipt (`wipeLocalStateDone` + timeout) was not built. Acceptable for F5; keep the row on the handover gate. |
| n-1 | NIT | 6 — the C3 claim path still has **zero** pin coverage (r1 m-4, unchanged) | `scripts/smoke-test.mjs` | `grep -c "claimWarmingOverlay\|warmClaimRequested\|warmPending" scripts/smoke-test.mjs` → **0**. Every other r1 fix in this batch is pinned. This one can still regress silently, and R2-4 is exactly the kind of hole a pin would hold. |
| n-2 | NIT | 2 — `wipeAccountData()` is dead code, and a pin now pins the dead code | `SettingsPage.xaml.cs:1136-1145` · `smoke-test.mjs:6899` · `:14628` | 0 callers (grep). The C1 order pin still slices `wipeEverything()` with `indexOf('private void wipeAccountData()')` as the end bound (`:14628`). r1 n-3's risk is **mitigated**, because `:6899` separately asserts the method exists — a removal fails a pin instead of silently widening the slice. Still: a pin that guards dead code should say so, or the slice should use a stable end bound. |
| n-3 | NIT | pins — the unguarded `.click()` in the B1 jsdom block (r1 n-2, unchanged) | `smoke-test.mjs:14578` | `items.find((b) => /Revoke request/.test(b.textContent)).click();` — a mutation that makes the pin fail also throws a TypeError that kills the rest of the run. `?.` keeps a mutated run reportable. |
| n-4 | NIT | i18n — the 5 skipped drafts, half-fixed (r1 m-5) | `src/strings/draft/{cn-cn,id-id,it-it,ja-jp,lt-lt}.json` | The retired `deleteWallet*` keys are **now gone from all 12 drafts** ✓. The ~18 new B/C keys are still absent from those 5 (`deleteAccountConfirm` undefined in each). Not a gate failure — the shipped locales are complete and `verify-locales` is clean. |
| n-5 | NIT | i18n — the drafts and the shipped files now **disagree** on the two confirm labels | `draft/fr-fr.json` vs `src/strings/fr-fr.json` | draft "Supprimer" / shipped "Supprimer définitivement le compte"; draft "Annuler" / shipped "Annuler l'invitation". Whichever is intended, the two stores now tell different stories for the app's most destructive dialog. See R2-2. |
| n-6 | NIT | 4 — the iOS sweep can add an empty id | `Platforms/iOS/SPushService.cs:275-279` | `string id = n?.Request?.Identifier ?? "";` — a null identifier becomes `""`, passes the `CALL_PREFIX` test and is added to `ids`. `RemoveDeliveredNotifications([""])` matches nothing, so it is harmless. Skip the empty string for tidiness. |

---

## VERIFIED CLEAN

### 1 — `connectToNetwork` resumes first, and nothing wants to stay isolated ✓

* `NetworkUtils.resumeNetworkOperations()` is `static public` at **`/root/Ixian-Core/Network/Network.cs:656`**, on `public class NetworkUtils` (`:392`) in namespace `IXICore.Network` (`:22`). `Spixi/Meta/Node.cs:6` has `using IXICore.Network;`. The call at `Node.cs:308` **resolves**.
* All three legs exist and are reachable: `NetworkClientManager.resume()` (`NetworkClientManager.cs:74` → `clientManagerBase?.resume()`, null-safe), `NetworkServer.resume()` (`NetworkServer.cs:146`), `StreamClientManager.resume()` (`StreamClientManager.cs:69`). Each body is a **pure flag clear** (`paused = false`, `NetworkClientManagerBase.cs:255-258`) — no I/O, no thread, a true no-op when nothing was paused.
* `StreamClientManager.resume()` dereferences `streamClientManager` without a null check, but the **next line** of `connectToNetwork` (`StreamClientManager.start()`, `:311`) already does the same. The new call adds no failure mode that was not there one line later.
* **Callers of `connectToNetwork` — exactly three, all "we want the network" paths:** `App.xaml.cs:381` (OnResume, node was stopped), `App.xaml.cs:1394` (`EnsureNodeRunning`, same shape), `HomePage.xaml.cs:337` (the ctor's `Task.Run` start block). No fourth (grep).
* **`isolate()` has exactly one Spixi caller** — `SettingsPage.xaml.cs:1092`, the wipe, which then routes to welcome. The only other site in Core is `GenericAPIServer.cs:915`, an API verb Spixi does not host. **There is no path that wants the process to stay isolated**, so healing the latch on every connect is correct and cannot un-isolate a deliberate isolation.
* Residual, pre-existing and NOT made worse: if the user backgrounds the app while sitting on the post-wipe welcome screen, `App.OnResume` / `EnsureNodeRunning` see `NodeStatus.stopped` and restart the node with no account data (the old D-6 shape). The resume call changes nothing there.

### 2 — the wipe ✓

* **`wipeLocalState` is step 0** — `SettingsPage.xaml.cs:1089`, first statement of `wipeEverything()`. ✓ (Ordering caveat: R2-7.)
* **Per-call tries: 20.** Every destructive call now has its own `try/catch` with its own log line — `:1089` · `:1092` · `:1093` · `:1098` · `:1099` · `:1100` · `:1101` · `:1102` · `:1103` · `:1107` · `:1108` · `:1109` · `:1110` · `:1111` · `:1112` · `:1113` · `:1116` (the wallet block) · `:1125` · `:1126` · `:1129`. r1 MINOR-2's "seven destructive calls under ONE try" is gone: the account half is seven separate tries at `:1107-1113`. ✓
* **`stopStorage` before `deleteData`** — `Node.storage.stopStorage()` `:1099` and `Node.activityStorage.stopStorage()` `:1100` both precede `Node.storage.deleteData()` `:1101` and `Node.activityStorage.deleteData()` `:1102`. r1 MINOR-3 closed. ✓
* **The pin slice still matches** — `node scripts/smoke-test.mjs` → **BASELINE OK — 2995 pass / the 3 KNOWN pre-existers (#136 · M5 · B3)**, exit 0. ✓

### 3 — the SingleChatPage call-row cancel is on the load path, and runs per open ✓

* The block sits at **`SingleChatPage.xaml.cs:947-957`**, inside the `Task.Run(() => {…})` lambda that opens at `:884` and closes at `:963`, which is inside **`private void onLoad()`** (`:684-964`). It is placed after the `clearNotifications` block (`:934-938`) and before `updateScreen()` (`:962`), exactly as specified.
* `onLoad()` is dispatched from `onNavigating` on the `ixian:onload` verb (`:120-123`), which the chat shell emits on every document load.
* **It runs on every open.** The chat push at `HomePage.xaml.cs:1993` is `pushPageLoaded(new SingleChatPage(...), 4000, "chat", wide ? 1 : -1)` — the 7th positional argument `parkOnClose` is **absent**, so it defaults false. A conversation is therefore never parked and never re-presented by `representParkedOverlay` (which deliberately skips `onLoad`). Every open constructs a fresh page and re-runs `onLoad`. ✓
* The cancel is guarded (`friend != null && friend.walletAddress != null`) and wrapped in its own try. ✓
* **The dead `onResume` copy is gone.** `onResume()` (`:2835-2848`) now holds only `base.onResume()`, `updateMessagesReadStatus()` and the pre-existing `clearNotifications`, plus a comment pointing at the load path. ✓

### 4 — iOS SPushService ✓

* **`OneSignalNative.Notifications.ClearAll()` is gone from `clearNotifications`.** The one remaining `ClearAll()` in the file is at `:199`, inside `clearRemoteNotifications`, which is dead behind an unconditional `return;` at `:189` (r1 NIT-5, pre-existing and unchanged). ✓
* **The enumerated removal remains** — `GetDeliveredNotifications` → skip `id.StartsWith(CALL_PREFIX, StringComparison.Ordinal)` → `RemoveDeliveredNotifications(ids.ToArray())` (`:266-291`), with its own inner try. ✓
* **`CALL_PREFIX` is used in both required places** — `showLocalNotification` builds the request identifier as `(kind == "call" ? CALL_PREFIX : "") + messageId` (`:355`), and `cancelNotification` removes **both** `messageId` and `CALL_PREFIX + messageId`, delivered and pending (`:237-238`). ✓
* The Android twin is intact and unchanged: `CALL_TAG` (`:233`), the tagged cancel (`:242`, `:248`), the enumerate-and-spare sweep with its API-23 guard and pre-M blanket-log fallback (`:267-296`), and the tagged `Notify` (`:392`). ✓

### 5 — the appSession ghost guard skips ONE row, never the rest ✓

* The guard is `SingleChatPage.xaml.cs:2279-2282`, inside the `if (message.type == FriendMessageType.appSession)` branch (`:2270`) of **`public void insertMessage(FriendMessage message, int channel)`** (`:1984`).
* **`loadMessages` iterates and calls it per message:** `foreach (FriendMessage message in messages)` (`:1841`) → `insertMessage(message, selectedChannel)` (`:1856`), inside a per-message `try/catch` (`:1854-1860`). A `return` inside `insertMessage` **returns from `insertMessage` only** — the `foreach` continues to the next message. Confirmed. The method already used that idiom three times before this batch (`:1988`, `:1998`, `:2005`).
* The other two callers pass a single message: `UIHelpers.insertMessage` (`UIHelpers.cs:303`) from `Node.cs:930` and `VoIPManager.cs:288`. No loop, no skip. ✓

### 6 — the three warm/claim interleavings ✓ (no double-present, no dropped tap on the intended paths)

**(a) Tap BEFORE staging — the `preloadPending` window.** `claimWarmingOverlay` branch 1 fails (`activePreload == null`), branch 2 matches (`preloadPending && warmPending`, `:1921`) → `warmClaimRequested = true`, **return true**; `HomePage:1878` returns. The staging turn consumes it (`:2022-2033`): `warmPending = false`, `warmClaimRequested = false`, `op.parkOnLoad = false`. `presentPreload`'s park branch (`:2349`) reads `parkOnLoadNow(op)` under the lock → false → the op **presents**. Exactly one present. ✓

**(b) Tap DURING load — after staging, before present.** Branch 1 matches → `activePreload.parkOnLoad = false` under the lock (`:1913-1916`), return true. `parkOnLoadNow(op)` (`:2244-2246`) reads the same field under the same lock → false → **presents**. ✓

**(c) Tap AFTER the park.** Both claim branches fail (`activePreload == null`, `preloadPending == false`) → return false → `HomePage:1882` `getParkedOverlay()` returns the parked `SettingsPage` → `representParkedOverlay` presents it. ✓

**Can BOTH the flag and the flip land (double-present)? NO.** `preloadPending` and `activePreload` are flipped **together, under one lock acquisition** (`:2054-2058`: `activePreload = op; preloadPending = false;`). For a given op the two claim branches are therefore mutually exclusive, and each call returns after the first match. A second tap in window (b) after a window-(a) claim finds `op.parkOnLoad` already false and `preloadPending` false → claim returns false → the fresh `pushPageLoaded` is dropped by `:1948` — but a present is already in flight from the first tap, so the user still gets the page. No path presents twice.

**Can NEITHER land (dropped tap) in the (b)→(c) handover? NO.** The park branch sets `parkedOverlay = op` at `:2363`, and `activePreload = null` runs later, in the enclosing `finally` at `:2522-2528`. So `parkedOverlay` is populated **before** `activePreload` is cleared — there is no instant where both are null. There is also no `await` between `:2349` and `:2528` on the park path, and both the WebView `onNavigating` claim and this block run on the main thread, so they cannot interleave. ✓

**The `warmPending`-stuck case the mandate asked about IS reachable**, but not through `pushPageLoaded`'s early-drop paths as feared — `warmParkedOverlay` releases the lock at `:1893` and re-enters it at `:1932` on the **same** main-thread turn with no `await`, and every observed `pushPageLoaded` / `pushModalLoaded` call site is on the main thread, so those two drops need an off-main-thread racer that does not exist today. The live hole is the `overlayMode` re-derivation one turn later. Full analysis and the exact fix: **R2-4**.

### 7 — no modal pin measures geometry ✓

`grep -c getBoundingClientRect scripts/smoke-test.mjs` → **0**. All seven `.c-modal__actions .c-button` pins (`:857`, `:1027`, `:1380`, `:1642`, `:1663`, `:1681`, `:3009`) select by **DOM order** (`btns[btns.length - 1].click()`) and assert labels, counts and focus. `flex-wrap` changes visual line breaking only, never DOM order — so no pin changes under wrap, and dropping `min-width: 0` (R2-1) keeps all 2995 assertions green. Under the corrected CSS the destructive confirm lands on the second (lower) row, which is the normal stacked-dialog order.

### 8 — the B1 pin shed ✓

`home.html:1257` — `if (pinnedChats.delete(chat.address)) savePins();` inside the `revokeRequest` branch of `onPersist`, matching the delete twin at `:1282`. r1 m-1 **closed**. ✓ (`requestAddrs.delete` is also there, `:1256`.) The refusal flush half is **R2-3**.

### Also clean

* The Android `clearNotifications` sweep, its pre-M fallback and its log line are unchanged and still pinned (r1 mutation (e) proved the pin).
* `IxianHandler.balances.Clear()` (`:1126`) and `IxianHandler.wallets.Clear()` (`:1125`) are still on the wipe path, each in its own try — F-3's duplicate-key half stays closed.
* `deleteInFlight`, the two-hop `BeginInvokeOnMainThread` and the `finally`-routes-to-welcome shape are unchanged.
* `wipeEverything`'s enumeration comment now names the loop r1 findings it answers (MINOR-1/-2/-3), so the file documents its own audit trail.

---

## VERDICT

**2 MAJOR · 5 MINOR · 6 NIT — six of the eight verified fixes hold; the seventh (M-1) does not fire at all and the eighth (m-2) is decorative.** Items 1–5 of the mandate verify completely: `resumeNetworkOperations` resolves and no path wants isolation, the wipe pushes local state first with 20 per-call tries and `stopStorage` before `deleteData` at BASELINE OK 2995, the call-row cancel is inside `onLoad`'s task lambda and runs on every open because a chat is never parked, iOS drops `ClearAll` while keeping the enumerated sweep and uses `CALL_PREFIX` on both sides, and the ghost guard's `return` cannot skip a sibling message. The C3 claim/park interleavings are correct in all three orders — no double-present, no dropped tap — with one narrow leak (`warmPending` is cleared only on the park branch) that needs a four-line fix and has zero pin coverage. What does not hold is the modal overflow: `min-width: 0` cancels the `flex-wrap: wrap` beside it, so the pair never wraps and the French delete-account label now spills 43px past its own button and off the 360px viewport — measured in Chrome 148, not inferred — while the shipped locales still carry the long strings (only the non-shipping drafts were shortened) and both overflow-harvester blind spots are still open, so the green gate says nothing about either label. Delete one token from `overlay.css:139`, decide where the short labels live, move `shouldRefreshContacts` into the revoke fail path in place of the `ixian:tab:tab1` emit, and hold the success toast until `cancelInviteResult` answers `ok`.
