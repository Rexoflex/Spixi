# Opus brief — adversarial review + fix loop (2026-07-05)

You are Opus, running an adversarial review-and-fix round over the entire Spixi
frontend codebase. Goal: **simple, maintainable code with every bug and edge
case fixed.** Nothing else. No new features, no redesigns.

## Read first (in this order)

1. `CLAUDE.md` — ground rules (note: Status section is stale; DECISIONS.md is current).
2. `DECISIONS.md` rows **#135–#150** (current era). Never contradict a ✅ row —
   architectural findings become 🟡 rows + a question to Damir, never silent changes.
3. Specs for whatever you're touching: `docs/chat-info-spec.md`,
   `docs/wallet-shell-spec.md`, `docs/chats-shell-spec.md`, `docs/apps-shell-spec.md`,
   `docs/overlays-spec.md`, `docs/settings-shell-spec.md` (+ its companion
   `docs/backup-ux-spec.md`).

## Current round (2026-07-05, per #146 + #147 + #148)

Scope: the **Account/Settings shell** (settings-shell.js / settings-backup.js /
settings-screens.js / their CSS / settings.html / the #146–#148 smoke blocks /
tokens.css additions [--disc-*, --preview-*, --chat-text-scale,
--switch-track-off/--switch-knob] / base.css `.c-disc` atom / **the #148
chat-info harmonization diff** [discs + card sections + switch pair + `> svg`
ink scoping — audited behavior must be UNCHANGED there]) — built +
premium-redesigned in a finalize-only session, NOT yet adversarially audited.
Run the full loop over it. Known watch-list: per-row commit mapping onto
`ixian:save:<nick>` (spec §2) · lock ON/OFF asymmetry · avatar sheet latch ·
password-modal lock (#135-C1) · `_backupBits`/`_statusBits` expando hooks ·
theme-tile latch vs option-sheet latch (duplicated grammar — dedupe candidate;
note the theme sheet now STAYS OPEN and re-releases its latch, #148②) ·
segGroup `current` closure · security-tier latch/spinner cleanup on BOTH
paths · `.c-settings__group` card grammar reuse inside settings-screens.css ·
hero art + clip-path + preview paints (layout-blind jsdom — reason about the
CSS) · error-hue reservation (one disc per surface, smoke-guarded) · the
`> * {flex:none}` scroll-body guard — check OTHER shells' scroll columns for
the same crushed-fixed-height class (#148③ root cause) · chat-info leftovers:
`.c-chat-info__txs-toggle > svg:first-child` rule is now inert (disc replaced
the bare glyph) — clean up. #149 adds full-bleed tx rows via negative margins
inside the card (check RTL + the gap-4 strips between rows) and the 52px row
height — verify no chat-info functional assertion regressed.

## Environment rules (violating these wastes Damir's money)

- **Use file tools (Read/Edit/Write) for all source files.** In the Cowork
  sandbox, bash serves STALE/TRUNCATED copies of files edited by file tools —
  bash is only safe for listing files and reading never-edited ones.
- **If you can run Node on the REAL files** (Claude Code on Damir's machine):
  run the build + smoke yourself, freely, every iteration.
- **If you cannot** (sandbox): write code + smoke assertions ONLY, then stop
  and ask Damir to run and paste the output:
  `node scripts/build-demo-bundle.mjs` then `node scripts/smoke-test.mjs`
  (PowerShell — two commands, no `&&`; run from the `Spixi` subfolder).
  Never claim assertion counts you haven't seen pass.
- Never hand-edit `src/demo/spixi.iife.js` — it is generated by
  `build-demo-bundle.mjs`. Fix the source component, rebuild.

## The loop (repeat until CLEAN)

1. **Audit.** Spawn read-only adversarial agents over disjoint scopes
   (components+css / demo pages+wiring / scripts+generators). Findings need
   file:line. Hunt specifically: unhandled null/empty/overflow states, latch
   and re-entry bugs on async buttons, listener/observer leaks, focus traps
   broken by early return, RTL and long-string overflow, dead code, duplicated
   logic, raw values where a token exists, contract violations (list below).
2. **Classify.** MAJOR (bug/edge case/contract breach) and MINOR (cleanup) get
   fixed. Architectural changes → 🟡 DECISIONS row + ask Damir first.
3. **Fix.** Targeted edits only. For every bug fixed, add a smoke assertion in
   `scripts/smoke-test.mjs` that would have caught it (regression guard).
4. **Verify.** Build + smoke (see environment rules). Red → fix → re-run.
5. **Review.** Fresh adversarial reviewer agent over the round's diff.
   Findings → back to 3. Exit only when a full review pass reports **zero
   findings AND smoke is green**.

## Simplicity mandate (the point of this round)

- Prefer **deleting** code over adding it. Dead branches, unused params,
  copy-pasted helpers: remove.
- Dedupe known repeats — named target: shared money module
  (`canonicalAmount`/`toUnits`, currently duplicated across wallet/chat paths).
- No new abstraction unless it deletes more code than it adds.
- Keep the house shape: one `.js` + one `.css` per component; free exported
  functions for updates (#44); semantic tokens only in component CSS (raw
  values need a `/* sanctioned */` comment); data-attributes mirror Figma names.

## Hard constraints

- Bridge protocol is FROZEN — new commands are ARCHITECTURE.md §8/§9 proposals
  only. Never invent an `ixian:` command.
- No behavior/UX changes without a DECISIONS.md row. When in doubt, ask.
- Demos must keep opening via double-click (file://) against the bundle.
- SECURITY.md invariants: shells emit payment intent only; no keys in WebView.

## Contracts that bite

- `setPaymentStatus(row, patch)` takes an OBJECT patch (a bare string is a
  silent no-op, #140②) and **returns a NEW row** — adopt it, re-wire menus.
- Latched buttons are released by re-render, never re-enabled in place;
  callbacks use the `(payload, ctrl)` contract, one-shot per attempt.
- `status:'failed'` rows are re-created, not patched.
- Fully-locked confirms: Esc/scrim/Cancel dead in-flight; sync throws in shell
  callbacks must route to the fail path (#141-m4).
- Whatever appends a chat row keeps the typing pill last (`keepTypingLast`, #140④).
- Amounts through `formatIxiAmount` — ≤2 decimals, truncated never rounded.
- Flex items that contain nowrap text need `min-width:0` (#140③) — jsdom is
  layout-blind, so overflow bugs need reasoning, not just tests.

## When done

Append one DECISIONS.md row (#143+): what was found, what was fixed, assertion
count delta, anything 🟡 for Damir. List any files whose diff he should eyeball
in GitHub Desktop.
