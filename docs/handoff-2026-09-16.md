# Handoff — 2026-09-16

Records: `DECISIONS.md` **#845–#860**. Walk sheet: published artifact (link in chat) or
`docs/f5-checklist-session-u.md`. Commit message: `docs/commit-message-session-u2.txt`.

---

## ⚠⚠ READ THIS FIRST — a session can no longer run anything on the machine

A **Windows update (2026-09-08)** stops Claude's Linux workspace reaching the repo, so
**`device_bash` fails**. Staging and committing files still work.

**What that changes (#860):**

| | |
|---|---|
| a session CAN | read files, write files, reason, prepare batches |
| a session CANNOT | run `git`, `node`, the generators, or any gate on Damir's machine |

So **every verification step is Damir's, in PowerShell**, and a batch lands as *files plus a
command block* — never as "I ran it, it is green". ⓘ Claude Code on the machine is
unaffected by the same update and is the fallback if a batch genuinely must run something.

Two working rules while this lasts:

* `DECISIONS.md` is **1.8 MB** — edit it by staging, appending in the container and
  committing back. Never read it into context.
* A doc **move** (archiving a consumed handoff) is a `git mv` in Damir's shell. The bridge
  writes files; it cannot delete them.

---

## Where the work stands

**Committed — `690b09c9`, Session U (#845–#857).** The seven queued dials, plus the gradient
option retired on Damir's answer, plus the icon-intent and bloat records.

**Uncommitted, verified, waiting for one commit:**

| file | what |
|---|---|
| `scripts/smoke-test.mjs` | the **8 pin fixes** (#859) |
| `scripts/pin-sweep.mjs` | **NEW** — needs `git add` |
| `DECISIONS.md` | rows **#859** and **#860** |
| `docs/handoff-2026-09-16.md` | **NEW** — this file |
| `docs/commit-message-session-u2.txt` | **NEW** |
| `docs/next-session-prompt.md` | rewritten |

⚠ The 8 fixes were each verified against the real files by evaluating their assertions
directly, but **`smoke-test.mjs` has never been run end to end since** — that is step 1
below and it is the only thing standing between this and a clean baseline.

---

## ★ The three things worth doing, in order

### 1 · Run the suite and commit (5 minutes)

Full commands in the chat and in `next-session-prompt.md`. Expect **BASELINE OK / the 2
KNOWN** — the baseline is **2, not 3**: M5 was re-read at #848 and is green. If M5 goes red
now, **it is new**.

**Second pass (#861).** Damir's run of the eight-pin fix left **two still red — #835 and
#755 — and the code was correct in both.** #855 retired the gradient option and closed all
three ground ladders on `'flat'`; those two pins still asserted the two-value allowlist the
retirement had removed. #755 is the one to remember: its **message** had been re-based at
#855 while its **assertion** had not, so prose and test described different worlds. Going red
was the lucky outcome — had the old spelling survived, it would have stayed **green while
advertising an invariant it no longer checked**.

Both are re-based with a **negative half** (no ladder may *admit* `'gradient'`, swept on
stripped code per #771), six mutations, zero survivors. Three stale comments were corrected
in the same pass — chat.html's ② docblock, settings.html's readChatPrefs block, and GATE 62's
note, all of which still documented the gradient as a live choice.

⚠ **Why `pin-sweep.mjs` did not catch these:** the shells were edited in the FIRST Session U
commit and the pins in the SECOND, so neither single-commit delta showed them together. Run
it across the whole session range, not one commit:

```
node scripts/pin-sweep.mjs <first-commit-of-the-session> HEAD
```

### 2 · ★ The stutter — one token, one build, and it answers itself

You reported stutter on contact details, chat open and scrolling, and suspected excess
WebViews. That suspicion is **already written into the code**. `HomePage.xaml.cs`,
`CHAT_SPARE_ENABLED`, its own docblock dated 2026-09-06:

> *"moving the warm does not remove the resident hidden WebView, and that is what the app
> now feels (Account → Backup, chat info, list scrolling)"*

Those are your three symptoms verbatim. The #800 chat pre-warm keeps a **resident hidden
WebView** — measured at **~15 MB PSS**, buying chat-open **300 ms → 113 ms**.

**Flip it to `false`, rebuild, use the app for five minutes.**

* **Stutter improves** → the resident spare is the cause. We have been trading the wrong
  way, and it also helps AND-40 (the 511 MB kill).
* **Stutter unchanged** → the spare is exonerated; it is the cold WebView boots, and
  contact details needs #804's treatment.

⚠ Contact details opens a **fresh WebView every time** (`loadPage(webView,
"contact_details.html")`) — #803 measured that class at **130–230 ms on the main thread**.
The cure we used for the Account rows (#804: delete the page push, render in the open
WebView) is **not directly available** here: ContactDetails keeps its own WebView
deliberately (#221/#247 isolation), so that move needs a security read first.
⚠ Contact-details open is **uninstrumented** — its `[CDPERF]` probe was removed at #668
once it had answered its question. If the flag experiment is inconclusive, the probe comes
back **before** anyone touches code (#294).

### 3 · The walk

`docs/f5-checklist-session-u.md`, or the published artifact. **§5.1 is the one that
matters**: a device that had Doodles selected must not open a chat to a blank canvas.

---

## Also waiting

* **Thinner icons (#856).** 79 SVGs re-exported and four new ones, left untouched and out of
  the Session U commit on purpose. Run `generate-icons` → `build-demo-bundle` →
  `build-shells` **as its own commit**, then look at the chat topbar — smallest glyphs in
  the app, so it is the honest test of a lighter stroke.
  ⚠ **No gate compares the SVG sources with the generated registry**, and
  `generate-icons.mjs` has no `--check`. They can disagree with the whole suite green.
  Cheapest gate on the board.
* **Two removable items (#857/#858).** The retired doodles tile — **233 KB of a 252 KB
  generated stylesheet, 94%**, unselectable since #853. And **~368 KB of duplicated CSS**
  (926 KB inlined across 18 shells against a 559 KB source tree).
* **AND-40** the memory kill — needs a run that actually kills. Page accumulation is ruled
  out (#830); the floor is ~400 MB, so 511 MB is a spike: chase native/graphics.
* **AND-36** rotation highlight · **AND-39** the tap fill — repro first (#294).
* **The freeze (#825)** — `maxLogCount 5 → 1` and retiring the probe set. Both remove
  instruments still in use while AND-40 is open, so it stays last.
* **The BE cutover** — `be-cutover-brief.md`, and walk the engineer through
  `security-review-for-be-engineer.md` first.

---

## Consumed — archive these (commands in the chat)

`handoff-2026-09-09.md` · `handoff-2026-09-09b.md` · `f5-checklist-session-t.md` ·
`commit-message-session-t.txt` · `commit-message-session-s.txt` ·
`walk-artifact-session-t2.html`
