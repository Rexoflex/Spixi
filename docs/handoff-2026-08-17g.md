# Handoff — 2026-08-17g (post-F5, rewritten in place). READ FIRST.

**LANGUAGE RULE: ASD-STE100 Simplified Technical English.** See `CLAUDE.md`.

Supersedes `docs/archive/handoff-2026-08-17f.md`. Rewritten IN PLACE after the
#377 F5 verdict (never consumed, so it keeps the letter).
★ Planning doc: `docs/master-worklist-2026-08-17.md` (N51–N59 + N36b struck ·
N60/N61 added · N57 re-observed). Open tally: **~29 N-items** (5 fix-first +
N60/N61 · 16 round items · 8 dials) + the §B carryovers (D-9 · D1+N6 · I-1 ·
I-9 · I-10 · I-12) + D-17b.

---

## 1. State — verify, do not trust

```
git --no-optional-locks log --oneline -3
git --no-optional-locks status --porcelain
```

Damir commits the #375–#377 batch after the F5 (title + body in
`docs/archive/f5-checklist-2026-08-17-fix2.md`, amended for N59b/N60/N61) —
that commit should be at HEAD when you read this; VERIFY. Parent = `af10a004`.

| Item | Value |
|---|---|
| Batch #375/#376 | The whole 17f §2 fix list. **F5 #377: ALL LEGS PASS** (Android back grammar · N36b — the one-liner was the whole layer · reading set · toasts/pin/cache/gap) |
| N59b | Same-day dial refinement (Damir screenshot): the disc centres against the title+sub GROUP — absolute disc, stack gutter, pull-up dropped. CSS-only, pins rebased |
| ★ N57 | **RE-OBSERVED post-F5 in normal use** — reproduces readily. Code pre-read done (`docs/n57-triage-group-visibility.md`): FE + C# push path exonerated; 3 Core-side mechanisms with log fingerprints. **The 4-leg protocol + dev-log capture = Damir's TOP action** |
| N60/N61 | Logged at deploy time (§2b). N61 verified INHERITED (backup never archived `Chats/`) |
| Smoke | **BASELINE OK 1903 / the 4 KNOWN pre-existers (#136 · #149③ · M5 · B3)** · lint ✓ · pseudo 9/9 · locales ALL CLEAN · NUL clean |
| Loop | #46 on Opus (#376): 3 MAJOR found+fixed PRE-F5 (back wedge · badge miscount · false toast) · reviewer PASS |
| A52 | No lag reported at the 50-row window → re-measure dormant unless it nags |

## 2. Damir — the short list

1. **★ Run the N57 protocol** (`docs/n57-triage-group-visibility.md` §2 —
   3 accounts, ~20 min, dev-mode log on the receiver). It re-observed today;
   the fingerprint lines decide Core-vs-transport and finish the be-cutover
   row. Everything points at Core — the log capture is the proof the BE
   engineer needs.
2. **Send the BE engineer** be-cutover `[Q1-ESC]` + `[NT1]`, and the N57
   verdict row once the protocol ran.
3. **N60 observation on the NEXT Android deploy:** update over the existing
   install WITHOUT uninstalling; watch for `INSTALL_FAILED_UPDATE_INCOMPATIBLE`
   in the output. One configuration per test device until then.
4. **Dials still open (log answers, no build until given):** N56 wash values
   (9% light / 6% dark shipped — eyeball on device) · N61 backup scope
   (include messages opt-in? size cap? Downloads/avatars?) · contact_new
   post-pop feedback (C# push / storage handshake / accept the pop) ·
   blind-bubble avatar photos (#372) · N48 chip on non-blind heros (#370) ·
   ru/lt legacy dashes (#371) · N3 voice-sweep target list · the A2 fallback.

## 2b. N60 + N61 (logged 2026-08-17 at deploy time)

- **N60** — the Android update-over-existing WIPED the account (restore
  forced). Suspect: APK signature mismatch → uninstall+reinstall. Protocol +
  keystore mitigation in the worklist row. Not app code until the deploy
  output proves it.
- **N61** — after restore, chats show excerpts but open EMPTY (bot rooms
  refill from the server). Verified INHERITED at source: `BackupPage`
  archives `Acc/` + account/avatar/wallet and never touches `Chats/`
  (byte-identical at fork `0e85a4b8`); history lives at
  `Chats/<addr>/<channel>/*.ixi` (Core LocalStorage:546). Fix = C# here
  (recursive `Chats/` in backupAccount + restore carry-through; LaunchRestore
  extracts generically at :207). Rides the R4 restore round beside N12/N13.

## 3. NEXT SESSION — N4 locale expansion (its own session)

The last un-started R2 item and the only (L) left: audit existing
translations, pick the launch 15–20 languages, add the FE dictionaries
(⚠ it/id/lt/cn/ja have C# strings but NO FE dictionary — the #360 residual;
dictionary + the Utils.cs culture gate move TOGETHER), per-language
button-overflow audit. `docs/next-session-prompt.md` is the paste-ready
prompt. After N4: the §C fix-first block (N13 top — data-loss class · N40 ·
N10 · N15 · N39 · N61 rides R4) then the rounds (R3 → R4 → R7 → R6 → R8 →
R5 → R9, per §F).

## 4. How to work — unchanged

Cloud twin recipe: clone → checkout the batch commit → `npm install jsdom
--no-save` → bundle BEFORE shells → smoke green at **1903/4** (state the
number when it moves). Standing set: verify plans against code (#215) · #294 ·
mutation-honest pins, PROVEN · Damir commits · DECISIONS rows at decision
time · the #46 loop on Opus (#352) · the security handover gate while
building (the #375 gate section is the template) · tarball delivery (plain
`tar xzf` in PowerShell; the mount cannot unlink; `git --no-optional-locks`
always). Ixian-Core reference: `097341a`. Deploy: wipe obj/bin → Android
`dotnet build Spixi\Spixi.csproj -f net10.0-android -c Release -t:Run` ·
Windows two-step: `dotnet build ... -f net10.0-windows10.0.19041.0 -c Debug`
then run the exe (⚠ `-t:Run` does NOT chain the build on Windows).

## 5. THE DEFERRED PILE — one pass, later

N13 (🔴 top, data-loss) · D-17b (dial locked) · N40 · N10 · N46 (★ remind near
completion) · N47 · legs 29–30 (untestable) · the #368/#367/#372/#373
log-onlys. From #376, fold into related rounds: the retained chat-info
takeover = a third off-stack back surface (mirror it if revived) · the ~400ms
stale-TRUE close window (inherited, one wasted press) · a >5s re-flush +
>250ms stall can still miscount the badge (fails loud) · one false chat-sheet
toast on a mid-session relation change (narrow).

## 6. Do not re-attempt — 17f §6 stands verbatim, plus:

- No optimistic toast on a surface whose C# can alert-and-stay without
  sending (#376 B-2) — feedback must come from a state the shell can SEE.
- `bursting` alone is NEVER a complete re-flush gate on paths without
  `onChatScreenLoaded` (#376 B-1) — pair it with the quiet window.
- A select-mode handle is live ONLY while `box.dataset.selecting` exists
  (#376 A-1) — never trust the variable alone.
- The N56 wash is a THIRD state-tinted row family: any new press/hover rule
  on chat rows needs its `[data-pinned]` arm.
- No FE attempt at group message delivery/visibility (N57) — the layer is
  Core; wait for the protocol verdict (#374 §6 rule extended).
