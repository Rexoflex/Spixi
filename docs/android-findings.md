# Android findings — first device run (2026-08-11, Samsung R5CRB1MWPPZ, Debug via Mac)

First-ever Android run of the redesign. Same rules as the iOS punch list
(ios-sim-findings.md): platform bugs only, fix small, log in DECISIONS.

| # | Finding | Root cause / read | Status |
|---|---------|-------------------|--------|
| AND-1 | **CRASH + wedged onboarding on fresh install**: fatal NRE dialog over the intro carousel, re-thrown 1 Hz (TransactionInclusion.onUpdate), language taps blocked | **ROOT-CAUSED from logcat (#329):** Android fires `RequestedThemeChanged` during boot → App.xaml.cs:127 `reloadAllPages()` → `HomePage.Instance()` — which CONSTRUCTS the singleton → its ctor `Node.start()` on a WALLET-LESS install → `IxianHandler.getWalletStorage` NRE (PresenceList.init) + the 1 Hz re-throw. Latent on EVERY platform (any pre-login `Instance()` touch); Android's boot-time theme event was simply the first trigger to fire. | **FIXED (#329)** — `HomePage.InstanceOrNull()` (read-only, never constructs) + all 7 read-only sites switched (reloadAllPages · Node UI tick · Node.shutdown · Android push-intent · iOS push-tap · 2× Utils detail lookups); `Instance()` construction remains ONLY for the App-start wallet-present path. Device re-run owed |
| AND-2 | Slow past splash (30s+) + language-change hang/ANR — SURVIVED the AND-1 fix | **ROOT-CAUSED (#330): the Android-only `localizeHtml(Stream)` overload built the page via `lines += line` — QUADRATIC over a ~22,000-line / ~1.3MB shell (gigabytes of copying per page load, on the UI thread); the language pick re-runs generatePage → same loop again. Legacy pages were small enough to hide it; the file overload (all other platforms) always streamed | **FIXED (#330)** — StringBuilder (linear, byte-identical output). Device re-run owed |
| AND-3 | Edge-swipe back (#325) vs Android system back gesture — who owns the left edge? | Gesture-nav Android reserves screen edges for system back; the in-chat shell swipe may never see those touches (system wins) — that is acceptable (system back routes through OnBackButtonPressed) but must be VERIFIED | OPEN — observe on device |
| AND-4 | Full Android walk owed once AND-1 verifies: language pick → account create → contact → chat → keyboard (native resize, no double-lift) → send-log share (R5 leg) → header physics (#327) → sl-si dates everywhere (iOS-55/#328 surfaces) | The S6 sweep leg, now unblocked on real hardware | OPEN — the walk |
| AND-5 | Onboarding "Back up now" chosen → flow just continues past it (no backup surface) | **= the known L3 launch row, now device-confirmed:** the onboarding tail has NO backup hand-off — no `ixian:backup` route on OnboardPage, so the tail completes regardless of the choice. Cross-platform (iOS has the same gap, untested there); already in the cheap-C# basket (ios-launch-gaps C: L1/L2/L5/L7 family — promote L3 into round 1) | OPEN — cheap-C# round 1 |
| AND-6 | Status bar renders a wrong BLUEISH color | Android chrome theming — almost certainly MAUI's default Material colorPrimary showing through (the Android twin of iOS-1/4: native chrome not following the app theme). Fix family: Android styles/colors + status-bar color follow the app theme incl. dark mode | OPEN — Android chrome batch |
| AND-7 | **BUILT #401 (2026-08-18)** — root-view TOP padding removed; the inset travels to the shells as the `*SL{AndroidInsetTop}` carrier + `window.setInsetTop` and is consumed through `--safe-top` in `base.css`. Bottom deliberately untouched (it carries the IME inset). ⚠ **RESIDUAL:** the carrier is baked at `generatePage` time and the activity handles `Orientation`/`ScreenSize` itself, so a rotation while a page is ON SCREEN leaves that document padding by the old value until the next page-chrome pass. Mostly invisible (the status-bar height rarely changes), visible on a CUTOUT device rotated to landscape. A live push from the insets listener needs a page enumerator — the #284 "every reload-them-all helper has missed a collection" class — so it was NOT written blind. | RESIDUAL |
| AND-7 (orig) | Welcome/onboarding screens not full-bleed (strips at system bars) | The #282 edge-to-edge class on Android: MainActivity has an InsetsListener but the launch surfaces don't bleed under the system bars. Same fix family as AND-6 — one Android-chrome round: edge-to-edge + themed bars + safe-insets, mirroring what #282 did on iOS | OPEN — Android chrome batch |
| AND-8 | After performing a backup, the Backup page still shows "not backed up yet" | The #283/#314 interim stamp (`spixi.backup.last`, localStorage, written on share LAUNCH) isn't being read back. Two suspects, in order: **(a) the C-9 storage class on ANDROID** — Android's generatePage loads shells via `HtmlWebViewSource` + assets BaseUrl (NOT the file:// path the other platforms use), and localStorage persistence for that origin is UNPROVEN on Android WebView — if dead, this is way bigger than backup (drafts · pins · mentions-seen · chat prefs · scan grant all dead on Android); (b) the stamp-write path simply not firing on the Android share leg. QUICK SELF-TEST that separates them: type a draft in a chat, kill the app, reopen — draft gone = storage dead app-wide (escalate to an Android-storage row, C# preference push per the #308 posture, "log it big, don't hack it small"); draft survives = (b), a small share-leg fix | OPEN — self-test decides the class |
| AND-9 | **localStorage DEAD on Android** — one root behind FOUR device symptoms: rating prompt on EVERY chat exit (snooze never persists) · drafts reset on chat exit · Backup page "not backed up yet" + Account badge "action needed" after a backup (stamp unreadable) · Account exit lands on the STALE tab, not the tapped one (the #314 landtab fix reads storage — the iOS bug's Android twin). AND-8's self-test answered | **ROOT-CAUSED + FIXED (#331):** `WebViewRenderer.cs:367` explicitly set `DomStorageEnabled = false` (legacy never used localStorage; Android WebView also defaults it off — every other platform ships it on). → `true`; DatabaseEnabled (WebSQL) stays off. Expected also healed: pins · mentions-seen · chat prefs/textscale · scan grant · media-loaded · settings stash. Device retest owed: rating snooze holds · draft survives · backup badge clears · Account exit lands on the TAPPED tab | ✅ built · 🟡 retest |
| AND-10 | Chat entry less snappy than iOS; chat details flickers + slower | Perf watch — re-judge AFTER AND-9 (storage churn removed) and on a RELEASE build (the iOS-32 rule: never judge perf on Debug). Also verify the #177/#225 warm-page machinery actually runs on Android (generatePage re-localizes ~1.8MB per cold entry via HtmlWebViewSource — iOS keeps warm WebViews) | WATCH — measure post-AND-9, Release |
| AND-11 | Answering the FIRST call → mic permission prompt; granting effectively DECLINES the call | Runtime RECORD_AUDIO requested at answer time; the permission flow eats the answer. Fix shape: pre-request at incoming-call UI (ring time) — or at first app-run like messenger peers — and/or re-drive the pending answer after grant instead of dropping | OPEN — call-flow round |
| AND-12 | Splash screen background BLACK (iOS shows the blue) | MAUI splash color/theme divergence on Android | OPEN — chrome round (with AND-6/7) |
| AND-13 | Notification-tray icon = bare Spixi mark, no background circle — reads broken next to other apps' notifications | Android notification SMALL ICON needs a proper white-silhouette asset + accent color (Material notification spec); currently falling back to the raw app icon | OPEN — chrome round |
| AND-14 | System-default dialogs (gray Material, pink OK) — off-brand vs the in-app dialog surface | MAUI DisplayAlert renders the platform default theme; fix = theme the MaterialAlertDialog palette (colors.xml/theme) or route alerts through the in-app dialog surface | OPEN — chrome round |
| AND-15 | Notifications CONTENT (x-plat, Damir: "we should discuss"): payment received shows "new message"; incoming CALL notification reads like a message → missed calls | Per-type notification composition: calls want ring-priority presentation (Android CallStyle/heads-up + caller name; iOS equivalent), payments their own copy. Composition is push-payload/BE-adjacent — needs the #232 privacy posture check (what may the payload disclose?) → DISCUSSION row before build, then its own batch | OPEN — Damir + BE discussion |
| AND-3 | ~~edge-swipe vs system back~~ | **VERIFIED (Damir 2026-08-11): BOTH work** — the system back arrow and the in-chat shell swipe coexist on this device. Best outcome; no action | ✅ CLOSED |
| AND-16 | Android keyboard: composer lifts correctly but OVERLAYS the last messages — the log never re-pins | **ROOT (#332):** Android's adjustResize path shrinks the layout viewport; neither #303/#324 inset writer fires (correct — delta≈0) so nothing re-pinned the log after the shrink. **FIXED:** window-resize listener re-pins to bottom while near it (chat.html, keyboardViewport IIFE); iOS structurally inert (constant innerHeight under keyboard, #294 measurement); desktop unreachable (IIFE guard) | ✅ built · 🟡 retest |
| AND-17 | Spixi-bot GROUP chat: sending contact requests to members "mostly didn't work" — only 1 of several actually sent | Unreproduced/unlogged — needs a repro WITH logcat attached (`adb logcat -c` → attempt several requests → dump). Suspects to check at repro: member-sheet request verb per-tap wiring vs a one-shot latch (#268 request-flow states) · C#-side request throttle · bot relay behavior (IXICore, #215-unverifiable in-tree) | OPEN — repro + logcat next round |
| AND-18 | Entering Account leaves the PREVIOUS tab slightly highlighted ("hover-ish") on the home nav | Sticky hover/focus after touch — Android WebView keeps :hover/:focus styling applied post-tap (iOS taps don't focus buttons). Fix shape: blur-after-tap in the bottomnav handler, or audit the nav styles to :focus-visible-only (the #43 hover-guard sweep's Android twin) | OPEN — small FE |
| AND-19 | Going to/from Account always FLICKERS (Android; iOS re-presents warm + clean) | Overlay present/close flash on Android — suspects: WebView default background (white flash class, the #229b family on a different engine) · the #315 park machinery's hide/show ordering on Android · cold first-present. Needs eyes-on with the Inspector equivalent (chrome://inspect) in its own round | OPEN — Android polish round |
| AND-20 | Keyboard open/close is CLUNKY + flickers (even when functional) | Android applies the whole IME inset in ONE jump (MainActivity InsetsListener pads the root view; no WindowInsetsAnimation sync) → the WebView relayouts a 22k-line document in one frame. Design direction, the iOS #303 route: STOP padding for IME natively; push the inset into the page (an insets-animation callback → `__setKbInset`-style per-frame or settled push) and let the composer's 280ms ride (#324) do the motion — viewport stays stable, no full-document relayout | OPEN — Android keyboard v2 round |
| AND-16b | Retest r1 FAILED ("still covers messages") — but the build was INCREMENTAL | Suspect: the iOS stale-Raw-html law's ANDROID TWIN — assets not repacked on incremental build. Retest r2 after a full obj/bin wipe decides: still-covers = mechanism wrong (then probe innerHeight through the transition, the #324 kb-probe Android edition); works = the wipe law extends to Android device builds (write it into the build recipe) | **FALSIFIED — full obj/bin wipe did NOT change the outcome (still covers)** → the resize-re-pin mechanism is wrong for Android's IME path, REVERTED per the #294 discipline (chat.html back to the #328-verified bytes). Next attempt lives in the AND-20 keyboard-v2 round and starts with a MEASUREMENT (the kb-probe Android edition: innerHeight/vv/insets sampled through the IME transition via chrome://inspect), never another guess |
| AND-21 | Sending a file with a locked Spixi → unlock prompt on RETURNING from the file picker (Damir 2026-08-11) | The app's OWN picker intent pauses the app → the resume lock armed on the way back. | **FIXED (#334)** — `App.noteOwnIntentRoundTrip()` one-shot, 10-min-bounded, consumed on EVERY resume (a stranded stamp can never suppress a later real background); set at both SFilePicker pickers + SFileOperations share/save. Retest: send a file locked — no unlock prompt on return; background the app >10 min inside the picker — still locks |
| AND-22 | Scan camera defaults to a fisheye-ish lens; trouble adding contacts by QR (Damir 2026-08-11) | The #263 class on Android: multi-lens Samsungs expose ultra-wide/tele/macro; bare `facingMode:'environment'` can rank the ultra-wide first → distorted preview + poor decode | **FIXED (#334)** — scan-page.js Android branch enumerates and prefers the MAIN back lens (back-facing only → named non-main lenses dropped → lowest camera index); every failure steps down to the old behavior; iOS untouched (#313-verified). Retest: scan preview undistorted + contact QR decodes |
| AND-23 | Chats-list search bar + chips STICK to the top on Android; on iOS they scroll away with the list (Damir 2026-08-11) | The #327 header pins sticky while "searching" (focus-within ∥ non-empty query); Android WebView keeps DOM focus in the tapped field through scrolls/outside taps (the AND-18 stickiness class) → the pin latched forever | **FIXED (#334)** — a touch-scroll with an EMPTY query blurs the field (standard mobile dismiss); a live query keeps the pin (deliberate #327). Retest: tap search, type nothing, scroll → header rides away; type a query → header stays pinned |

## #334 status sync (2026-08-11 overnight build — device retest = the 2026-08-12 checklist)

AND-5 ✅ built (L3 onboarding `ixian:backup` + stamp) · AND-6 ✅ built (surface-color bars + theme-driven icons + OS-flip re-run) · AND-7 ⏳ NOT built — same insets listener as AND-20, one architecture round AFTER the probe numbers · AND-11 ✅ built (accept gate + ring-time pre-request) · AND-12 ✅ built (v31 splash bg) · AND-13 ✅ built (silhouette ×5 + accent; audit note: statusicon was already a silhouette — the gaps were accent/densities/LargeIcon) · AND-14 ✅ built (accent + dialog theme; OS-vs-app theme residual documented in styles.xml) · AND-15 ✅ app-side built (per-type copy + call channel; payload typing for process-dead pushes = BE row) · AND-16/20 ⏳ PROBE SHIPPED (on-screen, chat.html, temporary) — measure FIRST, lever after · AND-17 ✅ root-cause fixed in code (missing `requestAddSent` marker + silent addFriend-null; Damir's "peers accepting invisible requests" fit exactly) — the logcat repro now VERIFIES the fix instead of hunting · AND-18 ✅ belts built (blur-after-tap + pointer:fine guards ×79+3) — the chrome://inspect probe (now possible: debug flag landed) can still classify the original residue · AND-19 ✅ mask built (transparent native WebView over the themed ViewGroup, miniapp-exempt) — eyes-on retest decides if the Opacity-staging half is needed.

## Device F5 round 2026-08-12 (Damir, Samsung) — verdicts + new findings (#336)

**AND-16 v2 FIXED (measured):** the kb-probe proved Android adjustResize shrinks innerHeight (774→451, vv tracks 1:1, delta=0) → the composer is already placed right; the ONLY defect was the log not re-pinning (st 5063/5386). ★ **The AND-20 "stop native IME padding / push inset into page" plan is REFUTED by the measurement — not needed, not built.** Fix = settle-tracked stickDuring on the innerHeight shrink, near-bottom judged pre-shrink (the #333 one-shot failed because nearBottom recomputes false against the shrunken clientHeight). Probe RETIRED. AND-20's "clunk/flicker" residue = the reflow feel only, judge on a RELEASE build (iOS-32 rule) — separate.

**PASS on device:** AND-6 (bars) partial — wallet TOP not full-bleed (AND-7, below) · AND-12 splash (wants logo animation, below) · AND-13 notif icon · AND-15 call notif · AND-18 tab highlight · AND-22 camera (implied) · AND-23 sticky header (GREAT) · AND-17 member-sheet request · file-send Cancel · iOS-58 live re-localize (GREAT) · iOS-60 swatch tiles (GREAT).

**NEW / OPEN:**
| # | Finding | Status |
|---|---------|--------|
| AND-7 | **Wallet (home) NOT full-bleed at the TOP — strip under the status bar.** Now UNBLOCKED (AND-20 refuted). Root: MainActivity InsetsListener pads the root view top by sysInsets.Top; SetDecorFitsSystemWindows(false) already draws edge-to-edge, so the fix = stop padding the top, push the top inset to the shells as a CSS var (Android env() is cutout-only), consumed by the existing iOS-#282 topbar/hero env() chrome. Needs its own careful round + device verify (which surfaces bleed). | OPEN — next Android round |
| AND-24 | **Native DisplayAlert dialogs read ugly (navy-on-dark now, was pink) — Damir: circumvent the OS dialog?** displaySpixiAlert → alertHost.DisplayAlert (~90 sites). The SHARE SHEET (backup save/share) is unavoidable OS. The DisplayAlerts CAN be routed to the in-app Spixi dialog surface (c-modal / settingsConfirm) — but that's an architectural change (a C#→WebView dialog bridge for a top-level alert host); DESIGN DECISION, big. #336 improved the invalid-address COPY (below) but not the dialog chrome. | OPEN — design decision |
| AND-25 | **Remove-contact → FATAL exception (native crash dialog).** Reproducible: removing a contact throws. Separate from AND-24 (this is a real crash, not just chrome). Needs logcat at repro. | OPEN — needs logcat |
| AND-26 | **Account↔Wallet switch flickers + is slow; picker round-trip flickers (Damir: "do it proper like iOS").** = AND-19 residual + the #315 park/re-present cost on Android. The AND-19 mask (transparent WebView) helps the white flash but not the perceived slowness/flick of the Account overlay present. The Opacity-staging half (stage at 0.01 so the WebView composites before the flip) is the next lever — device-verify-gated. | OPEN — AND-19 round 2 |
| AND-27 | **Mic denied → re-asked natively → NO call screen appeared** (#12). The denied-mic path re-arms the ring re-render (#335) but Damir saw no CallPage surface at all. Needs a clean repro (grant state + logcat) — possibly the CallPage native surface on Android or the ring not presenting. | OPEN — needs repro |
| AND-28 | **No avatars render at all on Android** (#14 — blocked the avatar-viewer test). X1 imageToDataUri is cross-platform; suspect either the test account has no stored avatar files (the known X1 "gradient both = no file" non-bug) or an Android data-URI/path issue. Re-check on redeploy. | OPEN — re-verify |
| #15 | **Settings sublevel back snapped hub to TOP** (About/How-to/Chat appearance/Contributors/Delete data — takeovers that detach the hub node; Theme/Language are sheets that held). | **FIXED (#336)** — settings.html saves hub scrollTop on the way into a takeover, restores on return |
| #10-copy | Invalid-address dialog title read "Invalid Address" for a valid own/known address. | **FIXED (#336)** — friendlier per-case titles (contact-self-title / contact-exists-title) ×8 locales + generic "That address didn't work" |
| iOS-64 | Draft excerpt ~2s latency — Damir: "too long but ok". | ACCEPTED dial (the shouldRefreshContacts → next tick cadence; O(1) push nudge = the harder alt) |

## #336 round-2 (later 2026-08-12) — more F5 + fixes

| # | Finding | Status |
|---|---------|--------|
| AND-29 | **OS hardware back EXITS the app instead of going back, when a home-shell takeover is open** (contacts list, FAB new-chat, viewing contacts, wallet Receive/Send). Root: those are WebView-internal covers (z-30, mounted in home.html), NOT native overlays — so HomePage.OnBackButtonPressed fell through closeTopOverlay → base → pop the root → EXIT. | **FIXED (#336)** — home.html pushes `ixian:homeoverlay:1\|0` on takeover open/close; HomePage tracks it + routes hardware back via `sendUiCommand(homeBack)` → the shell closes the top takeover. Device-verify. ~~NOTE (AND-29b follow-up): … not done~~ **AND-29b DONE in the v2 round below** (overlay.js sheets covered via the `body[data-overlay-open]` mirror). **#337 audit hardened the whole scheme:** deferred/coalesced state push (group-create verb was clobber-able — a `location.href` state push in the same task as a verb nav coalesces last-wins), `onLoaded` state reset (a home-shell reload stranded a stale `true` → back swallowed app-wide), native-overlay-first back order (details-over-directory: first back invisibly closed the directory underneath), homeBack stale-state self-heal. |
| Splash-anim | Damir asked for a premium splash logo animation. | **BUILT (#336, device-verify)** — Android-12 `windowSplashScreenAnimatedIcon`: the white Spixi mark scale-settles in with an overshoot, then the two "eyes" pop a beat later (sequenced reveal — a stroke draw-on doesn't suit the filled mark). Files: `drawable/spixi_splash_logo.xml` (VectorDrawable) + `spixi_splash_anim.xml` (AnimatedVectorDrawable) + values-v31 wiring. ⚠ Can't be visually verified in-session — if the mark clips the splash circle or reads off, tune (wrap in a ~0.85 inset group / add windowSplashScreenIconBackgroundColor) or REVERT by deleting the two windowSplashScreen*AnimatedIcon lines in values-v31/styles.xml (→ the #334 static blue splash). |
| AND-28 | **Avatars: EXISTING contacts show no avatar; NEW contacts load fine + full-size viewer works ("good work").** So X1 data-URI + the #336 viewer are correct; the gap is existing-contact avatar files not reaching the push (chats-list/topbar). Suspect: getAvatarPath for pre-existing contacts returns a path that isn't wrapped/readable on Android, or the avatar file was never stored for them. Needs a C# check of the existing-vs-new avatar path (HomePage addChat / getFriendMessageHelper avatar wrap vs where new-contact avatars come from). | OPEN — investigate C# avatar path (existing vs new) |
| AND-20 | **Keyboard/composer swing is CORRECT in movement but not SMOOTH — Damir wants the iOS glide.** The AND-16 v2 re-pin fixed the covered-messages defect; the residual is the adjustResize ONE-JUMP (no WindowInsetsAnimation sync) — the composer snaps rather than eases. The smooth path = the AND-20 keyboard-v2 lever proper: drive the inset per-frame via `WindowInsetsAnimationCompat` (or switch soft-input to adjustNothing + a CSS-transitioned `--kb-inset` push, the "inset into the page" approach — now justified for SMOOTHNESS even though the measurement refuted it for correctness). Needs device iteration + measurement of the animation frames. | OPEN — AND-20 keyboard-v2 (smoothness) round |

## #336 round-3 (F5 r2 close-out 2026-08-12)

| # | Finding | Status |
|---|---------|--------|
| AND-29 v2 | Wallet **Receive** back still exited (its shell takeover open never pushed state — only contacts did; v1 **Send** is a native page, so it was fine) + "**some sheets also exit**" (overlay.js sheets/menus were untracked). | **FIXED (#336)** — wallet takeover opens now push `ixian:homeoverlay`; state also mirrors `body[data-overlay-open]` via a MutationObserver; `homeBack` closes the top SHEET (dismissTopOverlay) before a takeover. Device-verify Receive + a row-menu/create-group sheet. |
| Splash-anim | Damir F5: barely noticeable, mark too big + clipped by the splash circle, dots too fast — "far from premium, revert to basic". | **REVERTED (#336)** — the two windowSplashScreen*AnimatedIcon lines removed → the clean #334 static blue splash. `drawable/spixi_splash_{logo,anim}.xml` kept for a future round (needs proper icon artwork sized to the Android-12 66% safe circle + one slower motion). |
| AND-30 | **Member sheet (from a group) offers "Add contact" even when the person is ALREADY a contact** — should show basic actions (Send message / View profile) instead (the #14 "bonkers" note; the desktop demo already handles this). Root: the member sheet has no relation datum (chat shell: "relation defaults 'none' — no contact roster"). | OPEN — next session. Needs a relation flag (already-contact vs not) threaded to the member sheet (roster check or a small C# relation push) → swap the request action for Message/View-profile when known. |

## Android pass 2026-08-16 (#349) — the owed #348/#348b device run, Release PERF, landscape findings

**Verdicts:** A9 landscape Account ✅ · A7 FAB residue + "New chat" title ✅ (5 fast taps clean) ·
A5/I-8 press fill on touch ✅ (hold = smooth centre-out fill; tx row identical) · reduced motion ✅
(instant flat tint; needs an app restart to take effect — accepted) · OS high-contrast /
highlight-buttons settings show no visible change in Spixi — no action, a11y is by design.
**Open leg:** flick-scroll cancel (not enough chats at test time; the account now has them).
**The press spec grew during the pass** — no retract on activation, fill always completes on a
tap — that is the D-16 build, not an AND row. See DECISIONS #349.

**Release `PERF` numbers (A52, Release, 2026-08-16) — the owed measurement:**
chat open tap→loaded = **234 ms cold / 212 ms warm / 229 ms** (generatePage 55–89 ms ·
handover+parse ~120 ms · push 19–25 msgs 9–36 ms). Chat info generatePage = 78 ms.
**Verdict (#294): no segment dominates; no further chat-entry work is justified.
The `PerfTrace` deletion is UNBLOCKED** (it must be gone before any release).

| # | Finding | Status |
|---|---------|--------|
| AND-31 | **Landscape wallet: only the small visible strip of the tx LIST scrolls** — the hero eats the height, so the grab area is tiny. Same class to check on all main screens in landscape | OPEN — landscape round |
| AND-32 | **Landscape apps: the Explore banner stays STICKY while the empty state scrolls** under it | OPEN — landscape round |
| AND-33 | **Landscape tx details: the surface is cut off and does not scroll.** Half of it falls to the logged R6 tx-detail rework; the cut-off itself is its own defect | OPEN — landscape round (with R6) |
| AND-34 | **Chat appearance: the text-size preview squeezes in landscape.** Check every view in landscape | OPEN — landscape round |
| AND-35 | ★ Damir dial (x-plat FE, copy + order): in Chat appearance, move the TEXT SIZE control ABOVE the pattern control · rename "Pattern style" → **"Background"** · rename "Background pattern" → **"Opacity"**. Strings = one locale round | OPEN — ride a locale round |
| AND-36 | **Rotating landscape → portrait leaves the last-active chat row HIGHLIGHTED** on the chats list, persistently. The selected-row state never clears on mobile after rotation | OPEN — FE, home shell |
| AND-37 | **OS back over an open Account bottom sheet (language, appearance, theme) lands on CHATS** instead of closing the sheet. The settings shell has no sheet-state push, so native back pops the whole page — the AND-29b class, unapplied to settings.html | OPEN — FE + small C#, AND-29 family |
| AND-38 | **Tapping the wallet balance (hide/show toggle) flashed a pressed state across the row** (found during the #351 F5, 2026-08-16). Damir's dial: NO pressed state on the toggle — the value just swaps. Verified cause: the NATIVE Android tap highlight over the click-bearing `.c-wallet-hero__balance` block — pressable.js is not involved (no press family matches the hero ancestry, `home.html:290`). Fix #355: `-webkit-tap-highlight-color: transparent` on all three toggle targets, and the eye's `:active` wash moved inside the desktop hover gate | BUILT (#355) — verify on the A52, checklist §3 |
| AND-39 | **The tap fill effect feels abrupt** (Damir, A52, during the #354 F5). Not yet characterized — clarify WHICH phase is abrupt (fill start / completion / fade), then get a screen recording, then measure (#294). The mechanism is the #351 press fill, already committed — the #354/#355 batch does not touch it. The same session's flick-cancel check FAILED but was under-powered: the chats list was too short to scroll, so the flick may never have become a scroll (no takeover → no touchcancel → the press commits by design). Re-run both with a populated list before any code moves | OPEN — clarify + repro first |
