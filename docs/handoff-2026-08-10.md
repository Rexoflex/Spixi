# Handoff — 2026-08-10 (F5-pass close, #314–#323) → THE FULL SWEEP

**Read this first.** Supersedes `docs/handoff-2026-08-07.md`, `…-cowork.md` and
`…-07b.md` (all consumed → `docs/archive/`). `docs/f5-checklist-2026-08-07-plate.md`
= consumed, kept for the record.

## State at close (all committed as ONE batch #314–#323 by Damir)

- **The plate is DONE and device-verified**: #314 (9 polish items) + #315 (Account
  peer tab, park/re-present) + F5-day rounds #320 (warm-Account visuals) ·
  #321 (R5 dev parity: send-log + live HUD) · #322 (body-level selectability ·
  tri-tab Connecting · hero/About i18n). #46 loop ran 2 adversarial rounds on the
  plate (4 MAJOR catches, all fixed + mutation-pinned).
- **Baseline: smoke 1006 pass / the SAME pre-existing 4** (#136 · #149③ · M5 ·
  B3-shell) · bundle 777 KB / 238 exports · 22 shells.
- Dials on record: #316 Figma mirroring RETIRED · #317 R3 = all platforms ·
  #318 R4 = ack-gated stamp · R6 shipped in-plate · iOS-44 UN-DEFERRED (below).
- ⚠ Build law, re-proven on device this pass: **obj/bin wipe before EVERY device
  build; plain build FIRST, then -t:Run; grep the .app bundle before deploying**
  (incremental builds silently ship stale Raw html).

## THE SWEEP (next session, in this order)

1. **S1 — iOS-53 composer-focus topbar dip** (topbar vanishes → reappears
   mid-screen → travels up). The iOS-6/29/#294/#303 keyboard family. ⚠ #294 LAW:
   measure FIRST (Safari Inspector on the cable: sample topbar
   getBoundingClientRect + visualViewport.height/offsetTop + scrollY through the
   focus transition), then fix with a NEW lever only. Suspects in the row
   (ios-sim-findings iOS-53).
2. **S2 — iOS-44, un-deferred**: the composer ⊕ attach sheet presents UNDER the
   composer (composer pinned + usable above it), never a full-screen overlay
   (mobile). Overlay-stack rework in the chat shell; same WebView, ★ #221
   untouched. Damir screenshot 2026-08-10 on record.
3. **S3 — Batch B** per `docs/fable-build-brief-parity-b.md` (now UNBLOCKED):
   B1 R3 media-cap flip ALL platforms · B2 R4 ack-gated stamp (small-C#) +
   one-time burned-stamp clear · B3 R7 share verb under the #301/#303
   bare-address contract. Same files as the plate — plate is committed, safe.
4. **S4 — iOS-54 subscreen Connecting** (small C#): fan HomePage.showWarning out
   to live overlay WebViews; chat topbar sub-line is the natural slot.
5. **S5 — i18n residue**: `node scripts/build-locales.mjs --todo` → clear the
   remaining english-fallback keys per locale (sl-si first, then W1 es/ru).
6. **S6 — R5 legs**: Android send-log share verify · Windows save leg rides the
   **A10 WINDOWS pass** (§A10 rebased in parity-a-f5-checklist.md) · Windows
   keyboard sanity · desktop scan re-verify.
7. **S7 — probe retirement**: scan storage line (#308) + `[cam-perm]` console
   noise (#310–#312) + the `frame black` dark-scene false positive — retire/gate;
   **KEEP the per-nav UIDelegate heal** (#312, load-bearing).
8. **S8 — standing residue** (pick up what fits): the 4 smoke pre-existers (own
   session) · B1/B2/B3 device-runsheet rows (MORE suspect post-#312) · iOS-41
   select-text dial · iOS-43 Launch clip · iOS-18 multiuser picker · iOS-31 (sim
   first) · iOS-32 heat (measure on Release) · #315 boot-preload dial (rejected
   once, revisit only with a staging-slot design) · parked-WebView jetsam probe.

**Damir-side inputs still open:** real Terms/Privacy documents (iOS-42/45) · icon
exports (dev-entry glyph replacing 'adjustments-alt' · `cursor-text` if iOS-41
goes ahead) · OG link previews want a be-cutover §82 priority call with the BE
engineer (recipient-side unfurl stays BANNED — IP leak, #232).

## Rules (unchanged)

Zero-C# unless evidence forces it (#215) · ★ #221 chat isolation · bundle BEFORE
shells · smoke baseline **1006 / the same 4** · #46 loop after every batch ·
DECISIONS #324+ (#302 retired) · `git --no-optional-locks`; commits = Damir in
GitHub Desktop (#306) · obj/bin wipe + bundle grep before device builds ·
device-bridge sessions: `tar --overwrite` (the mount forbids unlink), stage-cache
can serve stale copies — verify with device_bash hashes.

## Build recipe (device round)

```
rm -rf Spixi/obj/Debug/net10.0-ios Spixi/bin/Debug/net10.0-ios
dotnet build Spixi/Spixi.csproj -f net10.0-ios -c Debug -p:RuntimeIdentifier=ios-arm64 -p:CodesignKey="Apple Development" -p:ValidateXcodeVersion=false
grep -c "__setKbInset" Spixi/bin/Debug/net10.0-ios/ios-arm64/Spixi.app/html/chat.html
dotnet build Spixi/Spixi.csproj -f net10.0-ios -c Debug -p:RuntimeIdentifier=ios-arm64 -p:CodesignKey="Apple Development" -p:ValidateXcodeVersion=false -t:Run -p:_DeviceName=AI15
```

## Commit message (this batch — ONE commit, GitHub Desktop)

```
Plate + F5 pass (#314-#323): 9 polish items (selectability→body-level, mention
mentions-first fix, toast/CTA safe-area, iOS-47 wordmark-only Sora, iOS-48 short
Connecting on all 3 tabs, i18n drafts x7 + About x6 + hero title, backup-badge
poll + landtab via onSettingsClosed, R6 full-detail tx sheet, dev-mode 10-tap) +
Account as peer tab (iOS-46 route (a): park + instant re-present, root hub
topbar, nav-highlight snap-back) + R5 dev parity (ixian:sendlog share/save +
live HUD) ; #46 loop 2 rounds clean (4 MAJOR catches mutation-pinned); dials
#316-#318 (Figma mirroring retired, R3/R4 locked); smoke 1006 pass / same 4;
consumed handoffs archived
```
