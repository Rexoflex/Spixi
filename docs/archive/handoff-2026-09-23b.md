# Handoff — 2026-09-23 (b): Session AD WALKED 22 · 2 · 3 — commit, then the FE fix round, then the office

Read this, then DECISIONS **#928–#930**, then `docs/f5-checklist-session-ad.md`. The walk sheet is
`docs/walk-artifact-session-ad.html` (27 rows). The loop's verdict is §6 of
`docs/opus-review-brief-session-ad.md` — **CLEAN NOT CLAIMED**: three rounds on Opus, the third's fixes
have had no reader. Damir's walk was that reader: **22 P · 2 F · 3 N/A (#935)**; both fails are outside the r3 fixes.

## 0 · State of the tree you inherit

* HEAD `de214173` (Session AC `b333142c` + the docs-only #927 commit; the container's snapshot base was byte-identical to it, verified per file) + the UNCOMMITTED Session AD delta: **94 files** (19 C# +
  `Spixi/Meta/SChatPrefs.cs` NEW · 9 shells/components · 13 `lang/*.txt` · 12 locale drafts · the
  rebuilt Raw/html · the suite · docs). Commit is Damir's, message in `docs/commit-message-session-ad.txt`.
* Suite (container, WITH the `Ixian-Core` sibling @ `097341a`): BEFORE **4817** → AFTER **4848 / the 2
  KNOWN** (#136 · B3). Compare the delta locally (#895).
* ⚠ **NOTHING HERE COMPILED ANY C#.** Windows F5 + Android Debug are the first compile of 19 files;
  `Platforms/iOS/SPushService.cs` (#490, a deleted body) compiles first at the office.
* Gates on the tree: `build-shells --check` ✓ · `extract-strings --check` ✓ · lint ✓ · `cs-syntax-check`
  184 + 3 known gaps.

## 1 · What Session AD did (#928)

Batches 1 → 3 → 4 of the #927 work order, as far as the verbs reach; every row in the brief's §1 table
(`docs/opus-review-brief-session-ad.md`) with its anchor. The headline: five shell workarounds DELETED
because C# now pushes the fact (CH6 reverse map · S11 handshake · #274 stash · C16 exdel hint · S2 stamp)
— four `spixi.*` families left the shared `file://` partition; W11's dropped payment response is stored;
a media purge exists (CH3) behind three guards; favorites are an app preference (CH4).

Refuted at the tree, recorded, not built: C16's premise · C10 · A3 · A5b · CO5 · CH3 mark-read.
NOT REACHED: batch 4's CI6 · C22 · A8 · FC1 · C12 · NT1(b) · PV1 — a session of their own, after the office.
OUT by Damir: L8 · L2/MAJOR #8. HIS: CORE-10 (still open, logged). Own mini-session: L6.

## 2 · The loop (#929) — what to know before reading its code

Three fresh Opus reviewers in a row each found something the previous had blessed:
r1 W11 peer-writable by id / S9's modal from an overlay page / C17's legacy-state dead action;
r2 CH3's walk on the UI thread + the re-root's parent; r3 the re-root's LEAF (the peer's name while S16
is open) and SIX pins a one-token mutation left green — all in pins the author had mutation-tested.
The lesson is #798's again: a pin that matches a clause as a SUBSTRING is satisfied by its own negation.
W11's predicate is now compared WHOLE against a canonical string; if you change that predicate, the pin
tells you — do not widen the pin, widen the rule.

## 3 · Next, in order

1. **DONE — walked 22 · 2 · 3 (#935); commit = Damir's (`docs/commit-message-session-ad.txt`).**
1a. **★ FIRST: THE FREEZE (#935).** Windows Debug froze after remove-contact → leave-group with
   "delete media". Get from Damir: the `ixian.log` tail (is there a `leavegroup: media purge deleted n
   of m` line?) and the VS Break-All stack of the UI thread. Candidates in `HomePage.onLeaveGroupFor`:
   the SYNCHRONOUS `collectReceivedMedia` listing on the UI thread (reads every `.ixi` of the friend) ·
   `SContacts.leaveGroup`'s fan-out send · `popPageAsync` on the removed group's pane. No code before
   the stack. If it is the listing: list → op → purge as ONE background job, the op marshalled back.
1b. **THE NEXT FE FIX ROUND (its own session, before the office if there is a gap):** #934 (b) the
   bot-room Delete gate · the ⊕ attach sheet opening PARTIALLY until a keyboard round (#935 — log
   `--kb-inset` at sheet open first; suspect the #918 cap on a stale inset) · AD.13 a second tip refused
   (get the device + alert text first; the shell has no gate) · AD.19 C17 on a legacy-state contact
   (Damir: remind him) · AD.20 A4's three reasons · AD.23 the shared-file purge case. AD.14 is NOT ours:
   **CORE-12** (group typing fan-out) — the app half is built and waits.
2. If the walk fails a row: fix in a session that starts by reading #929's r3 table — the pins around
   W11/CH3/C17 are exact-form now and will go red on the fix; re-base them to the PROPERTY, never to the
   new spelling.
3. The office iPhone day (`docs/walk-artifact-ios-office.html`, 31 rows — the portal steps on it are
   ALREADY DONE, #932) → the endgame (#916 as shaped by #933). The parallel session's addendum landed
   with this batch as DECISIONS #931–#934 (nine dials · the portal · the strip · two notes); its file
   `docs/addendum-2026-09-23-decisions.md` was moved to `_to_delete/` (Damir deletes it).
3b. The next FE FIX ROUND carries #934 (b): the bot-room Delete gate — non-admins see Delete only on
   their OWN messages, admins on all; the select-mode bulk delete under the same predicate; shell-only.
4. Then their own sessions: batch 4's medium rows · L6 · the translator pass · the Android memory kill.

## 4 · Still open, unchanged (do not re-derive)

* AC.3's clean-launch line (`copyResources: 0 copied, 51 unchanged`, Windows) — now walk row AD.0b.
* AND-47 `Arg_PlatformNotSupported` on Android Release logging (verify-first in Core's `Logging.cs`).
* AND-48 side-inset residuals — one RENDER session before any padding.
* The Android 30-minute memory kill (511 MB, Release): `dumpsys meminfo` footer, the
  `SpixiContentPage.Dispose()` stack guard, `CHAT_SPARE_ENABLED = false` as the free discriminator.
* `docs/diagnostic-session-aa-findings.md` still armed (AA.8 did not reproduce).
* The 123 naive comment-strip regexes + 26 local strippers (Session R).
* The translator pass (M13) · counsel on the privacy policy (#914). The ASD-STE100 rule is SCOPED to chat replies (#931) — CLAUDE.md reworded.
* S16 (receive-time `transfer.fileName` uncleaned) — BE's; CH3's `hasDotSegment` is the app-side belt.
* #930 dials: the cancel-records-a-backup stamp · fee excluded from `insufficient` · a favorite survives a
  Core-side removal · the purge's orphan residual.

## 5 · The workshop

* `device_bash` works; `git --no-optional-locks status` only; never plain `git status` on the VM.
* The suite runs in the container on a SNAPSHOT copy with the Core sibling beside it (~5 min). Tar
  `git ls-files` minus the native libs; `npm i --no-save jsdom eslint globals playwright tree-sitter
  tree-sitter-c-sharp` TOGETHER (a single-package install prunes the rest).
* Landing: tar → `/mnt/user-data/outputs` → `device_commit_files` into `_to_delete/` → `tar --overwrite`
  on the VM → `sha256sum -c`. A file edited DIRECTLY on the VM must be staged back before the next tar.
* Scratch runners `scripts/_*.tmp.mjs` (`_ad-mut` · `_ad-batch1` · `_ad-batch3`) are gitignored; never
  land them. Mutation harness = the REAL pin text, green baseline in its own root first (#906).
* Housekeeping Damir owes locally: `git rm --cached crash-logcat.txt mem-*.txt` + gitignore; delete
  `_to_delete/`.

## 5b · What this session failed to do (Damir asked; recorded so the next one does not repeat it)

* **Built C21 on an unverified wire.** The app half (typist address in the push, the pill naming the
  typist) shipped without re-reading whether Core fans `msgTyping` out to group members. It does not
  (`CoreStreamProcessor.sendTyping:2889` addresses the GROUP; the relay is Core's) — #264 had flagged
  "relay verify" and the flag was not re-read. #215 broken; AD.14 is the bill; CORE-12 is the row.
* **CLEAN not claimed:** three loop rounds, no fourth reader over the r3 fixes (the walk stood in).
* **Batch 4's medium rows not reached** (CI6 · C22 · A8 · FC1 · C12 · NT1(b) · PV1).
* **Six of my own pins were vacuous** until the r3 reviewer broke them (substring matches, guardable
  validation, an un-negated gate) — every one had been "mutation-tested" by me. #798, again.
* **My own comment turned a pin red** (`#574 ①`, a 600-char raw window) on the first AFTER run.
* **Wrong shell for the operator:** `&&` handed to PowerShell 5.1; `Remove-Item obj/bin` while
  Spixi.exe was still running. Cost Damir two retries.
* **Two doc claims were wrong before landing** (the A4 reason vocabulary; the Core NIT's wording) —
  caught by reading the source, but only after they had been written (#772).
* **Two gate trips from my own naming**: a bundle-export name reused as a push (`setBackupStatus`),
  an empty `*SL{}` literal in a comment (#412).

## 6 · Rules this session inherits

#215 · #294 · #660 · #663 · #771 · #798 · #811 · #882 ③ · #895 · #906 · #911 · #912 · #926 · #927 ·
★ #929: a pin that matches a clause as a substring is satisfied by its negation — compare the rule WHOLE.

## 7 · Files

NEW: `Spixi/Meta/SChatPrefs.cs` · `docs/opus-review-brief-session-ad.md` · `docs/f5-checklist-session-ad.md`
· `docs/walk-artifact-session-ad.html` · `docs/commit-message-session-ad.txt` · this handoff.
MODIFIED docs: `DECISIONS.md` (#928–#930) · `ARCHITECTURE.md` §4 · `docs/be-cutover-brief.md` ·
`docs/security-handover-gate.md` · `docs/security-review-for-be-engineer.md` · `docs/next-session-prompt.md`.
Consumed → `docs/archive/`: `handoff-2026-09-23.md`. Consumed → `_to_delete/`: `docs/addendum-2026-09-23-decisions.md` (its rows are #931–#934).
LIVE, untouched: `docs/walk-artifact-ios-office.html` · `docs/diagnostic-session-aa-findings.md`.
