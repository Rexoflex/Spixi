SESSION AF — the AE walk fixes + the review of #944, then a walk sheet for Damir (DECISIONS #944 · #945)

0 · Before anything else (#215)
Read `docs/handoff-2026-09-24.md` FIRST, then DECISIONS #944 and #945. Check `device_bash`; use
`git --no-optional-locks status`; ask for delete permission on the repo root ONCE before any git write
(handoff §5). Verify `c0711262` (Session AE) and the #944 commit on top are in the log. Run the four
`--check` gates. The FULL suite: on a snapshot in the container with the Ixian-Core sibling, or Damir's
local run — expect his AE number +10 / the same 2 KNOWN. A predicted number is not a last line.

0b · THE REVIEW (handoff §1) — before ANY build
One fresh Opus break-my-verdict reader over (1) Session AE's round-4 fixes (owed since #943) and (2) #944.
Pin the model explicitly. Verdicts into `docs/opus-review-verdict-session-ae.md`. NOT CLEAN → fix, pin,
mutate, a next reader.

1 · Build (handoff §2, in this order): ③ remove "Cancel request" from contact details / chat info for an
outgoing request (+ log the `receiveData:1260` NRE as a CORE-13 sibling in the BE brief) · ④ the
member-sheet admin actions row (no CSS rule exists — render options, Damir picks) · ⑤ no hairlines
between rows of the same group (Account hub + bot-room roster; derive the selector set) · ② the
duplicate-tip "Unknown error" → a true message (find the path first; new key in 12 locales) · ① the
reaction pill at the composer band (lowest priority; render, Damir may say leave it).
Each: a DECISIONS row · renders in both themes where visual · pins · mutations through the real pin
text · the AFTER run.

2 · Deliver for the walk (handoff §3): `docs/walk-artifact-session-af.html` (#944 rows + each fix + AE.14/
AE.15 carried), published as an Artifact; `docs/f5-checklist-session-af.md` with exact PowerShell
commands for Windows F5 and the Motorola deploy; a commit message; CLAUDE.md status row; a new handoff +
this prompt rewritten for the OFFICE day (`docs/handoff-2026-09-23d.md` §3 stays the office plan).

3 · NOT this session
The office iPhone day (after AF's walk) · batch 4's medium rows · L6 · the memory kill · the translator
pass · the strip · CORE-9/-10/-11/-12/-13 (his) · #934 (a) · #936 · C14 · C20.

Rules #215 · #294 · #660 · #663 · #771 · #798 · #811 · #895 · #906 · #929 · #935 · #943 (the ceiling is
measured LAST; the loop ends on the round that finds nothing) · #892/#893 (render before you pin; let
Damir say no). Chat replies to Damir in ASD-STE100 (#931).
