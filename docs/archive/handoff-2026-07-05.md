> 🗄️ **STALE — superseded.** Historical only. Current entry point: **`docs/handoff-fable-next-batch.md`**. Decisions preserved in `DECISIONS.md`.

# Session handoff — Apps finish + Wallet shell (2026-07-05)

Paste this into the new chat. `DECISIONS.md` is the source of truth; rows **#127–#136** carry this session's detail.

## What was done

**Apps (closed out):** #127 details action-hierarchy (Open = header pill, quiet Uninstall row, screenshots-first, title/cover overlap fixed, a11y round) · #128 flags logged (share gap, full-bleed cover deferred, dev CTA, web→app flow) · #129 install-modal fix + identity icon + domain-only source, Explore web fallback, dev CTA, Advanced share hint, **feed contract locked** (`apps-feed.js` → `https://apps.spixi.io/data/apps.json`, CORS `*`) · #131 **live Discover built** (featured strip + cards + installed badges + category filters; demo live-fetches w/ snapshot fallback).

**Docs:** #130 illustrations plan (APPROVED, then **#132 PARKED** — nano-banana output poor; prompts stay ready) · #131 **backup UX spec** (`docs/backup-ux-spec.md`: one primary "Back up Spixi" — the account file already bundles wallet+contacts+avatar; wallet-only export → Advanced; status-driven standing nudge; Account-shell row + onboarding tail; BE asks: last-backup timestamp + dirty signal) — builds with the Account shell.

**Wallet shell (#133 spec → #134/#135/#136 built):** slices 1+2 DONE code-first (no Figma payment designs exist — sanctioned #31 exception; app-frame mock = visual reference).
- Home: `c-wallet-hero` (safe-area-owning hero — no statusbar seam; tap-anywhere-to-hide balance; uniform Send/Receive/Scan) + tx list (#55 rows) + sticky tools (FE search over names/addresses/txids + filter chips + #98 missing-tx pill → explainer sheet w/ `ixian:explorer` parity, single action) + **scroll choreography** (hero minimizes to compact title+balance, tools tuck away, brief scroll-up reveals, top restores) + tx-detail sheet (contact avatars, 4-state legacy status enum, fee ⓘ explain, "Sender's/Recipient's address" chip w/ real clipboard copy-morph, Explorer link latched).
- Send: one screen + #26 review sheet. **Money math = integer 1e-8 units via BigInt** (audit caught float bugs + a mid-send-dismissible sheet = double-send path — all fixed, #135). Address row = contact-aligned first row (qr glyph, expands input); contacts capped 5/8 + "keep typing" note; `inputmode=decimal`; **Max → "Yes, I understand" alertdialog**; exact strings at the confirm step (never `formatIxiAmount` there); fresh-tx **2s flash** on return home.

**Verification:** 64 standalone jsdom assertions green; wallet + send folded into `scripts/smoke-test.mjs` (run `node scripts/smoke-test.mjs` locally — full regression). Bundle regenerated via real build script, 152 exports, tail verified.

## Critical workflow gotcha (WORSE than last session)

The sandbox mount serves **stale truncated reads of ANY file edited this session** (old content, new length — bash/node see garbage tails). Read/Edit/Write tools hit the real files fine. Workflow that works: keep a staging tree in `/tmp/build`, mirror every edit into it (node string-replace patches or heredocs), run `build-demo-bundle.mjs` there, `cp` the bundle back through the mount (writes go through), verify the real bundle tail with the **Read tool**. NEW files copy fine through bash until edited.

## Verify on your machine first

1. Hard-refresh `src/demo/wallet.html` — search+filters+missing-tx should now be visible (real bug: flex-collapse, fixed via `flex:none` — #136①) and eye center-tap works (stopPropagation — #136②).
2. `node scripts/build-demo-bundle.mjs` + `node scripts/smoke-test.mjs` — clean local rebuild retires any sandbox-mirror doubt.

## Next session plan (agreed)

1. **Wallet slice 3 — Receive/Request**: QR of own address (`--surface-qr` tokens exist), copyable address chip, share; request = optional amount → `addr:send:amount` QR + send-as-message (attach-sheet "Request payment" exists).
2. **Chat TIPPING spec + build** (Damir: different UX from send; take logged in #136): message-menu/reaction-row → small sheet, preset chips 1/5/10 IXI + custom, sheet IS the #26-lite review (amount+recipient visible, one explicit confirm), lands as the #65 tip-pill + wallet tx. Bridge question: tip = plain payment w/ message ref? (§9).
3. Then: wallet on-device pass (#22 safe-area check is LIVE now — hero owns the inset) · remaining shells (Account — backup spec ready · Contacts/Settings/Scan).

## Open flags for Damir

- Failed sends list under "Sent" filter (badge carries it) — your call (#134).
- Hero fiat line kept per your mock; balance-fiat source = §9 BE ask (#134).
- B2 glyph exports: `link` (MISSING — renders empty in apps-add), `code`; full sweep pre-release.
- §9 BE asks accumulating: tx fee in addPaymentActivity, IxiScope/Explorer URL config, history paging, Discover transport (iOS whitelist), backup timestamp+dirty signal, tipping semantics.

## Key files

Wallet: `src/components/wallet-{hero,shell,send}.js` + matching CSS, demo `src/demo/wallet.html`.
Apps: `src/components/apps-{feed,discover,add,details}.js`. Docs: `backup-ux-spec.md`, `wallet-shell-spec.md`, `illustrations-plan.md` (parked). Bundle `src/demo/spixi.iife.js` (152 exports) · build `scripts/build-demo-bundle.mjs` · smoke `scripts/smoke-test.mjs`.
