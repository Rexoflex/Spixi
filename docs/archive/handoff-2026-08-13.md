# Handoff — next session (written 2026-08-12) — READ FIRST

The 2026-08-12 landing day + two live F5 rounds on the Samsung. Everything is
**on Damir's disk, UNCOMMITTED** (cloud-scratch snapshots exist; Damir commits in
GitHub Desktop per #306). Smoke **BASELINE OK 1040 / same-4** at close.

## ★ FIRST TASK: the #46 adversarial audit over EVERYTHING built today

Nothing built AFTER the overnight #334/#335 loop has been adversarially reviewed.
Run a full #46 loop (3 disjoint read-only auditors → fix agents (disjoint scopes)
→ fresh break-my-verdict reviewer → loop until CLEAN) over the day's deltas:

- **W-riders (#336):** W0 csproj RID OS-guard · W1 theme decode (empty_detail stub
  dispatcher + contact_details setTheme handler) · W3 contacts chip size · W4
  desktop bubble token.
- **AND-16 v2 (#336):** the measured Android keyboard re-pin (settle-tracked
  stickDuring on the innerHeight shrink) + the kb-probe removal. ★ verify the
  near-bottom-judged-pre-shrink logic + the Android UA gate can't fire on iOS.
- **F5 fixes (#336):** #15 settings hub scroll-restore · #10 friendlier
  invalid-address titles (8 locales + 2 new C# keys + 2 SingleChatPage sites) ·
  **AND-29 hardware-back on home takeovers** (shell state push + HomePage routing —
  ★ this touches hardware back, audit hard: can homeShellOverlayOpen desync? a
  sheet-over-takeover case? the homeBack handler vs a native overlay also open?).
- **Splash (#336):** REVERTED to the static blue splash (F5: not premium, clipped).
- **iOS-65 (#336):** media-viewer close moved bottom-centered (was unreachable under
  the iOS status bar) — media-viewer.js/.css + rebuilt chat/contact_details shells.
- **AND-29 v2 (#336):** wallet-takeover state push + overlay.js sheet coverage +
  homeBack dismissTopOverlay-first — ★ audit the hardware-back routing hard.

Land fixes via the bridge (mount forbids unlink → write files in place with
`git show <cloud-commit>:path > path` OR SendUserFile→device_commit_files; NEVER
`git checkout --` on the mount). Rebuild: bundle BEFORE shells. Smoke must stay
BASELINE OK 1040/same-4.

## Build/deploy recipes (proven this session)

- **Android:** `rm -rf Spixi/obj/Debug/net10.0-android Spixi/bin/Debug/net10.0-android`
  → `dotnet build Spixi/Spixi.csproj -f net10.0-android -c Debug -t:Run`.
- **iOS device (NOT sim):** `sudo dotnet workload update` (Xcode 26.6 vs 26.5 gate)
  → build then run as TWO steps with the device RID + UDID:
  `dotnet build Spixi/Spixi.csproj -f net10.0-ios -c Debug -p:RuntimeIdentifier=ios-arm64`
  then `… -p:RuntimeIdentifier=ios-arm64 -p:_DeviceName=<UDID> -t:Run`
  (UDID from `xcrun xctrace list devices`; single-step -t:Run races mlaunch = MT1044).
- **iOS F5 still OWED** (device deploy was being sorted at close): the platform-pending
  checklist — notification silhouette · **avatars (X1 is the iOS win)** · **iOS-54
  airplane-mode repro** (decides the chat-connectivity fan-out) · iOS-53 topbar dip ·
  iOS-56 edge-swipe · iOS-62 · plus the shared items already passed on Android.

## OPEN — Android (from the two F5 rounds; docs/android-findings.md #336 blocks)

- **AND-7** wallet not full-bleed at the TOP — UNBLOCKED (AND-20's big lever refuted
  by the kb-probe). Fix = MainActivity InsetsListener stops padding the top, pushes
  the top inset to the shells as a CSS var (Android env() is cutout-only), consumed
  by the iOS-#282 topbar/hero env() chrome. Its own careful round + device verify.
- **AND-20 keyboard SMOOTHNESS** — the AND-16 v2 re-pin fixed covered-messages;
  the composer swing is correct but not iOS-smooth (adjustResize one-jump). Smooth
  path = WindowInsetsAnimationCompat per-frame inset, OR adjustNothing + a
  CSS-transitioned `--kb-inset` push (the "inset into page" approach, now justified
  for smoothness). Measure the animation frames first (#294).
- **AND-24** native DisplayAlerts read ugly (navy-on-dark); Damir asks to circumvent
  the OS dialog. ~90 displaySpixiAlert sites → route to the in-app Spixi dialog
  surface (c-modal). DESIGN DECISION, sizeable. The SHARE SHEET (backup save/share)
  is unavoidable OS.
- **AND-25** remove-contact → FATAL native exception (real crash). NEEDS LOGCAT at
  repro (`adb logcat -c` → remove a contact → dump).
- **AND-26** Account↔Wallet flicker + slowness; picker round-trip flicker. = AND-19
  residual + #315 park cost. Next lever = the Opacity-staging half (stage at ~0.01
  so the WebView composites before the flip). Device-gated.
- **AND-27** mic-denied → re-asked natively → NO call screen appeared. Needs a clean
  repro + logcat (the CallPage native surface on Android, or the ring not presenting).
- **AND-28** existing-contact avatars don't load (new ones + the full-size viewer
  work). Investigate the C# avatar path for existing vs new contacts.
- **AND-29b** — DONE in #336 v2 (overlay.js sheets now covered); device-verify remains.
- **AND-30** member sheet offers "Add contact" for an ALREADY-contact — should show
  Send message / View profile (needs a relation flag to the member sheet).

## OPEN — iOS (device F5 2026-08-12; docs/ios-sim-findings.md #336 block)

- **iOS-56b** edge-swipe back works only IN CHAT, not in subscreens (settings
  sublevels, contact details, app details). Generalize the #325 shell edge-swipe
  lever to the other overlay-presented shells, or a shared native edge-pan on
  overlay-presented pages routed through the same back path (#294 measure-first).
- **iOS-66** Account screen SNAPS to the bottom on a language change — the setLocale
  live re-render (#334 iOS-58) vs the #336 hub-scroll-restore; the pick surface
  should stay at TOP after a re-localize.

## OPEN — Windows-findings (READ-ME-FIRST-WINDOWS-FINDINGS.md, Damir PC 2026-08-12)

W0/W1/W3/W4 landed (#336). REMAINING:
- **W2** desktop nav rail covered by full-window surfaces (legacy wallet Receive
  `PushAsync` pages + the contacts takeover). Fix: route wide-mode presentations
  through `pushPageLoaded` with the rail inset (stageMargin), and inset the contacts
  takeover left by the rail width on `:root[data-desktop]`.
- **W5** ★ chat background PATTERN STYLES (Damir prototyped + APPROVED on Windows —
  full spec in the findings file §W5). Three styles (Line art / Data matrix / Live
  flow) × the existing visibility dial. Data-matrix generator params + the live-flow
  engine tuning are LOCKED in the doc. **Live flow = DESKTOP-ONLY** (battery). Persist
  the style like the intensity pref, same pre-paint script. Substantial FE batch.
- **W6** Wallet Receive → "Request an amount": QR animates away upward, amount input
  focuses, per-contact send arrows disabled until a valid amount. Spec in §W6.
- **W7** Account → Change wallet password HANGS the Account surface on Windows
  (pane preload latch suspect; settings_encryption onload timing). Repro + fix per §W7.
- **F1** (future, design note) AI-agent conversations over QuIXI — capability
  handshake + bubble-less agent rendering + sanitized markdown + Agents filter chip +
  workflow side panel. Mock: `docs/agent-chat-demo.html`. Phase-3+ exploration, NOT
  the fix batch. Security rules in §F1 (sanitized-subset markdown, self-declared
  cosmetic flag, wallet stays user-confirmed).

## Dials / accepted

- iOS-64 draft excerpt ~2s latency — ACCEPTED ("too long but ok").
- Splash — animated icon REVERTED (F5: not premium); a real premium splash needs
  proper icon artwork sized to the Android-12 safe circle — future design task.

## What LANDED today (for the commit message when Damir commits)

#334/#335 overnight batch (security containment MAJOR#4 Android + 2 new MAJORs logged;
Android chrome/notifications; cheap-C#; live re-localize; swatch tiles; file-send
cancel; AND-11/17/21/22/23; bubbles 82/360; avatar viewer) · W0/W1/W3/W4 Windows
riders · AND-16 v2 measured keyboard re-pin · #15 settings scroll · #10 titles ·
AND-29 (+v2) hardware-back on home takeovers/sheets · iOS-65 viewer-close bottom-centered · splash reverted to static. smoke 1040/same-4.
