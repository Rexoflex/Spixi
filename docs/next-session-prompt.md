THE OFFICE iPHONE DAY — Session AF is committed and pushed; Damir pulls on the office Mac.

0 · Before anything else (#215)
Read `docs/handoff-2026-09-24c.md` FIRST — it is the only live handoff (09-23d and 09-24b are archived; their
office plan is folded in). Check whether `device_bash` works; use `git --no-optional-locks`; ask for delete
permission on the repo root ONCE before any git write. Confirm the pull: `git log --oneline -3` shows the AF
commit(s) and the tree is clean.

1 · The day (Damir on the iPhone; you on the chat)
Walk sheet: `docs/walk-artifact-ios-office.html` (39 rows; §6 = the AE–AF changes on the iPhone). Wipe
obj/bin → build Debug for the iPhone. It is the FIRST COMPILE of the push extension (Spixi-PushService/),
SPushPrefsShare.cs, #912 ②, #490's iOS line — a build error is a Session AC/AD bug: have Damir paste it whole;
do not guess. ★ iO.7 (the mute test) is the one question no document answers — record it as the note.
AE.6b rides along. Route B (#920) LAST. The office Mac's Ixian-Core is clean 097341a — a Debug break on the
late-accept NRE (#962) is expected there, not a regression.

2 · Anything that reads wrong on the device is a walk NOTE first (row, what he saw, a screenshot), a fix
second (#935). Collect the fixes; build them as a short follow-up round after the walk, with renders, pins,
mutations and a fresh Opus reader (the loop ends on the round that finds nothing, #943). Run the FULL suite
before calling anything done.

3 · Decisions to ask Damir, not assume: #970 (declined request returns: BE message vs local ignore list) ·
reply-to on-device check · the colon per locale.

4 · NOT this session: the code review/sweep (after the walk and its fix round — handoff §3) · L6 · the freeze ·
the strip · batch 4's medium rows · the memory kill · the translator pass · CORE-9…13 (his).

Rules #215 · #294 · #660 · #663 · #771 · #772 · #798 · #811 · #895 · #906 · #935 · #943 · #953/#959. Chat
replies to Damir in ASD-STE100 (#931).
