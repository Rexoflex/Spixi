# iOS Simulator findings — first pass (2026-07-22, iPhone 17 Pro sim, iOS 26.5)

First-ever iOS run. Phase-B rules apply: platform bugs only, fix small, log in
DECISIONS.md. Damir's walk-through findings, numbered as reported. Not yet triaged
into fixes — this file is the punch list.

| # | Finding | First read / suspected root cause | Status |
|---|---------|-----------------------------------|--------|
| iOS-1 | Entry/launch screen not full-screen — white strips at status bar + home indicator; should bleed edge-to-edge to feel premium | Safe-area class, same root as iOS-3/iOS-4: WKWebView not laid out edge-to-edge (native page background showing through). Needs viewport-fit=cover + env(safe-area-inset-*) padding in the shells + MAUI page/WebView ignoring safe area | **FIXED** (#282 edge-to-edge) — sim-verified 2026-07-27 (Damir: strips gone, both themes) |
| iOS-2 | Backup intro ("One file protects everything") uses a different illustration than the backup nudge + Account→backup pane — should be the SAME asset | Consistency nit, asset swap in the launch/backup shell | **FIXED** (#283) — launch tail now loads images/backup.svg (placeholder = onerror fallback); pending sim verify |
| iOS-3 | Wallet screen: same not-full-screen white strips | Same root as iOS-1 | **FIXED** (#282 edge-to-edge) — sim-verified 2026-07-27 (Damir: strips gone, both themes) |
| iOS-4 | Dark mode: web content follows the toggle but top/bottom strips stay SYSTEM-light — native chrome shows through | Confirms iOS-1 diagnosis: the strips are the NATIVE page background; it must follow the app theme (and ideally the fix for iOS-1 makes the strips disappear entirely) | **FIXED** (#282 edge-to-edge) — sim-verified 2026-07-27 (Damir: strips gone, both themes) |
| iOS-5 | **CRASH**: in a chat, tapping the composer ⊕ opened the attach/Share sheet and the APP QUIT ("Spixi quit unexpectedly") | Native crash, likely the native file/photo-picker bridge invoked from the attach sheet on simulator. Needs the crash report (`~/Library/Logs/DiagnosticReports/Spixi-*.ips`) or `simctl spawn booted log stream` capture on repro | **FIXED** (#280 guard + #281 selector export) — sim-verified 2026-07-22 |
| iOS-6 | With the software keyboard up, the top bar gets shoved off-screen — keyboard appears to RESIZE/push the whole page rather than the composer avoiding it | ROOT CAUSE (#283): the redesigned chat shell never inherited legacy chat.js's iosFixer — WKWebView pans the document to reveal the focused composer (screenshot 2026-07-27 confirms post-#282). | **FIXED** (#294) — DEVICE-DIAGNOSED on iPhone 15, after two wrong rounds. Measured with Web Inspector: `[innerHeight, vv.height, vv.offsetTop] = [852, 544, 0]`, so the #283 premise HELD (innerHeight constant, delta 308 >> 60) and the `visualViewport` resize event DID fire (852 → 544 logged). The bug was downstream of both: `document.body.style.height = …` **did not take** (competing `html, body { height: 100% }`), and once forced with `!important` the body genuinely shrank and lifted the composer — but ~40px too high, because WebKit keeps reporting `env(safe-area-inset-bottom)` at full value with the keyboard up, so the composer was still clearing a home indicator that is not there. Fix = both halves: set the height with `important`, and set `data-kb` on `<html>` while open so `composer.css` zeroes the inset for exactly that window |

Notes: iOS-1/3/4 are almost certainly ONE fix (edge-to-edge + themed native background
+ safe-area CSS), which the Android pass will also want. iOS-5 is the only crash-class
finding of the run. Everything else in the walk (boot, account create on healthy disk,
wallet render, chats list, chat view, composer, message send, dark-mode web content)
worked on first contact.

Context: account-creation hang from earlier in the evening was the FULL DISK
(51.5GB of duplicate simulator runtimes since removed), not an app bug — creation
succeeded immediately on a healthy disk.
| iOS-7 | **CRASH**: app quits when RECEIVING a contact request (sim, iPhone 17 Pro) — core Stage-5 flow | Unknown yet — receive path spans network parse → friend/roster update → UI event → possibly local-notification presentation (SPushService/UNUserNotificationCenter is a suspect class on sim). Crash report + console capture wanted | **FIXED** (#280+#281) — sim-verified 2026-07-22 |
| iOS-8 | Unread badge (row + Unread chip + tab) persists after opening and leaving the chat; the only prior action was accepting the contact request — the stuck row shows the #273 `request-done` "Contact Accepted" excerpt | ROOT CAUSE (#283, cross-platform confirmed from source): loadMessages zeroes the count but pushes NOTHING (updateMessageReadStatus only pushes setContactStatus when it marks a markable message — and skips requestAdd), so a requestAdd-only unread stayed painted until the next structural flush | **FIXED** (#283) — loadMessages now pushes setContactStatus after zeroing; Windows VERIFIED 2026-07-29 (row + tab badge clear after accept→open→back, #286 session); iOS verify still owed |
| iOS-9 | **CRASH**: app quits on INCOMING CALL from the counterpart (sim) — third crash of the pass | Pattern across iOS-5/7/9: all fire when native device feedback would trigger (attach tap-feedback · request notification/sound · call ringtone/vibration). Working theory: unguarded native haptics/sound/vibration call fatal on simulator (no haptics hw — the CHHapticPattern log noise supports it). Confirm via .ips reports | **FIXED** (#280+#281) — sim-verified: incoming call rings, app lives |
| iOS-10 | **CRASH** (4th report, 20:37): managed exception escaping the WKWebView `DecidePolicy` navigation callback (likely tapping an external/Terms link) | Same NSException-abort class as iOS-5/7/9 but in `iOSWebViewHandler.SecureNavigationDelegate`. Guarded (#280); underlying exception now logs instead of killing the app. NOTE: http/https links are Cancel-blocked with no external-browser handoff — behaviour gap vs Windows, separate row when confirmed | GUARDED — root cause from log |

**Crash triage 2026-07-22 (.ips analysis):** iOS-5, iOS-7 and iOS-9 are ONE bug — a managed exception thrown inside `SPushService.NotificationDelegate.WillPresentNotification`, reached via OneSignal notification-swizzling on every FOREGROUND presentation (attach-moment message, contact request, incoming call — the trigger was the notification, not the feature). `DidReceiveNotificationResponse` had a try/catch; `WillPresentNotification` did not — a managed exception escaping a native callback is an uncaught NSException → abort. Both callbacks guarded (#280) — but the actual root cause was #281: the .ips class died at SELECTOR LOOKUP ("Failed to lookup the required marshalling information") BEFORE any managed body ran, so the #280 guard alone could not fire (it cannot log this class). #281 exports the OneSignal-prefixed delegate selectors; #280 stays as defense-in-depth for exceptions thrown INSIDE the managed bodies.

## Second pass (2026-07-27, post-#282 plumbing checklist walk) — iOS-11+

Damir's findings from the plumbing pass on top of #282 edge-to-edge. Numbered as
reported. Tags: **[PLAT]** = likely iOS/platform bug (Phase-B fixable), **[XPLAT]** =
suspected cross-platform logic/UI bug (verify on Windows before classifying),
**[PROD]** = product/design gap or decision needed (backlog, not Phase-B).

| # | Finding | First read / suspected root cause | Status |
|---|---------|-----------------------------------|--------|
| iOS-11 | While sitting in the Account screen, another user scanned the QR and sent a contact request — NO unread indicator appears on the Chats tab in the bottom nav | **[XPLAT]** Sibling of iOS-8: the request-event kind seems disconnected from the unread pipeline in BOTH directions (iOS-8: never cleared; iOS-11: never raised on the tab badge). Fix both while in the request-event unread path; cross-check on Windows first (07-22 PC handoff step 4 recipe) | **FIXED** (#283 FE half) + (#292 C# half) — see iOS-31 for the three cache/recount bugs that kept the badge alive |
| iOS-12 | Chats screen: the FAB sits partially BELOW/behind the bottom nav | **[PLAT]** Likely a #282 rider: c-bottomnav height GREW by the safe-area inset but the FAB's bottom offset didn't — FAB needs to offset by nav-height + env(safe-area-inset-bottom). Small CSS fix in the chats shell | **FIXED** (#283) — FAB bottom offset += env(safe-area-inset-bottom) |
| iOS-13 | Wallet: transaction details opens as a NEW SCREEN instead of a bottom sheet (mobile view only) | **[PROD]** Design gap vs spec (tx-row-spec/wallet-shell), not a platform bug — mobile should get the sheet presentation. Backlog | **FIXED** (#283) — B3 regression: onTx verb now DESKTOP-only, mobile gets the built in-page tx sheet back |
| iOS-14 | Incoming contact request: the address shown in the chat row is too big | **[XPLAT]** Request-row rendering (contact-request-v2 / #273 request rows) — address should truncate/ellipsize at the standard row excerpt size. Verify on Windows | **FIXED** (#290) — NOT a truncation bug: the address rendered body-lg SEMIBOLD in the name slot (the #277 tx-row defect, never swept here). Regular weight + `[data-address]` → excerpt role; local 6…4 helper replaced by the shared `truncateAddressMiddle`. Damir eyeball owed |
| iOS-15 | Declining a contact request: should the requester be notified? | **[PROD]** Product/protocol decision, possibly BE-gated (does the carrier support a decline signal?). Decide before building; privacy argument for silent decline exists | **DECIDED** (Damir 2026-07-27): SILENT decline — matches C7 posture, zero protocol work |
| iOS-16 | Message read status is hard to read IN the chat view (fine in the chat list) | **[XPLAT]** Status glyph/contrast in bubble context — likely tick color vs bubble surface in one or both themes. Verify on Windows | **FIXED** (#283, Damir: states-hard-to-tell) — WhatsApp grammar: delivered ✓✓ = neutral meta ink (was success-400, 1 shade off read), read ✓✓ alone green. Damir eyeball owed |
| iOS-17 | GIFs in messages: bubble should wrap the GIF's actual size — currently extra empty space around it | **[XPLAT]** Media bubble sizing — placeholder/aspect box not collapsing to intrinsic media size. Verify on Windows | **FIXED** (#283) — fitTile in media-bubble.js: width capped at natural/height-cap size, no upscale, no letterbox; sent tiles hug the outer edge. Damir eyeball owed |
| iOS-18 | Apps: multiuser apps still use the OLD design for selecting a user | **[PROD]** Known redesign gap (apps-shell-spec scope) — the multiuser participant picker was never reskinned. Backlog | OPEN |
| iOS-19 | Account screen: "Action needed" chip stays visible even AFTER a successful backup | **[XPLAT]** Backup-state flag not consumed by the chip's visibility check (backup-ux-spec nudge state vs account chip reading different flags?). Verify on Windows — if it repros there it's a straight logic bug | **FIXED-interim** (#283) — local backup stamp (spixi.backup.last) written on share launch by settings + settings_backup shells; real signal stays S2. Caveat: canceled share sheet still clears the nudge |
| iOS-20 | Change password is missing entirely from the Account screen | **[PROD]** Feature gap vs settings-shell scope. Backlog (check settings-shell-spec for where it was parked) | **FIXED** (#283, S7 LANDED) — SettingsPage dispatches ixian:encpass → EncryptionPassword + setCaps(encpass); hub row un-gated behind the cap |
| iOS-21 | "How to use Spixi — learn more" link should open spixi.io's how-to page | **[PLAT]-gated** Content/link wiring is trivial but on iOS it's BLOCKED by iOS-10 (external links Cancel-blocked, no browser handoff). Land after the iOS-10 `Browser.OpenAsync` fix | **FIXED** (#290) — ungated once #283 landed the browser handoff: About/How-to links emit `ixian:openLink:` (new SettingsPage branch, SingleChatPage:334 mirror); "Learn more" repointed to the help centre (Config.guideUrl) |
| iOS-22 | Dev mode is not available to send a log | **[PROD]** Dev-mode/send-log entry missing from redesigned Account/settings shell. Backlog; useful for exactly this kind of bring-up debugging, consider pulling forward | **FIXED (#314 + #321)** — 10-tap on the chats-topbar logotype (rolling 1.2s window) toggles dev mode via the EXISTING HomePage verbs (enable/disable/dev, xaml:756-770, zero-C#); `*SL{devMode}` carrier persists the topbar dev-log action; ⚠ 'adjustments-alt' stands in for a proper dev glyph (icon export owed). Send-log LANDED (#321: ixian:sendlog → share sheet / Windows save) + the live HUD restored — full R5 parity, device-verified 2026-08-10 |
| iOS-23 | Terms of use + Privacy policy missing from Account → About | **[PROD]+[PLAT]-gated** About pane content gap; external open on iOS also gated on iOS-10. Same batch as iOS-21 | **FIXED** (#290) — Terms + Privacy added to About as the SAME in-app doc sheets onboarding uses (`openLegalDoc`, launch-shell.js) — not external links, so one source for legal copy and English-only per #169 |
| iOS-24 | Delete chat history (per contact): returns to the chat with history STILL VISIBLE; actual delete only happens on exiting the chat. Should delete immediately and land on the emptied chat | **[XPLAT]** Delete action appears deferred (queued to chat-exit teardown?) instead of immediate; UI not re-rendered on completion. Verify on Windows | **FIXED** (#283) — ContactDetails.onRemoveHistory now re-renders the open chat (getChatPage → loadMessages): empty conversation paints immediately |
| iOS-25 | Delete ENTIRE chat history: chats remain briefly visible in the chat list before disappearing | **[XPLAT]** Same family as iOS-24 — list refresh races the bulk delete; needs optimistic clear or delete-then-notify ordering | **FIXED** (#283) — SettingsPage.onDeleteHistory sets shouldRefreshContacts → first tick re-flush drops wiped rows |

Second-pass note: nothing crash-class this time — all findings are UI/logic/product.
The iOS-8+iOS-11 pair (request-event unread path broken in both directions) is the
strongest signal: one root cause suspected, cross-platform, highest value on the
XPLAT list.


## Third pass (2026-07-27 evening) — FIRST REAL-IPHONE RUN (iPhone 15, device build via Mac)

Historic: first Spixi on real iOS hardware — dev-signed Debug build, legacy App Store
Spixi uninstalled first (same bundle ID blocks dev-signed overwrite; mlaunch hangs
silently on that conflict — see handoff gotchas). Damir: "Works, looks good… major
win." Findings numbered on. Same tags as the second pass.

| # | Finding | First read / suspected root cause | Status |
|---|---------|-----------------------------------|--------|
| iOS-26 | Groups must stay in CONTACTS — if someone deletes chat history they can't find the group anymore. Reverts the earlier removal decision; also wants a people/groups filter in the contacts directory | **[PROD — decision REVERTED by Damir 2026-07-27]** Re-add groups to the contacts directory + a people/groups filter (chip grammar). Product change, not a bug fix — queue as its own batch row | **FIXED** (#290) — groups back in BOTH picker purposes with People/Groups chips (reverses Q5/#253). Money fence landed with it: `peopleRoster()` feeds createWalletSend + requestableContacts, closing the #255 backlog item |
| iOS-27 | **CRASH** on RECEIVING a contact request — on the real iPhone (was iOS-7 on sim, "fixed" by #280/#281 guards) | **[PLAT]** Device ≠ sim for the notification path: real push registration / OneSignal init runs on hardware where the sim path didn't. #280 guards WillPresentNotification/DidReceive — either a DIFFERENT unguarded native callback fires on device, or the crash is pre-guard (OneSignal registration). NEXT: capture the .ips crash report (Settings → Privacy → Analytics, or Xcode → Devices) / `xcrun devicectl` console — check FIRST for a marshalling/selector-lookup abort (the #281 class: the #280 guard CANNOT log those, they die before managed code runs); only a guarded-path crash shows a managed exception in the app log | **FIXED + DEVICE-CONFIRMED 2026-07-29** (#291) — ROOT CAUSE PROVEN by the device build, not the .ips: `MT4174: Unable to locate the block to delegate conversion method for OneSignalWillPresentNotification(...)'s parameter #3`. #281's hand-written [Export]s carry an `Action<UNNotificationPresentationOptions>` block param with no [BlockProxy], so no trampoline is generated → the abort at selector lookup. Debug's dynamic registrar downgrades it to a warning, which is why the sim "passed". FIX: the custom UNUserNotificationCenterDelegate is DELETED — its logic already exists in OneSignal's own managed events (Clicked = the "fa" deep link; WillDisplay = foreground display, which now also honours the "alert" flag). Also closes **B1** (our selectors were what starved OneSignal's class_addMethod) and **B2** (two owners of a push tap) |
| iOS-28 | Deleting a CONTACT does not remove its chat from the chats list | **[XPLAT]** ContactDetails remove path likely deletes the friend without the chats-list flush/tombstone (home shell keeps the row until a structural re-flush; deletedChats tombstone is FE-side and only set by the row menu, not by a contact-details delete). Same family as iOS-24/25 — verify + fix in the delete-refresh batch | **FIXED** (#290) — premise here was WRONG: ContactDetails:325 DOES set shouldRefreshContacts and the 2s tick DOES consume it, so the row survived ~2s, not forever. Immediate `updateScreen()` flush (iOS-24 pattern) + a JS watchdog so a half-completed loadChats can't latch the list render dead |
| iOS-29 | Keyboard (iOS-6 retest on device): topbar now STAYS (half win — #283 handler works) but the COMPOSER hides UNDER the keyboard | **[PLAT]** The #283 visualViewport handler resets the pan (topbar fixed ✓) but the body never shrinks — on-device WKWebView's innerHeight/vv.height relationship differs from the assumption (delta ≤ 60 → no resize branch). NEXT: set `platformView.Inspectable = true` (iOSWebViewHandler, already scaffolded) and watch window.innerHeight vs visualViewport.height live from Safari Web Inspector with the keyboard up; then retune the delta source (likely: compare vv.height against the INITIAL layout height, not live innerHeight) | **STILL OPEN — #294 attempt REVERTED.** Measurements from the device (iPhone 15, Web Inspector) that any next attempt should start from, NOT re-derive: `[innerHeight, vv.height, vv.offsetTop] = [852, 544, 0]` → the #283 premise HOLDS (innerHeight constant, delta 308 >> 60) · the `visualViewport` **resize event DOES fire** (852 → 544 logged live) · `document.body.style.height = …` read back as `""` and the composer stayed at 852 · forcing it with `setProperty(…, 'important')` DID shrink body and lift the composer above the keyboard **in the console** · but it landed ~40px high, because WebKit keeps reporting `env(safe-area-inset-bottom)` at full value with the keyboard up. **However, shipped, the same change did NOT work and introduced a NEW artifact: on composer focus the topbar slides up and a second topbar animates in below it** (Damir F5) — i.e. resizing `body` while WKWebView runs its own focus pan is the wrong lever, console-verified or not. Reverted to the pre-#294 shape. **Next attempt should try a different lever entirely** — e.g. leave body alone and translate/position the chat column against `visualViewport.offsetTop`/`height`, or hand the problem to native (adjust the WebView frame on keyboard notifications) rather than fighting WebKit from inside the document. Three fixes have now failed here; do not attempt a fourth without a plan that changes the lever |
| iOS-30 | Contacts + new-chat screens render NARROWER than the chats list | **[PLAT]** Post-#282 suspects: the contacts takeover / contact_new page still carries a side inset the redesigned chats list dropped (legacy-page chrome or a stale padding in the takeover), so content width differs. Measure both surfaces via Inspector | **FIXED** (#290) — 16px body inset + 8px rows vs the chats list's full-bleed 12px. Damir's call: keep the inset, add the missing CARD surface to `.c-contacts__list` so it reads as intent (a full-bleed alignment was built first and reverted) |
| iOS-31 | After ACCEPTING a contact request: bottom-nav Chats badge keeps 1 unread, and the chat row stays unread even after entering + exiting the chat | **[XPLAT — REGRESSION-CHECK #283]** The iOS-8/iOS-11 fix did not clear it on device. Hypotheses, in order: (a) the "Contact Accepted" system message lands AFTER the user leaves the chat → unread legitimately re-increments and the next open SHOULD clear it but doesn't (re-check the push routing); (b) UIHelpers.setContactStatus from SingleChatPage.loadMessages routes to a page instance that isn't the live home WebView; (c) FE navUnreadTotal counts a request remnant (request row not dropped from state.requests on accept-from-chat path). Reproduce with the Inspector console open on home.html. **Windows cross-check 2026-07-29 (#286 session): does NOT reproduce** — same flow clears both badges on the #285 tip → the shared-C# legs (recount push / struct-copy cache) either do not trigger in this flow or are masked by a structural flush; treat as iOS-specific/timing until the sim says otherwise (#215: live inspection BEFORE building the three-site fix) | **FIXED + DEVICE-CONFIRMED 2026-07-29** (#292) — three real bugs, all readable in-tree, exactly as the run sheet predicted. **(a)** `SingleChatPage:1270` pushed `getUnreadMessageCount()` (a re-derivation from message flags, and a requestAdd is never markable-read) instead of the literal `0` it had just written to metaData → the "zeroing" push re-asserted the badge. **(b)** `contactStatusCacheItem` is a **STRUCT**; `HomePage:2484` copied it, mutated the copy and never wrote back, so an existing cache entry never updated and `updateContactStatus` kept pushing the ORIGINAL values. **(c)** the `timestamp > cacheItem.timestamp` predicate silently dropped every `ts=0` display-only push (which is exactly what the zeroing/presence pushes send) → now applied as a live unread/online update that keeps the entry's own excerpt/timestamp |

**Backlog (not a bug):** WALLET SYNC STATUS — IXICore already exposes the wallet's
block height; with the network block height the shell can compute + show sync
progress (wallet hero/status slot). Damir 2026-07-27: wanted. Queue as a wallet
batch row (BE data is THERE — this is FE wiring + a C# push).
| iOS-32 | Phone ran HOT during the first device session — cause not yet separated (charging + install + Debug-interpreter build vs Spixi's own P2P workload) | **[PLAT — STANDING REQUIREMENT, Damir 2026-07-27: battery/thermal PARITY WITH LEGACY SPIXI is a ship gate.]** Triage order: (1) re-measure UNPLUGGED on a normal use session → Settings → Battery per-app share + background activity; (2) re-measure on a RELEASE (AOT) build before judging — Debug runs the Mono interpreter, several × the CPU; (3) if it persists, profile: HomePage/Node update tick loops on device, presence/block-height polling cadence, background-suspend behaviour of the redesigned pages vs legacy SPowerManager handling, OneSignal. Compare side-by-side against legacy Spixi on the same phone (it's reinstallable from the App Store) | OPEN — watch row, measure before fixing |

## Fourth pass (2026-07-29, Mac/Cowork session) — iOS-33+

| # | Finding | First read / suspected root cause | Status |
|---|---------|-----------------------------------|--------|
| iOS-33 | Long-press on a message opens the sheet but ALSO auto-selects part of the bubble text; and partial text selection is impossible | **[PLAT]** WKWebView runs its own long-press gesture (selection + callout) at ~500ms — exactly `LONG_PRESS_MS = 500` (message-menu.js:25) — so both fired and the selection handles ended up behind the scrim. No `user-select`/`touch-callout` rule existed on `.c-bubble` at all | **FIXED** (#290) — native selection suppressed on the menu's own targets, TOUCH surfaces only (`:root:not([data-desktop])`); desktop keeps drag-select. Partial selection: **whole-message Copy** per Damir's "what's the best UX" — matches iMessage/WhatsApp/Telegram/Signal, none of which offer in-bubble partial selection. A "Select text" menu row is the opt-in if wanted later (needs a `cursor-text` icon export) |
| iOS-34 | Lag switching bottom tabs; should be instant since every tab is pre-rendered | **[PLAT]** The swap itself is a pure `hidden` toggle, but `bridge.send` is a synchronous `location.href` (native.js:48) whose navigation-policy delegate runs in the SAME task as the click handler — the paint queued behind that native round trip, and behind whatever C# does on the verb (loadTransactions/loadApps) | **FIXED** (#290) — tab verb and `ixian:settings` both ride `deferToPaint` (rAF + timeout; FIFO preserves tap order). Residual cost is C#'s per-switch data re-flush, which is a BE call, not FE |
| iOS-35 | Pinch in a chat does a browser page zoom (raster blur, chrome scaled off-screen) instead of enlarging the chat TEXT the way Chat appearance does | **[PLAT]** chat.html's viewport meta carried no scale clamp, so WKWebView owned the pinch gesture entirely — the text-scale intent was never implemented | **FIXED** (#290) — viewport scale-clamped + a 2-touch handler mapping the gesture onto `--chat-text-scale`: the SAME variable, clamp (0.8–1.5) and storage key (`spixi.chat.textscale`) Account → Chat appearance owns, so pinch and picker are two controls over one preference and persist identically. Damir sim/device eyeball owed |
| iOS-36 | Group info shows an Address card + QR — but a private group has no wallet address; the value is a session id and the QR resolves to nothing | **[PROD/XPLAT]** The address card was suppressed only for BLIND groups; the premise is wrong for every group | **FIXED** (#291) — address card + QR suppressed for `kind === 'group'` outright; 1:1 and bot surfaces keep it |
| iOS-37 | Group opened from the contacts directory has no Message action — the surface dead-ends | **[XPLAT]** contact_details forces `context:'chat'` for groups (so the title reads "Group info"), and the component gated Message on `context === 'contact'`; the shell never passed `onMessage` for groups at all | **FIXED** (#291) — Message renders for a group whenever `onMessage` is supplied; the shell supplies it unless the surface was entered from inside the chat. Title unchanged |
| iOS-38 | Group creation: the "Select at least 2 people" hint disappears at the 2nd selection and the whole contact list jumps upward mid-tap | **[XPLAT]** `minHint.hidden = n >= GROUP_MIN_MEMBERS` collapsed the line box — a reflow at the worst possible moment (row moves under the finger) | **FIXED** (#291) — the line stays mounted and swaps text instead: the rule while unmet, "{n} selected" once met. Same height, no reflow, and role="status" makes it an SR announcement |
| iOS-39 | Wallet → Request → after entering an amount, the contact strip shows loose rows (not the card used everywhere else) and cuts off after 5, forcing you to type to reach anyone | **[XPLAT]** `cap = needle ? 8 : 5` with no scroll container | **FIXED** (#291) — card surface + scrolling box (max-height 264px); cap raised to 50 as a DOM-size guard only, so search is now a shortcut rather than the only route |
| iOS-40 | Account tab switch lags noticeably (better than before, still not instant) | **[PLAT]** Unlike chats/wallet/apps, Account is NOT an in-page tab: it pushes SettingsPage, a separate ~1.6MB WebView that parses and boots from scratch on every open. The #290 deferral removed the bridge-hop stall but not the WebView cold start | **FIXED (#315)** — iOS-46 route (a): the narrow-mode SettingsPage PARKS on close and re-presents warm; from the 2nd open on there is no construction, no shell reboot, no data re-flush. First open stays cold (costed dial). F5 owed |
| iOS-41 | Should there be an explicit "Select text" affordance in a message? | **[PROD]** iOS-33 gave long-press to the menu (whole-message Copy), matching iMessage/WhatsApp/Telegram/Signal. Partial in-bubble selection has no gesture left | OPEN — Damir call. Needs one `cursor-text` icon export + a string key if wanted |
| iOS-42 | About → Terms of Use / Privacy Policy render PLACEHOLDER copy, not the real agreed documents | **[PROD]** `TERMS_DEFAULT` / `PRIVACY_DEFAULT` in launch-shell.js are summary stand-ins (#169); the real documents were never supplied to the repo. Onboarding shows the same placeholders | OPEN — blocked on the real text from Damir. Drop-in: `strings.termsBody` / `strings.privacyBody`, or replace the two constants (English-only by #169; `# ` heading / `- ` bullet / `[label](https://…)` markup supported) |
| iOS-43 | App-invite card: the "Launch ..." button label is clipped | **[XPLAT]** Button label overflow in the typed-bubble action row | OPEN |
| iOS-44 | Composer ⊕ opens the attach sheet over the composer; it should present as a sheet with the composer kept pinned above it | **[PROD]** WhatsApp/Telegram grammar — the attach sheet is a continuation of composing, not a departure from it, so the composer should stay visible and usable above the sheet. Requires reworking the overlay stack (the sheet currently owns the full scrim and the composer sits under it) | ~~DEFERRED~~ **UN-DEFERRED (Damir 2026-08-10, device F5): build it — the sheet must present UNDER the composer (composer pinned + usable above it), not overlay everything (screenshot on record). Headlines the full-sweep session (#323)** |
| iOS-45 | About / onboarding Terms of Use + Privacy Policy show PLACEHOLDER summary copy (supersedes iOS-42's framing) | The real documents are **not in this repo**: no file, nothing in `git log -S`, not in `src/strings/`, not in the legacy `Resources/Raw/lang/*.txt`. `TERMS_DEFAULT` / `PRIVACY_DEFAULT` (launch-shell.js) have been summary stand-ins since `da72e812`, whose own text said "the full document ships with localization". launch-spec §2.1 describes clauses that were drafted (§3.3 minimum-age, the hybrid-PQ encryption clause) but that text never landed in the tree | OPEN — Damir to supply the two documents. Drop-in: replace the two constants (keeps them OUT of the locale pipeline, which is what #169 English-only requires). Markup supported: `# ` heading · `- ` bullet · blank line separator · `[label](https://…)` |
| iOS-46 | Make Account a true peer of the other tabs — preloaded, no back icon, instant | **[ARCH — costed, not built]** Today Account is NOT an in-page tab: `ixian:settings` pushes **SettingsPage**, a separate ~1.6MB WebView that cold-boots (parse + bundle eval + boot pushes) on every open — that IS the iOS-40 lag. Two routes: **(a) CHEAP, ~80% of the win** — keep SettingsPage but stop re-constructing it: hold one warm instance and re-present it, and hide the back affordance when it's presented tab-style. No shell merge, no verb moves, reversible. **(b) FULL peer tab** — merge the settings shell into `home.html` as tab4. Costs: every SettingsPage verb (~15: save/apply/avatar/appearance/language/lock/backup/encpass/downloads/deletes…) moves onto HomePage's navigation handler · unwinds the #240/#243/#245 Account-as-peer-PANE desktop work outright · home.html grows by the whole settings surface · **and a security question that needs BE sign-off: home.html already hosts the chats list AND the wallet tab; adding backup / delete-wallet / change-password to the same document widens the blast radius of any injection in chats-list excerpt rendering. Not a §1 breach (the conversation stays its own WebView, #221) but it is a real widening.** Recommendation: do (a) now, treat (b) as a post-BE-cutover architecture decision | **BUILT (#315, route (a))** — park + instant re-present (mobile; wide keeps #245), hub back arrow REMOVED everywhere, landtab consumption now rides onSettingsClosed (deterministic on iOS). Route (b) stays a post-BE-cutover decision. F5 owed |
| iOS-47 | Wallet + Account topbar titles render in a different typeface than the Chats/Apps titles | **[XPLAT]** `topbar.css:49` applies `font-family: var(--font-display)` (Sora) to `.c-topbar[data-variant="root"] .c-topbar__title` — i.e. to ANY title text in a root-variant topbar, not just the wordmark. #226/B1 is explicit that only the WORDMARK keeps Sora and all other UI is system-ui, so the root-variant tabs are the ones that are wrong, not wallet/account. Fix = scope the display face to the wordmark element (`> span`, topbar.css:59 already targets it) instead of the title | **FIXED (#314)** — exactly as pinned: Sora scoped to a `.c-topbar__word` class only the logotype sets; the plain root titles (Apps; desktop Chats) drop to the system face per #226/B1. F5 owed |
| iOS-48 | "Connecting to Ixian Platform..." is truncated in the topbar; wants shorter copy + an animated ellipsis so it reads as live | **[PROD/XPLAT]** The M16 (#252/#59) topbar title-state swap. Copy comes from the legacy `global-connecting-s2` carrier, so shortening it means either a new shorter string or overriding at the swap site; the animated "…" is a CSS step animation on a trailing span (must respect prefers-reduced-motion) | **FIXED (#314)** — the push is only recognized; the title renders short i18n copy ("Connecting"/"Povezovanje") + aria-hidden CSS-animated dots (reduced-motion: steady); system face while connecting (data-connecting). S2-vs-DLT copy distinction deliberately collapsed. F5 owed |

## Device F5 — 2026-07-29 (iPhone 15, post-#290/#291/#292)

**CONFIRMED FIXED on device:** iOS-27 crash (contact request in foreground no longer quits) · iOS-31 unread badge (row + tab both clear) · chats-list rubber-band header · QR sizes · group info (no address/QR, Message present) · group-create hint · chat-open speed (marginal, accepted).

| # | Finding | First read / suspected root cause | Status |
|---|---------|-----------------------------------|--------|
| iOS-49 | Scan: the OS camera permission sheet now appears and can be allowed, but the preview stays BLACK — nothing scans | **[PLAT]** Permission is no longer the blocker (#291's WKUIDelegate fixed that half). Remaining suspect is inline playback: on iOS a `<video>` stream only renders inline when the WKWebView's `AllowsInlineMediaPlayback` is true AND the element carries `playsinline`; otherwise the element stays black. `AllowsInlineMediaPlayback` must be set on the WKWebViewConfiguration **at construction** — assigning `platformView.Configuration.*` after `base.CreatePlatformView()` is a NO-OP, since the configuration is copied at init. **Do not guess whether MAUI already sets it** (#215): with Web Inspector on scan.html, check (a) any getUserMedia error in the console, (b) `document.querySelector('video')` → `videoWidth`/`videoHeight` (0 = no frames, non-zero = frames arriving but not painted), (c) whether the element has `playsinline`. Candidate fixes: construct our own WKWebViewConfiguration with `AllowsInlineMediaPlayback = true` + `MediaTypesRequiringUserActionForPlayback = None`, and/or force `playsinline`/`muted` on the html5-qrcode video element. **#301 UPDATE — the candidate C# fix is a VERIFIED NO-OP, do not build it** (a smoke assertion now fails the run if `AllowsInlineMediaPlayback` appears in the handler): MAUI's `MauiWKWebView.CreateConfiguration` (dotnet/maui `release/10.0.1xx`) already sets `AllowsInlineMediaPlayback = true` + `MediaTypesRequiringUserActionForPlayback = None` on every WKWebView this app constructs, and the vendored html5-qrcode already sets `playsInline` + `muted` on its video element. The Inspector check this row asked for was never run — the #301 scan probe now runs it ON-SCREEN at every scan start (video WxH/ready/paused · track live/muted · frame black?) plus a fail-soft `play()` re-kick. Next F5 either scans or names the failing layer: track MUTED = WebKit suspended capture natively → C#/WebKit config territory; video 0×0 = inline rendering refused despite both flags; live video + black frames = canvas readback; no diagnostic line + no scans = decode-side (read the `[scan-probe]` console lines) **#304 ROOT CAUSE (2026-08-04, live Inspector): NOT playback at all — the html5-qrcode CONSTRUCTOR stamps `style.position="relative"` inline on the feed container, beating the stylesheet's `absolute/inset:0`; the feed collapses to 0×0 in the centered flex camera and the library sizes its video (0px) + decode canvas (0×0) from it. Camera/track/readback measured healthy throughout. Fix: remove the inline stamp per start + `!important` video fill (scan-page.js/scan-shell.css).** | **FIXED (#304)** — device F5 of the fix owed; then A8 zoom observable |
| iOS-50 | Wallet → Request contact strip reads cramped after the round-1 fix | **[XPLAT]** The card treatment added a second surface INSIDE the request box, stacking its padding on the box's own; `.u-scroll` added a reserved gutter on top | **FIXED** (#293) — card + `.u-scroll` reverted; the strip keeps only `max-height` + `overflow-y` (the scroll was the actual fix; the raised 50-item cap stays) |
| iOS-51 | Scan asks for camera on EVERY visit — WebKit's OWN per-origin prompt ("Allow "Spixi" to use your camera?"), overlaying our loading consent card | **[iOS] ROOT-CAUSED on-device 2026-08-07 (#309): `WKWebView.UIDelegate` is a WEAK ObjC ref and `iOSWebViewHandler` kept no strong root → GC reaped `MediaCaptureUIDelegate` → WebKit fell back to its default prompt, and file:// origins get no persisted WebKit camera grant → prompt every visit.** A live delegate auto-grants (AVFoundation already authorized) so this UI is unreachable while it exists — the prompt itself was the proof of death. Same weak-ref hazard on `SecureNavigationDelegate` = security-review **MAJOR #7** (a collected nav delegate silently drops the http/https block). | **FIXED (#309)** — strong `_navigationDelegate`/`_uiDelegate` fields on the handler; device F5 owed |
| iOS-52 | **C-9 storage probe VERDICT (same-page leg): `file://` localStorage PERSISTS on iOS** — across ScanPage relaunches AND app kills | **[iOS] Evidence (2026-08-07 device round, #308 probe):** the WebKit dialog popped UNPROMPTED on entering Scan = the #305 auto-enter fired = `spixi.scan.granted` was read back after an app relaunch; the on-screen probe line was correctly absent (it paints only when the grant is MISSING). The "consent every visit" was iOS-51, never storage. → the big spixi.*-dead escalation does NOT fire; drafts/pins/mentions-seen/declined-apps/chat-prefs presumed persistent on iOS (same class). | **CLEAR (same-page)** — cross-WebView leg (backup stamp, ll_settings↔ll_scan) still unproven → runsheet C-9 Inspector variant, low priority now |
| iOS-53 | **Composer focus: the chat topbar disappears, reappears at MID-SCREEN, then travels back up** — fast but noticeable/bothersome (Damir, device F5 2026-08-10) | **[PLAT — the iOS-6/29/#294/#303 keyboard family.]** Suspects, in order: (a) WKWebView's own focus PAN moves the document while the #303 native `__setKbInset` push + the FE settle ladder re-anchor `position:fixed` chrome — the topbar repaints mid-transition; (b) the vv-resize handler's first early fire (the #303 class) re-runs layout against a half-settled viewport. ⚠ #294 LESSON IS LAW: three keyboard attempts have failed here; NO fix without a LIVE Inspector measurement first (topbar getBoundingClientRect + vv.height/offsetTop + scrollY sampled through the focus transition), and the fix must be a NEW lever, not a re-tune | **FIXED + DEVICE-CONFIRMED 2026-08-10 (#324, "works perfectly")** — kb-probe traces proved a NATIVE transient pan (DOM geometry pinned at 0; winScroll/vvScroll settled-0; rAF starved). Fix = KVO contentOffset pin on the WKWebView scrollView during the keyboard window (SpixiContentPage, armed in WillChangeFrame before the inset push, released WillHide/detach; chat-only + docked-width guards) + 280ms UIKit-curve margin slide + stickDuring(340) bottom-tracking. Falsified on the way: `interactive-widget` (WebKit rejects) · FE pre-lift at focusin (decision predates DOM focus) — both reverted. Probe retired with the fix |
| iOS-54 | Connecting-state is home-WebView-only — an open CHAT (or any pushed subscreen) shows no connectivity state (Damir 2026-08-10; #322 covered the three home tabs) | **[BE-small]** HomePage.showWarning pushes only to the home shell; routing the same push to live overlay WebViews (SingleChatPage etc.) is a small C# fan-out + a per-shell title-state hook (chat topbar sub-line is the natural slot) | OPEN — sweep row (C# leg) |
| iOS-55 | Wallet tx-row timestamps render UNTRANSLATED under a non-English app language (chat-row timestamps translate) — Damir, device F5 2026-08-10 | **[XPLAT — root-caused in-tree #324]** C# pushes a PRE-FORMATTED time string: `addPaymentActivity` (HomePage:2074) → `Utils.unixTimeStampToHumanFormatString` (Utils.cs:30) → `DateTime.ToString("dd MMM, yyyy, h:mm tt")` under the .NET culture, which never follows the APP language; chat rows format in-shell (timestamp.js, docLocale + dictionary). This is the #182 `timeText` accommodation ("C# time = pre-formatted string, not epoch") coming home. Fix shape (small C#+shell co-change): push the raw epoch — shell numeric-detects (old-exe string passes through verbatim = backcompat) and formats via the existing `formatTxTimestamp`/docLocale path = the SAME translation machinery as chat rows. Sweep `wallet_sent.html`'s setData time arg at the same time (same class) | OPEN — sweep row |
| iOS-56 | iOS left-edge back-swipe does NOTHING in a chat — should navigate back to the chats list (Damir, device F5 2026-08-10) | **[PLAT]** Chat is OVERLAY-presented (#225), not a NavigationPage push, so there is no system interactivePopGesture to inherit and the WebView eats the pan. Fix shape (small C#, iOS-only): `UIScreenEdgePanGestureRecognizer` (Left edge) on the platform view of overlay-presented redesigned pages → route through the SAME back path as hardware/topbar back (respects composer locks + the overlay close-audit; never a raw pop). v1 = trigger-on-edge-swipe (non-interactive); the tracked-finger interactive variant is a later dial. Scope: chat first; the class generalizes (contact details, settings sublevels, scan) once proven | OPEN — sweep row |
| iOS-58 | In-app language change FLICKERS — every live page visibly reloads (both iOS + Android, Damir walk 2026-08-11) | **[XPLAT — architectural]** `ixian:language` → reloadAllPages → every WebView regenerates + reboots in place (white/boot flash per surface). Directions, in preference order: (a) STAGED reload — reload each page HIDDEN behind its current paint and swap on ixian:onload (the #199/#251 cover grammar, generalized); (b) live re-localization — push the new dictionary into booted shells (`setStrings` + re-render) and skip the WebView reload entirely for shells that can re-render (components read strings at render time; audit which surfaces hold non-rerenderable state); (c) accept one flash on the PICKING surface only, stage the rest. Needs a design round before code — touches the same reload machinery as theme flips (#251), fix both at once | OPEN — design round, then build |
| iOS-59 | Settings SUB-SCREENS keep the bottom nav visible (chat appearance · how-to · about · contributors · delete data) — should be hub-only (both platforms, Damir walk 2026-08-11) | **[XPLAT — shell]** The #315 peer-tab settings shell renders its own bottom nav so the Account HUB reads as a tab — but drilled-in sub-views keep it painted. Fix shape: nav visibility keys off the settings shell's currentView (hub → nav shown; any sub-view → hidden, back affordance owns the exit); wide/pane mode unaffected (#245 layout has no in-shell nav) | OPEN — small FE |
| iOS-60 | Chat-appearance pattern labels (Off/Subtle/Standard/…) OVERFLOW in longer locales — render weirdly (Damir walk 2026-08-11) | **[XPLAT — i18n/UX]** The segmented options size to their longest translated label. Dials for Damir, cheapest first: (a) shorter label drafts per locale (sl-si "Standardno" class — pick tighter words); (b) fixed segment width + ellipsis/smaller type (reads badly for opaque labels); (c) REPLACE text with a visual intensity selector — pattern swatch tiles (the value IS visual; kills the i18n problem structurally, needs no new strings). Recommend (c) as the real fix, (a) as the quick patch | OPEN — Damir dial |
| iOS-61 | On the ACCOUNT tab, the chats unread badge never shows in the bottom nav — visible from every other tab (both platforms, Damir walk 2026-08-11) | **[XPLAT — the iOS-54 class]** Account is its OWN WebView (#315 peer tab) drawing its OWN nav replica; the unread count is pushed to the HOME WebView only (setUnreadIndicator → home.html navUnreadTotal), so the settings shell's nav is blind by construction. Fix = the SAME small-C# fan-out iOS-54 needs for showWarning: route the unread push to the live settings WebView too (+ a setNavBadge hook in the settings shell's nav) — build BOTH in one S4 round (one fan-out mechanism, two consumers: connectivity + unread) | OPEN — S4 rider |
| iOS-62 | Entering a chat sometimes lands NOT fully scrolled to bottom — file-transfer bubble / call bubble bottom below the fold (Damir walk 2026-08-11) | **[XPLAT — late-height class]** The boot render pins the bottom, then async bubbles GROW after the pin (file-transfer state, call bubble, media placeholders) and push content below the fold. Fix shape: post-boot re-pin — while the log is at-bottom during the boot settle window, late height changes re-assert `scrollTop = scrollHeight` (a bounded ResizeObserver on the log or a settle-window re-pin; kin of the #328 stickDuring but boot-scoped) | OPEN — small FE |
| iOS-63 | Pending-contact strip "Waiting for X to accept your request" UNTRANSLATED (Damir walk 2026-08-11) | **[XPLAT — i18n]** The #287 pending-strip copy — either the key never entered the pipeline or locale drafts are missing. Fold into the S5 residue pass (`build-locales --todo` will name it) | OPEN — S5 rider |
| iOS-64 | Draft indicator appears in the chat-list row only ~10s+ after leaving the chat (both platforms; observed on Android walk 2026-08-11) | **[XPLAT — latency, not loss]** The draft lands in localStorage on exit (flush on pagehide); the home shell only re-reads drafts at its next STRUCTURAL re-render (C# flush cadence) — nothing nudges the list when a chat overlay closes. Fix shape: on overlay-close (the existing onOverlayClosed C# hook → a home push, or the home shell's visibility/mutation retry machinery) trigger a rows re-render so the draft excerpt paints immediately | OPEN — small FE/C# |

## #334 status sync (2026-08-11 overnight build — device verify = the 2026-08-12 checklist)

iOS-54 — premise DISPROVEN in code (SingleChatPage pushes showWarning + is ticked; the s2 relay warning is structurally suppressed under `Config.enablePushNotifications=true`, only total-disconnect can show in chat) → device REPRO before any C#: open a chat, toggle airplane mode, does the topbar sub show anything within ~4s? · iOS-58 ✅ v1 built — LIVE re-localize on the pick surface (setLocale push, no self-reload; home/chat/EmptyDetail keep invisible-under-overlay reloads; ONE desktop-pane flash = residual; full home live-swap = follow-up) · iOS-59 ✅ built (sub-screens hide the peer-tab nav, non-desktop) · iOS-60 ✅ built (swatch tiles, labels → aria) · iOS-61 ✅ built (SettingsPage.updateScreen value-latched unread → Account nav replica; connecting-state on Account deliberately deferred to the iOS-54 verdict) · iOS-62 ✅ built (bounded abortable boot re-pin) · iOS-63 ✅ built (waitingForAccept drafts ×6) · iOS-64 ✅ built (shouldRefreshContacts on both fromChat branches).

## Device F5 2026-08-12 (Damir, iPhone AI15, iOS 26.6) — #336 round

PASS on device: iOS-54 connecting-state · keyboard (smooth, no topbar dip) · scan decode · Account unread badge · language live re-localize · swatch tiles · bubbles wider · file-send Cancel · member-sheet request + friendlier titles · draft excerpt · waiting-strip translated · edge-to-edge · edge-swipe back IN CHAT · chat-opens-at-bottom · **avatars (new + existing + full-size viewer) — the X1 iOS win confirmed** · native alert dialogs read clean on iOS (unlike Android's — AND-24 is Android-only).

| # | Finding | Status |
|---|---------|--------|
| iOS-65 | **Full-size media/avatar viewer: the top-left ✕ doesn't work + is hard to spot.** Root: the close sat in a top bar with no safe-area inset → on iOS edge-to-edge it's UNDER the status bar (not tappable). | **FIXED (#336)** — close moved to a prominent bottom-centered button (56px, visible circle) clear of the notch + home indicator (safe-area insets); top bar keeps only caption/Save with the top inset. media-viewer.js/.css. |
| iOS-56b | **Edge-swipe back does NOT work in SUBSCREENS on iOS** (settings sublevels, contact details, etc.) — only in CHAT (iOS-56 was chat-scoped). Damir wants it everywhere a subscreen is open. | OPEN — next session. iOS-56's shell edge-swipe lever (chat.html) needs generalizing to the other overlay-presented shells (settings sublevels, contact_details, app_details, etc.) OR a shared native `UIScreenEdgePanGestureRecognizer` on overlay-presented pages routed through the same back path (the #325/#326 machinery; #294 measure-first). |
| iOS-66 | **Account screen SNAPS to the bottom on a language change** — not premium. Suspect: the setLocale live re-render (#334 iOS-58) rebuilds the hub and the scroll lands at the bottom (interaction with the #336 hub-scroll-restore, or the re-render reflow). | OPEN — next session. Check setLocale's renderLayout path vs the #336 hubScrollTop restore; the pick surface should keep its scroll at TOP after a re-localize. |
