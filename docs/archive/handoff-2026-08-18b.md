# Handoff — 2026-08-18b (N12 + N40 TRIAGED **and BUILT**). READ FIRST.

**LANGUAGE RULE: ASD-STE100 Simplified Technical English.** See `CLAUDE.md`.

Supersedes `docs/archive/handoff-2026-08-18-post-n4.md` (consumed; it was renamed
on archive because an OLDER, unrelated doc already sat at `handoff-2026-08-18.md`).
★ Planning doc: `docs/master-worklist-2026-08-17.md`.
Open tally: **~25 N-items** — N13 closed, **N12 and N40 built** today (3 fix-first
+ N60/N61 · 15 round items · 9 dials) + the §B carryovers (D-9 · D1+N6 · I-1 ·
I-9 · I-10 · I-12) + D-17b.

**★ #380 SESSION BUDGET MODE stands.** Sessions are scoped SMALL. One Opus
review round, only when C#/money/data paths changed. Smoke as BOOKENDS. No
agent fan-outs. If an item balloons: STOP at a triage doc + plan, hand off.

---

## 1. What this session did

Started as triage-only. Damir closed N13 mid-session ("the backup on onboarding
works") and asked for the restore case instead; both items were then triaged AND
built. **One commit-ready batch: #381 · #382 · #383.**

⚠ **C# changed → wipe obj/bin before the build.**

| Item | Outcome |
|---|---|
| **N13** | **CLOSED** — works on device. The #334 L3/AND-5 wiring is live (`OnboardPage.xaml.cs:41-48` → `BackupPage.backupAccount()`). Struck from the worklist |
| **N12** | **BUILT (#383).** A restored account no longer gets a backup nudge: the onboarding tail opens on **"Join the community"** (the join step STAYS — Damir's dial) and the 30-day reminder clock is seeded at the restore. Created accounts unchanged |
| **N40** | **TRIAGED (#382) then BUILT (#383).** Connectivity and the update notice were the two arms of one `if/else`; the hourly version check (`Config.cs:47`) made the offline state unreachable once an update was advertised. Connectivity now runs first and unconditionally; both surfaces may show (Damir's dial); the notice is dismissable per session |
| **Free find** | **`build-demo-bundle.mjs` was DEAD in the working tree** — 8 component sources are CRLF and the import stripper is LF-shaped. Any component change would have failed the local rebuild. Fixed at the generator (same fix as the smoke harness's #340 normalisation) |
| Review | ONE Opus adversarial round → **PASS**, 5 MINOR / 5 NIT. Landed: an invisible focusable Dismiss control · a failed-restore path that burned a *created* account's first reminder · **a C# latch of mine that re-created the #357 strand class** · the "" belt · the worker-order fix. Logged, not built: NIT-9 (banner lives in tab1) · NIT-10 (one benign "Unknown localization key" log line on the 4 non-onboarding launch pages) |
| Gates | bundle 262 exports · 22 shells · i18n-lint ✓ · NUL clean · **smoke 1971 / the same 4** (was 1947; +24 pins, all mutation-proven in two batched runs) |
| Docs | DECISIONS #381/#382/#383 · `docs/n40-triage-connecting.md` · worklist · security gate §#383 · this handoff · `docs/f5-checklist-2026-08-18-n12-n40.md` · consumed docs archived |

## 2. ★ Damir — the short list

1. **F5 this batch** — `docs/f5-checklist-2026-08-18-n12-n40.md`. Short: 5 restore
   legs + 9 connectivity legs + 2 spot checks. **Wipe obj/bin first.**
   ★ **N40 caveat, from Damir's F5 screenshot:** the served file advertises
   **0.9.22 — exactly the real build number**, so on an unmodified build the
   compare is 0 and the update arm NEVER wins. **M1 therefore cannot explain the
   symptom Damir originally reported** (unless that device was on an older
   build). The fix stays correct and necessary — it bites every user the moment
   0.9.23 ships — but the original D-21 observation is NOT yet proven fixed.
   **Leg B6 is now decisive:** revert `Config.cs` to 0.9.22, go offline, leave
   the app running past an hour. If "Connecting…" disappears again, the live
   mechanism is M2/M3/M4 in `docs/n40-triage-connecting.md` §3, and §4 step 4
   (compare home against the chat screen) splits them.
   ⚠ §B needs to know whether an update is advertised: open
   `https://resources.ixian.io/spixi-update.txt` and compare with `0.9.22`
   (`Config.cs:44`). Above it → B1 is testable today. At or below it → B1 needs a
   build with a lower version, and the rest of §B still tests fine.
2. **★ Run the N57 protocol** (`docs/n57-triage-group-visibility.md` §2) — still
   Core-side, still the top build-unblocking action. Then send the BE engineer
   be-cutover `[Q1-ESC]` + `[NT1]` + the N57 verdict row.
3. **N60:** keep one configuration per test device; keep watching every Android
   deploy.
4. **Native review of the 5 machine drafts** — `docs/n4-review-notes.md` +
   `src/strings/draft/<code>.json`.

## 3. Dials

**Answered today (both are BUILT to the answer):**

* **N12 — does a restoring user still see "Join the community"?** ★ YES. The
  backup step is skipped, never removed.
* **N40 — may an update notice and the offline state show together?** ★ YES,
  and the notice is dismissable + returns after a restart.

**Standing (unchanged):** the launch language set (#378) · N56 wash values ·
N61 backup scope · contact_new post-pop feedback · blind-bubble avatar
photos (#372) · N48 chip on non-blind heros (#370) · ru/lt legacy dashes
(#371, broad sweep) · N3 voice-sweep target list · the A2 fallback ·
zh→cn-cn C# map (#378 rider).

## 4. NEXT SESSION — the queue

Budget mode: pick ONE, two at most.

1. **N10 · N15 · N39** — the triage items deferred from the #380 scope. Same
   deliverable shape as `docs/n40-triage-connecting.md`: verdict or protocol,
   build only if the mechanism is named and small.
2. **N63 — the English-fallback tail** (NEW, Damir F5 2026-08-18: the chat
   "Show older messages" pill is always English). Small, visible, well-scoped:
   **10 keys are untranslated in ALL 7 older locales** (de/fr/es/pt/ru/sl/sr);
   the 5 N4 locales already have them. Per-locale tails on top (de 26 · fr 35 ·
   es 20 · pt 24 · ru 16 · sl 18 · sr 18 of 708). ⚠ a few are legitimately
   identical in some languages — needs a human eye, not a script. Full row +
   the key list in the worklist. Strings change → the FULL locale pipeline
   including `i18n-overflow-audit`.
3. **N64 — the update-notice design round** (NEW, Damir F5 2026-08-18). #383 made
   that strip render for the first time and it shows four problems: it is
   chats-only by accident (an app-level notice inside `#chats-view`, while
   connectivity reaches all three tabs) · **it instructs "please update" and
   offers no way to do it** — the biggest flaw, and a CTA means a new outbound
   verb + a Damir dial · it wears the warning role for neutral information · the
   copy is ~100 chars of which ~40 are filler (fold into N63's copy round).
   Full row in the worklist. ⚠ ② needs a security-gate row.
4. Then the rounds **R3 → R4 → R7 → R6 → R8 → R5 → R9** per worklist §F. N61
   rides R4; N12's remaining half (the community bot post-restore, address
   check) opens R4.

**Do not touch:** N57 (Core-side, protocol pending) · the §5 deferred pile ·
anything in §6 of the archived 17g handoff.

**Worth one cheap session on its own:** the 4 known smoke pre-existers
(#136 · #149③ · M5 · B3) are real and unrelated; they have been carried since
#290–294.

## 5. How to work — unchanged

Cloud twin recipe: clone/tar → checkout the batch commit → `npm install
jsdom --no-save` → bundle BEFORE shells → smoke green at **1947/4** (state
the number when it moves). String-change pipeline: extract → build-locales
→ build-strings-iife → (bundle → shells) → verify-locales + i18n-lint +
pseudo + i18n-overflow-audit. Standing set: verify plans against code
(#215) · #294 · mutation-honest pins, PROVEN · Damir commits · DECISIONS
rows at decision time · the #46 loop on Opus (#352) when C#/money/data
paths change · the security handover gate while building (the #381–#382
section is the docs-only template) · tarball delivery (plain `tar xzf` in
PowerShell; the mount cannot unlink; `git --no-optional-locks` always).
Ixian-Core reference: `097341a`. Deploy: wipe obj/bin → Android `dotnet
build Spixi\Spixi.csproj -f net10.0-android -c Release -t:Run` · Windows
two-step: `dotnet build ... -f net10.0-windows10.0.19041.0 -c Debug` then
run the exe (⚠ `-t:Run` does NOT chain the build on Windows).

⚠ **This batch DOES need a build and an F5** — C# changed in `HomePage.xaml.cs`,
`LaunchRestorePage.xaml.cs` and `LaunchCreatePage.xaml.cs`, so the obj/bin wipe is
REQUIRED. The generated artifacts (bundle · 22 shells) are already rebuilt here;
rebuild locally only to confirm. **Bundle BEFORE shells, always.**

**New standing gotcha (#383):** a green smoke run does NOT prove the GENERATORS
work. `build-demo-bundle.mjs` had been dead in the tree for an unknown number of
sessions because 8 component files are CRLF. If a batch touches components, run
the bundle build early and read its output.

## 6. The deferred pile + do-not-re-attempt

`docs/archive/handoff-2026-08-18-post-n4.md` §5 and §6 stand verbatim (they carry
17f/17g forward). N13 leaves the §5 pile — it is closed. N40 stays in the
pile only until the §2 check answers it.
