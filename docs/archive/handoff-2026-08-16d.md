> ⚠ **SUPERSEDED 2026-08-17 by `docs/handoff-2026-08-17b.md`** (named -17b: the archive already holds an OLD handoff-2026-08-17.md from 2026-08-13 — read dates, not names). The #356-#360 F5 RAN: D-20/I-2/I-6/2.3b PASS; D-19 display dial and D-17 FAILED — reworks specced in the new handoff. Archive this file with the next batch.

# Handoff — 2026-08-16d. READ FIRST.

**LANGUAGE RULE: ASD-STE100 Simplified Technical English.** See `CLAUDE.md`.

Supersedes `docs/handoff-2026-08-16c.md` for state. That file carries a superseded
banner; archive it with the NEXT batch. This batch archives `handoff-2026-08-16b.md`,
`f5-checklist-354.md` and `f5-checklist-351.md` (checklist §1 has the `Move-Item` lines).
⚠ The archive-name warning stands: `docs/archive/handoff-2026-08-16.md` (no letter) is an
OLD file from 2026-08-13. Read the date in the header, never the file name.

★ **ENVIRONMENT: Damir is back on the WINDOWS PC** (pulled `c9ec975e` from the Mac
session). Checklists are PowerShell again. The cloud twin stays where builds and smoke
run. The 2.3b Windows leg is now testable (checklist §7).

---

## 1. State — verify it, do not trust it

```
git --no-optional-locks log --oneline -3
git --no-optional-locks status --porcelain
```

At session start the tree was CLEAN at `c9ec975e` (pushed; the Mac session's #354–#355
batch was committed by Damir with the D-18 + AND-38 F5 PASSES — table A row 0 consumed).
This session produced ONE UNCOMMITTED batch, delivered as `spixi-360.tar.gz`: rows
**#356–#360**.

| Item | Value |
|---|---|
| Smoke | **BASELINE OK — 1703 pass / the 4 KNOWN pre-existers (#136 · #149③ · M5 · B3)** — +32 pins vs 1671, every one mutation-verified (and two of this session's own pins were caught defective by their own mutations: a vacuous ICU canary and a self-contradicting culture-gate clause — both fixed, both now proven) |
| Compiler | ⚠ **NEVER COMPILED** — `SingleChatPage.xaml.cs`, `HomePage.xaml.cs`, `Utils.cs`, `WalletContactRequestPage.xaml.cs`. Damir's build is the first pass |
| Device | 🟡 F5 owed per `docs/f5-checklist-360.md` (Windows) + two A52 legs logged there |
| Loop (#46/#352) | r1 = THREE parallel Opus auditors (1 CRITICAL / 8 MAJOR-class / ~14 MINOR+NIT) → all CRITICAL+MAJOR fixed, cheap MINORs applied, rest logged. r2 = fresh Opus break-my-verdict (2 MAJOR — the paste route re-opening the r1 CRITICAL, and the culture gate — both fixed + pinned + mutation-proven; 5 MINOR/3 NIT applied or logged). r3 = targeted mutations, green. PRODUCT-CLEAN at r3 |

## 2. What landed this session

1. **#356 — D-19 SOLVED (Damir's live report).** Bot-room rows with a destroyed sender
   no longer impersonate the GROUP ("Spixi …p Chat" + group avatar + group name as a
   copyable IXIAN ADDRESS). Root cause verified end to end: Core 0.9.8k `5643e5b` nulls
   the sender address at STORE time (the D-14 commit) — **BE question 1 ESCALATED** in
   `docs/f5-findings-2026-08-15.md` (identity loss is permanent per stored row; every
   day it ships corrupts more history). Our whole delta was audited before blaming
   Core: the resolution pipeline is byte-identical to legacy; the impersonation display
   was ours and is fixed (empty slot + "Hidden member" + neutral avatar + nick-keyed
   runs + copy-path attribution + an NRE guard the audit found lurking beside it).
2. **#357 — D-20 BUILT (Damir's second live report).** The offline "Connecting…" state
   survives the language re-bake: the latch resets on every document birth AND onLoaded
   pushes an unconditional clear (the flag-only reset had a queue-race hole, loop r2).
3. **#358 — I-2 BUILT** (table A row 1). Selected-chip outline ladder, contrast now
   EXECUTABLE in smoke (six pairs, both modes, live token resolution).
   ★ Dial rider: the selected press keeps its fill (checklist §4.3).
4. **#359 — D-17 BUILT** (table A row 2). Apps header born-unknown ghost + watchdog.
5. **#360 — I-6 BUILT, APP-GLOBAL** (table A row 3). Locale digit grouping: inputs
   group as you type (caret-safe), all display surfaces + C# alert bodies agree, the
   #77 wire untouched and its pins green. ★ The loop caught a CRITICAL in my own first
   cut (mid-edit round-trip mangled magnitudes) and a MAJOR in the second (paste path)
   — the r2 record in DECISIONS #360 is the honest history.

## 3. THE PLAN — table A continues (Damir's rule holds)

| Next | Item | Note |
|---|---|---|
| 0 | **The F5 on `docs/f5-checklist-360.md`** | First compile + the five device proofs + 2.3b (§7) |
| 1 | **D-5** — relation arg on addContact/addMember | C# here; = AND-30 |
| 2 | **D-9** — close delete-account on device | Throwaway account |
| 3 | **★ D1 reply-to** | Two-device id round-trip FIRST; the body marker is hostile under the gate |
| 4 | I-1 + welcome flicker · I-10/I-9 (measure first) · D-4/D-15 honest halves · D-1 part B · D-7 + I-11 (one locale round) · I-3 design round | Then the security sweep LAST |

Also queued: the AND-31…AND-37 landscape round · AND-35 renames dial (locale round) ·
AND-38 compact-balance leg · flick-cancel re-test · **I-12** (dial talk; the honest
raise is the #298 prepend) · **AND-39** (clarify the phase first).

**Table B is TEN entries of weight on question 1** — the D-19 escalation makes q1 the
urgent one. Parked, do not build against it. **A5 and CI7 stay BLOCKED.**

## 4. Known residuals logged this session (do not re-derive)

- I-6: IME `insertCompositionText` takes the settled inverse (A52 leg, checklist §6.8).
- I-6: C# alert grouping is plain 3s — an sl alert can say "1.500" where the FE says
  "1500" (CLDR min-grouping). Cosmetic, alert-only; rides the next locale round.
- I-6: the 5 languages with no FE dictionary render en-convention EVERYWHERE now (the
  C# culture gate matches the shell). Full support = a dictionary + add the language to
  the Utils.cs gate, together.
- D-17: a first apps push later than ~1.1 s re-creates ONE bounded appear-vanish (the
  watchdog trade — strictly better than the unbounded hole it closes).
- D-19: a NAMED-but-address-less sender's tap opens nothing (no address to show). The
  member-sheet dead-end dial is Damir's when it starts to itch.
- D-20: a stale "Connecting…" title under a long-lived update banner is pre-existing
  and untouched.

## 5. How to work — unchanged

The cloud twin recipe, build chain, tarball delivery: as in 15c §7 (in `docs/archive/`),
PowerShell rules again (⚠ no `--overwrite` to bsdtar · the mount cannot unlink · bundle
BEFORE shells). The standing set: verify plans against code · #294 · #215 (Ixian-Core is
NOT in this repo; READ it in the cloud twin at `097341a`) · smoke green at **1703/4** ·
mutation-honest pins, proven · Damir commits · DECISIONS rows at decision time · the #46
loop on anything substantial · Opus for every adversarial pass (#352) · the security
handover gate while building (nothing in this batch adds a verb, a key, a sink, or a
fetch; the D-19 half REMOVES an information leak — the group name styled as an address
with a copy affordance). Suggested commit message:
`D-19 honest sender, D-20 connecting latch, I-2 chip outline, D-17 apps ghost, I-6 locale grouping (#356-#360)`.

## 6. Do not re-attempt — grew by three

All of 16c §5, plus:
- **Do not re-interpret a MID-EDIT amount string with the settled heuristic** (pattern
  tests on "1,2345"-class strings). That was the r1 CRITICAL; the per-edit inverse
  routed on pre-edit emptiness is the design. Touch `amountInputToCanonical` only with
  the #360 pin set green and re-mutated.
- **Do not give the selected chip's press a tonal-pressed fill** without re-running the
  executable contrast pins — 200-on-500 in dark is 2.31:1.
- **Do not "fix" the 5 dictionary-less languages by widening the C# culture gate
  alone.** The shell would still render en; that re-creates the r2 mixed-convention
  MAJOR. Dictionary + gate move together.

## 7. Open device items

| ID | Finding | State |
|---|---|---|
| D-19 | Sender-less bot rows | ✅ built (#356) · 🟡 F5 §2 · Core loss stays until BE q1 |
| D-20 | Connecting… vs language change | ✅ built (#357) · 🟡 F5 §3 |
| I-2 / D-17 / I-6 | This batch | 🟡 F5 §4–§6 (+ chip-press dial, §4.3) |
| 2.3b residual | Windows tonal-hover on selected row | OPEN — F5 §7, full obj/bin wipe FIRST |
| Flick-cancel · compact-balance · AND-39 · I-12 | A52 / dial items | OPEN — unchanged from 16c |
