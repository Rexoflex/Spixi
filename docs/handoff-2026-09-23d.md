# Handoff — 2026-09-23 (d) → the OFFICE iPhone day (after Session AE lands and walks)

**Read this first (#215).** Then DECISIONS #938–#943 and `docs/opus-review-verdict-session-ae.md`. This file
supersedes 09-23c as the entry point; 09-23c stays live until Session AE is COMMITTED (it holds the order
Damir gave for the round), then goes to `docs/archive/`.

## 0 · State of the tree you inherit

* Session AD is committed (`a664d59f`). **Session AE is UNCOMMITTED and UNWALKED** — it was built in the
  container and delivered as a tarball because Damir turned the PC off before landing (device bridge down);
  landing = unpack over the repo folder, then `docs/f5-checklist-session-ae.md` §1 (bundle → shells → suite),
  F5 + Android deploy, the walk `docs/walk-artifact-session-ae.html` (19 rows), ONE commit
  (`docs/commit-message-session-ae.txt`). **No C# changed in AE** — nothing to compile, no `obj`/`bin` wipe.
* Suite: **BEFORE 4848 → AFTER 4862 / the 2 KNOWN (+14)**, container, snapshot, Core sibling @ `097341a`
  (#895: deltas in one environment). Four `--check` gates green. `cs-syntax-check` untouched (no C#).
* `CHAT_KB_CEIL` 646 → **658** (measured LAST, after the fourth round — #943's rule); headroom 1 528 chars.
* The iOS-only code (#912 ②, #919 the push extension, #920 Catalyst vendoring, #490's iOS line) has STILL
  never compiled — the office is its first compile. The Apple portal is DONE (#932; iO.1 = P).

## 1 · What Session AE did (so the walk reads right)

| item | outcome | walk |
|---|---|---|
| the Windows Debug freeze | **DIAGNOSED, not fixed, nothing changed in the app** (#938): a caught NRE in Ixian-Core `receiveData:823` on the member's leave receipt after the contact is already removed; the "freeze" was VS's first-chance break. BE row **CORE-13**. | AE.1 (Continue → resumes) · AE.2 (Android: the log line) |
| the ⊕ tray partial on Android | `height: max(slot, content)`, content measured live (#939) | ★ AE.3 on a FRESH `spixi.kb.slot` · AE.4 |
| the fade behind the composer | per-row masks at the BAR's live rect, Damir's pick **A** (#940); a lifted row is unmasked; iOS slide tracked | ★ AE.5 · AE.6 · AE.6b (iOS, office) · AE.7 · AE.8 (residual: the pill pops) |
| the bot-room Delete gate | one predicate `canDeleteRec` across menu / bar / belt (#941) | ★ AE.9 · AE.10 · AE.11 |
| AD.13 | CLOSED as designed — one tip per sender per message is Core's rule (#942) | AE.12 (the note is the answer) |
| AD.19 / AD.20 / AD.23 | re-walk rows carried | AE.13 · AE.14 · AE.15 |

## 2 · The next session's ITEM 0 — the fifth reader (#943)

The loop ran four rounds on Opus and every round was NOT CLEAN; round 4's fixes are pinned and
mutation-killed but **no fifth reader has read them**. Before ANY build: one fresh Opus break-my-verdict
reviewer over the r4 delta ONLY — `src/shells/chat.html` (`setChatMode`: `mode.answeredFor` before
`chatSelect.refresh()`; `setInset`: `composerFadeTrack(340)` on an inset CHANGE), `attach-sheet.js` (the
removed redundant `unwatchTrayContent(existing)`), `scripts/smoke-test.mjs` (the tracking fixture's `setRaf(7)`
+ the statement-level `composerFadeNow()` clause + `CHAT_KB_CEIL = 658`). Write the verdict into
`docs/opus-review-verdict-session-ae.md` §Verdict (a verdict not written back is a verdict nobody can find,
#660). If it is CLEAN, say so there; if not, fix, pin, mutate, and a sixth reader.

## 3 · The office iPhone day — `docs/walk-artifact-ios-office.html` (31 rows)

1. **iO.1 is DONE** (#932) — mark P. The two development profiles: install them (Xcode → Accounts, or
   double-click) before the build.
2. **Wipe `obj`/`bin` → build Debug for the iPhone.** ⚠ This is the FIRST COMPILE of `Spixi-PushService/`
   (`SpixiPushGate.cs` · `NotificationService.cs`), `Platforms/iOS/SPushPrefsShare.cs`, `SPushService.cs`'s
   iOS changes, #912 ②'s `NSURLIsExcludedFromBackupKey`, and #490's iOS line. **A build error is a Session
   AC/AD bug — paste it whole into the chat; do not guess at it.** Known first suspects are in
   `docs/ios-nse-spec.md` and DECISIONS #919/#921 (the extension's `SupportedOSPlatformVersion`; the App
   Group id string must be IDENTICAL in both Entitlements; `OneSignal_app_groups_key` in both Info.plist).
3. **§1 is the reason for the day**: iOS notifications on par with Android, and **iO.7 the mute test** is the
   one question no document could answer (nothing, or a blank row? — `UNNotificationServiceExtension` can
   only MUTATE, never cancel; Suppress = rewrite to EMPTY). Record the answer as the note.
4. §2 (iOS-43/44/55 — already BUILT, #905, never WALKED on an iPhone) · §3 the smoke of every surface · §4
   landscape (#918/#922 on iOS: the bar stays, the rail refuses) · **AE.6b** rides along (the fade through the
   keyboard slide) · §5 optional: APP-1 restore · route B (#920) LAST and only if the iPhone walk is done —
   ⚠ the first Mac run is the `dlopen` test the July stamp never reached.
5. Sessions I → AE have never been on an iPhone. Anything that reads wrong on the device is a walk NOTE
   first (which row, what you saw, a screenshot), a fix second (#935).

## 4 · Then, unchanged

* **The endgame** (#916 as shaped by #933): L6 (its own mini-session, security-gated) → freeze
  (`maxLogCount 5 → 1`, retire the instruments — `[CDPERF]`, `[SCROLL]`, the `chats-after-close` probe) →
  sweep (`docs/audit-refactor-plan.md` phases 1–4) → the strip (ONE branch, three comment tiers, the workshop
  docs to a private `spixi-workshop` repo, DECISIONS rewritten by area, a `pre-strip` tag) → the
  introduced-vs-inherited gate re-run → merge → TestFlight (the extension's DISTRIBUTION profile then).
* Their own sessions: batch 4's medium rows (CI6 · C22 · A8 · FC1 · C12 · NT1(b) · PV1) · the Android
  30-minute memory kill (`dumpsys meminfo`, `SpixiContentPage.Dispose()`, `CHAT_SPARE_ENABLED = false` as the
  free discriminator) · the translator pass · the 123 comment-strip regexes.
* LATER VERSIONS: #934 (a) · #936 · C14 · C20 · L8 / L2 · CORE-9/-10/-11/-12/-13 (Core's).

## 5 · Still open, unchanged (do not re-derive)

AND-47 · AND-48 · `docs/diagnostic-session-aa-findings.md` (armed; AA.8 never reproduced) · counsel on the
privacy policy · S16 (BE) · the #930 dials · the #940 residuals (the reaction pill pop under the band; an
in-place row-height change keeps its mask until the next pass; own UNSENT message in a bot room — Delete
dead, pre-existing) · AE.6b (iOS keyboard slide) · CORE-13 (new).

## 6 · The workshop

The suite runs in the container on a SNAPSHOT copy with the Core sibling (~5 min); the mutation harness
root MUST carry `docs/legal` or the bundle build fails silently and "survivors" are the unmutated bundle
(#906, three false survivors this session) — assert the build before reading a kill. Landing by tar when
the bridge is down: unpack over the repo folder, `sha256sum -c`, then the pipeline; never `git add -A`.
Scratch runners `scripts/_*.tmp.mjs` are gitignored and were NOT shipped. Damir owes locally: `git rm
--cached crash-logcat.txt mem-*.txt` + gitignore; delete `_to_delete/`.

## 7 · Rules this round paid for

#215 · #294 · #660 · #663 · #771 · #798 · #811 · #882 ③ · #895 · #906 · #926 · #929 · #935 ·
★ #892/#893 → the fade was the THIRD shape at the composer's level, and PIN-B (no stacking context on the
  scroller) is what forced the per-row form — a pin that forbids the cheap way of drawing something is the
  one that keeps another feature honest ·
★ #943: **the ceiling is measured LAST** — `CHAT_KB_CEIL` was re-based twice mid-loop and overtaken twice by
  the loop's own docblocks · a fix a round blesses is the next round's headline; the loop ends on the round
  that finds nothing, and this one has not had it yet.

## 8 · Files

NEW: this handoff · `docs/f5-checklist-session-ae.md` · `docs/walk-artifact-session-ae.html` ·
`docs/commit-message-session-ae.txt` · `docs/opus-review-verdict-session-ae.md` · `docs/sheets/session-ae/`
(reference + tray + fade renders, the landed shots) · the prompt rewritten for the office day.
CHANGED: DECISIONS #938–#943 · `docs/be-cutover-brief.md` (CORE-13) · `docs/security-handover-gate.md`
(Session AE) · `src/components/attach-sheet.js` · `message-menu.js` · `chat-select.js` ·
`src/styles/components/attach-sheet.css` · `message-menu.css` · `reactions.css` · `src/shells/chat.html` ·
`scripts/smoke-test.mjs` · the built bundle + 18 shells.
Consumed → `docs/archive/`: `handoff-2026-09-23b.md` · `f5-checklist-session-ad.md` ·
`walk-artifact-session-ad.html` · `commit-message-session-ad.txt`. LIVE: `docs/handoff-2026-09-23c.md`
(until AE commits) · `docs/walk-artifact-ios-office.html` · `docs/diagnostic-session-aa-findings.md`.
