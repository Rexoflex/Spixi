# Handoff — 2026-08-07 (scan-close day, #307–#312) → next session

**Read this first.** Supersedes `docs/handoff-2026-08-04b.md` (consumed → `docs/archive/`,
with `handoff-2026-08-04.md` + `next-session-prompt.md`). `docs/f5-checklist-2026-08-07-scan.md`
= consumed, kept for the record (5 build rounds, one file).

## What closed today (all F5-confirmed on the iPhone, committed as ONE batch #307–#312)

- **Scan DECODE fixed (#307)** — min.js-verified root cause: with no qrbox the decode region
  maps PER-AXIS client→intrinsic ratios, so the r4 full-bleed feed stretched every frame
  1.41× → zero decode. Fix = aspect-locked contain-fit feed (letterboxed, the classic look);
  the qrbox fallback was proven UNSOUND before anyone built it. Bracket-filling QRs decode
  promptly. Speed dials (fps 10 · A8 min×2) deliberately untouched — measure before moving.
- **#309b staged-latch re-latch** — auto-enter starts the camera while C# still has the page
  STAGED; one silent stop+relaunch on a >1.25× post-'playing' box grow re-latches the decode
  region (the #46 r3 reviewer PROVED the no-restart residue clips a bracket QR's finder).
  Bed ResizeObserver + always-re-fit handle the geometry half.
- **C-9 CLOSED, both legs (#308/#311, iOS-52)** — the self-serve storage probe delivered
  `grant:1 · probe:23 · appearance:1 · write:ok`: file:// localStorage PERSISTS on iOS and
  crosses ll_* files. The spixi.*-dead escalation is DEAD; drafts/pins/backup-stamp/prefs
  are architecturally fine. The runsheet C-9 Inspector variant is no longer owed.
- **★ The consent saga root-caused: TWO C# bugs (#309–#312, security MAJOR #7)** —
  (1) WKWebView delegates are weak ObjC refs and were GC-collectable (strong roots added);
  (2) **MAUI's post-connect property sync RE-ASSIGNS UIDelegate** — the real reason WebKit
  showed its own camera prompt every visit since #293. Fix: `SecureNavigationDelegate.
  DecidePolicy` re-heals the UIDelegate on EVERY navigation (main-frame file:// load precedes
  page JS → deterministic). The permission delegate is now an explicit NSObject+IWKUIDelegate
  adopter with a hand-written [Export] + `[cam-perm]` observability. Permission UX is now
  platform-normal: iOS asks ONCE at the OS level; our card gates first-ever visit only;
  revoke in Settings → honest denied card.

## THE PLATE — Damir's 2026-08-07 test list (next session builds BOTH batches, his dial)

**Batch 1 — zero-C# polish (verify-first per #215 where marked):**
1. **Bottom-nav labels selectable** + **chat-row long-press pops the iOS text callout** —
   `-webkit-user-select:none` / `-webkit-touch-callout:none` sweep over interactive chrome
   (nav, rows, topbars). Keep message TEXT selectable (iOS-33/41 long-press copy).
2. **Mention pill doesn't wrap the whole nickname** — suspect: linkify splits the text node
   when the nick resembles a URL, mention highlight wraps only its fragment (message-bubble.js
   linkify ↔ mention ordering). Verify with a URL-looking nick first.
3. **"Settings saved" toast renders under the bottom bar** + **CTA button too low** —
   Damir has screenshots; GET THEM before fixing (safe-area/z-index class, #282 family).
4. **Screen-title fonts differ** = **iOS-47** (root cause already pinned: `topbar.css:49`
   `--font-display` on wallet/account titles vs secondary elsewhere — unify to one canon).
5. **"Connecting…" treatment** = **iOS-48** — shorter copy + animated ellipsis (the row
   already carries the direction; i18n the copy).
6. **How-to-use steps not translated** + **Contacts chips All/People/Groups: only "All"
   translated** — i18n gaps (Q3 class); sweep `createSettingsHowTo` + the contacts picker
   chips through extract-strings.
7. **Account → Backup row still shows the nudge badge** after a backup — check the
   `spixi.backup.last` stamp path end-to-end on iOS (write site settings_backup.html:424 ·
   read site settings.html; C-9 says storage works, so this is LOGIC, not storage).
8. **Wallet tx sheet = FULL detail (R6 DIAL LOCKED today: full detail IN THE SHEET)** —
   avatar + nickname + amount + fiat + status + time + txid/explorer, no navigation.
   Mind the data gaps (`addPaymentActivity` carries no address/fee — W-rows) and the
   global-hide mask rules (#285/#288 fail-safe pattern).
9. **Dev mode 10-tap activation missing** = iOS-22/R5 dev cluster — at minimum restore the
   10-tap-on-logo → dev entry (send-log needs its own look; R5 is the fuller row).

**Batch 2 — Account as a true peer tab (iOS-46, ARCH, costed-not-built + iOS-40 lag):**
preloaded in-page tab, NO back arrow, instant switch. C# (HomePage/SettingsPage hosting) +
shell. Read the iOS-46 row's costing notes before designing; the #245 desktop peer-pane
grammar is the desktop analogue, mobile needs its own shape.

**BE rows written today (not next-session work):** be-cutover **PA1** (password/biometric
payment-confirm gate — native, before signing) + **PV1** (privacy toggles that actually
work — per-toggle core signal suppression). Damir dial: rows now, FE groundwork later.

## Outstanding beyond the plate (unchanged carry-overs)

- Device-pass runsheet: **B1 (OneSignal delegate starvation) · B2 (double tap-handling) ·
  B3 (static-registrar Release gate)** — never run; C-9 is done. ⚠ The #312 delegate saga
  makes B1/B3 MORE suspect, not less (same registrar/delegate families).
- iOS rows open: iOS-42/45 (REAL Terms/Privacy documents needed from Damir) · iOS-43
  (Launch button clip) · iOS-44 (attach-sheet presentation) · iOS-41 (select-text
  affordance dial) · iOS-18 (multiuser app picker legacy design) · iOS-15 (decline-notify,
  BE) · iOS-31 (parked: sim inspection before code) · iOS-32 (heat, measure on Release).
- Still owed: >64k A7 paste · W1 es/ru · **A10 on WINDOWS** (rebased §A10 in
  parity-a-f5-checklist.md — decides part of batch B) · desktop scan re-verify · Windows
  keyboard sanity · the 4 pre-existing smoke failures (#136 · #149③ · M5 · B3-shell — own
  session) · scan speed re-judge (measure fps/zoom only if slow).
- Batch B: R6 is now LOCKED (full detail in sheet — being built in the plate); **R3
  (media-cap scope) + R4 (timestamp burn) dials still open** — ask Damir at next session
  start if batch B planning begins.
- Probe retirement: the on-screen storage line (#308) + `[cam-perm]`/probe C# diagnostics
  (#310–#312) did their job — retire/gate them in a cleanup pass once the consent fix has
  a few days of device time (keep the per-nav HEAL, retire only the console noise).
  The scan probe's `frame black` dark-scene false-positive (<16 threshold) rides the same
  cleanup.

## Rules (unchanged)

Zero-C# unless evidence forces it (#215 verify-first — it paid FIVE times today) · ★ #221
chat isolation · full pipeline bundle-BEFORE-shells · smoke baseline now **953 pass / the
same 4 pre-existing failures** · #46 loop after the batch (adversarial reviewer caught 2
MAJOR-class errors today) · DECISIONS #314+ (#302 retired, #313 = today's close) ·
`git --no-optional-locks`; commits = Damir in GitHub Desktop (#306) · obj/bin wipe before
device builds; verify the bundle after.

## Build recipe (device round)

```
rm -rf Spixi/obj/Debug/net10.0-ios Spixi/bin/Debug/net10.0-ios
dotnet build Spixi/Spixi.csproj -f net10.0-ios -c Debug -p:RuntimeIdentifier=ios-arm64 -p:CodesignKey="Apple Development" -p:ValidateXcodeVersion=false
# + the same with -t:Run -p:_DeviceName=AI15
grep -c "__setKbInset" Spixi/bin/Debug/net10.0-ios/ios-arm64/Spixi.app/html/chat.html   # ≥1
```
