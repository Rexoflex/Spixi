# Session handoff — chat-info fix round + WORKFLOW CHANGE (2026-07-05, session D)

Paste this into the new chat. `DECISIONS.md` is the source of truth; row **#142**
carries this session's detail.

## ⚠️ WORKFLOW — READ FIRST (supersedes the old gotcha playbook)

**Sandbox builds, staging trees, and sandbox e2e are DEAD.** The stale-mount
gotcha (bash sees stale content for file-tool-edited files — it's still live,
DECISIONS.md read 15 lines short this session) made in-chat verification cost
~20% of Damir's weekly tokens in one round. New loop:

1. Session writes code + smoke assertions ONLY (file tools, real files).
2. Damir runs locally and pastes the output:
   `node scripts/build-demo-bundle.mjs; node scripts/smoke-test.mjs`
   (PowerShell — no `&&`; run from the `Spixi` subfolder)
3. Failures → targeted edits, re-run. Visual review = Damir's demo pass.

Consequences: don't claim assertion counts before Damir's run; don't rebuild
`src/demo/spixi.iife.js` in-session (his build does it); bash is fine for
LISTING files and reading never-edited ones, never for verifying edited content.

## What was done (#142 — Damir's 5-item chat-info round, verified GREEN locally)

1. **Caps dead, lists scannable:** chat-info members + wallet-send picker
   render FULL A–Z sorted lists (body scrolls); search = pure filter
   (members ≥8; send always). `#136③` caps + "+N more" notes removed both
   places. Send flow safe: picking collapses the picker, CTA recipient-gated.
   Receive's request-contact strip intentionally untouched.
2. **Payments fixed + accordion:** root cause of the "weird components" =
   `chat.html` never linked `txlist-item.css` (rows were naked native
   buttons). Linked + static smoke guard. Payments now a collapsed
   "Payments (N)" toggle (QR reveal grammar, lazy) → 5 recent txlist-items +
   "View all {n}" → `onTxAll`. Body sections got `* + *` top dividers.
3. **Contact page:** `context: 'chat' | 'contact'` on `createChatInfo` —
   contact = "Contact info" title, Message (fill, `onMessage`) leads, Pay
   demotes to outline, delete-history + disappearing-messages stay chat-side.
   member-sheet `onViewContact` passthrough wired (demo: group → Han Solo's
   identity block → contact page).
4. **Destructive zone:** separated bordered rows, gap-12 (mistap guard).
5. **Disappearing messages:** per-chat row in chat info (Damir accepted rec
   over composer control) → radio sheet Off/1h/1d/1w, commit-per-pick
   `onSelfDestruct(seconds, ctrl)` latched; `capabilities.selfDestruct`
   gated — NO bridge command, §9 ask added to spec.

## Verify on your machine (mostly done — visual pass remaining)

Build + smoke: **GREEN (Damir ran it)**. Demo pass: chat.html 1:1 header →
sectioned info, payments accordion (styled rows, collapsed, View all),
disappearing-messages sheet, spaced danger rows; group header → full A–Z
member scroll + search filter at 9 members; member sheet (Han Solo) →
identity tap → CONTACT page (Message leads, no delete-history).

## Next session

1. Any fallout from Damir's visual pass on this round.
2. Remaining figma-sweep screens: account shell (backup-ux-spec.md ready) ·
   add contact · settings hub · scan · lock/onboarding · desktop passes ·
   wallet safe-area (#22). The contacts shell should OPEN the new contact
   page (`context:'contact'`) — it's ready for that.
3. 🟡 standing flags (carried): copy-morph honesty sweep · shared money
   module (`canonicalAmount`/`toUnits`) · media/card rows in multi-select ·
   B2 glyph exports · §9 asks (now incl. self-destruct window command +
   both-peer expiry enforcement · 1:1 mute · shared-media inventory ·
   presence · group name/avatar edit · room-wide request · tip fee ·
   share command · sendrequest payload · backup timestamp · Discover
   transport · history paging · tx fee in payload).

## Key files

`src/components/chat-info.js` (contexts, accordion, sd sheet, no caps) ·
`src/styles/components/chat-info.css` (dividers, accordion, danger cards,
setting row, sd options) · `src/components/wallet-send.js` (no-cap A–Z
picker) · `src/demo/chat.html` (txlist-item.css link, contact-page wiring,
7-tx demo, captions) · `scripts/smoke-test.mjs` (#142 blocks + css guard) ·
`docs/chat-info-spec.md` (contexts, §5/§7/§8/§9 rewrites, §9 ask) ·
`DECISIONS.md` #142.
