| #865 | **★★ THE ICONS WERE NOT CLOSED ON DISK — TWO WAYS AN SVG ARRIVES WRONG, AND THE SAME GATE CAUGHT BOTH (Session W, #215 verify-first).** Session V closed the icons as "82 in the frame, 91 in the registry, `--check` green" and asked Session W for the suite line before treating it as done. The line never came (Damir had not run the paste), so §0 was run in the container against the staged tree instead: **`generate-icons --check` exited 1 before it could compare** — the four glyphs pulled by URL after the second Figma export went to the wrong folder (`alert-small layout-grid message-plus phone-end`) were NOT node exports. Each carried the page (`<rect fill="#ECEEF0">`), the "Icons, avatars" canvas paths (`fill="white"`, `fill="black" fill-opacity="0.1"`) and an `id` on every `<g>`/`<path>`; the content gate rejected them on the `id`, wrote nothing, and the registry on disk (17:24) predated the four files (18:44) — so it still shipped their PREVIOUS versions and nothing was red anywhere Damir could see. ① **Mechanism: a Figma MCP asset URL renders the node WITH its ancestors.** The frame export is the single source (#862); a glyph pulled by URL is a picture of the glyph on its page. Cleaned to the one `<path d fill="#131415"/>` every frame export has — measured, not assumed: (b) 86/86 `#131415`, (c) 61 measurable all **1.50** (`layout-grid` is a closed shape, unmeasurable by rule — it had read 4.00 only because the canvas rect's corner arcs were being measured), (d) 97 paints all `currentColor`, (e) clean; `--check` then named exactly the 4 stale registry lines, regenerated, green. ② **The bridge stamps a C2PA content-credentials `<metadata>` manifest (7 774 bytes) into ANY SVG it writes to Damir's disk** — recognised by content, so a `.svg.txt` name did not dodge it — and reported the writes as "rejected: changed since stage" while the bytes were already on disk (the mtime guard tripping on its own write; re-staging is the only honest read, #215 again). The generator refused those too: `tag <metadata>` + `content outside an element tag` — **fail-closed on a payload nobody had listed, which is the whole argument for that gate (#862)**. SVG bytes therefore travel ZIPPED: `docs/icons-fix-session-w.zip`, hash-verified on the round trip, `Expand-Archive -Force` in the paste; the registry (`icons.js` + `icons.iife.js`, plain JS) landed byte-identical directly. ⚠ Two rules, both derived: **a glyph enters the repo only by exporting the frame or the node from Figma, never by asset URL**; **an SVG reaches the disk from a session only inside an archive, and every committed file is re-staged and compared before it is called landed**. | The brief said the icons were closed and not to reopen them; §0 said verify the mechanism first. Verifying cost one `--check` run and found the tree red in a way the suite would have caught at Damir's step 3 — which is fine — but the registry would have kept shipping the old four with no one the wiser until then, and the second defect (the manifest) was created by the fix itself. Both are the class of thing that only a byte-for-byte gate sees | ✅ fixed in the container, registry landed · 🟡 Damir: the Expand-Archive lines, then `--check` |
| #866 | **★★ THE DOODLES TILE IS RETIRED FROM `chat-pattern.css` — #857's "single largest removable item", built, measured (#294).** `generate-chat-pattern.mjs` no longer reads `chat-bg-doodles.svg`, no longer emits `--chat-pattern-uri-doodles` / `--chat-pattern-size-doodles`, and the DOODLES DRIFT GUARD, `DOODLES_SCALE` and `scaled()` are gone with the asset they existed for — the generator now reads NO file (`readFileSync` is not even imported): the one tile left is the seeded-PRNG data matrix. **Measured: `src/styles/chat-pattern.css` 254 358 → 15 133 bytes (−239 225, −94.05%); release form (comments stripped) 252 234 → 13 513.** The matrix URI is **byte-identical** before and after (diffed), the `:root` default and the `[data-chat-pattern='matrix']` block are unchanged, so no screen changes by one pixel — the only thing that stops happening is 234 KB of data-URI CSS being fetched and parsed by every chat and settings WebView boot, and shipped once in the APK as `spixi.chat-pattern.css` (#858's "4.6 MB → 4.4 MB in release form"). The generator is deterministic (two runs, one hash). **Pins: 8 re-based in BOTH halves (#861)** — the "emits BOTH tiles" pin, the drift-guard mechanism pin, the E1 negative (widened to the doodles URI/size/scale), the AUG-TILE 220×383 pin, the DOODLES_SCALE pin, the #853 negative (now sweeps URI AND size for all four retired styles), the Session F "#835: still EMITTED but not selectable" pin (now: neither, and the word does not occur in the stripped sheet) and the "generator reads the DOODLES export" pin (now: reads no export). Verified on the harness: 16/16 green on the new tree, **8/8 red on the committed tree**; mutations — a doodles URI re-emitted (aliased to matrix) · the asset read again without emission · a doodles SELECTOR back in the sheet · a doodles SIZE variable back · a real declaration outside a comment — **5/5 killed**; a sixth, the word and the declaration form inside a COMMENT only, correctly does NOT fire, because the three negatives that had swept RAW css (inherited from the #690 pin) were moved onto stripped css (#771) — a retirement note in the header must not trip the pin that records the retirement. **Four comments that the change made false were corrected in the same pass (#861):** settings-screens.js ("the doodles TILE still exists… flagged, not done here"), chat.html's head-script note (still said a mobile client "falls back to DOODLES" — stale since #835), settings.html's `<link>` comment ("generated doodle pattern"), message-bubble.css's canvas header; and the reachability gate's own comment, which named the doodles SVG as a corpus example that no longer exists there. ⓘ NOT touched: `settings-screens.css`'s base swatch `mask-size: 110px 191px` — the doodles tile's aspect surviving in settings CSS. It is reached only by the None tile, which paints at opacity 0, so it is invisible dead weight of the 2-line class; named here, left for the swatch's own pass. The three retired asset files (`chat-bg-doodles.svg` 237 KB · `chat-bg-pattern.svg` 378 KB · `doodle-pattern-aug.svg` 522 KB) stay in `src/assets/images` unreferenced — they are not shipped (the reachability gate covers `Raw/html/images`, and nothing copies them there), and deleting artwork is Damir's call, offered as one `Remove-Item` line. | #835 retired the selector and deliberately left the URI emitted because the drift guard owned the asset; #857 then put a number on the leftover (233 KB, 94% of the sheet, selectable by nothing). Once the number existed, "pipeline change, not a dial" stopped being a reason to wait — the pipeline change is thirty lines removed and a guard that guarded nothing | ✅ built · 🟡 Damir: `generate-chat-pattern` in the paste rewrites the committed bytes (deterministic — proves it on his machine), `build-shells` re-copies the shipped sheet; the asset deletion is his line to keep or drop |
| #867 | **★ #858's SECOND REMOVABLE ITEM — THE ~368 KB OF SHELL-CSS DUPLICATION — IS NOT A DELETION; IT IS A TRADE, AND #345 ALREADY TOOK THE OTHER SIDE OF IT ON A MEASUREMENT. Read, not built.** What the 368 KB is: each of the 18 shells inlines the component stylesheets IT links (`build-shells` → `inlineHtml`), so the same `button.css`/`topbar.css`/… bytes recur across shells — 926 KB inlined against a 559 KB source tree. What removing it would take: a seventh EXTERNAL beside the six #345 emits (`spixi.components.css`, every component sheet concatenated) and the shells linking it. What #345 measured before choosing inline: on Android `generatePage` re-reads, localizes and base64-encodes the whole document on EVERY navigation, LINEAR in size at ~0.08 ms/KB — that is why the six SHARED payloads went external (chat.html 2 019 KB → ~370 KB) and why "per-shell CSS and per-shell page script stay INLINE — they are not shared, so externalising them would only add requests". The duplication #858 found is real, but it is duplication ACROSS shells, not within one; a shell only ever pays for its own copy. Externalising it would save ~368 KB of APK and per-shell `generatePage` bytes (a few ms per open at 0.08 ms/KB) while making EVERY WebView boot fetch and parse the union of 63 stylesheets instead of its subset — and cold WebView boot on the main thread is exactly the stutter class #803 measured at 130–230 ms and #864 is queued against. Both sides are small and both are guesses until timed; under #294 that is a measurement (`[CDPERF]`-style, one shell, inline vs external, same device) before it is a change, and under #860 it is not this session's to run. | The brief filed it under "pure deletion, measured", and the read says the first word is wrong: nothing here is dead, it is the same live bytes in several places, and the mechanism that put them there was a measured decision. Recording that stops the next session from "cleaning up" a deliberate trade | 📋 read · deferred behind a measurement · not a bloat item |
