# Handoff — 2026-07-30 (Mac/Cowork audit session) → next build session

**Read this first.** Supersedes nothing — `handoff-2026-07-29d.md` stays live for its build
gotchas + the ★ v1.0 scope section. Post-freeze rules still apply (`handoff-post-freeze.md`).

## What happened this session

The **#295 mandated legacy-parity audit ran and is DONE** → **`docs/legacy-parity-audit.md`**
(#296). Read-only; no code touched. 6 parallel auditors + an adversarial refutation pass over
full-tree snapshots (legacy baseline `0e85a4b8` vs HEAD `20089c10`). Verdict: the v1.1
deferral holds, BUT 13 parity regressions (nearly all zero-C#), 10 security rows pinned to
v1.0, C9's premise refuted, be-cutover-brief stale ×5. Details, evidence and batching live in
the audit doc — do not re-derive.

## State of the tree (verify before starting)

- HEAD `20089c10`, pushed. Uncommitted docs (this session + last): `docs/legacy-parity-audit.md`,
  `docs/fable-build-brief-parity-a.md`, `docs/handoff-2026-07-30.md`, DECISIONS #295+#296,
  CLAUDE.md tail lines, the #295 scope section in `handoff-2026-07-29d.md`.
  **Damir commits these as one docs batch BEFORE the next build session** (msg suggestion:
  `docs: v1.0 scope lock (#295) + legacy-parity audit (#296) + parity batch A brief`).
- `_to_delete/` + `_gitlock_trash/` at repo root: junk (incl. two audit tarballs). Damir
  deletes locally — the session bridge cannot delete mounted files. Never commit them.
- Owed from earlier, still open: local `smoke-test` + `pseudo-locale-smoke` for the #290–#294
  batch (green everywhere else) · Damir F5 of #291 foreground-push display + tap deep-link.

## Next session = PARITY BATCH A

Work order: **`docs/fable-build-brief-parity-a.md`** — 11 zero-C# items (A1 loadmore · A2
paid-bot cost · A3 in-chat unread badge · A4 presence render · A5 rating nudge · A6 bot
description · A7 64k guard · A8 scan zoom · A9 dual-app multi choice · A10 wallet Share ·
A11 backup nudge FE leg). All evidence pre-gathered in the audit doc; the brief has per-item
build notes, gates and the NOT-in-batch list. Full build pipeline + #46 loop + one commit.

## Damir dials owed (block batch B, not batch A)

1. **R3** media-cap flip scope: iOS-only (strict legacy parity) or all platforms?
2. **R6** mobile tx detail: keep the iOS-13 sheet-only dial, or add a "View details" row
   (audit showed fee/address/live-status are reachable nowhere on mobile)?
3. **R4** backup-timestamp burn: take the small-C# fix (write timestamp on ack, or clear the
   pref at the FE-fix commit)?
Also queued for Damir: C9 old-exe check (was bot tipping ever live in a shipped build?) ·
re-mark the 5 stale be-cutover rows (S5/L4, S7, S13, CO2 landed; C9 premise note) before any
BE handoff.

## After batch A (standing order, unchanged)

Batch B (the dial items + small-C# legs) → R5 dev cluster (v1.0 item 4) → Windows polish
(iOS-47/48/43/46-cheap) → Android bring-up → security engagement (audit §c + #234 + MAJORs,
one BE briefing) → send-flow redesign (v1.0 item 5, LAST). iOS-29 keyboard: do NOT attempt
without changing the lever (#294). Terms/Privacy + thermal parity remain Damir-side.
