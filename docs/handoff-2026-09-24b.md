# Handoff — 2026-09-24 (b) → Session AF walk + commit, then the OFFICE iPhone day

**Read this first (#215).** Then DECISIONS **#946–#961** and `docs/opus-review-verdict-session-ae.md`.
`docs/handoff-2026-09-23d.md` §3–§7 stay the live plan for the office day; this file supersedes
`handoff-2026-09-24.md` (moved to `docs/archive/`).

## 0 · State of the tree

* HEAD = `7f27e5f4` (#944 + the AE walk log). **Session AF is landed in the working tree, UNCOMMITTED and
  UNWALKED.** Landing order: `docs/f5-checklist-session-af.md` §1 (bundle → shells → suite) → F5 Windows +
  Android Debug deploy → the walk `docs/walk-artifact-session-af.html` (23 rows; also an Artifact) → ONE commit
  (`docs/commit-message-session-af.txt`).
* **C# changed** in HomePage · SingleChatPage · ContactDetails + all 13 `lang/*.txt` — F5 + the Android build
  are the first compile.
* Suite (container, Core sibling): BEFORE 4868 + 4 red (#944 had broken four CH6 pins) → **AFTER 4880 / the
  2 KNOWN**. Four `--check` gates · lint · pseudo 9/9 · verify-locales · cs-syntax 184 + 3 green. Expect your
  AE number **+18**.
* `CHAT_KB_CEIL` unchanged at 658 (chat.html gained two comment lines; the ceiling pin is green).

**Addendum (same day, after walk AF 16 · 0 · 7, #964):** #963 multi-select circles TOP-aligned (chat-select.css + chat shell;
re-walk: select a tall bubble → the circle is level with its first line). #962 Ixian-Core late-accept guard patched in the
sibling working copy, UNCOMMITTED there — BE adopts (`docs/core-patches/962-accept-null-guard.patch`); re-walk: the Debug
break is gone and `ixian.log` shows "Received an accept for a contact request that no longer exists; ignored." AF.17 pick
(LEAVE or B) → Damir picked B = #966 (pill lower + padding reserve). #965 language sheet tall + drawn scroll indicator.
Re-walk: open Account → Language on a small phone (indicator visible, sheet under the top bar) · a reacted bubble at rest
(pill clear of the text) and scrolled under the composer (pill fades, never cropped). Suite 4883 / the 2 KNOWN.

**Addendum 2 (walk AF2 15 · 0 · 2, #967):** dials built — (a) #967 dead undorequest handler deleted · (b) stacked kept ·
(c) #968 no row lines on chat info / contact details · (d) #969 "You:" in room rows. #970: a declined contact request
returns (Q18/RC1, BE or a local ignore list — Damir's call). Re-walk: a group where YOU sent last → "You: …" (and a
missed call → no prefix) · contact details → no lines between rows, lines only under the search field / payments toggle.

## 1 · What AF did

| item | outcome | walk |
|---|---|---|
| review: AE r4 | CLEAN (fresh Opus reader); one pin hole closed | — |
| review: #944 | NOT CLEAN → 7 rounds → CLEAN. ★ E-1 a BLIND room showed a nameless member's ADDRESS in the list (INTRODUCED) → fixed; E-2 four CH6 pins red → re-based | AF.10–AF.16, ★ AF.12 |
| ③ | Cancel request removed from the pending profile; ContactDetails' verb deleted; CORE-13 sibling (`receiveData:1260`) logged | AF.1–AF.3 |
| ④ | the admin-button rule was in chat-info.css (chat.html does not load it) → member-sheet.css, stacked | AF.4–AF.5 |
| ⑤ | no hairlines inside an Account card or between roster members | AF.8–AF.9 |
| ② | a second tip is refused before any payment dialog, with a true message (new lang key ×13) | AF.6–AF.7 |
| ① | the pill at the band: rendered NOW vs B, NOT built — Damir picks | AF.17 |
| build-batch loop | 10 Opus rounds, 0 MAJOR, CLEAN (#952–#961); design calls #953 / #959 | AF.20 |

## 2 · Dials Damir owes (AF.17 · AF.22)

(a) delete HomePage's `ixian:undorequest:<addr>` (no sender since #562) like #947, or keep · (b) admin buttons
stacked (built) or two-up · (c) extend "no lines inside a group" to Groups-in-common / Payments / the
contact-details action card · (d) a localized "You:" for own tails in rooms + the colon per locale · (e) the
pill: LEAVE or B (+ the select tick re-centre).

## 3 · Then

After the walk and the commit: **the office iPhone day** — `docs/handoff-2026-09-23d.md` §3 and
`docs/walk-artifact-ios-office.html` (31 rows). If AF's walk turns up fixes, they are a short AF-2 round first.

## 4 · Workshop notes this session paid for

* **A stale splice range makes every mutation look GREEN** (the block line numbers moved after an edit). The
  mutation runner must recompute ranges each time AND assert the pin under test actually printed.
* The full suite in the container needs an ABSOLUTE root path; three parallel runs got OOM-killed (exit 137) —
  run one at a time.
* `cs-syntax-check` needs `npm i --no-save jsdom eslint globals tree-sitter tree-sitter-c-sharp` in ONE call
  (a lone `--no-save` prunes the others).
* Landing to the PC: the tree was archived with `git archive`, built in the container, and copied back as a
  tar of changed paths; `git --no-optional-locks` on the mount.

## 5 · Rules this round paid for

#215 · #660 (a record that repeats an old status) · #771 · #772 (a pin's MESSAGE is part of the pin: every
clause it states must be enforced — #954) · #798 · #895 · ★ #930's rule used twice (#953, #959): when a
reviewer finds the same class twice, stop patching and question the design — both answers were "claim less,
let the render/walk carry the behaviour" · #943 (the loop ends on the round that finds nothing).
