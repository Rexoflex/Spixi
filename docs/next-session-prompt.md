Post-Session-AD — close the walk, then the road to the office (DECISIONS #928–#930 · #915 · #916)

0 · Before anything else (#215)
Read `docs/handoff-2026-09-23b.md`, then DECISIONS #927–#930, then `docs/f5-checklist-session-ad.md`.
Check `device_bash`; use `git --no-optional-locks status` (never plain `git status` on the VM). Verify
whether Session AD is COMMITTED (its 94-file delta on `de214173`; message in
`docs/commit-message-session-ad.txt`) and whether the walk sheet `docs/walk-artifact-session-ad.html`
carries results — CHECK DECISIONS before accepting any "owed" row from this prompt (#660). Run the four
`--check` gates on the real tree. The FULL suite runs in the container on a SNAPSHOT copy with the
Ixian-Core sibling (expect the AD number, 4848 / the 2 KNOWN, in the container; compare the DELTA
locally, #895). Reviews run on Opus — pin the model explicitly. Nothing here compiles C#.

1 · The FE fix round (the walk was 22 · 2 · 3, #935 — read it and handoff §5b first)
Build, shell-only unless the anchor says otherwise: #934 (b) the bot-room Delete gate (own or admin;
bulk delete under the same predicate) · the ⊕ attach sheet opening PARTIALLY until a keyboard round —
INSTRUMENT first (`--kb-inset` at sheet open), suspect the #918 cap on a stale inset (#770 class), no
CSS before the number · AD.13 a second tip refused — ask Damir which device and the alert text before
any code (the shell has no gate) · AD.19 / AD.20 / AD.23 re-walk rows. AD.14 is CORE-12, not ours.
The pins around W11 / CH3 / C17 / C2 are EXACT-FORM (#929 r3): a fix there goes red — re-base to the
PROPERTY, never to the new spelling; mutation-kill every re-base. Then a fresh break-my-verdict
reviewer on Opus over the round.

2 · The road (Damir's order, #927/#916 — the portal steps are DONE, #932)
The office iPhone day on `docs/walk-artifact-ios-office.html` (31 rows; the first compile of #919's
extension and #490's iOS line; the portal rows there are already done — #932) → the endgame (#916 as
SHAPED by #933: L6 → freeze → sweep → the strip = ONE branch, three comment tiers, the workshop docs to
a private `spixi-workshop` repo, DECISIONS rewritten by area → gate re-run → merge → TestFlight).
The NEXT FE FIX ROUND (before the office if there is a gap, else after) carries #934 (b): the bot-room
Delete gate (own message or admin) — shell-only, its own walk rows.

3 · Their own sessions, NOT this one unless Damir asks
Batch 4's medium rows (CI6 · C22 · A8 · FC1 · C12 · NT1(b) · PV1 — #927's batch 4, not reached) · L6
(restore destroys before verifyWallet — ours, security-gated, its own mini-session) · the Android
30-minute memory kill · the translator pass · the 123 naive comment-strip regexes.
★ L8 · L2/MAJOR #8 (wallet-password work) stay OUT. CORE-10 stays his.

4 · Records this session owes
DECISIONS rows for anything decided · the walk verdict written into `docs/f5-checklist-session-ad.md`
(then both it and the walk sheet → `docs/archive/`) · a new handoff + this prompt rewritten ·
`docs/handoff-2026-09-23b.md` → `docs/archive/` when consumed.
