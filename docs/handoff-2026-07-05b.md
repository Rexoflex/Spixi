> 🗄️ **STALE — superseded.** Historical only. Current entry point: **`docs/handoff-fable-next-batch.md`**. Decisions preserved in `DECISIONS.md`.

# Session handoff — Wallet slice 3 + Tipping + Chat money/multi-select (2026-07-05, session B)

Paste this into the new chat. `DECISIONS.md` is the source of truth; rows **#137–#139** carry this session's detail.

## What was done

**#137 — Wallet slice 3 (Receive/Request), built + audited.** ONE progressive surface
(`c-wallet-receive`): receive QR `address:ixi` on the `--surface-qr` card (+ new
`--on-qr` token, fixed dark both themes), FULL address chip w/ HONEST copy morph
(✓ only after clipboard resolves), Share via onShare (no legacy share cmd — §9),
"Request an amount" reveal → QR morphs in place to `addr:send:amount` + contact
strip → onSendRequest. QR = vendored qrcode-generator (`src/components/qr.js`,
module-scoped, **jsQR-verified round-trips**). Audit C1: `setRequestAmount(1e-7)`
would've become a 17 IXI request — fixed (+canonicalAmount at every payload exit,
state-held latch, focus-safe rows). Failed sends confirmed under "Sent".

**#138 — Chat tipping + a CROSS-CUTTING overlay fix.** `c-tipsheet` (#26-lite: sheet
IS the review, presets 1/5/10 + ghosted Custom slot, latched confirm carries the
amount). Bridge grounded: legacy `ixian:contextAction:tip:MSGID:AMOUNT` exists.
**Audit CRIT: `setOverlayOpts` on an OPEN overlay was a silent no-op** — every
"in-flight lock" (incl. wallet-send's) held on NO path. overlay.js now reads
dismissal policy LIVE (Esc/scrim/back), setOverlayOpts MERGES, dismissTopOverlay
consumes-without-closing when escDismiss:false. Balance guard in BigInt units
(`toUnits` exported). Damir polish: chat.html was missing chip.css (native-button
look + dark break — fixed + static smoke guard); roomier sheet, 44px amount chips,
CTA clearance, custom slot ghosted (no grow).

**#139 — Chat round.** ① Bug: bad-address error now under ITS input (was below the
contacts). ② Attach **Pay** → send screen takeover w/ **`lockedRecipient`** (new
createWalletSend opt — pre-picked peer, no ✕; groups keep the member picker);
attach **Request** → **`openRequestSheet`** (tip machinery generalized:
`openAmountSheet` kinds tip/request; request = NO balance guard) → request-out
bubble. ③ **Multi-select** (`chat-select.js`): menu Select → selection mode
(count bar, Esc exits), copy = single raw / multi "Sender: text" lines →
clipboard + buffer; **split-paste**: composer value-match → "Send as N separate
messages" offer (never auto-splits). Damir picked all three UX options.

**Verification:** `node scripts/smoke-test.mjs` → **99 assertions CLEAN** (includes
overlay-lock, BigInt-precision, ghost-slot, split-paste, bad-address regressions).
Bundle `src/demo/spixi.iife.js` = 164 exports, Read-verified tail.

## Workflow gotcha (STILL LIVE)

The sandbox mount serves TRUNCATED reads (new content, old length) to bash for any
file edited with the file tools this session. Working pattern: staging tree in
`/tmp/build` (mirror every edit via node string-replace patches — mind CRLF in
scripts/*.mjs + tokens.css), build+smoke there, `cp` artifacts back through the
mount (whole-file cp lands fine), verify real files with the Read tool. NEW files
are safe until edited.

## Verify on your machine

1. `node scripts/build-demo-bundle.mjs && node scripts/smoke-test.mjs` (99 CLEAN).
2. `wallet.html`: Receive → QR + address + Request-an-amount morph.
3. `chat.html`: long-press received bubble → Tip (tonal chips both themes; Custom
   never resizes the sheet) · attach ⊕ → Pay (locked recipient) / Request (sheet →
   request bubble) · long-press → Select → tap several → Copy → paste in the OTHER
   phone's composer → "Send as N separate messages" offer.
4. Send: bad address → error sits under the input now.

## Next session candidates (agreed trajectory + flags)

1. Wallet polish pass (slice 4): on-device safe-area check (#22) — hero owns the inset.
2. Account shell (backup-ux-spec.md is ready) · Contacts/Settings/Scan shells.
3. 🟡 flags: copy-morph honesty sweep (member-sheet/wallet-shell still fire-and-forget);
   `canonicalAmount`/`toUnits` → shared money module; media/card rows in multi-select;
   B2 glyph exports (`link` missing, `code`); §9 BE asks (tip fee/aggregation,
   request-in-group semantics, share command, sendrequest single-contact payload,
   backup timestamp+dirty, Discover transport, history paging, tx fee in payload).

## Key files

Money: `src/components/{wallet-send,wallet-receive,tip-sheet,qr}.js` · overlay fix:
`overlay.js` · multi-select: `chat-select.js` (+ css each) · demos:
`src/demo/{wallet,chat}.html` · specs: `docs/{wallet-shell-spec,tipping-spec}.md` ·
build: `scripts/build-demo-bundle.mjs` · smoke: `scripts/smoke-test.mjs`.
