# Handoff — 2026-08-11 (sweep close + Android first contact) → THE FIX SESSION

**Read this first.** Supersedes `docs/handoff-2026-08-10.md` (consumed → archive
at the commit). State assumes the ONE commit below has landed.

## State at close

- **Committed (or committing now) as ONE batch, #328–#333:** the #46 loop fixes
  over the whole #324–#327 sweep (2 MAJOR + 8 MINOR, iOS F5 PASSED — the 6-point
  list) · #329 InstanceOrNull (fresh-install crash class, all platforms) · #330
  StringBuilder localizeHtml (the 30s Android splash) · #331 DomStorageEnabled
  (Android localStorage was DEAD — rating spam/drafts/backup badge/landtab, all
  device-verified healed) · #333 session close (AND-16 lever falsified+reverted).
- **Android runs the redesign.** First contact 2026-08-11 (Samsung, Debug via
  Mac): onboarding → account → chats → wallet all walk; sl-si dates good; both
  back gestures coexist (AND-3 closed).
- Smoke baseline: **1029 pass / the same 4 pre-existers** (#136 · #149③ · M5 ·
  B3-shell) — includes the 23 #324–#328 pins (11 mutation-verified).
- iOS state: the "pretty good version" + the loop fixes, all device-verified.
- Punch lists: `docs/android-findings.md` (AND-1…20; 1/2/3/9 closed, 16
  falsified→AND-20) · `docs/ios-sim-findings.md` (…iOS-64) ·
  `docs/ios-launch-gaps.md` · the #46 report `docs/opus-review-sweep-2026-08-10.md`.

## NEXT SESSION (the fix session), suggested order

1. **R1 — Android keyboard v2 (AND-16/20).** ⚠ #294 LAW, now Android too:
   MEASURE FIRST — kb-probe Android edition through the IME transition
   (chrome://inspect or on-screen), THEN the lever. Design direction on record:
   stop padding the root view for IME (MainActivity InsetsListener), push the
   inset into the page (`__setKbInset` twin from an insets-animation callback)
   so the #324 composer ride + stickDuring do the motion — no full-document
   relayout, fixes covers+clunk+flicker in one move.
2. **R2 — Android chrome round:** AND-6/7 (blue bars, not full-bleed) · AND-12
   (black splash) · AND-13 (notification silhouette icon) · AND-14 (Material
   default dialogs) · AND-18 (sticky tab highlight — blur-after-tap) · AND-19
   (Account transition flicker — inspect first).
3. **R3 — cheap-C# round 1** (ios-launch-gaps §C + walk riders): iOS-54 + iOS-61
   as ONE fan-out (connectivity + unread to overlay WebViews) · L1/L2/L5/L7 +
   **L3/AND-5** (onboarding backup hand-off, device-confirmed) · W9 · iOS-59
   (sub-screens hide the bottom nav) · iOS-62 (below-fold boot re-pin) · iOS-64
   (draft-excerpt nudge on overlay close).
4. **R4 — AND-11** (first-call mic permission eats the answer: pre-request at
   ring / re-drive post-grant) · **AND-17 repro** (bot-group contact requests —
   logcat running).
5. **Dials/discussions for Damir:** AND-15 notifications content (+#232 privacy
   check, BE-adjacent) · iOS-58 language-change flicker (staged-reload vs live
   re-localize design) · iOS-60 pattern swatches vs shorter labels.
6. **Sweep remainder** (handoff-2026-08-10 order): S2 attach tray · S3 batch B ·
   S5 i18n residue (+iOS-63 pending-strip) · S6 legs · S7 probe retirement · S8.
7. Launch gates unchanged: `docs/ios-launch-gaps.md` §A (L8 keychain! · L6 ·
   security walkthrough · thermal · Terms/Privacy · delegate families).

## Rules (unchanged + one addition)

Zero-C# unless evidence forces (#215) · ★ #221 · bundle BEFORE shells · smoke
1029/same 4 · #46 loop per batch · measure-first on EVERY keyboard/viewport
lever, all platforms (#294, re-proven twice this session) · DECISIONS #334+ ·
commits = Damir in GitHub Desktop · `git --no-optional-locks` · iOS device
builds: obj/bin wipe + bundle grep + plain build then -t:Run · **Android device
builds: same wipe precaution** (`rm -rf Spixi/obj/Debug/net10.0-android
Spixi/bin/Debug/net10.0-android`), deploy = `dotnet build Spixi/Spixi.csproj -f
net10.0-android -c Debug -t:Run` (JDK + SDK live under ~/Library/Android/sdk;
adb at ~/Library/Android/sdk/platform-tools/adb) · bridge sessions: hash-verify
staged copies; appends via device_bash; no unlink on the mount.

## Commit message (this batch — ONE commit, GitHub Desktop)

```
Loop fixes + Android first contact (#328-#333): #46 loop over #324-#327 (2 MAJOR:
slide-stage input-dead, stick change-gate+touch-cancel; 8 MINOR incl. 3 more W1
timestamp surfaces, channel-panel swipe, desktop search-collapse; 23 pins, 11
mutation-verified; iOS F5 passed) + InstanceOrNull fresh-install crash class
(Android RequestedThemeChanged fired it; latent everywhere) + StringBuilder
localizeHtml (quadratic 30s Android splash) + DomStorageEnabled (Android
localStorage was dead: rating spam, drafts, backup badge, landtab — all healed,
device-verified) + AND punch list opened (docs/android-findings.md); AND-16
keyboard lever falsified+reverted (measure-first next session); smoke 1029/same 4
```

## QoL rider (next session, 5 minutes)

The smoke suite's FAILED block reads as a failure when it lists exactly the 4
known pre-existers — it tricked Damir twice. Make the summary honest: when the
failure set == the known baseline set, print
`BASELINE OK — 1029 pass / the 4 KNOWN pre-existers (#136 · #149③ · M5 · B3)`
and exit 0; any OTHER failure (or a missing known one — a silent fix!) keeps
the red FAILED block + exit 1.
