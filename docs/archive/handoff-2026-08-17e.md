# Handoff — 2026-08-17e. READ FIRST.

**LANGUAGE RULE: ASD-STE100 Simplified Technical English.** See `CLAUDE.md`.

Supersedes `docs/archive/handoff-2026-08-17d.md`. Read dates in headers, never
file names.

★ Planning doc: `docs/master-worklist-2026-08-17.md` (R1 struck; N46–N50 added).
★ Damir's course setting stands: KEEP BUILDING BATCHES. Exception below: the
D-19 family left the deferred pile — Damir escalated it at the #369 F5.

---

## 1. State — verify it, do not trust it

```
git --no-optional-locks log --oneline -3
git --no-optional-locks status --porcelain
```

Damir commits the #364–#369 batch himself (title + body handed to him — the
commit should be at HEAD when you read this; VERIFY).

| Item | Value |
|---|---|
| Batch #364–#368 | **R1 identity round: N1 avatar rework · N34 owner chip · N26+D-5 relation · N27 remove-blocked. F5 PASSED on Windows AND Android (#369) — all legs; findings logged, none a batch regression** |
| Smoke | **BASELINE OK — 1813 pass / the 4 KNOWN pre-existers (#136 · #149③ · M5 · B3)** |
| Locales | verify-locales ALL CLEAN · i18n-lint ✓ · pseudo 9/9 · zero em-dashes in all 8 built dictionaries |
| Compiler | ✅ Damir built + ran Windows AND Android (A52) for the F5 — the C# risk is retired for this batch. It returns for every future batch |
| Loop | Opus #46, TWO rounds (r2 broke r1's verdict; its finds were fixed) → CLEAN (#368) |

## 2. NEXT SESSION — the fix + R2 batch (build, no Damir input needed to start)

Order inside the session: ① the D-19 family (Damir's escalation) → ② the two
cheap #369 finds → ③ R2 as capacity allows. Verify each item against code
first (#215); pins + the #46 loop as always; anything needing a dial → log it,
skip it.

### ① The D-19 family rework — dial LOCKED + AMENDED (#369)

The 17d §5 D-19b dial, carried verbatim in `docs/archive/handoff-2026-08-17d.md`,
with ONE amendment from the #369 F5:

1. **AMENDED label ladder:** nickname when known — IN BLIND GROUPS TOO (Damir:
   "keep nicknames visible in blind groups, only addresses are completely
   hidden"). Truncated address when known and the room is NOT blind. NOTHING
   when neither (legacy parity — no "Hidden member" on non-blind surfaces;
   blind rooms with no nick keep a placeholder, never an address).
2. **Reverse-resolve** (roster nick→address, EXACT single match only —
   money-safety) restores the member sheet, copy, and tip for roster-known
   senders. **NON-BLIND ROOMS ONLY** — it must never hand out an address in a
   blind room.
3. The Spixi PUBLIC bot room is the acceptance case (#369 C1/C5): senders must
   never render masked there, and a known user's sheet must not dead-end.
4. Keep from #356: no group-name impersonation · no group avatar on strangers'
   rows · nick-keyed bubble runs · the NRE guard · the empty-slot C# contract.
5. Retire/replace the affected pins honestly (placeholder pins die,
   reverse-resolve pins are born; count the delta).
6. Cheap rider, dial GIVEN: **N48** — blind groups show MY OWN owner status
   (self-only `amOwner` push; no other identity leaks). Same C# area.

### ② The cheap #369 finds

| Item | What | Size |
|---|---|---|
| **N49** | Deselecting a chat leaves the row highlighted (desktop; clear the selected tint on convo close) | S |
| **N50** | contact_details: OS back dismisses the TOP OVERLAY first (the remove-blocked modal), then pops — add the dismissTopOverlay wiring chat.html/home.html already have | XS |

### ③ R2 — copy & locale round (as capacity allows)

| Item | What | Size |
|---|---|---|
| **N3** | Copy sweep: remove em-dashes app-wide, simpler voice; shorten the apps empty-state line (sl text in the worklist) | S |
| **D-7 + I-11 + AND-35** | Restore copy · Account row subtitles (SOME rows, one line, truncates — 15c Table A row 14) · the AND-35 string | S–M |
| R2 one-liners | de-de/sl-si en-dashes · "1 members" plural · SR aria-dash joiners · the #366 "Request received" badge copy variant | XS each |
| **N4** | Locale expansion (L) — only if the session has room; dictionary + Utils.cs culture gate move TOGETHER |

D-9 and D1 (reply-to; two-device id round-trip verify FIRST) still need Damir
present. **A3 WATCH:** two green-family avatar rows read close on Damir's list —
palette spread is his dial; no build until his call.

## 3. How to work — unchanged

Cloud twin recipe as in 15c §7 (archive): clone, checkout the branch tip,
`npm install jsdom --no-save`, bundle → shells → smoke; tarball delivery
(⚠ the mount cannot unlink — plain `tar xzf` in PowerShell; Move-Item/mv works;
`git --no-optional-locks` always). Standing set: verify plans against code ·
#294 · #215 · smoke green at **1813/4** (state the new number when it moves) ·
mutation-honest pins, PROVEN · Damir commits · DECISIONS rows at decision time ·
the #46 loop on anything substantial · **Opus for every adversarial pass
(#352)** · the security handover gate while building. Ixian-Core reference:
clone + checkout `097341a` in the twin. Deploy commands (Damir): wipe obj/bin →
Android `dotnet build Spixi\Spixi.csproj -f net10.0-android -c Release -t:Run` ·
Windows `dotnet build ... -f net10.0-windows10.0.19041.0 -c Debug` then run the
exe (⚠ `-t:Run` does NOT build on the Windows TFM).

## 4. THE DEFERRED PILE — fix/triage later, in one pass (do NOT build now)

| ID | Item | State + pointer |
|---|---|---|
| **N13** | Onboarding "Back up now" does nothing at account creation | 🔴 TOP of the pile — data-loss class |
| **D-17b** | Apps search bar ALWAYS visible | Dial LOCKED — 17d §5 (archive), verbatim |
| **N36b** | A4 select-mode tap flash on Android | Evidence pre-gathered (#363); on-device repro first (#215) |
| **N40** | "Connecting…" stops on a LIVE document after long offline use | Suspect on file: update-branch starvation (#357) |
| **N10** | App-invite Cancel in chat does nothing | Triage (C# here + maybe BE) |
| **N46** | Delete-flow rework: branded checkboxes + UX pass | ★ REMIND Damir near master-list completion (his ask, #369) |
| **N47** | Nickname reset to address-only? | Dial (#369 B3) |
| Legs 29–30 | Compact-balance · flick-cancel (A52) | Untestable two rounds running |
| #368 log-only | Own-bubble Owner asymmetry · long-nick chip overflow · modal aria-describedby · agent-chat-demo stale avatars · step-2 oldest-message relation edge · a distinct BOT glyph needs a Tabler export | Fold into related rounds |
| #367 follow-up | Remove-blocked path-out ACTIONS (leave-group buttons; "leave groups where you are together with the user") | Damir: later |

## 5. Do not re-attempt

All of 17d §6 (archive), plus:
- No per-theme avatar S/L, no flipping ink token — #37 is RETIRED (#364); the
  palette is contrast-gated in smoke. Change values only WITH the gate.
- No relation on a blind row from ANY push site — broad botInfo predicate,
  never the '[Unknown]' mask alone (#366).
- No second inline copy of the guarded contact-request body — both pages call
  `SpixiContentPage.sendContactRequestGuarded` (#366).
- No unbounded content in a c-modal on contact_details without a cap (#367).
- No reverse-resolve of a nick to an address on a MULTI-match, and NEVER in a
  blind room (money + masking; #369 amendment).
- No "Hidden member" placeholder over a KNOWN nickname — in any room type
  (#369 amendment; the placeholder is a last resort, not a mask).
