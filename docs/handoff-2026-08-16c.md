# Handoff — 2026-08-16c. READ FIRST.

**LANGUAGE RULE: ASD-STE100 Simplified Technical English.** See `CLAUDE.md`.

Supersedes `docs/handoff-2026-08-16b.md` for state. That file carries a superseded
banner; archive it with the NEXT batch. This batch archived `handoff-2026-08-15c.md`
and `next-session-prompt.md` (checklist §1 has the `rm` line).
⚠ The archive slot `docs/archive/next-session-prompt.md` was ALREADY OCCUPIED by the
consumed 2026-08-04 file (#301), so the 2026-08-15 one is archived as
`docs/archive/next-session-prompt-2026-08-15.md` — the one archive file whose name
gained a date.
⚠ The archive-name warning stands: `docs/archive/handoff-2026-08-16.md` (no letter)
is an OLD file from 2026-08-13. Read the date in the header, never the file name.

★ **NEW ENVIRONMENT (2026-08-16): Damir moved to a Mac.** The repo is a fresh clone at
`/Users/damir/Documents/GitHub/Spixi`. The #349–#353 batch was committed and pushed
before the move — nothing was lost. Checklists now use macOS shell, not PowerShell.
The CRLF reason for the cloud twin is gone with the Windows tree, but the recipe
stays — the cloud twin remains where builds and smoke run.

---

## 1. State — verify it, do not trust it

```
git --no-optional-locks log --oneline -3
git --no-optional-locks status --porcelain
```

At session start the Mac tree was CLEAN at `81b30838` (pushed). This session produced
ONE UNCOMMITTED batch, delivered as `spixi-354.tar.gz`: rows **#354–#355**.

| Item | Value |
|---|---|
| Smoke | **BASELINE OK — 1671 pass / the 4 KNOWN pre-existers (#136 · #149③ · M5 · B3)** — +4 pins vs 1667, all mutation-verified |
| Compiler | ⚠ The #354 guard (6 lines, `onLoadMore`) was NEVER COMPILED — no .NET in the cloud. Damir's build is the first pass |
| Device | ✅ **F5 RAN 2026-08-16, A52.** D-18 PASS (checklist §2 steps 1–7, incl. the third press and a reopen). AND-38 PASS (balance + eye). OPEN legs: compact-balance tap (needs wallet rows) · flick-cancel re-test (list too short to scroll; see AND-39) · 2.3b = Windows, waits for the PC |
| ★ Workflow rule (#352) | Adversarial review runs on OPUS, always. Fable builds only. Standing |

## 2. What landed this session

1. **#354 — D-18 SOLVED (the first action, done).** Root cause at source, both repos:
   Ixian-Core `Friend.getMessages` re-reads storage only when the channel is uncached
   or `msg_count != 100` (`Friend.cs:910`; `0.9.8k` = commit `097341a`, no git tag) —
   the literal 100 is the cache-bypass test. Our #343
   dial (chunk 25) made the THIRD pill press ask for exactly 100, get the stale 75-row
   cache, and die with history intact on disk. Exonerated with evidence: `5643e5b`, the
   shell burst path, the #349–#353 batch, legacy. FIX: `onLoadMore` steps over the
   poisoned value → windows 25→50→75→125→…. Zero Core edits (#215).
2. **#355 — AND-38 BUILT.** Verify-first overturned the `data-nopress` hypothesis: the
   row-wide flash was the NATIVE Android tap highlight on the click-bearing balance
   block, not pressable.js. CSS-only fix; the eye's `:active` wash is now desktop-gated.
   ★ One dial edge rides the checklist (§3.4): the quick-action circles keep their wash.
3. **BE question 9 FILED** (end of `docs/f5-findings-2026-08-15.md`): the magic-100
   cache test, PLUS two adjacent Core defects from the same read — a permanent
   message-loss race on cache replacement (debounced flush, mismatched locks), and the
   25-row cache multiplying an inherited null-`Find` NRE class
   (`TransferManager.cs:654` the worst site). Table B is now NINE questions.
4. **The #46 loop ran on Opus (#352), four rounds:** r1 = 1 MAJOR (analysis
   incompleteness → BE 9c) + 6 MINOR + 3 NIT — the MAJOR filed, the 6 MINOR applied,
   1 of 3 NIT applied (NIT-10 logged not fixed; NIT-8 accepted at r3). r2 = 2 MAJOR (BOTH
   documentation integrity: a pre-written CLEAN verdict in this very file, and an
   archive-slot collision that would have overwritten the CONSUMED 2026-08-04
   `next-session-prompt.md`) + 9 MINOR + 5 NIT — the 9 MINOR applied, 3 of 5 NIT
   applied, NIT-13 needed no edit (a historical DECISIONS row stands; the corrected
   floor lives in the 16b handoff), NIT-16 = STE house-wide, logged standing.
   r3 verdict, verbatim: "1 MAJOR / 2 MINOR / 4 NIT. No code findings — all four
   pins and both fixes are sound." The r3 MAJOR was the checklist F5 target
   (maccatalyst on a fresh clone cannot be relied on to boot — retargeted to the A52 in r4), the
   MINORs were record accounting, both fixed in r4. PRODUCT-CLEAN at r3. r4 (record
   texts only) = CONFIRMED: 3 findings — an r1 NIT over-claim, an overstated
   maccatalyst claim, and the #354 row's own NIT accounting — all applied. Pins: 4,
   every one mutation-verified, three hardened by reviewer-proven evasions.
5. **The F5 ran the same day (Damir, A52).** D-18: steps 1–7 ALL PASS — the third
   press no longer dies, the full history loads, the reopen repeats it. AND-38:
   balance and eye PASS; the compact-balance leg waits for wallet rows. The §3.4
   dial is CLOSED: the quick-action wash stays ("I don't notice it, so not
   important"). Two observations logged, not built (#294): **I-12** (the initial
   window of 25 feels low — a dial talk; the honest raise is the #298 transport
   prepend, not a bigger re-flush) and **AND-39** (the tap fill feels abrupt —
   clarify WHICH phase before anything moves; the flick-cancel check also needs a
   longer chats list to be meaningful).

## 3. THE PLAN — table A continues (Damir's rule holds)

Rows renumber after D-18 closes. In order:

| Next | Item | Note |
|---|---|---|
| 0 | **The F5 on `docs/f5-checklist-354.md`** | First compile of #354 + the D-18 and AND-38 device proofs + two carry-over re-checks |
| 1 | **I-2, chip outline only** — the row half died with #353 | One small token/outline pass + contrast check |
| 2 | **D-17** — Apps search-field flash | Read #340 C-MAJOR-1a/b/c first |
| 3 | **I-6** — digit grouping, **APP-GLOBAL** (Damir 2026-08-16) | Display only; never touch the wire format (#77) |
| 4 | **D-5** — relation arg on addContact/addMember | C# here; = AND-30 |
| 5 | **D-9** — close delete-account on device | Throwaway account |
| 6 | **★ D1 reply-to** | Two-device id round-trip FIRST; the body marker is hostile under the gate |
| 7 | I-1 + welcome flicker · I-10/I-9 (measure first) · D-4/D-15 honest halves · D-1 part B · D-7 + I-11 (one locale round) · I-3 design round | Then the security sweep LAST |

Also queued: the AND-31…AND-37 **landscape round** and AND-35's renames dial (rides a
locale round). AND-38 is CONFIRMED on device (#355; compact-balance leg open). New,
logged not built: **I-12** initial-window dial (talk first; the real lever is the #298
prepend) · **AND-39** abrupt-fill feel (clarify the phase, then video, then #294).

Table B grew to **NINE BE questions** — parked, do not build against it.
**A5 and CI7 stay BLOCKED.**

## 4. How to work — unchanged

The cloud twin recipe, the build chain, the tarball delivery: as in 15c §7 (now in
`docs/archive/`), with the platform note from the banner above — delivery lands on the
Mac, extraction is plain `tar xzf`, deletions are `rm`. The standing set: verify plans
against code · #294 · #215 (Ixian-Core is NOT in this repo — but READ it in the cloud
twin: `git clone https://github.com/ProjectIxian/Ixian-Core` then `git checkout 097341a` — the `xcore-0.9.8k` commit, master HEAD today; NO git tag exists — gave D-18 its evidence) · smoke green at **1671/4** · mutation-honest pins, proven · bundle before
shells · Damir commits · DECISIONS rows at decision time · the #46 loop on anything
substantial · Opus for every adversarial pass (#352) · the security handover gate while
building. ★ Reply-to stays the one to watch under the gate.

## 5. Do not re-attempt — grew by one

All of 15c §8 and 16b §5, plus: **do not "fix" D-18 by changing `Config.messagesToLoad`
back to 100.** That re-ships the #343 chat-entry cost for a workaround the guard already
provides, and the fork-state entry window hides the Core landmine instead of dodging it.
The guard in `onLoadMore` stays until Core removes the magic-number test (BE question 9).

## 6. Open device items

| ID | Finding | State |
|---|---|---|
| D-18 | Load-more guard | ✅ CONFIRMED on the A52 (2026-08-16, §2 steps 1–7 PASS) |
| AND-38 | Balance-toggle flash | ✅ CONFIRMED on the A52 (balance + eye); compact-balance tap OPEN until the wallet has rows. Dial closed: quick-action wash STAYS |
| 2.3b residual | Windows tonal-hover on a selected row | OPEN — Windows leg, waits for the PC (full obj/bin wipe first) |
| Flick-scroll cancel | A52, from f5-checklist-351 §3.3 | RE-TEST with a longer chats list — the first try could not scroll, so a "fill on flick" there may be the no-scroll-takeover class, not a defect |
| AND-39 | Tap fill feels abrupt (Damir, A52) | OPEN — clarify WHICH phase (start / completion / fade), then a recording, then #294. #351 mechanism, committed batch — NOT this one |
| I-12 | Initial chat window of 25 feels low | OPEN — dial talk. Raising it re-buys the #343 chat-open cost; the honest fix is the #298 transport prepend |
