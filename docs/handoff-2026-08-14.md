# Handoff — next session (written 2026-08-13 at session close) — READ FIRST

Session 2026-08-13 ran ONLY the owed #46 audit over #336 → **CLEAN (#337)**, then
closed early (fable usage cap). `docs/handoff-2026-08-13.md` is CONSUMED (its
★ FIRST TASK is done — archive it); this file is the live work order.

## State

- **#337 is on Damir's disk, UNCOMMITTED** (bundle untouched — no component JS
  changed; 22 shells rebuilt in place; smoke BASELINE OK **1065**/same-4).
  Full detail: DECISIONS #337. Damir's finalize steps are in the session chat
  (git rm 3 scratch scripts · build · F5 list · commit ONE batch).
- ★ F5 load-bearing test: **create a group (≥2 members)** — the committed #336
  had group-create silently dead (state-push nav clobber); #337 fixes it.
- Known-4 smoke pre-existers unchanged (#136 · #149③ · M5 · B3).

## Work order (unchanged priorities from handoff-2026-08-13, minus the audit)

1. **W2** desktop rail covered (READ-ME-FIRST-WINDOWS-FINDINGS.md §W2): legacy
   wallet `PushAsync` pages → `pushPageLoaded` + stageMargin(railWidthDip) on
   wide; contacts takeover inset left by the rail on `:root[data-desktop]` +
   define rail-tap-while-open behavior.
2. **W5** ★ chat pattern STYLES (§W5 — spec + tuning LOCKED, big FE batch):
   Line art / Data matrix / Live flow × existing visibility dial; live flow
   DESKTOP-ONLY; persist like the intensity pref, same pre-paint script;
   extend generate-chat-pattern.mjs (params in §W5); stacking recipe in §W5.
3. **W6** wallet Receive "Request an amount" (§W6): QR animates away up, amount
   focuses, per-contact send arrows disabled until valid amount.
4. **W7** change-password hangs Account on Windows (§W7): preload-latch suspects
   + settings_encryption onload timing; pointers in the doc. Verify = Damir PC.
5. Android (device needed): AND-25 remove-contact crash (logcat first) · AND-28
   existing-contact avatars (C# path, existing vs new) · AND-26 flicker
   (Opacity-staging half) · AND-7 wallet top inset (unblocked) · AND-20
   smoothness (WindowInsetsAnimation, measure frames first #294) · AND-24
   dialog routing (design decision) · AND-27 mic-denied no call screen (repro) ·
   AND-30 member-sheet relation flag.
6. iOS: iOS-56b edge-swipe in subscreens (#325 lever generalization) ·
   iOS-66 = FIXED in #337, device-verify only (scroll hub → pick language →
   hub stays put).

## Residuals logged in #337 (do not re-derive)

- call.html can't live re-theme (CallPage outside the setTheme fan-out) — needs
  a C# re-push at showRing/showBar; cosmetic, mid-call theme picks only.
- Wide (≥700dp) Android tablet: back closes a column-1 chat pane before a home
  sheet (2nd press recovers). Naive fix is WRONG — op.column is relayout-stale;
  pair with live rightContent.IsVisible. Re-test when a tablet enters the matrix.
- overlay.js data-overlay-open removal lags ≤400ms after dismiss (self-heals).

## Rules (unchanged)

zero-C# unless evidence (#215) · ★ #221 chat isolation · measure-first on
keyboard/viewport levers (#294) · bundle BEFORE shells · smoke stays green
(baseline now 1065/same-4) · commits = Damir · git --no-optional-locks · mount
FORBIDS unlink (write in place / SendUserFile→device_commit_files; NEVER
`git checkout --` on the mount) · full smoke > 45s = run it on a cloud tarball
twin (git archive src scripts Spixi/{Resources,Pages,Utils,Lang,Platforms,
App.xaml.cs,Meta/Node.cs,Spixi.csproj} → stage → extract → overlay edits →
node scripts/smoke-test.mjs; the bridge kills backgrounded device processes).

## Cleanup for Damir (bridge can't unlink)

- `git rm scripts/_f5repro.mjs scripts/.dbg.tmp.mjs scripts/.launch-only.tmp.mjs`
  (tracked debris committed in 7390f0c5).
- Purge `_to_delete/audit-336/` (audit patch + smoke logs + 2 tarballs ~61MB).
- Archive `docs/handoff-2026-08-13.md` → docs/archive/.
