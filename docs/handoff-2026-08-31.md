# HANDOFF — after session B, 2026-08-31

Session B shipped **L7 · L11**, removed the `[RCPT]` probe, and folded the member-context
check into `cs-syntax-check`. Decisions **#657–#661**.

★ **It also ran the #46 loop that #646 marked `🟡 re-review`, and that is where the session
went.** Five rounds. **22 MAJORs. 228 mutations. No pin deleted.** The verdict is
`docs/opus-review-verdict-session-b.md`. Read it before you touch the delivery path.

## ★★ DAMIR WALKED IT — 22 pass · 0 real failures · 2 n/a

The walk ran on Windows on 2026-08-31. **22 pass · 0 real failures · 2 n/a (C2, F6).**

He marked two items "fail" only because the sheet gave him no way to record a pass with a
note. **Both were passes with a design note, and both notes are BUILT and shipped in the
same batch.**

* **A2 — the composer placeholder lost its padding.** `.c-composer__field` carries
  `padding-inline-start: var(--spacing-4)`, and its own comment calls that the "attach pill
  side" — the 36 px pill supplied the rest of the optical gap. `hidden` is
  `display: none !important`, so hiding the ⊕ took the pill out of the row and the
  placeholder sat on the field edge. ★ **The guarantee this batch added — a bot room offers
  no ⊕ — created a defect one surface over, and only the device walk found it.** Fixed with
  `.c-composer__field[data-no-attach]` at `--spacing-12`, driven from the SAME expression
  that hides the button, so the padding and the pill cannot disagree.
* **B3 — the empty receipt row now speaks.** Damir: *"the row should say - nobody has seen
  this yet"*. New key `noneSeenYet`, drafted into all 12 locales. ⚠ **This reverses the "no
  counts, no line" rule for ONE case only.** The other four empty returns stay silent — an
  incoming message, a 1:1, a bot room, and a roster under 2 — because there we have no
  honest answer. This case is different: we hold the roster, the message is ours, and every
  member reported nothing.

## ★★ THE WINDOWS BUILD TRAP — it cost a walk, and it has its own DECISIONS row (#663)

On Windows the app reads its html from `AppDomain.CurrentDomain.BaseDirectory`, the folder
beside `Spixi.exe` (`SPlatformUtils.cs:30-33`).

**`dotnet build` for the Windows target does not stage the `MauiAsset` files there.** Only
the Visual Studio deploy step does. When the file is missing,
`SpixiLocalization.localizeHtml` logs an error and **returns without writing**, and
`generatePage` still returns the URL to `ll_chat.html` in `Documents\Spixi\html\` — the
user folder, which survives every wipe.

★ **So the app silently serves the previous build's shell and looks completely normal.**
Damir hit exactly this and reported a fixed defect as still live.

**THE RULE: build Windows with F5, or copy `Resources\Raw\*` into the output by hand.**
Android is unaffected — it reads assets from the APK and keeps no user-folder copy.

⚠ Also: `dotnet build -t:Run` does not work for the Windows target. It replaces the Build
target and exits 9009.

## The numbers

```
bundle 301 · shells 18 · smoke BASELINE OK 3586 / the 3 known (#136 · M5 · B3)
· locales ALL CLEAN 776 · cs-syntax 140+1 · Ixian-Core 097341a, verified clean
```

⚠ **TWO MORE NUMBERS MOVED AFTER THE WALK, and both are the two fixes above.**

* **locales 775 → 776** — the new key `noneSeenYet`.
* **smoke 3582 → 3586** — the pins for the two walk fixes.
* Everything else is unchanged: bundle 301 · shells 18 · cs-syntax 140+1 · the 3 known
  pre-existers.

The build baseline before the walk was `bundle 301 · shells 18 · smoke 3582 · locales 775`.

⚠ **Two numbers had already moved during the build, and both are deliberate.**

* **bundle 299 → 301** — `attachTilesFor` and `hasAttachTiles`, so the composer ⊕ and the
  attach sheet share ONE predicate.
* **smoke 3496 → 3582** — new pins, the EXECUTING ones among them.
* ★ **locales stay 775, and that is a coincidence worth stating.** `readOf`,
  `downloadedOf`, `callBusy` and `callUnavailable` were added. `deliveredOf`,
  `downloadedBy`, `readBy` and `markRead` were dropped with the features that used them.
  english-fallback fell 54 → 50 per locale.
* ★ **Ixian-Core is verified at `097341a`, and `git diff --ignore-cr-at-eol` is EMPTY.** The
  170 modified files are CRLF churn, not edits. Do not read them as a change.

## What actually happened, in one paragraph

The session was planned as a review plus five small rows. The review was owed, so it ran
first. Three auditors found **10 MAJORs** in session A's repairs. A break-my-verdict pass
over the fixes found **6 more inside the fixes**. A second pass found **6 more inside
those**. A focused fourth check found the new menu line shipping **English in all 12
locales** and a quadratic walk on a 1 Hz tick. **Two rows shipped. Three moved to session
C.** The loop earned its time.

★★ **THE HEADLINE.** Two auditors with disjoint scopes independently found that *"was this
group message delivered"* was answered by an **enumerated list of reaction keys, in four
homes that drifted**. Session A read `received`. Round 1 added `seen`. Round 2 added
`fileReceived`. Round 3 found `like` and `tip`. **The list was open-ended, so every reviewer
found the next item on it.** Damir ruled it in one sentence — *"perhaps we don't need in the
long press menu to show delivered … having delivered seems redundant"* — and the ruling made
the fix **smaller** than any of the patches. The rule now lives in ONE C# home,
`UIHelpers.anyOtherMemberHasMessage`, and **it names no reaction key**. It can never need a
fourth patch. Read **#658**.

## ★★ Two honest lessons, in Damir's interest

1. **Three rounds were spent patching an enumerated list before anyone asked whether the
   list should exist.** Each round added the next key and each round shipped a new
   divergence. Damir's one-sentence ruling deleted the class and made the fix smaller than
   any of the patches. ★ **WHEN A REVIEWER FINDS THE SAME CLASS OF DEFECT TWICE, STOP FIXING
   AND QUESTION THE DESIGN.** Two rounds of that time was avoidable.
2. **The session took about seven hours for what was planned as a review plus five small
   rows.** Say it plainly: **the loop earned its time, the three wasted rounds did not.**
   Size the next session around the review, not around the rows.

## A third lesson, and it is a records lesson

★★ **ITEM 0 WAS BASED ON A STALE ROW.** The session prompt opened with *"run the #46 loop
that is still owed over #507–#511"*. **It was not owed.** It ran on 2026-08-22 and
**DECISIONS #515 records it CLEAN**. The only thing missing was the verdict in §6 of
`docs/opus-review-brief-507-511.md`, which read "(append here)". **Three handoffs read that
empty section, concluded the loop was owed, and copied the row forward.**

★ **A verdict that is not written back into its own brief is a verdict nobody can find.**
DECISIONS held the answer the whole time; nobody looked, because the brief said the work was
unfinished, and a brief is what the next session reads first.

⚠ **THE RULE: a loop is not finished until its verdict is written into the brief that
ordered it.** §6 is written now. `docs/handoff-2026-08-30.md` marks the old row SUPERSEDED
rather than deleting it.

## What session C owes

| | |
|---|---|
| **L6 · L5 · L10** | carried from session B. ⚠ **L10 is NOT a one-line reorder** — see below. |
| **L3 · L4 · L9** | the gesture batch. L3 is the row that decides the size of the session. |
| **L13** — the leave-group check box | ★ NEW, Damir's words. ⚠ **VERIFY AT SOURCE FIRST.** |
| **L14** — a tab flickers into Wallet | ★ NEW, 2026-08-31. Chats or Apps → Account → Wallet, that order only. **ANDROID only so far; iOS NOT checked — Damir rules it a MOBILE row.** ⚠ **MEASURE FIRST** (#294). The parked peer tab is mobile-only (#315), so a DESKTOP repro would refute the theory. 🟡 The iOS leg is owed. |
| **L15** — Windows language picker shows no flags | ★ NEW, 2026-08-31. Same shell both platforms, so it is RENDERING. Suspect: WebView2 has no colour flag glyph. ⚠ **VERIFY AT SOURCE.** |
| **L16** — splash: smaller logo, no light-mode logo background | ★ NEW, 2026-08-31. New blue **#175595**. ★ **The splash IS theme-aware already (#534, Damir asked for it).** The squircle is ONE missing line: the LIGHT theme declares no `windowSplashScreenAnimatedIcon` (`values-v31/styles.xml`), so Android falls back to the LAUNCHER ICON and draws its background layer. The dark theme supplies its own drawable and has none. The logo size is the group transform inside `drawable/spixi_splash_icon_night.xml`, not `BaseSize`. |
| **L17** — the launcher icon | ★ NEW, 2026-08-31. Damir owes the new logo SVG. ★ **He marked it safe to do early.** |
| **L18** — the logotype is blue in dark mode | ★ NEW, 2026-08-31. *"should be neutral01 … on light mode we keep it"*. **ONE declaration:** `topbar.css:64` paints mark and wordmark together (the SVG is `currentColor`). `--text-action-default` is blue in BOTH themes. Fix = a new semantic token in `tokens.css` — ⚠ `tokens.css:435` forbids a `[data-theme]` selector in a component file. Cheap; safe to do early. |
| **The `[CDPERF]` probe** | STAYS until L10 re-measures. Remove it with the shell's `ixian:cdpainted` emit and the four-line pin, in ONE batch. |
| **L12** — bot-room kick/ban | Damir's, needs an admin account. #637 is not done. |
| **CORE-1 … CORE-6 + the membership question** | Ixian-Core, frozen. BE engineer. **CORE-4, CORE-5 and CORE-6 are new this session.** |
| **The nine iOS rows** | office Mac. ⚠ A1 (wallet password in cleartext Preferences) is still a real launch blocker — no `SecureStorage` anywhere in the tree. |
| **Two `.Last()` sites** | `AppDetailsPage.xaml.cs:400` and `Platforms/Android/WebViewRenderer.cs:163`. Neither runs on the network thread, so both are uniformity, not a defect. |

### ★ L10 is not a one-line reorder, and the worklist said it was

Auditor C read the multicast ordering at `SpixiContentPage.cs:322-326`. The base handler
calls `signalPreloadReady()` **after** the page's own handler, so `onLoad` has already
issued `setPaneMode` and its `addMember` burst when the present happens. A hoist keeps that
order **only while it stays on the same synchronous turn**. ⚠ `deferPreloadReady` is the
trap: setting it removes the belt that covers a skipped call. ★ And the premise is probably
wrong — a hoist moves the present earlier by ONE dispatcher turn, and cannot recover the
constructor that rewrites a 168 KB shell to disk or the WebView boot. **Read the probe
before building.**

### ★ L13 — verify before you build

> *"we need a 'leave group' check box when long press on a group in chats list and tap on
> delete chat. so similar to 1:1 where we offer to remove the contacts as well."*

⚠ Leaving a group needs a **real Core verb**, and **#253 already found that `undorequest`
and `sendLeave` are not the same path.** Do not assume one covers the other. **If no verb
exists, do not build the check box** — it becomes a control that reports an outcome it did
not cause, which is Damir's ⑪ rule, and that is worse than the missing option. Read Core at
`097341a`, tell Damir what the box can truthfully promise, and make it a BE row if it cannot
promise anything.

## Deferred by Damir — these must survive into session C

* **The sticky `.c-money-cta` sits UNDER the iOS soft keyboard**, and
  `contact_details.html` publishes no `--kb-inset` at all. The one lever C# offers,
  `window.__setKbInset`, has no handler in that shell.
* **The iOS menu re-anchor runs on the FIRST resize only.** The rows keep moving for up to
  280 ms after it, because `--kb-inset` drives a composer margin with a 280 ms CSS
  transition and no resize event fires at the end of it. **The menu can point at one message
  and act on another.**
* ⚠ **CARRIED, not introduced: in a blind group two members with the same nick collapse into
  ONE roster entry**, so the denominator reads one short. The clamp then turns an obvious
  `4 of 3` into a plausible lie.

## The BE rows this session added

* **CORE-4** — `PendingMessageProcessor.cs:326` calls `onMessageExpired(friend, 0, …)` with
  a hardcoded 0, while the sibling `onMessageSent` passes the real channel at `:504`. A bot
  room's expired message writes to the wrong channel. Mitigated on our side; **the source
  fix is one word.**
* **CORE-5** — is a repeat `connectToNetwork()` safe on an already-running node?
* **CORE-6** — `Node.start()` has no failure unwind. `running` is latched before the first
  failure return, and no failure path clears it.
* **The membership question** — `anyOtherMemberHasMessage` answers *"any ADDRESS that
  reacted"*, not *"any MEMBER"*. Every writer in this tree passes a real member, but
  `Friend.addReaction` is frozen Core and absent, so nothing proves Core refuses a
  non-member. **A crafted reaction could turn a group bubble to a double check no member
  supports, and could stop an honestly failed message from ever being painted red.** ⚠ The
  old key list did not close this either — it is **carried, not introduced**.

## What the pins learned, and what to keep doing

* ★★ **EXECUTING PINS.** `receiptDetail` and the attach predicate are lifted and RUN — from
  the **shipped bundle, the way the WebView loads it**, not from the source module. Keep
  doing this. A mutation that fixed the source and broke the shipped bundle stayed green
  until the pins read both homes and differenced them.
* ★★ **PIN THE WIRE.** *"Every pin asserts a named line or runs a pure function on
  hand-built inputs. Not one asserts a WIRE — that the writer of the state calls the
  reader."* Ten mutations beat a 164-pin harness on exactly that shape. **The repair was not
  more pins. It was deleting the state** — five writers became one field, and six of the ten
  mutations became impossible.
* ★★ **FIVE OF THE PIN OWNER'S OWN PINS READ PROSE, and mutation found them — reading did
  not.** The same defect he had rewritten four other pins to remove, hours earlier. **This
  is the most repeatable failure in this project.** Strip comments before a negative sweep,
  and mutate every pin before believing it.
* ★ **A pin that reports a gap is not a pin that can detect one.** `ok(inline || extracted, …)`
  was satisfied by the inline half alone, which is exactly the state it described.
* ★ **A pin can be RIGHT to go red.** The trio pin proved three C# sites carried the same
  LIST, and a list can always grow. It was the best pin available and still the wrong shape.

## The gate

`cs-syntax-check` now catches a **member declared inside a method body** — the #593 / #648
defect that broke Damir's build. It survived three review rounds of its own: it once
**failed on legal C#** (`using static` is live in this tree, and every page inherits from a
base declared in another file), and it once **printed a tick over a file that does not
compile**. Both are closed. **17 positive shapes, 25 negative shapes, 1.7 s, SKIP-LOUD when
tree-sitter is absent.** ★ Its promise is now **two separate promises**, because the single
absolute one was disproved in one line.

⚠ **It PARSES, it does not COMPILE.** That has not changed. The only compiler in this loop
is still Damir's build.

## Do NOT re-open

* The single check stays as #649/#650 left it.
* A bot room stops at a double check. It never shows a green double check.
* A group bubble is never a green double check.
* **The long-press menu shows READ and DOWNLOADED only.** "Delivered" is removed by Damir's
  ruling, and the bubble tick answers that question. Do not add a delivered clause back.
* **The delivery rule names NO reaction key.** Do not "just add `like`" to the walk. A pin
  goes red if you do, and it is meant to.
* **Only the roster case speaks when the receipt list is empty.** `noneSeenYet` is the ONE
  exception (#662). The other four empty returns — an incoming message, a 1:1, a bot room,
  and a roster under 2 — stay SILENT. Do not widen the exception.
* "Mark as read" is decided and removed.
* kick/ban stay bot-room only. The wallet SEND redesign stays last.
* No Ixian-Core changes.
