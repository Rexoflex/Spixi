# Handoff — 2026-09-22 (Session AB closed) → the next session

**Read this first (#215).** Then `DECISIONS.md` #902–#912. Session AB is **BUILT, WALKED (15 P · 0 F · 2 N)
and UNCOMMITTED** — the commit message is `docs/commit-message-session-ab.txt`. One late addition (#912,
four small app-side fixes) landed AFTER the walk and needs **one more F5 + Android build** before the
commit: `docs/f5-checklist-session-ab.md` §1b (four rows, ten minutes).

## 0 · State of the tree you inherit

* **UNCOMMITTED: all of Session AB** (#902–#912). C# in **six** files now: `HomePage`, `WalletSentPage`,
  `SingleChatPage`, `SpixiContentPage` (walked, compiled) + `App.xaml.cs` and `Platforms/iOS/AppDelegate.cs`
  (#912, NOT yet compiled) + `Pages/Home/HomePage.xaml.cs` again (one latch, #912). New files:
  `Platforms/Android/Resources/xml/backup_rules.xml`, `…/data_extraction_rules.xml`; the manifest gained
  two attributes. Two stylesheets, one shell, `smoke-test.mjs`, `scripts/lib/legal-docs.mjs`, two demos, docs.
* **Suite (container, no Core sibling): `4756` → `4774` / the same 2 KNOWN.** With an `Ixian-Core`
  checkout beside the repo (= Damir's machine) the same tree reads **4776** — compare the DELTA (#882).
  +18 = seven from the loop and the bubbles, five for #907, four for #912, two M1 gates when Core is there.
* `generate-icons` · `build-shells` · `extract-strings` · `build-legal-docs` `--check` all green;
  `cs-syntax-check` 138 clean + 1 known gap.
* ⚠ **Nothing in this workshop compiles C#.** The walked four compiled on Damir's machine (Windows F5 and
  the Android build). `App.xaml.cs` (#912 ③④) and `AppDelegate.cs` (#912 ②) have NOT been compiled —
  Windows/Android compile the first on the next F5; the iOS file waits for the next iOS build.
  `NSUrl.SetResource(NSUrl.IsExcludedFromBackupKey, NSNumber.FromBoolean(true), out NSError err)` is the
  line to look at if that build complains.
* `Ixian-Core` @ `097341a` is cloned beside the container tree (`/home/claude/Ixian-Core`), read-only. Every
  Core claim in #907–#912 was READ there, not assumed. The #912 ① pin reads the folder names from it.

## 1 · What the walk settled (#911, `docs/walk-session-ab-results.md`)

* #902 · #903 · #904 · #907 all hold on the device. **#907 held on the very chat from the screenshot.**
* **AB.9 — the draft SURVIVES the tab-sweep close** inside the 400 ms debounce. Hypothesis closed; no flush.
* **AB.11 — 7×11 is the tail.** 6×10 stays on the sheet as the other dial (`docs/sheets/session-ab/`).
* **AB.17 is a finding, not a gap:** call cards cannot be selected or deleted BY DESIGN (`isMenuableRec` in
  chat.html; the same predicate feeds `selectable`). So "a deleted call comes back as No answer" is
  unreachable from Spixi's own UI. CORE-9 re-scoped in the cutover brief.
* **AB.5 — NOT walked** (the one starred row without a device result). It heads the next walk: Wallet tab →
  Account → Contacts → a contact → Message → close Contacts → tap a tx → back arrow → **empty pane, not
  the chat**. The pin is green; the device has not said so.
* Android deploy hit `ADB1000` once (fast-deploy stream cut) — adb/cable, not code. Recipe in the checklist.

## 2 · What #912 is, and what it is NOT

Damir asked two things on 09-21: why the cold start is "a good few seconds" and — after the delete thread
— "do the easy ones we can do on the app end". Built, all pinned (`#912` ①–④ in the suite):

* ① **Android backup exclusion (APP-1):** `Spixi/Chats`, `Spixi/MsgQueue`, `ixian.log` + five rotations
  are out of Google/adb backup, on ≤11 and 12+ (cloud AND device transfer). `allowBackup` stays true; the
  wallet, `Acc`, the avatar and the preferences are still backed up — a restore that brings back
  `wallet.ixi` is what a backup is for. ⚠ That means the plaintext `walletpass` preference still travels
  with a backup, as it always did — that is **L8**, BE's, deliberately not touched here.
* ② **iOS half:** `NSURLIsExcludedFromBackupKey` on the same two folders from `FinishedLaunching`, per
  folder, never throwing. UNCOMPILED.
* ③ **Windows html copy is per file:** same length AND same UTC write time → skipped (Win32 CopyFile
  preserves the write time, so a newer staged shell still copies). Receipt logged after `Logging.start`.
* ④ **`[STARTDIAG]`** — five milestones: logger up · node constructed · wallet decrypted · root page set ·
  home shell loaded (once per process). **The next launch log is the startup answer.** From reading, the
  suspects in order: a Debug build (no AOT, fast-deploy, log mirroring); the whole node booted on the UI
  thread before any screen (two RocksDB opens, contacts, stream processor, wallet); then a WebView cold
  start + a 540 KB home shell. The wallet KDF (10 000 PBKDF2 rounds) is tens of ms, not seconds.

NOT built, and why: **moving the node boot off the UI thread** (the expensive win; several pages assume the
node exists — measure first, then plan it with BE) · **the Windows data folder out of `Documents`/OneDrive**
(moves the wallet: copy-verify-switch, BE's eye) · **CORE-9/10/11, L8** (Core / BE) · **disappearing
messages** (#908). The order is in #910: CORE-10 → CORE-9 → L8 → (APP-1 ✅) → Windows folder → CORE-11 → #908.

## 3 · ★ Things that look settled and are not

* **The loop stopped at "0 MAJOR", not "CLEAN".** r3's six-line fixes (the posted drain in
  `SpixiContentPage.sendMessage`, a base class) and r4's #907 fixes have had no independent read. The walk
  exercised them on a device (AB.1–AB.10, AB.15–AB.16 green), which is evidence, not a review.
* **AA.8 / AA.9 / AA.10 are still not closures** (`docs/diagnostic-session-aa-findings.md` is live).
* **The privacy wordings** (`docs/legal/proposed-amendments-2026-09-19.md`, now THREE: Mini Apps, GIF
  auto-load, and what "delete" does) were READ (AB.14) but not RULED on. They go to counsel after Damir says
  accept / reword / reject for each.
* **#912's startup numbers do not exist yet.** Do not plan the node-boot change until Damir has pasted five
  `[STARTDIAG]` lines from a cold launch — Debug and, ideally, the Release build the checklist now names.
* **A restore test for APP-1 is owed** (a second phone; a restore that brings back `wallet.ixi` without its
  password must land on the retry screen, not crash). Not a blocker for the testing build.

## 4 · What is genuinely next

1. **Damir: checklist §1b → F5 + Android → paste the STARTDIAG lines → ONE commit** with
   `docs/commit-message-session-ab.txt` (`git add` the two new XML files, the new docs, `docs/sheets/session-ab/`,
   and the `docs/archive/` moves). Then delete `_to_delete/`.
2. **Read the startup numbers.** If "node constructed" is the bulk: plan the off-thread boot with BE (which
   pages touch `Node.Instance` before `HomePage` — grep first). If "home shell loaded" is: the WebView, ours.
   If Release is already under ~1.5 s: it was the Debug build; write that down and stop.
3. **AB.5** at the top of the next walk.
4. **The landscape round** (AND-31/32/33/34), render-first — unchanged from the last handoff.
5. **An iPhone build** — it now also compiles `AppDelegate.excludeHistoryFromBackup` for the first time.
6. Still Damir's, none blocking: the privacy rulings → counsel · the Z.10 chooser copy · S1 timed (#872 ②) ·
   AND-40 · the #890 dials · the translator pass · the freeze (#825) · the BE cutover brief hand-over
   (it now carries CORE-9 re-scoped, CORE-10 with the proposed rule + relay questions, CORE-11, APP-1 ✅) ·
   the #232/#523 money-path review.

## 5 · The workshop

* `device_bash` worked all session; the bridge dropped and came back twice (harmless — retry once).
  `git --no-optional-locks status` only; never plain `git status` on the VM.
* **The suite runner is the container**, on a SNAPSHOT copy (`cp -r tree snap`), ~5 min. Landing: tar the
  changed files → `/mnt/user-data/outputs` → `device_commit_files` into `_to_delete/` → `tar --overwrite`
  on the VM → `sha256sum -c`. Files edited DIRECTLY on the VM (python read-modify-write) must be staged
  back into the container before the next tar, or the tar reverts them — it nearly did this session
  (`CLAUDE.md`, the commit message).
* Archive moves are `mv -n` on the VM (the container cannot delete there); mirror them in the container.
* The mutation harness (`/home/claude/mut.py`) runs the REAL pin text sliced from `smoke-test.mjs`; it must
  assert a green baseline in its own root first (#906).

## 6 · Rules this session paid for

* **A walk closes hypotheses in both directions** (#911): the flush that was feared is not needed; the gap
  that was feared cannot be reached. Neither could be known from the tree.
* **"Can't do X" from a walk is a finding to READ, not a row to fix** (#911, AB.17): the exclusion was
  designed and commented; the fix would have been a regression.
* **Measure before moving a boot off a thread** (#912): five log lines cost nothing; the change they gate
  touches every page that assumes the node exists.
* **Excluding a backup path is a security dial with a restore-side cost** (#912): the wallet stays IN for
  that reason, and the plaintext preference rides with it until L8 — written down, not hidden.
* **A file edited on the VM is not in the container** (#912, workshop): stage it back before the next tar.

## 7 · Files

`DECISIONS.md` (575 rows, #902–#912) · `docs/commit-message-session-ab.txt` · `docs/f5-checklist-session-ab.md`
(§1b added) · `docs/walk-session-ab-results.md` (new) · `docs/be-cutover-brief.md` (CORE-9 re-scoped, APP-1 ✅) ·
`docs/legal/proposed-amendments-2026-09-19.md` (§3) · `docs/sheets/session-ab/` ·
`docs/diagnostic-session-aa-findings.md` (still LIVE) · `docs/next-session-prompt.md`.
Consumed → `docs/archive/`: `handoff-2026-09-19b.md`, `f5-checklist-session-z.md`, `commit-message-session-z.txt`,
`commit-message-session-aa.txt`, `walk-artifact-and-45.html`, `walk-artifact-session-y.html`,
`walk-artifact-session-z.html`, `walk-artifact-session-ab.html` (walked; published copy: the Session AB artifact).
