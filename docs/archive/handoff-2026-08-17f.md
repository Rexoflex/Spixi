# Handoff — 2026-08-17f (post-F5 rewrite). READ FIRST.

**LANGUAGE RULE: ASD-STE100 Simplified Technical English.** See `CLAUDE.md`.

Supersedes `docs/archive/handoff-2026-08-17e.md`. Read dates in headers, never
file names. Rewritten IN PLACE after the #374 F5 verdict (this file was never
consumed, so it keeps the letter).

★ Planning doc: `docs/master-worklist-2026-08-17.md` (R1 + R2 + the D-19
family + the five stale #361 rows struck; N51–N59 added). Open tally:
**38 N-items** (14 in the fix-first block · 16 round items · 8 dials) + the
§B carryovers (D-9 · D1+N6 · I-1 · I-9 · I-10 · I-12).
★ Damir's course: KEEP BUILDING BATCHES; the §4 pile waits for one pass.

---

## 1. State — verify it, do not trust it

```
git --no-optional-locks log --oneline -3
git --no-optional-locks status --porcelain
```

Damir commits the #370–#373 batch after the #374 verdict (title + body in
`docs/f5-checklist-2026-08-17-fix.md`) — the commit should be at HEAD when you
read this; VERIFY. He also deletes the stale root `docs/handoff-2026-08-17e.md`.

| Item | Value |
|---|---|
| Batch #370–#373 | ① D-19b family (amended ladder · C# reverse-resolve + render→spend tip binding · pseudo-nick guard) + N48 · ② N49 + N50 · ③ R2. **F5 #374: B/C/D/E PASS — committed** |
| ★ F5 section A | **ALL FIVE LEGS FAIL — root cause is IXIAN-CORE, not the batch** (#374, verified line-by-line): 0.9.8k stores every bot-room message with a NULLED address; the nick stores only if the 500-cap roster is warm at receive; **and Core's own nick backfill is structurally DEAD in bot rooms** (it keys on the address the same function just nulled). Rows since the bump are identity-less PERMANENTLY. B1's pass proves the FE ladder works wherever identity exists. **Nothing FE remains to build here.** → be-cutover **Q1-ESC** |
| Smoke | **BASELINE OK — 1858 pass / the 4 KNOWN pre-existers (#136 · #149③ · M5 · B3)** |
| Locales | ALL CLEAN · lint ✓ · pseudo 9/9 · zero em/en-dashes in the 8 draft sets |
| Loop | #46 on Opus ×2 rounds (#372/#373): 3 MAJOR found+fixed r1 (CS0136 · the money binding · the raw-echo guard), r2 PASS |
| Owed F5 legs | D5 (theme-flip back on contact details) · the E3 "Request received" badge leg (interrupted by the N57 find) |

## 2. NEXT SESSION — the F5 fix batch (build, no Damir input needed to start)

Verify each against code first (#215); pins + the #46 loop; anything needing a
dial → log it, skip it. Order:

| # | Item | What (evidence in the worklist §C rows) | Size |
|---|---|---|---|
| 1 | **N51** | ★ Android back over a chat bottom sheet EXITS THE CHAT. The N50/#370 cdoverlay/cdBack grammar applied to chat.html (chat is a HomePage overlay; the sheet is invisible to C#). + the hand-rolled channel selector needs its own arm. **AND-37 (settings.html sheets) = same family — do both in one pass** | S |
| 2 | **N54** | Typing indicator yanks the view to bottom — gate `showTyping()`'s scroll on `nearBottom()` (chat.html:1214; predicate exists at :1416) | XS |
| 3 | **N53** | New-message indicator while scrolled up — `setScrollLatestCount` shipped in #74 and was NEVER fed; wire live arrivals, reset at bottom | S |
| 4 | **N52** | @-jump target must visibly HIGHLIGHT (a pulse is claimed at chat.html:3672 — verify why it does not read on device FIRST) · `Config.messagesToLoad` 25→≥50 (⚠ #343 cut it 100→25 for chat-entry speed; every row is a bridge marshal (#298) — re-measure the A52 after) | S |
| 5 | **N55** | Optimistic "Contact request sent" toast at the emit sites (fire-and-forget verb, no ack push) | S |
| 6 | **N56** | Pinned rows: very light tint (the #236 marker dial, now given). Ladder: selected > pinned > hover; dark contrast check | XS |
| 7 | **N58** | Chats-list avatar PHOTOS flicker on every screen entry — the apps-list class (#340 BUG-2② decode-cache precedent). Verify the mechanism, then cache | S |
| 8 | **N59** | Reduce the Account row title↔subtitle gap (E5 dial, I-11 stack) | XS |
| 9 | **N57 TRIAGE ONLY** | Group message visibility vs connectivity (Damir: "legacy worked without this"). Write the 2-device repro protocol for Damir; determine arrive-vs-hidden; NO build — likely BE (the MESSAGE twin of N33/q11) | triage |
| 10 | **N36b** | ★ RE-OBSERVED by Damir 2026-08-17 ("selecting multiple messages still has some pressed effect") — that IS the on-device repro the pile was gated on. Apply the pre-gathered #363 candidate (`-webkit-tap-highlight-color: transparent` scoped to `.c-bubble-row` / `[data-selecting]`; message-bubble.css:43 sets none today) + pin. ⚠ #363 caveat stands: if the flash survives the candidate, a SECOND layer hides beneath — report, do not stack fixes | XS |

NOT in this batch: the Android notification stacking (**be-cutover NT1**, BE) ·
anything bot-room identity (**Q1-ESC**, BE — see §1) · **N4 locale expansion**
(L) = the session after.

## 3. Damir — two things beside the F5

1. **Send the BE engineer be-cutover `[Q1-ESC]`** (+ NT1 while you are there).
   The one-word Core fix now also revives Core's own dead backfill — the row
   has the exact lines. Old rows stay anonymous even after the fix; the row
   says to state that honestly.
2. **Dials still open (log answers, no build until given):** blind-bubble
   avatar PHOTOS (hide in blind rooms? #372) · N48 chip on non-blind heros
   (#370) · ru/lt legacy dashes (#371) · de requestSent retune (#371) · N3
   voice-sweep target list (#371) · the A2 fallback: until Q1-ESC lands,
   anonymous bot-room rows are UNLABELED (your D-19b dial); the alternative is
   the "Hidden member" placeholder you rejected — recommendation: keep
   unlabeled, pressure Q1-ESC (#374).

## 4. How to work — unchanged

Cloud twin recipe as in 15c §7 (archive): clone, checkout the branch tip,
`npm install jsdom --no-save`, bundle → shells → smoke; tarball delivery
(⚠ the mount cannot unlink — plain `tar xzf` in PowerShell; Move-Item/mv works;
`git --no-optional-locks` always). Standing set: verify plans against code ·
#294 · #215 · smoke green at **1858/4** (state the new number when it moves) ·
mutation-honest pins, PROVEN · Damir commits · DECISIONS rows at decision time ·
the #46 loop on anything substantial · **Opus for every adversarial pass
(#352)** · the security handover gate while building. Ixian-Core reference:
clone + checkout `097341a` in the twin. Deploy (Damir): wipe obj/bin →
Android `dotnet build Spixi\Spixi.csproj -f net10.0-android -c Release -t:Run` ·
Windows `dotnet build ... -f net10.0-windows10.0.19041.0 -c Debug` + run the exe.

## 5. THE DEFERRED PILE — fix/triage later, in one pass (do NOT build now)

*(N36b left the pile 2026-08-17 — Damir re-observed it on Android, which was the
repro gate; it rides the §2 batch as row 10.)*

| ID | Item | State + pointer |
|---|---|---|
| **N13** | Onboarding "Back up now" does nothing at account creation | 🔴 TOP of the pile — data-loss class |
| **D-17b** | Apps search bar ALWAYS visible | Dial LOCKED — 17d §5 (archive), verbatim |
| **N40** | "Connecting…" stops on a LIVE document after long offline use | Suspect on file: update-branch starvation (#357) |
| **N10** | App-invite Cancel in chat does nothing | Triage (C# here + maybe BE) |
| **N46** | Delete-flow rework: branded checkboxes + UX pass | ★ REMIND Damir near master-list completion (his ask, #369) |
| **N47** | Nickname reset to address-only? | Dial (#369 B3) |
| Legs 29–30 | Compact-balance · flick-cancel (A52) | Untestable two rounds running |
| #368 log-only | Own-bubble Owner asymmetry · long-nick chip overflow · modal aria-describedby · agent-chat-demo stale avatars · step-2 oldest-message relation edge · BOT glyph Tabler export | Fold into related rounds |
| #367 follow-up | Remove-blocked path-out ACTIONS (leave-group buttons) | Damir: later |
| #372/#373 log-only | F-4 ≤400 ms back-eaten window (self-heals) · dead C# contextAction request/kick/ban cases (pre-existing) · de settings-backup-description doubled id (legacy-only) · reverse-resolve residual (nick reassigned BEFORE render — BE/Core nick authority) | Fold into rounds / BE list |

## 6. Do not re-attempt

All of 17e §5 (in `docs/archive/handoff-2026-08-17e.md`), plus:
- No SHELL-side reverse-resolve of nick→address — partial roster = false
  confidence on a money path; the rule lives in C# only (#370).
- No spend-time tip resolve WITHOUT the render→spend binding (#372 A-2).
- No unconditional present-time `selectChat` stamp (#373 F-1).
- No `amOwner` for BOT rooms, either side (#372 A-5 / #373 F-5).
- `senderHasNick` is the ONE nick predicate; never re-inline `nick !== address`
  checks (#373 F-2).
- The pseudo-nick guard fires on '[Unknown]'-masked rows ONLY (#370/#372).
- ★ No FE attempt to conjure bot-room sender identity for rows Core stored
  without any — no per-message hashing, no synthetic labels, no placeholder
  flip-flop. The fix is Core (Q1-ESC); FE churn there is waste (#374).
