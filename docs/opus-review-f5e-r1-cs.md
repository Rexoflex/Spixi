# Opus #46 loop — Auditor A (C#), round 1

Scope: the six C# files of the F5 fix batch (#553 F5-3 · #554 F5-1 · #555 F5-2) and
their callers. Read-only. Tree: `redesign/frontend`, uncommitted on `0acd5ada`.
Ixian-Core consulted read-only at the frozen sibling checkout.

**Counts: 1 MAJOR · 4 MINOR · 4 NIT.**

The MAJOR is not in the diff — it is in the code the diff's headline claim depends on.
F5-1 routes the missed call to `endVoIPSession` correctly, and `endVoIPSession` then
**cancels** the tray row instead of re-posting it, on every incoming call, always.

---

## Findings

| ID | Sev | file:line | Claim | Evidence | Proposed fix |
|---|---|---|---|---|---|
| **A-1** | **MAJOR** | `Spixi/VoIP/VoIPManager.cs:212` (predicate) · `:92` (the latch) · `:220` (the dead branch) — claim made at `Spixi/Network/StreamProcessor.cs:812-821` and `docs/f5-findings-2026-08-24-overnight.md:9-13` | **The tagged "Missed call" re-post is UNREACHABLE. F5-1's headline claim — "the pair now resolves in order: session up → hangup → tagged missed-call row" — is false, and Damir's symptom (the tray row disappears) survives the fix.** | `onReceivedCall` sets `currentCallCalleeAccepted = true` at ring time, unconditionally (`VoIPManager.cs:92`), for **every** incoming call. `endVoIPSession`'s branch is `if (currentCallAccepted \|\| currentCallCalleeAccepted \|\| currentCallInitiator)` (`:212`) → **always true for an incoming call** → `SPushService.cancelNotification(callNotifId)` (`:215`). The else branch that posts "Missed call" (`:220-227`) can never run: reaching it needs all three false, and `currentCallContact != null` (the enclosing guard at `:209`) means a session existed, which means one of them was set. Note the SAME file computes the correct predicate 54 lines later — `bool callAccepted = currentCallAccepted && currentCallCalleeAccepted;` (`:266`) — so `:212` is a defect, not a convention. `cancelNotification` cancels the tagged row too (`Platforms/Android/SPushService.cs:242` `manager.Cancel(CALL_TAG, messageId)`), so #549's tag does not protect it. ⚠ The fix makes the eater **more deterministic**, not less: pre-fix the end-session bailed on the fetch thread and the 45 s `startRingTimeout` (`VoIPManager.cs:586`) reached `endVoIPSession` anyway — which is exactly a row that appears and then vanishes. Post-fix it vanishes at once. | One line: `if (callAccepted \|\| currentCallInitiator)` — hoist the `:266` computation above `:206` and use it at `:212`. Then re-verify on device: a fetched missed call must leave ONE tagged row on the shade. ⚠ Do **not** close F5-1 on "the row now appears" alone — the cold-push lane posts its own untagged row under a DIFFERENT id (`SPushService.cs:734`, `notificationIdFor(addr, false)`), which the sweep still eats; that half is the brief's accepted residual (BE §1e-5) and is stated honestly. |
| **A-2** | MINOR | `Spixi/Network/StreamProcessor.cs:728-751` · `:773-795` · `:823-845` | The three deferrals moved **more than the VoIP re-check** onto the main thread: the whole mini-app `getAppPage` fall-through went with them. That is scope the fix did not need, and it adds a second queue hop that can invert mini-app SDK event order. | `MiniAppPage.appRequestAcceptReceived` / `RejectReceived` / `EndSessionReceived` **already** post to the main thread themselves (`Pages/MiniApps/MiniAppPage.xaml.cs:504-546`). Wrapping them in another `BeginInvokeOnMainThread` means the actual `SpixiAppSdk.onRequestAccept` command is enqueued one drain **later**. `handleAppData` (`StreamProcessor.cs:576-596`) still runs synchronously on the stream thread and posts once. So an `appData` packet arriving immediately after an `appRequestAccept` can now deliver `SpixiAppSdk.onNetworkData` **before** `onRequestAccept` — an inversion that was impossible when both were posted from the same stream thread. (Conditional on MAUI's `MainThread.BeginInvokeOnMainThread` not short-circuiting when already on the main thread; if it does short-circuit the inner post runs inline and order is preserved. No .NET toolchain here to settle it — flagged rather than asserted, per #294.) Second effect, unconditional: in the deferred case `VoIPManager.onHangupCall` → `endVoIPSession` now runs `audioPlayer.Dispose()` / `audioRecorder.Dispose()` / `requestWriteMessages` / `Thread.Interrupt` on the **UI thread** (`VoIPManager.cs:236-323`); it used to run on the stream thread. `page.accepted` is a write-only dead field (`MiniAppPage.xaml.cs:35`, no reader) so nothing regresses there. | Defer ONLY the re-check. Shape: `if (hasSession) {sync}` → else `MainThread.BeginInvokeOnMainThread(() => { if (hasSession) {VoIP…; return;} runMiniAppFallThrough(); })` is what shipped; instead keep the fall-through on the calling thread by posting a **VoIP-only** block and letting the mini-app path run synchronously after it, or hop back with `Task.Run` for the page branch. Either removes the inversion and keeps the VoIP teardown off the UI thread. |
| **A-3** | MINOR | `Spixi/App.xaml.cs:1383-1387` | The F5-3 guard's boundary is **one step early**. "The launch flow owns the node until a wallet is loaded" is not the real boundary — it owns it until `HomePage` **starts** the node, which is strictly after `Node.loadWallet()`. The window between them still admits the double start. | Restore: `restoreAccountFile` calls `Node.loadWallet()` (`Pages/Launch/LaunchPage.xaml.cs:664`) — `IxianHandler.wallets.Count` becomes 1 — then `goHome()` (`:735`) posts `HomePage.Instance(true)` to the main thread, whose ctor runs `Node.preStart()` (`Pages/Home/HomePage.xaml.cs:237`) and `Task.Run(… Node.start() …)` (`:331`). Identical shape on the retry path (`proceed` → `loadWallet` `:704` → `goHome`). An `OnResume` landing inside that window now passes the guard and runs `preStart + start + connectToNetwork` (`App.xaml.cs:1399-1410`); `HomePage`'s own `Node.start()` then returns false and takes the **fatal-alert-and-return** branch (`HomePage.xaml.cs:331-335`) — which is exactly the face of Damir's F-3 ("fatal exception, no network, empty lists"). Narrower than the logged race (the picker's `OnResume` fires long before `loadWallet`), so the fatalexception.txt sequence IS healed; the residue is a different trigger (screen off/on, a permission dialog, an OEM resume) in a shorter window. Also note both callers can pass `Node.preStart()`'s `if (running) return` simultaneously (`Meta/Node.cs:144`) and run `localStorage.start()` + `FriendList.loadContacts()` twice — pre-existing, not closed by this guard. | Make the ownership explicit rather than inferred: a `launchFlowOwnsNode` flag set when the launch flow stops the node and cleared by the site that starts it (HomePage ctor / `App` boot), tested alongside `wallets.Count == 0`. Cheap alternative that covers most of it: also bail when `HomePage.InstanceOrNull() == null` (no host has claimed the node yet). |
| **A-4** | MINOR | `Spixi/Pages/Contacts/ContactDetails.xaml.cs:347-352` · `Spixi/Pages/Home/HomePage.xaml.cs:4095-4099` | The breadcrumbs bracket only the **synchronous** portion of the teardown, and the findings doc's own prime suspect ("the UI teardown around the removed row") is **asynchronous** — so the diagnostic will print "teardown done" and then the app dies in unbracketed code, wrongly exonerating the path. | `popToRootAsync` is async on **both** of its branches: the overlay branch calls `closeOverlay(...)` which stages/animates, and the native branch is a whole `MainThread.BeginInvokeOnMainThread(async () => …)` (`Utils/SpixiContentPage.cs:3023-3053`). `displaySpixiAlert` and `chat_page.popPageAsync()` are likewise fire-and-forget. So all four ContactDetails lines (and all three HomePage lines) land within one main-thread turn, before any navigation work runs. | Add a breadcrumb inside the async continuation — e.g. one line at the end of `popToRootAsync`'s posted lambda and one after `removeDetailContent`'s work — or log from `OnDisappearing`/`Dispose` of the page being torn down. Otherwise state plainly in the F5 checklist that "teardown done" does **not** mean the teardown completed. |
| **A-5** | MINOR | `Spixi/Pages/Contacts/ContactDetails.xaml.cs:331,345,348,352` · `Spixi/Pages/Home/HomePage.xaml.cs:4090,4093,4099` | Seven **unbounded** `Logging.flush()` spin-waits were added to UI-thread code paths. `flush()` has no timeout. | `Ixian-Core/Meta/Logging.cs:456-470`: `while (running && logFileStream != null && getRemainingStatementsCount() > 0) Thread.Sleep(25);` then `lock (logfilename) { logFileStream.Flush(); }`. The drain is done by a separate writer thread (`threadLoop`, `:263`), whose `log_internal` takes `lock (logfilename)` and calls `Logging.roll()` (file rotation, `maxLogCount` = 5) inside it. Normal case is milliseconds. Failure case is not bounded: a stalled writer, a slow rotation, or a busy queue holds the UI thread with nothing to break the loop. **Not a deadlock** — `flush()` never holds a lock while spinning, and there is no inversion between `statements` and `logfilename`. In the crash hook (`MainApplication.cs:56`) the spin is harmless (the process is dying); on the leave/remove paths it is a real, if low-probability, ANR shape. | Keep ONE flush per path (after the last breadcrumb) rather than four/three, or bound it — a `flushWithTimeout(ms)` in Spixi that stops spinning after ~250 ms. The diagnostic value is the same. |
| **A-6** | NIT | `Spixi/Pages/Chat/SingleChatPage.xaml.cs:373-393` | A **third** leave path exists, byte-identical to the bracketed ContactDetails body, and has no breadcrumbs. It is currently unreachable, so this does not invalidate F5-2 — but the fix's comment claims coverage without naming it. | `chat.html:2542` emits `ixian:leave` from the in-chat chat-info takeover, and `chat-info.js:944` renders the Leave row for `kind === 'bot'` as well as `'group'` — i.e. exactly the repro. It is dead only because `chat.html:872` routes every info tap to `ixian:details` and the takeover block is marked "RETAINED BUT CURRENTLY UNREACHABLE" (`chat.html:2440-2445`). One shell edit away from being live. | Either bracket it too (three log lines, same shape) or delete the handler with the takeover it belongs to. At minimum, say in the F5 checklist which surface the repro must use. |
| **A-7** | NIT | `Spixi/Platforms/Android/MainApplication.cs:39-46` (comment) · `docs/security-handover-gate.md:551` | Two small overstatements in the F5-2 prose. The acceptance itself is honest — the gate row exists, names the reach, and carries a retirement condition — but two of its sentences are wrong. | (a) "This hook also sees exceptions that the Java side raises into the runtime" — `AndroidEnvironment.UnhandledExceptionRaiser` fires for exceptions unhandled on a thread **with managed frames**, about to cross back to Java. A pure Java-side throw with no managed frame (an Android framework `IllegalStateException` inside a Java call chain) goes to Java's `DefaultUncaughtExceptionHandler`, not here. The comment hedges SIGSEGV but not this. (b) The gate row says "The breadcrumb lines are fixed text + two booleans, no address" — `HomePage.xaml.cs:4093` logs `status`, a **string**. The security property still holds: `SContacts.removeContact` returns only `"ok" \| "left" \| "blocked" \| "fail"` (`Utils/SContacts.cs:101-135`), a fixed vocabulary with no user data. | Correct both sentences. The `args.Exception` verbatim reach stays accepted as written. |
| **A-8** | NIT | `Spixi/VoIP/VoIPManager.cs:294-300` | `currentCallInitiator` is **not** reset in `endVoIPSession` while every other call field is. It stays latched after an outgoing call until the next `initiateCall`/`onReceivedCall` writes it. | The reset block clears `currentCallSessionId`, `currentCallContact`, `currentCallCalleeAccepted`, `currentCallAccepted`, `currentCallCodec`, `currentCallStartedTime`, `currentCallInitiated` — and nothing else. Harmless today only because both writers set it explicitly; it becomes a live wrong answer the moment A-1's predicate starts reading it in isolation. Fix A-1 and this together. | Add `currentCallInitiator = false;` to the reset block. |
| **A-9** | NIT | `Spixi/Pages/Home/HomePage.xaml.cs:4089-4099` | On the HomePage path the breadcrumbs add less than they look like they do: the whole block already sits inside a catch-all. | `onRemoveContactFor`'s body is wrapped in `try { … } catch (Exception) { Logging.error("ixian:removecontact failed …"); }` (`:4062`, `:4102-4106`), so a **managed** exception thrown between the breadcrumbs is already caught and logged today. The added value on this path is therefore limited to a native crash or an escape on a later main-thread turn (A-4). The ContactDetails path has no such wrapper — it is a raw WebView `Navigating` handler — and there the breadcrumbs are genuinely load-bearing. | None required; worth one sentence in the F5 checklist so a clean HomePage bracket is not read as "the crash is not here". |

---

## Verified clean (checked, no finding)

| Question | Verdict | Evidence |
|---|---|---|
| Does the F5-3 guard block a **legitimate** restart after a background shutdown? | No. | `Node.stop()` (`Meta/Node.cs:574-630`) never touches `IxianHandler.wallets`, and Core's `removeWallet` refuses to remove the primary (`Ixian-Core/Meta/IxianNode.cs:374-384`). Once loaded, the wallet stays for the process's life → `Count > 0` → the guard falls through and the old behaviour runs. |
| Cold boot / OS-kill resume — who loads the wallet, and when? | Before any `OnResume`, and the guard is inert either way. | `App()` ctor (`App.xaml.cs:161`) runs inside `MauiApplication.OnCreate`, ahead of any Activity lifecycle, and calls `Node.loadWallet()` at `:323`. And the node has never been started at that point, so `IxianHandler.status` is still its initial `warmUp` (`Ixian-Core/Meta/IxianNode.cs:131`, `:49`) → `EnsureNodeRunning` takes the "already running" else branch and does nothing, guard or no guard. Wallet present but undecryptable → retry view, `Count == 0`, guard fires, correct. |
| Is the fatalexception.txt sequence actually healed? | Yes for the logged trigger. | At 12:28:00 the create flow had been backed out of, so no `generateWallet` had run — a completed create navigates to Home and never returns to the launch flow (`LaunchPage.xaml.cs:488-491`) — hence `wallets.Count == 0` and the guard returns before `Node.preStart()`/`start()`. The zombie (running=true latched at `Node.cs:169` before the wallet read at `:230`) cannot form from this entry point. Residual trigger: A-3. |
| Interaction with the #545 delete-account wipe | **Positive** — the guard closes a hole the wipe opened. | `wipeEverything` calls `IxianHandler.wallets.Clear()` (`Pages/Settings/SettingsPage.xaml.cs:1124`) and `IxianHandler.shutdown()`, then `goToWelcome()`. Backgrounding and returning on that welcome screen used to hand `EnsureNodeRunning` a stopped node with no wallet — the same zombie. It now returns early. |
| Interaction with #505 `SDesktopIdle.start()` | None. | It is called after `EnsureNodeRunning` in `OnWindowCreated` (`MauiProgram.cs:186-189`); the guard's `return` exits only its own method. The watcher itself is gated on `app.isLockEnabled()` (`Platforms/Windows/SDesktopIdle.cs:157`), a preference that a create clears, so it cannot fire on the launch flow. |
| Is `MainThread` FIFO ordering behind the queued session creation a sound premise? | Yes, on the delivery path that matters. | Fetched messages are processed **synchronously in a foreach on the fetch thread** — `OfflinePushMessages.fetchPushMessages` → `streamProcessor.receiveData(data, …)` (`Ixian-Core/Streaming/OfflinePushMessages.cs:165`), and `receiveData` is synchronous through to the handlers (`Ixian-Core/Streaming/CoreStreamProcessor.cs:644`). So `handleAppRequest` posts block A, then `handleAppEndSession` posts block B, from one non-main thread; Android `Handler.Post`, iOS `DispatchQueue.MainQueue`, WinUI `DispatcherQueue` are all FIFO at equal priority. The mainLoop caller is a `Task.Run` thread and the push lane is an SDK callback thread — neither is main, so no inline-execution reordering of A vs B. |
| Can the same end-session run BOTH paths (double-handling)? | No. | The sync check returns before the deferral (`StreamProcessor.cs:718-722` / `:761-765` / `:804-808`); the deferred block is only ever reached when the sync check failed. One invocation, one path. |
| Deferred re-check finds a session created **and already ended** | Benign. | `hasSession` compares against `currentCallSessionId`, nulled at `VoIPManager.cs:294`; a stale sid → false → falls to `getAppPage` → null → `"App session does not exist."` and return. Session ids are GUIDs (`:54`), so no cross-call collision. A stale fetched accept/end for a call from a previous session behaves the same. |
| Does a stale fetched call ring audibly, and is the residual stated honestly? | Yes and yes. | Block A runs `SPlatformUtils.startRinging()` (`VoIPManager.cs:129`), block B runs `stopRinging()` via `endVoIPSession` (`:183`) on the **same** main-thread drain — sub-frame. The brief's residual ("may ring for one main-thread drain … start→stop within the queue drain") is accurate. ⚠ One rider worth a checklist line, pre-existing and unchanged by this batch: block A also posts a **nested** mic-permission request when the grant is missing (`:121-127`), and that nested post lands *after* block B — so a stale fetched missed call can pop the Android microphone dialog for a call that is already over. |
| `Logging.error(string)` and braces in an exception body | Safe. | `Logging.error(string message)` → `Logging.log(severity, message)` verbatim; the `params object[]` overload is a separate signature (`Ixian-Core/Meta/Logging.cs:500`, `:541`). No `String.Format` on the hook's message, so a `{` in an exception text cannot throw. |
| The mainLoop guard's effect on `fireLocalNotification` | Correct. | The flag is cleared only inside the branch that actually ran the fetch (`Meta/Node.cs:419`), so a wallet-less skip cannot eat the first-pass notification burst. |
| Unlocked `IxianHandler.wallets.Count` read in both guards | Acceptable, with precedent. | Core locks the dictionary for mutation (`IxianNode.cs:357`, `:376`) but `Count` is a field read that cannot throw, and the identical unlocked predicate already ships two hundred lines away in the same file (`App.xaml.cs:1319`, the `CreateWindow` push guard the comment cites — citation verified accurate). |
| Any address or user data in the new **breadcrumb** lines | None. | Two booleans, one `chat_page != null`, and a fixed-vocabulary status string (A-7b). The hook's `args.Exception` body is the only reach, and it is the accepted, retirement-conditioned row at `docs/security-handover-gate.md:551`. |

---

## What round 2 must re-check

1. **A-1 first.** It is one line, and it is the batch's stated purpose. Do not accept a
   fix on reading — the F5 must show a tagged row **surviving** on the shade after a
   fetched missed call, and the untagged cold-push row (BE §1e-5) must not be mistaken
   for it.
2. Whether the A-2 fix reintroduces the original race in the mini-app branch.
3. A-3's window: if a `launchFlowOwnsNode` flag lands, verify it is cleared on **every**
   path that starts the node, including the `App.xaml.cs:370` resume branch.
