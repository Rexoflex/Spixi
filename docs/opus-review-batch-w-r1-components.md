# Opus #46 loop — Batch W · AUDITOR 1 (components + CSS + demos)

Scope: `wallet-send.js` · `wallet-receive.js` · `contact-row.js` (NEW) · `contact-row.css` (NEW) ·
`wallet-send.css` · `wallet-receive.css` · `base.css` press lists · `pressable.js` ·
`src/demo/{wallet,chat,desktop}.html`. Tree = uncommitted delta on `42e72109`.

**Method.** Read the delta, then DROVE the live components in jsdom (the smoke harness `load`
helper, throwaway scripts, since removed). Every MAJOR below was REPRODUCED, not reasoned.
The full suite was also run: `BASELINE OK — 2867 pass / the 3 KNOWN pre-existers`, so nothing
here is a suite regression — these are defects the suite does not ask about.

**Verified sound** (checked, no finding): every `var(--…)` in `contact-row.css` and the new
`wallet-receive.css` block resolves in `tokens.css` · `extract-strings --check` = 0 fallback
conflicts, and `addressOnly` / `requestSent` / `contactPendingLabel` all exist in `en-us.json` ·
the W-j drift fence is MUTATION-HONEST (flipping `min-height` or `gap` in a scratch copy makes it
fail) · `contact-row.js` is a faithful port of `contacts-shell.pickerRow` incl. the `aria-label`
grammar · `base.css` press-family membership is complete (4 lists) and `pressable.js` classifies
the row as `row`, not `control` · `button { font: inherit }` exists, so the sub-line is not a UA
font · no import cycle (`wallet-receive → wallet-send` is one-way) · all 10 icon ids resolve ·
hover is `(hover:hover) and (pointer:fine)` everywhere · overlay `setOverlayOpts` merges, so the
in-flight lock does not drop `onDismiss` · the mobile `.c-addr-sheet` cap fits a 320px-tall
viewport · `wallet.html` / `chat.html` / `desktop.html` all mount with ZERO page errors and carry
both new stylesheets · no residual `.c-wallet-send__contactname` / `__pendingtag` / `__addrglyph` /
`.c-wallet-receive__check` references anywhere.

---

## Findings

| # | Sev | File:line | Failing scenario | Fix |
|---|---|---|---|---|
| M1 | **MAJOR** | `src/components/wallet-send.js:627-642`, `:655-668` | **A quote that lands AFTER Confirm was pressed rewrites the money on screen to numbers that are not in the submitted payload.** REPRODUCED: open the sheet with fee `0.1`, press Confirm (payload leaves with `fee:"0.1"`, total 10.5), then `setQuote({fee:'9'})` → the sheet reads **Fee 9 IXI · Total 19 IXI** while the spinner runs on a payment of 10.5. `setQuote` calls `render()` unconditionally, and `render()` is not aware of `sending`. Same call ALSO does `confirm.disabled = false` on a button `setLoading` had just disabled — re-creating the exact state `button.js:124-128` documents as "a double-payment hazard on a processing Pay" (the second fire is caught by the `sending` guard, but the button is visibly live under a spinner with `aria-busy="true"`). Reachable: `chat.html:3968` pipes every `setSendQuote` push straight into `setQuote`, and C# can re-quote at any moment. Violates #26 / audit M3 "what you approve is what is sent". | `if (sending) return;` at the top of `render()` (or snapshot `feeU`/`aU` into the row text at Confirm and stop re-rendering). `render()` must never touch `confirm.disabled` while `data-loading` is set. |
| M2 | **MAJOR** | `src/components/wallet-send.js:636-641` | **The sheet never clears its own insufficient-balance error.** REPRODUCED: `setQuote({balance:'5'})` → red `role=alert` "Not enough IXI…", Confirm shut (correct). Then `setQuote({balance:'100'})` → Confirm **enabled** and the red alert **still on screen**. `render()` sets `sheetErr` when `over` and has no else-branch. A money confirmation that simultaneously says "not enough" and offers "Confirm & send" is the worst possible reading. Same class: a `qErr === 'address'` return at `:660-665` never calls `render()`, so the Fee row stays frozen on "Calculating network fee…" beside the address error forever. ⚠ The W-d pin at `scripts/smoke-test.mjs:13997-13999` tests the recovery quote but asserts only `!confirmBtn.disabled` and the two row values — **it walks straight past this**. | Track what `render()` itself wrote (`let overShown`), and clear `sheetErr` in the `!over` branch — without wiping a `fail(msg)` message. Add `err.hidden` to the recovery pin. |
| M3 | **MAJOR** | `src/components/wallet-send.js:584-619`, `:669`, `:490-499` | **A throwing `onConfirm` bricks the money sheet permanently.** REPRODUCED: `onConfirm: () => { throw … }`. The handler sets `cancel.disabled = true` and `setOverlayOpts(…lightDismiss:false, escDismiss:false)` BEFORE calling out at `:619`; the throw escapes, so `fail()` never runs. Result: Esc → refused · scrim → refused · hardware back (`dismissTopOverlay`) → returns `true`, consumed, refused · **`ctrl.close()` → refused** (it is gated on the same `sending`). No exit but killing the WebView. The compose is wedged too — `state.sending` stays `true` at `:492`, so Review can never open again. `bridge.send` throwing is enough to trigger it, and W-d hands this API to a second shell. | Wrap the hand-off: `try { onConfirm(payload,{done,fail}); } catch (e) { fail(strings.sendFailed); }`, and mirror it around `onSend` at `:496`. Consider making `ctrl.close()` a real escape hatch (it is the only caller-side lever and it currently refuses exactly when it is needed). |
| M4 | **MAJOR** | `src/components/wallet-send.js:484-500` | **Double-tapping "Review" stacks two review sheets, and the survivor can fire a SECOND send for the same payment.** REPRODUCED: `cont.click(); cont.click()` → 2 stacked `.c-sendreview` sheets. Confirm the top one → `onSend` fires, `done()` → success morph → `onDone` → **the shell navigates away** (chat: `closeSendTakeover()`; wallet: `showHome()`). Sheet 1 is hosted on `document.body`, so it SURVIVES the navigation, still shows an un-latched Confirm, and pressing it emits a second identical `ixian:signSend` — `onSend` was called twice with byte-identical payloads. `openSendReview` guards only `state.sending`, which is false until Confirm. `cont` carries no `[data-acted]` latch. Contradicts #72④ ("one send in flight; latches; no double fire"). ⚠ Pre-existing SHAPE (HEAD has the same guard), but W-d is the batch that made this an exported shared surface and gave the sibling entry point the guard it lacks here (`chat.html:2751 if (chatPayReview) return;`). | Hold the open sheet in a per-view slot and return early if it exists (mirror `chatPayReview`), and/or latch `cont` with `dataset.acted` released on the sheet's `onDismiss`. |
| M5 | **MAJOR** | `src/components/wallet-send.js:596-603` | **`ctrl.done()` is neither idempotent nor terminal.** REPRODUCED: (a) `ctrl.done(); ctrl.done()` in one attempt → **`onDone` fires twice** (the wallet demo inserts two pending tx rows; a shell toasts twice / navigates twice). (b) `ctrl.fail('Network down')` then `ctrl.done()` in the same attempt → the sheet shows the red error AND morphs to "Sent", and `onDone` fires — a failed attempt reported as a success. The `my !== attempt` guard only rejects a SUPERSEDED attempt; there is no `if (sent) return;` and no "this attempt is finished" state. The `sent` flag at `:599` is computed but only ever read by `onDismiss`. | `const done = () => { if (my !== attempt \|\| sent) return; … }` and have `fail()` mark the attempt finished (`++attempt`) so a late `done()` for it is rejected. |
| m1 | MINOR | `src/components/wallet-send.js:400-401` | **W-i drops focus to `<body>` on the production path.** With an amount already typed, `pick()` hides `picker` (which holds the focused row) and then calls `cont.focus()`. In the quote flow — `fee:null` + `onQuote`, i.e. exactly `home.html:2198-2206` — `sync()` has just set `cont.disabled = true` (no fee answers the new pair yet), and `focus()` on a disabled button is a no-op. VERIFIED `cont disabled after pick: true`. Before W-i, `pick()` always focused `amtInput`, which always worked. The docblock's stated benefit ("focus goes to Review, not back up into the field") therefore never happens in production either. jsdom does not blur on `hidden`, so no pin can see this. | Focus `cont` only when `!cont.disabled`; otherwise fall back to `amtInput` (or focus the `picked` row and make it programmatically focusable). |
| m2 | MINOR | `src/components/wallet-send.js:658`, `:666`, `:471-476` | **A non-numeric bridge value throws out of `setQuote` / `_applySendQuote`.** REPRODUCED: `setQuote({fee:'0.2', address:'A', amount:'not-a-number'})` → `Cannot convert not-a-number to a BigInt`; `setQuote({fee:'abc'})` → same. `toUnits` (`money.js:43-49`) is unguarded by design because every other caller feeds it sanitized text — but `chat.html:3968` and `home.html:3326` pass the bridge's raw echo through. The throw escapes the push handler; the sheet stays gated on "Calculating network fee…" with no way forward. | Guard the three entry points: `const n = (v) => /^-?\d+(\.\d+)?$/.test(String(v)) ? toUnits(v) : null;` and ignore a value that fails. |
| m3 | MINOR | `src/components/wallet-send.js:707-720` | **`setSendRecipient` ignores `lockedRecipient`.** REPRODUCED on a locked (chat-Pay grammar) compose: `setSendRecipient(view, {name:'Attacker', address:'ATTACKER…'}, '…:send:500')` → the picked row silently becomes **Attacker / ATTACKERA…999999** with amount **500**. `lockedRecipient` is the #139 "the peer is fixed" contract — it is why the ✕ is suppressed at `:371`. Not reachable today (only `home.html:3362` calls it, on the unlocked wallet compose), so this is a latent contract hole on a money surface, not a live exploit. `setSendAddress` is defanged by the hidden picker but still seeds the amount and leaves `aria-expanded="true"` on a row inside a hidden container. | One line at the top of both: `if (el.querySelector('.c-wallet-send__picked') && !el.querySelector('.c-wallet-send__clear')) return false;` (or expose a `locked` flag on `el`). |
| m4 | MINOR | `src/components/contact-row.js:47-93` vs `contacts-shell.js:115-121` | **The shared row dropped the directory's F2 address-less guard, and wallet Receive re-inherits the defect it was written to fix.** `pickerRow` blocks `!c.address` from multi-select because "falsy/dupe address collapses distinct rows into one Set entry"; `createContactRow` has no `blocked` concept, and `wallet-receive.js:344-346` keys `state.selected` on exactly `c.address`. REPRODUCED with two address-less contacts: ticking A then B leaves **A visibly checked, B unchecked, and the CTA reporting "Pick at least one contact"** — one `undefined` key toggled by two rows, with `setContactRowChecked` patching only the clicked one. With one address-less + one real contact the hint reads "2 selected" and the CTA "(2)" while `selectedContacts()` (`:222-225`) returns 1 and only one request is sent. | Port the guard: `createContactRow({ disabled })`, and in `renderContacts` pass `disabled: select === 'checkbox' && !c.address` (Send keeps them pickable only if an addressless contact can even be paid — it cannot, so block there too). |
| m5 | MINOR | `src/styles/components/wallet-receive.css:111-117` | **The receive roster's height cap was not re-tuned for the taller W-j row.** `max-height: 264px` was set when the row was `min-height: var(--size-target-min)` (44) with an avatar-40 → ~56px effective, i.e. ~4.7 rows. The shared row is 64px (48 + 2×`--spacing-8`) → **~4.1 rows**. The comment two lines above records Damir's F5 that the strip "stopped at 5 contacts, so reaching anyone further down MEANT typing" — this batch quietly walks part of that back. | Raise to ~320px (5 rows × 64) or express it as `calc(5 * 64px)` so the two numbers move together. |
| m6 | MINOR | `src/styles/components/wallet-send.css:42-49` + `wallet-send.js:255-261`, `:301` | **"Send to an address" is now 4px out of line with the contacts it sits on top of.** W-j put the contact rows inside a new card (`.c-wallet-send__contacts`, `padding: var(--spacing-4)`), so their avatars start at 4 + 8 = 12px from the card edge. `addrRow` is appended to `picker`, OUTSIDE that card, with only its own `--spacing-8` → its glyph starts at 8px. The code comment at `:254` still claims it is "aligned with them", and the picked row (`:98`, `padding: 8 12`) was deliberately matched to 12 — so the address row is the one element that missed the new arithmetic. | Move `addrRow` inside `rows`' card, or give `.c-wallet-send__addrrow` `margin-inline: var(--spacing-4)`. |
| m7 | MINOR | `src/components/wallet-send.js:527` | **`openPaymentReview` does no sanitation on `amount`.** `toUnits(amount)` throws on anything non-canonical (a grouped `"1,234.00"`, a localized `"1.234,00"`, an empty-ish `"—"`), and it throws BEFORE the sheet exists, so the caller's controller is never assigned. The only thing standing between a bridge value and this is `chat.html:1864`'s `/^\d+(\.\d+)?$/` regex — one caller, one level up, undocumented in the new export's docblock. If it ever throws, `chat.html:2782 if (!chatPayReview) renderLog();` never runs and the card's Pay `oneShot` latch stays latched → **Pay is permanently dead on that card**. | Run `amount` through `canonicalAmount(sanitizeAmount(amount))` inside `openPaymentReview`, and state the RAW-decimal contract in the docblock the way `createWalletSend`'s NB contract does. |
| m8 | MINOR | `src/styles/components/wallet-receive.css:165-178` | **The W-c caps are not derived from the dialog they live in, so on desktop the fix can fail to engage.** `:root[data-desktop] .c-sheet` (`overlay.css:143-151`) already has `max-height: 76%` **and `overflow-y: auto`** — W-c adds a SECOND nested scroller inside it. Usable inner height ≈ `0.76H − 48px padding − ~30px title` ≈ **606px at H=900**, while `.c-addr-sheet`'s cap is **640px**. The inner cap therefore never binds first at common desktop heights: the OUTER dialog scrolls, with its own native scrollbar — the exact symptom W-c set out to remove. `:root[data-desktop] .c-sheet--addr { width: min(440px, 92%) }` narrows the dialog from 480 → 440, which makes the text taller and pushes it further over. Estimated content ≈ 600px, i.e. right on the boundary. Analysis only — jsdom has no layout; **eyeball on desktop, both themes**. | Derive the inner cap from the dialog on desktop (e.g. `:root[data-desktop] .c-addr-sheet { max-height: none; }` and let the sheet's own 76% scroller do it), or drop `overflow` on `:root[data-desktop] .c-sheet--addr` so exactly one element scrolls. |
| m9 | MINOR | `src/components/wallet-receive.js:409`, `:414`, `:518-521` | **The address sheet outlives its screen, and the module singleton can strand.** REPRODUCED in the demo (the shell pattern): hero Receive → "Show my address" → topbar **Back** → the wallet home renders and the address sheet **plus its scrim are still on top of it**. And REPRODUCED at the component level: remove the sheet node without `dismissOverlay` (a host teardown / a `replaceChildren` on the overlay host) and `addrSheetLive` is never cleared → "Show my address" is **dead for the rest of the document's life** (the `if (addrSheetLive) return addrSheetLive` guard returns a detached node). Pre-existing (#527), but W-c is the pass that polished this surface without touching its lifecycle. | Have `createWalletReceive` return a teardown, or clear the singleton when the stored sheet is `!isConnected`: `if (addrSheetLive && addrSheetLive.isConnected) return addrSheetLive; addrSheetLive = null;`. |
| m10 | MINOR | `src/components/wallet-receive.js:337-349` | **A pending contact is now BADGED in Receive but is still tickable and still reported as sent.** W-j gives receive rows the `#255` "Request sent" badge for the first time; nothing else changed, so the CTA loops `ixian:sendrequest` at a peer who has not accepted the handshake and the result line says "Request for N IXI sent to X". The whole #275/#301 composer-lock line of work exists because messages to a non-`Approved` friend fake-deliver. The directory blocks pending rows from multi-select (`contacts-shell.js:121`, C9 reason line); Receive does not. | Block pending rows in the checkbox form (same fix as m4), with the `noGroupPending`-style reason on the sub-line. |
| n1 | NIT | `src/components/wallet-send.js:77-86` | `attachAmountKeyboardDismiss` has no IME guard. The house pattern is `if (e.isComposing \|\| e.keyCode === 229) return;` (`composer.js:156`, `:284`); Enter committing a composition would `preventDefault` + `blur` the amount field mid-commit. Low risk on `inputmode="decimal"`, but it is a one-line house rule. |
| n2 | NIT | `src/styles/components/wallet-receive.css:123` | `.c-wallet-receive__contact:disabled { opacity: 0.6 }` survived the move and now fights `contact-row.css:25-26`, which dims only the avatar + name at `var(--opacity-disabled)`. Both are dead today (Receive never disables a row) — but m4/m10 would light them up, in two different grammars, one of them a hardcoded number. |
| n3 | NIT | `wallet-send.js:340-402` (`picked`), `:536-549` (`who`) + `wallet-send.css:93-120` | W-j's stated reason is "three screens drifted because each built its own row". Two MORE hand-rolled copies of the same anatomy remain in this very file — the picked row and the review sheet's `who` block — with their numbers duplicated in CSS and **outside the new drift fence**. The fence pins `contact-row.css` ↔ `contacts-shell.css` only. |
| n4 | NIT | `scripts/smoke-test.mjs:13997-13999` | The W-d recovery pin cannot see M2 (no `err.hidden` assertion). Separately, the **W-h gate self-test** (`:13833-13837`) re-implements the gate's two-line predicate against a synthetic string instead of running the gate over a synthetic shell — it proves the `includes()` check, not the shell-discovery regex or the `exportOwner` map, which is where the gate could actually go blind. |
| n5 | NIT | `src/components/wallet-send.js:731-739` | `setSendError`'s primary target is `.c-wallet-send__addrfield .c-wallet-send__error`, which is inside `picker` and therefore **hidden after any pick**. No shell calls the export today (only the internal `addrUse` handler, where the field is open), so it is latent — but the exported free fn will silently write an invisible error. |
| n6 | NIT | `src/components/wallet-send.js:401` | The `catch (e) { /* not mounted yet */ }` around `cont.focus()` is dead: `cont` is a `const` initialised at `:406`, and every `pick()` call path (`:503` construction, row clicks, `addrUse`, `el._pick`) runs after it. The comment describes a TDZ that cannot occur and hides the real failure mode (m1: a disabled button silently swallowing the focus move). |
| n7 | NIT | `src/demo/components.html` | The new shared `c-contact-row` has no entry in the component gallery — the surface Damir reviews the design system on. Three screens now share it; none of them is the gallery. |

---

**Environment caveat (not a finding).** `src/shells/chat.html` was rewritten twice under me during
this run (mtime churn at 23:03 and 23:08, and a window where its `wallet-send.css` /
`contact-row.css` links read as absent). A concurrent process is editing the tree; any line number
I cite in a shell file may have moved. All component/CSS citations above were re-verified after
the churn.

**VERDICT: 5 MAJOR · 10 MINOR · 7 NIT.**
