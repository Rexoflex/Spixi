> 🗄️ **STALE — superseded.** Historical only. Current entry point: **`docs/handoff-fable-next-batch.md`**. Decisions preserved in `DECISIONS.md`.

# Session handoff — Bug round c + CHAT INFO (2026-07-05, session C)

Paste this into the new chat. `DECISIONS.md` is the source of truth; rows **#140–#141**
carry this session's detail. Damir opens the next session with a bug list for the
CHAT INFO screen (+ one issue that reaches into the SEND PAYMENT screen) — fix
round, same loop as #140.

## What was done

**#140 — bug round (4 items, screenshot):** ① "−" on Payment sent = BY DESIGN
(signed amounts, #76 family; Damir can still veto) ② live request-out card was
BARE (`status:'actionable'` is a request-IN state) → 'pending' + Cancel → in-place
Canceled flip; adjacent: `setPaymentStatus(row,'completed')` STRING patch = silent
no-op → object form + smoke guard ③ reply strip hid the ✕ — `.c-composer__ctx` is
a nested flex container, `min-width:auto` resolved to the nowrap excerpt (642px in
a 417px composer, real-Chrome measured) → `min-width:0` ④ typing pill pushed above
money cards → `keepTypingLast(box)` on EVERY demo append path.

**#141 — CHAT INFO (`c-chat-info` + `docs/chat-info-spec.md`):** ONE takeover for
1:1/group/bot, header identity tap (topbar `onIdentity`). 1:1 = nickname edit
(`ixian:userdefinednick`, wire name stays visible) · FULL address + honest copy
morph + lazy `address:ixi` QR · Pay/Request → #139 machinery · payments list
(txlist-item) · media strip (flagged, §9 — no legacy command) · delete history /
remove contact. Group = member rows (#136③ caps) → member-sheet (new `actions`
opt) w/ admin Kick/Ban behind LOCKED alertdialog confirms (`ixian:kick/ban:ADDR`)
· notifications toggle (groups/bots real; 1:1 designed but capability-gated, §9)
· leave (`ixian:leave`). Blind groups: no address anywhere. Audit: 3 MAJ fixed
(nickname Enter+blur double-commit · stale row re-offered kick · members 6-7
unreachable at cap 5/search-at-8) + 6 MIN/NIT. Callbacks = (payload, ctrl),
one-shot per attempt; destructive confirms are #135-C1-locked via the #138
live-opts fix.

**Verification state:** `node scripts/build-demo-bundle.mjs && node scripts/smoke-test.mjs`
→ **119 assertions CLEAN**. Bundle `src/demo/spixi.iife.js` = **165 exports**
(+createChatInfo), tail Read-verified. Real-Chrome e2e drove both info surfaces
end-to-end.

## Workflow gotchas (ALL still live)

1. **Truncated-mount reads PERSIST ACROSS SESSIONS.** bash sees stale/short
   content for any file edited via file tools — last session's AND this one's.
   Detector: file-tool line count (Grep pattern `^`, output_mode count) vs bash
   `wc -l`. **EQUAL counts can still hide it** (message-menu.js did). Trust only
   file-tool reads for anything recently edited; NEW Write-created files are safe
   until edited (held for chat-info.js this session, but verify).
2. **Working pattern:** staging tree in `/tmp/stage` (dead now — sandbox resets
   /tmp and orphans old dirs unreadable; REBUILD it): `cp -r` repo src+scripts
   from the mount, then overwrite every recently-edited file via file-tool
   Read → Write into `outputs/` (bash sees outputs fresh) → `cp` into staging.
   A subagent does the Read→Write transfers cheaply. Build+smoke in staging;
   `cp` artifacts back through the mount (whole-file cp lands fine); Read-verify.
   Mirror every real-file Edit into staging with node string-replace patches.
3. **Real browser in the sandbox:** playwright CDN is allowlist-BLOCKED; npm is
   not. `npm i playwright-core @sparticuz/chromium` → launch with
   `executablePath: await chromium.executablePath()`. jsdom is layout-blind —
   the reply-strip bug (#140③) and the tools collapse (#136①) both needed real
   layout; e2e-drive money/destructive flows there. Overlay teardown is
   animation-async: settle ≥400ms before asserting removal; select the LAST
   `.c-modal` (a prior one may still be tearing down); beware duplicate button
   labels across static demo cards (use scoped/tagged selectors).
4. Background bash processes don't survive between calls; per-call 45s cap.
   npm i inside the call, quick; chromium comes via the npm package (no CDN).

## Verify on your machine

1. `node scripts/build-demo-bundle.mjs && node scripts/smoke-test.mjs` → 119 CLEAN.
2. chat.html 1:1: tap header → chat info: nickname edit (wire name stays under
   it) · Show QR · Pay/Request (locked recipient / request sheet) · Delete
   history (Esc mid-spin = locked) · payments rows · media strip.
3. chat.html group: tap header → members search (9 mock members, 5-row cap) →
   tap one → member sheet w/ Kick/Ban → confirm → row + BOTH counts drop ·
   notifications toggle · Leave group.
4. #140 fixes: ⊕ Request → pending card w/ Cancel; ⊕ Pay → Pending→Completed;
   long reply → ✕ visible; money actions keep the typing pill glued above the
   composer.

## Next session (agreed)

1. **Damir's bug list: chat-info issues + one that reaches the SEND PAYMENT
   screen** (`wallet-send.js` — mind #135/#136 history there: BigInt money math,
   in-flight locks, lockedRecipient path from #139).
2. Then remaining figma-sweep screens: account shell (backup-ux-spec.md ready) ·
   add contact · settings hub · scan · lock/onboarding · desktop passes ·
   wallet safe-area (#22).
3. 🟡 standing flags: copy-morph honesty sweep (member-sheet/wallet-shell) ·
   `canonicalAmount`/`toUnits` → shared money module · media/card rows in
   multi-select · B2 glyph exports (`link`, `code`) · §9 asks (now incl. 1:1
   mute · shared-media inventory · presence · group name/avatar edit ·
   room-wide request · tip fee/aggregation · share command · sendrequest
   single-contact payload · backup timestamp+dirty · Discover transport ·
   history paging · tx fee in payload).

## Key files

Chat info: `src/components/chat-info.js` + `src/styles/components/chat-info.css` +
`docs/chat-info-spec.md` · member-sheet `actions` opt: `src/components/member-sheet.js`
· #140 fixes: `src/demo/chat.html`, `src/styles/components/composer.css` · demos:
`src/demo/chat.html` (header taps wired, captions updated) · build:
`scripts/build-demo-bundle.mjs` (FILES + chat-info.js) · smoke:
`scripts/smoke-test.mjs` (119; #141 block + #140 static guards) · bundle:
`src/demo/spixi.iife.js` (165 exports).
