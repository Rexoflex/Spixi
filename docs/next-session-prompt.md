Next session — the Session Z walk verdict, the two picks, then the queue

0 · Before anything else (#215) Read `docs/handoff-2026-09-18b.md`. Check `device_bash` (node 22 / git / npm on the VM). Use `git --no-optional-locks status` (a plain `git status` leaves an undeletable `.git/index.lock`, #882 ②). Run `node scripts/generate-icons.mjs --check`, `node scripts/build-shells.mjs --check` and `node scripts/extract-strings.mjs --check` on the real tree before believing any statement about it. CHECK DECISIONS before accepting any "owed" row from this prompt (#660). ★ The FULL suite runs in the container (#882 ①, recipe in the handoff §4 — tar from `git ls-files`, install jsdom/eslint/globals/tree-sitter TOGETHER, ~6 min) — run it on the tree you inherit BEFORE and AFTER your change; "predicted" is not an acceptable last line. ★ Reviews run on Opus (Damir: "so we don't waste Fable tokens") — pin the model explicitly.

1 · ★★ Ask first
* Did Session Z commit (one batch, `docs/commit-message-session-z.txt`)? The last suite line — expected BASELINE OK — 4754 pass / the 2 KNOWN on his Windows run.
* The Session Z walk (`docs/walk-artifact-session-z.html`, 17 rows): **Z.1–Z.3 are the flags in WebView2** — the one leg the container could not verify. Letters on Z.1 → the checklist §2's two F12 lines name the fork (script never loaded vs font loaded and not applied); take the verbatim console line, do not guess a fix.
* **The two picks:** Z.5 the glass default (ON/OFF) + a Chat-appearance toggle? · Z.10 the chooser copy (base / trimmed / actions-first, `docs/sheets/session-z/chooser-grid.png`).
* S1 timed? (#872 ②) — #864 and the spare stay as they are until a number exists.
* The #46 loop's recorded dials (#890): the sheet-card hover at 1.103 · the member-sheet uppercase micro-label · the Downloads/apps-grid SCREEN hover 1.04 light · MINOR-6 (the probe at boot on every shell — measure before moving it).

2 · Build — only what the picks open, each rendered BOTH themes through the wire before pins, the suite's existing gates run on every change
* Z.5 ON → the toggle in Chat appearance (settings-app.js, the pattern-pref grammar; the key `spixi.chat.glass` and the attribute already exist) + the default flip in chat.html's pre-paint read; a pin on both halves.
* Z.10 trimmed / actions-first → the copy lands in `contacts-shell.js` from the render; drafts ×12; `extract-strings --check`.
* Any Z.1 fork → the fix the console line names, not the one the brief predicted (#215).
* Any walk FAIL → the row's note decides; a design note on a pass is a pass.
★ #221 untouched in all of it. CHAT_KB_CEIL moves only with its delta stated (648 today, headroom 1 491).

3 · Then
* The #46 loop on whatever was built (Opus). Records (#891+), the handoff, the checklist + walk artifact, the commit message.
* #864 after S1 · AND-40 (needs a run that kills) · AND-36/39 (repro first) · the #881 latent gate holes (recorded) · the freeze (#825) last · the BE cutover (`be-cutover-brief.md`, `security-review-for-be-engineer.md` first — §1a CLOSED, #874) · then pre-release per `docs/release-readiness.md`. The translator pass over `src/strings/draft/*.json` is Damir's.

Rules #215 · #294 · #663 (F5, never dotnet build on Windows) · #771 · #798 · #828 · #861 (both halves) · #865/#882 ③ (a file is landed when its hash reads back) · #880 ⑪ (never `throw` in a render path) · #881 (ask every gate the loud/silent question) · #883 (a one-sided walk is a false green) · ★ #890: a runner whose harness differs from the suite's proves less than the suite — synthetic events hid a real `file:` fetch; jsdom's interceptors never see `file:` URLs; a `file://` document cannot fetch a font beside itself (CORS) but can run a script.

Files: `DECISIONS.md` (553 rows, #885–#890) · `docs/handoff-2026-09-18b.md` · `docs/f5-checklist-session-z.md` · `docs/walk-artifact-session-z.html` · `docs/security-handover-gate.md` (Session Z section) · `docs/flags-on-windows-brief.md` (status) · `docs/sheets/session-z/` · `docs/release-readiness.md` (unchanged since 09-17).
