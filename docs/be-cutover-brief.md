# BE cutover brief — deferred C# asks

*The running list of C#/bridge changes intentionally deferred while we wire the redesigned frontend to the FROZEN bridge (zero C# change). Strategy (Damir, 2026-07-07): max out the zero-C# surface → get the whole app working + tested → then ONE focused BE pass against a working app. Nothing here blocks the frontend; each item is an enhancement to raise fidelity or unlock an inline action.*

*Add rows as we hit them; this becomes the BE work order. Sources cited file:line. Related: `docs/chat-batch2-spec.md`, DECISIONS #179.*

## Ground rules for the BE pass
- **Every arg-signature change lands JS + C# in the SAME commit** (the WebView handler and the `Utils.sendUiCommand(...)` call site must agree).
- **No signing/broadcast moves into the WebView.** SECURITY.md invariant: shells emit intent, C# signs. Inline-pay etc. stays a confirm-page open unless BE builds a signed path.
- Prefer **additive args** (append to the end of existing `sendUiCommand` signatures) over new commands, to keep the change surface small.

## Chat surface

| # | Type | Ask | Why | C# touch point | Effort |
|---|---|---|---|---|---|
| C1 | **Payments** | Add a stable `statusEnum` (`actionable\|pending\|processing\|failed\|completed\|declined\|canceled`) + `kind` (`request\|sent`) to `addPaymentRequest` / `updatePaymentRequestStatus`. | Today role/status live only in the **localized `title`** + a 2-value `statusIcon` (`fa-clock`/`fa-check-circle`). Mappable but lossy; a real enum lets the redesigned card show correct role + colored status badge. | `SingleChatPage.xaml.cs:1310/1314/1361/1365/1665` (add args, compute from `FriendMessage`/activity status) | ~1–2h |
| C2 | **Payments** | Send `fiat` + an `insufficient` (balance < amount+fee) flag to `addPaymentRequest`. | Card supports a fiat sub-line + a "not enough IXI" disabled-Pay note; both are C#-known, absent from the current args. | same call sites | ~1h |
| C3 | **Payments (inline Pay)** | *Decision, not code yet.* Keep inline **Pay** = open the existing confirm page via `ixian:viewPayment` (**zero C#**, recommended, keeps money-confirm step), OR add `ixian:payRequest:<id>` + a signed inline path. | Inline signing removes the confirmation screen for a money action → SECURITY.md §8 territory. | `onViewPayment`/`onConfirmPaymentRequest` already exist (xaml.cs:781/817) | decision only |
| C4 | **Calls** | Enrich `addCall(id, message, declined, time)` → add `missed` (bool), `duration` (secs or preformatted), `outgoing`/`direction`. | `addCall` is lossy: can't tell missed vs answered vs duration; the redesigned call card needs them. `VoIPManager` already knows the outcome. Call-back reuses existing `ixian:call`. | `SingleChatPage.xaml.cs` `addCall` call site + `VoIPManager` | ~½ day |
| C5 | **Reactions** | Include a per-user **own** flag (did *I* react) in the `addReactions` aggregate. | Bridge sends only `key:count` (xaml.cs:1607) → pills can't show "you reacted" (`own:false` hardcoded in the shell). | `updateReactions` (xaml.cs:1602) | ~1h |
| C6 | **Reactions (tips)** | Confirm/normalize the **tip** reaction token. Today it's `tip:<txid>` (xaml.cs:979), aggregated as `tip:<txid>:count` — a txid, not a display amount. | Shell omits tip pills in v1 (won't render money it can't format). Either send a preformatted amount, or document the txid→amount lookup. | `updateReactions` + reaction storage | ~1h + decision |
| C7 | **Apps (decline + Get URL)** | (a) Add `ixian:declineApp:<id>` if a Decline-invite affordance is wanted. (b) **Ensure the app-invite message carries the install URL** so "Get app" works. | (a) No legacy decline verb exists; the shell now HIDES Decline (was a dead button — Damir F5 "can't decline, nothing happens") until this lands. (b) Damir F5: "if app is not present, Get app does nothing" — `app_install_url` is only set when the invite message contains `||url` (SingleChatPage:1415-1419); otherwise `installApp:` fires with an EMPTY url and no-ops. The invite message needs to always include the install URL (or Get needs a fallback source). | `SingleChatPage.xaml.cs` dispatch (decline) + the appSession message format / `installApp` (:1415/213) | decline trivial · URL ~1–2h |
| C9 | **Tip in bots (BUG)** | Remove `friend.bot ||` from the tip guard so bot-channel messages can be tipped again (legacy parity). | `onContextAction` tip case **returns early for `friend.bot`** (xaml:942), but the very next block (xaml:951–955) resolves `msg.senderAddress` *for bots* — a dead branch that proves bot-tip was intended. The early-return is a bug; Damir F5: "on Bot we now can't tip, legacy could." FE keeps tip hidden in bots (no dead action) until this is removed. | `SingleChatPage.xaml.cs:942` (drop the `friend.bot ||`) | trivial |
| C8 | **Reactions (more emoji)** | Support arbitrary emoji reactions, not just `like`. C# `addReaction` hardcodes `"like:"` (xaml:1054) and `contextAction` only has a `like` case; the redesigned menu can offer 👍❤️😂😮😢🔥. Needs: dispatch the emoji from `contextAction:react:<id>:<emoji>` (or extend `like`), store per-emoji, and `updateReactions` already sends `key:count` so multi-emoji display works once keys vary. | Damir F5: "can we have more reactions?" Bridge currently supports one reaction type → shell offers only ❤️. | `onContextAction` like case + `addReaction`/`updateReactions` (xaml:1045/1054/1607) | ~2–3h |
| C10 | **Payments (fulfilled request = two cards — UX/link)** | **Scenario confirmed** (Damir F5 2026-07-08, 2 accounts / 2 machines): A sends a payment **request**; B reacts + **pays**. On **A's** screen the request card ("Payment Request Sent", 1.02 incl. fee) then sits **above a second "Payment Received" card** (1, net) — same time, both Pending. Damir: "should be one." Ask: when a request is fulfilled, **collapse into the single request card updated to Paid/Completed** instead of spawning a separate received-funds card. | FE renders **one card per C# message** (`addPaymentRequest`→`upsertPayment`, keyed by unique `hashToString(message.id)`; no optimistic bubble). The request (`requestFunds`, `SingleChatPage.xaml.cs:1268/1332`) and the fulfilling payment (`sentFunds`, `:1340/1387`) are **separate messages with separate ids/txids** and **no linkage** is pushed to the WebView, so the shell can't know card B fulfills card A. `updateRequestFundsStatus(msg_id, txid, status)` already exists (`:1673`) → the intended path is likely to **update the request card in place** on fulfillment and **suppress the standalone `sentFunds` card** for a payment that answers a known request. Needs a BE decision: (a) send the originating request's `msg_id` on the `sentFunds` message so FE can merge, or (b) C# updates the request card + omits the duplicate `sentFunds` push. **No FE-only dedup** (collapsing by amount/time would hide legitimately-distinct payments). | `SingleChatPage.xaml.cs:1268`/`:1340`/`updateRequestFundsStatus:1673` + the `sentFunds`↔originating-request linkage in `FriendMessage` | decision + ~2–3h |

## Chats list (home tab1)

*Track B depth (DECISIONS #189). The live per-contact status + lone-`addChat` bugs were fixed zero-C# in `src/shells/home.html`; the rows below need a C# change and are gated OFF (built + ready) until the cutover.*

| # | Type | Ask | Why | C# touch point | Effort |
|---|---|---|---|---|---|
| CH1 | **Groups filter** | Add a **contact-type flag** (`isGroup`/`type`) to `addChat`. Append as a new trailing arg. | The Groups filter chip is dead: `chatMatchesFilter('groups')` needs `chat.type==='group'`, but `addChat`'s 8 args carry no contact-type — its `type` arg is the *message-status* (read/confirmed/pending/…), not group-vs-1:1. Damir F5: "in 2 groups, Groups filter shows nothing." FE filter logic is ready — flip on when the flag lands. | `HomePage.xaml.cs:1030` (updateChat) **and** `:1091` (loadChats) — both `addChat` call sites; source from `friend.type == FriendType.Group` | trivial |
| CH2 | **Contact-request feed** | Push pending contact requests as a distinct feed (e.g. `addRequest(address,nick,avatar,timestamp)` / `clearRequests` / `removeRequest`) + accept/decline/complete verbs (`ixian:acceptRequest`/`declineRequest`; a handshake-complete signal for #109). | `createContactRequest` Accept/Decline + the #109 staged-handshake UI are **built but UNFED**: `state.requests` is hardcoded `[]` and HomePage only pushes `loadChats`, so incoming requests arrive as ordinary chat rows with a "waiting" excerpt (xaml:910-961). A request feed lets the redesigned inline Accept/Decline + handshake staging render. | `HomePage.xaml.cs` (new push in the refresh loop + dispatch branches; `FriendState.RequestReceived`) | ~2–4h |
| CH3 | **Delete / mark-read persistence + history/media wipe** | *(⚙ FE interim #193: `capabilities.delete` ON + a session TOMBSTONE (`deletedChats`) — delete now STICKS across re-flushes this session and resurrects on a real new-message push; but it's session-only and does NOT wipe history/files/contact. These verbs still needed for true persistence + the deeper wipe.)* Persistence verbs for row-menu actions — `ixian:markread:<addr>` + a delete flow that offers to **also wipe the on-device chat history AND its media/downloads** and remove the contact (Damir 2026-07-07: **two-step** — Delete removes the row; a SECOND choice in the modal opts into also wiping on-device history + its media/downloads AND removing the contact; counterpart's copy untouched for now). Reuse `ixian:removehistory`/`remove` (they exist on SingleChatPage/ContactDetails) + a media/downloads purge. | Row-menu Delete + Mark-read change only the local model (`onPersist:()=>{}`, home.html) and are **wiped on the next `shouldRefreshContacts` re-flush** (loadChats, xaml:1303). Damir F5: "delete only removes from the list." The chats-shell already routes these through `onPersist(action,chat)` — slot the verbs in behind a confirm modal. | `HomePage.xaml.cs` dispatch (delete → history + downloads purge + optional `remove` contact; markRead → clear unread) | ~½ day |
| CH6 | **Excerpt canon (structured kind)** | *(✅ FE best-effort DONE #192 — `excerptFromRaw` in home.html reverse-maps the en-us `_SL` phrases + lone GIF URLs to canon glyphs; this BE row remains the clean end-state for other locales + missed-vs-answered calls.)* *(Damir 2026-07-07: build the zero-C# best-effort NOW; this BE row remains the clean end-state.)* **FE interim (next batch):** hardcode the English C# `_SL` excerpt strings (`index-excerpt-file`/`-payment-*`/`-voice-call`/`chat-app-invite-*`/`chat-waiting-for-response`, + the `index-excerpt-self` "Sent a " prefix) and reverse-match the incoming excerpt → an excerpt KIND, then render the built canon glyphs (chatlist-item supports file/gif/call/call-missed/payment/app-invite/draft/mention). Works in en-us, degrades to plain text in other locales (missed-vs-answered call can't be split — C# collapses both to `index-excerpt-voice-call`). **BE end-state:** add an **excerpt kind** enum + payload to `addChat`/`setContactStatus` (e.g. `file`/`gif`/`payment-req`/`payment`/`voice-call`/`missed-call`/`app-invite`/`reply`/`added`/`mention`/`text`), instead of only a pre-composed localized STRING + a message-STATUS `type`. | The redesigned excerpt canon (Damir's spec: 📄 File.doc · GIF url · Voice call/Missed call icons · Payment request 25 IXI · Invited you to {app} · "Name Replied:" · "Name was added" · @mention · Draft) needs the KIND to pick the icon + descriptive text. C# already KNOWS the kind (`getFriendMessageHelper` switches on `lastmsg.type`, xaml:907-970) but collapses it to a localized string; the shell can't reliably reverse it per-locale. Send the kind (+ e.g. amount, filename, sender for groups) and the FE renders the canon. **Interim (zero-C#, optional):** map the known `_SL` excerpt strings back to kinds via the shell's own dictionary (fragile across locales). | `HomePage.xaml.cs:907-970` (emit a kind alongside the excerpt) | ~2–4h |
| CH7 | **Composer drafts** | *(✅ FE DONE #192 — chat.html saves/restores/clears `spixi.draft.<addr>` in localStorage; home.html renders the "Draft:" excerpt. This BE row stays as the OPTIONAL durable/cross-device draft store.)* *(Damir 2026-07-07: WebView **localStorage** NOW — zero-C#.)* **FE (next batch):** save the composer text (debounced) to `localStorage` keyed by peer address on input/leave, restore it on chat open; the chats list reads the same store (same-origin) to show the **"Draft: …"** excerpt (chatlist-item `draft` type is built). A draft is the user's OWN unsent text (not message history) → acceptable to cache per SECURITY.md; clear the key on successful send. **BE end-state (optional):** a `saveDraft`/`getDraft` bridge pair if a durable/cross-device draft is later wanted. | FE localStorage (chat.html write + home.html read) | ~2–4h |
| CH4 | **Pin / mute / favorites** | Persistence + a per-chat `pinned`/`muted`/`favorite` flag on `addChat` (or a prefs push). **Also:** compute `setUnreadIndicator` (xaml:1046-1053) **muted-excluded** to match the FE badge. | Pin/mute/favorites (+ swipe) are wholly parked behind `capabilities:{}` (home.html) — the UI is built + gated OFF. Rendering needs the state pushed on `addChat`; toggling needs a persist verb. Favorites filter chip is hidden until then. **Badge semantics (audit NIT-2):** the FE nav badge (`chatsUnreadTotal`, #42) excludes muted chats, but C#'s `setUnreadIndicator` sums *all* unread — they agree only because `muted` is always false today; once mutes exist the badge will flicker between the two sources unless C# also excludes muted. | `HomePage.xaml.cs:1030/1091` (add flags) + a persist verb + storage + mute-aware unread total | ~½ day |
| CH5 | **Mention flag** | Add an **@-mention** flag to `addChat` (or the unread payload). | No bridge signal marks a chat as containing an unread @-mention; the redesign's mention indicator + Unread-filter mention bump (`chatMatchesFilter('unread')`) can't light up. | `HomePage.xaml.cs` (compute per-friend, add arg) | ~1–2h |
| CH8 | **Reaction excerpt signal** | Push a structured **reaction** indicator to the chats list when a counterpart reacts — e.g. an excerpt kind `reaction` + the reactor + emoji ("Nancy reacted ❤ to your message"), or at minimum a stable reaction-excerpt token. | *(⚙ FE interim #193: `home.html` maps a lone-heart excerpt → the built `reaction` chatlist glyph + "Reacted" — best-effort, en-us, and a literal-heart text message is a false-positive.)* Reactions are `ReactionMessage`s that never become `lastMessage`; an incoming reaction calls `UIHelpers.updateReactions → HomePage.updateChat` (UIHelpers.cs:69-77) which re-pushes the excerpt from the last REAL message — so there is no reliable "who reacted to what" signal. C# knows it (the `msgReaction` handler, StreamProcessor.cs:464-472). | `HomePage.xaml.cs` updateChat / getFriendMessageHelper (emit a reaction excerpt kind when the trigger was a reaction) | ~2–3h |

*Chats-list notes (no C# ask, done zero-C# in #189):* live `setContactStatus` (online + unread) is now wired (was a no-op stub) → the online dot / unread badge / nav badge update every tick, not only on a full flush. `addChat` now **upserts by address** and, for a lone `updateChat` push (a message event, xaml:1020 — no surrounding clear/done), schedules a coalesced re-render — previously a lone `updateChat` appended a **duplicate** row and never re-rendered, so a new message's excerpt/timestamp/unread were dropped until the next structural flush. `clearContacts`/`addContact` remain intentional no-ops (they feed the contacts **directory** roster, a separate surface — a friend with no messages appears there but not in the chats list; Track D). The All/Unread/Requests filter chips are wired correctly in code (`setChatsFilter`→re-render); "clicking filters does nothing" (Damir F5) = Groups dead (CH1) + Requests unfed (CH2) + Unread previously stale (now live via the status fix).

## Other shells
*(to be filled as wallet / apps / settings / launch are bridge-wired)*

| # | Shell | Ask | Why | Effort |
|---|---|---|---|---|
| W1 | **Wallet** | Send the **raw unix timestamp** to `addPaymentActivity` instead of a pre-formatted string. | Today `time` = `Utils.unixTimeStampToHumanFormatString(...)` (HomePage:1218) — an absolute display string. The redesign shows relative/locale-aware timestamps (like `addChat`, which already sends epoch); a string forces verbatim display via a `timeText` shim. | `HomePage.xaml.cs:1218` (send `activity.timestamp` raw) | trivial |
| W2 | **Wallet** | Add the counterparty **address** + **fee** (+ a stable **status enum**) to `addPaymentActivity`. | The redesigned tx-detail sheet has address + fee rows and a status badge; the current 7-arg payload has none, so the sheet omits them (data-honest). C# knows all three per `Transaction`/`ActivityObject`. | `HomePage.xaml.cs:1167–1219` (extend args) | ~1–2h |
| W3 | **Wallet** | Add a **tx-level Explorer** verb, e.g. `ixian:txexplorer:<txid>` (mirror `WalletSentPage`'s `viewexplorer`). | Only address-level `ixian:explorer` exists (HomePage:413); the tx sheet's "View in Explorer" opens the user's address page, not the specific tx. | `HomePage.xaml.cs` dispatch (one `Contains` branch) | trivial |
| W4 | **Wallet** | Add an **isContact / address** signal so `counterparty` isn't an ambiguous nickname-or-address string. | `tx_text` is a friend nickname when known, else a raw address (HomePage:1171–1192). The shell can't tell which, so it can't reliably personalize the sheet with the contact avatar. | `HomePage.xaml.cs:1167–1219` (pair nick + address, or a flag) | ~1h |
| W5 | **Wallet** | Add a **signed compose→sign handoff verb** — proposed `ixian:signSend:<addr>:<amount>` (single-recipient; leave plural-ready `\|`-batch room). C# signs + broadcasts the WebView-composed tx WITHOUT reopening the native WalletSendPage. | "WebView composes, C# signs" model (SECURITY.md — no keys in the WebView). The redesigned in-page Send takeover (`createWalletSend` review→Confirm) is BUILT + wired behind `SPIXI_ENV.capabilities.composeSend`, OFF in v1; onSend emits this verb only. Until it lands, Send delegates to the native signed flow (`ixian:sendixi` → WalletSendPage). Ideally the verb reports sign success/failure back so the review sheet's done/fail can replace the interim optimistic resolve. | `HomePage.xaml.cs` dispatch (new branch → `Node.sendTransactionFrom` / native confirm) | ~½ day + decision |
| W6 | **Wallet** | Push the **network fee** to the home wallet tab (a `setFees`-style command), so the Send review's fee + total lines are real. | Fee only reaches the native `WalletSendPage` via `setFees` today; the home tab gets no fee push. The BE-gated compose review currently shows a `WALLET_FEE_ESTIMATE` placeholder (0) — it can't show a real fee zero-C#. Pairs with W5 (compose is off until both land). | `HomePage.xaml.cs` (push fee on tab2 load / balance tick) | ~1h |
| W7 | **Wallet** | (a) Add a **native share verb** for the Receive address (share sheet beyond copy), if sharing richer than clipboard is wanted; the Receive takeover uses the Web Share API where present, else a clipboard copy — no verb invented. (b) **Confirm** `setAddress`'s `ExtendedAddress(primary, OfflineTag).ToString()` (HomePage:673) is the bare base58 address; the shell now uses it VERBATIM (legacy parity) before the component appends `:ixi` for the QR. | Receive/Request is wired zero-C# (QR + copy + client-side request-amount); share + the address-format assumption are the only open items. | `HomePage.xaml.cs` (optional share verb); confirm address format | trivial |
| W8 | **Wallet** | Add a **HomePage-dispatched `ixian:sendrequest:<addr>:<amount>`** (it exists only on WalletReceivePage today, not HomePage). | The Receive takeover's "send request to a contact" strip is OMITTED on the home wallet tab (the verb is dead on HomePage → would falsely confirm "sent"). `createWalletReceive` now hides the strip when `onSendRequest` is absent; wire it once HomePage handles the verb. | `HomePage.xaml.cs` dispatch (mirror WalletReceivePage's `sendrequest`) | ~1h |
| S1 | **Settings** | Push the **own address** on `onLoad` (e.g. `setAddress`) so the account hub's QR-forward identity hero (QR + address chip + copy/share — the #147 "add me" marquee) can render. | `SettingsPage.onLoad` (:43–66) pushes nick/appearance/lock/avatar but NOT the address; the redesigned hub omits the whole QR/address block without it. HomePage already has the address (`setAddress` stub). | `SettingsPage.xaml.cs:onLoad` (one `sendUiCommand`) | trivial |
| S2 | **Settings** | Push a **backup status** signal (last-backup date + dirty-since count, or a completion callback). | Hub backup row is the standing nudge (backup-ux-spec §3.1); with no signal it shows the conservative "Not backed up yet / Action needed" always, even for users who HAVE backed up. Same ask as backup-ux §5. | `SettingsPage` / backup flow | ~1–2h |
| S3 | **Settings** | Push the **current language code** on `onLoad` (`setLanguage`-style). | Legacy hardcoded the dropdown + never told JS the current pick; the shell defaults the Language row's shown value to `en-us`. The picker still commits any code correctly — this only fixes the displayed "current". | `SettingsPage.xaml.cs:onLoad` | trivial |
| S4 | **Settings** | Push a **version string** (or expose via `SPIXI_ENV`) for the About row. | No bridge push exists; the hub's About/Version row is omitted. Same as the settings-spec §8-3 ask. | `SettingsPage` onload or env | trivial |
| S5 | **Settings (i18n)** | After `ixian:language:<code>` C# reloads `settings.html`, but the self-contained shell re-boots from its OWN bundled dictionary (en-us) — so the settings UI doesn't re-render in the picked locale (the choice DOES persist app-wide + translates native/legacy pages). | Holistic i18n live-locale gap (CLAUDE.md, #175): shells need to read the persisted locale on boot (e.g. from `SPIXI_ENV` or a `?lang`/`window.SL` injection) rather than defaulting to en-us. Not settings-specific. | i18n live-locale pass (all shells) | (holistic) |
| S6 | **Settings (app lock)** | On `ixian:lock:off` auth **cancel/fail**, push `setLockEnabled("True")` (C# only pushes on SUCCESS today). | Lets the shell implement the spec's pending-OFF security UX (switch stays busy until auth resolves, reverts on cancel). Without a cancel signal the shell uses optimistic-OFF (legacy parity), which can briefly show OFF while C# keeps the lock ON until the next load. | `SettingsPage.HandleAuthSucceeded` (:234 — add an else-branch push) | trivial |
| S7 | **Settings (change password)** | Add a **SettingsPage → EncryptionPassword nav verb** (e.g. `ixian:encpass`). | The redesigned `settings_encryption.html` shell is BUILT + wired to the real EncryptionPassword bridge (`mountEncPassPage`), but `SettingsPage.onNavigating` has NO verb that opens it (bridge-audit-B §1/§3) — so the hub's "Change wallet password" row is capability-gated OFF (`capabilities.changePassword`). Adding the verb + flipping the flag lights it up. | `SettingsPage.xaml.cs` dispatch (one branch → `PushAsync(EncryptionPassword)`) | trivial |
| S8 | **Settings (downloads)** | Add a **SettingsPage → DownloadsPage nav verb** (e.g. `ixian:downloads`). | Redesigned `downloads.html` shell is BUILT + wired to the real DownloadsPage bridge (open/delete/clear/list). Today Downloads is opened from **HomePage** (:312/436), not SettingsPage — so the hub Downloads row is gated OFF (`capabilities.downloads`). Verb + flag re-enables it in the account hub. | `SettingsPage.xaml.cs` dispatch (→ `PushModalAsync(DownloadsPage)`) | trivial |
| S9 | **Settings (developer)** | Add a **SettingsPage → DevPage nav verb** (e.g. `ixian:dev`). | Redesigned `dev.html` shell is BUILT + wired (setLog viewer + copy). DevPage is HomePage-driven today; the hub Developer row is gated OFF (`capabilities.dev`). | `SettingsPage.xaml.cs` dispatch (→ `PushModalAsync(DevPage)`) | trivial |
| S10 | **Settings (contributors)** | Add a **SettingsPage → ContributorsPage nav verb** (e.g. `ixian:contributors`). | Redesigned `contributors.html` shell is BUILT (static). ContributorsPage is HomePage-driven today; the hub Contributors row is gated OFF (`capabilities.contributors`). | `SettingsPage.xaml.cs` dispatch (→ `PushModalAsync(ContributorsPage)`) | trivial |
| S11 | **Settings (peer-tab)** | Make **Account a true home tab** — either HomePage pushes the settings data (so Account can be a client-side home tab like chats/wallet/apps), or add a **home-tab-select verb** so a nav tap lands on the precise tab. | Damir wants Account to feel like a peer bottom-nav screen. The shell now renders a `c-bottomnav` (Account active) for the peer READ, but SettingsPage is a standalone PUSHED page whose data comes from SettingsPage (not HomePage) — so non-Account taps do a best-effort `ixian:back` (pop to home, no precise tab). | HomePage settings-data push, or a `ixian:tab:<id>` select verb | design + ~half-day |
| S12 | **Settings (backup password)** | *(⚙ FE #199: password prompt DROPPED — backup + wallet-export are now CONSISTENT, neither prompts; C# still encrypts with the stored `walletpass` pref.)* If a real password-protected backup is wanted, have `ixian:backupAccount` **accept + validate a password arg** (the confirm field can then return); today it takes none. | BackupPage encrypts with the STORED `walletpass` preference and takes no password arg (bridge-audit-B §2 Unusual) — the removed confirm-password modal was legacy-parity intent only; the typed value was never transmitted and a wrong entry was uncaught JS-side. Pairs with S2's completion callback. | `BackupPage.xaml.cs:onBackupAccount` (accept a password param) | ~1h |
| S14 | **Settings (save without pop)** | Add a **save-without-pop verb** — proposed `ixian:apply:<nick>` — that persists the nickname (+ promotes avatar-tmp) WITHOUT navigating away. `ixian:save` persists AND pops (`SettingsPage.onSaveSettings:221`), so the topbar Save check can today only "save + leave". | Damir wants the account Save to STAY on the page with a "Saved" topbar morph + toast (not pop to chat). The FE is BUILT + wired behind `bridge.cap('settingsApply')` (OFF today → falls back to the frozen `ixian:save` persist-and-pop). Flip the flag once the verb lands; ideally it reports success/failure so the morph can reflect a failed save. | `SettingsPage.xaml.cs` dispatch (new `ixian:apply` branch → persist nick/avatar, NO PopAsync) | ~1h |
| S13 | **Settings (open external link)** | Make an **open-external-URL verb reachable from SettingsPage** — cleanest = promote the existing `ixian:openLink:<url>` (SingleChatPage-local, `Browser.Default.OpenAsync`, bridge-audit-A:85) into `onNavigatingGlobal` so every page gets it (or add a SettingsPage dispatch branch). | The account hub's About links + the How-to "Learn more" link should be TAPPABLE to open the Spixi website in the system browser (Damir). SettingsPage handles NO open-URL verb (global handler = app/call only; `openLink` is chat-local, `about`/`guide` are HomePage-local), and letting the WebView navigate to an http URL falls through `e.Cancel=false` and REPLACES settings.html (shell breakage). So the links stay SELECTABLE TEXT (`createSettingsAbout`/`createSettingsHowTo` `onOpenLink` deliberately UNWIRED) until the verb is reachable — then wire `onOpenLink` → `ixian:openLink:<url>` and the rows become tappable. | `SpixiContentPage.onNavigatingGlobal` (promote `openLink`) or `SettingsPage.xaml.cs` dispatch | trivial |
| A1 | **Apps (uninstall)** | Add a **home-tab-reachable uninstall verb** (e.g. `ixian:uninstall:<id>`) so the apps list can offer Uninstall directly. | Today uninstall is scoped to `AppDetailsPage` only; from the home apps tab there is no dispatch branch, so v1 routes uninstall through tap → `ixian:appDetails` (details page owns it) and shows NO per-row ⋮ menu. A list verb re-enables the redesign's ⋮ Uninstall (apps-menu.js already flags this §8). | `HomePage.xaml.cs` dispatch (one `Contains` branch → `MiniAppManager.uninstall`) | ~1h |
| A2 | **Apps (Discover)** | Decide the **Discover directory feed source** — FE fetches `apps.spixi.io/data/apps.json` directly (domain already whitelisted) vs a new C#→JS command supplying the feed. | The standalone apps shell has a full Discover view (categories, live feed) parked behind a capability flag; the home tab hides the Explore banner until this is settled (apps-shell-spec §0/§2.4). Damir to get the feed contract from the team. | FE fetch + CORS check, or a `sendUiCommand` feed push | decision + ~1–2h |

| L1 | **Launch (create)** | Add an explicit **create-failure release** signal (e.g. `removeLoadingOverlay`, or `createFailed(msg)`). | `LaunchCreatePage.onCreateAccount` generates the wallet on a background thread; on FAILURE it shows a native alert but pushes NO UI command → the redesigned "Create my account" button stays in its indefinite loading morph (the wedge, spec §6②). On success C# navigates to Home + tears the page down, so no success signal is needed — only the failure path wedges. | `LaunchCreatePage.xaml.cs:175` (the `else`/wallet-error branch → one `sendUiCommand`) | trivial |
| L2 | **Launch (create)** | C#-side guard for the `create:<nick>:<password>` parse. | C# splits on the FIRST `:` for the nick, then `Replace(nick+":","")` for the password (LaunchCreatePage.xaml.cs:54–60) — a nick containing `:` or a password containing the substring `<nick>:` is silently corrupted. The shell GATES both inline (never sends a bad payload), but the parse itself stays fragile. Same class as the ENC_DELIM ask. **Worse (support-session finding 2026-07-08):** ALL three flows run the password through `HttpUtility.UrlDecode(e.Url)` before parsing, and they parse DIFFERENTLY — create = `split[1].Replace(nick+":","")`, unlock/restore = raw `split[1]`. So a password containing `+` (UrlDecode → space), a `%xx` sequence, a space, or the `<nick>:` substring is transformed differently between where it ENCRYPTS the wallet (create) and where it is CHECKED (unlock/restore) → the user is silently locked out of their own wallet. Fix = one canonical, decode-safe password capture shared by create/unlock/restore/encpass (length-prefixed or a non-colliding delimiter, and DO NOT UrlDecode the password segment). | `LaunchCreatePage.xaml.cs:52–63` (length-prefix or a delimiter that can't collide) | ~1h |
| L3 | **Launch (tail / onboarding)** | Give the onboarding tail a **backup hand-off** — either an `ixian:backup` route on `OnboardPage` (onboarding-complete + open the Backup screen) or confirm the tail should just complete and let the settings Backup row carry it. | `OnboardPage.onNavigating` handles only `joinbot`/`finish`/`back` — there is NO backup verb, so the tail's **"Back up now"** can't initiate a backup; v1 just advances to the join step and defers the real backup to the settings Backup row. spec §2.5/§9-2. | `OnboardPage.xaml.cs` dispatch (+ route to Backup, mirror `HomePage` backup) | ~1h |
| L4 | **Launch (welcome i18n)** | Same holistic **live-locale** gap as settings S5: `ixian:language:<code>` reloads `intro.html` C#-side, but the self-contained shell re-boots from its OWN bundled dictionary (en-us) → the welcome UI doesn't re-render in the picked locale (the choice DOES persist app-wide + translates native/legacy pages). Appearance is moot on welcome (it's pinned-dark by brand design). | i18n live-locale pass (all shells) — read the persisted locale on boot (`SPIXI_ENV`/`?lang`/`window.SL`) | (holistic) |
| L5 | **Launch (create — blank error dialog)** | Define the missing `intro-new-walleterror-title`/`intro-new-walleterror-text` strings + fix the `"global -dialog-ok"` typo (LaunchCreatePage.xaml.cs:67, note the stray space), and give the "wallet already exists" case its OWN message. | **Support-session finding 2026-07-08 (Damir's real device):** creating an account showed an EMPTY native alert (just "Ok"). Cause: `Node.generateWallet()` returns false when a wallet ALREADY EXISTS (`getWalletList().Count != 0` / `WalletStorage` won't overwrite `wallet.ixi`), and the failure branch (`LaunchCreatePage.xaml.cs:176`) calls `_SL("intro-new-walleterror-title/-text")` — keys that DON'T EXIST in the lang table → blank dialog. So the app was correctly refusing to clobber an existing account but said nothing intelligible. Pairs with **L1** (also release the button — it stays in the loading morph). | `LaunchCreatePage.xaml.cs:176` + lang table + the :67 typo | trivial |
| L6 | **Launch (restore — destructive BEFORE verify)** | Reorder `onRestore` to VERIFY the password first and only THEN mutate state. | **Support-session finding 2026-07-08:** `LaunchRestorePage.onRestore` (:124–130) runs `Remove("onboardingComplete")` + `Remove("lockenabled")` + `Set("walletpass", pass)` **before** checking the password. A WRONG password therefore (a) wipes the onboarding/lock flags → the app stops booting to the lock screen and drops the user into the welcome/create flow, and (b) overwrites the stored plaintext `walletpass` (the only device-side copy that still decrypts the wallet). Also a `Remove("waletpass")` typo at :126 (removes the wrong key). Verify → then commit. | `LaunchRestorePage.xaml.cs:122–146` | ~1h |
| L7 | **Launch (restore — wrong filename shown)** | Push the user's PICKED filename to `setUploadedFileName`, not the internal staging path. | **Support-session finding 2026-07-08:** the restore screen shows `wallet.ixi.tmp` after the user picks `spixi.account.backup.ixi`, making it look like the wrong file is selected. Cause: `onSelectFile` copies the picked bytes to `<spixiUserFolder>/wallet.ixi.tmp` and pushes THAT path to `setUploadedFileName` (LaunchRestorePage.xaml.cs:118) — the original filename from `SFilePicker.PickFileAsync()` is never sent. The restore itself is CORRECT (reads the .tmp copy); only the label misleads. Pass the picked filename (or add it as a 2nd arg). | `LaunchRestorePage.xaml.cs:80,118` | trivial |
| L8 | **Launch (SECURITY — plaintext wallet password)** | Stop storing the wallet password in CLEARTEXT device Preferences; use secure storage / device keystore (or encrypt at rest). | `walletpass` is written and read in plaintext (`Node.cs:250` `// TODO: decrypt the password`, `LaunchCreatePage.xaml.cs:147`, `LaunchRestorePage.xaml.cs:130` `// Todo: secure this`). SECURITY.md-relevant: anyone with device/file access reads the wallet password. (Its one upside — a plaintext recovery copy — is exactly why it should move to `SecureStorage` with an explicit recovery story.) | `Node.cs` + create/restore/retry `Set("walletpass")` sites | ~2–3h |

*Launch notes (no C# ask):* the redesigned launch-shell absorbs all five legacy pages (`intro`/`intro_new`/`intro_restore`/`intro_retry`/`onboarding`) but wires to them **unchanged** — one shell source (`src/shells/launch.html`) is dropped in over all five filenames by `build-shells.mjs`, each booting its own view (`window.__LAUNCH_VIEW__`). The welcome CTAs emit the NAVIGATION verbs (`ixian:create`/`ixian:restore`) so C# pushes the real create/restore pages (a component hook `onGoCreate`/`onGoRestore`, added additively). `ixian:accept` is recorded once at the first welcome CTA tap (the only page that handles it). Avatars fall back to gradient tiles (`loadAvatar`'s absolute path doesn't resolve in the self-contained shell — the repo-wide avatar flag, not launch-specific). Onboarding illustrations (`images/onboarding/step1–4.svg`/`restore.svg`/`join-community.svg`) fail-soft to hidden until re-export (#174).

*Apps notes (no C# ask):* the 6-arg `addApp(id,name,icon,publisher,isSingleUser,isMultiUser)` **already carries publisher + capability flags** — the apps-shell-spec §7/§8 "add publisher" ask is satisfied (home tab renders the creator subtitle from it). Direct-launch (`ixian:startApp`/`startAppMulti`) + a recents strip + the in-tab details/add surfaces are FE work deferred to the **standalone apps-shell repoint** (DECISIONS + apps handoff), not C#. One FE component bug to fix there: `apps-item.js` renders the ⋮ `menuBtn` even when `onMenu` is undefined (only the click handler is guarded) — home.html hides it via CSS for now; the real fix is to guard `menuBtn` *creation* on `onMenu`.

## Chat info / Contact details (Batch 2 — #196)

*Both surfaces wired zero-C# against the frozen bridge: redesigned `contact_details.html` shell (1:1/bot, ContactDetails page) + an in-chat group/bot info takeover (SingleChatPage). The rows below are gated OFF or best-effort until the cutover.*

| # | Type | Ask | Why | Effort |
|---|---|---|---|---|
| CI1 | **Group roster** | **✅ FE DONE (Damir 2026-07-08) — the roster push ALREADY EXISTED, was just unwired.** `ixian:loadContacts` → `addContact(address, nick, avatar, role)` per member (`SingleChatPage.xaml.cs:451-474`, full `friend.users.contacts`). The chat shell now emits `ixian:loadContacts` on chat-info open + builds the member list from it (was a no-op stub → showed only ~39 seen senders). **Remaining [BE], optional:** (a) a numeric member **count** (the true bot total is `botInfo.userCount`, already in the localized `setOnlineStatus` string shown in the topbar; the roster list = synced participants, which may be fewer); (b) a per-member **admin flag** in the push (kick/ban is gated on `mode.admin` today, so members aren't individually flagged). | Was best-effort from SEEN senders only. | `SingleChatPage` (optional numeric count + per-member admin flag) | ~1–2h |
| CI2 | **Contact activity enum** | `addPaymentActivity(txid,type,time,amount,confirmed)` sends `type` as a LOCALIZED "Sent"/"Received" string (direction is a fragile heuristic) and `confirmed` as `"true"/"error"` (collapses pending vs failed). Send a direction + status enum. | Contact-details payments accordion shows verbatim label + best-effort arrow; pending/failed indistinguishable. | ~1–2h |
| CI3 | **1:1 mute** | 1:1 notifications/mute command (today only groups/bots have `en/disableNotifications`). | Contact-details notifications row is `capabilities.notifications:false` (hidden, ready). | ~1–2h (ties §9) |
| CI4 | **Bot destructive** | A bot chat's info takeover has no in-chat leave/remove verb (`ixian:leave` is group-only; bot removal lives on ContactDetails via `ixian:remove`). Decide: surface a bot "remove/leave" from the chat, or route to ContactDetails. | Bot info takeover currently shows no destructive row. | decision |
| CI5 | **Component hero polish** | `createChatInfo` renders neither a hero avatar IMAGE nor a presence line from `setAvatar`/`showIndicator` (stored, unused; gradient hero + `capabilities.presence` only). FE component gap, not a bridge gap — pair with the repo-wide avatar path-resolution flag. | Hero is gradient-only; presence line hidden. | FE, ~1–2h |

## Contacts (Batch 3 — #197)

*Contacts wired into the home shell zero-C#: FAB→picker, topbar→directory (fed by HomePage `clearContacts`/`addContact`), add-contact = redesigned `contact_new.html` shell. Below need C#.*

| # | Type | Ask | Why | Effort |
|---|---|---|---|---|
| CO1 | **Roster flags** | Add a `pending`/RequestSent flag + a contact `type` (0 normal/1 group/2 bot) to HomePage's `addContact(address,nickname,avatar,online,unreadCount)`. | Directory/picker rows can't show the "Request sent" badge or gate group-add on type (both built, unfed) — HomePage's 5th arg is unreadCount, no pending/type. | ~1–2h |
| CO2 | **Group create host** | Either dispatch `ixian:select:<flag+name>:\|addr…` on HomePage, or accept the native WalletRecipientPage multi-picker for v1 (current). | The in-shell `createGroupSetup` (multi-select→setup→create) is built but its result verb is a WalletRecipientPage verb → v1 routes "Create group" to the native picker (`ixian:newchat`). A HomePage `select` (or a redesigned recipient shell) un-defers it. | decision + ~½ day |
| CO3 | **Add-contact failure push** | Add `onRequestFailed(msg)` (or reuse an error push) after `ixian:request:<addr>` rejection (self / already-a-contact → native alert, no push today). | Send-request is fire-and-forget: success pops the page, failure leaves Send latched. FE interim = a 6s timeout fallback restoring the button with soft copy; a real push replaces the guess. | trivial |
| CO4 | **checkAddress correlation** | `onValidAddress(address)` carrying the validated address. | Live-validation ✓ can't correlate which in-flight check succeeded (silent-on-failure, no address in the reply) → a narrow stale-✓ race; C# re-validates on request so cosmetic. | trivial |
| CO5 | **Dual-nick** | Roster `addContact` sends ONE resolved nickname → can't show a contact's ORIGINAL nick under a user-set custom name; append `originalNick`. | contacts-spec §7①; row subline is the truncated address until then. | ~1h |

## Apps details / add (Batch 4 — #198)

*`app_details.html` (AppDetailsPage) + `app_new.html` (AppNewPage) redesigned as shells, zero-C#. Below need C#.*

| # | Type | Ask | Why | Effort |
|---|---|---|---|---|
| A3 | **App verified flag** | `AppDetailsPage.init`'s `verified` arg is hardcoded `"False"` (+ a hardcoded trailing `"True"`). Send a real signed-app trust flag. | The details page can't show a trustworthy "verified publisher" badge; suppressed until real. | ~1–2h |
| A4 | **App fetch failure push** | Add `onFetchFailed(msg)` (or reuse an error push) after `ixian:fetch:<url>` fails (today only `showUrlError`, no per-cause message). | Add-app "Get app" is indefinite-loading; FE interim = 8s timeout unwedge; a clean push lets the button restore with a real reason. Same class as CO3/L1. | trivial |
| A5b | **installed casing** | `init`'s `installed` arg mixes `"false"` (installing) with `Bool.ToString()` ("True"/"False") — FE handles defensively (`asBool`); normalize server-side. | Fragile bool parse. | trivial |

## Cross-cutting (later)
- **Canonical §5 shell filenames + `setRoute`** (ARCHITECTURE item 5): repoint C# page classes to the redesigned shell names once all shells are wired. Currently Stage-4a drop-in over legacy filenames.
- **[Track C] Retire `SetLockPage` + `settings_lock.html`** (DECISIONS #203, resolves the gap-audit [DEC]). The legacy set-app-lock page is ORPHANED in the redesign — App-lock is now an inline hub toggle (`ixian:lock:on|off` → `SettingsPage.xaml.cs:153`) and nothing navigates to `SetLockPage` (only legacy HomePage's bare `ixian:lock`, unused by any redesigned shell). No FE drop-in was built (a redirect to `settings.html` is unsafe — SetLockPage handles only `ixian:back`). Delete `SetLockPage.xaml(.cs)` + `settings_lock.html`, and drop the legacy `HomePage.xaml.cs:243` bare-`ixian:lock` → `PushAsync(new SetLockPage())` branch. | `SetLockPage.*` + `HomePage.xaml.cs:243` + `settings_lock.html` | trivial |
- **[X1] Avatars + app-icons → data-URI push (Option A, DECISIONS #204 — Damir chose A).** C# pushes filesystem PATHS (`getAvatarPath`/`getOwnAvatarPath`; app `icon.png`, MiniAppManager.cs:374) as `<img src>`. Per-platform resolution of a raw path from the Raw-assets origin (`SpixiContentPage.cs:179`): **WinUI/WebView2 resolves** (file://→file//, #190 confirmed) · **Android likely resolves** (`file:///android_asset/` origin + `AllowFileAccess/AllowFileAccessFromFileURLs=true`, WebViewRenderer.cs:356 — UNTESTED, device-check) · **iOS/WKWebView is the prime suspect for BLOCKING** (strict file:// directory sandbox; the avatar dir ≠ the html dir). Legacy used the identical `<img src=path>` mechanism, so wherever legacy showed photos the redesign does too. **Ask:** at each push site, read the image bytes → base64 → push `data:image/png;base64,…` instead of the path (uniform on every platform; small payload — avatars/icons are KB; add a C# encode-cache keyed on file mtime). **FE is ready NOW (zero-C#, #204):** every avatar/icon consumer threads `src` and degrades to the deterministic gradient on `onerror` — validated end-to-end by `src/demo/avatar-datauri.html` + the smoke `avatar-datauri.html` block (a real `data:image/png` renders through c-avatar / c-app-icon / app-invite bubble / createChatInfo hero+members). Consuming Option A needs NO further FE change. | avatar/app-icon push sites (HomePage/SingleChatPage/ContactDetails/Settings/Launch `.xaml.cs`) + a shared `Utils.imageToDataUri(path)` helper | ~2–3h + cache
- **[N1] Chat-open blue flash (Damir F5, HIGH-visibility).** Entering a chat shows a ~½s blue pane before the WebView paints — it's the **native page/WebView background** showing during navigation, BEFORE `chat.html` loads (the shell's own instant-bg + boot spinner only apply once the HTML paints, so they can't cover the pre-paint window). **Fix A (trivial, do first):** set the `SingleChatPage` ContentPage + WebView `BackgroundColor` to the dark surface (`#13171b` / a resource) so the flash is dark→dark, imperceptible, instead of blue. **Fix B (Damir's idea, best for mobile):** preload the chat WebView in the background (offscreen) on chat-row tap, keep the user on the chats list for the ~½s until it signals `ixian:onload`, then present the already-loaded chat — lands them on a populated conversation with no flash. B is a native navigation change; A is a 1–2 line cosmetic. | `SingleChatPage.xaml`/`.cs` + the chats-list tap handler (HomePage) | A: trivial · B: ~½ day |
- **[A8] Media preview + file-open affordance (Damir F5).** (a) A downloaded image/GIF shows as a generic file bubble — the demo's media previewer/`openMediaViewer` can't render because the shell can't tell media from a file: needs a **media flag on `addFile`** (or `addMedia`). [BE] (b) **✅ FE DONE #192** (`typed-bubbles.js` createFileBubble/setFileProgress + typed-bubbles.css): a completed file bubble now shows an explicit accent **"Open file"** label (gated on `onOpen`); tap still fires `ixian:openfile:<fileid>`. (c) **"View in downloads"** (open the folder + briefly highlight the file) has no verb → add e.g. `ixian:revealfile:<fileid>`. [BE] | `SingleChatPage` addFile (media flag) + `file-bubble.js` (open label) + a reveal verb | (b) trivial · (a)/(c) BE |

- **[#82 / N-media] Remote media LOAD-BY-DEFAULT (Damir 2026-07-08 posture, no verb needed).** The chat now auto-loads remote GIFs/images by default (legacy parity) and persists per-peer loaded URLs (`spixi.media.loaded.<addr>`) so they survive leaving + re-entering a chat (fixes the "GIF hidden again on re-open" bug). Auto-loading a remote asset **reveals the reader's IP to the host** (P2P has no proxy). Accepted by Damir as the default; the opt-out is `spixi.media.autoload='off'` → tap-to-load (a settings toggle wires to this key — UI deferred). **BE/security:** confirm the posture; consider a relay/proxy for privacy-sensitive users. OpenGraph link-preview cards stay CORS-blocked client-side (need sender-composed meta or a proxy). Sent remote-media tiles now carry a 2px outgoing-bubble-colour border. [posture/decision]
- **[N2] Call UI is legacy (Damir F5).** `ixian:call` correctly initiates the call, but the call SCREEN that appears is the legacy VoIP page — the redesigned `call-overlay.js` isn't wired. Like ContactDetails, the call surface is a SEPARATE redesign target (its own page/overlay repoint), out of the chat-screen scope. Wire at the call-surface repoint. | `CallPage`/VoIP surface repoint | separate batch |
- **[N3] Pushed-page flash + Account slide-in flicker (the "solution for ALL flickering" completion).** The FE fix removed the two WebView-internal flicker causes on the pushed utility shells (contact_details / app_details / app_new / contact_new / settings_backup / settings_encryption / downloads / dev / contributors + home/settings): (1) a head-top synchronous `data-theme` script now sets the theme BEFORE any paint, so no light↔dark flip, and (2) the instant-bg `<style>` keys off `data-theme` + a static themed boot spinner covers the bundl