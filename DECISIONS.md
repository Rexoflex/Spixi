# Decision Log

Every significant decision gets a row when it's made — this is the feedback loop that prevents drift.
Status: ✅ locked · 🟡 provisional (locks after named review) · ❌ superseded (keep row, link replacement).

| # | Date | Decision | Rationale | Status |
|---|---|---|---|---|
| 1 | 2026-07-02 | Bridge protocol frozen; new commands spec-only (ARCHITECTURE.md §8), mocked in JS until BE implements | Zero C# changes to ship frontend; BE stays in control | ✅ |
| 2 | 2026-07-02 | 29 pages → 9 flow shells; MAUI page classes and navigation untouched (shells + route param) | Kills duplication without touching BE surface (ARCHITECTURE.md §5) | ✅ approved by Damir (BE assumed) |
| 3 | 2026-07-02 | Vite + vanilla JS; built output committed to `Resources/Raw/html` | No Node on MAUI dev machines/CI | ✅ |
| 4 | 2026-07-02 | Conservative WebView CSS baseline; modern features flagged per-case at demo | 4 platforms, unknown minimum WebViews | ✅ |
| 5 | 2026-07-02 | Theme follows OS + manual override; dark = token swap via `data-theme`, set by JS | Replaces 2×115KB parallel stylesheets | ✅ |
| 6 | 2026-07-02 | i18n: keep `*SL{}` channel; per-shell `window.SL` dict + `window.SPIXI_ENV` for config | No C# changes; XSS-safe textContent rendering (ARCHITECTURE.md §7) | ✅ |
| 7 | 2026-07-02 | Token architecture: primitives (mode-less) → keys (mode-less vocabulary) → tokens (LIGHT/DARK live here, per token) | Damir's structure; per-token dark control (DESIGN_SYSTEM.md §1) | ✅ |
| 8 | 2026-07-02 | Naming: numeric = scales (`spacing/16`); role names = semantic tokens (`layout/bar-top`) | Value-truth for scales; roles outlive values | ✅ |
| 9 | 2026-07-02 | Single `action` role for interactive color; no separate accent role at component level | Wallet needs one unambiguous interactive color; old file proved accent drift | ✅ |
| 10 | 2026-07-02 | z-index, motion, elevation, opacity = code-only tokens (not Figma variables); elevation in Figma = effect styles when needed | Figma can't use them; dead variables pollute pickers | ✅ |
| 11 | 2026-07-02 | Figma is source of truth for tokens; cleanup happens in Figma, code regenerates (`tokens.css` header documents re-sync) | One-way sync, diffable updates | ✅ |
| 12 | 2026-07-02 | Buttons: 3 heights named by value (32/44/56); width = instance-level auto-layout (hug/full), NOT a variant axis | Avoids 432-variant explosion; height is the real design axis | ✅ |
| 13 | 2026-07-02 | Build split: Claude = component structure/bindings + composed-screen drafts + code-first utility screens; Damir = polish + brand-heavy screens; Figma native AI = throwaway exploration only; dark mode = verification only | figma-sweep.md §3 | ✅ |
| 14 | 2026-07-02 | Chat bubble width: `max-width: min(var(--bubble-max-pct, 75%), var(--layout-bubble-max))` — % of message-list container, absolute cap 320 (responsive-ready) | Messenger best practice (75–80%); % is code-only, cap is a Figma variable | ✅ |
| 15 | 2026-07-02 | Per-component process: code always; Figma when design-valued; screens = compositions (Figma drafts for design-heavy, code-first for utility); every component demoable via mock bridge | Q3 agreement | ✅ |
| 16 | 2026-07-02 | Component code conventions: one `.js` + one `.css` per component · plain functions returning DOM · CSS consumes semantic tokens ONLY (no primitives/keys) · modifiers = data-attributes mirroring Figma names (`button/44` ↔ `data-size="44"` ↔ `createButton({size:44})`) · states = native pseudo-classes (`:hover`, `:focus-visible`, `:disabled`) + `data-loading` · no inline styles · demo case per component | Anti-drift; taxonomy identical across Figma/CSS/JS | ✅ locked — button reviewed by Damir 2026-07-02 |
| 17 | 2026-07-02 | 44-height buttons use `label/sm` (14px) text | Reviewed with button demo, no objection | ✅ |
| 28 | 2026-07-02 | Outline + text buttons get state-layer backgrounds: hover = `surface/interactive/hover` (neutral), pressed = intent tonal surface (`action-tonal`/`destructive-tonal` default). Applied in code AND all 24 Figma variants | Damir review finding: color-only feedback invisible on touch (no hover state exists there) | ✅ |
| 29 | 2026-07-02 | Button success morph: `setSuccess()` = loading off → solid `surface-success` + check + label swap (~1.4s, `role=status` announced) → restore. ONLY for in-place actions with no other confirmation — never when the action navigates or fires a toast/alert (one confirmation per action). All types/intents morph to the same solid success. Figma: documented morph spec, no variant-axis explosion. **Amended after Damir review: all button state swaps (loading↔idle↔success) animate width via measured morph (`morphWidth`, 200ms `easing-standard`, hug buttons only) and color transitions at `duration-200` — no abrupt snaps.** Measured inline `width` during morph is a sanctioned exception to #16's no-inline-styles rule (runtime layout, self-clearing) | Completion feedback for save/delete-style ops; double-signaling is noise; snap transitions feel broken | ✅ |
| 30 | 2026-07-02 | Button icon API: `iconPosition: leading/trailing` + icon-only (`data-icon-only`, square 32/44/56, `ariaLabel` REQUIRED — dev console warning if missing) | Figma variants already show both positions; icon-only needed across the app frame | ✅ |
| 18 | 2026-07-02 | JS: per-component source files bundled by Vite into one file per shell; shared utils = explicit imports; no global JS | Zero runtime cost for many source files; kills the chat.js-monolith pattern; imports make dependencies visible | ✅ |
| 19 | 2026-07-02 | Desktop: target single-WebView layout (all panels as DOM) enabling resizable dividers + third details panel in pure frontend; requires "hosted panes" bridge proposal (ARCHITECTURE.md §8) — BE approval pending. Fallback: two-WebView with fixed proportions, polish only | One contained C# change vs MAUI work on every future layout tweak | 🟡 pending BE |
| 20 | 2026-07-02 | Hero screens (e.g. Wallet): `surface/hero` region token + `on-hero` text/icon tokens; top bar adopts hero color via scoped override (`.screen--hero` reassigns `--surface-topbar` etc.), NOT separate topbar variants | One pattern covers any future colored-header screen; components underneath unchanged | ✅ |
| 21 | 2026-07-02 | Balance figures use tabular numerals (`font-feature-settings: "tnum"`) — code utility class, no color token needed (color from context: on-hero or text-neutral-01) | Digits align, no jitter on balance updates; both fonts ship tnum | ✅ |
| 22 | 2026-07-02 | ⚠️ Watch item: native safe-area padding is painted C#-side with theme background (`checkIfPageLoaded`, iOS/Android) — hero screens may show a neutral strip under the status bar. Verify on first device build; if seam appears, propose tiny C# tweak (transparent inset / edge-to-edge WebView + CSS `env(safe-area-inset-top)`) | Found in audit; affects hero design | 🟡 verify at device demo |
| 23 | 2026-07-02 | Interactive-surface state tokens (shared by list rows/menu items/icon buttons): `surface/interactive/{hover, pressed, selected}` only. Focus = `outline/focus` ring, never a fill; disabled = `text-neutral-disabled` + `--opacity-disabled`, no surface (buttons keep their own disabled surfaces) | One state set for all row-like interactives; focus-as-outline for a11y; disabled fills make lists look broken | ✅ |
| 24 | 2026-07-02 | Message context menu: bubble gets NO active-state fill — promotion = scrim (`--z-40`) + bubble above (`--z-50`, elevation). New `surface/region/menu` token (light neutral/10, dark neutral/700 — elevated surfaces lighten in dark). Mobile = long-press + scrim + anchored panel; desktop = hover affordance + light-dismiss menu at `--z-30`, full keyboard support. Spec: DESIGN_SYSTEM.md §5b | Matches messenger conventions; menu ≠ card semantics; one component, two presentations | ✅ |
| 25 | 2026-07-02 | Reply and Edit message actions do not exist in the current bridge — proposed as `ixian:contextAction:reply/edit` (ARCHITECTURE.md §8); menu renders them only behind the capability handshake | Menu contents must be constrained by bridge reality, not design wishes | 🟡 pending BE |
| 26 | 2026-07-02 | No slide-to-send/slide-to-confirm for payments. Deliberateness = review step (recipient/amount/fee + explicit confirm); optional future: LockPage(true) biometric/PIN gate for sends above user threshold (small C# wiring, proposable) | Poor discoverability + a11y (needs parallel path anyway), awkward on desktop, security theater vs real gate | ✅ |
| 27 | 2026-07-02 | ❌ superseded by #35 (inline copies didn't scale past two components) | — | ❌ |

| 31 | 2026-07-02 | Screen-level work MUST reference the 📱 Screens frames (screenshot + specs pulled before building); no invented UI elements. Fonts ship embedded (Sora + Source Sans 3 variable WOFF2 Latin, `src/assets/fonts/`) — a demo without real fonts is not reviewable | Damir review: first app-frame demo drifted from design (invented actions, wrong nav pill, fallback fonts) | ✅ |
| 32 | 2026-07-02 | Icon pipeline: Damir bulk-exports Figma icons frame → `src/assets/icons/` (canonical filled glyphs + `logo.svg`) → `scripts/generate-icons.mjs` regenerates `icons.js` (ESM) + `icons.iife.js` (classic script for file:// demos). Ink #131415 → `currentColor`; brand #3050BD → `var(--icon-accent)`. logo.svg untouched, root-header only (Chats/home) | Figma glyphs are filled, not stroke Tabler; assets-from-Figma kills icon drift; iife file spares demos from inlining path data | ✅ |
| 33 | 2026-07-02 | Unread chat row: NO background tint — bold name + count/mention indicator only (resolves chat-list-spec ①④: weight is state-driven). Tinted surfaces stay reserved for selected (desktop) and pressed states | Messenger convention (WhatsApp/Telegram/Signal); tint would collide with selected/pressed and looks noisy at high unread counts | ✅ |
| 34 | 2026-07-02 | Placeholder avatar: code-generated, DETERMINISTIC per address — hue derived from address hash → two-stop gradient + initials (named contacts) or user glyph (address-only). No asset files; same algorithm documented for Figma mockups | Stable identity cue (random would reshuffle identities), zero assets, works in both themes; Telegram-style proven pattern | ✅ |
| 35 | 2026-07-02 | Demos load real component code via generated classic-script bundle `src/demo/spixi.iife.js` (+ `icons.iife.js`): `scripts/build-demo-bundle.mjs` strips imports/exports from component ESM sources. Demo HTML contains ONLY demo wiring. Supersedes #27 inline copies | file:// blocks module imports but not classic scripts; zero-drift (bundle IS the source), demos stay double-clickable | ✅ |
| 36 | 2026-07-02 | Logo ships in the icon registry with `currentColor` fills — inherits topbar title ink, correct in both modes. Root topbar logotype = mark (28px) + wordmark; view titles = `heading-sm`, root logotype text = `heading-md`; hero amount tracking −1px | Damir review round | ✅ |
| 39 | 2026-07-02 | Bottomnav selected state = FILLED icon twin (`<name>-filled` in registry) crossfading over the outline glyph (fade + 0.7→1 scale pop, `duration-200`, decelerate) + tonal pill as `::before` layer that GROWS in (scale 0.85→1 + fade, decelerate) and shrinks/fades out on deselect; ink/label transition both ways. Twin detection at build (`ICONS[name+'-filled']`); no twin → outline stays, ink transition only. Label animates `font-weight` (variable fonts; older WebViews snap — flagged per conservative-CSS rule). Reduced motion: durations zero via existing token override | Selection change was abrupt (instant color/weight/pill swap); filled-vs-outline is the standard iOS/Android nav pattern; pill-on-pseudo lets background animate independently of content; graceful fallback keeps icon exports non-blocking | 🟡 pending Damir look; `user`-filled icon missing from Figma export |
| 56 | 2026-07-02 | Overlay layer (`overlay.js` + c-sheet/c-modal): host-mounted scrim + one stack; DOCUMENT-level focus containment while stack non-empty (Tab trap on top entry + `focusin` bounce-back, all focus `preventScroll`); Esc = safe dismiss path, default ON for BOTH components (`escDismiss`) while scrim click is per-component (`lightDismiss`: sheet true, modal false — confirmations stay explicit, ARIA APG); `dismissTopOverlay()` = shell back-hook; host scroll lock held until the exit transition ends; removal via transitionend + 400ms fallback, reopen-during-exit finishes the pending removal (WeakMap, #29 pattern). c-sheet = light-dismiss, z-40; c-modal = explicit, z-50, alertdialog autofocuses the SAFE action. Constraints: never open a sheet ABOVE a modal (z-40 renders under z-50); overlay.css loads after base.css; inner `.u-scroll` children stay scrollable behind the scrim by design (only the host locks) | Replaces `toggleAnimatedSlider` menus + `showModalDialog`; Esc-as-always-safe + explicit scrim policy per ARIA APG; host mounting keeps demos and shells identical | 🟡 pending Damir look |
| 55 | 2026-07-02 | c-txlist-item mirrors Figma tx list-chat (sent/received/pending/failed): `data-type` derived from direction+status (pending/failed override presentation); interaction states code-first per #43 (Figma rows static); amounts/fiat arrive PRE-FORMATTED from bridge (component dumb); absolute `formatTxTimestamp` ("20 Mar, 9:15"), NO ticker; direction announced to SRs via labeled arrow icon (`strings.sent/received`) — color/sign alone insufficient; strings keys tx-scoped (`txPending/txFailed`). Figma fixes needed: failed amounts use raw unbound `#6a717c` → `text-neutral-disabled`; sent-fiat ink `-01` normalized to `-02` | Wallet list = next shell dependency; grounded in `addPaymentActivity` contract (rebuild-per-tick → rebuild-only API, no update fns) | 🟡 pending Damir look |
| 54 | 2026-07-02 | c-badge mirrors Figma badge 5 types × tonal/solid exactly (tonal = `{type}-inverse` surface + `{type}` ink; solid = `{type}` surface + `inverse-01` ink); STATIC — no interaction states by design; label-xs bold, radius-8, optional 16px leading/trailing glyphs. Tx-row glyphs: pending = `clock-hour-10`, failed = `alert-square-rounded` (best match in exported set — Damir to confirm) | Badge is used "everywhere" per DESIGN_SYSTEM §3; tx rows needed warning/error tonal now | 🟡 pending Damir look (glyph choices) |
| 53 | 2026-07-02 | Theme switch suppresses ALL transitions for the swap frame: `.theme-switching` class on `<html>` (base.css, `transition: none !important` incl. pseudos), removed after two rAFs | Damir: chips/topbar flickered on toggle — components transition background/color at mixed 100/200ms, so a token flip repaints staggered | ✅ |
| 52 | 2026-07-02 | Search scope: chat-LIST search (contact names + last-message excerpts) is frontend-only — now with match HIGHLIGHTING via `highlight.js` (`setHighlights`: TreeWalker over TEXT nodes, wraps matches in `span.c-highlight` warning-tonal wash; structured children survive; XSS-safe). FULL message-history search CANNOT be frontend-only: histories live C#-side, WebView never receives them, and caching messages in WebView storage would breach SECURITY.md — proposed `ixian:search` command instead (ARCHITECTURE §8) | Damir asked for message-content search; honest boundary: list-level now, history search behind a BE-approved command | 🟡 §8 proposal pending BE |
| 51 | 2026-07-02 | c-search-field = specialization of Figma text-field/large (no dedicated search comp exists): search glyph 18 + `input[type=search]` body-md + clear button visible only while filled (replaces the trailing arrow; 44px pseudo tap target); min-height 44 (`size-target-min`, Figma ≈40); permanent transparent border (Figma default has none, hover adds one = layout jump); focus = container action border + 1px shadow (≈ Figma 2px, no shift) INSTEAD of the #23 ring — sanctioned text-field focus treatment; Esc clears (consumed only when non-empty); WebKit native cancel suppressed. TOKEN FIX (Damir): Figma's hover `outline-info` / focus `text-action-default` borders are errors → `outline-action-hover` / `outline-action-default`; mirror to Figma text-field/large | Search is the chats-shell entry point; text-field tokens reused so the eventual c-text-field shares the vocabulary | 🟡 pending Damir look |
| 50 | 2026-07-02 | c-chip mirrors Figma chip/{small,large} + code-first additions: SELECTED state (`aria-pressed=true`) = TONAL fill + action ink (Damir review: solid was "too strong"; pressed-on-unselected keeps its outline border so the states stay distinct) and DISABLED; icons opt-in (Figma defaults leading+trailing ON — filter chips get none); dismissible = whole-chip trigger, X glyph decorative (nested button = invalid HTML); radius-16 per Figma (demo placeholder had radius-full drift); 44px tap target via vertical pseudo extension. Add selected+disabled variants to Figma chip set | Filter chips (wallet All/Sent/Received) need persistent selection; Figma set only covers transient interaction states | 🟡 pending Damir look (also: solid vs tonal+check for selected — spec doc ①) |
| 49 | 2026-07-02 | Topbar action icon-buttons get hover/pressed INK change (→ `icon-action-hover/pressed`), not just the background wash; ink flows ONLY through button private vars (literal `color:` removed — it out-cascaded button.css's :hover). Hero surfaces pin ink AND state backgrounds to on-hero values (white washes 0.12/0.18, token candidates) — action-blue ink and near-white neutral washes both fail on the gradient. Demo eye: hover ink → `text-on-hero-muted` (periwinkle tint; on-hero ink is already near-white so "lighter" = tinted) | Damir: icon-button hovers too subtle (eye, contacts); ink change adds a second visible cue | 🟡 pending Damir look |
| 48 | 2026-07-02 | Bottomnav unread badge = ERROR role (`surface-error` + on-action ink) with a 2px `surface-bottombar` ring (box-shadow, no layout shift). Deliberately diverges from the chat-list count indicator (stays action-blue): on the selected item, an action-blue badge disappeared into the tonal pill + filled icon (same hue family). Error-role badges are the Material/iOS convention for exactly this reason | Damir screenshots: badge unreadable on selected pill in both themes; error is the one role guaranteed to read on all nav surfaces | 🟡 pending Damir look + Figma mirror |
| 47 | 2026-07-02 | Icon registry stays monolithic for demos; tree-shaking/splitting strategy (per-shell subsets) is decided AT Vite integration, not before | Audit flagged 90 KB untreeshakeable registry; splitting now would guess at shell import patterns — Vite build is where the cost/benefit is real | 🟡 revisit at first shell build |
| 46 | 2026-07-02 | **AUDIT LOOP RULE (Damir):** after every completed component set + behavior, or any major feature, spin up agents automatically: (1) read-only audit agent over the touched surface, (2) fixer agents on the findings (disjoint file scopes, explicit cross-file contracts), (3) adversarial reviewer agent verifying fixes + hunting drift the fixes introduced — loop 2↔3 until verdict CLEAN. Mechanical fixes land directly; anything architectural becomes a 🟡 DECISIONS row for review, never silently invented | Small files, small components — drift compounds fast; catching it per-milestone is cheap, retrofitting isn't. First run (this session) found 40+ issues incl. 2 shipping bugs | ✅ |
| 45 | 2026-07-02 | JS baseline: ES2020 floor pre-Vite (optional chaining, `\p{L}` unicode escapes OK); final target set by Vite config at integration | Audit flagged undocumented JS floor; ES2020 ≈ the conservative-CSS-era WebViews already assumed | 🟡 confirm floor vs oldest target WebView |
| 44 | 2026-07-02 | Component update-API convention: FREE EXPORTED FUNCTIONS operating on the element (`setNavActive(nav, id)`, `setNavBadge(nav, id, count, strings)`, button's `setLoading/setSuccess`) — no element-method augmentation (`el.setActive` removed) | One pattern across components (audit MED); free functions tree-shake and keep elements clean | 🟡 pending Damir |
| 43 | 2026-07-02 | EVERY interactive element gets a `:hover` state from #23 interactive tokens (or its own component hover tokens), wrapped in `@media (hover: hover)` so touch WebViews don't get sticky hover. Bottomnav items: hover = `surface-interactive-hover` (unselected) / `tonal-hover` pill (selected). Demo placeholders (eye, quick-actions, chips) got interim hovers; real states land with their components | Damir found hover missing on nav/eye/quick-actions while chat rows had it — state coverage was per-component ad hoc; one rule prevents recurring drift | ✅ |
| 42 | 2026-07-02 | Bottomnav badges: CHATS ONLY (numeric, 99+ cap), wired to existing `setUnreadIndicator(count)` — bridge-real, no new commands. NO wallet/apps badge in v1: bridge has no "unseen tx" concept (`addPaymentActivity` rebuilds per tick), and payments/files/app-invites arrive AS chat messages so they already count toward chat unread — a wallet badge would double-signal. Account: no badge now; future dot for "update available" possible if BE exposes it. OPEN (BE): does `setUnreadIndicator` total exclude muted chats? Convention (WhatsApp) excludes | Badge = actionable user-directed content only (HIG/Material); grounded in bridge audit A §home/chat; avoids inventing BE work for a duplicate signal | 🟡 muted-exclusion pending BE answer |
| 41 | 2026-07-02 | Scroll regions use `.u-scroll` utility (base.css): mobile WebViews keep native transient overlay indicators (untouched); desktop gets thin (6px) themed thumb, transparent until `:hover`/`:focus-within`, gutter always reserved so content never shifts. Dual impl: `scrollbar-width/color` (FF, Chromium 121+) + `::-webkit-scrollbar` (older WebKit/Blink) | Persistent default scrollbars look wrong inside app panes; matches the mobile "thin stripe only while scrolling" convention without JS; never style `<body>` scrollbar | 🟡 pending Damir look |
| 40 | 2026-07-02 | Icon generator normalizes fill-less `<path>`s to explicit `fill="currentColor"` | Raw Tabler downloads carry fill on the root `<svg>` (stripped by the pipeline) with bare paths — they rendered invisible against the factory's `fill="none"` root (apps/messages-filled bug); Figma exports (per-path fills) unaffected | ✅ |
| 38 | 2026-07-02 | Avatar hue hash gets murmur3-style avalanche finalizer (`h ^= h>>>16; h = imul(h, 0x45d9f3b); h ^= h>>>16`) before `% 360` | Plain `(h*31+c) % 360` clustered similar Latin names into a ~30° band — 6 of 12 demo contacts rendered olive; avalanche scatters across the full wheel. NOTE: changes existing identity hues; Figma mockup algorithm doc (#34) must reference the new function | ✅ |
| 37 | 2026-07-02 | Avatar gradients are THEME-AWARE: JS sets only deterministic hues (`--av-h1/--av-h2` custom props); saturation/lightness are CSS vars themed in avatar.css — light = deep gradient (58%/48% → 55%/34%) + white ink, dark = pastel gradient (52%/62% → 48%/48%) + dark ink (`text-neutral-inverse-01` flips per mode). Extends #34 | Fixed hard-coded gradient gave dark-on-dark initials in dark mode; hue stays identity-stable across themes, S/L tunable in CSS without touching JS | 🟡 dark-mode S/L values pending Damir look |

| 57 | 2026-07-02 | Overlays batch B/C built code-first (no Figma designs existed — sanctioned by #13 split): c-banner (in-flow, warning-inverse, max-height collapse), c-toast (z-70 above modals, surface-menu card, one-per-host + capped deduped queue, 3.5s auto-dismiss, #29 discipline), c-callbar (z-60 above modals, surface-success + on-action ink, live tabular timer, bridge re-call mutates in place, white hangup glyph — red-on-green rejected). NOT modal overlays: no scrim/trap/focus steal. #46 loop ran: CLEAN + 4 hardening MINORs landed. Mirror to Figma with the #48/#49 batch after Damir approval; state washes on success/hero = token candidates | Complete the app-frame set; spec: docs/overlays-spec.md batch B/C | 🟡 pending Damir demo review |

| 58 | 2026-07-02 | ALL screen titles = `heading-sm`: topbar view titles, root logotype wordmark (was heading-md — overpowered the 28px mark), wallet hero title (was heading-md). One title scale app-wide | Damir review with DevTools; logotype proportion | ✅ |
| 59 | 2026-07-02 | Connectivity states render as TOPBAR TITLE STATE, not banner ("Connecting…"/"Waiting for network…" replaces/sub-lines the title — WhatsApp/Telegram convention: quieter, zero layout shift). c-banner is retained for actionable/critical notices only (e.g. update available). CAVEAT: bridge's `showWarning(text)` sends pre-localized strings with no type — routing needs a warning-class param (§8 proposal added); until BE approves, shells route ALL showWarning → banner (correct, just less premium) | Damir "what's more premium" question; title-state wins for status, banner stays for actions | 🟡 pending BE (§8) + title-state prototype with home shell |
| 60 | 2026-07-02 | Modal actions: side-by-side confirmed for TWO short labels (cancel/confirm — iOS convention); STACK full-width when any label wraps, 3+ actions, or both destructive+safe need clear separation. Component may auto-stack on label overflow later | Damir + standard dialog UX; resolves overlays-spec flag ② | ✅ |

| 61 | 2026-07-02 | **Figma mirroring part 1 DONE** (connector): wash tokens created 3-tier (alpha primitives `color/alpha/*` → keys `color/wash/*` → tokens `surface/region/wash-on-{hero,success}(-pressed)`) + adopted in code (tokens.css/topbar/callbar/demo, rgba literals replaced) · #51 text-field borders rebound (hover `outline-info`→`outline/action/hover`, focus `text/action/default`→`outline/region/focus`, filled+empty) · #54 failed-tx amounts: wrong-role `inverse-02`→`text/neutral/disabled` (audit's "raw hex" was actually a role error) · #48 nav badge: error fill + 2px bottombar ring + on-action ink, both variants · #49 verified — Figma hover/pressed inks were already correct (code cascade was the only bug) · #58 logotype wordmark + Wallet hero title bound to `typography/heading/sm` style. **Part 2 DONE 2026-07-03:** chip/small + chip/large → 10 variants each (`selected=false/true × default/hover/pressed/focused/disabled`, mirrors chip.css exactly — existing unselected states were already code-true) · new "Overlays" section on Components page: c-banner, c-toast, c-callbar, c-sheet, c-modal reference components, all token-bound, modal composes live button/44 instances (decorative icon slots hidden) · effect styles `elevation/2` + `elevation/3` created (match tokens.css shadows) | One-pass design-code resync | ✅ |

| 62 | 2026-07-03 | Chat assembly process: pipelined — audit agent adversarially reviews component N while N+1 is built; #31 design-scan before each; everything lands in a live conversation demo. Build order: core conversation (text bubbles+grouping+separators+chat topbar+composer) → typed bubbles (payment/file/app/call) → reactions/context-menu/typing | Damir: sentient/sensible/careful, token-efficient, premium | ✅ |
| 63 | 2026-07-03 | Message grouping (AMENDED post design-scan + audit): consecutive same-sender messages cluster — gap 2 in-group / 12 between groups, sender label once on first, avatar once on last (received groups). **Time+status render INSIDE every bubble (per Figma design), NOT last-only; no tails — grouping = logical-corner tightening (16→8 on group-adjacent corners)**. Positions single/first/middle/last | Design is authority (#31); original row predated the scan | ✅ |
| 64 | 2026-07-03 | Composer: attach + input + send always visible (send disabled-styled when empty) + MIC SLOT designed in but feature-flagged OFF in v1 (`voice: false`) — voice messages planned; §8 proposal added for BE (record/playback commands + voiceNote message type). No WhatsApp-style morph | Damir: voice support coming soon | ✅ |
| 65 | 2026-07-03 | Reactions placement RESOLVED with both on screen (batch 3): **OVERLAP the bubble's bottom outer corner** (Telegram-style) — Damir leaned overlap, AI concurred (bubble content stays clean, scans at a glance, no layout coupling with the floated meta). `inside` kept as a superseded param option; **Figma design updates to overlap at next mirror**. Tips = amount pill in same row; count/own/tip variants extend Figma's single `reaction` chip (design gap, code-first). Live-added reactions get a cheap scale-pop (`animate: true`, token durations → reduced-motion-safe) | Compact/premium; decided side-by-side per the parked plan | ✅ 2026-07-03 |
| 66 | 2026-07-03 | Chat design gaps flagged (code-first drafts in our language unless Damir designs first): file-bubble progress/failed/accept states · reaction count/tip variants · composer attach + assembly + mic slot · typing indicator (conversation) · scroll-to-latest button w/ unread count · image/photo bubble variant · **chat-list `reaction` excerpt type** ("You/X reacted 👍 to …", Damir 2026-07-03) — excerpt string is C#-composed: check whether the bridge emits it on reaction events, else BE question | Damir: "stuff probably not in the design that I missed — flag it" | 🟡 rolling |

| 67 | 2026-07-03 | Chats-list upgrades queued (Damir, build with home shell): ① search field collapses under the top bar on scroll, reveals when scrolled to top (premium scroll pattern — pure frontend) ② filter chips under search, WhatsApp-style: All / Unread / Favorites / Groups — Unread+Groups are client-side (addChat already carries unread+type); **Favorites need BE persistence → feature-flagged off like voice (#64), §8 proposal added**. NOTE: `pinned` display exists in design/chat-item but addChat's 8 args carry NO pinned flag — same BE boat, bundled into the §8 proposal | Damir 2026-07-03 | 🟡 build at home shell |

| 68 | 2026-07-03 | Batch 1a review round (Damir) + BATCH 1b BUILT: ① failed message = thin error outline (inset shadow, no layout shift) + 28px retry circle + "Not delivered · Tap to retry" caption, `onRetry` cb ② chat canvas = `chat-bg-pattern.svg` tile (opacity .3 light / .05 dark) over `--gradient-chat` (primary-50→screen / primary-900→screen), pattern fixed under scroll ③ read status = SUCCESS GREEN checks (`--icon-bubble-read`: success-300 light / success-600 dark) ④ c-topbar `chat` variant (avatar-40+dot, name label-md, sub body-sm text-02, bridge-driven "Online"/"6 members", `setTopbarSub`) + phone/⋮ actions ⑤ c-composer (⊕ in field, auto-grow ≤5 lines, Enter-sends on hover-capable devices only, mic⇄send behind voice flag). Suggest lang-file rewording: "Offline" → "Last seen recently" (frontend lang/*.txt edit, no BE). avatar-40 tokenized. NOTE: retry has NO bridge command — shell re-emits `ixian:chat:` with same text; flag if BE wants a dedicated resend | Damir review 2026-07-03 + identity-bar spec/mock | ✅ (1b audit pending) |

| 69 | 2026-07-03 | Batch 1b audited (FIX-NEEDED → fixed): composer focus ring on the FIELD (`:focus-within` inset, was suppressed with no replacement) · 5-line cap arithmetic (border-box padding, JS+CSS agree at 136px) · clearComposer no longer fires spurious typing (isTrusted guard) · demo delivered-swap keeps size/a11y. Damir live notes applied: received bubbles = WHITE (`neutral-10` light — lightest token; pops on canvas) · live-send entrance animation (`c-bubble-row--in`, translateY+fade, token durations → reduced-motion-safe, live appends only) · retry circle HUGS bubble (`c-bubble-line`, gap-4). Accepted judgments: Enter-to-send proxied on `hover:hover` (no hardware-kbd media query exists; iPad+kbd gets button-send) · caption is redundant touch affordance (button carries keyboard path) | #62 pipeline round 2 | ✅ |

| 70 | 2026-07-03 | FULL chat audit (Damir-ordered, wide net): 3 MAJOR fixed — IME composition guard on Enter-send · sender-label dedicated S/L (34%/74% — avatar-gradient lightness failed AA on white bubbles for yellow/green hues) · composer RTL logical paddings. 9 MINOR fixed — bundle avatar parity · datesep pill hairline (vanished on same-value canvas) · received bubbles get elevation-1 (pale canvas bottom) · failed-bubble width rule moved to stack (75% vs row restored) · retry hit-area ::after (−8px) · action button → --size-target-min + sanctioned-value annotations · presence dot RTL-logical · stale docblock/data-failed comment · sent-meta light → primary-100 (was 4.3:1 sub-AA). NITs: aria-live on topbar sub · entrance class removed on animationend · unused demo destructure. Damir r2 items: light canvas gradient primary-100→50 (stays tinted — white-on-white at bottom was the contrast bug) · failed outline REMOVED (red-on-blue clashed; retry+caption carry error) · chat topbar hairline bottom (vs white bubbles). Unread-divider check: addMe/addThem carry per-message `read` → **frontend-only, NO BE work**; joins batch 2 | #62 pipeline, full-surface round | ✅ |

| 72 | 2026-07-03 | **FULL chat audit r2** (Damir-ordered, 3 parallel adversarial agents: JS/bundle · CSS/contrast-math · a11y/bridge/fidelity — report `docs/audit/chat-full-audit-r2.md`): 7 MAJORs fixed — ① invalid-timestamp crash guards (isNaN before toISOString, everywhere) ② invalid-`lang` crash → shared `docLocale()` in timestamp.js ③ loading buttons now truly disabled (keyboard could re-fire a processing Pay; CSS keeps loading skin) ④ action guards: `oneShot` latch for state-changing (Pay/Join/Cancel/Accept — **shell contract: bridge re-render replaces the card**) + `reentryGuard` for repeatables (Retry/Open/Launch/Details) ⑤ file-bubble persistent state dispatcher + setFileProgress refreshes aria/glyph (completed downloads were dead buttons announcing "Downloading") ⑥ dark `--icon-bubble-read` → success-800 (600 was 2.3:1 on primary-400) ⑦ sender-label light L → 28% (34% sub-AA at hues 60–180). Lesson: audit-fix token picks need their own contrast math against the ACTUAL surface. +14 quick wins (width chain +32 both branches, canvas scroll thumb -03, dark chat-topbar hairline -02, NaN clamps, "undefined" text fallbacks, avatar initials aria-hidden, log tabindex, animationend filter+reduced-motion fallback, progressbar name, …). Bridge coverage gaps tabled (setChatMode/channel selector/contact-request pane/loadmore/message-status updaters = UNCOVERED, no code home yet) — sync to ARCHITECTURE §9. Backlog MINORs listed in report, fold into related batches | Damir: "another full audit with several adversarial agents on everything" | ✅ |
| 71 | 2026-07-03 | **BATCH 2 BUILT + audited**: typed message cards in the Figma card language — createPaymentBubble (request-in/out + sent/received × actionable/pending/processing/failed/completed/declined/canceled, gold pending amounts, tonal badges, Pay/Decline/Retry/Cancel/Details, insufficient caption), createAppBubble (invite/invited/missing/in-session/ended, lifecycle title), createCallBubble (normal/missed, direction param), createFileBubble (offer/progress/complete/failed — progress+failed = #66 gap fills; role=progressbar; setFileProgress updates meta + auto-completes at 100), createUnreadDivider (full-width, frontend-only). Audit fixes: received-payments were RIGHT-aligned (direction map) · dark progress track invisible (= card surface; scoped neutral-04) · progress transfers silent to AT · divider hairline -02 · lifecycle titles · direction params for call/app (bridge knows localSender) · ' · ' separators · info flex-1 · pressed feedback. SHELL NOTE in JSDoc: bridge status text is C#-localized — derive enum from statusIcon/:/:: prefixes | #62 pipeline round 3; Figma scans 11305:6344 et al. | ✅ |

| 73 | 2026-07-03 | Demo restructure (audit r2 C17): chat.html = TWO phones — true 1:1 (no sender labels/avatars) + group chat (#63 calibration surface) — plus a STATES MATRIX section on the real canvas rendering every typed-card variant side by side (payment 17 cells incl. #77 rule demos, app 5, file 5, call 3, reactions, reply/edited, typing) | One demo misled reviewers (1:1 topbar over group-chat bubbles); matrix = review surface for states no conversation naturally shows | ✅ |
| 74 | 2026-07-03 | **BATCH 3 BUILT**: c-reactions (`addReactions` #44-style, replace-on-repeat, aria-pressed pills, tip = c-badge) · c-typing (received-bubble language, token-driven wave, explicit `animation:none` reduced-motion guard) · c-scroll-latest (component-managed visibility @200px threshold, shell-managed count via `setScrollLatestCount`, #48 badge recipe) · c-msgmenu (SHEET-based §5b: quick-react row + capability-gated action list, long-press 500ms/10px-cancel + right-click; keyboard path lands with the chat shell). r2 backlog folded in: C8 card gutter param · A17 call-meta join · C15 insufficient aria-describedby · B4 action-button overflow · B6 attach hit-area. ⚠ trash glyph missing from icon export (x stands in — Damir export queued). **#46 audit loop DEFERRED for batch 3/3b (Damir, token economy) — run next session** | #62 pipeline round 4 | ✅ build · 🟡 audit pending |
| 75 | 2026-07-03 | Message context menu presentation = c-sheet reuse (overlay stack/scrim/Esc/focus for free) INSTEAD of §5b's anchored panel + promoted bubble (scrim + bubble at z-50). Anchored-panel stays the design-doc spec; revisit after Damir feels the sheet version | CLAUDE.md batch-3 note said sheet-based; overlay infra is proven (#56/#57) | 🟡 pending Damir feel |
| 76 | 2026-07-03 | Chat pattern LOCKED (Damir via demo ink switcher): data-URI mask (CORS-clean on file://, `scripts/generate-chat-pattern.mjs` → generated `src/styles/chat-pattern.css`; paint = generated file, layer = message-bubble.css, fail-soft to gradient-only) inked "Primary soft" — **light primary-200 / dark primary-800, opacity 0.5 both modes**. Light `--gradient-chat` v2: `linear-gradient(195deg, rgba(170,220,245,.5), rgba(24,146,206,.5))` over surface-screen (raw rgba sanctioned like --gradient-hero-overlay; soften dial = alphas). Figma mirror: --chat-pattern-ink + gradient colors as token candidates; recheck canvas-tuned inks (scroll thumb, datesep) at UX review | "White isn't premium"; ink+opacity per theme = two dials | ✅ |
| 77 | 2026-07-03 | IXI amount DISPLAY rule (Damir): chain has 8 decimals; UI shows **at most 2 — string-TRUNCATED, never rounded** (display must not overstate; ⚠ BE confirm rounding stance) — trailing zeros trimmed, round numbers bare (25.00→25, 25.50→25.5, 0.12345678→0.12). `formatIxiAmount` in typed-bubbles.js = reference impl, applied in the payment card as a guard; **C# composes the real strings — mirror the rule there (§9)** | Consistent premium numerals; bridge pre-formats per #55 | ✅ frontend · 🟡 BE mirror |
| 78 | 2026-07-03 | Processing payment card: spinner + check double-icon fixed — while `processing` the Pay button drops the check icon and reads **"Processing"** (spinner only, loading=disabled per r2 guard) | Damir screenshot: two icons while loading | ✅ |
| 79 | 2026-07-03 | **REPLIES + EDITS BUILT, §8-GATED (#25)**: bubble `reply` quote (identity-hue bar via shared hashHue; sent side = wash + on-bubble inks; tap = `onReplyClick` scroll-to-original, shell duty) · `edited` marker in meta (italic, precedes time) · composer context strip `setComposerContext/getComposerContext` (#44 free fns; edit prefills via synthetic input — no spurious typing; Esc cancels ctx first; flex-wrap keeps composer DOM stable) · menu Reply/Edit items behind capability getters (edit = own messages only). Demo "Reply/Edit (§8)" toggle simulates the handshake; NOTHING renders without it | Menu constrained by bridge reality (#25); build now so BE approval is drop-in | ✅ build · 🟡 pending BE (§8) |
| 80 | 2026-07-03 | r2 backlog C9 RESOLVED: read vs delivered stay COLOR-ONLY (grey vs AA-checked green double-check) — WhatsApp-proven convention, lowest noise; color-blind limitation logged as ACCEPTED RISK | Damir interview at chat finalization | ✅ accepted |

| 81 | 2026-07-03 | **Media bubbles (GIF/image) BUILT P2P-honest** (`c-mbubble`): default = **TAP-TO-LOAD** — a remote GIF/CDN fetch reveals the reader's IP (no proxy server exists), so the tile renders from a **sender-embedded blurred preview** until the user opts in; `autoload` = future "auto-load media" setting. Anatomy: aspect-ratio tile (sender dims, 320 cap), preview blur layer, scrim affordance (play/download/spinner/retry), GIF badge, scrim time pill, `setMediaSrc` for the file-transfer path. BE's image standard ("compress into a short message") = BlurHash/ThumbHash family — component takes the DECODED thumb as `preview`; decoder + exact standard = **BE eval (§9)** | Damir: gifs via Giphy keyboard/link + image previews; P2P/no-servers constraint drives tap-to-load | ✅ component · 🟡 BE (standard + autoload setting) |
| 82 | 2026-07-03 | **Links in messages**: URLs render as **BUTTONS** (never `<a>` — default-nav/middle-click would bypass the warning), XSS-safe DOM linkify (trailing punctuation stays text), every activation routes through the existing external-link confirm (c-modal, Cancel autofocused). **Link previews are §8-gated**: P2P can't unfurl server-side → SENDER composes `{url,title,domain,image}` into the message (Signal-style), bridge must carry the payload — §8 proposal | Damir: show URLs properly + keep the click warning + standard-style preview | ✅ linkify+confirm · 🟡 §8 preview payload |
| 83 | 2026-07-03 | **Heavy reactions**: pills cap at 3 TYPES + a "+N" pill (N = folded types); "+N" opens `openReactionsSheet` — c-sheet listing every type with count + sender names (bridge aggregation must carry senders — verify at §9 sync) | Damir: "load of reactions" needs folding + a way to inspect all | ✅ pending Damir look |
| 84 | 2026-07-03 | Typing indicator HUGS the composer: `c-bubble-row--typing` margin-top spacing-8, list bottom padding **spacing-4** (was 16) — pill sits 4px above the input bar | Damir: "closer to the input bar, spacing token 4 so we can check" | ✅ pending Damir look |

| 85 | 2026-07-03 | **#46 AUDIT LOOP r3+r4 RUN** (2 adversarial agents + 1 reviewer): 7 MAJORs fixed — setMediaSrc dead-tile (img now in DOM from creation + mediaCtl WeakMap) · Android long-press double-open (contextmenu/timer mutual guard; r4 follow-up: pointerdown resets suppression so consecutive right-clicks work) · media excluded from reactions (new `.c-mbubble-anchor` wrapper carries the bubble width rule — r4: width on the ANCHOR, tile 100%, else 75% went cyclic) · sent-wash meta inks 3.6:1 → white · dark on-scrim inks inverted → new MODE-LESS `--text-on-scrim`/`--icon-on-scrim` (Figma mirror) · canvas scroll thumb → text-02 (v2 gradient) · reply-label L quote-scoped 26% (`--reply-label-l`). +11 MINORs (edit-cancel draft restore, edit gated to text rows, demo react-merge, scroll-latest visibility-hidden, reactmenu list roles + senders wrap, linkify unbalanced-paren, build-script let/var collision detection, matrix overlay hosts, spinner reduced-motion parity, typing specificity, demo label ink). Backlogged: typing role=status re-announce on re-append (shell announces from static region) · dark error-badge recipe 3.85:1 (shared with #48 — fix both at Figma mirror) · datesep/pattern washout vs v2 gradient TOP (Damir eyeball at UX review) · dust amounts <0.01 render "0" (BE floor question) | Damir: audit now, before shell assembly | ✅ loop CLEAN after r4 fixes |
| 86 | 2026-07-03 | **CHAT V1 FINALIZATION SCOPE (Damir interview)**: IN — bot chat + channels (setChatMode cost line + selector) · load-more = **LAZY** (auto-fires ixian:loadmore at a scroll-top sentinel, spinner row, scroll-anchor preserved — no button) · **secure-chat first-message notice** ("PQC-secured chat" + learn-more link through the external-link warning; text from lang file) · full-screen media viewer (tile onOpen target) · in-chat call UX: start call mid-chat + INCOMING-CALL overlay (accept/decline/ignore) · message updates = **IN-PLACE free fns** (#44): setMessageStatus, setPaymentStatus (re-create card, replace in place), removeMessage w/ #63 grouping repair. Contact-request pane: Damir leans list-only accept — RECOMMEND minimal accept/decline bar anyway (bridge calls showContactRequest on the conversation; composer must be disabled for non-contacts regardless) — 🟡 Damir decides. OUT (post-v1): stickers (v1.1), reaction inspect per-emoji tabs | Damir: "finalize the chat screen" | 🟡 building |
| 87 | 2026-07-03 | **Damir finalization notes logged**: ① composer ⊕ menu contents (bridge-real today): App invite / Pay / Send payment request / Send file — attach-sheet component at shell assembly, augment later ② composer ⊕ optical alignment fixed (2px block-end offset) ③ **self-destructing messages** — needs BE (message TTL + deletion signal): §8/§9 eval ④ **voice-note lifecycle design** (recording UI in composer, cancel mid-record, sending/sent/received playback bubble w/ waveform+duration) — joins the voice batch (#64, §8) ⑤ P2P transfer UX: "keep Spixi open" muted hint on progress file-bubbles (DONE, progress-only) + BE proposal: notify sender when receiver starts a download + toast to stay online (§8 candidate) ⑥ emoji-only messages render BIG (40px) with meta below (DONE, 1–3 emoji) ⑦ **declined call = YES, third call-card state** (AI concurs): answered (duration) / missed (rang out — "Tap to call back") / declined (actively rejected, no call-back nudge) — needs bridge to distinguish declined vs missed (BE question) + phone-x glyph ⑧ stickers parked v1.1 | Damir messages 2026-07-03 (round 4) | 🟡 rolling |

| 88 | 2026-07-03 | **IN-PLACE UPDATERS BUILT (#86 item 1)**: `setMessageStatus(row, status)` — swaps just the status glyph (sending/sent/delivered/read; 'failed' warns → shell re-creates, the failed layout is structural) · `setPaymentStatus(row, patch)` — payment rows remember creation opts (WeakMap), the card re-renders from opts+patch and swaps in place, RETURNS the new row (callers adopt it); re-render releases the audit-r2 one-shot latch by construction · `removeMessage(row)` — removes + repairs #63 grouping: head passes the sender label (with its hue) down, tail passes the avatar up, positions re-derive. Demos: chat.html Pay→processing→completed loop, delivered→read ticks on live sends, menu Delete = live group repair | #86: no flicker, scroll stays put | ✅ pending Damir look |
| 89 | 2026-07-03 | **DESKTOP SPLIT-VIEW DEMO built** (`src/demo/desktop.html`, #19 pitch evidence): left rail = c-bottomnav VERTICAL (CSS-only override — proves the parked #67 idea) · chat-list pane (search + filter chips + rows, aria-current selection) · DRAGGABLE divider (280–520px clamp, double-click resets — the #19 "resizable dividers in pure frontend" claim, live) · conversation pane with empty state; Han 1:1 + Falcon Crew group wired end-to-end (cards, reactions, typing, scroll-to-latest, right-click menus, live composer + #86 ticks). Everything is the SAME components as mobile — zero new component code, demo wiring only | Damir ask; makes the #19 single-WebView pitch tangible for BE | 🟡 pending Damir look |

| 90 | 2026-07-03 | **LAZY HISTORY BUILT (#86 item 3)**: `attachLazyHistory(box, {onLoadMore})` — nearing scroll-top auto-fires the shell hook (→ ixian:loadmore), spinner row while loading, **scroll anchor preserved** (scrollTop += height delta so the visible message never jumps), re-entrancy guarded, `resolve(false)` = history exhausted (detaches), failed pages stay retryable, `setDone()` for chat switches. No button — Damir: "auto pressed load more + keeps scroll position" | #86 interview | ✅ pending Damir look |
| 91 | 2026-07-03 | **SECURE-CHAT NOTICE BUILT (#86)**: `createSystemNotice` (c-sysnotice, role=note) — centered action-tonal card, square-asterisk glyph, lang-file copy + "Learn more" BUTTON-link through the external-link warning. Demo shows the premium ordering: the notice appears at the TOP of history only when pagination exhausts — it marks the true conversation start | Damir: "this is a super secure chat pqc + link" | ✅ pending Damir look |
| 92 | 2026-07-03 | Reply quotes to NON-TEXT originals carry an identifier (Damir + AI concur): shell-composed `thumb` (32px, data-URI — P2P-safe, same channel as media previews) for gif/image, else a KIND GLYPH chip (photo/file-isr/wallet/phone/microphone) + kind label fallback when there's no excerpt text. `reply.kind: gif|image|file|payment|call|voice` | Quotes to media/payments were text-only — unidentifiable | ✅ pending Damir look |
| 93 | 2026-07-03 | Payment card WORKING states pattern: Decline/Cancel get `setLoading` on the pressed button, then the #88 in-place flip (declined/canceled) — matches the Pay→processing→completed loop; no instant snaps on the money path. Desktop demo empty-panels bug fixed: `icon` was destructured from window.Spixi but ships on window.SpixiIcons — script died on first call | Damir review round 5 | ✅ |

| 94 | 2026-07-03 | Round 6: ① secure-chat notice RESTYLED — success-inverse card + hairline + elevation, solid success MEDALLION (36px, square-asterisk), title line + body + "How it works" link (success ink); copy locked: **"Encrypted. Peer-to-peer. Yours alone."** + "Messages go straight from device to device, sealed with post-quantum encryption. No server carries or stores them — every message exists only with you two, nowhere else." (lang-file keys at shell assembly) ② demo reply-to-non-text FIXED: wireMenu derives quote kind/thumb/excerpt from the row (media→thumb, file→name, cards→title·amount; shell composes from its model instead) ③ reply kinds gained `app` (rocket glyph) | Damir round 6 | ✅ pending Damir look |

| 95 | 2026-07-03 | **ROADMAP LOCKED (Damir + AI)**: finish the 4 true conversation-surface gaps (attach sheet · bot chat/channels · media viewer · call overlays + declined card) → final #46 audit + Damir UX review + big Figma mirror → **CHAT COMPONENTS V1 FROZEN** → then SHELL ASSEMBLY in order: chats shell (where #67 search-collapse, mute, pin, chat-info/group-settings panes land properly) → wallet shell + payment flows (#26 review-step) → contacts/settings/scan (code-first utility shells). ACTION ITEMS logged, not built now: chat-info + group-settings panes · mute plumbing · self-destruct (BE) · #67 list upgrades · wallet polish. Principle: shell-level features get built in their shell home, not as demo bolt-ons — no double-touching | Damir: "flag and move on vs polish everything" | ✅ |

| 96 | 2026-07-03 | **ATTACH SHEET BUILT (#87①, interview)**: TILE GRID on c-sheet (AI call, Damir delegated: creation verbs scan better as tiles; 6 entries; still the overlay stack — downgrade to rows is CSS-only if it doesn't feel right). Bridge-real: Send file · Send payment · Request payment · App invite; **Photo + GIF designed-in, feature-flagged** (`media:false`, #81/#64 precedent). 48px action-tonal medallions, 3-col grid. `openAttachSheet({host, media, onAction})` | Damir interview round 7 | ✅ pending Damir look |
| 97 | 2026-07-03 | **BOT SURFACE BUILT (#86, interview)** — principle: bot chat = BIG GROUP CHAT + channels, experience mirrors groups (sender labels/avatars/gutter identical): ① channel switching = **topbar identity TAP → channel sheet** (`createTopbar onIdentity` — identity becomes a button w/ hover wash; same affordance opens chat-info later; zero new chrome) · c-channels sheet rows: # glyph, name, C#-composed status line, unread indicator, aria-current+check on selected ② per-message cost = **standing hint line above the composer field** (`setComposerCost`, wallet glyph + "Each message costs 0.1 IXI"; money facts don't disappear while typing — placeholder option rejected; sits ABOVE any reply/edit strip) ③ desc/admin → chat-info pane (shell). Bridge: setChatMode / addChannelToSelector / setSelectedChannel / setChannelSelectorStatus now all have code homes; channel switch re-render = shell duty. Demo: desktop "Ixian News" | Damir interview round 7 | ✅ pending Damir look |
| 98 | 2026-07-03 | **Wallet "Missing a transaction?" (Damir design, ANALYZED — logged for wallet-shell polish)**: chip-style info trigger (info glyph + label, action-tonal wash) at the END of the filter-chip row — right call: chain-read lag is a STANDING property, so a persistent quiet affordance beats a banner; reuses the c-chip vocabulary almost verbatim. Tap → c-sheet explainer ("Spixi reads history directly from the Ixian chain — recent txs can take a moment; very old ones may not list") + optional refresh + **"View on IxiScope" link for THIS wallet address** (Damir addendum — routes through the external-link warning; URL pattern = BE/lang config). Watch-outs for build: ① at ≤360px the chips + pill wrap — collapse the trigger to icon-only ⓘ ② two tonal families on one row compete — keep chips neutral-outline when unselected so the info pill owns the wash ③ needs a string key + BE confirm on refresh semantics | Damir screenshot round 7 | 🟡 build at wallet shell |

| 99 | 2026-07-03 | **Group member identity (proposal, from Damir's nickname-tap idea)**: instead of tapping a nickname to TOGGLE it into a shortened address (tiny target inside a long-pressable bubble, poor discoverability, per-message vs global ambiguity), tap on the sender label OR its avatar opens a **MEMBER SHEET**: avatar · nickname · FULL address (copyable — better verification than a truncated swap; nicknames are user-set and spoofable, the address is the truth) · "Send contact request" button. Capability-gated by group type: **blind groups** = labels non-interactive or sheet says "identity hidden in this group" (no address, no request); **normal/bot groups** = full sheet. ⚠ BE question: does the bridge expose the blind-group flag + per-member addresses to the WebView? Build with the last v1 batch (viewer/calls) if Damir approves | Damir round 8: verify-who-is-who need is real — solve it with a richer affordance | ✅ APPROVED + BUILT same day ("make it sexy"): c-member sheet — avatar-48, name heading-xs, FULL address in a tabular input-surface chip + copy button (check-morph confirm), full-width "Send contact request" (heart-handshake, latched), blind-group state = eye-off medallion + "Identities are hidden…"; `createMessageBubble onSenderClick` makes label+avatar buttons (hover underline; blind groups just omit the callback). Demos: both group surfaces + bot. BE question stands (blind flag + member addresses) |

| 100 | 2026-07-03 | **Duplicate-nickname collision flag (Damir, logged for LATER — bot-chat scale mostly)**: when 2+ members share a display name, surface it — proposal: warning-tonal hint in the MEMBER SHEET ("2 members use this name — verify the address") + optional subtle marker on the label; detection is shell-side (name→address map per chat). Depends on #99 BE answer (member addresses available). Not in v1 freeze | Spoof pairs ("Alex" vs "Alex") are the real attack; sheet is where verification already lives | 🟡 post-v1 |

| 101 | 2026-07-03 | **LAST V1 GAPS BUILT (#86 complete — chat surface feature-complete pending Damir review, audit HELD until after his review per instruction)**: ① `openMediaViewer` (c-mviewer) — full-screen on the overlay stack (Esc/scrim/back dismiss, focus contained), fit-to-screen v1 (zoom post-v1), top bar = close · caption · Save (shell `onSave`, P2P local file op); on-scrim inks; c-mbubble onOpen wired ② `showIncomingCall` (c-callin, z-60 alertdialog) — identity + ringing pulse (reduced-motion static) + three labeled circles: Decline (error) / Ignore (quiet wash) / Accept (success); ONE outcome per ring (latched); **Esc/scrim = Ignore, never auto-decline**; `hideIncomingCall` = caller hung up; Accept chains into c-callbar (shell) ③ start-call mid-chat = topbar phone → existing c-callbar (demoed both surfaces) ④ `createCallBubble declined` state — "Call declined", phone-x glyph, NEUTRAL ink (vs missed's error), direction meta only, NO call-back nudge (#87⑦); BE distinguish-question stands ⑤ demo: "Incoming call" toolbar button — Accept→callbar, Decline→declined card drops into the log | #95 roadmap; Damir: "finalize these items, review before audit" | ✅ pending Damir REVIEW |

| 105 | 2026-07-03 | Member-sheet QR (Damir question) — RECOMMENDATION: keep the QR OFF the default sheet. A QR's job is in-person/cross-device address exchange — that's the FULL CONTACT PAGE (and your own account/receive screen, where --surface-qr tokens already exist); the member sheet's job is quick verify-and-act, and a QR block would push the actions below the fold on small phones. Compromise on offer if wanted: a small `qrcode` icon-button NEXT TO the copy button that expands a QR overlay on demand — zero default height cost. Also: media-viewer mouse-drag dismiss FIXED (native image ghost-drag hijacked the pointer stream — draggable=false + dragstart preventDefault + user-drag CSS) | Damir round 11 | 🟡 Damir picks (off-sheet vs icon-button) |
| 104 | 2026-07-03 | Member sheet round 10 (Damir): ① payment actions IN the sheet — **contacts only** (guard rail: money affordances pointed at unverified group strangers = scam bait; non-contacts keep request-first flow, can still pay via wallet from the copied address): Pay + Request outline pair under the Message button ② identity block (avatar+name) for CONTACTS is a BUTTON with a chevron affordance → `onViewContact` = full contact page (shell nav; page lands with the contacts shell). Sheet hierarchy: identity → address+copy → relation badge → Message → Pay|Request | Damir: payments in sheet? tap-through to contact details? | ✅ pending Damir look |
| 103 | 2026-07-03 | Media viewer SWIPE-TO-DISMISS (Damir): vertical drag either direction — image rides the finger, viewer fades progressively; release past 80px dismisses, under springs back (decelerate token); `touch-action:none` on the stage only (bar buttons untouched). Desktop drag works too (pointer events) alongside Esc/scrim/✕ | Intuitive close without hunting the ✕ — photo-viewer convention | ✅ pending Damir look |
| 102 | 2026-07-03 | Damir sanity round: ① member sheet is RELATION-AWARE — `relation: none|pending|contact` (shell knows): none → "Send contact request" · pending → info badge "Contact request sent" (no button) · contact → success badge "In your contacts" + **"Message"** button (onMessage → shell opens the 1:1); demo shows all three (Alex/Han/Lando). ⚠ BE: bridge must expose per-member relation state ② incoming-call composition = **halves**: identity centers in the top half, action circles center in the bottom half (was edge-pinned padding) | Damir round 9 — request-to-existing-contact was uncovered | ✅ pending Damir look |

| 106 | 2026-07-03 | **FREEZE AUDIT RUN + FIXED (#46/#95 gate, 2 adversarial agents)**: 4 MAJORs — ① `hideIncomingCall` fired the shell's onIgnore on REMOTE hang-up (data-silent marker; user-outcome vs bridge-event now distinct) ② desktop `.dt-frame` had no positioning context — every overlay pinned to the PAGE viewport, not the frame (position:relative) ③ channel rows: role=listitem on `<button>` destroyed button semantics for SRs (roles dropped) ④ incoming-call autofocus landed on DECLINE — reflexive Enter killed the call (data-autofocus → Accept, APG safe-action). MINORs fixed: member addr-chip logical padding + chevron RTL flip · avatar-btn 44px hit-area · on-scrim focus-ring override (light primary-600 was 1.7:1 on scrim) · composer ctx-replace now restores the pre-edit draft · desktop demo react-merge (wipe bug had crept back) · copy-confirm announced to SRs · caps-slot gap · viewer doc claim corrected (scrim-tap unreachable — swipe/✕/Esc close). ACCEPTED/backlog: call-circle fills dim on light scrim (icons+labels carry, waive w/ comment) · emoji-only regex misses flags/keycaps · post-flip payment rows need shell re-wire of menus (contract note) · lazy spinner track faint (arc carries) · Mac-rebuild deltas (MAX_LINES rename, docLocale export). Both syntax sweeps PASS on verbatim copies | Damir: "sweep before committing" | ✅ CLEAN after fixes |
| 107 | 2026-07-03 | Sender-side transfers (Damir): the keep-open hint + progress bar apply to the SENDER too — component was already direction-agnostic (state-driven), so this is a SHELL/BE contract: outgoing transfers render `state:'progress'` with "Uploading · x of y" meta; **BE confirm: sender receives progress events** (updateFile covers both directions?). Matrix cell added ("sent · uploading") | P2P: both peers must stay online | ✅ frontend · 🟡 BE confirm |
| 108 | 2026-07-03 | Mention excerpt marker (Damir): switch from the count-style @-in-circle indicator to a **plain `at` glyph** (action ink, no surface) beside the excerpt — DISTINCT SHAPE from numeric unread circles (two similar circles = scan confusion; @ + count can coexist on one row). Icon exported (registry 77, incl. shield-lock → secure-notice medallion upgraded). ~~Implement at the chats shell~~ BUILT SAME DAY (Damir: app-frame showed nothing — the old indicator rendered a literal '@' character, never the icon): `createIndicator` mention = `at` glyph 14px, action ink, transparent, no min-width; `createIndicators` now emits @ AND count as separate indicators (distinct shapes coexist); the mention EXCERPT keeps its @token text highlight (a glyph there would double up). app-frame + desktop lists pick it up via the shared component | Damir: "a bit more distinct — think about it"; agreed | ✅ pending Damir look |

| 109 | 2026-07-03 | **Contact-request Accept = staged handshake (Damir, logged for the CHATS SHELL)**: tapping Accept must NOT drop either party straight into the chat — a message sent before the key exchange completes would fail/arise issues. Design: Accept button → latched "Accepting…" loading state; list-row excerpt flips to **"Establishing a quantum-secure handshake…"** (typing-excerpt styling, on-brand with the #91 notice) for BOTH parties; entry into the conversation unblocks only on the bridge's handshake-complete signal. ⚠ BE: needs an explicit handshake-done event (or Accept-ack semantics) — add to §9 questions | Damir round 12 — race between accept and first message | 🟡 build at chats shell |

| 110 | 2026-07-03 | **Figma mirror part 3 (token deltas) + delivered indicator (Damir interview).** MIRRORED code→Figma in the `tokens` collection (aliased to `keys`, scopes + `var(--…)` WEB syntax): CREATED `colors/icon/region/bubble-read` (L success/300, D success/800), `…/bubble-failed` (L error/200, D error/700), `…/bubble-delivered` (L success/400, D success/700), `colors/text/region/on-scrim` + `colors/icon/region/on-scrim` (mode-less neutral/10), `colors/surface/region/chat-pattern-ink` (L primary/200, D primary/800). FIXED light-mode bindings: `bubble-sent-meta` primary/200→**primary/100**, `bubble-received` neutral/100→**neutral/10** (white). Per #10, `--chat-pattern-opacity` + `--gradient-chat` stay code-only (gradient stops flagged as token candidates for Damir). **Delivered-glyph amendment (amends #80):** delivered gets its own muted-green tone instead of the neutral timestamp tint — dedicated `--icon-bubble-delivered`, contrast ~50% of read; **L=success/400 (3.4:1, ≥3:1 a11y-safe), D=success/700 (3.1:1, ~50% target)** — Damir chose "split" (a11y-safe light, truest dark). Read unchanged (success/300·800, full-strength). Sent (single check) stays neutral. Scope: bubble-only override — chat-LIST delivered stays neutral (list read=accent-blue, its own context). Code adopted: tokens.css (both modes) · message-bubble.css sent override · chatlist-item.css list neutral rule · chatlist-item.js STATUS `delivered.tone='delivered'`. ⚠ **bundle + tokens.css regen pending** (Damir runs build scripts). | Damir: delivered should read as ~50% muted vs read for clear state contrast | ✅ Figma written · 🟡 pending Damir Figma verify + bundle rebuild |

| 111 | 2026-07-03 | **Typing indicator must stay pinned above the composer on scroll (Damir, logged — not built now).** Today the typing pill lives in the message-list flow (hugs composer at spacing-4, #84) and scrolls away when the user scrolls up through history — but the peer is still typing, so the signal shouldn't disappear. Fix: pin the typing row to the bottom of the scroll viewport (just above the composer, `--z` above the list) independent of scrollTop, driven by bridge typing events; releases when typing stops. Build at chat shell / typing-indicator component; keep the #84 spacing. | Damir: "should always be visible in the same area above input even if user scrolls up" | 🟡 backlog (chat shell) |

| 112 | 2026-07-03 | **Figma component mirror batch (part 3, this session)** — built reference components on the Chat & Composite page in a new section **"Chat refs — batch 4"**, all token-bound including **spacing variables** (Damir ask: 463 padding/gap bindings to `spacing/*`). New refs: reactions overlap set (default/own/+N, #65) · reply-quote set (text/media, #92) + edited marker · typing set (direct 3-dot / group avatar+dots) · scroll-latest (error count badge) · system-notice (#94 copy) · message-menu (quick-react row + Reply/Copy/Edit/Delete-destructive) · media-tile (tap-to-load + GIF badge + time pill, #81) + media-viewer (#101①) · attach-sheet tile grid (#96) · member-sheet relation-aware (#99/#102/#104) · channel-sheet (#97) · incoming-call overlay (#101②/#102②). In-place set edits: call-card `status=declined` (#87⑦), indicator `mention` @ (#108). **FLAGS for Damir:** ① `message-bubble` set has a DUPLICATE variant-name conflict (`Direction=Outgoing, Tail=false, type=default` ×2) → throws on variantGroupProperties, needs reconcile · ② `elevation/1` has no Figma effect style (only /2,/3) — used inline shadow, create the style · ③ emoji glyphs (reactions/msgmenu) are runtime placeholders · ④ reply-quote hue bar uses a sample hue (runtime = per-sender hsl). Build-split #13: structure+bindings+polish attempt done, Damir polishes proportions. | Damir: Figma mirror batch + spacing via variables | ✅ built · 🟡 pending Damir polish/review |

| 113 | 2026-07-03 | **Chats shell — step 2: collapsing header (`c-chats-header`).** Search field + filter chips (All/Unread/Favorites-off§8/Groups/Requests) as ONE unit. `attachChatsCollapse`: scroll-linked, finger-tracking **height+opacity** retract (no transform — height collapse frees list space directly), reveals near top / on upward scroll, reduced-motion → snap. **GRANTS** the runtime-layout inline-style exception (extends #16/#29) for the per-frame `height`/`opacity` writes (self-clearing on detach). a11y: fully-collapsed header = `inert` + `aria-hidden`, PLUS a fallback (`data-collapsed`→`pointer-events:none` + `tabindex="-1"` on focusables) because `inert` needs Chromium ≥102 / Safari ≥15.5 (#4 conservative baseline); a11y attrs toggle on collapsed⇄revealed transitions only. Verified: 40 jsdom assertions (wiring + collapse math + a11y fallback + resize re-pin + lazy pre-mount measure). ⚠ **Needs on-device tuning (spec §3):** per-frame height reflow — documented triggered-transition fallback if janky on the WebView baseline. Audit: 2 agents (demo CLEAN; header FIX-NEEDED → 1 MAJOR inert-fallback + MINORs fixed → re-verified) | Damir away — build the spec, agents cross-check to CLEAN | ✅ built + audited · 🟡 on-device collapse tuning pending |

| 114 | 2026-07-03 | **Chats shell steps 3–4 + collapse redesign + full batch audit (Damir away).** Step 3: `c-contact-request` (real component, avatar-48 + name/addr + "Wants to connect" + Decline→c-modal + Accept→onAccept + timestamp + requester-naming aria-labels) replacing the stub. Step 4: chat-row context menu (`chats-row-menu.js`: long-press/right-click → c-sheet Pin/Mute/Mark-read/Chat-info(stub)/Delete-confirm, reusing c-msgmenu infra) + `applyChatRowAction` free-fn (mutate model → re-render → onModelChange). **Collapse REDESIGNED (Damir review):** the finger-tracking scroll-linked build felt "too fast / not smooth" and revealed on partial scroll-up — replaced with a smooth **binary triggered CSS transition** (`max-height`+`opacity`, `--duration-300`/`--easing-standard`): collapse on downward scroll, **reveal ONLY at the absolute top** (scrollTop ≤ 1); guards an unmeasured (offsetHeight 0) header. Spec §3/§10 updated. **BUNDLE now hand-maintained** (`spixi.iife.js`): the 4 chats modules appended + export map extended (this is why demos render with no terminal — the missed workflow that broke the demo; `node scripts/build-demo-bundle.mjs` regenerates cleanly later). **Full #46 audit: 3 adversarial agents (header/row-menu · shell/contact-request · integration/bundle-parity) → 1 MAJOR (unmeasured-header collapse) + null-element hardening + opacity-release, all fixed in source AND bundle → verification agent CLEAN, bundle↔source parity confirmed.** Verified: 34 model + 13 collapse + 13 row-menu + 14 contact-request + guard/null jsdom assertions. | Damir: finish the spec, agents cross-check to super-clean | ✅ built + audited CLEAN · 🟡 on-device collapse feel + Damir review |

| 115 | 2026-07-04 | **Chats shell step 5 — swipe Pin/Mute accelerator + CAPABILITY-FLAG convention (Damir).** Swipe = Damir option 1 (iOS-Mail reveal-button + full-swipe): `wrapChatRowSwipe` — pointer-only, leading=Pin / trailing=Mute (RTL-mirrored physical sides), drag reveals a labelled action button (tap to fire) or full-swipe past COMMIT to fire, springs back otherwise; single-open, tap-to-close, `touch-action:pan-y` so vertical scroll still works; sheet is the a11y path (action buttons `tabindex=-1`+`aria-hidden`). **NEW CONVENTION — `opts.capabilities`:** an object of booleans gating BE-dependent features so they PARK (affordance simply not rendered) until BE ships, then reveal when the flag flips (Damir: many features are build-FE-now / reveal-when-BE — pin/mute persistence #67/§8, favorites, voice #64, media #81, self-destruct #87③, handshake #109). Pin/Mute swipe + menu items gated on `capabilities.pin`/`.mute`; favorites chip on `.favorites`. `applyChatRowAction` fires `onPersist(action, chat)` = the bridge intent (mock no-op; real `ixian:` command when BE ships). Demo has a **"Park pin/mute" toggle** proving the parking. **Full #46 audit (2 agents): 3 MAJOR fixed** — ① single-open leaked a stale closure across re-render (→ `closeChatRowSwipe()` called before list clear) ② `swiped` could stick and swallow a fresh tap (→ reset in pointerdown) ③ long-press menu could fire mid-swipe in the 8–10px lock band + pointer-capture retarget (→ dispatch non-bubbling `pointercancel` to the row on lock) — + a11y/tap-close MINORs, all fixed in source AND bundle → 2 verification agents CLEAN + source↔bundle parity. Verified: 17 + 11 swipe + gating + demo jsdom assertions. | Damir: build option 1 + parkable BE-gated features | ✅ built + audited CLEAN · 🟡 on-device swipe feel (thresholds/spring) pending Damir |

| 116 | 2026-07-04 | **Chats shell step 6 — #109 staged accept handshake (final build step).** Accepting a contact request must NOT open the chat immediately (key-exchange race). Flow: Accept → `setRequestAccepting(row)` latches the button to "Accepting…" (loading+disabled, aria-label synced) and disables Decline → `acceptContactRequest(listEl,state,req,opts)` removes the request and prepends a **handshaking chat** (excerpt `{type:'typing'}` "Establishing a quantum-secure handshake…", reusing the action-colored typing style + a gentle `[data-handshaking]` pulse, reduced-motion off) → the row is `aria-busy`, **un-openable** (tap routes to `onHandshakeBlocked`, never `onOpen`), **un-swipeable**, and shows only a **"Cancel handshake"** menu item → only the bridge signal `completeHandshake(...)` clears the flag, resets the excerpt, and bumps the timestamp so the just-secured contact surfaces on top. Timers live in the DEMO (mock bridge: 650ms ack → establishing → 2600ms complete), NOT the shell. **Recovery**: `failHandshake(...)` (symmetric to complete) removes a stranded chat — driven by the bridge failure/timeout signal (§9) OR the row's Cancel-handshake action — so a never-completing handshake is **never an un-removable trap** (the MAJOR the audit caught). Double-accept guarded (model `indexOf` + DOM disable); a late complete-after-cancel cannot resurrect a removed chat (triple guards). New free-fns exported: `setRequestAccepting`, `acceptContactRequest`, `completeHandshake`, `failHandshake`. **Full #46 audit (2 parallel agents): parity CLEAN; 1 MAJOR (trap) + 1 NIT (stale aria-label) fixed in source AND bundle → verification agent CLEAN + parity.** Verified: 32 step-6 jsdom assertions + regressions (swipe 11+17, model 34, collapse 18) all green. | #109 (spec §7); Damir "check edge cases, proceed without my input" | ✅ built + audited CLEAN · 🟡 handshake copy + on-device feel pending Damir · BE §9 needs handshake complete/fail signals |

| 117 | 2026-07-04 | **Chats shell step 7 — full-surface adversarial round + hardening (chats shell steps 1–7 COMPLETE).** 2 parallel agents across the whole shell (edge-case matrix + interaction coexistence). **2 MAJOR + 3 MINOR/NIT fixed in source AND bundle → verification CLEAN + parity.** MAJORs: **(F2)** a non-string `excerpt.text` from a bridge feed threw `TypeError` in `createExcerpt` (`.includes`) and aborted the **entire** `renderChatsList` loop (blank list) → coerce `text = String(text ?? '')` at the top, so one bad row can never blank the list; **(F10)** no keyboard path to the row context menu (the swipe accelerator is pointer-only; menu was long-press/right-click only) → added `keydown` Shift+F10 + `ContextMenu` key so keyboard users reach Pin/Mute/Mark-read/Chat-info/Delete. MINOR/NIT: **(F2-int)** an open swipe drawer wasn't closed when a sheet opened on a non-swipe row (handshaking) → `closeChatRowSwipe()` at the top of `openChatRowMenu` (single-open across row types); **(F3)** a negative/corrupt `unread` dragged the nav badge below true → `Math.max(0, …)` clamp; **(F4)** `timestamp:0` (an "unset" sentinel) rendered a 1970 date → `if (timestamp)` treats 0/NaN/undefined as no-time; **(F11)** a permanent `will-change` pinned a compositor layer per row → scoped to `.c-swipe[data-open]`. **Confirmed CLEAN by the agents:** gesture arbitration (swipe's non-bubbling `pointercancel` cancels the long-press), collapse/scroll/swipe coexistence, overlay stacking sheet→modal focus restore (removed-row opener guarded by `isConnected`), handshake lifecycle guards, and the **full reduced-motion surface** (global duration-token zeroing + explicit `@media` escape on the one raw keyframe = the handshaking pulse). Verified: 31 step-7 assertions + regressions (step6 32, swipe 11+17, model 34, collapse 18) = **106 green**. **🟡 logged NITs (backlog, i18n polish):** far-future timestamp shows time-of-day not a date (F5); `formatCount` digits not locale-aware (F6). | Damir "test every edge case, loop until clean, proceed without my input" | ✅ **chats shell steps 1–7 built + audited CLEAN — ready for Damir UX review + commit** |

| 118 | 2026-07-04 | **Requests interleave chronologically + accept-slides-to-top (Damir UX review).** Reverses #114/§5's "requests pinned on top": Damir noticed accepting a top-pinned request made the new chat appear to *drop down* into the list. New model — pending requests sit at their **arrival time** interleaved with chats; the only motion is *upward* when you act (accept → the new chat's timestamp = now → slides to the **top of the unpinned flow**). New pure helper **`orderedTimeline(state)`** → `[{kind:'chat'\|'request', item}]`: **pinned chats stay on absolute top** (pins sacred — a fresh accept lands below them, my judgment call, Damir can flip to above-pins), then unpinned chats + requests merge sorted by recency (stable tie → chat before request). `renderChatsList` refactored to render from the timeline via `renderRequest`/`renderChat` closures (all step-4/5/6 wiring preserved: handshaking gate, capability-gated menu + swipe, empty state on `!timeline.length`). Requests still only in All + Requests filters; Requests chip isolates them newest-first; rows stay visually distinct (tinted) so they read as "needs action" even interleaved. Demo mock updated (one recent request near top, one 5-day-old buried) to show the interleave. **Audited: verification agent CLEAN — no dropped wiring, no sort bug, source↔bundle parity, `continue`→`return` closure conversion safe.** Verified: 12 timeline assertions (pin-top, recency interleave, accept-to-top, filter isolation, query, null-hardening, ties) + regressions (model 34, step6 32, step7 31, swipe 11, collapse 18) all green. | Damir 2026-07-04 review | ✅ built + audited CLEAN · 🟡 pins-stay-above-accept is my default (flip to absolute-top if Damir prefers) |

| 121 | 2026-07-04 | **First on-device Android render SUCCESS + loading-button icon fix.** The chats shell + chat screen ran on the real Android WebView (Pixel 7 emulator) via a throwaway `WebViewTest` MAUI app (blank app hosting a `WebView` that loads the self-contained packaged shells) — chosen because the full Spixi build is blocked on the custom **RocksDB 0.0.42** package (not on nuget.org; Ixian-internal feed needed — Damir to source). Both screens rendered cleanly full-screen: filter chips, swipe (Unpin revealed), interleaved contact request, pin→bridge-intent toast, and the chat screen's payment/file/call/app-invite bubbles + composer. Damir feedback: looks/feels good; emulator softness = downscaling artifact (real phone will be sharp). **Bug found on device + fixed:** the Accept button kept its **check icon** next to the spinner while "Accepting…" — `setLoading` adds a spinner but never hid the content icon. Fix = one design-system CSS rule `.c-button[data-loading] .c-button__icon{display:none}` (button.css), so ANY loading button drops its icon (also tidies the Pay "Processing" state, #78). Pure CSS → re-run `build-test-shells.mjs` to see it. | Damir first device test | ✅ renders on Android · bug fixed · 🟡 RocksDB feed for full app · 🟡 real-phone sharpness/perf pass pending |
| B1 | 2026-07-04 | **BACKLOG (Damir, not now): chat-screen multi-select messages.** Selection mode on the chat screen — select multiple message bubbles and batch-act: delete, copy, + anything sensible (forward/share/star TBD). Design notes for later: enter selection mode from the long-press message menu; bubbles get a checkbox/selected state; the topbar swaps to a contextual action bar (count + actions + close); must coexist with the existing message menu + reactions; BE confirm on batch-delete semantics (own vs others, per-message vs bulk). Logged to the task list. | Damir 2026-07-04 | 🅱️ backlog |
| 120 | 2026-07-04 | **First MAUI integration test planned (Round 1: chats shell + chat screen on device).** Damir wants a real device test now — the first end-to-end since the redesign started. Deliverables: `docs/maui-integration-test-plan.md` (comprehensive) + `scripts/build-test-shells.mjs`. Approach: Round 1 tests **rendering + touch behaviour only, on mock data, NO bridge wiring** (isolates WebView/CSS/JS from bridge fidelity, which becomes Round 2). Shells packaged as **single self-contained HTML** (CSS + `spixi.iife.js` + `icons.iife.js` + fonts base64-inlined) → `Resources/Raw/html/{chats,chat}.test.html`, removing per-platform asset-path resolution as a variable. Non-destructive **`RedesignTestPage`** (inherits `SpixiContentPage`, `loadPage(...)`, shell switcher) reached via a `#if DEBUG` MainPage swap. Grounded in the real WebView mechanics (Explore agent): Android `HtmlWebViewSource` @ `file:///android_asset/html/`, `DomStorageEnabled=false` (our no-storage rule holds); iOS **WKWebView** blocks non-whitelisted http/https + disables webview scroll + hides keyboard accessory (all logged as watch-items); bridge = `ixian:` Navigating intercept + `executeUiCommand` via `EvaluateJavaScriptAsync` (Round 2). **Android testable on PC** (VS 2022 + MAUI workload + emulator; `chrome://inspect` for live DevTools); Windows/WebView2 = fastest pre-check; **iOS needs a Mac** (Xcode + simulator) — flagged for Damir tonight. Plan includes a full rendering+touch checklist, known-issue/fix table (fonts, iOS keyboard-avoidance, inner-scroll momentum, media allow-list), and Round-2 bridge smoke. | Damir: "first proper test… something I can visually see… flag if Mac needed" | ✅ plan + tooling ready · ⏳ Damir to run (Android PC now, iOS on Mac) |
| 123 | 2026-07-04 | **Apps premiumisation — slice 1 built + audited (Damir interview → build).** Ran a full clickable interview (docs/apps-ux-interview.md) covering add/install/details/launch/uninstall/consistency; Damir answered. Built this slice: **(1) Consistency (Damir directive "never build new — add variants to existing"):** added a **`readonly` variant to `c-chip`** (display span, no toggle/hit-area) and reuse it for capability chips; **reuse `c-badge`** (type accent) for the verified badge + a `type info` **Multiplayer** badge — deleted the custom `c-app-cap`. **(2) Install rework (Damir's focus):** confirm is now a **c-modal** (added a reusable **`content` slot** to `c-modal`) showing permission chips **tap-to-explain** (plain-language line) + source; on confirm the **Install button morphs inline** (`setLoading` → `setSuccess` check → re-render installed) — dropped the installing/success modals. Animated check now = CSS/`setSuccess`; **custom animated SVG later (nano-banana)** logged. **(3) Details polish:** description **clamp + Read more**, a **"Runs securely inside Spixi" trust line**, dev install-URL + app-id moved into an **`<details>` Advanced disclosure**, publisher reads `publisher||creator`. **(4) Banner:** Damir-supplied light gradient `linear-gradient(90deg,rgba(102,102,102,0),rgba(2,6,25,.45)),var(--surface-action-default)` + `discover-apps.svg` illustration + inline CTA arrow. **All spacing tokenised** (verified). **Audit: CLEAN** — no undefined tokens, no collisions (APP_CAP_EXPLAIN/capExplain/runInstall unique), source↔bundle logic-parity, publisher-field bug fixed. 🟡 bundle provenance: hand-maintained copy differs from `build-demo-bundle.mjs` output (comments/order) — regenerate with node when convenient. **⏭ QUEUED (agreed, next slice):** combined **Add & Discover** surface (tiles + inline preview), **tap→launch** (⋮=details/uninstall), **hero cover banner** (blurred-icon/gradient indie fallback), **sticky bottom Install**, **Recently-used** row, **hero launch transition**, scaffolds (screenshots/developer/social/related), dark-mode banner gradient. | Damir: "build what we agreed, audit fully" | ✅ slice 1 built + audited CLEAN · ⏭ slice 2 queued |
| 126 | 2026-07-04 | **Apps premiumisation — SLICE 2 (the "supreme premium" pass) built + audited CLEAN.** Damir: "complete the apps supreme premium experience, full audit loop, reuse components + tokens always." Shipped in one pass, all reusing existing components + semantic tokens: **(2A) Details hero cover** — App-Store-style `appHero`: real `app.cover` when present, else the app's deterministic hue gradient (reuses the c-app-icon `--ai-h1/h2` + grad-S/L recipe so the hero always matches the tile) with a blurred copy of the icon + a theme-stable bottom scrim; the 72px app icon overlaps the hero with a surface-ring + elevation. App name back up to heading-sm (topbar is title-less now, so no clash). **(2B) Interaction model** — row/card **tap now LAUNCHES** (records a recent); the ⋮ menu trimmed to **App details + Uninstall** (Launch removed as redundant). **(2C) Recently-used strip** — new `c-apps-recents` component (reuses c-app-icon), horizontal, hidden when empty; model `recordRecent`/`orderedRecents` in apps-shell (most-recent-first, deduped, capped, resolves ids→live apps, drops uninstalled). **(2D) Details depth** — horizontal **screenshots** scroll-snap strip + **"More mini apps"** related tiles (reuse c-app-icon, open that app's details) + a **Developer** meta row; all render only when data is present (graceful, pending BE payloads). **(2E) Combined Add & Discover** — `c-apps-add` rewritten into three method **tiles** (Paste-link reveals an inline URL field, Scan QR, From file) + trust banner + **embedded Discover** (reuses c-apps-discover, parked); Explore banner + FAB both route here; the standalone Discover view is gone. **Bundle: regenerated via the real `build-demo-bundle.mjs`** (node now runs in-sandbox against full sources) — **127 exports, syntax+collision checks pass; this retires the long-standing hand-maintained-bundle drift flag.** **Audit — jsdom apps-demo smoke ALL GREEN (14 assertions: recents seed+launch-to-front, tap→launch, ⋮=details/uninstall, hero, screenshots, related, sticky actions, back-only topbar, add tiles+embedded discover+reveal) + 2 disjoint review agents.** Agents caught + fixed: **a duplicate `.c-apps-add__methods` rule** collapsing the 3-up tile grid into a stack (deleted), a **physical `margin` shorthand** on the hero (→ margin-block/inline), a **keyboard-unreachable screenshots strip** (→ tabindex+aria-label), an unconditional **ResizeObserver disconnect** (leak-safe), and a `44px`→`--size-target-min` token swap. All tokens verified present; logical properties; no innerHTML. 🟡 sandbox note: the file mount truncates large-file *reads*, so the in-sandbox smoke ran against the first full build; the 2 post-audit tweaks were mirrored into the (valid, complete) real bundle by hand. ⏭ remaining polish (optional): hero launch transition, screenshot lightbox, live Discover feed (needs the apps.spixi.io endpoint from Damir). | Damir: "finish it, full audit, reuse + tokens" | ✅ slice 2 built + audited CLEAN |
| 125 | 2026-07-04 | **Apps premiumisation — slice 1c: 4 details/topbar polish fixes + audited CLEAN.** Damir review of the details screen. **(1) Duplicate title** — the details topbar showed the app name AND the hero showed it right below (two identical titles stacked). Fix = App-Store pattern: details topbar is now **back-only** (empty title); the hero owns the name. Add/Discover secondary screens keep their `pushTopbar` titles (no hero there). **(2) Missing hairline** — only the topbar `chat` variant had a bottom border; the **`view` variant** (details/add/discover) had none. Added `.c-topbar[data-variant="view"]{border-bottom:1px outline-neutral-01}` + dark → `-02`, mirroring the chat bar. Root header deliberately left borderless. **(3) "Read more" always visible** — even on a 1-line description the toggle showed and did nothing. Now measured after layout: `more.hidden` unless `desc.scrollHeight > desc.clientHeight+1` (via a `ResizeObserver` that disconnects on first non-zero height, `requestAnimationFrame` fallback). **(4) Permission chips** — Damir: "tapping should give info or be non-tappable." Made details chips **tap-to-explain** too (was readonly), consistent with the install modal. Refactored `capChips(caps, strings, {explain, reserve})`: details `{explain:true}` (line grows, page scrolls), modal `{explain:true, reserve:true}` (reserves ~2 lines so the dialog doesn't flicker). Moved the `min-height` CSS to `.c-app-caps__explain[data-reserve]`. **Audit (2 disjoint agents): CLEAN** — source↔bundle parity on all call sites (no positional booleans left), all tokens defined, `.c-app-details__more` has no `display` so `hidden` holds, no double-border/contrast issue. | Damir details review | ✅ built + audited CLEAN |
| 124 | 2026-07-04 | **Apps premiumisation — slice 1b: image-feedback fixes + audited CLEAN.** Applied Damir's screenshot notes on the built slice. **(1) Banner:** identical in both themes → pinned to theme-STABLE primitives `--primary-600` (surface under gradient) + `--neutral-10` (ink) instead of semantic action tokens (which flip to lighter-blue/near-black in dark); padding `--spacing-12` all sides (Figma), title/cta gap `--spacing-4`; illustration absolute, `inset-inline-end`, vertically centred, `height:132px` so it overflows and is **clipped** by `overflow:hidden`; removed **"coming soon"** text + the `data-parked` opacity. **(2) App title** in details → `--font-size-heading-xs` (was heading-sm; "fought with" the screen title). **(3) Removed the redundant Multiplayer badge** (the Multi-user capability chip already says it). **(4) Install modal rework (kill the flicker):** the lead is now a **muted subtext** (`--text-neutral-02`, body-sm) *above* the chip row; `.c-app-caps__explain` is **always in the DOM** (no `hidden` toggle) with a reserved `min-height:2.8em` (~2 lines) so tapping a chip fills text **without resizing the modal**. **(5) Sticky bottom Install bar:** `.c-app-details__actions` = `position:sticky; bottom:0` on opaque `--surface-screen` with a top hairline. **Audit (2 disjoint agents): 1 CRITICAL caught + fixed** — bundle had a stale `if (multi) …Multiplayer…` line after `const multi` was removed → would `ReferenceError` in `createAppDetails`; deleted. Re-verified: no `multi`/Multiplayer/`installLeadTap`/`line.hidden` left in source or bundle; all sticky-bar tokens exist + flip per theme; banner CLEAN (no leftover semantic tokens, no physical props). | Damir image feedback | ✅ built + audited CLEAN · ⏭ slice 2 (hero cover, tap→launch, recents, scaffolds) still queued |
| 122 | 2026-07-04 | **Apps shell (#7) built end-to-end (steps 1–7), Damir "build the entire screen" while away.** Redesign of the mini-apps screen (absorbs apps + app_new + app_details) to the Figma ref (node 11475-2996). **8 new components** (each .js+.css): `c-app-icon` (rounded-square avatar sibling, reuses `hashHue`), `c-app-item` (row + card renderings, two sibling buttons — big open target + ⋮ overflow), `apps-shell` (model + render pipeline + list⇄grid toggle + query + empty + `applyAppAction`), `apps-header` (search + self-syncing layout toggle + Explore banner), `apps-menu` (⋮ sheet: Open/App-details/Uninstall-confirm), `apps-add` (URL/QR/file + info + inline error), `apps-details` (header/verified badge/description/**capability chips**/version-size-id/install-URL+copy/Install|Open+Uninstall + install-confirm **sheet** + installing/installed/failed/removed **modals**), `apps-discover` (**PARKED** scaffold: category chips + coming-soon, revealed when the feed lands). Damir decisions: **list⇄grid toggle**, **compact permission chips**, per-item ⋮ menu, **Discover parked** pending the apps.spixi.io feed (Damir to source endpoint), installed+add+details first. **Full #46 audit loop:** step-1 agent CLEAN (1 minor fixed); steps 3–6 agent found **2 MAJOR** (`size:48` invalid button size → 6 unstyled CTAs → `size:56`; undefined token `--text-neutral-03` → `-02`) + a physical-`left` padding nit, all fixed; final verification agent CLEAN — source↔bundle parity for all 8 modules, 20 exports mapped, no collisions/TDZ, demo wired. Verified: 17+17 apps jsdom assertions + regressions (timeline 12, model 34) green. Bundle hand-appended (+ build-script FILES). Demo: `src/demo/apps.html`. **🟡 flags for Damir:** tap-item → details vs → launch (I chose details; app-launcher UX could prefer launch); Discover feed source (endpoint/§8); **§8 `addApp`+publisher** so installed rows show the creator the design wants (gracefully omitted until then); uninstall-from-list-⋮ needs the bridge to accept uninstall-by-id from the apps list (today details-page-scoped) — §8/integration note; layout persistence (in-memory now). | Damir: "build this entire screen… same methods, audit loop, flag anything" | ✅ built + audited CLEAN — ready for Damir review |
| 119 | 2026-07-04 | **ARCHITECTURE §9 sync — all tabled BE questions consolidated (handoff item 2, long overdue).** New subsection **§9.5 "Redesign feature dependencies — open questions & signals for backend"**: one table giving the BE team a single authoritative list of every data-shape question / one-way signal the redesign needs, each with the affected FE feature, current mock behavior, and DECISIONS row ref. 11 rows: contact-accept handshake complete+fail/timeout (#109/#116/#117), blind-group flag + member addresses (#99), per-member relation state (#102), reaction senders (#83), declined-vs-missed call (#87⑦), self-destruct TTL + deletion signal (#87③), sender-composed link-preview payload (#82), image thumb standard + autoload owner (#81), sender-side transfer progress (#107), download-start notify (#87⑤), IxiScope URL + refresh semantics (#98). Footer cross-refs the asks already fully specified as §8 command proposals (favorites/pin #67, voice #64, search, patchList, showWarning) so BE reviews everything in one pass. Framing holds the frozen-bridge line — all questions/proposals, mock-backed, none blocking. **Audited: verification agent CLEAN — completeness (nothing in DECISIONS #64–#118 missed), every ref accurate incl. #87 sub-items, no frozen-C# violation, no §8↔§9.5 contract drift, table renders.** | Damir: sync §9 BE questions | ✅ done + verified CLEAN |

| 127 | 2026-07-05 | **Apps premiumisation — slice 3: details ACTION HIERARCHY + title/cover + a11y (Damir review round, all three calls approved via interview).** **(1) Open/Uninstall demoted (Damir: "too prominent"):** installed apps lose the sticky two-button bar — **Open = compact 44 pill in the hero header** (App-Store pattern; `c-app-details__openpill`), **Uninstall = quiet destructive TEXT row at the page bottom** (after Advanced, scrolled-to not sticky; ⋮ menu keeps its copy). NOT-installed keeps the sticky Install bar (single conversion CTA) — now **edge-bled** (negative margin-inline/block-end + matching padding) fixing the gutter-scroll-past quirk, + `z-index:1` so the z-lifted header can't paint over it, + `inset-block-end` (logical). **(2) Title/cover overlap fixed (Damir):** header's negative margin removed — **only the icon lifts** over the hero (`align-self:flex-start` + `-spacing-48` → 32px overlap); name sits fully below the cover and drops to **heading-xs** (heading-sm "fought with" the cover). **(3) Screenshots ABOVE description** (Store order, Damir approved). **(4) a11y round:** related/recents tiles lose `role=listitem`-on-button (#106③ regression); shots strip = labelled `role=region` + numbered img alts; cap-explain line = `role=status` (chip taps now announced); Read-more gets `aria-expanded` + RO survives hidden-build AND stays live for rotation re-clamp; icon initial scales at 56/72. **No-screenshots fallback = GRACEFUL OMIT confirmed as best practice (Store convention — placeholders signal low quality); skeletons only if a live fetch is ever in flight.** Bundle regenerated via real build script (127 exports, staged in-sandbox — mount served STALE truncated reads of freshly-edited files; staged via fresh-file copies, real bundle tail verified via Read tool). **Verified: 15 jsdom assertions green; #46 audit (adversarial agent): 0 CRITICAL/MAJOR, 3 MINOR + 4 NIT — ALL fixed** (z-order, RO, alts, aria-expanded, physical `bottom`, initial sizes, demo related-tile resolves live installed entry) | Damir: buttons too prominent + title overlaps cover + smaller type | ✅ built + audited CLEAN |
| 128 | 2026-07-05 | **Apps — Damir asks logged (build later, flagged now):** ① **App SHARING gap** — single-player apps can't be shared via App invite; today = dig URL out of Advanced → paste in chat. Proposal: **"Share app" action** (details + ⋮ menu) → contact picker → sends the install link as a message (§8: needs a compose/send-to-contact path from the apps surface). ② **Full-bleed cover under the status bar** — premium (real App-Store look) but needs the #22 C# safe-area change + an OVERLAY topbar (on-scrim back over the cover, materializes on scroll) — **defer, bundle with #22 device verification; don't complicate the 4-platform baseline now**. ③ **"Anyone can build a mini app" encouragement** — add a quiet developer CTA at the bottom of Add & Discover ("Built something? Anyone can create a mini app — learn how") → external-link warning → spixi.io/mini-apps resources (the directory site already has this section — reuse its copy/links). ④ **Discover feed = apps.spixi.io** — the page is client-rendered (raw HTML ships an EMPTY app grid); FE needs the **JSON the page itself loads** (guessed paths 404) — ask for the endpoint or a published manifest; the site's per-app modal fields (icon/title/category/publisher/description/url/QR) map 1:1 to our details model and its category chips EXACTLY match apps-discover's set, so the contract is basically defined. ⑤ **Web→app install flow simplification** (site currently: copy URL → Spixi → + → paste) — deep link / QR-first — **Damir: "for another time", flagged**. | Damir round 2026-07-05 | 🟡 logged |

| 129 | 2026-07-05 | **Apps — slice 3b: install-modal polish + approved #128 items + FEED CONTRACT LOCKED.** **(1) Install modal (Damir screenshot — source line touched the buttons):** root cause = c-modal's rich `content` slot carries no trailing margin (only `__body` does) → design-system fix `.c-modal__actions{margin-block-start:24}` (collapses with body's margin, plain modals unchanged). Premiumised while in there (Damir invited a suggestion): **centered c-app-icon identity anchor** at the top of the content (same deterministic hue as tile/hero — the dialog reads as THIS app, Store-style, zero new assets) + **source shows the DOMAIN only** (trust signal at the decision moment; full URL kept on `title` + one tap away in details→Advanced). **(2) Parked-Discover FALLBACK (Damir):** `onBrowseWeb` renders an outline "Browse on the web" button in the coming-soon block → shell routes through the external-link confirm — Explore never dead-ends if the feed slips. **(3) Developer CTA (#128③ approved):** quiet bottom row on Add & Discover — "Anyone can build a mini app — web-friendly tech, no backend to run" + Learn how → resources page. ⚠ interim `rocket` glyph ('code' not in the export). **(4) Advanced share hint (Damir):** line under the install-URL row — "Share this link with anyone — they can add the app in Spixi from it" (interim answer to the #128① share gap). **(5) FEED ENDPOINT CONFIRMED by Damir (DevTools): `https://apps.spixi.io/data/apps.json`** — CORS `*`, max-age 600, static GitHub Pages (site repo: ixian-platform/Spixi-Mini-Apps-Website). New **`apps-feed.js`** locks the contract: `feedEntryToApp` (spixiUrl→url, multiUser→MultiUser capability, `installed` resolved against state.apps — powers the Damir ask that Discover flags installed apps → routes to installed details) + `parseAppsFeed` (feed categories override the built-ins; they match today). **Schema wishlist for the site repo: cover, screenshots[], related[], size, verified.** ⚠ transport question for BE (§9): iOS WKWebView http/https whitelist (#120) — direct fetch vs bridge proxy. Bundle regenerated (130 exports, staged workaround again — mount still serves stale reads of freshly-edited files). **Verified: smoke now 25 jsdom assertions, ALL GREEN.** | Damir round 2 answers + endpoint | ✅ built · 🟡 live Discover next slice (needs transport answer) |
| B2 | 2026-07-05 | **BACKLOG (Damir): pre-release GLYPH SWEEP.** Near app finalization: full sweep of every `icon(...)` call across components/shells vs the icons.js registry → deliver Damir a list of Tabler glyphs to export from Figma into `src/assets/icons/` (+ regenerate) so iconography is 100% cohesive (no fallbacks/stand-ins). **Known so far:** `link` (apps-add Paste-link tile — MISSING today, renders empty, caught by smoke), `code` (dev CTA, rocket stands in), `trash` was a batch-3 stand-in note (#74 — since exported? verify), filled twins audit for bottomnav (#39 `user`-filled). Also sweep demo-only glyph uses. | Damir 2026-07-05 ("make this a to-do") | 🅱️ backlog |
| B3 | 2026-07-05 | **BACKLOG (Damir): simplify the web→app install journey** on apps.spixi.io (today: copy URL → Spixi → + → paste → Get). Candidates when picked up: deep link (`spixi://addapp?url=…` — needs C# URL-scheme registration), QR-first on the site's app modal (already has "Scan with Spixi"), or clipboard-detect on the Add surface. Bundle with the §9 transport answer + #128⑤. | Damir: "for another time, flag it" | 🅱️ backlog |

| 131 | 2026-07-05 | **Damir interview round 3 — illustrations APPROVED + backup UX locked + LIVE DISCOVER BUILT.** ① Illustrations plan #130 approved in full (style system, P1 = 8 assets); Explore banner **regenerates** for consistency (prompt 17 added, right-weighted, must read on indigo). ② **Backup UX** (research first: `BackupPage.xaml.cs` — the account backup is an encrypted ZIP that ALREADY bundles wallet.ixi + contacts (`Acc/`) + account.ixi + avatar, i.e. a strict superset of the wallet-only share): **one primary "Back up Spixi"** (full backup) + **wallet-only export demoted to Advanced** · **nudge = status-driven quiet-but-standing** (row badge states: never/date/dirty-since; no popups) · **home = Account-shell row w/ live status + onboarding-tail prompt**, wallet shell cross-links. Full spec `docs/backup-ux-spec.md` (state machine, copy draft, reuse map — no new component types). ⚠ BE asks (→§9.5 at Account build): last-backup timestamp + dirty-since signal; today NOTHING records that a backup happened. ③ **Live Discover built** (per approved next-slice): `setDiscoverFeed`/`setAddDiscoverFeed` free fns flip the parked view live — Featured strip + card grid (icon·name·publisher·**Installed c-badge**, tap → onOpen → details resolves installed → Open pill), category chips filter client-side (feed categories override built-ins), filtered-empty state; component never fetches (shell owns transport, §9). Demo: attempts the real `apps.spixi.io/data/apps.json` fetch, falls back to a bundled snapshot on file://; TicTacToe seed id switched to reverse-DNS so the Installed badge shows live. **Verified: smoke 31 jsdom assertions ALL GREEN; bundle 132 exports, tail Read-verified.** | Damir approvals + interview (4/4 recommended options) | ✅ discover built · 🟡 backup spec pending Damir read · 🟡 §9 transport answer |
| 130 | 2026-07-05 | **Illustration & animated-SVG plan drafted** (`docs/illustrations-plan.md`, Damir ask): full surface inventory (17 assets — onboarding intro ×3, create/restore/backup, 6 empty states as one visual family, install-success + handshake animations, error/teaser) + a locked **style system** (flat geometric vector, Tabler-weight strokes, brand indigo/periwinkle palette with verbatim hexes, theme-stable-first, no faces, no baked text, transparent bg) + a **master prompt + 16 per-asset nano-banana prompts**. Key principle: generator delivers layered STILLS; **animation is ours in CSS** (transform/opacity, token durations, reduced-motion stop) so motion stays theme-aware and consistent. P1 cut proposed: 8 assets. 4 open questions for Damir in §7. | Damir: "plan all illustrations + prompt set for nano-banana" | ✅ approved by Damir 2026-07-05 (P1 cut locked · banner regen yes · backup asset follows #131) |

| 132 | 2026-07-05 | **Illustrations PARKED (Damir): nano-banana output quality is poor.** The approved plan + style system + 17 prompts (`docs/illustrations-plan.md`, #130/#131) stay ready — revisit with a better generator or hand-drawn assets; empty states ship copy-only until then (no blocking dependency anywhere). **Next: WALLET SHELL** (#95 order). | Damir 2026-07-05 | 🅿️ parked · wallet shell starting |

| 133 | 2026-07-05 | **WALLET SHELL kicked off — interview locked, spec written (`docs/wallet-shell-spec.md`).** Damir decisions: ① **Send = ONE screen + review c-sheet** (recipient+amount+live fee on one surface → #26 review step: recipient·amount·fee·total + explicit latched Confirm → success morph) ② **Home = hero balance (#20/#21) + Send/Receive in hero + All/Sent/Received chips + the #98 "Missing a transaction?" pill** (Damir re-confirmed: explains chain-read lag + links THIS address on IxiScope) + c-txlist-item activity; NO fiat line in v1 ③ **Tx details = bottom sheet** (badge, tnum amount, fee, counterparty, confirmations, IxiScope link) ④ **Design grounding: Damir authorizes the Figma MCP** — #31 screens scan is the slice-1 prerequisite (frames pull next session; connector unauthorized in this one). Legacy research (Explore agent): send/send2/sent + recipient picker + request pages, setBalance/setRecipient/setFees/addEntry bridge fns, confirmation enum, QR formats `address:ixi`/`address:send:amount`, polling-only status, multi-recipient commented out C#-side (model stays plural-ready). Slice order: home → send → receive/request → device polish; BE asks queued (IxiScope URL, history paging, dust floor #85). | Damir interview 2026-07-05 | ✅ spec ready · ⏳ Figma auth → slice 1 |

| 134 | 2026-07-05 | **WALLET SLICE 1 BUILT + audited + Damir round folded in.** Built code-first (no payment-flow designs exist — sanctioned #31 exception; the app-frame wallet mock = the visual reference, per Damir). **Components:** `c-wallet-hero` (hero region #20, tnum #21, title #58; pre-formatted balance + optional fiat line — fiat KEPT per the app-frame reference Damir pointed at, balance-fiat source = §9 ask; hide-balance eye; Send/Receive/**Scan** quick actions — Scan sanctioned from the mock) + `wallet-shell` (tx model incl. **legacy 4-state confirmation enum** confirmed/pending/failed/unknown; All/Sent/Received chips + **#98 missing-tx pill → explainer sheet** with Refresh + "View all transactions on Explorer" = `ixian:explorer` parity, explorer.ixian.io grounded in Config.explorerUrl; **tx-detail bottom sheet**: contact txs personalize with the deterministic avatar (Damir), Status row always, Fee when provided (**§9 ask: addPaymentActivity carries no fee**), FULL address in the member-sheet chip pattern, txid copy, View-in-Explorer). **Damir scroll UX:** `attachWalletScroll` — scroll down → hero MINIMIZES (compact title+balance bar, #114-family binary CSS transitions, inert'd collapsed zones) + sticky tools (c-search-field **FE tx search: names/addresses/txids** + filters) tuck away → single-page list; brief scroll-up reveals tools; absolute top restores the hero; mid-search guard (never hides tools holding focus/query). **Hero seam fixed** (Damir screenshot): hero owns `env(safe-area-inset-top)`, status bar floats transparent — the #22 surface. **#46 audit (adversarial agent): 1 CRITICAL + 5 MAJOR + 9 MINOR — ALL fixed**: real clipboard copy w/ check-morph (was toast-only lie) · sheets got accessible names (were "Menu") · on-hero focus ring pinned to on-hero ink (#106 family) · APG toggle contract (constant label + aria-pressed) · latched sheet-closing bridge intents (#72④) · txlist-consistent sheet states/tokens · 44px copy hit-areas · scoped string keys · primary-first missing-tx actions · sanction comments. Wallet suite folded into `scripts/smoke-test.mjs` (finding 12). 🟡 flags: failed sends list under "Sent" (badge carries it) — Damir call; ≤360px pill collapse is viewport-based (container queries off the #4 baseline — revisit at desktop). **Verified: 28 standalone jsdom assertions ALL GREEN; bundle 148 exports, staged rebuild; committed smoke runs on Damir's machine.** | Damir: proceed code-first + fee/avatar/status + smooth hero + scroll UX | ✅ built + audited · 🟡 Damir demo review → slice 2 (send) |

| 135 | 2026-07-05 | **Wallet — Damir review round FIXED + SLICE 2 (SEND) built + audited (1 CRIT + 4 MAJ found & fixed).** **Review fixes:** ① demo tools "missing" = stale `file://` script cache (page verified rendering end-to-end in jsdom) — hard-refresh ② tx-sheet **Fee row gained ⓘ → one-line explain** ("Network fee — paid to the Ixian network…", role=status + aria-expanded) ③ **address labelled by owner** (received → "Sender's address", sent → "Recipient's") ④ **missing-tx sheet = single action** (no refresh command exists; list rebuilds per addPaymentActivity tick) ⑤ **tap-anywhere-on-balance hides** (+ compact balance; eye stays the a11y path; ghost-region guarded via pointer-events) ⑥ **quick actions uniform** (special filled Send circle removed) ⑦ tx-sheet title → **label-lg** (heading-xs overpowered). **SLICE 2 — `c-wallet-send`** (spec §3, one screen + #26 review sheet): contact picker (search, deterministic avatars) / address input + QR (`setSendAddress` parses the legacy `addr[:send:amount]` formats, supersedes a picked recipient) / amount entry (comma-safe sanitize, Max, live fee+total, inline insufficient) → **Review c-sheet: lightDismiss OFF (money = explicit)**, latched Confirm → loading → success morph → onDone (pending row lands via bridge); fail → inline error + retry. **#46 audit caught the real ones: (C1) review sheet was dismissible MID-SEND** → double-send path + failures written into a dead sheet — fixed with a per-view in-flight token, Cancel disabled + Esc/scrim locked while in flight, attempt counter invalidating stale bridge callbacks; **(M1) binary-float money math** falsely rejected exactly-fitting amounts, Max could overshoot by 1e-8, ×1e8 overflowed 2^53 at Ixian-scale balances → **all money math now integer 1e-8 units via BigInt** (ES2020 floor #45); **(M2) pasted '1,000.5' became 1.0005** (1000× reinterpretation) → comma = grouping when '.' present, else decimal; **(M3) `formatIxiAmount` truncation rendered the default fee as "Fee 0 IXI" AT THE CONFIRM STEP** → #77 scoped as a FEED-display rule: review/fee lines show EXACT strings (what you approve is what is sent); **(M4)** compact-balance tap desynced shell persistence → routed through onToggleHidden. +6 MINOR/NIT (focus flow on pick/clear, alert announce order, scan-over-picked, ghost click region, pressed washes, fee-ⓘ aria-expanded). Payload plural-ready (`recipients:[…]`, multi-recipient stays commented out C#-side). **Verified: 55 standalone jsdom assertions ALL GREEN; send section folded into scripts/smoke-test.mjs; bundle 151 exports, tail Read-verified.** | Damir 6-point review + "move on to slice 2" | ✅ built + audited · 🟡 Damir demo review → slice 3 (receive/request) |

| 136 | 2026-07-05 | **Wallet — Damir's overnight list DONE ahead of the next session (+ tx-flash idea, agreed).** ① **Tools invisible = REAL BUG, root-caused statically** (not cache): `.c-wallet-tools` had `overflow:hidden` → automatic min-height 0 → **flex-shrink collapsed it to nothing** between hero and the tall list in the flex-column scroll container (jsdom smoke is layout-blind — couldn't catch it). Fix: `flex:none` (load-bearing, commented). Committed smoke gained a presence guard. ② **Eye center-tap dead = double-toggle race**: the toggle re-render DETACHED the tapped svg, the balance-block guard's `closest()` returned null on the orphaned target → the bubbled click toggled BACK (net zero); padding-area taps kept the button as target → worked. Fix: `stopPropagation` in the eye handler (load-bearing, commented). ③ **Send-to-address = contact-aligned row ON TOP of contacts** (qr glyph in the avatar-40 slot, `aria-expanded`, tap expands the input beneath) — Damir's exact sketch. **Contacts scaling**: unqueried shows 5, query shows 8, "+N more — keep typing to narrow it down" note; search is the path through hundreds. ④ **Numpad: already correct** — `inputmode="decimal"` on the amount field (mobile keyboards show the decimal pad); asserted in smoke. ⑤ **Max = explicit stop**: alertdialog — "Send your entire balance? … You would be left with 0 IXI." Cancel autofocused (APG safe action) / "Yes, I understand" fills the field. ⑥ **Fresh-tx FLASH** (Damir idea, agreed — the "your action landed here" cue, #69 entrance-animation family): `flashWalletTx(listEl, txid)` → one ~2s action-tonal wash pulse on return-from-send (raw 2s keyframe sanctioned per #117 precedent, explicit reduced-motion escape, animationend + timeout cleanup). **Verified: 64 standalone jsdom assertions ALL GREEN; bundle 152 exports; committed smoke updated.** **NEXT SESSION (Damir plan, my take logged): slice 3 receive/request, then CHAT TIPPING as its own UX** — tipping ≠ send: in-context, tiny amounts, speed over ceremony — preset chips (1/5/10 IXI + custom) on a small sheet from the message menu/reaction row, ONE explicit confirm tap (#26-lite: the sheet IS the review — amount+recipient visible), result lands as the #65 tip-pill on the message + a wallet tx; needs a bridge question (tip = plain payment w/ message ref? §9). | Damir overnight notes + "what's your take on this plan?" | ✅ fixes built + smoked · 🟡 Damir verify on his machine → slice 3 + tipping spec |

| 137 | 2026-07-05 | **WALLET SLICE 3 (RECEIVE/REQUEST) built + audited; Damir calls locked; QR pipeline landed.** Damir (3-answer round): ① #136 fixes verified on his machine ✅ ② **Receive/Request = ONE progressive surface** (send-screen grammar) ③ **failed sends LIST under "Sent"** (predicate already did it — comment locked, smoke asserts it; 🟡 in #134 resolved). **QR pipeline:** vendored `qrcode-generator` (MIT, same lineage as legacy qrcode.min.js) as `src/components/qr.js` — module-scope IIFE (nothing leaks into the bundle scope), `createQrSvg`/`setQrValue` emit an SVG path with `var(--on-qr)` ink on the `--surface-qr` card (**new `--on-qr` token: FIXED dark in both themes — the QR pair never theme-flips, scan contrast**); **verified against a real decoder (jsQR): `addr:ixi` + `addr:send:amount` payloads round-trip exactly.** **`c-wallet-receive`:** receive QR = legacy `address:ixi` (wallet_request parity) + FULL address chip (#99) w/ HONEST copy morph (✓ only after clipboard write resolves — new pattern, stricter than member-sheet's; sweep flag below) + Share via onShare (**NO legacy share command exists — §9 ask**; shell can navigator.share) · "Request an amount" reveal (aria-expanded/-controls) → shared `sanitizeAmount` (exported from wallet-send) → QR morphs IN PLACE to `addr:send:amount` (the format setSendAddress parses) → contact strip (5/8 cap, #136③) → `onSendRequest({contact, amount})` = legacy `ixian:sendrequest` payload mirror · collapse clears the amount (state honesty). **#46 audit (adversarial agent): 1 CRIT + 5 MAJ + 6 MIN — ALL C/M + 4 MIN fixed:** (C1) `setRequestAmount(0.0000001)` → `String()` gave '1e-7' → sanitize made **'17'** — silent 170,000,000× magnitude change; numbers now expand via toFixed(8) · (M1) QR could encode `12.`/`.5`/`007` → `canonicalAmount()` at every payload exit · (M2/M5) row-DOM latch died on search re-render (double-request) + stale ✓ after amount edits → **latch lives in STATE**, amount edit cancels it · (M3) disabling the focused row dropped keyboard focus → acted row stays enabled, hidden live region announces "Request sent" · (M4) chevron rotation selector also flipped the glyph circle (descendant → child combinator) · (m1) copy morph honest-on-resolve · (m2) empty-address guard (no garbage QR) · (m3) caption live-spam → transition-only announcements · (m5/demo) scrollTop reset between views · (n2) aria-controls added HERE AND to wallet-send's address reveal. 🟡 sweep flag: member-sheet/wallet-shell copy morphs still fire-and-forget (audit m1 pattern-wide). **Verified: smoke suite 27 receive/audit assertions folded in, ALL GREEN; bundle 157 exports (+sanitizeAmount, createQrSvg, setQrValue, createWalletReceive, setRequestAmount), tail Read-verified.** §9 asks +: share-sheet bridge command; `ixian:sendrequest` semantics for the single-contact request (legacy fired addr:amount pairs). | Damir 3 answers + spec §4 | ✅ built + audited · 🟡 Damir demo review → tipping |

| 138 | 2026-07-05 | **CHAT TIPPING specced + built (`docs/tipping-spec.md`, `c-tipsheet`) + a CROSS-CUTTING #56 overlay fix the audit surfaced.** Design per the agreed #136 take (tipping ≠ send: in-context, tiny, fast — but still money): **message-menu Tip → small sheet = the "#26-lite" review** (recipient avatar+name + amount on the SAME surface as the confirm) · **presets 1/5/10 IXI + Custom** (shared sanitize + canonical amount) · **ONE latched confirm whose label carries the amount ("Tip 5 IXI")** · light-dismiss ALLOWED pre-confirm (deliberate divergence from the send review — nothing committed; a dismissed tip costs one long-press) · in-flight = locked, fail → inline retry · success → #65 tip pill + wallet tx land via the bridge. **Bridge grounding: legacy already fires `ixian:contextAction:tip:MSGID:AMOUNT`** (chat.js:1670) — tip IS a first-class context action w/ message ref; §9 asks narrowed to: fee handling, pill-state push vs optimistic paint, group MSGID→sender resolution, repeat-tip aggregation. **#46 audit (adversarial): 1 CRIT + 4 MAJ — the CRIT was an INFRASTRUCTURE lie:** `setOverlayOpts` on an OPEN overlay was a silent no-op (opts frozen at open; scrim bound only when lightDismiss started true) → the tip sheet's "in-flight lock" held on NO path (Esc/scrim/back all dismissed mid-send = the #135-C1 double-spend class resurrected) AND wallet-send's Esc lock was silently void too. **Fix in overlay.js: dismissal policy read LIVE on every path** (Esc + scrim check current opts at event time; `setOverlayOpts` now MERGES; `dismissTopOverlay` with escDismiss:false CONSUMES the back press without closing — back must not dismiss what Esc can't). Rest fixed: (M1) onDismiss bumps the attempt counter — a dismissed sheet's pending bridge callback is void · (M2) balance guard compares in **integer 1e-8 units** (`toUnits` exported from wallet-send; Number() loses 1e-8 at ≥1e8 IXI) · (M3) chips/input freeze + sync() stands down in flight (nothing un-disables a loading confirm) · (M4/m2, demo) recipient resolved by group-head walk + rows stamped with msgids (shell note: resolve from the MODEL, never the DOM) · (m1) ctrl one-shot per attempt · (m3) preset supersedes → stale custom cleared · (m4) failure alert and insufficient guard are separate elements. 🟡 flags: `canonicalAmount` imported cross-feature from wallet-receive (shared money-module candidate); demo never exercises ctrl.fail. **Verified: tip suite + overlay-lock + M2-precision regressions in smoke — ALL GREEN incl. the pre-existing overlay/modal assertions (no #56 semantics drift); bundle 160 exports (+toUnits, canonicalAmount, openTipSheet), Read-verified.** | Damir #136 plan + logged take; #46 loop | ✅ built + audited · ✅ Damir round 1 folded: chips looked native/off-brand + broke dark BECAUSE chat.html never linked chip.css (fixed + a static smoke guard: demos must link every component stylesheet they render); sheet roomier (gap 24, padded), amount chips upsized to equal-width 44px targets (#50 tonal selected carries the brand), ~40px clearance before the money CTA (misclick guard) · ✅ round 2: custom-input slot GHOSTED (visibility:hidden, layout kept) — the sheet opens at full size and picking Custom never grows/reflows it under the finger |

| 139 | 2026-07-05 | **CHAT ROUND: multi-select/copy/split-paste + in-chat Pay/Request wired + send-error bug.** ① **Bug (Damir screenshot): bad-address error rendered below the CONTACTS** — errLine moved into the address field (validation sits under ITS input) + smoke assert. ② **In-chat money** (Damir plan + accepted suggestion): attach **Pay → the full SEND screen as an in-phone takeover with `lockedRecipient`** (new createWalletSend opt: pre-picked, picker gone, NO ✕ — the peer is known; group chats keep the member picker since the recipient IS the open question) → payment bubble (role 'sent', pending→completed); attach **Request → `openRequestSheet`** — the tip sheet generalized into ONE audited amount-sheet machinery (`openAmountSheet` internal, kinds tip/request: request copy set, **NO balance guard — it's the payer's money**) → 'request-out' bubble. Sheet over the QR-centric receive screen because the counterparty is known (Damir approved). ③ **Multi-select (Damir asks, 3 picks locked): `chat-select.js`** — message menu gains capability-gated **Select** → selection mode (capture-phase taps toggle text rows, count bar overlays the top w/ Copy/Cancel/aria-live count, Esc exits, count-0 exits); **copy = single→raw text, multi→"Sender: text" lines** (Telegram-style, no timestamp noise) → system clipboard + module buffer; **split-paste = OFFER not auto-split**: `attachSplitPaste` value-matches the composer against the buffer (covers every paste path incl. mobile menus) → inline "Send as N separate messages" → RAW texts re-sent as own messages; ignoring it keeps the standard one-multiline-message behaviour. Demo: sender resolution via group-head walk, both phones wired (copy in one → split offer in the other). **Verified: 99 smoke assertions ALL GREEN; bundle 164 exports (+lockedRecipient path, openRequestSheet, enterChatSelect, attachSplitPaste, getChatCopyBuffer), Read-verified.** 🟡 for the real shell: selection of media/card rows (copy skips them today) · forward-as-quote vs re-send distinction · request bubble in GROUPS = room-wide (anyone pays) — §9 confirm. | Damir round 3 (bug + 3 UX picks via question round) | ✅ built + smoked · 🟡 Damir demo review |

| 140 | 2026-07-05 | **BUG ROUND c (Damir screenshot, 4 items) — 3 fixed, 1 confirmed by-design.** ① **"−" on Payment sent = intentional** (#76 family: amounts ship SIGNED from the bridge — received completed shows '+', sent '−', mirroring the wallet tx rows per Figma; say the word and the card drops the sign in favour of the title carrying direction). ② **Live request-out card rendered BARE** (no badge, no Cancel): the #139 demo wiring passed `status:'actionable'` — a request-IN state — now 'pending' → standard card (Pending badge + **Cancel request → loading → in-place flip to Canceled**, #86 swap), real-browser e2e-verified. Adjacent catch: `setPaymentStatus(payRow, 'completed')` — a bare STRING patch is a silent no-op (`{...str}` spreads chars) → live-sent payments sat on Pending forever; object form + a smoke guard banning the string form. ③ **Reply-strip overflow hid the cancel ✕** (CROSS-CUTTING, real component CSS): `.c-composer__ctx` is itself a flex container, so as a flex ITEM its `min-width:auto` resolves to the NOWRAP excerpt's full width — 642px inside a 417px composer, measured in headless Chrome (jsdom is layout-blind; this is the #136① `.c-wallet-tools` lesson again from the other direction) — **fix: `min-width:0`** restores basis+shrink sizing, ✕ verified on-screen. ④ **Typing pill pushed above the money cards**: the #139 pay/request append paths bypassed sendMessage's pill re-append — extracted `keepTypingLast(box)`, called on EVERY demo append path (smoke guards ≥3 call sites); shell duty note stands: whatever appends a row keeps the pill last. **Verified: 103 smoke assertions CLEAN (+4 static guards); e2e in REAL Chrome via @sparticuz/chromium + playwright-core (npm-shipped binary — the playwright CDN is allowlist-blocked in the sandbox); bundle re-synced (the mount had truncated last session's write by 399 bytes) + tail Read-verified.** WORKFLOW: the truncated-mount gotcha PERSISTS ACROSS SESSIONS (DECISIONS.md, chat.html, overlay.js … read short in bash until refreshed); detector = file-tool line counts vs `wc -l`, but EQUAL counts can hide it (message-menu.js) — trust only file-tool reads for last-session files. | Damir bug round (4 items + screenshot) | ✅ fixed + e2e/smoke-verified · 🟡 Damir verify on his machine |

| 141 | 2026-07-05 | **CHAT INFO built (`c-chat-info` + docs/chat-info-spec.md) — the last missing chat surface; Damir picks locked via question round.** Picks: ① **ONE takeover for 1:1 AND group** (send-screen grammar, header identity tap — `onIdentity` existed on the topbar since #86 for exactly this) ② 1:1 = **legacy parity** (nickname edit → `ixian:userdefinednick` · FULL address + copy morph + lazy `address:ixi` QR · payments-with-contact via txlist-item · delete history `ixian:removehistory` / remove contact `ixian:remove`) **+ Pay/Request** (routes into the #139 machinery) **+ shared media strip** (flagged `capabilities.media` — NO legacy command, §9) ③ group = member rows (#136③ caps) → **existing member-sheet, `capabilities.admin` injects Kick/Ban** (new member-sheet `actions` opt; each behind a LOCKED alertdialog confirm → `ixian:kick/ban:ADDR`) + leave (`ixian:leave`) ④ **notifications toggle designed for BOTH, 1:1 gated off** (bridge = groups/bots only via `ixian:en/disableNotifications`; §9 ask for 1:1). Bridge inventory mined from legacy (contact_details.html + chat.js + SingleChatPage/ContactDetails.xaml.cs); blind groups: no address card, member rows name-only, avatar seeds stripped. Destructive confirms follow #135-C1 (in-flight = Esc/scrim/Cancel dead — LIVE via the #138 setOverlayOpts fix), callbacks use the house (payload, ctrl) contract, one-shot per attempt. **#46 audit (adversarial agent): 3 MAJ + 6 MIN/NIT, ALL fixed:** (M1) nickname Enter→disable→blur **double-committed** (two ctrls per edit) → committing latch, Esc dead mid-flight · (M2) **kicked member's row stayed** → re-offered `ixian:kick` for someone already out → ctrl.done splices + re-renders + fixes BOTH counts (section label + hero line); contact request marks relation pending immediately · (M3) **members 6-7 of a 6/7-member group were UNREACHABLE** (cap 5, search only at ≥8) → search renders the moment the cap can hide anyone + filter matches addresses (nameless members findable) · (m4) sync throw in a shell callback wedged the fully-locked modal → try/catch to the fail path · (m5) confirm-fail refocuses the confirm (never drop focus) · (m6) `host` opt (demo-selector fallback only) · (m7) confirm titles built at CLICK time (stale nickname) · (m8) member-sheet actions row latched · (n9) overlayId for the QR box. E2e also caught: the hero sub was written but never appended when initially empty — nickname's wire-name line silently vanished. **Verified: 119 smoke assertions CLEAN (16 new: capability gating, QR, M1/M2/M3 regressions, confirm lock incl. Cancel-dead + fail-retry, optimistic toggle, css link guard); real-Chrome e2e drove BOTH surfaces end-to-end (header tap → QR → nickname → locked confirm → kick → toggle); bundle 165 exports (+createChatInfo), Read-verified.** §9 asks (spec §"asks"): 1:1 mute command · shared-media inventory (paged) · presence line · group name/avatar edit · room-wide request (carried). | Damir "only thing missing is chat info" + 4-pick question round | ✅ built + audited + e2e · 🟡 Damir demo review → remaining figma-sweep screens (onboarding · lock · add contact · settings hub + backup · scan · desktop passes) |

| 142 | 2026-07-05 | **CHAT-INFO FIX ROUND (Damir, 5 items) + the send-picker cap reversal.** ① **Caps KILLED list scanning** (Damir: "a user can't scroll to find one — they need to know who they're searching for"): chat-info members AND the wallet-send picker now render the FULL list **A–Z sorted**, body scrolls; search stays as a pure FILTER (members: from 8; send: always) — the #136③ 5/8 caps + "+N more" notes are dead in both (receive's request-contact strip intentionally untouched — compact strip inside a progressive surface, different grammar). Send-flow quirk audit: picking still COLLAPSES the picker to the picked row and the CTA stays recipient-gated, so a long list never competes with the amount section; #141-M3's unreachable-member hazard dies with the caps. ② **Payment rows rendered as NAKED NATIVE BUTTONS (Damir screenshot) — the #138-round-1 class AGAIN: chat.html never linked txlist-item.css** (the component was already txlist-item) → link added + static smoke guard extended. Payments also became a **collapsed accordion**: "Payments (N)" toggle (QR reveal grammar, lazy-built) → 5 most recent + "View all {n} payments" → `onTxAll` (no callback → full list inline) — a long tx list no longer greets every visit. **Sections gained dividers** (body `* + *` top separators — the screen read as one undivided blur). ③ **Contact page = SAME component, `context: 'chat' | 'contact'`** (Damir pick): contact context retitles "Contact info", **Message (fill, `onMessage`) leads** and Pay demotes to outline (member-sheet precedent), delete-history + disappearing-messages stay chat-side; member-sheet `onViewContact` passthrough wired (identity block → contact page; demo: group → Han Solo). ④ **Destructive rows = separated bordered cards, gap-12** (flush rows invited mistaps between delete-history and remove-contact). ⑤ **Disappearing messages = per-chat setting in chat info** (Damir accepted rec over a composer control — conversation-level policy, Signal grammar, composer stays clean): setting row (current value) → radio sheet Off/1h/1d/1w, commit-per-pick via `onSelfDestruct(seconds, ctrl)` (latched + loading, close on done, live-region fail) — **capability-gated `capabilities.selfDestruct`, NO bridge command → §9 ask** (window command + both-peer expiry enforcement). **Verification: ~18 new smoke assertions written (no-cap A–Z renders both surfaces, search-as-filter, accordion collapsed/preview-5/View-all, contact-context title/Message-leads/no-delete-history, sd row+sheet+latch+gating, css link guard); build + smoke run by DAMIR on his machine — GREEN (2026-07-05). No sandbox e2e this round; visual review = Damir's demo pass.** WORKFLOW DECISION (Damir): sandbox builds/e2e are DEAD — the stale-mount workaround burned ~20% of a weekly token budget in one round. New loop: sessions write code + tests ONLY; Damir runs `node scripts/build-demo-bundle.mjs; node scripts/smoke-test.mjs` locally (PowerShell: no `&&`) and reports output; failures get fixed as targeted edits. | Damir 5-item bug list (screenshot) + 4-pick question round | ✅ built + smoked + e2e · 🟡 Damir verify on his machine |

| 143 | 2026-07-05 | **#142 round APPROVED (Damir demo pass — "good to go") + finalization era opened.** ① `docs/opus-review-brief.md` written — standing instruction set for an Opus adversarial review-and-fix loop (audit → fix + regression assertion → build/smoke → fresh reviewer, repeat until zero findings + green; simplicity mandate: delete > add, shared money module named as dedupe target; environment + contract gotchas from #140–#142 baked in). ② **Figma is GONE → all remaining surfaces go code-first** (extends the figma-sweep §3 utility-screen recommendation to everything, incl. launch/onboarding — Damir art-directs in the demo instead of Figma). ③ `docs/finalization-roadmap.md` written — full map to done: Phase 0 Opus cleanup · Phase 1 screens (Account/Settings shell [backup-ux-spec ready] → Contacts [profile already exists via `context:'contact'`] → Scan → Lock → Launch/onboarding) · Phase 2 cross-cutting (desktop passes, #22 safe-area, dark verification, copy/a11y sweeps) · Phase 3 integration (§9 sync, native.js, SL i18n, Vite→Resources/Raw/html, C# repoint table, device tests) · Phase 4 final freeze audit. | Damir demo approval + "map what remains, no Figma" | ✅ docs written · 🟡 Damir reviews roadmap order → Phase 0 dispatch |

| 144 | 2026-07-05 | **PHASE 0 OPUS ROUND 1 — chat-info adversarial audit + the shared money module (opus-review-brief scope this round: chat-info + money dedupe).** Chat-info has already been through #141/#142 audits, so the functional logic (nickname M1 latch, confirm #135-C1 lock, member M2/M3, accordion, sd sheet) held up; the adversarial CSS pass found **1 MAJOR** the layout-blind jsdom couldn't: **① the hero name (`.c-chat-info__name`) is a flex item in `name-row` (a ROW flex) with `white-space:nowrap` but NO `min-width:0`** → its min-width resolved to the nowrap content width, so a long name/nickname (or RTL string) **overflowed the hero and shoved the edit-pencil off screen** — the #140③/#136① class again, from the same direction. Fix: `min-width:0` on the name (its own overflow/ellipsis then bites). **② Money dedupe (brief's named target):** the sanitize/units/canonical/format helpers were scattered — `sanitizeAmount`+`toUnits` in wallet-send, `canonicalAmount` in wallet-receive, `formatIxiAmount` in typed-bubbles — and the CHAT paths reached CROSS-FEATURE into the wallet path (tip-sheet's 🟡 flag, #138). Pulled all four into **`src/components/money.js`** (audit-history rules preserved verbatim: comma grouping #135-M2, BigInt 1e-8 units #135-M1/#138-M2, canonical payload #137-C1, truncate-not-round #76/#77). `fromUnits` stays wallet-send-local (only the Max display uses it). Rewired wallet-send / wallet-receive / tip-sheet / typed-bubbles to import from money.js; added money.js FIRST in the bundle FILES (before every consumer); public bundle surface unchanged (same 4 names, relocated). Net: deletes the cross-feature coupling + the 🟡, no behavior change. **+9 smoke assertions** (4 functional: sanitize/canonical/format/toUnits still correct via `window.Spixi`; 5 static: chat-info name `min-width:0` guard + the money-row layout guard [jsdom is layout-blind], tip-sheet/wallet-receive/typed-bubbles now import from money.js not cross-feature). **Damir ran build + smoke — one RED surfaced and fixed:** the pre-existing `search filters the full list` assertion expected only `Leia` for query `le`, but `le` is a SUBSTRING of both `aLEx` and `LEia` — the filter was correct (both match, A–Z), the EXPECTATION was the latent bug (Damir: keep substring matching, consistent with chat-list search); assertion corrected to `Alex+Leia` ("no hidden remainder" now means every match shows). **Message-width 🟡 RESOLVED (Damir demo):** contact-context money row → **Message LEADS a full-width row, Pay + Request share a 2-column row below** (`.c-chat-info__money` gets `flex-wrap:wrap`; `.c-chat-info__message` `flex-basis:100%` forces the wrap; chat context has no Message so Pay+Request stay one row) + a static layout guard. **Eyeball in GitHub Desktop:** `src/components/money.js` (new) · `wallet-send.js` · `wallet-receive.js` · `tip-sheet.js` · `typed-bubbles.js` · `src/styles/components/chat-info.css` · `scripts/build-demo-bundle.mjs` · `scripts/smoke-test.mjs` · regenerated `src/demo/spixi.iife.js`. **NEXT: Damir re-runs build + smoke (expect green) → fresh-reviewer pass over the round diff → loop to clean.** | opus-review-brief loop, round 1 (Damir: chat-info scope + do the money dedupe) | ✅ MAJOR + dedupe fixed, Message-width + search-semantics resolved · 🟡 Damir re-runs smoke (green?) → fresh-review pass |

| 145 | 2026-07-05 | **PHASE 0 OPUS ROUND 1b — chat-info demo-review polish (Damir, 6 items across the two live surfaces).** All CSS + small-JS, no behavior/contract change. **① Demo takeover covered the mock statusbar** — `.demo-sendpanel` was `inset:0`, so the chat-info/send takeover painted over the 44px `.statusbar` (base view showed it because it's a normal sibling). Demo-chrome fix: `inset:44px 0 0 0`. NOT a component bug — the real app reserves this via native safe-area (Phase 2 #22); the demo now mirrors that. **② Disappearing-messages row was glued to Pay/Request** — the row is a `<button>` whose `border:0` + `padding` shorthands CLOBBERED the `.c-chat-info__body > * + *` section divider (equal specificity, later in source → won), so it alone lost its hairline + top padding. **Wrapped the row in a `.c-chat-info__setting-section` div so the `> * + *` divider lands on the WRAPPER, not the button** — a first attempt restated the divider on the button itself, but `padding-top` inside the button's border-box made its pressed/focus state bleed up into the divider gap (Damir: "tap area + pressed state off"); the wrapper keeps the interactive box tight to the ~44px row. **③ Disappearing sheet — tapping shifted the label**: `setLoading` PREPENDED the spinner (first flex child) → label jumped right, spinner on the left. Rebuilt the option: label in its own span + a fixed right-hand `__sd-status` slot that holds the success check OR the spinner, so the label never moves and loading lands where the check will be; check recolored `--icon-action-default` → **`--icon-success`** (theme-correct: light 600 / dark 400). **④ Address value now sits on its OWN chip** (`--surface-input`, member-sheet parity) with a **32px icon copy button** (was a 44px text button that sat misaligned) — Damir: it should stand out from the section like the contact sheet. **⑤ QR/payments expand flickered** — `.u-scroll` never actually set `scrollbar-gutter` (the #41 comment promised "gutter always reserved so content never shifts" but the property was missing), so an appearing thin scrollbar reflowed content. Added `scrollbar-gutter:stable` inside the `(hover:hover)` guard (touch keeps transient overlay). **GLOBAL utility fix** — helps every `.u-scroll` region; eyeball for unintended desktop gutter shifts. **+7 smoke assertions** (2 functional: spinner in the check slot + label stays first, disappearing row wrapped in its divider section; 5 static text guards: demo inset, setting-section wrapper, sd `--icon-success` + `[data-loading]` check swap, address `--surface-input` chip + 32px copy, `scrollbar-gutter:stable`). **Needs a bundle rebuild (chat-info.js changed): `node scripts/build-demo-bundle.mjs` then `node scripts/smoke-test.mjs`.** **Eyeball:** `src/components/chat-info.js` · `src/styles/components/chat-info.css` · `src/styles/base.css` (global) · `src/demo/chat.html` · `scripts/smoke-test.mjs` · regenerated `spixi.iife.js`. | Damir demo review (6 items + 2 screenshots) | 🟡 Damir runs build + smoke → paste output → visual re-check on the demo |

## How to use this file

- Adding a decision: append a row, keep rationale to one line, link the doc with details.
- Changing a decision: mark old row ❌ with "superseded by #N", add the new row. Never delete.
- Reviews (Damir, BE, second AI) check 🟡 rows first.
