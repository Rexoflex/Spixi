# CLAUDE.md — Spixi Frontend Redesign

Orientation for any AI or human picking up this work. Keep it current; keep it short.

## What this is

Rework of the Spixi MAUI app's WebView frontend: consolidate 29 HTML pages → 9 flow shells, drop Bootstrap/jQuery/Font Awesome, move to Vite + vanilla JS with a token-driven stylesheet and inline SVG icons. Branch: `redesign/frontend`. The C# side stays as-is; new bridge commands are proposed only.

## Ground rules

- **★ PARAMOUNT SECURITY INVARIANT (read first — overrides everything below): Chat, and ANY surface that renders untrusted/remote content, ALWAYS lives in its OWN dedicated WebView, isolated from the wallet and every other pane.** Never compose chat into a shared WebView/DOM/JS context with other surfaces (incl. desktop split-view panes). On desktop each pane = its own native WebView; cross-pane coordination goes through C# (`selectChat`/`selectTx`), never shared JS. A chat exploit must never be able to read wallet, keys, or other-pane data. The ARCHITECTURE §8 "Hosted panes" single-host-WebView idea is **REJECTED** — see SECURITY.md §1 + DECISIONS #220. Desktop "demo parity" (the one-page `desktop.html`) is art-direction ONLY, not production architecture.
- **★ C# TOUCHES NO RISKY PARTS.** Any C#/bridge work must NOT: sign/broadcast a tx from WebView-composed data without a NATIVE confirm · move keys/passwords/seed across the bridge or into the WebView · feed a WebView-supplied path/filename into a filesystem op (C# names its own temp files) · wire a JS bridge between chat and other panes (breaks §1) · extend the password-over-URL pattern · auto-fetch remote resources that leak IP without the media-autoload gate. If a task appears to need any of these → STOP, don't code it, log it in `docs/security-review-for-be-engineer.md` (the doc Damir shows the BE engineer). Shipped + planned C# risk review lives there.
- **★ SECURITY HANDOVER GATE (Damir, 2026-08-15) — THE REDESIGN MUST INTRODUCE NOTHING.** Before the app goes to the BE engineer for review, an **introduced-vs-inherited security sweep** runs over the whole delta from the fork point `0e85a4b8`. One question per finding: *does this exposure exist at the baseline?* **No → we introduced it → we FIX it before handover. Yes → legacy → it goes to him untouched.** He must see only his own legacy issues, never ours. Scope, method and the running "ours" list live in `docs/security-handover-gate.md` — read it before any batch that adds a verb, a `spixi.*` storage key, a WebView setting, an HTML sink, a network fetch, or a log line. **Apply the lens WHILE building**, so the sweep finds nothing; the sweep is the gate, not the design step.
- **Plan before building.** Nothing ships without a doc reviewed by Damir + BE engineer (+ a second AI review pass).
- **Bridge protocol is frozen** unless BE approves a new command (ARCHITECTURE.md §8). Existing `ixian:` commands and `executeUiCommand` calls must keep working.
- **Security is non-negotiable** — see SECURITY.md. Shells emit payment *intent*; only C# signs/broadcasts. No keys/passwords in the WebView.
- **Figma is direction, not gospel.** Tokens are the source of truth once locked; adapt layouts where implementation reveals better options, and flag deviations.
- **WebView baseline is conservative CSS.** Flag modern features per-case at demo time.
- **Demos run in a plain browser** via the mock bridge, and are mirrored to Figma.
- **Every doc is concise.** Short tables over prose. If a fact is verifiable in source, cite the file:line.
- **★ LANGUAGE RULE (Damir, 2026-08-13; SCOPED 2026-09-23, DECISIONS #931): Write chat replies to Damir in ASD-STE100 Simplified Technical English.** The rule applies to chat replies only; docs, code comments, commit messages and handoffs are exempt. Write short sentences. Use a maximum of 20 words in a procedural sentence. Use a maximum of 25 words in a descriptive sentence. Use the active voice. Give only one instruction in one sentence. Use one word for one meaning. Do not use slang, idioms, or metaphors. Do not use noun clusters of more than three words. Keep technical names, verb names, file names, and token names as they are.

## Doc index

| Doc | Purpose |
|---|---|
| `DECISIONS.md` | **Decision log — read before changing anything.** Every locked/provisional decision with rationale |
| `ARCHITECTURE.md` | Bridge command inventory, per-view data contracts, 29→9 consolidation, stack, i18n plan, proposed commands, BE findings |
| `SECURITY.md` | Wallet/payment isolation invariants every shell must preserve |
| `CLAUDE.md` | This file — orientation + workflow loop |
| `docs/security-review-for-be-engineer.md` | **Security handoff for the BE engineer** — shipped-C# verdict + planned-C# risk ranking + the "C# touches no risky parts" rule. If Damir mentions the risks / showing the BE engineer / the wallet wall-off, point him here. |
| `docs/security-handover-gate.md` | **★ The pre-handover gate.** The introduced-vs-inherited sweep: scope, method, output format, and the running list of exposure WE introduced (fix before handover) vs legacy (his). |
| `docs/audit/bridge-audit-A.md` | Source-level bridge audit: Chat, Contacts, Home, Launch, Wallet |
| `docs/audit/bridge-audit-B.md` | Source-level bridge audit: Settings, Scan, MiniApps, Downloads, Dev, Contributors + base class |
| `docs/audit/assets-audit.md` | HTML/JS/CSS inventory, localization mechanism, duplication analysis |
| `DESIGN_SYSTEM.md` | *(pending)* Tokens + component inventory with variants/states |

## Workflow loop (per unit of work)

1. **Scope** — pick the smallest next unit (a shell, a token group, a component).
2. **Draft doc** — write/update the relevant concise .md before or alongside the work.
3. **Build** — against the mock bridge; runnable in a browser.
4. **Verify** — check against the command inventory + SECURITY.md checklist; note file:line for any source claim.
5. **Review** — Damir + BE, plus a second-AI pass. Capture corrections in the doc, not just the code.
6. **Commit** — one logical unit per commit; Damir reviews the diff in GitHub Desktop and pushes.

**Hard rule: every significant decision gets a row in `DECISIONS.md` when it's made** — architecture, naming, conventions, scope. Reviews check 🟡 (provisional) rows first. Superseded decisions are marked, never deleted.

**Hard rule: audit loop per milestone (DECISIONS #46).** Whenever a component set + its behavior is complete, or a major feature lands: spin up an audit agent (read-only, findings with file:line) → fixer agents (disjoint file scopes, explicit cross-file contracts) → adversarial reviewer agent → loop fix↔review until CLEAN. Mechanical fixes land directly; architectural findings become 🟡 DECISIONS rows, never silent changes. Rebuild generators + run the jsdom smoke test between fix and review passes.

## Status

- [x] Repo audited; `ARCHITECTURE.md` written and committed
- [x] `SECURITY.md` written
- [x] `CLAUDE.md` written
- [x] Assume ARCHITECTURE §5 (consolidation) and §8 (new commands) approved — proceeding
- [x] Figma access working (Components page node `9275:74`); variables sampled across Buttons/Text fields/Nav/Chat sections
- [x] `docs/tokens-analysis.md` written — token issues + proposed 2-layer structure (primitives + semantic aliases incl. topbar/bottombar/bubble tokens)
- [x] Token architecture decided: primitives → keys (light/dark modes) → semantic aliases; numeric naming (`spacing/16`); single `action` role; cleanup happens in Figma (source of truth)
- [x] `DESIGN_SYSTEM.md` drafted — ~55-component inventory (18 in Figma, 8 partial, ~29 missing) + missing token groups (z-index, elevation, motion, sizes, layout)
- [x] Damir restructured Figma to primitives (`Spixi - primitives`) → `keys` (mode-less) → `tokens` (light+dark modes live HERE, per token); `keys-responsive` holds desktop/mobile type + spacing; `typography` composes type tokens
- [x] Full 443-variable dump taken; missing variables written via connector: scale fills, numeric spacing renames, size/* keys, layout/* dims, `region` semantic tokens (topbar/bottombar/bubbles/input/qr/presence/progress), pattern-fix renames — see DESIGN_SYSTEM.md §2
- [x] Buttons decision: 3 heights named by value (32/44/56) × width hug/full/fixed; component workflow: Claude builds structure+bindings via MCP, Damir polishes, Figma native AI = exploration only
- [x] 3 mis-bindings fixed in Figma (outline/info dark → info/300; icon/on-action dark → neutral/950; accent-hover dark → accent/200)
- [x] `src/styles/tokens.css` generated (375 custom properties: primitives → keys → tokens light/dark + code-only z/motion/elevation/opacity/scrim); validated — no undefined refs, conservative CSS only
- [ ] **TOKENS LOCKED pending Damir's final look in Figma + tokens.css commit**
- [x] Full Figma sweep done → `docs/figma-sweep.md`: 27 component sets + chat composites (message/payment/app/call bubbles ✅) + 6 screens (Chats/Wallet/Apps×2/Account/Conversation, mobile light). Build split agreed: Claude = structure/bindings/screen drafts + code-first utility screens; Damir = polish + brand-heavy launch screens; dark mode = token swap + verification only
- [x] `layout/bubble-max` (320) added — chat bubble max width; content-max = desktop pane, distinct
- [x] WEB code syntax set on 425 variables (Dev Mode shows exact tokens.css `var(--…)` names)
- [x] **Remote-binding repair (important):** components/screens were bound to a stale REMOTE library copy of the variables — 1,072 bindings repointed to local collections file-wide (Components 991, Screens 81, Chat&Composite already clean; Archive intentionally skipped). Legacy names mapped (`colors/text/01`→`text/neutral/01`, t-shirt radii by resolved value, etc.)
- [x] Buttons in Figma ✅: sets renamed `button/56|32/*`, new `button/44/*` default+destructive (24 variants each, all token-bound, exact 44px). Width = instance auto-layout, not variants. `spacing/10` added for 44 padding
- [x] Tokens added: hero region (+on-hero text/icon), topbar text/icon, interactive hover/pressed/selected, surface/menu, bubble-max — DECISIONS #20–24
- [x] Message context-menu interaction spec (mobile long-press + desktop) → DESIGN_SYSTEM.md §5b; reply/edit = new §8 proposals (DECISIONS #25)
- [x] Button in CODE: `src/components/button.js` + `src/styles/components/button.css` + `src/styles/base.css` + no-build demo `src/demo/components.html` (open directly in a browser; theme toggle included). Syntax + token cross-refs verified
- [x] Button reviewed by Damir → conventions LOCKED (#16, #17); review findings implemented: state layers on outline/text (#28), success morph + width animation (#29), icon API leading/trailing/icon-only (#30). Demos self-contained (#27). All committed + pushed
- [x] App frame part 1: `icons.js` registry (Tabler, sprite source) + `c-topbar` (root/view variants, hero-aware) + `c-bottomnav` (tonal pill, badges, avatar slot) + `src/demo/app-frame.html` (phone frame, hero toggle wired to Wallet tab)
- [x] Damir design review round: fonts embedded + demo rebuilt against real Screens frames (#31); hero gradient tokenized (`--gradient-hero-overlay`); bottomnav equal-width items; filter chips + search field in demo are PLACEHOLDERS (proper c-chip/c-search-field components pending)
- [x] `docs/chat-list-spec.md` — token-exact chat-item anatomy, status-icon colors, timestamp rules, excerpt types, 4 flags for Damir (①–④)
- ⚠️ Icons: Figma icons are FILLED paths (#131415), not stroke Tabler as in icons.js — awaiting Damir bulk-export of icons frame → `src/assets/icons/` → icons.js regenerated from those (canonical pipeline)
- [x] Icons/logo pipeline live: 64 assets in `src/assets/icons/` (Damir export) → `scripts/generate-icons.mjs` → registry (ink→currentColor, brand→icon-accent, logo→currentColor). MISSING: `tabler-icon-search` (temp glyph in demo)
- [x] Chat-list flags resolved: unread = bold + indicator, NO tint (#33) · avatar/48 added · @ indicator unified · deterministic gradient avatars (#34)
- [x] List primitives BUILT: `timestamp.js` (format + single ticker), `avatar.js`, `chatlist-item.js` (indicator/status/excerpt/row) + CSS; demo bundle system (#35: `scripts/build-demo-bundle.mjs` → `src/demo/spixi.iife.js`); Chats demo shows 12 real rows incl. contact-request block, live timestamp buckets
- ⚠️ Sandbox mirror truncates Edit-modified files — generators were run inline / bundle written directly; re-run scripts locally when mirror behaves
- [x] List demo reviewed by Damir 2026-07-02: excerpt glyphs approved; committed. Session continues on his Mac (clone fork → open this file first)
- [x] Avatar gradients now THEME-AWARE (#37 🟡): JS sets hue custom props only, S/L themed in avatar.css per mode — fixes dark-on-dark initials in dark mode; dark pastel values await Damir's look
- [x] Avatar hue hash: avalanche finalizer added (#38) — old hash clustered similar names into an olive band; hues now span the wheel. Bundle rebuilt (19,222 B). If dark mode shows white initials/same avatar colors as light → stale cached avatar.css, hard-refresh
- [x] Mac session 2026-07-02 — review findings done: avatar gradients darkened (48%/34% lightness, was 62%/45%) · muted chat shows BOTH count-muted + bell-off via new `createIndicators()` (chatlist-item.js; single-indicator `createIndicator` unchanged) · Safari fix: `format('woff2')` in base.css (Safari rejects `'woff2-variations'`; demos now work in Safari) · bundle rebuilt via script (18,982 B, 14 exports), jsdom smoke test: 12 rows, QWERTZ = count-muted + bell-off ✓
- [x] Bottomnav selection ANIMATED (#39 🟡): filled icon twins crossfade+scale-pop over outline on select; tonal pill = `::before` layer, grows in (0.85→1)/shrinks out; ink/label transition both ways; `[data-dual]` guards items without a twin (account keeps outline). Generator fix #40: fill-less Tabler paths → explicit currentColor (apps/messages-filled were invisible); registry 67 icons
- [x] Scrollbars tamed (#41 🟡): `.u-scroll` utility in base.css — thin themed thumb, invisible until hover/focus-within, reserved gutter; mobile WebView native overlay indicators untouched. Demo `.content` regions use it
- [x] Bottomnav badge scope decided (#42 🟡): Chats only, numeric via existing `setUnreadIndicator`; no wallet/apps badge (tx/files/invites arrive as chat messages — would double-signal; no "unseen tx" in bridge). BE question open: muted chats excluded from total?
- [x] Chats badge wired in demo (=4, muted excluded per #42); BE muted question added to ARCHITECTURE §9.4; demo rows refactored to `CHAT_ROWS` data array
- [x] Hover coverage rule (#43 ✅): every interactive gets `:hover` from #23 tokens inside `@media (hover: hover)` — added to bottomnav (+ tonal-hover pill), button.css hovers now guarded too; demo placeholders (eye/quick-actions/chips) got interim hovers
- [x] **Code audit done** (2nd-AI pass) → `docs/audit/frontend-code-audit.md`: 40+ findings (drift/inconsistencies/JS/CSS/generators/perf)
- [x] **Audit findings ALL FIXED via agent loop** (3 fixer agents w/ disjoint scopes → reviewer agent → punch-list fixes → re-review = CLEAN). Highlights: selected-hover cascade, -webkit-user-select, .u-scroll fully hover-guarded, initials() Unicode-hardened (ИП/张 work, whitespace safe), timestamp ticker (visibilitychange + minute-aligned + true singleton), morph via transitionend + data-morphing (width transition scoped), free-function APIs `setNavActive/setNavBadge` (#44 🟡), formatCount shared, a11y labels on indicators/badges, strings threading for i18n, generators fail loudly (derived EXPOSE, collision detection, syntax smoke test, non-path fill normalization, stroke theming), font preloads, hover/active source-order fixed everywhere. Deferred logged: #45 ES2020 floor 🟡, #47 icon-registry split 🟡. Audit-loop rule = #46 + hard rule above
- [x] Bottomnav badge recolored (#48 🟡): error role + 2px bar-surface ring — action-blue badge was unreadable on the selected tonal pill (Damir screenshots, both themes); list indicators stay action-blue
- [x] Icon-button hover ink (#49 🟡): topbar actions + demo eye now change color on hover (action ink / on-hero tint), literal `color:` removed from topbar rule (was freezing button hover); hero pins ink + state backgrounds. Change reviewed by agent (cascade/tokens/both themes verified), 2 findings fixed
- [x] Figma file key stored: `cQ8yMZF5R0LGM9O1q9502F` (Components page node `9275:74`)
- [x] **c-chip + c-search-field BUILT** (#50/#51 🟡, spec `docs/chip-search-spec.md`): specs pulled via connector; UX review added selected/disabled chip states, opt-in icons, whole-chip dismiss, 44px targets, clear-on-filled search, layout-jump border fixes; Figma token ERRORS confirmed by Damir (text-field hover `outline-info`, focus `text-action-default` → action outline roles) — MIRROR TO FIGMA. #46 loop ran: reviewer caught `[hidden]`-vs-display:flex (global `[hidden]{display:none !important}` in base.css now), disabled-hover leak, null-safety, clear tap target. Demo: live search filter + exclusive chip group
- [x] Chat-list search UPGRADED (#52 🟡-BE): matches name + excerpt with match highlighting (`highlight.js`, TreeWalker text-node wrap, XSS-safe; `.c-highlight` warning wash in base.css). Full message-history search = new §8 proposal `ixian:search` (histories are C#-side; WebView caching would breach SECURITY.md)
- [x] Theme-switch flicker FIXED (#53 ✅): `.theme-switching` transition suppression during swap (base.css + both demo toggles, two-rAF removal)
- [x] Chip refinements per Damir: selected = TONAL fill (#50 amended); `tabler-icon-x` exported → registry 69, chip dismiss glyph = `x` (16/14)
- [x] **c-badge + c-txlist-item BUILT** (#54/#55 🟡, spec `docs/tx-row-spec.md`): badge = Figma-exact 5 types × tonal/solid, static; tx rows sent/received/pending/failed with `formatTxTimestamp` (absolute, +year if not current), direction labeled for SRs, amounts pre-formatted+u-tabular, failed = struck `text-neutral-disabled` (Figma had raw #6a717c — UNBOUND, fix there). #46 loop ran: badge demo matrix added to components.html, tx-scoped string keys, overflow guards, demo decoupled via data-direction. Wallet demo: 6 rows wired to filter chips
- [x] `scripts/smoke-test.mjs` COMMITTED (13 assertions, computed-style based; `node scripts/smoke-test.mjs [root]`, needs jsdom) — the #46 loop's verification step, no longer recreated ad hoc
- [x] Search clear glyph switched to plain `x` per Damir (was circle-x)
- [x] **Overlays batch COMPLETE** (#57 🟡): c-banner + c-toast + c-callbar built code-first (home session, PC) — spec in overlays-spec.md batch B/C; #46 reviewer ran CLEAN, 4 hardening MINORs landed (callbar pre-enter race + in-place bridge update, toast queue cap/dedupe, reduced-motion queue latency, banner label "Status"). Demo: Banner/Call toolbar toggles, toasts from sheet actions. Bundle appended MANUALLY (PC mirror truncates Edit-modified files — re-run `node scripts/build-demo-bundle.mjs` on the Mac to verify parity; build script FILES already includes the three)
- [x] Topbar view title verified `heading-sm` (Damir ask; root logotype stays heading-md)
- [x] Damir overlay/title review round: ALL titles unified to heading-sm incl. root logotype + hero (#58) · connectivity → topbar title-state decided, banner = actionable only (#59 🟡, §8 `showWarning(text, kind)` proposal added) · modal side-by-side confirmed for short labels (#60, spec flag ② resolved)
- [x] **Figma mirroring PART 1 done** (#61): wash tokens 3-tier + code adoption · #51 text-field rebinds · #54 failed-amount role fix · #48 nav badge (error+ring+ink) · #49 verified already-correct in Figma · #58 wordmark + Wallet hero title → heading/sm style
- [x] **Figma mirroring PART 2 done** (2026-07-03, #61 updated): chips → 10 variants/set (selected axis + disabled) · "Overlays" section with c-banner/c-toast/c-callbar/c-sheet/c-modal reference components (token-bound; modal uses live button/44 instances) · elevation/2+3 effect styles
- [x] **CHAT ASSEMBLY started** (#62 pipeline: build N ∥ audit N−1). Interview done: grouping #63 · composer mic-flagged #64 + voice §8 proposal · reactions overlap #65 · gaps flagged #66 (+ reaction excerpt type) · chats-list scroll-collapse search + filter chips (All/Unread/Favorites/Groups) #67 + favorites/pinned §8 proposal
- [x] **Batch 1a DONE + audited**: c-bubble (sent/received × single/first/middle/last, meta-in-bubble w/ <time>, labeled status glyphs, identity-hued sender labels via shared hashHue, logical-corner grouping, flow-root, RTL-safe) + c-datesep (shared dayBucketLabel) + `src/demo/chat.html` (group-chat conversation, role=log). Audit: 4 MAJORs fixed (failed/read on-bubble contrast → NEW tokens `--icon-bubble-read/-failed` in tokens.css both modes — **ADD TO FIGMA at next mirror**, clearfix, RTL, hue single-source) + DRY/a11y MINORs. ⚠️ REACTION PLACEMENT CONFLICT parked: design = pill inside bubble; Damir interview = overlapping corner — decide at reactions batch with both on screen
- [x] **Batch 1a review fixes + BATCH 1b BUILT** (#68): failed = outline+retry+caption · chat canvas pattern+gradient (`--chat-pattern-opacity`, `--gradient-chat`) · read = success-green checks · c-topbar chat variant (identity per Damir mock) + setTopbarSub · c-composer (⊕/auto-grow/Enter/mic-flag) · avatar-40. Demo chat.html is LIVE (type+send with on-the-fly grouping, retry toast, voice-flag toggle). Bundle: manual SUPERSEDE section appended — **Mac rebuild normalizes**. Tokens for next Figma mirror: --icon-bubble-read/-failed, chat gradient/pattern opacity, wash usage
- [x] **FULL audit round (#70)** — 3 MAJOR / 9 MINOR / NITs all fixed (IME guard, sender-label AA, composer RTL, canvas contrast, failed r2 clean-bubble + width rule, datesep hairline, bubble elevation, sent-meta AA, chat-topbar hairline). Unread divider = frontend-only (per-message `read` flags exist) — NO BE work
  - Figma mirror queue (next pass): --icon-bubble-read/-failed · white received bubble · --gradient-chat/--chat-pattern-opacity values · sender-label S/L · sent-meta primary-100 · datesep hairline · chat-topbar hairline
- [x] **BATCH 2 done + audited (#71)**: typed-bubbles.js/css — payment/app/call/file cards + unread divider, all states, bridge-mapped; demo shows the full conversation incl. live download simulation. 3 MAJOR audit finds fixed (received-payment side, dark track, progress a11y)
- [x] **FULL chat audit r2 (#72, Damir-ordered, 3 adversarial agents)** — report `docs/audit/chat-full-audit-r2.md`. 7 MAJORs FIXED: ts/locale crash guards (`docLocale()` now shared from timestamp.js) · loading=disabled (keyboard re-fired processing Pay) · oneShot/reentryGuard action guards (**shell contract: bridge re-render replaces latched cards**) · file-bubble state dispatcher + setFileProgress aria/glyph refresh · dark read-check → success-800 · sender-label light L→28%. +14 quick wins. Bundle: AUDIT r2 SUPERSEDE section appended (syntax-checked) — **Mac rebuild normalizes**. Bridge coverage gaps TABLED in the report (setChatMode/channels/contact-request pane/loadmore/status-updaters have NO code home) — sync to ARCHITECTURE §9 + decide updater-API vs full-re-render. Backlog MINORs in report, fold into related batches
- [x] **Session 2026-07-03 (round 2): C17 demo split + STATES MATRIX (#73)** — chat.html = 1:1 phone + group phone + matrix (every payment/app/file/call/reaction/reply state on the real canvas)
- [x] **Pattern premium treatment DONE + LOCKED (#76)**: `scripts/generate-chat-pattern.mjs` → generated `src/styles/chat-pattern.css` (data-URI mask, CORS-clean on file://; paint in generated file, layer in message-bubble.css, fail-soft) · ink = "Primary soft" primary-200/-800 @ 0.5 both modes (Damir via demo switcher) · light `--gradient-chat` v2 sky-blue diagonal (Damir rgba values over surface-screen)
- [x] **BATCH 3 BUILT (#74)**: c-reactions (#65 LOCKED overlap + pop animation) · c-typing · c-scroll-latest (+unread badge) · c-msgmenu (sheet-based §5b 🟡 #75, long-press+right-click, capability-gated) · r2 backlog C8/C15/A17/B4/B6 folded in · #77 amount rule (`formatIxiAmount`, ≤2 dp truncated, round=bare — C# must mirror) · #78 processing Pay = "Processing"+spinner only
- [x] **BATCH 3b BUILT (#79, §8-gated #25)**: bubble reply quote + edited marker · composer context strip (`setComposerContext`/`getComposerContext`) · menu Reply/Edit behind capability getters · demo "Reply/Edit (§8)" toggle simulates the handshake
- [x] **Damir round 3 (2026-07-03)**: pattern ink LOCKED Primary soft @0.5 (#76) · light gradient v2 sky-blue diagonal (#76) · processing Pay label fix (#78) · #65 reactions LOCKED overlap + pop animation · #77 amount rule (`formatIxiAmount`) · **c-mbubble GIF/image tiles, P2P tap-to-load + sender-embedded preview (#81, BlurHash/ThumbHash eval = BE)** · **linkify as button-links + confirm modal, link preview §8 sender-composed (#82)** · **reactions cap 3 types + "+N" → inspect sheet (#83)** · typing pill hugs composer @ spacing-4 (#84)
- [x] **#46 audit loop r3+r4 RUN + CLEAN (#85)** — 7 MAJORs + 11 MINORs fixed (money path, media tile, contrast on the v2 canvas, a11y); backlog items in row #85
- [x] **Damir round 4**: composer ⊕ alignment · file-transfer "keep open" hint · emoji-only big bubbles · C9 accepted (#80 ✅) · declined-call decision + icon-gap list (#87)
- [ ] ⚠️ **FIRST ACTION next session (parked 2026-07-03 — flag immediately):** run `node scripts/build-demo-bundle.mjs` + `node scripts/smoke-test.mjs` — normalizes the manual BATCH 3/3b bundle sections (expected diff: comment/order cleanup + `docLocale` joins the export map). Commit separately as "normalize generated bundle". NOTE: does NOT require the Mac — any terminal with Node on the REAL files works (Damir's PC terminal included); only Claude's sandboxed mount serves stale copies of session-edited files. Damir committed 2026-07-03 (32 files) without this step — bundle works, is hand-maintained until then
- [ ] **Next — CHAT V1 FINALIZATION (scope locked in #86, build in this order):**
  1. ~~In-place updaters~~ DONE (#88): `setMessageStatus` / `setPaymentStatus` (WeakMap re-render, returns new row) / `removeMessage` (grouping repair) — live in chat.html (Pay loop, read ticks, menu Delete)
  1b. **Desktop split-view demo DONE (#89)**: `src/demo/desktop.html` — vertical-bottomnav rail, draggable divider, empty state, 2 wired conversations; #19 pitch evidence, pending Damir look
  2. ~~Secure-chat notice~~ DONE (#91): c-sysnotice, appears when history exhausts (true chat start)
  3. ~~Lazy load-more~~ DONE (#90): attachLazyHistory — auto-fire, spinner, anchor-preserving; live in chat.html (2 pages) + desktop Han (1 page)
  3b. Round-5 adds: reply-quote thumbs/kind glyphs (#92) · cancel/decline working-state loading (#93) · desktop demo FIXED (SpixiIcons bug) — desktop polish pass queued AFTER chat v1
  4. ~~Bot chat + channels~~ DONE (#97): topbar-tap → channel sheet, composer cost line, group-consistent posts — desktop "Ixian News" demo. ~~Attach sheet~~ DONE (#96): tile grid, media tiles flagged
  5. ~~LAST v1 GAPS~~ ALL BUILT (#101) + rounds 8–11 (#99/#102–#105) + **FREEZE AUDIT RUN + CLEAN (#106)**. **CHAT SURFACE V1 = FROZEN pending Damir's commit.** Remaining before shells: bundle normalization run (any Node terminal) · Figma mirror batch (queue in item 9 + #106 backlog) · #105 QR placement pick · #108 mention-@ at chats shell
  5b. Icon exports for the freeze: `shield-lock` (secure-notice medallion — square-asterisk stands in) · illustration language = post-v1 DS decision (#95 notes)
  5c. Wallet-polish queue grew: "Missing a transaction?" chip-trigger + explainer sheet (#98, Damir design analyzed)
  6. Contact-request pane — 🟡 Damir decides minimal-pane vs list-only (#86)
  7. Voice-note lifecycle design (#87④, with the voice §8 batch) · self-destructing messages (BE eval #87③)
  8. ~~Icon exports~~ DONE 2026-07-03 (registry 75): arrow-back-up (menu Reply wired) · trash (menu Delete wired) · player-pause · phone-x · arrows-maximize · hourglass-empty (self-destruct) — voice = existing `microphone` (Damir; no suitable waveform in Tabler). STILL PENDING: `user-circle-filled` (Damir preps later; avatars are user-generated so default-state only)
  9. Figma mirror queue: batch-2 cards · #70 list · r2 deltas · reactions OVERLAP redesign (#65) · --chat-pattern-ink + gradient-v2 candidates (#76) · --text-on-scrim/--icon-on-scrim (#85) · dark error-badge recipe fix (#85 backlog, w/ bottomnav #48) · reply/edited/typing/scroll-latest/msgmenu/media reference components
  10. Then: desktop split-view demo (#19 pitch) · chats-list upgrades at home shell (#67)
  11. **On Mac: `node scripts/build-demo-bundle.mjs` + `node scripts/generate-chat-pattern.mjs` + smoke test** (PC mirror stale for Edit-modified files; bundle carries manual BATCH 3/3b sections)
  12. UX review checklist (Damir eyeball): pattern ink visibility on v2 gradient bottom (#85 backlog) · datesep/divider at v2 top · dark-green read checks · sheet-vs-anchored menu (#75) · typing hug spacing-4 (#84) · composer ⊕ after the 2px fix
- [x] `tabler-icon-search` exported by Damir → registry regenerated (68 icons), temp glyph removed from demo
- [x] Stale `.git/index.lock` (0 B) removed — was blocking GitHub Desktop commits
- ⚠️ Sandbox note (Windows session): mounted-folder cache served stale copies of freshly edited files. Mac mirror verified OK 2026-07-02 (bash sees fresh edits; scripts run normally)
- [x] **Mac session 2026-07-05 (fable): CONTACTS BATCH BUILT (#153, Phase 1 #2)** — `contacts-shell.js/.css` (NEW files only, #152 merge-safety honored): FAB picker (purpose 'start') + topbar-Contacts DIRECTORY (purpose 'directory', tap = details) · add-contact (debounced checkAddress ✓, 20–128 gate, request latch → open convo) · two-step group creation (multi-select → setup: avatar/name/chips/blind toggle; `:|` name hazard gated, bridge-audit-A:544) · `createPendingContact` minimal non-friend profile (ONE Cancel-request action — undorequest removes the friend, bridge-audit-A:86) · Create group NOT in the directory (fable call, spec §1). Smoke +40, full suite CLEAN. Spec `docs/contacts-spec.md`; audit loop → **Opus** via `docs/opus-contacts-audit-brief.md` (not run here, Damir call)
- [x] **WORKFLOW UPDATE: Mac sandbox mirror serves FRESH session edits (re-verified 2026-07-05)** — bundle rebuild + smoke now run IN-SESSION on the Mac (jsdom installed in gitignored `node_modules/`); Damir's local run = pre-commit confirmation only. PC sessions stay no-sandbox (#142). The parked ⚠️ bundle-normalization FIRST ACTION ran this way (BATCH 3/3b manual sections normalized) — commit "normalize generated bundle" separately
- [x] **Mac↔PC MERGE done + reconciled (#157)**: 2-parent merge verified (196 exports, smoke 306 GREEN); duplicate keep-both DECISIONS rows renumbered monotonic (park #152→#154 · contacts #153→#155 · contacts-Opus #154→#156); merge-safety rule LIFTED. Pre-merge docs still cite the OLD numbers — map via row #157
- [x] **PC session 2026-07-05 (fable): SCAN BATCH BUILT (#158, Phase 1 #3)** — `scan-shell.js/.css` (full takeover: prompt/denied/scanning, torch `aria-pressed`, one-shot decode w/ hostile-payload gates, `ixian:qrresult:`/`ixian:back` only) + chats.html wiring (add-contact stub → real takeover, auto-fill + return) + ~22 smoke assertions. Spec `docs/scan-spec.md`. PC session → build+smoke = DAMIR local (#142), then Opus loop via `docs/opus-scan-audit-brief.md`
- [x] **PC session 2026-07-05 (fable): LOCK BATCH BUILT (#159, Phase 1 #4)** — `lock-shell.js/.css` (unlock/confirm boot takeover, §3 no-callback auto-release contract, encpass w/ delimiter hazard + scrub coverage) + settings hub `onChangePassword` row (first sanctioned settings-file edit post-#157) + settings.html demo + ~24 smoke assertions. Spec `docs/lock-spec.md`; SECURITY.md checklist in spec §5. Set-lock = absorbed by hub switch
- [ ] Next: Damir build+smoke + demo pass (scan-spec §5 + lock-spec §6 flags) → **COMBINED Opus loop over #158+#159 via `docs/opus-scan-lock-audit-brief.md`** → Phase 1 #5 Launch/onboarding (last Phase 1 surface; brand-heavy, code-first draft + Damir art-direction, `illustrations-plan.md` + backup onboarding tail fold in)
- [ ] Then: Phase 2 cross-cutting (desktop split-views · dark verification · a11y/copy sweeps · B2 icon exports incl. bulb/torch, user-plus, world, lock, shield-lock, user-circle-filled)
- [x] **i18n EXTRACTION BATCH done (#166, pre-Phase-3):** `scripts/extract-strings.mjs` sweeps all components → canonical `src/strings/en-us.js` (600 keys, 113 mapped to legacy SL ids, 487 new) + translator context sheet `docs/i18n-strings.md`. Provider `src/strings/index.js` + demo global `src/demo/strings.iife.js` (`build-strings-iife.mjs`); desktop.html boots from the dictionary (`?pseudo` marker locale). Desktop Theme/Language text-match intercept → **structural `data-setting-key` hook** (settings-shell `settingRow`). Leak gates: `i18n-lint.mjs` CLEAN + `pseudo-locale-smoke.mjs` 9/9. Flagged: **25 fallback conflicts** (same key, different English — resolve at source; `extract --check` gates). Absorbs the parked launch SL extraction. 🟡 Damir runs extract→iife→bundle→smoke on PC; wire remaining demos (one-liner each); resolve conflicts
- [x] **PHASE 3 item 1 (#172): ARCHITECTURE §9.5 re-synced into the ONE BE-review table** (50 rows, Kind legend, consolidates #120–#171 over the #119 sync; §8 +5 fully-specified rows: sendlog · Share app · OS share sheet · settings pref family · addApp publisher). Adversarial completeness/ref review CLEAN
- [x] **PHASE 3 item 2 (#173): `src/bridge/` native adapter BUILT** — core transport (raw `ixian:` emit · executeUiCommand dispatcher w/ 3 deliberate legacy divergences · onload latch · SPIXI_ENV capability handshake) + SCAN & LOCK/ENCPASS reference adapters (scan = first C# repoint target, Damir). ENC_DELIM exported from lock-shell (one truth). Spec `docs/native-bridge-spec.md`; smoke +~35. ⚠ PC mount served corrupted large-file reads (#165 class) — Damir runs bundle+smoke locally
- [x] **PHASE 3 item 3 (#175): i18n LIVE — `window.SL` runtime.** `src/components/strings-runtime.js` (`getStrings`/`setStrings`); 92 component `strings = {}` defaults + 10 shell `opts.strings || {}` sites → `getStrings()` (auto-pick `window.SL`, explicit arg still wins, unset → English fallbacks); 9 demos wired (`strings.iife.js` + `window.SL` boot, `?lang=` switch, `?lang=pseudo` leak test); spec `docs/i18n-wiring-spec.md`; 0 fallback conflicts; #46 audit CLEAN (1 coverage-gap fix). No C# changes; bridge frozen. ⚠ **Damir runs locally** (sandbox mount truncates large files): `node scripts/extract-strings.mjs && node scripts/build-locales.mjs && node scripts/build-strings-iife.mjs && node scripts/build-demo-bundle.mjs && node scripts/smoke-test.mjs` — then demo pass (`?lang=de-de` / `?lang=pseudo`) + commit
- [~] **PHASE 3 item 4 IN PROGRESS (#176): build pipeline = extend custom scripts, NOT Vite** (spec `docs/build-pipeline-spec.md`). `scripts/build-shells.mjs` + `scripts/lib/inline.mjs` authored: inline demos → self-contained shells → `Spixi/Resources/Raw/html` per §5. **Stage 4a: mock-data drop-in, ZERO C# change, git-reversible.** ⚠ **Damir runs locally** (mount corrupts node round-trips): `node scripts/build-shells.mjs` (default = chat + home) → build/run net10.0-windows → maui-integration-test-plan §7 checklist. scan/contacts/lock need dedicated `src/shells/` entries at Stage 4b (native.js + setRoute).
- [x] **STAGE 4b BRIDGE WIRING PROVEN ON WINDOWS (#177):** `src/shells/{home,chat}.html` wired to the real C# bridge — real account chats + 1:1 conversations render + send/receive works, single-pane AND desktop dual-pane, ZERO C# change. Key fix: fire `bridge.ready()`/`ixian:onload` on window `load` (not during parse) — premature signal raced C#'s push → empty-until-refresh. Payments/files/groups/reactions = v1 stubs. Desktop split reparents the WebView (HomePage:778) — being redesigned anyway, not the target.
- [x] **CHAT DEEPENING batch 1 (#178): FILE TRANSFER + GROUP SENDER LABELS wired into `src/shells/chat.html`** (bridge frozen, zero new verbs, zero C# change). Row model generalized to typed `kind` ('text'|'file'); `addFile`/`updateFile`→`createFileBubble`+`setFileProgress` (fileIndex map, accept/open verbs, offer→progress→complete routing); `updateGroupChatNicks`+grouping fix → per-sender labels/avatars. #46 audit ran **CLEAN, zero findings**. createFileBubble/setFileProgress already in the shipped bundle (no rebuild for the shell). 🟡 Damir F5-tests in the WinUI app.
- [x] **NEXT-BATCH CONTRACT FINDINGS logged (#179):** payments/apps/calls/reactions are NOT clean drop-ins — legacy payment/app cards are **view-only** (`ixian:viewPayment`/`joinApp`/`installApp`; inline Pay/Join would need a NEW §8 intent command, C# signs — SECURITY.md); `status`/`statusIcon` are localized strings not enums (fragile mapping); `addCall` is lossy (freeform message + declined bool only, no call-back verb); `addReactions` is display-only. 🟡 Damir + BE decide §8 inline-Pay/Join + enum-status + richer call event, vs view-only v1, BEFORE that batch.
- [x] **STRATEGY (Damir 2026-07-07): ship every ZERO-C# type now → whole app working + tested → THEN one focused BE pass.** Deferred C# asks accumulate in `docs/be-cutover-brief.md` (C1–C7).
- [x] **CHAT DEEPENING batch 2a (#180): APP INVITES + REACTIONS wired into `src/shells/chat.html`** (zero C#, frozen bridge). Apps: `addAppRequest`→`createAppBubble`, reuse `installApp`/`joinApp`. Reactions: likes interactive (toggle → `contextAction:like`, keyword confirmed xaml.cs:1045), persisted across re-renders; tips omitted (txid not amount) + no `own` flag (both → BE brief). #46 audit **CLEAN**. Spec `docs/chat-batch2-spec.md`.
- [~] **DEFERRED (need C#, brief C1–C4): payments** (statusEnum+kind, fiat+insufficient, inline-Pay decision — component assumes request-in Pay/Decline the frozen bridge can't feed) · **calls** (enrich lossy `addCall`). Not shipped fragile.
- [x] **Chat shell polish (#181): secure notice + center-when-short layout** (Damir screenshot). `createSystemNotice` always first in the log (mirrors legacy `private-messages-note`, was missing); `#messages` flex column + balancing `::before`/`::after` auto-margins → notice/log centered on new chats, rises + scrolls (newest bottom) as it fills. Zero C#. 🟡 Damir F5 visual check + confirm security URL.
- [x] **WALLET TAB wired (#182)** — breadth pass, first non-chat surface on the real bridge. Wallet = tab2 INSIDE `src/shells/home.html` (ARCHITECTURE §5 — not a separate page; a standalone wallet.html would get no pushes); bottom nav swaps chats↔wallet client-side (apps/account = neutral placeholder). Wired pushes: `setBalance`/`setHideBalance`/`clearPaymentActivity`/`addPaymentActivity`/`selectTx`. Emits `ixian:sendixi`/`receiveixi`/`quickscan` (native signed flows, SECURITY.md — no signing in WebView) · `ixian:filter:<f>` (**bridge round-trip, C# re-flushes**; search client-side) · `ixian:balance:hide\|show` · `ixian:explorer`. Tx tap = in-page sheet (Damir: partial data — addPaymentActivity has no address/fee → omitted). #46 loop **CLEAN, 2 MAJOR fixed**: money un-grouped/un-rounded (ported legacy `amountWithCommas`+`limitToTwoDecimals`) · bare-`null` C# arg crashed `atob`, dropped the whole push → `native.js` guard (helps chat/home too). Component edits (**need bundle rebuild**): `txlist-item.js`/`wallet-shell.js` `timeText` (C# time = pre-formatted string, not epoch) + `unknown` tx badge. BE gaps → be-cutover-brief **W1–W4**. ⚠ Damir: `node scripts/build-demo-bundle.mjs` (components changed) → `node scripts/build-shells.mjs` → `node scripts/smoke-test.mjs` → F5 in WinUI; confirm fiat sign (`+$1,234.50` vs legacy bare) + avatars = gradient fallback. All 10 working-tree files (source + rebuilt bundle/index.html/chat.html + docs) are this ONE #182 batch; prior chat batch #178–181 already in HEAD (534c1e6b).
- [x] **SETTINGS/ACCOUNT HUB wired (#183)** — `src/shells/settings.html`, first STANDALONE separate-page shell on the real bridge (loaded by `SettingsPage`, ARCHITECTURE §5 #8; ZERO C#, frozen bridge). Wired pushes `setNickname`/`setAppearance`/`setLockEnabled`/`showRemoveAvatar`/`loadAvatar`/`onBack`; emits `ixian:save`(persist-on-EXIT, since it also POPS)/`back`/`avatar`/`remove`/`appearance:<int>`/`language:<legacy-code>`/`lock:on\|off`/`backup`(→legacy BackupPage)/`deleteh`/`deleted`/`deletea`/`delete`. OMITTED (no SettingsPage support → hidden): QR/own-address · notifications/privacy/security/payment-auth (§9) · change-password (encpass not routed here) · downloads/dev/contributors (HomePage-driven, not repointed) · version · chat-appearance. Component edit: `createSettingsHub` gained optional `onBack`→topbar back (standalone page, no desktop hardware back; **needs bundle rebuild**). `build-shells.mjs` manifest repointed `settings`→`src/shells/settings.html` + added to DEFAULT (chat+home+settings). #46 audit CLEAN post-fix (1 real bug: pick→remove→exit resurrected the removed avatar via a stuck `dirtyAvatar` → fixed). BE gaps → be-cutover-brief **S1–S6**. ⚠ sandbox mount truncates large-file reads (#175) → node/jsdom impossible in-session; shell inline `node --check` green + strict inliner resolves all refs. 🟡 **Damir: `node scripts/build-demo-bundle.mjs` → `node scripts/build-shells.mjs` → `node scripts/smoke-test.mjs` → F5 SettingsPage in WinUI.**
- [x] **APPS TAB wired (#184)** — breadth pass, `src/shells/home.html` tab3 (ARCHITECTURE §5 — internal HomePage view like wallet, not a separate page). Pure shell wiring, ZERO C#, **NO component change → no bundle rebuild this batch**. Wired pushes `clearApps`/`addApp(id,name,icon,publisher,isSingleUser,isMultiUser)` (6-arg — publisher→creator, so spec's §8 "add publisher" is already satisfied; icon path ignored → gradient tile). Reuses `createAppsList`/`createAppsHeader` (list⇄grid + client-side search + empty state). Tap → `ixian:appDetails:<id>` (**NOT `ixian:details:` — that's CONTACT details inside HomePage**; HomePage:274 vs :408); Add → `ixian:newapp`. DEFERRED to the standalone apps-shell repoint: Discover feed (Explore banner CSS-hidden, be-cutover **A2**), direct-launch+recents, ⋮ menu OFF (list-scoped uninstall has no verb, be-cutover **A1**). #46 loop **CLEAN, 1 MAJOR fixed**: dead ⋮ `menuBtn` still rendered under `appMenu:false` (only handler was guarded) → shell CSS hides it; root-cause component fix (guard `menuBtn` on `onMenu`) tracked in be-cutover. Sandbox mount TRUNCATES `home.html` (510/622 lines, #175) → verified via file tool + strict inliner. 🟡 **Damir: `node scripts/build-shells.mjs` (no bundle rebuild) → `node scripts/smoke-test.mjs` → F5 Apps tab in WinUI** (installed apps render as gradient tiles, list⇄grid + search, tap→details, +→AppNewPage).
- [x] **LAUNCH/ONBOARDING shell wired (#185)** — the LAST breadth shell, `src/shells/launch.html` on the real bridge (ZERO C#, frozen). Launch is FIVE separate C# pages (intro/intro_new/intro_restore/intro_retry/onboarding), so ONE shell source is dropped in over all five filenames by `build-shells.mjs`, each booting its own view via injected `window.__LAUNCH_VIEW__`. **welcome** (LaunchPage): CTAs emit `ixian:create`/`ixian:restore` to NAVIGATE to the real create/restore pages (new additive component hooks `onGoCreate`/`onGoRestore`); `ixian:accept` recorded once at the first CTA tap (only page that handles it); `ixian:language:<code>`/`ixian:appearance:<int>` (C# reloads); ready = `ixian:introload`. **create** (LaunchCreatePage): `ixian:avatar` · `ixian:create:<nick>:<pass>` (indefinite loading — C# navigates on success / native-alerts on failure = the wedge, L1) · `ixian:back`; ready = `ixian:onload`; pushes setVersion/loadAvatar. **restore**: `ixian:selectfile` · `ixian:restore:<pass>` · `ixian:back`; pushes setUploadedFileName(basename)/showPasswordError→`fail(msg)`/removeLoadingOverlay→`fail('')` (one-shot latch); no ready verb. **retry**: `ixian:proceed:<pass>` · `ixian:back`; removeLoadingOverlay→silent `fail('')`; no ready. **tail** (OnboardPage): `ixian:joinbot` OR `ixian:finish` (**mutually exclusive** — joinbot already finishes+pops; guarded so Join doesn't double-fire); "Back up now" has no OnboardPage verb → advances only (L3). Outgoing serialized through a queue (accept+create both deliver on a single-navigation WebView) via `bridge.send`. Component edits (**need bundle rebuild**): `launch-shell.js` welcome-CTA nav hooks + autoplay gated to welcome boot. #46 loop **CLEAN, 1 MAJOR fixed** (tail joinbot+finish double-pop → flag guard). BE gaps → be-cutover-brief **L1–L4**. ⚠ sandbox: inliner + component syntax verified in-session; large-file node/jsdom is a local step. 🟡 **Damir: `node scripts/build-demo-bundle.mjs` (launch-shell.js changed) → `node scripts/build-shells.mjs launch` (writes all 5 files) → `node scripts/smoke-test.mjs` → F5 the full create/restore/onboarding flow in WinUI.** Handoff: `docs/handoff-launch-next.md`.
- [x] **SINGLE-PANE GAP AUDIT + DEPTH PHASE (2026-07-07, `docs/gap-audit.md` + finalization-roadmap **Phase 3.5**).** Damir flagged that breadth-wiring left lots missing even for single-pane (FAB dead-ends, send-payment on legacy, settings not saving, no QR in account, no auto-save/toast). 4 read-only agents audited every single-pane surface (chat/chats/wallet/apps/settings/contacts/scan/lock) against the C# contract, file:line. **Decided model (interview):** full redesign coverage — **NO legacy design remains in the final app** (the redesign replaces every legacy page); wire the redesign where the FROZEN bridge supports it, and where BE can't yet drive a feature (e.g. reply/edit) **omit it behind a capability flag — built + ready to switch on when C# lands** (never left on legacy, never shown broken); **build FE now, batch C#**; money = WebView composes / C# signs (native sign step, not a legacy screen); settings = **auto-save + selective toast** (needs a save-without-pop verb); FAB → the contacts shell. **Key findings:** chat payments+calls render NOTHING (stubbed) · whole context-menu/member-admin/chat-info/attach/media surface unwired despite existing C# verbs (zero-C# wins) · retry is a dead no-op (needs C# `resend`) · chats-list contact-request/handshake UI built but UNFED + live `setContactStatus` no-op'd · wallet send/receive/QR fully built but UNWIRED (legacy native carries all money entry) · **settings "not saving" = C# reload-on-theme/language nukes unsaved nick edits + lock not persisted on clean back-exit** · contacts has NO adapter + no legacy host page (architecturally homeless → embed in home shell) · scan+lock are near-drop-ins (adapters+grammar+QR lib present, just need `src/shells/` entries). **Phase 3.5 = 8 tracks (A chat depth · B chats-list · C scan+lock shells · D contacts shell · E wallet money · F settings auto-save · G apps depth · H cross-cutting), zero-C# first, BE asks batched into the one cutover.** 4 residual [DEC]s for Damir (contacts embedding · chat-info takeover · settings_lock retire/redirect · media-vs-file signaling).
- [x] **Track A · A1 (#187): PAYMENT + CALL VIEW-ONLY CARDS wired into `src/shells/chat.html`** (ZERO C#, frozen). Closes the payments/calls-render-nothing black-hole. Payments: 14-arg `addPaymentRequest` (verified xaml.cs:1310/1361) + `updatePaymentRequestStatus`/`updateTransactionStatus` → informational card (amount + status badge from `statusIcon` + Details→`ixian:viewPayment` when `enableView`); role by `localSender` (no inline Pay/Decline — C3); C#-localized `title` passed VERBATIM (only reliable request-vs-payment signal). Calls: best-effort — `declined`→missed (call-back via `ixian:call`), else `(m:ss)` parsed; localized label verbatim. Component edits (**need bundle rebuild**): `typed-bubbles.js` `createPaymentBubble`/`createCallBubble` `title` override + details-link guard + empty-meta guard. #46 audit **CLEAN, 0 MAJOR** (M1 call wording/dir · M2 empty meta · N1 txIndex leak fixed). No new BE ask (C1–C4 already logged). 🟡 Damir: `node scripts/build-demo-bundle.mjs` → `build-shells.mjs` → `smoke-test.mjs` → F5.
- [x] **Track A · CHAT DEPTH A2–A7 + polish (#188), autonomous push to "chat screen complete".** A2 context menu (react ❤️/copy/delete/tip · reply-edit cap-gated) + reaction own-state (`myLikes` interim; C5 for persistence) · A3 in-text link confirm · A4 member sheet + kick/ban (non-blind groups) · A6 attach sheet (File/Pay/Request; media/app gated) · A7 start-call button + connectivity banner + contact-request pane · blank-on-open flash fixed. #46 audit → 2 MAJOR fixed (dead Retry gated behind `cap('resend')` + clean "Not delivered"; typing pill survives re-render). Component tweaks (**need bundle rebuild**): message-menu/attach-sheet/message-bubble (+ #187 typed-bubbles/money). 🟡 Damir: `node scripts/build-demo-bundle.mjs` → `build-shells.mjs` → `smoke-test.mjs` → F5.
- [ ] **DEFERRED from Track A (findings logged, DECISIONS #188 + handoff):** A5 chat-info takeover = **ContactDetails shell repoint** (rich actions live there, not SingleChatPage; header tap stays `ixian:details` interim) · A8 media tiles = media-vs-file BE flag (GIF-link complaint handled by A3 + #82 no-auto-load — **confirm/reopen #82**) · lazy-history = C# full-reflush incompatible with `attachLazyHistory` (needs C# prepend or flush-complete signal) · unread divider = no read-boundary signal. BE-gated flags → cutover: `resend` (C) · inline Pay/Decline (C1–C3) · enriched call (C4) · reaction own-flag (C5).
- [x] **Track B · CHATS-LIST DEPTH — live status + lone-`addChat` fix (#189, zero C#, no component change → no bundle rebuild).** `src/shells/home.html` tab1. (1) **Live `setContactStatus`** (was a no-op stub) → online dot / unread badge / nav badge update every tick, not only on a full flush (C# always sends excerpt=""/ts=0 → pure online+unread refresh; unknown address = contacts-directory-only → ignored). (2) **Lone-`addChat` BUG fixed** — `updateChat` (xaml:1020) emits a lone `addChat` on message events; old handler blind-pushed → **dup row + no render** → new-message excerpt/ts/unread dropped until the next structural flush. Now `addChat` **upserts by address** + schedules a coalesced render when outside a flush; `chatsFlushing` (set across clearChats…clearChatsDone under the same C# `refreshLock`) suppresses per-row renders during a full flush. (3) `clearContacts`/`addContact` kept as documented no-ops (contacts **directory** roster ≠ chats list; Track D). Filter chips: All/Unread/Requests wiring CORRECT in code — Damir F5 "does nothing" = **Groups dead** (no contact-type flag, CH1) + **Requests unfed** (CH2) + Unread previously stale (now live). #46 audit **CLEAN, 0 MAJOR** (landed MINOR-1 rAF-bails-mid-flush + NIT-1 no-0-ts-clobber; MINOR-2 dropped-clearChatsDone + NIT-2 mute-badge folded into CH4). BE gaps → be-cutover-brief **new "Chats list" CH1–CH5** (group flag · contact-request feed+verbs · delete/mark-read persist · pin/mute/favorites+mute-aware total · mention flag). 🟡 Damir: `node scripts/build-shells.mjs` (NO bundle rebuild) → `node scripts/smoke-test.mjs` → F5 home tab; confirm live online/unread + new-message row update (no dup) + All/Unread filter.
- [ ] **Track B remaining (all [BE], gated OFF, built+ready):** CH1 group-type flag → Groups filter · CH2 contact-request feed + accept/decline/handshake verbs (built UI unfed) · CH3 delete/mark-read persistence + history/media wipe · CH4 pin/mute/favorites persistence + mute-aware unread total · CH5 mention flag. Land at the one BE cutover.
- [~] **DAMIR F5 ROUNDS (#190/#191) — chat/chats-list live-app fixes, IN WORKING TREE, UNCOMMITTED.** #190: chat-open FLICKER (fragment/replaceChildren + load-burst gate) · composer autofocus · react/tip on file/payment/app cards · excerpt HTML-entity decode (`&#x2764;`) · counterpart AVATARS (avatar.js onerror→gradient + home-shell wiring — path CONFIRMED resolving on F5) · dead app-invite Decline gated · **remote GIF/image TILES (client-fetch, reopens #82 — needs BE/security sign-off)**. #191: flicker r2 (removed the empty-notice intermediate paint → spinner→conversation one transition) · chat-screen AVATARS (1:1 topbar + group bubbles). Components changed → bundle rebuild needed (avatar.js, typed-bubbles.js). **⚠ FIRST ACTIONS next session:** (1) Damir greenlights → run the **#46 audit** on the F5 batch → fix → (2) Damir `build-demo-bundle`+`build-shells`+`smoke`+F5+commit → (3) next zero-C# batch. Handoff: `docs/handoff-phase35-f5.md`. BE reaffirmed: contact-request rich card in LIST = CH2 · Get-app empty install URL = C7 · outgoing-call-on-left (no direction) = C4 · legacy call banner = N2 · media-vs-file = A8 · website OG meta card = §82 (CORS-blocked client-side).
- [x] **NEXT ZERO-C# BATCH DONE (#192, self-#46 CLEAN, uncommitted).** CH6 excerpt canon (`home.html` `excerptFromRaw` reverse-maps en-us `_SL` phrases + lone GIF URLs → chatlist canon glyphs; degrades to text in other locales) · CH7 composer drafts (`chat.html` save/restore/clear `spixi.draft.<addr>` in localStorage; `home.html` renders "Draft:" excerpt; **`DRAFT_PREFIX='spixi.draft.'` shared contract**) · CH3 two-step delete modal BUILT but **gated `capabilities.delete` OFF in `home.html`** (HomePage has no delete verb → would reappear on reflush; demos pass `delete:true`) · A8b "Open file" label on completed file bubbles. **Components changed (typed-bubbles.js · chats-row-menu.js · chats-shell.js) + demos (chats.html/desktop.html delete:true) → `build-demo-bundle` REQUIRED.** New string keys (inline en fallbacks → next `extract-strings`): `openFile` · `deleteDataTitle`/`deleteDataBody`/`keepContact`/`deleteEverything`. 🟡 Damir: `node scripts/build-demo-bundle.mjs` → `node scripts/build-shells.mjs` → `node scripts/smoke-test.mjs` → F5 (home: canon glyphs + Draft: excerpt; chat: draft restore + clear-on-send; file bubble: Open file) → commit. Sandbox #175: shells validated via file tools (bash/`node --check` see the stale mount).
- [x] **DAMIR F5 ROUND DONE (#193, post-#192, self-#46 CLEAN, uncommitted).** 3 fixes: **(1) outgoing file transfer** (`chat.html`) now shows the progress bar + "keep Spixi open" hint + a one-shot "File sent" success toast + the #192 "Open file" label — `fileStateFrom`/`updateFile` no longer short-circuit own files to complete (the sender gets real per-packet ticks, TransferManager:410); shell-only. **(2) reaction excerpt** — `chatlist-item.js` new `reaction`→`heart` glyph + `home.html` maps a lone-heart excerpt → heart + "Reacted" (best-effort; reactions aren't `lastMessage` → clean signal = BE **CH8**); **component change → `build-demo-bundle`**. **(3) two-step delete made testable** — enabled `capabilities.delete` + a session TOMBSTONE (`deletedChats`) so delete sticks across re-flushes and resurrects on a new message (amends #192's gated-OFF; deeper wipe still **CH3** BE). New string keys: `fileSent` · `reacted`. 🟡 Damir: `node scripts/build-demo-bundle.mjs` (chatlist-item changed) → `node scripts/build-shells.mjs` → `node scripts/smoke-test.mjs` → F5 (send a file · get a like · delete a chat) → commit.
- [x] **DAMIR F5 ITERATION DONE (#194, post-#193, uncommitted).** Independent #46 audit of #192/#193 ran (agent) → its 1 MAJOR (reaction glyph `heart` not registered) fixed. 5 fixes: **(1)** reaction glyph → `heart-plus` (Damir exported `tabler-icon-heart-plus.svg`) + `createExcerpt` guards glyphs on `ICONS[]` (degrade to text until registered) · **(2)** delete modal REDESIGNED — avatar+nickname header + checkboxes (Delete chat / Delete media & files) + step-2 "Delete contact"; `deleteAll`→`deleteContact` + `detail{media}` threaded to onPersist · **(3)** chat THEMING — dark canvas `--neutral-950` + subtle glow (Telegram-like); outgoing bubble NEW `--gradient-bubble-sent` (saturated, **Damir tunes the palette**) + WHITE text (was black) + light status glyphs · **(4)** nameless bot/group sender → address middle-truncated + click-to-copy (`message-bubble.js` `senderIsAddress`) · **(5)** dual-pane selected chat tint → `--surface-action-tonal-default` (was invisible primary-900). **Components changed (chatlist-item · chats-row-menu · chats-shell · message-bubble) → bundle rebuild AFTER `generate-icons`.** New keys: `deleteChatOpt`/`deleteMediaOpt`/`deleteContactTitle`/`deleteContactBody`/`deleteContact` · `copyAddress`/`copied`. 🟡 **reviewer pass over items 2–5 owed before commit.** 🟡 Damir: `node scripts/generate-icons.mjs` → `node scripts/build-demo-bundle.mjs` → `node scripts/build-shells.mjs` → `node scripts/smoke-test.mjs` → F5 → tune sent-bubble gradient → commit.
- [x] **TRACK C — Scan + Lock shells wired (#203, post-#195–#202, last zero-C# surfaces; frozen bridge, NO component change → no bundle rebuild).** Adapters (`scan-page.js`/`lock-page.js`) + vendored `js/html5-qrcode.min.js` already shipped — this batch = the 2 production shells + manifest. **NEW `src/shells/scan.html`** (ScanPage drop-in, mirrors settings_encryption boot): `mountScanPage` on window `load`; html5-qrcode loaded at RUNTIME (injected `<script src="js/html5-qrcode.min.js">`, co-located) not statically → strict inliner keeps the shell lean; missing lib → honest 'denied' CTA. **NEW `src/shells/lock.html`** (LockPage drop-in): boot takeover, always-dark (`#13171b` instant-bg + dark boot spinner; `.c-lock` pins its own subtree over `--gradient-lock`); `mountLockPage` (biometrics←SPIXI_ENV, default false = hidden on WinUI); no-callback unlock (1600ms auto-release); exposes `setJustConfirm`. **build-shells.mjs**: scan+lock → SHELLS + DEFAULT. **settings_lock.html → RETIRE via §5 (Damir [DEC])**: orphaned legacy set-app-lock page (App-lock is now the inline hub toggle `ixian:lock:on|off`→SettingsPage:153; nothing navigates to SetLockPage); NO FE drop-in (redirect unsafe — SetLockPage handles only `ixian:back`); delete SetLockPage/settings_lock.html + HomePage:243 bare-`ixian:lock` branch at the §5 repoint (be-cutover cross-cutting). **Verified in-session:** strict inliner CLEAN on both (no unresolved refs, bundle inlined, html5-qrcode kept as co-located runtime ref) · inline boot JS `node --check` green · smoke 345+ assertions 0 failures incl. lock-shell tests (no component/bundle edit → unchanged). 🟡 **Damir: `node scripts/build-shells.mjs` (NO bundle rebuild) → `node scripts/smoke-test.mjs` → F5 ScanPage (prompt/denied/scan→qrresult) + LockPage (unlock/confirm, wrong-pw auto-release) → commit.** Resolves handoff-next-phase item 1 (scan+lock); remaining Track-C polish = Explore-banner svg. BE: §5 SetLockPage retire logged.
- [x] **AVATAR/APP-ICON PATH RESOLUTION (#204, zero-C#, frozen bridge, next after Track C).** Investigated per-platform `<img src=path>` resolution (WinUI resolves #190 · Android likely — `file:///android_asset/`+`AllowFileAccess*=true` · **iOS/WKWebView the likely blocker** — strict file:// dir sandbox); legacy used the same mechanism so redesign matches legacy per platform. **Damir chose Option A (C# data-URI push)** over B (per-platform virtual-host) → **be-cutover X1**. FE landed zero-C#: apps-tab `addApp` icon threaded (`resolveIcon`, home.html) · CI5 silent drops closed — `createChatInfo` gained an `avatar` param → hero photo (chat.html `identity.avatar`, contact_details `state.avatarPath`+rebuild-on-setAvatar) + per-sender member-row photos (collectGroupMembers carries `rec.avatar`; blind suppresses) + member-sheet avatar · every other avatar surface already threaded `src` (wallet/tip/call have no bridge avatar → gradient correct) · `src/demo/avatar-datauri.html` + smoke block prove a real `data:image/png` renders through every consumer (6/6 green). onerror→gradient added to `c-app-icon`+`createAppBubble` (matches c-avatar; no broken-image glyph). **#46 review CLEAN post-fix** (MAJOR: c-app-icon had no onerror despite home.html promising it — added; `renderAppPlaceholder` renamed to avoid a bundle symbol-collision with avatar.js. MINOR: member-sheet avatar drop — fixed). Components changed (chat-info/apps-icon/typed-bubbles) → **bundle rebuilt in-session** (229 exports, syntax-checked). ⚠ **Mount TRUNCATED large Edit-tool writes this session (#175/#165 class)** — chat-info.js/chat.html/contact_details.html/home.html/smoke-test.mjs (and CLAUDE.md itself, pre-existing from a prior session) were reconstructed from HEAD+edits via node `fs` (writeFileSync writes intact; the Edit tool + Read go through the truncating mount) — on this PC mount, apply edits via node/git and verify, NOT the Edit tool. ⚠ **`.git/index` is CORRUPT** ("bad signature 0x00000000 · index file corrupt") → `git diff`/`stash` fail; fix: `rm .git/index && git reset` (working tree intact, no work lost). 🟡 Damir: `node scripts/build-demo-bundle.mjs` (done) → `node scripts/build-shells.mjs` → `node scripts/smoke-test.mjs` (runs to completion locally — the contacts block's real-timer tail exceeds the sandbox 45s window; 301/301 reachable assertions green + harness 6/6 in-session) → F5 (WinUI: real avatars in chats/chat/contact-details/member sheet + real app-tile icons) → commit. BE: **X1** (data-URI push) at the cutover.
- [ ] Then (AFTER Phase 3.5 depth): **full-app Windows test** · item 5 (C# §5 repoint, BE) · item 6 (Android) → the **one BE cutover pass** (be-cutover-brief: chat C1–C9 · **chats-list CH1–CH5, CH8** · wallet W1–W4 · settings S1–S6 · apps A1–A2 · launch L1–L4) → Phase 4 freeze audit.

- [x] **PHASE 2 A11Y SWEEP DONE (#205, 2026-07-08, zero-C#, one SHELL fix → `build-shells` only, NO bundle rebuild).** Read-only a11y audit (focus/SR) over ALL components + shells → `docs/a11y-sweep-phase2.md`. Component library is CLEAN (every overlay rides `overlay.js`; icon-only controls/decorative imgs/form labels/live regions all correct; no positive tabindex or inline outline:none). One real gap FIXED: the chat SHELL's hand-rolled top-anchored channel selector (Damir Option A) lacked focus mgmt → added focus-move-in + Tab-trap + `focusin` containment + focus-restore + `role=dialog`/`aria-modal` (kept the top-anchored visual; demo already uses the correct `channel-sheet.js`). #46 review → 0 MAJOR; 2 MINOR + 1 NIT fixed (row-select focus race → `focusTitleAfterRender`; `menu`→`dialog` semantics; exiting-scrim `pointer-events:none`). Smoke gained a shell static-read guard (shell isn't jsdom-loaded). 🟡 DEFERRED for Damir: idiomatic ARIA radio-group roving-tabindex + Arrow-keys (`settings-screens`/`settings-shell`/`chat-info` — operable today via Tab+Enter, minor, 5-site behavior change; shared `rovingRadioGroup()` proposed). ⚠ Pre-existing: DECISIONS.md rows 203+204 share one physical line (earlier-session truncation merge — cosmetic, untouched). 🟡 **Damir: `node scripts/build-shells.mjs` (NO bundle rebuild) → `node scripts/smoke-test.mjs` → F5 (keyboard the bot channel selector: Tab stays in panel, Esc closes + restores focus, pick a channel → focus lands on new title) → commit.**

- [x] **ZERO-C# BUG-FIX BATCH DONE (#206, 2026-07-08, Damir F5 list; components+strings changed → FULL build sequence).** 10 fixes, #46 review CLEAN, built + smoke-green IN-SESSION (bundle 229 exports · 14 shells · 314 ✓ · exit-124 = known contacts-timer tail, not a failure). **(1)** contact-details title "Contact details" + Message button (`contact_details.html` context:'contact' + onMessage→`ixian:chat`; `chat-info.js` title + delete-history un-gated → **revises #142**, contact page now keeps BOTH destructive actions + gains Message). **(2)** bot member count kept next to bot name in channel mode. **(3)** full member roster via `ixian:loadContacts` (implemented the no-op `addContact` stub → `groupRoster` → `collectGroupMembers`; blind-safe; per-peer reset) — **corrects be-cutover CI1** (the roster push already existed). **(4)** channel selector `z-index:15` → slides UNDER the topbar. **(5)** GIF/media **load by default** (Damir posture #82; `spixi.media.autoload` pref default-ON + per-peer persisted loaded set fixes "hidden on re-open") + 2px outgoing-colour border on sent tiles. **(6)** `--gradient-lock` softened/desaturated (Damir's dials). **(7)** account default avatar: `settings.html loadAvatar` filters the `img/spixiavatar.png` sentinel → gradient (was showing legacy avatar on WinUI). **(8)** apps-detail icon threaded for parity (gradient until X1). **(9)** settings save stay+Saved+toast BUILT + **gated OFF behind `bridge.cap('settingsApply')`** (new BE row **S14** = save-without-pop verb; today still persist-and-pops). Strings: `contactDetails`, `saved`. #46 fixed 2 MINOR (cross-peer roster/nick leak → per-peer clear) + 1 NIT (avatar `\`→`/`). Backlog: member-list scroll not preserved on roster refresh (minor); media autoload = a fetch per tile (accepted, #82). be-cutover updated (CI1 corrected · S14 · #82 posture note). 🟡 **Damir: `node scripts/build-demo-bundle.mjs` → `build-locales` → `build-strings-iife` → `build-shells` → `smoke-test` → F5 the 10 fixes → commit** (all ran green in-session; local run = pre-commit confirmation). ⚠ NOTE: settings save STILL pops until S14 lands (built + gated, as agreed).
- [x] **DESKTOP SPLIT-VIEW batch (#207, 2026-07-08, CSS/token only → NO bundle rebuild).** Item 1 (wallet/apps/settings split-view panes + `dtOpenSettings`/`dtOpenWalletDetail`/`dtOpenAppDetail` routers in `src/demo/desktop.html`) was already built + Damir-reviewed in the 2026-07-06 demo passes (desktop-split-spec §6b/c/d); this batch finalizes + adds queued **item 2** (desktop-only chat background). Item 2: new `--chat-canvas-base` token split out of `--gradient-chat`'s trailing solid layer in BOTH modes (defaults byte-identical → **mobile chat unchanged**); scoped to `.dt-frame`, `.c-chat-canvas::before{display:none}` drops ONLY the doodle pattern (gradient wash stays) + dark desktop `--chat-canvas-base:var(--neutral-1000)` deepens the ground to grey-1000. Not a `min-width` query (misfires in phone-frame demos). Self-#46 CLEAN. ⚠ sandbox bash/grep served a STALE tokens.css (dark edit invisible + false brace-mismatch) — verified via Read; known #175 mount class. 🟡 **Damir: NO bundle rebuild → `node scripts/smoke-test.mjs` → F5 desktop chat (light: pattern off / gradient stays · dark: pattern off / grey-1000 ground) + confirm MOBILE chat pattern still shows → commit.** Also logged **C10** (fulfilled payment-request = 2 cards, Damir F5 2-machine — a `requestFunds`+`sentFunds` link/UX decision, be-cutover).
- [x] **BE-CUTOVER BATCH 1 RITUAL DONE (Opus batch 2, 2026-07-09, #208–#210) — the owed #46 audit + doc sync for fable's landed rows.** #46 loop over CH1/CH5/CH8/C4/C5/S1/S3/S4 + the `updateMessage` direction fix (3 disjoint read-only auditors + adversarial reviewer) → **CLEAN, 0 MAJOR**. Settings CLEAN (S1 confirmed display-only identity, no wallet access). **3 mechanical FE fixes landed (shell-only → `build-shells`, NO bundle/component change):** C5 own-key authority (`chat.html parseReactions` — bridge own-arg honored exclusively when present, `myLikes` = old-exe fallback) · CH8 tombstone prune (`home.html onPersist`) · CH8 read-clear (`home.html setContactStatus` unread=0). Reviewer PASS both + caught that clearing `reactionExcerpts` in `clearChats` is UNSAFE (CH8 must survive the post-reaction re-flush) → NOT done. **Docs synced:** DECISIONS #208 (batch-1 landed + embedded decisions) / #209 (audit + fixes) / #210 (Damir findings + mentions scope); be-cutover-brief CH1/CH5/CH8/C4/C5/S1/S3/S4 marked LANDED (end-state notes kept) + new **C11** (group delivery ticks, Damir finding 4) + C4 active-call-nudge (finding 3) + CH8 empty-nick fallback (finding 2); ARCHITECTURE §4 push contract updated (`addChat`+kind/mention · `addChatReaction` · `addCall` 7 · `addReactions`+own · Settings +setVersion/setAddress/setLanguage) + §9.5 version/declined-call rows marked landed. 🟡 **Damir: `node scripts/build-shells.mjs` (NO bundle rebuild) → `node scripts/smoke-test.mjs` → F5 (own ❤ persists · counterpart ❤ excerpt clears on read · group/bot in Groups chip · @nick indicator · Account QR/version/language) → commit #208–#210.** ⚠ bash serves STALE files (#175) — all edits via file tools; DECISIONS latest was #207 (bash showed #206).
- [x] **PREMIUM MENTIONS batch BUILT + address-truncation canon ENFORCED (#212, 2026-07-09, FE-only zero-C#; components+shell → FULL build).** (a) @-mention HIGHLIGHT in bubbles (`message-bubble.js`/.css — bold + tonal pill, name-anchored + boundary-guarded so emails/`@bobby` don't false-fire, direction-scoped colour for both bubble sides, self = warning) · (b) @-AUTOCOMPLETE composer picker (`composer.js`/.css — `mentionSource` cb, avatars+filter, ↑/↓/Enter/Tab/Esc+tap, IME-guarded, group/bot only) · (c) @ JUMP-TO-MENTION FAB (`chat.html` — above the scroll-latest chevron; best-practice CYCLE: counts unseen mentions of me, oldest-first per tap + pulse, baseline-seed on open, per-peer reset) · (d) #211 truncation canon: `truncateAddressMiddle` moved to `avatar.js` (no cycle) + used by chat topbar `identityTitle()` + chat-list rows → nick-or-truncated-address, never full base58 (fixes the pending-contact topbar). **GATED:** self-emphasis + FAB need the local nick (not pushed to chat) → `window.SL.SelfNick`, empty until **be-cutover C13** (`addCustomString("SelfNick", …)`); highlight-all + picker work without it. #46 review → 1 MAJOR (IME guard) + 2 MINOR (addThem invalidate · RegExp try/catch) FIXED, else CLEAN. New keys: `mentionMembers`/`jumpToMention`. **Components changed → `build-demo-bundle` REQUIRED.** 🟡 Damir: `node scripts/build-demo-bundle.mjs` → `build-shells.mjs` → `smoke-test.mjs` → F5 (group: @ picker + highlighted mentions; nameless topbar/list = truncated) → commit · BE: C13 lights up self-emphasis + FAB.
- [x] **BE-CUTOVER BATCH 2 — C13 + C11 (#213, 2026-07-09, Opus; frozen bridge).** Both C# edits in `Pages/Chat/SingleChatPage.xaml.cs`; C13 also a shell edit. **C11 (group delivery-tick BUG) — ✅ Damir F5-CONFIRMED ("tick works" → H1 holds, DONE):** own group/bot msgs stuck on clock. Traced: FE pipeline is CORRECT + applies any push (kind-preserving); the GAP was the `msgReaction` case (`StreamProcessor:464-476`) never pushing `updateMessage`, so a group `received:`/`seen:` aggregate that flips flags in core (`Friend.addReaction`, Ixian-Core read-only) never refreshed the tick (1:1 works via the separate `msgReceived`/`msgRead` codes). Fix = one line in `updateReactions(msg_id,channel):1631` → `updateMessage(fm, channel)` after `updateReactions(fm)`; idempotent, channel-guarded, promotes to delivered/read via `statusFrom` even if `sent` never flipped. **C13 (SelfNick) — CORRECTED after Damir F5 ("FAB doesn't appear"):** the first cut used `addCustomString("SelfNick", …)`, but the redesigned self-contained shells build `window.SL` from their BUNDLED dictionary (chat.html:233), NOT from `*SL{}` substitution — so a C# custom string never reaches them (theme = localStorage `data-theme`; every C# datum arrives as a `sendUiCommand` PUSH). **Redone as a bridge push:** C# `sendUiCommand(this,"setSelfNick", IxianHandler.localStorage.nickname)` in `onLoad` (:490, after `onChatScreenReady`) + shell `setSelfNick(nick)` handler → `selfNickPushed` → `invalidateMentions()`+`updateMentionFab()`; `selfMentionKeys()` reads it first. Lights up self-mention emphasis + the @ FAB. **Finding: never feed a redesigned shell via `addCustomString` — always `sendUiCommand`.** **@ FAB → Telegram-style catch-up (Damir ask, same shell edit):** dropped the baseline-seed → counts EVERY unseen mention of me anywhere, jumps oldest-first, PERSISTS the seen set per peer (`spixi.mentions.seen.<addr>`) so caught-up mentions don't resurface. Docs: DECISIONS #213 · be-cutover C13/C11. 🟡 **Damir: `node scripts/build-shells.mjs` (C13 shell changed; NO bundle rebuild) → build `net10.0-windows` → F5 (C13: group @your-nick shows self/warning style immediately; @ FAB = scroll up then receive a new @you below → appears+cycles) → commit.**
- [x] **BE-CUTOVER BATCH 3 — C7 (#214, 2026-07-09, Opus; frozen bridge; component changed → FULL build).** App-invite **decline** + always-carry-**install-URL**. **C7(a) decline = FE-LOCAL persistent "Declined" card, NO C# verb (Damir mid-batch: "feels weird that it just disappears").** First cut was a C# `ixian:declineApp`→`onDeclineApp` that DELETED the message → REVERTED (deleting is the vanish Damir rejected). Revised: the invite STAYS and flips to a terminal `'declined'` state, remembered per peer (`spixi.app.declined.<addr>` localStorage, same class as `myLikes`) so it survives a C# re-flush (loadMessages re-pushes the invite) + re-open. FE only: `chat.html` `declinedApps` set + `loadDeclinedApps()` (per-peer reset in `onChatScreenReady`, paints declined on first frame — no actionable flash) + `declineApp(id)`; `buildAppRow` renders `'declined'` for incoming + withdraws the handler once declined (own invites never flip); `typed-bubbles.js createAppBubble` adds the terminal `'declined'` state (sub "You declined this invite", no buttons). Sender NOT notified, message NOT deleted; no message content cached. **C7(b) install URL (the ONLY C# change):** `MiniApp` ctor now `if (url==""&&IxiUtils.IsValidUrl(app_url)) url=app_url;` inside `if(app_url!=null)` — explicit `url=` line still wins; fixes `fetch()`/`extractAppInfo()` dropping the source origin so `getAppInfo` emitted `id||||name` → receiver "Get app" no-op. Sideloaded apps (no url) unaffected. **#46 audit (self + independent adversarial reviewer over real files, TWICE — C#-verb cut then FE-local revision) → code CLEAN, 0 MAJOR/MINOR**; reviewer's one MAJOR was docs-drift → docs corrected. NITs accepted (declinedInvite inline fallback → extract-strings next; declined-id list unpruned like myLikes). **C7 FOLLOW-UP (Damir F5 same day, 2nd #46 pass CLEAN):** (i) dead "Get app" fixed — FE hides it when the invite has no install URL (empty URL → `installApp` no-op'd; caveat: C7(b) only fills the URL for apps FETCHED after the fix → **re-install from URL to test**); (ii) app-icon nicety — `getAppInfo` now appends the app IMAGE url (`id||url||name||image`, additive/backward-compat; StreamProcessor:663 + all consumers guard the index), receiver shows a real icon for a Missing app (`http`-gated), FE `<img>`+rocket onerror. **PRIVACY — GATED (Damir "privacy first"):** the remote-icon auto-fetch (leaks receiver IP to publisher, #82 class) renders only when the media-autoload pref (`mediaAutoloadOn()`) is ON, else rocket fallback; local installed-app icons never gated. **C7 IN-SESSION FIX (Damir F5 — card kept showing Decline/Join after joining):** C# only reported ""/"Missing", never an active session. `SingleChatPage` installed-branch now sets `app_state="Minimized"` when `am.getAppPage(friend.walletAddress, app_id)!=null`; FE `appStateFrom` maps `'Minimized'→'in-session'` (Resume→`joinApp`); `createAppBubble` gates End-session (no verb → no dead button). Round-trips (close app→`removeAppPage`→reload→`invite`). #46 reviewer CLEAN; logged pre-existing (not blocking): Resume spawns a NEW session (`onJoinApp` no dedupe = same as old Join) + `hasUser` also matches a group session with the friend (edge, invites 1:1). **⚑ FUTURE flagged (be-cutover C7 OPEN):** decline-**notify** variant (signal the inviter) wanted later — new C# stream message, its own row. Docs: DECISIONS #214 · be-cutover C7. 🟡 **Damir: `node scripts/build-demo-bundle.mjs` (typed-bubbles.js changed) → `build-shells.mjs` → `smoke-test.mjs` → build `net10.0-windows` → F5 (incoming invite: Decline → "Declined" card persists on reopen, sender unaffected · app you don't have + re-installed sender app: Get app installs · own invite still Cancel · missing-app tile shows the publisher icon) → commit.**
- [⛔] **C8 arbitrary emoji reactions — BUILT then REVERTED → 🟡 BE/CORE (#215, 2026-07-09, Opus).** The mandated Ixian-Core check LOOKED 🟢 (the reaction store holds `tip:`/`fileReceived:` beside `like:`, and `updateReactions` serializes any `key:count`), so C8 was built zero-core (C# `case "react"` mirroring `like` + FE emoji menu/`parseReactions`/`reactToMessage`, #46-CLEAN). **Damir on-device F5 DISPROVED it: core persists ONLY `like` for USER reactions** — every other key (raw emoji AND ASCII shortcodes `joy`/`fire`/…) renders LIVE but is dropped on chat re-enter (`tip`/`fileReceived` survive only because they're app/system-added, not user `contextAction` `addReaction`). So arbitrary emoji need an **Ixian-Core** change (persist non-`like` user-reaction keys). **ALL C8 code reverted to exactly pre-C8** (C# `case "react"` removed; FE menu back to `['❤️']`; `reactToMessage`/`parseReactions`/`onToggle`/`onMenuAction`/`onAction` restored; shortcode maps removed) — grep-verified no remnants. Kept as **BE recommendation** (be-cutover C8) with the full FE+C# re-apply plan in DECISIONS #215. **LESSON: a store holding SYSTEM keys (`tip`/`fileReceived`) does NOT prove USER reactions are key-agnostic; the user-`addReaction` persistence path isn't verifiable from the app tree → flag 🟡 pending an on-device persistence F5 BEFORE building.** Nothing to build/commit for C8; next 🟢 = A1/A2/X1.
- [x] **BE-CUTOVER — A1 apps-tab uninstall (#216, 2026-07-09, Opus; frozen bridge; JS+C# co-change, component already built → `build-shells` only, NO bundle rebuild).** The apps tab's ⋮ menu was gated OFF (uninstall was `AppDetailsPage`-scoped, no list verb). C#: new `ixian:uninstall:<id>` in `HomePage.onNavigating` (after `appDetails`; Ordinal+colon, no collision, not in `onNavigatingGlobal`) → `onUninstallApp` = `MiniAppManager.remove(appId)` (same removal AppDetailsPage uses) + `shouldRefreshApps=true` + `loadApps(true)` UNCONDITIONALLY (FE optimistic tile-drop ↔ C# authoritative re-push converge). FE (`home.html` only): apps `appMenu:true` + `onUninstall:(app)=>ixian:uninstall:<id>` — un-gates the ⋮ (App details + Uninstall behind a confirm modal; `openAppMenu`/`applyAppAction`/`apps-item.js` menuBtn-guard all already built). #46 reviewer: 0 MAJOR; 2 MINOR fixed (loadApps unconditional; stale home.html:68 comment). Parity NITs logged (uninstalling a RUNNING app can throw uncaught + dangle a `MiniAppPage` — pre-existing in AppDetailsPage, both paths → be-cutover A1 backlog). ⚠ mount truncated HomePage tail (#175) → handler written via Edit (real disk). 🟡 **Damir: `node scripts/build-shells.mjs` (NO bundle rebuild) → `node scripts/smoke-test.mjs` → build `net10.0-windows` → F5 (apps ⋮ → App details opens; Uninstall → confirm → tile drops + stays gone on reload) → commit.** Docs: DECISIONS #216 · be-cutover A1. **A2 RESOLVED same batch (Damir clarified): KEEP the banner (website LINK, legacy), drop only the IN-APP feed.** The Explore banner STAYS + links out to the mini-apps WEBSITE (`ixian:spixiAppsLink` → HomePage:278 Browser.OpenAsync, external — compliant, it's an outbound link not an in-app catalog); the app-store-like IN-APP Discover FEED (`apps-feed.js` / standalone apps-shell view) stays PARKED. Home apps tab = installed-apps launcher + website-link banner. **Net: no functional code change** — an interim banner-hide (gating on `discover`) was REVERTED; `home.html` keeps `discover:false` (="no in-app feed") + `onExplore→ixian:spixiAppsLink`; banner renders unconditionally. #46 source CLEAN. A1 F5-CONFIRMED ("uninstall works well"); commit A1 together.
- [x] **BE-CUTOVER — X1 avatar/app-icon data-URI push (#217, 2026-07-09, Opus; Option A per #204; C#-ONLY — no FE/bundle/shell change).** C# pushed avatar/app-icon PATHS as `<img src>`; iOS/WKWebView can't resolve a raw path from the Raw-assets html origin → invisible avatars on iOS (WinUI/Android resolve). Fix: `Utils.imageToDataUri(path)` (Utils.cs) — readable LOCAL file → `data:image/<mime>;base64` (mime by ext; `ConcurrentDictionary` cache keyed on path+mtime; try/catch→raw path). Passes through UNCHANGED null/empty · `http(s)` remote URLs · `img/` sentinels · missing paths (shell gradient/rocket-fallbacks those). Wrapped at every redesigned-shell avatar/icon push at the finalized value: SingleChatPage (avatar :1278 · app_image :1498 · member :463 · loadApps icon :1116) · HomePage (addContact :866 · getFriendMessageHelper :1037 → both addChat · loadAvatar :676/:1453 · addApp :1654) · ContactDetails setAvatar :194 · SettingsPage loadAvatar :81/:389/:427 (raw sentinel check :71 preserved) · LaunchCreatePage :123 · AppDetailsPage init icon :153 (remote app.image passes through). EXCLUDED legacy/native (do at repoint): WalletRecipientPage/WalletSentPage/WalletSend2Page + AppsPage. #46 reviewer CLEAN 0 MAJOR/0 MINOR (helper + all sites + fence verified; no money touched; sentinels/empties unchanged → no WinUI regression). **Win is iOS — verify at Phase 2; WinUI shows no visible change.** 🟡 **Damir: build `net10.0-windows` (C#-only, NO bundle/shell rebuild) → F5 (avatars/app icons still render in chats/chat/contact-details/settings/apps) → commit.** Docs: DECISIONS #217 · be-cutover X1. **X1 FOLLOW-UP (#217b, Damir F5 "avatar didn't translate to chat topbar but shows on details"):** the chat shell only scavenged the header avatar from the first received 1:1 message → message-less new contacts + groups got a gradient/wrong topbar. Fix (shell+C#, no bundle): `SingleChatPage.onLoad` pushes `setAvatar`=`getAvatarPath(friend)` via `imageToDataUri` (after onChatScreenReady); shell `setAvatar` handler sets `identity.avatar` (authoritative over the message-scavenge fallback); `onChatScreenReady` resets `identity.avatar=null` per-peer. Topbar now shows the peer avatar (message-less 1:1) + the GROUP avatar (groups); member-bubble gradients unchanged (correct for non-contacts). #46 CLEAN. **FOLLOW-UP 2 (#217c, Damir F5 "brief gradient flash on chat enter"):** the synchronous first topbar paint ran before the avatar push → default-gradient flash; guard = `if(!identity.address) tb.classList.add('is-avatar-pending')` + CSS hides the avatar slot until the identity lands (coalesced rAF → appears once, real avatar). shell-only, #46 CLEAN. 🟡 Damir: `build-shells` (NO bundle) → build → F5 (no flash) → commit.
- [x] **CH2a — chats-list filter-chip COUNTS + Requests-hide (#218, 2026-07-09, Opus; FE-only, component+shell → FULL build; Damir F5 "looks and works good").** Damir asks: a plain trailing NUMBER on Unread + Groups + Requests chips; Groups number only when unread; Requests chip HIDDEN unless pending requests. `chats-header.js` `setChipCount`/`setChatsHeaderCounts` (plain `.c-chip__count` span, not a badge pill) + createChatsHeader `counts`; `chip.css` `.c-chip__count` (tabular-nums, tints on selected); `home.html chatsHeaderCounts()` (unread = unread CONVERSATIONS `!muted && (unread>0||mention)`, groups = unread groups, requests = state.requests.length) routed through `onModelChange` so counts never go stale on row-menu delete/accept. **DECISION:** chip count = unread CONVERSATIONS (nav badge keeps the message total — can differ). Requests count = 0 until the CH2b feed → chip hidden now. #46 CLEAN (1 MINOR fixed). Flag for CH2b: hide Requests while it's the ACTIVE filter → reset filter to 'all'. 🟡 Damir: `build-demo-bundle` → `build-shells` → `smoke` → commit. **CH2b (C# request feed + accept/decline verbs) NEXT.**
- [x] **CH2b — contact-request FEED + accept/decline verbs (#219, 2026-07-09, Opus; JS+C# co-change; component → FULL build).** The other half of CH2: feed `state.requests` + light up the built Accept/Decline + #109 staged-handshake UI. C# `HomePage.loadChats` routes INCOMING requests OUT of the chat list into a `clearRequests`/`addRequest(addr,nick,avatar,ts)` feed — detection `!approved && lastMessage.type==requestAdd && !localSender` (outgoing=localSender stays a chat row); pushed within the chats flush (clearChatsDone renders both). New verbs `ixian:acceptRequest:<addr>` (mirror `SingleChatPage.onAcceptFriendRequest`: approved=true + `sendAcceptAdd`) + `ixian:declineRequest:<addr>` (`FriendList.removeFriend`); self-heals (accept→approved→chat list; decline→gone). FE `home.html`: `clearRequests`/`addRequest` handlers → `state.requests`; `opts.onRequestAccept` (verb + `acceptContactRequest` optimistic handshake) / `onRequestDecline` (verb + drop); `leaveRequestsFilterIfEmpty` resets filter→'all' + syncs the header chip when the last request clears while Requests is active. #46 reviewer CLEAN 0 MAJOR (no money/signing, no misrouting, no dup card+row). MINOR (graceful degrade): keys on the LAST message being requestAdd — a follow-up before accept falls through to the chat list (in-chat Accept/Decline still works). 🟡 Damir: `node scripts/build-demo-bundle.mjs` → `build-shells` → `smoke` → build `net10.0-windows` → F5 (2-account contact request → Requests chip w/ count + Accept/Decline card; Accept→chat list; Decline→gone; last clears→filter falls to All) → commit. Docs: DECISIONS #219 · be-cutover CH2.
- [ ] **DEFERRED (another session):** paste-to-send screenshot/image (desktop) — spec `docs/paste-image-spec.md`, be-cutover **C12**; build with the full Desktop pass.
- [x] **FLICKER + NATIVE-PUNCH SESSIONS (#222–#230, 2026-07-09/10) — pickup doc = `docs/handoff-session-flicker-native.md`, READ IT FIRST next session (§5b = latest state, §5c = workflow split: fable builds only / Opus reviews separately / Damir runs smoke+builds+F5).** Overlay nav (#225) · system-ui + density (#226) · type dial 2 + `:root[data-desktop]` platform flag (#227/#228) · lock load-then-present (#229) · **lock SHOWN IN PLACE (#230 — Damir F5 "looks awesome")**. ⚠ Owed: separate Opus adversarial session over #227–#230 · Damir's quirks-list triage = next session item 1 · Figma type mirrors · launch.html gets the #228 flag at its rebuild batch. Build rule: F5 net10.0-windows only — Rebuild Solution trips the pre-existing android RocksDbSharp errors.
- [x] **F5 VERDICT #227–#230 = PASS + NATIVE-PUNCH CLOSEOUT (#231–#231c + #232, 2026-07-10, fable build-only per §5c; UNCOMMITTED; message-bubble.js changed → FULL build; one safe C# constant).** Damir: "works well, lock without noticeable flicker"; app-wide quirks triage DEFERRED until most is done. Landed: brand-font opt-ins (lock/launch titles → `--font-display`) · **B4** excerpt canon (`home.html canonExcerptText`: base58→truncated, URL→domain, GIF chip preserved) · **B5** avatar border normalization (gradient borderless, hairline on photo `<img>`) · **B2** code-clean + F12 probe · **#231b** excerpt>name size flip fixed (`.c-excerpt` + tx `__time`/`__fiat` body-md→body-sm; body-md = BUBBLE role since #227) · **#231c** linkify (www./bare-domain pickup w/ TLD whitelist + email guard, long-URL display truncation, https:// prefixed for click only; OG/thumbnail preview → **be-cutover C14**, sender-composed §8, ★ human BE review first) · **#231c** desktop min-width 480→384 (App.xaml.cs, TG-class). **#232 strategy row:** wallet-send LAST · security-flagged items gate on human BE review · ★ chat isolation REAFFIRMED (#221 model) · desktop-pass scope (left rail · Account as pane · missing Account entries · reply-to [BE claims zero-C# — VERIFY carrier on-device first, #215 lesson] · pin chat · chat-info integrated pane). **Pickup doc next session: `docs/fable-build-brief-desktop-pass.md`** (item 1 = #234 lock fix, then the desktop-pass units; consumes handoff-desktop-pass §0/§2). ✅ **Opus review DONE 2026-07-10 (#233) → PASS** (brief archived to `docs/archive/`): 2 mechanical fixes landed — `SpixiContentPage.pushPageLoaded` drops a staged overlay while a lock is shown in place (#230 cover-the-lock gap) + `.c-contact-request__sub` body-md→body-sm (#231b sweep miss) + tokens.css comment NIT. ⚠ **#234 = PRE-EXISTING MAJOR (resume-lock Cancel unlocks without password; NOT this batch)** → Damir on-device confirm + BE fix decision (security-review §1a). 🟡 Damir: `node scripts/build-demo-bundle.mjs` → `node scripts/build-shells.mjs` → `node scripts/smoke-test.mjs` → F5 (incl. #234 confirm: does the resume lock show Cancel + unlock?) → commit (add "+ Opus #233 fixes").
- [x] **DESKTOP PASS batch 1 BUILT (#236, 2026-07-10, fable build-only §5c; ZERO-C#; components changed → FULL build; UNCOMMITTED).** #235 FE MINORs (`displayUrl` always surfaces the real host — userinfo stripped, host-first, end-truncate · `LINKIFY_MAX` 4096 kills the O(n²) crafted-token freeze) + brief §3 zero-C# units, spec rows in `desktop-split-spec.md` §6e: **(1) left NAV RAIL** — `c-bottomnav--rail` component variant (demo round-4 dials) + `home.html` gates on `:root[data-desktop]`; the `.is-desktop` opt-in SUPERSEDED (`?desktop=1`/`?mobile=1` = browser-preview forcing) incl. chat.html's #207 canvas re-gate — desktop pattern-off/dark-ground now LIVE on WinUI (F5-visible change) · **(3) Account entries** — Share address (navigator.share → clipboard fallback) + Chat appearance takeover (closes #147: `spixi.chat.pattern`/`.textscale` in localStorage, chat.html applies pre-paint, dark = light pick × 0.36 keeping the #76 ratio, `.c-bubble` scales via `--chat-text-scale` calc — flagged component change) · **(5) pin chat** — `capabilities.pin` ON (menu + swipe light up), `spixi.pins` interim persist re-seeded in `addChat` (structural flush rebuilds rows), delete sheds the pin; durable = CH4; NO visual pin marker yet (Damir dial). GATES HONORED: #234/C15 (BE sign-off) · reply-to (BE-verify-first) · wallet-send LAST · units 2/6 (small C#) not built. ⚠ mount served STALE bottomnav.css + TRUNCATED shell tails — head scripts node-checked in-session; main-script syntax rides Damir's local `build-shells`. 🟡 **Damir: `node scripts/build-demo-bundle.mjs` → `node scripts/build-shells.mjs` → `node scripts/smoke-test.mjs` → F5 net10.0-windows (rail on desktop + `?mobile=1` sanity · desktop chat canvas · Account Share + Chat appearance → open a chat · pin across reflush/restart · linkify labels) → commit · then the SEPARATE Opus session over `docs/opus-review-brief-desktop-pass-1.md`.**
- [x] **DESKTOP PASS r2 (#237, same day, Damir F5 round; ZERO-C#; FULL build; UNCOMMITTED with #236).** Rail 84→72 + logo pinned on top (`createBottomNav logo:true`) · chats topbar = "Chats" title on desktop (logotype = mobile) · FAB hidden on desktop → topbar message-plus action · Account: rail replaces bottom bar + no topbar back + **Save only-when-dirty** (`setSettingsSaveVisible` free fn + shell syncSave) — **save still pops = S14 (BE), FE built+gated** · pane hairline `outline-neutral-03` on home body right edge · bubble-size dial 3 RETRACTED (Damir: Chat-appearance pref covers it) · pattern-off on desktop = intended #207 (acked) · **resizable left pane = native divider → be-cutover D1** (unit-2/6 small-C# batch). Spec: desktop-split-spec §6e.1 r2 block. 🟡 Damir: same FULL build sequence → F5 → commit #236+#237 → Opus brief (covers both rounds).
- [x] **DESKTOP PASS r3 (#238, same day; ZERO-C#; FULL build; UNCOMMITTED with #236/#237).** Damir F5 fixes: hairline → fixed `body::after` strip z-31 (takeovers painted over the border) + rail hairline → neutral-03 · **app-lock persist**: `ixian:lock` only flips the C# page field, `Preferences.Set` lives in onSaveSettings:249 → lock toggle now marks `dirtyLock` (Save shows, exit saves) · rail selected icon grey → explicit `[aria-current]` action-ink rule (source-order tie) · **Account exit lands on the TAPPED tab** via `spixi.landtab` localStorage handshake (storage event + focus fallback, 15s staleness guard; S11 verb = BE end-state) · selected-row right cutoff = `.u-scroll` track → desktop tab scrollers hide it (full-bleed rows, dial noted). **NEXT: Opus session over `docs/opus-review-brief-desktop-pass-1.md` (#236–#238) → build session per fable-build-brief §4a (Account-as-pane C# + settings master-detail + Contributors un-gate + D1 resizable divider) → desktop sweep rest.** ★ isolation reaffirmed to Damir: conversation stays its own WebView (#221), the native feel is #222–#231 work, no wall touched.
- [x] **DESKTOP PASS batch 2 BUILT (#240, 2026-07-10, fable per brief §4a; small C# + component + shell → FULL build + net10.0-windows; UNCOMMITTED).** ① **Account as a PANE** — `onSettings` pins SettingsPage to the detail column via the #225 overlay machinery on wide windows (`new SettingsPage(wide)`, tag "settings", col 1); `setPaneMode('1')` pushed first in onLoad (pre-present, re-pushed on language reload); re-tap = no-op/resurface guard. ② **Close-audit** — tab switch/chat open/tx open dismiss the pane THROUGH the shell (`requestSettingsOverlayExit` → `onExitRequest` → save-if-dirty exit); hardware back routed through the shell too (**fixes the pre-existing #225 gap: closeTopOverlay bypassed the Account save path on Android back**); resize-narrow strand = #225-M2, next unit. ③ **Settings MASTER-DETAIL in-pane** (spec §2.1 → production): `body[data-pane]` hides the peer rail; ≥640px in-pane = hub master (360px) + detail column w/ dt-empty state, back-empties-detail, `aria-current` + tonal tint via new settingRow `key:` (component change → bundle rebuild); narrow pane = mobile takeovers; `?pane=1` preview. ④ **Contributors UN-GATED** (static component — S10 verb obsolete, be-cutover updated). ⑤ **D1 native divider** — 6px transparent grip on mainGrid col 0 edge, pan 280–520/dbl-tap 400/`Preferences leftPaneWidth`, hidden narrow; overlays+locks cover it (be-cutover D1 LANDED). Lock invariants #230/#235 re-verified for the pane-hosted SettingsPage (overlay-host fallback → full-window in-place lock). New string key `settingsEmptyHint` (inline fallback → next extract-strings). ⚠ mount stale/truncated (#175): new shell JS node-checked via an outputs harness; C# compile = Damir's build. 🟡 **Damir: `node scripts/build-demo-bundle.mjs` → `build-shells` → `smoke-test` → build net10.0-windows → F5 (pane + master-detail + empty state · dirty nick survives tab-switch/chat-open exits · back routes hub→exit · mobile/narrow unchanged · Contributors both modes · divider drag/reset/persist · delete-wallet lock = full-window over the pane) → commit → SEPARATE Opus session over `docs/opus-review-brief-desktop-pass-2.md`.** Then: unit 6 chat-info pane · #225-M2 · reply-to (BE-verify-first) · wallet-send LAST.
- [x] **#241 SEMANTIC PRIMITIVES REWORKED (same session):** green/orange/red/blue scales regenerated in OKLCH around Damir's anchors (#2D7A53→green-600 · #CA761D→orange-500 · #A63030→red-600 · #1178AC→blue-600, replacing the indigo that fed info/links) on the brand-scale lightness ladder; tokens.css primitives only, semantic keys auto-populate both themes; WCAG table verified (one flag: surface-info-500 white text 3.6:1 → repoint to 600 if solid info badges carry small text). ⚠ MIRROR TO FIGMA (primitives). Rides the #240 build (`build-shells` inlines tokens).
- [x] **#242 ACCOUNT-PANE F5 ROUND 1 fixed (component+shell+small C# → FULL build):** ① divider 10px + WinUI ↔ cursor (ProtectedCursor reflection) ② pane-rendered-own-rail = CSS specificity bug → `:root[data-desktop] body[data-pane]` hide ③ avatar-only change never dirtied (X1 data-URI broke the tmp sniff) → `avatarPickPending` latch ④ Theme/Language = detail screens in split (pickers gained `inline:true`, hub `onThemeNav/onLanguageNav`; MOBILE KEEPS SHEETS) + Backup pinned to the detail column (true in-detail backup = be-cutover **S15**) ⑤ **S14 LANDED**: `ixian:apply` (saveSettingsCore refactor, no pop) + `setCaps('settingsApply')` push → Save stays + "Settings saved" toast. Scope answer: pane/rail/split = DESKTOP-ONLY; mobile untouched. New string key `settingsSaved`. 🟡 Damir: FULL build → F5 the 5 items → commit #240–#242 together → Opus session (brief §addendum covers this round).
- [x] **#243 ACCOUNT-PANE F5 ROUND 2 — MASTER-DETAIL DROPPED → SINGLE-PANE SUBLEVELS (Damir's explicit pick; shell + small C# → FULL build):** the #240 hub+detail split WAS the "double pane" Damir kept flagging — deleted (~70 lines); pane = ONE column, rows open sublevels in place, content per the DESKTOP DEMO grammar (640px centered, sheet padding on lifted pickers, hugging CTAs — Damir: "refer to desktop demo"). **Backup = in-pane sublevel, S15 LANDED**: static `BackupPage.backupAccount()/backupWallet()` + SettingsPage forwards the two verbs + cap `backupInline` (mobile keeps the push). Left pane max 520→**624** (+20%, Damir) w/ window-aware clamp (`min(624, Width−320)`) so the detail pane never starves. 🟡 Damir: FULL build → F5 (one-column pane + sublevels incl. Backup share-sheet · divider to 624 · mobile untouched) → commit #240–#243 → Opus session (brief addendum 2).
- [x] **#244 SEMANTIC PRIMITIVES v2 (hotter, Damir r2 hexes) + LINK ROLE:** green #26994C→500 (vivid step; 600 #007030 = surfaces) · orange #EF9132→400 (warning 600 #884b00 under white) · red #B12525→600 (= the error surface) · blue #0B87B4→500 (= surface-info, white 4.1; 600 = text/links 6.3) — same OKLCH/brand-ladder method, AA table green. NEW `--text-link/-hover` + `.c-bubble__link` consumes them (bundle rebuild). **⚠ accent flip = OPEN Damir call** (accent aliases BRAND and tints launch/lock medallions + list read ticks — 1-line repoint when called). ⚠ MIRROR TO FIGMA.
- [x] **#245 ACCOUNT = PEER PANE (Damir r3 + demo image — supersedes the #243 LAYOUT; its sublevels/caps/backup-forwarding carry over):** wide onSettings pins the settings WebView over the grid MINUS the 72dip rail strip (new `stageMargin` on `pushPageLoaded`, presentation-only; `railWidthDip=72` const coupled to the #237 rail dial) — rail stays visible+live; HUB renders in the LIST-column slot (`setPaneMetrics` = leftPaneWidth−72 → `--sd-master-w`, hairline aligns with the pane divider) and SUBLEVELS open in the detail region (dt-empty default, aria-current tint, backup row `key`); rail highlights Account while open (home.html keeps it active; C# pushes `onSettingsClosed` on close → falls back). Locks still cover everything (no margin). Divider not draggable while Account open (accepted). Mobile untouched. 🟡 **Damir: `node scripts/build-demo-bundle.mjs` → `build-shells` → `smoke-test` → build net10.0-windows → F5 (hub replaces the LIST · sublevels right w/ empty state · rail live + Account highlighted, falls back on close · colors: presence dot/read ticks/links both themes) → commit #240–#245 → Opus session over the brief (addendum 3).**
- [x] **#245b F5-PASSED ("we can ship this") + SHIP PREP (2026-07-10):** sublevel title bars full-width (cap moved to non-topbar children, `.sd-empty` excluded) · Backup sublevel = the ONE backup illustration (`images/backup.svg`, byte-identical to the onboarding/nudge asset). Consumed handoffs ARCHIVED (flicker era + desktop-pass-1 + be-batch 3/4 + the #239 review handoff → `docs/archive/`); live docs = `fable-build-brief-desktop-pass.md` (remaining units: #234 lock fix [BE-gated] · unit 6 chat-info pane · #225-M2 · reply-to [BE-verify-first] · wallet-send LAST) + **`docs/opus-review-brief-desktop-pass-2.md` = the NEXT-SESSION work order: full #46 multi-agent adversarial AUDIT+FIX LOOP over #240–#245b + #241/#244 colors (loop protocol + agent scopes + non-negotiables in the brief header)**. Commit message prepared (chat). After the Opus loop: Figma mirror queue (#241/#244 primitives + link tokens) · accent-flip decision (#244 open) · remaining desktop sweep.
- [x] **#246 OPUS #46 AUDIT+FIX LOOP over DESKTOP PASS 2 (#240–#245b + colors #241/#244) → PASS** (brief `docs/opus-review-brief-desktop-pass-2.md` verdict appended; real files via Read tool #175, §5c author-only). 4 disjoint read-only auditors (C# overlay/lock/divider · shell pane state machines · settings-shell.js deltas + mobile-sheet byte-identical · #244 color sweep) + a fresh break-my-verdict re-review over the 2 highest-risk CLEANs (#235 lesson). **0 MAJOR.** Lock un-dismissability with `stageMargin` HOLDS (`stageMargin` only on `pushPageLoaded`, never on the lock path `pushModalLoaded` → locks stay zero-margin full-span over the rail), save-if-dirty close-audit HOLDS, chat isolation #221 HOLDS, mobile sheet BYTE-IDENTICAL, accent NOT flipped. **1 mechanical fix landed (`src/shells/settings.html` avatar `avatarPickPending` else-branch clear — canceled-pick stale-latch false-dirty; shell-only → `build-shells`, NO bundle rebuild)**, re-reviewed CLEAN. **Damir/BE follow-ups (logged, not silently changed):** 🟡 solid info badge white-on-`--surface-info` ≈3.96:1 <AA (his reserved dial — repoint surface-info→info-600 or badge-only, `badge.css:25` sole consumer) · 🟡 sent-bubble read/delivered ticks <3:1 on the vivid re-anchored green (F5 eyeball) · 🟡 warning-surface hue NIT · 🟡 resume-lock STAGING input-freeze wrong grid for a non-Grid legacy host (pre-existing, ≤1.2s input-only, security-flagged → BE gate #232, in security-review doc) · 🟡 3 gated-OFF nav rows lack `key:` (add at cap-enable). ⚠ Damir: `build-shells` picks up the avatar fix; if committing with #240–#245 the FULL sequence already applies. Wallet-send + chat wall untouched. Next: commit #240–#246 · Figma mirror (#241/#244) · accent-flip decision · remaining desktop sweep (unit 6 chat-info pane · #225-M2 · reply-to BE-verify-first · wallet-send LAST).

- [x] **DESKTOP PASS 3 — UNIT 6 CHAT-INFO PANE + #225-M2 BUILT (DECISIONS #247, 2026-07-10, fable §5c build-only; small C# + shells + 1-line component → FULL build; UNCOMMITTED, sits on the uncommitted #246 delta).** ★ Damir mid-session reaffirm honored: chat-info = SHELL-side ContactDetails (its own WebView); the conversation WebView stays walled, coordination C#-only. mainGrid col 2 + `openContactDetails` router (wide+chat → 360px pane BESIDE the convo — margin-staged, pinned at present via new `onOverlayPresented`/`rehomeOverlay` = zero empty-strip frames; wide no-chat → detail slot; narrow → unchanged takeover; same-contact re-tap = toggle) · close audit (chat close/switch, tab, tx, back; locks/Account still cover col 2; convo keeps ticking under a pane top) · **#225-M2 FIXED structurally**: `relayoutPinnedOverlays` re-homes column-pinned overlays full-span on narrow / re-pins on wide (incl. mid-stage ops); divider pan live-clamps the pane (convo ≥320), squeeze closes it · shell `setPaneMode('1'|'2')` CSS (640 body cap, pane-2 hairline) · **quirk 13 fixed** (row-menu Chat info: 1:1 → details, group → opens chat) · **quirk 7 fixed** (`chat-info.js` group title fallback — component change → bundle rebuild; new key `groupInfo`). BE asks logged: **CI6** shared media/files feed · **CI7** group-info shell pane + admin rename/re-avatar. **Damir's 17+18 polish list TRIAGED → `docs/polish-roadmap.md`** (state + owner per item, batch order; #247 closed M4-pane/Q7/Q13). Spec: desktop-split-spec §6e.5. ⚠ mount truncates chat-info.js (761/774 lines) — component edit verified via file tools; shell inline JS `node --check` green in-session. 🟡 **Damir: `node scripts/build-demo-bundle.mjs` → `node scripts/build-shells.mjs` → `node scripts/smoke-test.mjs` → build net10.0-windows (NOT Rebuild) → F5 (#247 checklist: pane open/toggle/switch/resize/divider/narrow) → commit (together with the #246 avatar fix) → separate Opus session over `docs/opus-review-brief-desktop-pass-3.md`.** Then per roadmap: chat-polish FE batch (Q4/Q9/Q10/M16/Q15) next · reply-to still BE-verify-first · wallet-send LAST.

- [x] **CHAT-INFO F5 ROUND (#248, 2026-07-10, same session as #247; C#+shells+component → FULL build; UNCOMMITTED with #247).** Damir's 5 findings + 2 flicker reports, all addressed: **(1)** entry-dependent context — header/row-menu = "Chat info"/"Group info" (`setContext` push + new `ixian:chatinfo:<addr>` verb), directory keeps "Contact details"+Message · **(2)** GROUP info = shell-side (ContactDetails group surface: `setGroupInfo` + buffered roster + leave/notifications/kick/ban forwards mirroring SingleChatPage; groups now get the same desktop pane/takeover as 1:1; BOTS keep the in-chat takeover — follow-up in CI7) · **(3)** "Owner" chip via `users.getOwner()` push (never for blind groups); **group rename/re-avatar = VERIFIED NO VERB EXISTS → CI7 [BE]** (#215 lesson, not built) · **(4)** degrade = COL 1 not full-span (squeezed/no-chat wide windows: info covers ONLY the convo region, list stays; narrow keeps full-span) · **(5)** row-menu works for both kinds (`ixian:chatinfo:`); media strip still CI6 (no feed) · info-circle action added to EVERY chat topbar · **flicker:** overlay/modal stages now paint `target.pageSurfaceColor` (divider-drag dark slivers → theme-matched; WebView2 resize lag itself = compositor limit; zero-resize FLOAT pane = queued dial) · **empty state redesigned**: NEW `src/shells/empty_detail.html` (token-themed both modes, `*SL{}`-localized) replaces the always-dark legacy right-pane welcome. New keys: `owner`, `groupInfo`. Opus brief = pass-3 §ADDENDUM (one loop over #247+#248). 🟡 **Damir: `node scripts/build-demo-bundle.mjs` → `build-shells` → `smoke-test` → build net10.0-windows → F5 (#248 row checklist) → commit #247+#248 → Opus session over `docs/opus-review-brief-desktop-pass-3.md`.**

- [x] **CHAT-INFO F5 ROUND 3 + IN-SESSION #46 LOOP → CLEAN (#249/#250, 2026-07-10; C#+shells+components → FULL build; UNCOMMITTED with #247/#248).** #249 (Damir r3): **self row** in group rosters (own nick via `localStorage.nickname` + `getOwnAvatarPath`, both `loadMembers`+`loadContacts`) · **bots ride the shell-side surface** (`isGroup` incl. `friend.bot`, `setGroupInfo` kind arg; chat.html always emits `ixian:details`; in-chat takeover = retained dead code) · **col-2 ONLY for the open conversation's own info** (`scp.friend == friend` target match — directory/other-chat info lands col 1, never "attached" to a convo it doesn't belong to). #250: the audit loop Damir delegated — 3 Opus auditors + break-my-verdict re-reviewer → **1 MAJOR fixed** (tag-replace col2→col1 orphaned the empty expanded column) + 8 fixes (null-guards, star-reads, kick/ban parse guard, member-SHEET Owner/Admin badge, stateSig sort, empty_detail hardening) · B-1 "margin leak" REJECTED (the strip = the live width-independent rail, by design) · dials logged (B-2 no col1→col2 auto-promote on widen · FLOAT pane variant if open-flicker persists · CI1(b) role semantics arms admin badges/protection). Verdict appended to `docs/opus-review-brief-desktop-pass-3.md`. ⚠ Known non-bug: a contact whose avatar shows as gradient in BOTH the roster and the chats list on the same device simply has no stored avatar file there (X1 data class). **ROUND 4 (#251, 2026-07-11, session close):** EmptyDetail dark-on-light after a theme pick — the resting pane sat in NO re-theme collection (SettingsPage `setTheme` push + `reloadAllPages` + a real shell `setTheme` global all fixed; ⚠ `sendUiCommand` passes the command as a BARE global identifier — a no-op `executeUiCommand` stub can't receive pushes) · group-pane member search unstyled → `contact_details.html` gained `search-field.css` + `member-sheet.css` links. 🟡 **Damir: `node scripts/build-demo-bundle.mjs` → `build-shells` → `smoke-test` → build net10.0-windows → F5 (#247–#251 checklist: self row named+photo'd · bot header/info → styled pane · directory contact → col 1 not beside-convo · row-menu group info works · Owner chip row+sheet · dark→light pick → back out of a convo → welcome pane LIGHT) → commit #247–#251 as ONE batch.**

- [x] **CHAT POLISH batch BUILT + IN-SESSION #46 LOOP CLEAN (#252, 2026-07-11, fable; ZERO-C#; components changed → FULL build; COMMITTED 2026-07-11).** Roadmap batch 2: **Q4** received chat payments show '+' (locale-proof via NEW `*SL{}` equality carriers — hidden `#sl-carriers` spans substituted by generatePage with the exact strings C# pushes; requests/sent stay bare, collision-guarded, no wrong sign possible) · **Q9** bot channel-selector title tap TOGGLES closed · **Q10** composer autofocus desktop-only + desktop focus ring 2px→1px (dial) · **M16 (#59 implemented)** connectivity = topbar title-state — chat: presence-sub swap IN PLACE (aria-live-safe, shared `topbarSubText()`); home: root-title swap + FIRST banner surface (update-available now renders; was a dbg stub); banner = actionable only; the #59 §8 warning-class param is NOT needed · **Q15** mention picker rides `.u-scroll`. Loop per #250: 3 Opus auditors → 0 MAJOR → 6 fixes (incl. A-2 SR-announce in-place sub, B-1 wordmark ellipsis) → break-my-verdict re-review HOLDS. Smoke +9 assertions. Files: chat.html · home.html · composer.js · composer.css · topbar.css · smoke-test.mjs (+ DECISIONS/roadmap/brief). 🟡 **Damir: `node scripts/build-demo-bundle.mjs` → `node scripts/build-shells.mjs` → `node scripts/smoke-test.mjs` (order matters — the Q15 jsdom assert needs the fresh bundle) → build net10.0-windows (NOT Rebuild) → F5 (#252 checklist) → commit as ONE batch.** Next per roadmap: batch 3 (Q12 delete-excerpt sync · Q5 groups out of directory · M5 outgoing-request row).

- [x] **CHATS-LIST POLISH batch BUILT (#253, 2026-07-11, fable per `docs/fable-build-brief-chats-polish-3.md`; ZERO-C#; chatlist-item.js/.css changed → FULL build; UNCOMMITTED; §5c: the Opus #46 loop = SEPARATE session, OWED before commit-final — brief gets written AFTER Damir confirms testing).** Roadmap batch 3, all three verify-first (#215): **Q12** delete→stale excerpt — verified NO C# re-push on a local delete (SingleChatPage:1150-1159 pushes only the chat WebView; UIHelpers.deleteMessage:69 = remote-only + HomePage-top-only; core lastMessage recompute unverifiable, core outside tree) → `spixi.exdel.<addr>` localStorage handshake (#238 trio + addChat fold-in, ts-equality expiry, bots skipped; ⚠ FLAGGED for Opus/BE: a text-tail hint persists ONE capped excerpt line of possibly counterpart text) · **Q5** groups filtered out of the contacts takeover, BOTH purposes (CH1-kind `groupAddrs` + `img/spixi-group-avatar.png` sentinel; 'start'-keeps-groups = Damir dial; zero-message custom-avatar group edge → §9 type-arg ask) · **M5** outgoing request = "Request sent" (CORRECTED round 2, Damir F5 — the first-cut carrier matched the Approved-state-only branch = dead air; real signal = the unapproved `chat-waiting-for-response` OVERRIDE (xaml:1273-1279) as carrier + direction guard on the non-empty localSender status-type from the ContactNewPage:203 `requestAddSent` marker; incoming fall-through pushes type='' → never mislabeled). Damir's ask honored: outgoing rows ride the **Requests chip** (chatMatchesFilter admits them, orderedChats short-circuit removed, chip count = cards + rows, leave-guard holds) + contacts-picker **pending badge** lit (`requestAddrs` → `directoryRoster` pending:true → the built #153 grammar); rows stay under All too (dial). `request` excerpt type, `user-plus` glyph degrades until the B2 export; `requestSent` key already existed. Housekeeping: consumed `fable-build-brief-chat-polish.md` → `docs/archive/`. Built + verified in-session: bundle (232 exports) · 15 shells · round-1 smoke 363 ✓ / 0 fail; round-2 model fns PASS via direct node import + static guards grep-confirmed. ⚠ **#175 escalated MID-SESSION: mount began truncating chats-shell.js + smoke-test.mjs for bash/node (real files intact; chats-shell resynced via bash rewrite, #204 method) — full smoke = Damir's local run.** **Damir F5-PASSED both rounds ("perfect") → `docs/opus-review-brief-chats-polish-3.md` WRITTEN = the next-session Opus work order (full #46 loop: 3 disjoint read-only auditors → fix agents w/ explicit cross-file contracts → fresh break-my-verdict re-review until CLEAN; ★ non-negotiables incl. the 🟡 Q12 text-persistence SECURITY RULING; accepted dials listed so they aren't re-litigated; verdict appended to the brief).** 🟡 Order: Opus loop → Damir commits #253 + fixes as ONE batch → then roadmap batch 4 (M12 · Q14 · Q1/Q2).

- [x] **OPUS #46 LOOP over #253 → CLEAN (#254 + #255, 2026-07-11; UNCOMMITTED with #253; components + shells + smoke + generator changed → FULL build).** 3 disjoint read-only auditors → 3 fix agents (disjoint scopes) → FRESH break-my-verdict re-reviewer = **0 MAJOR**. **1 MAJOR found+fixed:** the Q12 writer fired on **REMOTE** deletes too — a remote `msgDelete` (StreamProcessor:459-462 → UIHelpers:69-77 → SingleChatPage:1727) pushes the **byte-identical** `deleteMessage(id)` UI command but **never mutates core** (only the local menu path calls `friend.deleteMessage`) → the hint could never expire while the message **reappears on the next chat open** → fixed with a `pendingLocalDeletes` latch (bot-skipped + self-expiring); the C# half = be-cutover **C16**. **★ SECURITY RULING (#254): the exdel hint persists NO message text — shape `{del,t,kind}`.** Evidence: the shells load from a bare local path → `file://`, and mini-apps load from `file://<app_entry_point>` (MiniAppPage:58) = third-party code on the **same Chromium `file://` localStorage partition** → the store is plausibly readable by untrusted mini-app code. Text tails degrade to a blank excerpt. **Escalated to `docs/security-review-for-be-engineer.md` MAJOR #4** (the same premise implicates the SHIPPED `spixi.draft.*` **own-plaintext** key + address-bearing keys → the real fix is a separate storage partition for the mini-app WebView, C#/platform; BE must verify on-device first, #215). Other fixes: typing-excerpt clobber (a typing flush carries the stale ts → the hint overwrote a live signal) · applyExdelHints precedence (draft > typing/handshake > reaction > hint) · Requests-filter dead-end (leave-guard now fires on flush-done + row delete) · orphan-hint prune · synthetic-ts + load-burst guards · **`build-demo-bundle.mjs` fail-loud integrity gate** after a **NUL byte** was found in the generated bundle (#175 debris → makes the bundle + every built shell **binary to git**; the rebuild clears it) · demo Requests-chip surface + shipped grammar · a11y/docblock/dial corrections. 🟡 Backlog: **`createWalletSend` consumes the UNFILTERED roster** (groups as money recipients; cap-gated OFF, deliberately untouched → **fix as a prerequisite of the wallet-send batch**) · be-cutover **C17** (addContact state arg → pending-contact cancel path + the Q5 residual) · dial: outgoing-request rows are still deletable/pinnable. 🟡 **Damir: `node scripts/build-demo-bundle.mjs` → `node scripts/build-shells.mjs` → `node scripts/smoke-test.mjs` → F5 → commit #253 + #254 + #255 as ONE batch.** ⚠ Commit hygiene: do NOT commit the current bundle/built shells (they predate the fixes + carry the NUL); check `git status` for CRLF-only churn (12 C# files show as modified but `git diff --ignore-cr-at-eol` = zero) and delete `src/components/.fuse_hidden0000001100000001`.

- [x] **MISSING-BITS BATCH A BUILT (#256 interview + #257 build, 2026-07-11, fable per `docs/fable-build-brief-missing-bits.md`; ZERO-C#; components+shells+scripts+translation-drafts changed → FULL build sequence = DAMIR LOCAL; UNCOMMITTED; the Opus #46 loop = SEPARATE session, OWED before commit).** **A1/A2 calls (the app had NO call UI):** NEW `src/components/call-ui.js` (`attachCallUi` — ring overlay Accept/Decline-ONLY per #256, callbar w/ dialing "0"-state, 400ms clear-grace vs the clear→re-add flash, 4-arg vs 13-arg `addAppRequest` collision guarded) wired into **12 shells** (home+chat roster-backed caller lookup; lock/scan/launch/empty_detail EXCLUDED, logged); component edits call-overlay.js (`ignore:false`) + callbar.js (null startedAt). **Verification finding beyond the brief:** mid-session delivery is SINGLE-CONSUMER (global one-shot flag, stack-last ticked FIRST + VoIP targets stack-last) → live events reach home's WebView only; mobile-in-chat ring stays invisible until be-cutover **C18** (broadcast + return-to-call address arg — FE idempotent + pre-wired). **A3 unread divider:** roadmap M15 was WRONG — the 11-arg push carries `read` BEFORE the flip (xaml:1603/:1661); `addThem` extended from its 6-param truncation, one-shot in-place boundary (Damir) derived only from the load burst, grouping breaks at it, per-peer reset. **A4 languages (the silent bug):** production was HARD-WIRED en-us — shells resolved `?lang= \|\| 'en-us'`, C# never appends a query, and i18n-wiring-spec:42's "C# injects window.SL" was never implemented → fixed via `*SL{language-code}` carrier (key verified in all 13 lang files + the C# parser; un-substituted marker falls back to en-us in `SpixiStrings.get()`; `?lang=` overrides intact). extract-strings + i18n-lint now sweep `src/shells` (shell keys were NEVER extractable); home.html `const strings = {}` leak fixed; 5 fallback conflicts fixed at source (callAction · Loading · copyFailed · received/sent casing · addMiniApp); 5 dictionary-less locales (cn/it/id/ja/lt) HIDDEN from both pickers per #256 (launch-shell also carried a bogus `zh-cn` code); **~42 new keys machine-drafted ×7 locales** into `src/strings/draft/*.json` (native review flagged). Docs: polish-roadmap M2/M3/M13/M15 + order corrected · i18n-wiring-spec corrected · be-cutover **C18** + [i18n-C#] hardcoded-English row (HomePage:854 verified) + [SPLASH-ART] + N2 partial-resolve. Batch B/C locks + create-group deferral recorded in #256. ⚠ Mount truncated chat.html/home.html (>90KB) for bash/node this session → generators + smoke = Damir's local run; every edit via file tools on the real files; call-ui.js/callbar.js/call-overlay.js syntax-checked in-session (node --check, small files served fresh). 🟡 **Damir: `node scripts/extract-strings.mjs` (expect ~+42 keys, 0 conflicts) → `build-locales.mjs` → `build-strings-iife.mjs` → `build-demo-bundle.mjs` → `build-shells.mjs` → `i18n-lint.mjs` + `pseudo-locale-smoke.mjs` + `smoke-test.mjs` → F5 (2nd-device ring: overlay on the chats list + at chat open · Accept→callbar in-call ticks · dialing shows no timer · hang-up clears · divider appears once on an unread open, gone next open, absent live-arrival · Settings→Deutsch: shell copy translated, only 8 locales offered) → commit Batch A as ONE batch → SEPARATE Opus #46 loop.** **F5 round 1 (same day): blank app TRACED + FIXED** — `build-demo-bundle` failed locally on the #255 NUL gate: `apps-discover.js` carried two LITERAL 0x00 bytes as a join-separator (the gate's one false positive, NOT mount corruption) → the bundle stayed stale → `build-shells` inlined new shell code against it → `attachCallUi` undefined → home.html died blank. Fix: `LIST_SEP = String.fromCharCode(0)` (same runtime string, byte-clean source; a backslash-u escape was avoided — it round-trips back into a literal NUL through tool-param decoding). Rebuild order matters: bundle BEFORE shells. Version-skew hardening = Opus brief §5.6/5.7. **F5 round 2:** hang-up on a chat-shown callbar ended the call but the bar ticked on (C#'s hideCallBar → stack-last home, the C18 staleness class) → `call-ui.js onHangUp` now hides the local bar OPTIMISTICALLY (failed hang-up healed by the next displayCallBar re-push); remote-hang-up staleness stays C18 — Opus brief §5.1 updated, reviewer audits the fix. Component changed → bundle+shells rebuild before the next F5. **Pickup docs written:** Opus work order = `docs/opus-review-brief-missing-bits-a.md` (loop protocol, auditor scopes, accepted dials, §5 known edges incl. the stale-secondary-ring-overlay hazard — verdict gets appended there) · next fable session = `docs/fable-build-brief-missing-bits-batch-b.md` (B2 pattern default/levels · B3 `wallet_sent.html` shell w/ demo look + `onTx` opt · splash boot blocks · B1 = no FE work, S8 BE-gated).

- [x] **OPUS #46 LOOP over BATCH A (#258, 2026-07-11) → CLEAN.** 3 disjoint auditors → 4 fix agents → a FRESH break-my-verdict reviewer (**found 1 MAJOR the fixers missed**) → round-2 re-review → CLEAN. Verdict + evidence tables in `docs/opus-review-brief-missing-bits-a.md`. **3 MAJORs: 1 fixed FE-side, 2 are C#** — (1) a **stale ring on a second surface can KILL a live call** (`VoIPManager.rejectCall` has no accepted-guard; the answer pushes `displayCallBar` to stack-last only and sends no `clearAppRequests`) → FE half landed (`displayCallBar` drops the local ring + latches the answered session), residual = **C18(a) + new C18b(a)** · (2) a stale callbar's Hang-up fires on a dead session (`hangupCall`, no `hasSession` guard) → **C18b(b)** · (3) an **outgoing call from a chat shows NO call UI** (`initiateCall` bars stack-last = home) → **C19**. **Landed zero-C#:** no-op **`addAppRequest`** in `attachCallUi` (C# pushes it as a bare global into all 12 shells; an undefined global throws BEFORE `native.js` can catch → 10 shells exposed) · answered-session latch · caller re-resolve on re-push · **`.c-callbar__main` = a button only when the return address EXISTS** (the reviewer's MAJOR: home passes `onReturn` unconditionally while C# sends no address → home shipped a dead SR-announced button) · **A3 divider now `loadPhase && bursting`** (loadPhase alone stayed true 5s on the channel-switch/OnAppearing paths → would latch a LIVE message as unread; >250ms history gaps now fail safe to no divider) + per-channel reset + delete re-anchor · **i18n MAJOR:** `App.xaml.cs` persists the OS culture on first run → it/ja/id/lt users land on a HIDDEN locale with an **empty picker** (tapping any row silently moved them off their language) → non-actionable "pending" row (`languagePending`) in both pickers; `launch.html` never passed `language:` at all (pill misreported every locale) → fixed · **§5.6 RULED fail-loud: `build-shells.mjs` PREFLIGHT** asserts every shell-destructured symbol exists in the bundle it inlines → exit 1 before writing (the gate for the skew that killed home.html on F5; **bundle BEFORE shells, always**) · NUL-gate hint + **+14 smoke assertions**. ★ #221 wall / frozen bridge / zero C# / money untouched / #248 all re-verified. 🟡 **Damir dials logged (not changed):** divider = TEXT rows only → an unread block with no text gets **no divider at all** (6-line fix ready on your word) · callbar occludes the chat back button (a11y = occlusion, not a trap) · no ring while locked (release-notes line) · `extract-strings` still has no shell `bareRefs`. 🟡 **Damir: FULL build (extract-strings → build-locales → build-strings-iife → build-demo-bundle → build-shells → i18n-lint + pseudo-smoke + smoke) → F5 → commit Batch A + this loop as ONE batch.**

- [x] **MISSING-BITS BATCH B BUILT + LOOP CLEAN (#259/#260, 2026-07-12, fable session; loop = OPUS-DELEGATED AGENTS in-session per Damir — token conservation, same #46 independence).** **B2** pattern: the #207 desktop hard-force DELETED (the setting was dead on desktop); ONE key, platform-aware default when unset (desktop Off / mobile Standard = token defaults, byte-identical); settings `readChatPrefs` mirrors it; the `?desktop/?mobile` forcing script MOVED above the pattern boot read (ordering now matters). **B3** NEW `src/shells/wallet_sent.html` (WalletSentPage drop-in — full parity: address/fee/txid/live status; the demo detail-card look via the §2.2 GHOST-LIFT; buffered burst renders at `setData`; lone `clearEntries` = buffer reset only; `hideBackButton` rebuilds the topbar; unknown counterparty via `wallet-unknown-*` `*SL{}` carriers; `attachCallUi` spread per #258 §0; VIEW-ONLY) + `wallet-shell.js onTx` opt + home.html tx tap → `ixian:txdetails:<txid>` (HomePage wide pane / narrow page, verified) + build-shells SHELLS/DEFAULT (16). **W2–W4 marked OPTIONAL; new BE row W9** (ActivityStatus-0 first-poll spinner edge + hideBackButton-first defense). **Splash** `.app-boot` cover (pulsing registry logo, reduced-motion, pointer-events:none, dropped at window load) on home/settings/launch(×5)/empty_detail; launch also gained its MISSING instant-bg (dark) + the queued #228 flag. **B1** downloads gate re-verified (S8 stays BE). Loop: 0 MAJOR; fixes = app-boot pointer-events + a focus re-home across the tx shell's live status re-render; A-1/A-2 logged (W9). Zero new strings. Verdict in `docs/opus-review-brief-missing-bits-b.md`.
- [x] **MISSING-BITS BATCH C BUILT + LOOP CLEAN (#261/#262, same session).** **M6 (zero-C#):** the desktop overlay grammar PROMOTED from the demo — `overlay.css` `:root[data-desktop]` block (every c-sheet = centered dialog; anchored `[data-dt-anchor]` variants fade in place; `[data-dt-ctx-source]` highlight; modals unchanged; mobile byte-untouched) + NEW `src/components/desktop-anchors.js` (the demo's 600ms observer recipe + `anchorSheetAbove`; no-ops without data-desktop) wired into chat.html (`.c-bubble-row` menus + attach-⊕ popover) and home.html (`.c-chatlist-item` row menus); channel selector already the top-anchored exception (untouched); money locks JS-side (CSS can't weaken — re-verified); desktop.html demo NOT forked (production selectors out-specify `.dt-frame` with identical values). **M7/M8 (small C#, presentation-only):** HomePage `ixian:newcontact`/`newapp` → detail-column panes (tag "formpane" tag-replaces between the two; narrow unchanged; close-audit at tab/chat/tx + BIDIRECTIONAL cross-close with the chat-info pane per the loop fix; QR hand-off verified root-nav). Loop: 0 MAJOR (matrix all PASS); dials logged (AppDetailsPage still full-window at fetch · staging-window tap drop · stale-ctx call-row NIT). Smoke +~20 static guards (B+C blocks). Verdict in `docs/opus-review-brief-missing-bits-c.md`. 🟡 **Damir: `node scripts/build-demo-bundle.mjs` → `build-shells.mjs` → `smoke-test.mjs` → build net10.0-windows (HomePage.xaml.cs changed — NOT Rebuild) → F5 (#259 + #261 checklists) → commit B (#259+#260) + C (#261+#262) as two batches (or one if F5'd together)**. ⚠ the BUILT launch shells in Raw/html carry pre-existing #255 NUL debris — the rebuild regenerates them; never commit stale built artifacts. **Next: Batch D (create group) stays DEFERRED to the wallet pass (#256) · remaining desktop sweep per fable-build-brief (#234 lock fix BE-gated · reply-to BE-verify-first · wallet-send LAST).**

- [x] **F5-ROUND FIXES + BATCH D BUILT + REVIEWED (#263/#264, 2026-07-12, same session; small C# + components + shells + CSS → FULL build; both Opus-reviewed CLEAN 0 MAJOR).** **#263:** multipane tx tap did NOTHING (the rightContent swap landed UNDER the #225 pinned chat overlay) → rerouted via `pushPageLoaded(…, "txdetail", 1)` w/ back + close-audit · desktop camera BLACK feed → scan `getCameras()` device pick (IR/virtual filtered) + user-facing fallbacks + real errors logged (F12) · **GLOBAL HIDE** (Damir ask): hidden balance now masks every tx row's amount/fiat/counterparty (`tx._raw` swap; C# re-pushes `setHideBalance` on load → restart persistence free; detail page = explicit reveal, dial) · desktop-ring ruling: full-viewport = C18(a) broadcast (or a native cover) — logged on C18. **#264 (Damir's "fix all" list):** Downloads = **S8 LANDED** (SettingsPage verb → Backup-style pane/takeover; hub un-gated; `DownloadsPage.onBack`→popPageAsync; ★ the pre-existing `..`-traversal on open/delete is now USER-REACHABLE → **escalated to security-review-for-be-engineer.md**, ~10-line BE guard owed) · payment request ACTIONABLE in chat (latent #187 bug fixed: viewPayment now sends the MESSAGE id — Details was silently dead on paid/sent cards; incoming pending request = request-in card, **Pay → the native WalletContactRequestPage review/confirm, C# signs, zero new C#**; Decline renders only when wired) · group typing un-gated (non-bot groups; generic pill — attribution + relay verify = **C21** [BE]) · secure-notice recolor (Damir spec: neutral-02 / action border / action link / accent medallion) · "accepted your request" = centered EVENT CHIP (template carrier w/ `{0}` slot; #211 address canon — the bare-base58 case reads clean) · launch/create gradient SOFTENED (lock #206⑥ recipe, violet identity kept — art dial) · lock ≤560px height scrolls (no overlap) · call cards = file-bubble MEDALLION grammar (outcome tint; `data-call-outcome`) · desktop call polish (ring = centered card · callbar = floating pill; delivery stays C18) · money comma/dot = ALREADY DONE (`sanitizeAmount` #135-M2) · `docs/wallet-parity-analysis.md` (Send = W5+W6+#255 filter, then flip `composeSend`; Receive ≈ parity mod W8). **FLAGGED OUT (BE/Damir):** reply-to M1 (no protocol carrier) · call broadcast C18/C18b/C19 · **create group = DAMIR CALL** (re-opens the #256 deferral; touches the shared money-picker page) · CI7 group rename · C10 2-bubble request · A8 media preview · Q16 delete-wipe verify · Q17 restore-nudge signal · Q18 reject tombstone · pin-message/pinch-zoom backlog · search-bar thresholds (recommendation: chats ≥8 rows; wallet already scroll-tucks — Damir picks) · skeletons: recommendation NO. 🟡 **Damir: FULL build (`build-demo-bundle` → `build-shells` → `smoke-test`) → build net10.0-windows (NOT Rebuild) → F5 the #263+#264 checklists → commit (B/C/D batches as tested).**

- [x] **QUIRKS-FINAL ROUND BUILT — ALL FOUR BATCHES (#266–#270, 2026-07-12, fable per `docs/fable-build-brief-quirks-final.md` + Damir's desktop-scrim add-on; UNCOMMITTED; the Opus #46 loops = SEPARATE session, work order `docs/opus-review-brief-quirks-final.md`).** **Q1 (#266/#267):** ⑪ ★ composer LOCK on pending contacts — `showRequestSentModal` (was a dbg stub) → 3-state lock; outgoing = "Waiting for {name}…" strip + **Cancel request** (`ixian:undorequest`); incoming = request pane only; explicit unlock ("0" push / Accept click); early lock via a `chat-waiting-for-response` carrier; the app can no longer show delivered/read ticks on a message the peer never gets · ⑩ Decline = SINGLE-CLICK (the confirm modal was the "two clicks" — dropped, one-shot) · ① app-details → the "formpane" detail column (tag-replace with add-app; close-audit extended) · ② Downloads = a TRUE Account sublevel (S16a: `ixian:loadDownloads`/`openDownload:`/`deleteDownload:` on SettingsPage + cap `downloadsInline`; mobile keeps the takeover) **+ the `..`-traversal guard LANDED** (`TransferManager.resolveDownloadPath`, both hosts — security-review §1 item CLOSED; residual = receive-time `transfer.fileName`, be-cutover S16). **Q2 (#268):** ④ excerpt canon → LOCALE-PROOF carriers (12 new `#sl-carriers` spans; the #265 missed/no-answer strings now map to the orphaned `phone-off` glyph — Damir's "no icon" bug; contact rows mapped too; enumeration table in the review brief) · ⑥ wallet request-from-contact LIVE (**W8 LANDED**: `ixian:sendrequest:` lifted verbatim onto HomePage; people-only roster minus pending; a request = a chat message, nothing signed) · ⑨ added-to-group chip = **BE row GJ1** (verified: `StreamProcessor.createGroup:529-533` emits NO message — nothing to render) · desktop contextual menus (right-click dropdown · ⊕ popover · channel selector) = **NO backdrop wash** (`[data-dt-clear]` scrim tag; element stays for outside-click dismissal). **Q3 (#269):** the i18n sweep — the whole WALLET TAB rendered English under any locale (`walletStrings` one-key override → now live-dict merge + new `viewExplorerAddress` key) + hardcoded nav/topbar/FAB chrome threaded (new key `newChat`); 4 raw-English C# alerts enumerated → [i18n-C#]; both lint gates proven BLIND to all of it (inventory). **Q4 (#270):** ★ THE NATIVE CALL SURFACE — new `CallPage` + `src/shells/call.html` (17 shells): ONE C#-presented cover (lock-grammar in-place stage, own static slot, ZIndex 100; locks now 200 = still cover calls); ring = full-window (back swallowed; modal-push fallback when a legacy page covers the host) · in-call = a 64dip TOP STRIP over every pane (C19 closed); identity pushed by C# (nick + X1 avatar) — always right; `broadcastCall*` keep their names but drive the surface; per-page call pushes + the dead 4-arg mini-app card DELETED; **call-ui.js + its 12 shell wirings REMOVED** (tombstoned — **Damir: `git rm src/components/call-ui.js`**); ring timeout + C18b guards + `acceptsCallPushes` kept; be-cutover C18→superseded, C19→landed, new **C22** (return-to-call route) + **GJ1**. New string keys: `waitingForAccept` · `viewExplorerAddress` · `newChat` (inline fallbacks → extract). Smoke re-based (decline single-click · call-surface block + 13-shell no-wiring sweep). ⚠ Damir's open questions saved in the review brief §last (② on mobile · scrim scope · C22 · grammar sign-off). - [x] **OPUS #46 LOOPS over QUIRKS-FINAL — ALL FOUR BATCHES DONE (#271 = Q1/Q2/Q3 · #272 = Q4) → PASS.** Q4 (the native call surface) was the last: **5 MAJOR, and the two headline ones were LOCK-INTEGRITY bugs.** The ring's MODAL fallback (used whenever a legacy page covers the host) lands on MAUI's ModalStack — **above the entire page tree, i.e. above the lock**: it painted the caller's nick+avatar + Accept/Decline **on a locked device**, an unlock popped the RING instead of the lock, and a remote hang-up / the 45s timeout could **pop the LOCK itself** (`PopModalAsync` = pop-the-TOP) with `isLockScreenActive` left latched ⇒ **no lock for the rest of the session**. **★ DECISION: the lock and the call surface are now MUTUALLY EXCLUSIVE and the lock wins** (`lockUp()` incl. a *staging* lock · `App.OnResume` teardown · a top-of-stack pop guard) — a call arriving while locked **rings audibly but shows no UI**, and the ring/bar returns within one UI tick of the unlock (#258's "no ring while locked" dial, now enforced). Also: answering a modal-fallback ring left the call with **no UI and no hang-up** (dispatched-teardown ordering) · the desktop bar was a **clipped pill on an opaque band** (callbar.css out-specified the shell) · `smoke-test.mjs:3476` would have **FAILED Damir's build** (stale DEFAULT regex). ★ #221 re-verified: the outbound wall is now structural (no broadcast exists — `setCallUi` reaches only CallPage's own WebView); the INBOUND `acceptsCallPushes` gate was deliberately KEPT when the dead `getLivePages()` enumerator was deleted. Escalated to `docs/security-review-for-be-engineer.md` **MAJOR #5** (+ the class-wide `PopModalAsync` = pop-the-top finding). 🟡 **ON-DEVICE (#215 gate): ZIndex-vs-Children-order on WinUI is the ONLY thing keeping the lock above an in-place call stage** — re-order-Children fallback specified, not pre-implemented.
🟡 **Order: Damir: `git rm` call-ui.js → FULL build (extract-strings +3 keys → locales → iife → bundle → shells → lint/pseudo/smoke) → build net10.0-windows (NEW files `Spixi/Pages/Call/CallPage.xaml(.cs)`) → F5 per-batch checklists (⑪ + ③ are 2-DEVICE; ③'s checklist = the #270 status column) → commit Q1/Q2/Q3/Q4 as four batches → freeze → `git tag audit-baseline` → audit-refactor-plan phase 1.**

- [x] **★ FROZEN + COMMITTED + TAGGED `audit-baseline` (2026-07-12).** Q1–Q4 (#266–#270) + both Opus #46 loops (#271, #272) shipped. **NEXT SESSION: read `docs/handoff-post-freeze.md` FIRST** — the project has changed shape (build → test/BE/platform), and that doc says which phase you're in and what NOT to do. Order: desktop testing (Damir) → Android → iOS → **BE cutover** (`docs/be-cutover-brief.md` + `docs/security-review-for-be-engineer.md`, walk the engineer through the security doc first) → **only then** refactor. **The refactor is explicitly DEFERRED** (zero user value; makes platform bugs ambiguous; the smoke suite is grep-only over the shells a sweep would target → behavioural shell coverage is its prerequisite). Wallet-send stays LAST; reply-to stays BE-blocked.

- [x] **PHASE B OPENED — Mac bring-up session 1 (2026-07-20).** First non-Windows build+launch ever: net10.0-maccatalyst compiles and WKWebView renders the launch shell (fonts/icons/layout OK). BLOCKED at boot on RocksDB: the public RocksDbSharp wrapper misdetects MacCatalyst as Windows (no OSX match -> kernel32 probing) - needs the dev's custom RocksDB-Sharp, same as Android/iOS. Dev asked for `RocksDB.0.0.42.nupkg` (-> `local-nuget/` feed, wired) + optional maccatalyst xcframework slice. csproj: Windows TFM now OS-conditional; catalyst NativeReference shim (gitignored). Details + next steps: `docs/mac-bringup-log.md` + `docs/handoff-2026-07-20-pc-next.md`. Next: PC/Android once the nupkg lands; iOS Simulator on the Mac same-day after that.

- [x] **Mac session 2 (2026-07-20, fable): TWO ZERO-C# BUG BATCHES BUILT (#273 + #274; components changed → FULL build = bundle BEFORE shells; UNCOMMITTED; NOTE: Node still not installed on this Mac — build runs on the PC or after the owed Node install).** **#273 Requests-filter phantoms** (Damir F5: chip=21 on a legacy account, rows accepted years ago): CH6 canon typed "Contact Accepted" as excerpt kind `request`, and the Requests filter/chip/leave-guard key on that kind → every settled contact whose last message is still the requestAdd event counted as pending. Fix: new `request-done` kind — same user-plus glyph/tint (chatlist-item.js/.css), excluded from filter+chip; "Contact Request" stays `request` (C# emits it only `!approved`); M5 "Request sent" rows = genuinely pending, correctly stay. **#274 Account-pane Language picker** (Damir F5 r2): (a) list clipped ~480px — the sheet cap (`opts--scroll`) leaked into the inline pane sublevel → gated on `!inline` (settings-shell.js), pickerScreen body scrolls; (b) pick → C# reloads settings.html (SettingsPage:289) → pane reset to empty detail → `spixi.settings.view` one-shot stash (landtab class, 'language' only, no user data) + boot restore + `langBuilt` refresh guard for the post-reload `setLanguage` push. Smoke +4 assertions total. 🟡 **Damir: `node scripts/build-demo-bundle.mjs` → `node scripts/build-shells.mjs` → `node scripts/smoke-test.mjs` → F5 (Requests chip = real pendings only, accepted rows keep glyph but leave the filter · language list fills+scrolls in the pane · pick Deutsch → pane returns ON the Language picker, new check, translated UI) → commit as two batches (#273 · #274).**

- [x] **Mac session 2, round 2 (2026-07-20): #275 COMPOSER LOCK (small C#) + #276 TRUNCATION SWEEP (FE; component changed → FULL build).** **#275**: legacy-account chats in non-Approved states beyond RequestSent/RequestReceived composed + fake-delivered messages (⑪ class) while their list row said "Request sent" — SingleChatPage now locks on `state != Approved && != RequestReceived` (onLoad) and the updateScreen pending branch is a plain `else` (waiting presence + unlock edge-detector for legacy states too). C#-only. **#276**: #211 canon swept over wallet + contacts — contacts rows/pending-profile (`hasNick` guard, address-echo ≠ nick), wallet tx rows (truncated display + raw kept as `tx.address` for search), tx-detail "Sent to" title; full addresses remain only on copyable address fields. Smoke +4. **Open from this round: Account-pane Language panel (#274) reported NOT fixed on first VM test — awaiting Damir's F12 check (`VIEW_RESUME_KEY` present in the running shell?) to split stale-artifact vs logic bug.** 🟡 **Damir: `node scripts/build-demo-bundle.mjs` → `build-shells` → `smoke-test` → build net10.0-windows (SingleChatPage.xaml.cs changed — NOT Rebuild) → F5 (open a "Request sent" row → composer locked, waiting strip + Cancel · contacts directory + wallet rows + tx detail show truncated addresses) → commit.** ⚠ Housekeeping: delete `_to_delete/` + `_to_delete_index.lock*` at repo root (sandbox can't delete; contains only moved-aside git locks + restage copies) — do not commit them.

- [x] **Mac session 2, round 3 (2026-07-20): #277 tx-row title parity + #278 misstx width-aware collapse (FE; component+CSS → FULL build, same batch as #275/#276).** **#277**: `.c-txlist-item__name` label-lg-semibold (a stranded pre-#227 role, 16px desktop) → body-lg regular = exact `.c-chatlist-item__name` parity; amount keeps label-lg semibold (money emphasis, Damir dial). ⚠ Figma mirror queue: tx-row text style. **#278**: the #98 "Missing a transaction?" pill collapsed only via a 360px VIEWPORT query → cut off in a narrow desktop PANE (the flagged container-query gap) — now collapses by MEASUREMENT (ResizeObserver on the filter row → `data-compact` → ⓘ-only + title tooltip; media query kept as the no-RO belt). Placement kept per #98 (no design re-litigation). Smoke +2. 🟡 F5 adds: wallet rows/addresses = chat-row size · shrink the pane → pill becomes ⓘ, widen → label returns, never clipped.

- [x] **SESSION CLOSE 2026-07-20 — NEXT SESSION: read `docs/handoff-2026-07-20-home-pc.md` FIRST** (test + close the #273–#278 bug-fix batch on the home PC, then the owed Opus #46 loop; #274 Language-panel verdict protocol inside). Consumed handoffs/briefs ARCHIVED to `docs/archive/` (incl. the superseded `handoff-2026-07-20-pc-next.md` — its Android steps stay valid there for when the nupkg lands, and `fable-build-brief-desktop-pass.md` — its open units were only the three gated ones: wallet-send LAST · reply-to BE-verify-first · #234 BE sign-off). Live docs now: the new handoff · `handoff-post-freeze.md` (phase rules) · `mac-bringup-log.md`/`-checklist.md` · `be-cutover-brief.md` + `security-review-for-be-engineer.md` · `polish-roadmap.md`.

## Conventions (to firm up as we build)

- Source in `src/` at repo root; built output committed to `Spixi/Resources/Raw/html`.
- Bridge access only via `src/bridge/` (`mock.js` for browser, `native.js` for MAUI) — shells never touch `ixian:`/`executeUiCommand` directly.
- Strings via a per-shell `window.SL` dictionary; config via `window.SPIXI_ENV` (ARCHITECTURE.md §7).
- [x] **Mac session 3 (2026-07-22): RocksDB gate CLEARED → first-ever iOS run + crash-family fix (#279–#281).** Dev nupkg → `local-nuget/` (sha512-verified, gitignore exception) — NU1603 gone, android/ios restore unblocked. Node 26.5.0 on the Mac; full pipeline + smoke ran → **#279** (smoke caught the stale pre-#276 §3a expectation; sort canon = display canon via `hasNick`) · `rebuild generated artifacts (#273–#279)` committed = home-PC rebuild step CLOSED. csproj: RID-aware RocksDB xcframework slice + Debug iOS RID default `iossimulator-arm64`. iPhone 17 Pro sim: boot/account/wallet/chats/messaging all work; findings punch list `docs/ios-sim-findings.md` (iOS-1…10). Crash triage via .ips + guarded-run log: OneSignal prefixed-selector swizzle vs .NET registrar → **#280** (callback guards) + **#281** (export prefixed selectors) — sim-verified (incoming call rings, app lives).
- [x] **SESSION CLOSE 2026-07-22 — NEXT (PC): read `docs/handoff-2026-07-22-pc.md` FIRST** (Windows F5 #273–#279 + iOS-8 cross-check + Android bring-up per archived steps). Mac later: edge-to-edge trio first, then iOS-2/6/10, locale/lock, Safari pass, Stage-5. Owed: Opus #46 loop over #273–#281. Consumed `handoff-2026-07-20-home-pc.md` → `docs/archive/`.
- [x] **Mac session 4 (2026-07-27, Cowork/cloud): iOS-1/3/4 EDGE-TO-EDGE BUILT (#282; CSS+shells+small C# → `build-shells` only, NO bundle rebuild — no JS changed; built + NUL-checked + smoke 348✓ in-session).** Root cause = `applyPlatformPageChrome`'s blanket iOS page padding; shells were env()-ready all along. C#: padding dropped for redesigned shells, kept for the 8 no-viewport-fit legacy pages (`hasLegacyPageChrome`); riders: `surfaceColorFor` wallet_sent stale legacy-blue entry + WalletSentPage backing → surface (both #259-stale). CSS: topbar/bottomnav own the inset regions (height grows, surface paints under); composer/scan/callin/launch/lock bottoms. CallPage bar stage grows by the top inset (iOS only). Android untouched (native insets stay). ⚠ Workflow note: this session ran via the device bridge — edits staged→committed per file, generators run on the mounted repo via the device VM's node 22 (smoke's contacts-timer tail exceeds the 45s window = known exit-124; Damir's local run confirms the tail). 🟡 **Damir: sim-verify #282 (launch/wallet/chats strips gone · dark chrome · status-bar text both themes) → commit; plumbing checklist findings → ios-sim-findings (next = iOS-11). Owed unchanged: Opus #46 loop over #273–#282 · iOS-2/6/10 · Deutsch/lock/Safari/Stage-5.**
- [x] **SESSION CLOSE 2026-07-27 — #282 sim-verified by Damir + COMMITTED. NEXT SESSION: read `docs/handoff-2026-07-27-issues.md` FIRST** (fix order: iOS-6 keyboard · iOS-8 unread cross-check · iOS-10 link handoff · iOS-2 illustration · any iOS-11+ checklist findings; owed Opus #46 loop now spans #273–#282). PC handoff (`handoff-2026-07-22-pc.md`) stays live — Windows F5 + Android untouched.

- [x] **iOS device pass (#283, 2026-07-27, committed b3059f67):** 12 fixes from Damir's 2nd sim pass — chat keyboard viewport · request-unread pair · external-link browser handoff (iOSWebViewHandler) · FAB inset · mobile tx sheet · tick grammar · media fitTile · backup local stamp · Change-password (`ixian:encpass`) · delete-history immediacy · launch backup illo. Device walk → new findings iOS-26..iOS-31 (`docs/ios-sim-findings.md`). ⚠ Committed WITHOUT re-verification of the fixes — see the review row below.
- [x] **Cowork session 2026-08-19 №2: N71 LANDED + Damir's CHAT PALETTE (#421–#424; UNCOMMITTED; C# + components + all 18 shells + tokens → FULL pipeline; smoke 2070 → 2107 / the same 4).** ★ **N71**: the OS theme flip PUSHES `setTheme` instead of calling `reloadAllPages()` — which also closes **N78**, because a push creates no document, so `home.html`'s Fix #8 never fires and no empty-state gate is re-armed. **Fix #8 is untouched** (it is correct for the reload it was written for). Both theme paths now run ONE shared enumerator `UIHelpers.getLiveShellPages(bool includeModal)`; the explicit-pick path's hand-rolled list is gone, including its DEAD `getDetailContent()` branch. All 18 shells define `setTheme` (13 newly) via a new shared `src/components/theme-runtime.js` — C# emits it as a BARE GLOBAL, and an undefined one throws before the dispatcher can catch it (#258). ★ **N81** (Damir 2026-08-19): cream `#fcfbfa` / midnight `#0f1115` canvas · incoming `#ffffff`/`#1a1d24` + hairline · ONE outgoing `#1956B2` · light's blue wash DELETED, dark keeps its glow. The structural half matters more: the pattern ink is opaque and the level is an INDEX resolved to a per-theme alpha **by CSS**, retiring the `× 0.36` dark derivation that lived in three JS copies — and closing the #239 ⓐ flag for free. Ladder is Off / Default / Strong (0.1). **#46 loop: 4 MAJOR over 4 rounds** — the 8 LEGACY pages silently stopped re-theming (sweep is HYBRID now: push where there is a handler, `reload()` where the theme is baked) · `chat.html` called two bundle exports it never destructured, at module top level = **every conversation would have booted to a permanent spinner**, and I reintroduced the same class in the fix round inside a handler · the flat bubble INTRODUCED a sub-AA dark timestamp (4.28) · and my own fix regressed the #407–#410 bar round (`applyPlatformPageChrome` is NOT page-local on Android — it paints the ONE window insets controller, so per-page calls let the LAST page pick the glyph colour). ★ **Two of the four were invisible to every gate we had, because every gate reads SOURCE** → new **DESTRUCTURE GATE** over all 18 shells + a **jsdom BOOT gate** on the built chat shell + an end-to-end push pin. Mutation runs caught 3 self-defeating pins. ⚠ **`src/components/theme-runtime.js` is NEW — `git add` it.** 🟡 **Damir: `git add` the new file → FULL pipeline (extract → locales → iife → bundle → shells → lint + pseudo + smoke) → wipe `obj`/`bin` → build → F5 `docs/f5-checklist-2026-08-19-n71-n81.md` (the acceptance test is §1: flip the OS theme on Wallet and STAY on Wallet) → commit.** Dials answered (#424): N80 = open counter · N79 = let the redesign retire it · N72 = confirm-as-PENDING, design with N69(b). **NEXT SESSION: read `docs/handoff-2026-08-19h.md` FIRST.**
- [x] **SESSION CLOSE 2026-07-27 (review) — reconciled GitHub↔local + ran the owed Opus #46 loop over #282+#283.** Local == origin tip `fd5b308b` (the 116 "modified" files = CRLF-only churn; `git diff --ignore-cr-at-eol` empty — a `.gitattributes eol=lf` would silence it). Findings → `docs/opus-review-macsession-282-283.md`: **5 MAJOR, incl. two #283 fixes that never landed** — the 5 launch shells were never rebuilt (iOS-2 backup illo absent from built output; `build-shells` DEFAULT excludes LAUNCH_KEYS) · delete-history immediacy is a no-op (`loadMessages` early-returns before `clearMessages`) · iOS-31 + iOS-29 root causes pinned in code · mini-app WebView regressions (global external-link handoff + lost safe-area inset). **NEXT SESSION: read `docs/handoff-2026-07-27-postreview.md` FIRST.** Consumed → `docs/archive/`: `handoff-2026-07-27-device.md`, `handoff-2026-07-27-issues.md`. PC handoff (`handoff-2026-07-22-pc.md`) stays live.
- [x] **Cowork session 2026-07-28: OWED OPUS #46 LOOP over #273–#281 RUN + #283 plate fixes LANDED (#284; UNCOMMITTED; NO bundle rebuild — components untouched; 17+5 shells rebuilt + NUL-swept in-session; smoke = Damir LOCAL, the device VM killed long node runs). READ `docs/opus-review-273-281.md` + `docs/handoff-2026-07-28.md` FIRST.** 3 auditors → verify → fix → break-my-verdict → round-2. **3 MAJOR fixed:** ① #274 restore was DEAD CODE — boot `showHub()` clobbered the restored view (THE "NOT fixed on first VM test" cause; fresh artifacts never mattered) → `renderLayout()` + langBuilt/non-pane/stash hardening · ② #275 lock had NO group guard (non-Approved group → outgoing lock, Cancel = removeFriend w/o sendLeave) → C# both sites + shell `!mode.isMulti` belt (group pending-state = 🟡 core ask) · ③ the loop's OWN A5 one-shot broke RE-ENTRY (latch never reset in onLoad; reload of a live pending chat = UNLOCKED composer) → caught by the re-reviewer, latch joins the onLoad reset block. MINOR fixed: 1 Hz waiting-push topbar churn → pending-edge one-shot + mid-session lock edge + offline-accept presence · **#276→#263 regression: tx sheet leaked the FULL counterparty address under global hide** → address rides the hide flag end-to-end (+ smoke assertion honesty) · DecidePolicy one-shot `decide` · csproj `== Debug` RID + catalyst Exists() gate · ios-sim-findings crash-triage premise corrected (iOS-27 hunts a MARSHALLING abort first — the #280 guard cannot log the #281 class) · archived-brief NUL stripped. **Plate 1+2 landed:** launch shells rebuilt = **iOS-2 SHIPPED** (⚠ `build-shells all` also overwrites LEGACY apps.html/wallet_send.html — restored from HEAD; **use `launch`**) · delete-history BOTH legs (clearMessages before the empty return + R2 request-pane guard · SettingsPage delete-all re-renders via getChatPages · **getChatPages gained the desktop detail-content branch** — also fixes onLowMemory + reloadScreen-all misses). **VERIFY-GATED (#215):** B1 OneSignal class_addMethod starvation (device: does handleNotificationReceived still fire?) · B3 static-registrar Release/device gate · B2 double tap-handling · A2 FriendState enum + non-destructive resend (BE) · C-9 file:// localStorage persistence probe (~8 mechanisms). 🟡 **Damir: local `smoke-test` → build net10.0-windows (NOT Rebuild) → F5 the review-doc 5-point list → commit ONE batch → `git rm --cached _to_delete_index.lock*` + delete `_to_delete/review-273-281/`.**
- [x] **Cowork session 2026-07-29: #284 F5 CLOSED + #285 landed & F5-VERIFIED (3 fixes + same-session regression round). Owed device pass: READ docs/device-pass-runsheet-2026-07-28.md.** Pre-F5: c2af158e verified committed/pushed (untested; tree = CRLF churn only) · 3 stale smoke assertions fixed (launch iOS-2 real backup.svg + zero placeholders ×2 · MINOR-5 stripHeight per #282) · ⚠ bridge gotcha: plain git status strands a 0-byte .git/index.lock on the mount — use git --no-optional-locks. **F5:** #274 ✅ (Deutsch pick returns ON the picker) · #275 ✅ (group live; waiting-strip styling → polish backlog) · delete-history ✅ both legs · **wallet hide ❌ → #285:** the desktop tx-details pane (wallet_sent.html) leaked amount/fiat/name/address under global hide — the old "detail stays unmasked" dial only ever held for the mobile sheet→Details flow → WalletSentPage pushes setHideBalance pre-burst; shell masks money+who, per-view "Show amounts" reveal (Damir dial; jsdom cycle verified on BUILT output) · **language propagation:** #242 deferred reload NEVER fires on desktop (pane tabbed away, not exited) → pick-time HomePage.reloadShell() + getChatPages reload; round 2: shell Fix-#8 boot ixian:tab:tab1 ran the #240 overlay-exit (tore the settings pane) → one-shot suppressNextTabOverlayExit · WinUI flush race → latch re-drive + refresh-flag belt · createButton icon must be an ELEMENT (string threw in the fail-soft dispatcher = detail spinner hang; reveal = text-only, bundle exports no icon()) · **de-de starter fill:** 22 drafts (about*/howTo*/groupNeedsTwo/waitingForAccept…, + revealAmounts — English in the other 6 locales, translator todo). Pipeline: extract 665 → locales → iife → shells DEFAULT 17 (export guard caught the icon miss) → lint/verify/NUL ✓; smoke = Damir local ✓. DECISIONS #285. Also committed: docs/device-pass-runsheet-2026-07-28.md (B3→B1→B2→C-9→iOS-31→iOS-29, file:line log points) · security-review MAJOR #6 (mini-app link handoff + lost safe-area; global handler trust-tier ask). 🟡 **Damir: local smoke → commit ONE batch → hygiene: git rm --cached _to_delete_index.lock* · purge _to_delete/ (review-273-281 scratch + ws-verify-285.html + 2 stale-git-index.lock). NEXT SESSION: read docs/handoff-2026-07-29.md FIRST (plate: device pass per run sheet · mini-app MAJOR #6 · polish). Consumed → docs/archive/: handoff-2026-07-28.md.**
- [x] **Cowork session 2026-07-29 №2 (PC): F5 residue CLOSED + #286/#287 landed & F5-VERIFIED + review now OWED (#284–#287).** Windows F5 of the 07-22 PC-handoff table: #273/#276/#277/#278/#279 ALL PASS · iOS-8 cross-check CLEARS on Windows → findings rows updated (iOS-8 Windows leg verified; **iOS-31 de-escalated from [XPLAT]** — does not reproduce; sim inspection before any code per #215, three-site fix stays parked). Two Damir screenshot finds fixed as **#286** (narrow desktop pane: status badge painted over the fiat → meta overflow:hidden + badge min-width:0 + label ellipsis · "Missing a transaction?" clipped to NOTHING → fit() second measure → data-wrap wraps the ⓘ to its own line + tools max-height 160→220) and **#287** = plate-3 polish (waiting strip token-exact: bogus --spacing-3/-4 px-fallback refs → --spacing-12/-16, composer-hairline parity, NEW avatar-32 action-tonal medallion + user-plus 18 with the #117 pulse cadence + reduced-motion escape; text body-md · revealAmounts drafted ×6: es/fr/pt/ru/sl/sr — per-locale english-fallback 22→21, residual = #285 de-fill tail). ★ FULL pipeline ran IN the device VM (bundle 235 exports → locales → iife → 17 shells → i18n-lint + NUL ✓; built output spot-verified); smoke Damir local ✓; ALL F5-verified in-session. Docs: DECISIONS #286/#287 · ios-sim-findings iOS-8/iOS-31 · **handoff-2026-07-29b.md = live plate (1 Opus loop #284–#287 with paste-ready prompt · 2 device pass runsheet · 3 mini-app MAJOR #6 · 4 Android bring-up)** · consumed → archive: 07-27-postreview / 07-29 / 07-22-pc. 🟡 Damir: commit ONE batch (msg in session chat). **NEXT SESSION: read docs/handoff-2026-07-29b.md FIRST; the #46 loop over #284–#287 is OWED.**
- [x] **Cowork session 2026-07-29 №3: OWED OPUS #46 LOOP over #284–#287 RUN → 2 MAJOR + 11 MINOR FIXED (#288; report `docs/opus-review-284-287.md`; UNCOMMITTED). READ THAT REPORT FIRST.** 3 disjoint auditors → verify-against-tree → fixes → break-my-verdict over the FIXES (broke 2 of mine, corrected) → round 2. **MAJOR-1:** the wallet hide flag was pushed to NOBODY — an OPEN desktop tx-detail pane kept showing amount/fiat/fee/name + the FULL address + copy button after the eye was tapped (3rd regression of this class; #285 closed only hide-THEN-open). Fixed C# (re-push to open overlays) + shell (mask is now fail-SAFE until the flag arrives). **MAJOR-2:** the 5 launch drop-ins shipped a **664**-key dictionary vs 665 everywhere else — `build-shells` `DEFAULT` never held the launch keys, so #285/#287 never rewrote them (German → English launch picker). The last loop fixed this by hand; now **structural** (DEFAULT spreads LAUNCH_KEYS → 22 shells) + `all` no longer clobbers legacy `apps.html`/`wallet_send.html`. MINORs: `suppressNextTabOverlayExit` unexpiring one-shot → tab1-keyed + **epoch-guarded** belt clear · belt TOCTOU + silent catch · language pick never re-localized the desktop **welcome pane** (`reloadDefaultDetail` had zero callers) + unguarded `reload()` sweep · bot-flip left the composer permanently locked · **#287 strip + request card lacked the iOS home-indicator inset** (Cancel/Accept under the indicator) · #286 `fit()` never restored the label after wrapping · tx `__time` couldn't ellipsize · **#285's de-de `developer` fill silently didn't land** (legacy-reuse shadowed the draft; blast radius 5 values, all now matching en-us) · `{name}` `String.replace` `$&` expansion · **13 phantom/wrong-value custom props in chat.html's channel-selector + app-picker** (⚠ VISUAL DELTA — eyeball both themes) · **`smoke-test.mjs:3468` would have FAILED the local run** (stale DEFAULT regex, #272 class) → de-staled + 5 regression pins. Flagged not fixed: 🟡 list-vs-chat pending-GROUP disagreement (core-gated) · 🟡 `onDeleteAccount` lacks the live-chat sweep · `getChatPages`'s #284 branch is dead code (`detailContent` never assigned) · txid/explorer survive the mask (Damir dial). Pipeline in-session: locales → iife → bundle (235) → 22 shells → i18n-lint ✓ · verify-locales ALL CLEAN ✓ · NUL ✓. 🟡 **Damir: local `smoke-test` + `pseudo-locale-smoke` → build net10.0-windows (NOT Rebuild) → F5 the report's 7-point list → commit #288 together with #284–#287.**
- [x] **#288 F5 ROUND (#289, same session): tx-detail "Show amounts" showed a BLANK card — root cause NOT layout** (shell-only → `build-shells`, NO bundle rebuild; audited 0 BREAKS). `checkTransaction` pushes `clearEntries` FIRST then early-returns on an unchanged status (`WalletSentPage:121`/`:129-133`), and `updateScreen` polls at 1 Hz until `isConfirmedDisplayed` — only ever set for a **Final** tx (`:137`) — so on any PENDING tx the shell's buffer is empty a second after open while the card still shows the last burst. Safe while `setData` was `render()`'s only caller; #285's reveal added a second → revealing rebuilt from `{}`. **A confirmed tx stops polling and reveals fine, which is why it looked narrow-only.** Fix = **staging buffer** (clearEntries/setReceivedMode/addEntry stage; setData commits) → `render()` idempotent for every caller. jsdom against the BUILT shell: pre-fix **5 failures matching the report**, post-fix **13/13**. Audit landed: reveal eye now waits for the flag (was offered in the fail-closed unknown window, where the first push silently undid the tap) · one `isMasked()` instead of a duplicated predicate · comment corrections — note an UNCHANGED-value `setHideBalance` re-push **preserves** the reveal, which is what makes #288's balance broadcast safe, and the "received `+` flipped to `-`" symptom is NOT observable (recognise a recurrence by the BLANK card). Also caught on myself: the refactor broke my own #288 smoke assertion → re-pinned before it could fail the local run. BE (verify-first): `WalletSentPage` `activity == null` → NRE at `:153-160`; `lastActivityStatus` latched before `ctransaction`; the `= 0` sentinel. 🟡 **Damir: local smoke → build net10.0-windows → F5 a PENDING tx: hide → open detail → Show amounts.** **NEXT SESSION: read `docs/handoff-2026-07-29c.md` FIRST.**
- [x] **Cowork session 2026-07-30: LEGACY-PARITY AUDIT DONE (#296, the #295 mandated first task) → `docs/legacy-parity-audit.md` — READ THAT DOC before planning v1.0 work.** 6 auditors + adversarial verifier over baseline `0e85a4b8` vs HEAD `20089c10`. Deferral theory holds (rationale claims all confirmed; open cutover rows = mostly enhancements) **BUT 13 parity regressions found, nearly all zero-C#** (chat loadmore · paid-bot cost · media cap one-liner · backup nudge dead+burned · dev cluster/iOS-22-wider · mobile tx depth [iOS-13 dial, 🟡 re-decide] · share sheet Win/Android · in-chat unread badge · contact-details presence · rating prompt · bot description · scan zoom · 64k guard · dual-app multi-launch) + **10 security rows pinned to v1.0** (C15+S13-sink · C16 · L1/L2/L5/L6/L8 · W9 · #82 posture) + C9 premise refuted + be-cutover-brief stale ×5 (S5/L4·S7·S13·CO2 = landed). 🟡 **Damir: read the doc → 3 dials (R3 media-cap scope · R6 mobile tx-detail re-decision · R4 timestamp-burn fix) → then parity batch A (zero-C#, components mostly exist, #46 loop after).** ⚠ The #295 scope section + DECISIONS #295 were still UNCOMMITTED in the tree — commit them together with this audit doc + #296 row.
- [x] **SESSION CLOSE 2026-07-30 — NEXT SESSION: read `docs/handoff-2026-07-30.md` FIRST, then build PARITY BATCH A per `docs/fable-build-brief-parity-a.md`** (11 zero-C# items, evidence pre-gathered in the audit doc — don't re-derive). 🟡 Damir before that session: commit the docs batch (#295 scope + #296 audit + brief + handoff) · delete `_to_delete/` + `_gitlock_trash/` locally (bridge can't) · 3 dials (R3/R6/R4) unblock batch B, not A.
- [x] **Cowork session 2026-07-30 №2: PARITY BATCH A BUILT — 11/11 zero-C# items + 2 free fixes (#297–#299; UNCOMMITTED).** Damir asked to analyse before building; that preflight (`docs/parity-a-preflight.md`, #297) found **6 of the 11 briefed plans were defective — two would have shipped something worse than the existing stub** (A7's shell-side guard would have DESTROYED the user's 64 000 chars because `composer.js send()` clears unconditionally; A3's numeric badge would have rendered a stale count from an edge-latched push whose first value includes the chat you just opened). Also: A2's cost line would have read "Each message costs Sending messages costs…"; A6 targeted a demo-only component and A10 would have silently dropped a requested payment amount. **Two free fixes found in passing: W1 — the chat presence dot has been DEAD in 5 of 8 shipped locales** (a `/online/` regex against a localized string) · W2 `avatar.css` had no `[data-size="64"]` rule. **#298 = `docs/chat-transport-spec.md`, a standalone BE work order** written after Damir asked for scroll-triggered history: his three asks (scroll history · faster chat entry · drop the flicker delay) share ONE root cause — there is no prepend, and every message is its own `EvaluateJavaScriptAsync` (100+ marshals to open a chat). Four small C# parts (`addMessages` batch · prepend · `messagesDone` · smaller window) fix all three; the A1 pill is deliberately the honest affordance for today's re-flush transport and ~60% of it carries over. **#46 loop ran: 2 MAJOR + 8 MINOR fixed** — incl. an A8 **regression on a shipped feature** (torch-off left the LED lit) and an A9 header overflow that squeezed the app name to zero on a 360px phone. Pipeline green: bundle 237 · 22 shells · i18n-lint ✓ · pseudo 9/9 · **smoke 868 pass / 4 fail = the SAME 4 already failing at HEAD** (zero regressions; 54 new assertions). ⚠ Those 4 are the owed #290–294 runs — real, unrelated, worth their own session. 🟡 **Damir: F5 per `docs/parity-a-f5-checklist.md` → ONE commit. Batch B still needs the 3 dials (R3/R6/R4).**
- [x] **BATCH A F5 ON DEVICE (2026-08-04, iPhone 15) — 6/8 testable items PASS, ZERO batch-A regressions (#300). NEXT SESSION: paste `docs/next-session-prompt.md`; evidence in `docs/handoff-2026-08-04.md`.** Four findings, **none of them batch-A regressions**. **F1 (priority): iOS scan is completely dead — pre-existing, C#, = iOS-49 from #293 left half-finished.** Torch works while nothing scans, which IS the diagnosis: torch rides a live track, so the camera is running and the failure is video RENDERING (html5-qrcode decodes the `<video>` via canvas; no inline playback → nothing to draw). `MediaCaptureUIDelegate` landed in #293, `AllowsInlineMediaPlayback` never did (zero hits). Can't be set in `ConnectHandler` — `WKWebViewConfiguration` is immutable post-construction; override `CreatePlatformView()`. **A8/R12 is UNOBSERVED, not closed.** **F2: pinch raster-zooms every shell except chat** — chat.html has carried the correct viewport clamp since 2026-07-29 (for pinch-to-TEXT) and it was never swept to the other 17. ⚠ forces a call: `--chat-text-scale` is chat-only, so clamping everywhere removes the only text-size control on wallet/apps/account. **F3: wallet Share must not carry `:send:<amount>`** (Damir — that request format isn't shippable yet); share the bare address always, which collapses A10's amount gate. **F4: presence stays "online" ~2 min after a peer quits — NOT FE.** The render shows what C# pushes; `Node.cs:418-455` only goes false on PresenceList EXPIRY, never on a clean quit → BE/Ixian-Core trust-signal row. Still owed: the >64 000 paste A7's guard has never actually seen (20k passed = the normal-use half only) · W1 in es/ru · **A10 on WINDOWS, which decides part of batch B** · A2/A6/A9/A11 (need a paid bot, a bot, a dual-capability app, a 30-day timer). Batch B still blocked on the 3 dials (R3/R6/R4). ⚠ Bridge gotcha that cost time: plain `git status` on the mounted folder strands a 0-byte `.git/index.lock` and GitHub Desktop then refuses to commit — always `git --no-optional-locks`; the bridge can't delete it, move it to `_to_delete/`.
- [x] **Cowork session 2026-08-04 №2: F5 FIX BATCH BUILT (#301; UNCOMMITTED; components+bridge+17 shells+2-line C# belt → FULL pipeline ran in-session, smoke 903/4 = the pre-existing 4 only). NEXT SESSION: read DECISIONS #301 + `docs/f5-checklist-2026-08-04-fixes.md` FIRST.** The pre-build analysis caught the batch's own brief being wrong, again (#297 class): **F1's prescribed `AllowsInlineMediaPlayback` C# fix is a VERIFIED NO-OP** — MAUI's `MauiWKWebView.CreateConfiguration` already sets it (+ `MediaTypesRequiringUserActionForPlayback = None`) for every WKWebView, and the vendored html5-qrcode already sets `playsInline`; shipped the never-run #293 Inspector verification instead as an ON-SCREEN scan probe + `play()` re-kick (zero-C#; a smoke gate now fails the run if the no-op "fix" ever lands). F2 = clamp swept to all 17 shells + 2-line iOS zoom belt (Damir: pinch is chat-only, full stop — text-scale concern moot; belt reaches mini-apps → MAJOR #6 tally). F3 = Share always bare address + button HIDES while an amount is set; iOS cancel no longer pops a second sheet; `parity-a-f5-checklist.md` A10 REBASED (Windows leg tests the F3 contract now). **iOS-29 attempt 4 rides** (Damir call): `--kb-inset` + composer margin lever, body NEVER resized — only chat.html change, one-block revert. F4 = trust-signal row in the security review doc, shell untouched. #46 loop (independent agent): 0 MAJOR; #302 RETIRED (stale batch-A comments cite it; next row #303). 🟡 **Damir: F5 the checklist (scan probe line is the key evidence either way) → ONE commit.** Consumed: `docs/next-session-prompt.md`. Still owed, unchanged: >64k A7 paste · W1 es/ru · A10 WINDOWS · batch B blocked on R3/R6/R4 dials.
- [x] **Same day, r2 (#303; UNCOMMITTED with #301): Damir's 2nd F5 round → KEYBOARD BOTH LEVERS + AMOUNT-QR DROP.** His clarification cracked the keyboard case: composer lifted only after 7-8 typed chars = the first vv resize fires pre-settle and nothing re-fires until a keystroke → FE settle ladder + composer-focus kick + NATIVE C# lever (`UIKeyboard.WillChangeFrame` → `__setKbInset` push, `SpixiContentPage` `#if IOS`, chat.html-gated → mini-apps structurally excluded; shell latches native, vv stays as belt). Amount-request QRs ruled unsupported → wallet-receive QR = constant `address:ixi` (contact-request strip stays; receiving `:send:` QRs still parses). #46 r2 loop caught a REAL MAJOR: OnAppearing never fires for overlay-presented pages — and chat IS one — so the native lever also attaches at the overlay-present site; iPad floating-keyboard guard added. Pipeline green: 22 shells · 672 keys · smoke 917/4 (same pre-existing 4). 🟡 Damir: F5 the r2 addendum in `docs/f5-checklist-2026-08-04-fixes.md` (composer on FIRST focus) + amount-QR → commit #301+#303 as ONE batch. ⚠ STILL OWED for scan: the probe line under the frame, word for word.
- [x] **Same day, r3 (#304; UNCOMMITTED with #301/#303): iOS-49 ROOT CAUSE FOUND + FIXED — zero-C#, 2 lines.** Live Inspector session over the cable (Damir driving, console probes): camera/track/readback all HEALTHY (`frameMax:33` = real pixels) but `.c-scan__feed` = **0×0 at mid-screen, computed position `relative`** — the vendored html5-qrcode CONSTRUCTOR stamps `style.position="relative"` INLINE on its container, beating scan-shell.css's `absolute/inset:0`; the collapsed box then sizes the video ("0px") and the decode canvas (0×0). Black preview + zero scans + live torch, fully explained; the #293/#300 inline-playback theory was never it. Fix: `feedEl.style.removeProperty('position')` per start + `!important` video fill; probe now counts 0×0 box / readback errors as DEAD and prints the box size (both blind spots exposed today). Smoke 921/4 (same pre-existing 4). 🟡 Damir: obj/bin wipe → rebuild → scan should preview AND scan; A8 zoom finally observable. Windows scan re-verify queued.
- [x] **Same day, r4 (#305; UNCOMMITTED with #301–#304): SCAN CONFIRMED WORKING on iPhone (first iOS decode ever) + the two leftovers closed, zero-C#.** html5-qrcode re-stamps `position:relative` inline in start() too (device-measured round 2: feed 262×349 in-flow, frame shoved to x262 — users aimed at a cutout the decoder wasn't watching = the "slower than legacy") → `.c-scan__feed` position/inset now stylesheet-`!important` (war over, structurally). Consent card no longer gates every visit: `startScanRequest` free fn + `spixi.scan.granted` boolean (#254-clean; set on success, cleared on failure → Settings-revoke degrades honestly). Bundle 238 · smoke 925/4 (same 4; two #301 F1 pins correctly tripped on the wrapper reshape and were rebased). 🟡 Damir: wipe→rebuild→F5 (full-bleed preview · centered frame · scan speed · revisit skips the card) → **commit #301–#305 as ONE batch**. Speed re-judge after this build; fps/auto-zoom dials only with measurements (#215).
- [x] **SESSION CLOSE 2026-08-04 (r4 F5 + commit): scan layout ✅ · keyboard ✅ · pinch ✅ · share/QR ✅ — scan DECODE ✗ + consent persistence ✗ remain (#306). NEXT SESSION: read `docs/handoff-2026-08-04b.md` FIRST** (decode = verify html5-qrcode's region math against the r4 full-bleed feed before touching CSS; consent = run the C-9 file:// localStorage probe FIRST — a per-page origin would implicate EVERY spixi.* localStorage feature on iOS, fix would be C# pref push, not a scan patch). Committed as ONE batch (#301–#306) by Claude on Damir's ask; push = Damir. `docs/f5-checklist-2026-08-04-fixes.md` = consumed. Still owed beyond scan: >64k A7 paste · W1 es/ru · A10 Windows · batch B dials (R3/R6/R4).
- [x] **Cowork session 2026-08-07: SCAN DECODE FIXED (#307) + C-9 PROBE SELF-SERVE (#308); zero-C#; UNCOMMITTED. NEXT: Damir F5 per `docs/f5-checklist-2026-08-07-scan.md` → ONE commit.** The #215 verify-first gate paid again, twice: (1) the vendored min.js shows html5-qrcode's decode region maps PER-AXIS client→intrinsic ratios, so decode is undistorted only when the feed box has the stream aspect — r4's full-bleed 393×737 stretched every frame 1.41× (zero decode) while r3's accidental 262×349 was uniform (scanned); fix = aspect-locked contain-fit feed (default 3:4/4:3 pre-ctor, real aspect at loadedmetadata which precedes the 'playing' region-latch, rotation-aware re-fit), CSS inset demoted to fail-soft, position keeps its !important war — **and the handoff's qrbox fallback was proven UNSOUND before anyone built it** (same per-axis ratios distort a qrbox sample identically). (2) The consent-flag suspect was narrowed IN-REPO: generatePage writes a LITERAL `"ll_"` prefix → `ll_scan.html` is a STABLE name (per-visit origins dead); cross-page keys live on different files (per-FILE theory alive); no custom WKWebsiteDataStore. The device question — does file:// localStorage persist at all — is now a self-serve on-screen probe (`spixi.probe.scan` counter + grant/appearance/write-error line under the consent card, removed on a successful start): two scan entries + one relaunch read as a verdict matrix, no Mac tether; if probe sticks at 0, EVERY spixi.* localStorage feature is dead on iOS → be-cutover row (C# pref push), not a scan patch. #46 loop: 0 MAJOR, 2 MINOR fixed (lingering probe line · stale-aspect rotation re-fit); r2 caught the fix's own smoke pins being decorative → rebuilt placement-aware + mutation-verified. Pipeline green: bundle 238 · 22 shells · lint ✓ · pseudo 9/9 · **smoke 937 / the same pre-existing 4** (+12 assertions). Consumed: `docs/handoff-2026-08-04b.md`. Still owed, unchanged: >64k A7 paste · W1 es/ru · A10 WINDOWS (rebased §A10) · desktop scan re-verify · Windows keyboard sanity · the 4 smoke failures (own session) · batch B blocked on R3/R6/R4 dials.
- [x] **Same day, r2 (#309; UNCOMMITTED with #307/#308): device round → THREE root causes closed — ★ WKWebView delegates were GC-COLLECTABLE (weak ObjC refs, no strong root → WebKit's own per-visit camera prompt; the same hazard on SecureNavigationDelegate silently drops the http/https block → security-review MAJOR #7; fixed with strong fields on iOSWebViewHandler, the batch's only C#) · staged-mount feed box (auto-enter starts the camera pre-present; bed ResizeObserver + always-re-fit) · #309b staged-latch re-latch (the #46 r3 reviewer PROVED my accepted-residue math wrong — a bracket-filling QR's finder falls outside the small latched region → one silent stop+relaunch on a >1.25× post-'playing' box grow, triple-bounded, r4-hardened to hook the relaunched video). C-9 SAME-PAGE VERDICT: file:// localStorage PERSISTS on iOS (iOS-52 — the unprompted WebKit dialog = auto-enter fired = the grant survived app kills; the spixi.*-dead escalation does NOT fire; cross-WebView leg stays open, low priority). Decode itself confirmed working on device (#307 held). Loop: 4 rounds, 2 reviewer-caught MAJOR-class corrections, all pins mutation-verified. Smoke 947 / the same pre-existing 4. 🟡 Damir: obj/bin wipe → full iOS rebuild (C# changed) → F5 checklist §ROUND 2 → ONE commit (#307–#309).
- [x] **Same day, r3 (#310; UNCOMMITTED with #307–#309): the WebKit camera prompt SURVIVED the strong-rooting → the delegate override was NEVER invoked (GC falsified as sole cause; plausibly broken since #293 — every "allow each time" fits). Rebuilt as an explicit NSObject + IWKUIDelegate adopter with a hand-written [Export] (registrar-proof, #280/#281 precedent) + [cam-perm] console/log observability so the #304 Inspector workflow can prove whether WebKit consults the selector at all. Feed geometry confirmed ✅ on device (letterbox = intended #307 look). C#-only → no bundle/shell rebuild; smoke 949 / same 4. 🟡 Damir: wipe+rebuild → F5 (dialog gone = fixed; still there → Inspector: any [cam-perm] line?).
- [x] **Same day, r4 (#311; UNCOMMITTED with #307–#310): Inspector session → C-9 CLOSED BOTH LEGS (`grant:1 · probe:23 · appearance:1` — localStorage persists AND crosses ll_* files; iOS-52 final) · the mic probe PROVED WebKit never consults the delegate (fresh audio getUserMedia → WebKit's own dialog, zero [cam-perm]) → built the runtime fork-splitter: one-shot probe at the first ixian: navigation logs the RUNTIME UIDelegate identity + respondsToSelector and RE-ASSERTS ours if swapped (= the fix if MAUI clobbers it post-connect — prime suspect). C#-only; smoke 951 / same 4. 🟡 Damir: rebuild → the [cam-perm] probe line names the fork; dialog gone = clobber confirmed + healed.
- [x] **Same day, r5 (#312; UNCOMMITTED with #307–#311): fork (a) CONFIRMED — MAUI's post-connect property sync re-assigns UIDelegate, and the #311 first-ixian heal RACED the boot getUserMedia (cold wins, warm loses = the exact observed split). Fix: heal on EVERY DecidePolicy nav incl. the main-frame file:// load (precedes page parse → deterministic), fixed-vocabulary log (no raw-URL eval injection, smoke-pinned). MAJOR #7 addendum (mechanism refined; nav delegate empirically not clobbered). C#-only; smoke 953 / same 4. 🟡 Damir: rebuild → WARM re-entries must be silent → decode end-to-end → ONE commit #307–#312.
- [x] **SESSION CLOSE 2026-08-07 (#313): scan saga CLOSED on device (r5 F5 pass — silent warm entries, e2e decode, torch); permission UX confirmed as-designed. NEXT SESSION: read `docs/handoff-2026-08-07.md` FIRST** — plate = Damir's 15-item list (both batches in one session, his dial): zero-C# polish (selectability/callout · mention-pill wrap · toast+CTA position w/ screenshots owed · iOS-47 fonts · iOS-48 Connecting · how-to+contacts i18n · backup badge · R6 tx sheet FULL-detail [dial locked] · dev-mode 10-tap) + Account-as-peer (iOS-46). PA1/PV1 (payment-auth gate · working privacy toggles) written into be-cutover. B1/B2/B3 runsheet rows still owed and MORE suspect post-#312. Smoke baseline 953/4. Consumed → archive: handoff-2026-08-04(+b).md · next-session-prompt.md. Commit #307–#313 = Damir (GitHub Desktop, #306 rule).
- [x] **Cowork session 2026-08-07 №2 (cloud, unattended after Damir's lid-close): THE WHOLE PLATE BUILT — #314 (all 9 zero-C# polish items) + #315 (Account as a peer tab, iOS-46 route (a)) + #316–#318 (parallel-session dials logged) (UNCOMMITTED; FULL pipeline in-session: bundle 777 KB/238 exports · 22 shells · lint ✓ · pseudo 9/9 · smoke 996 pass / the SAME pre-existing 4, +43 net assertions).** Verify-first rewrote a third of the brief (#314 row: toast = MISSING STYLESHEET not z-index · backup badge = REFRESH gap not logic-at-read + the same iOS hole fixed in landtab · mention pill repro'd BOTH split modes then inverted to mentions-first · iOS-47: APPS was the wrong one · i18n gaps = missing DRAFTS, 7 locales filled). #315 = park-don't-dispose + instant re-present (mobile-only; hub back arrow gone; onRepresented shell contract; reloadAllPages/onLowMemory teardown). **#46 loop: 2 adversarial rounds, 4 MAJOR-class catches total (frozen re-present · URL @handle theft · stale-theme park · dirty-state reconcile), ALL fixed + mutation-pinned.** Parallel Cowork session docs synced off the Mac pre-lid-close (A7 done · FIGMA MIRRORING RETIRED #316 · batch B dials locked #317/#318 — build batch B AFTER this lands). **NEXT: Damir — obj/bin WIPE (C# changed) → device build → F5 per `docs/f5-checklist-2026-08-07-plate.md` → ONE commit #314–#319 (message in that doc). NEXT SESSION reads `docs/handoff-2026-08-07b.md` FIRST.**
- [x] **Device F5 day 2026-08-10 (Damir on AI15, live triage from the cloud session): PLATE F5 PASSED + three same-day fix rounds (#320–#322), all device-verified.** #320 warm-Account visuals (root-variant hub topbar · peer-nav highlight snap-back) — plus the stale-bundle gotcha proven live (incremental iOS builds do NOT repackage Raw html; wipe + plain-build-then-Run is LAW). #321 R5 dev parity (send-log verb + share/save; live HUD — the updateDebugInfo feed never stopped, only the render was missing; DOMParser-inert, never innerHTML). #322 BODY-level selectability (the wallet-hero labels tell), connecting-state on all 3 tabs, hero "Denarnica" i18n, About drafts ×6. Closing baseline **smoke 1006 / the same 4**. OPEN for the sweep: iOS-53 composer-focus topbar dip (keyboard family, Inspector-first) · iOS-44 UN-DEFERRED (attach sheet under composer) · subscreen connecting routing (C#) · OG previews = be-cutover §82 (recipient unfurl stays banned). Consumed handoffs → docs/archive/. **NEXT SESSION: read `docs/handoff-2026-08-10.md` FIRST — the FULL SWEEP. Commit #314–#323 = Damir (GitHub Desktop, #306).**
- [x] **THE FIX SESSION — overnight autonomous build (2026-08-11→12, Cowork/cloud on a clone of be7657a0; DECISIONS #334 + loop #335; LANDS 2026-08-12 morning via the bridge — READ `docs/handoff-2026-08-12.md` FIRST).** L0 pre-audit paid: ★ #331 had RE-OPENED security MAJOR #4 on Android → contained (miniapp-scoped DomStorage) + 2 NEW Android MAJORs logged (#8 file-XHR wallet.ixi · #9 permission auto-grant) + 6 defective briefs caught (iOS-54 premise false in code · AND-6/12 wrong suspects · live re-localize ~80% pre-built · L1 co-change · #288 misses the parked Account). Built: Android chrome+notifications round (AND-6/12/13/14/19 + AND-15 per-type copy + call channel) · cheap-C# round (L1/L2/L5/L7 · L3/AND-5 backup hand-off · W9 · iOS-59/61/62/63/64 · AND-11 accept-gate) · Damir's live adds ALL in (bubbles 82%/360 · avatar viewer · file-send Cancel interim [fileCancel = BE row FC1] · AND-21 lock-on-picker · AND-22 camera pick · AND-23 sticky header · AND-17 root fixed: the member-sheet path never wrote the requestAddSent marker) · iOS-58 v1 live re-localize (zero flash on the pick surface) · iOS-60 swatch tiles · smoke summary now BASELINE-HONEST · Debug chrome://inspect flag + TEMPORARY Android kb-probe (smoke-pinned present). NOT built by design (#294 LAW): AND-20 lever + AND-7 edge-to-edge (probe numbers first) · iOS-54 fix (repro first). Pipeline green in-session: smoke BASELINE OK 1031/same-4 · lint ✓ · pseudo 9/9. Morning: land → generators on the Mac → Damir wipe+deploy both platforms → kb-probe screenshots FIRST → F5 checklist → lever round → ONE commit.
- [x] **Cowork session 2026-08-13: THE OWED #46 AUDIT over #336 RAN → CLEAN (#337; 3 auditors → fixes → opus break-my-verdict → r2; UNCOMMITTED).** 4 MAJOR-class fixed: AND-29 state push deferred/coalesced (**group-create was silently dead in the committed #336** — the state nav clobbered the create verb; ★ F5 must create a group) · `homeShellOverlayOpen` reset on onLoaded + homeBack self-heal (theme-flip reload stranded back-swallowing state) · back order = native-overlay-first (details-over-directory) · W4 ride-along: NEW `--layout-card-max` 360 — cards/file/media/sysnotice hold the rail, only TEXT bubbles take the 680 desktop raise. **iOS-66 root-caused + FIXED free** (stale hubScrollTop latch + hub re-attach; jsdom-proven). W0/W1/W3 verified clean as shipped. +25 smoke pins; smoke **BASELINE OK 1065/same-4** (full run = cloud tarball twin; bridge kills >45s). Damir: `git rm` scripts/_f5repro.mjs scripts/.dbg.tmp.mjs scripts/.launch-only.tmp.mjs (tracked debris) → build → F5 (#337 row list) → commit ONE batch. Details: DECISIONS #337.
- [x] **THE LAUNCH FLOW ROUND (#391–#394, 2026-08-18, cloud twin; C# + components + shells → FULL pipeline; UNCOMMITTED).** Four items, one body of work. **N76:** the onboarding tail is DELETED (`OnboardPage`, `onboarding.html`, both onboarding preferences, `completeOnboard`, `handleOnboardDone`, the `ixian:onboardingComplete` verb) — create and restore land straight in the app. Its two steps MOVED: the backup nudge waits for the FIRST REAL ASSET (`HomePage.hasBackupWorthyAsset` — an APPROVED, non-`pendingDeletion`, non-bot contact OR `getAvailableBalance() > 0`; a create clears `backupReminderTimestamp` to ARM it, a restore seeds it, delete-account clears it), and the join-community step became a second CTA in the chat-list EMPTY STATE on the verb HomePage already had (`ixian:joinBot`) — **no new verb**. **N75:** `LaunchCreatePage`/`LaunchRestorePage`/`LaunchRetryPage` merged into `LaunchPage` — ONE page, ONE WebView, views switch in place (five WebView boots of one document was the flicker); the boot view rides a `*SL{LaunchBootView}` carrier so a cold unlock paints retry on the first frame; the three password parses moved VERBATIM; `build-shells` emits one launch output (22 shells → 18). **N73:** one ground colour `#1b163c` for the launch flow in three layers, and on Android the system-bar strip + icon appearance now follow the PAGE (luminance), repainting on `OnAppearing` and on overlay/lock close. ⚠ iOS still shows dark glyphs on the fixed-dark screens — `setEdgeToEdge` is a no-op there (DECISIONS #394, screenshot first). **N72:** the welcome appearance pill is gone. **Opus review (#393): 4 MAJOR + 3 MINOR + 3 NIT, ALL FIXED** — a re-introduced #334 L1 form wedge · a MONEY-PATH verb hijack (a `Contains("ixian:create:")` dispatch could have restored into a NEW EMPTY-PASSWORD wallet) · the asset gate counting the bot this batch's own CTA adds · no strip restore after an overlay/resume-lock. Smoke **1982 → 1992 / the same 4 pre-existers**; 19 new pins, mutation-proven in two batched runs. 🟡 **Damir: FULL pipeline (extract → locales → iife → bundle → shells → smoke + lint + pseudo) → wipe `obj`/`bin` → build → F5 `docs/f5-checklist-2026-08-18-launch.md` → commit. NEXT SESSION: read `docs/handoff-2026-08-19d.md` FIRST.**
- [x] **#391 F5 VERDICT + the next two rounds scoped (#395/#396, 2026-08-18).** Damir F5'd the launch round on Android: **10 pass / 5 fail.** The merge itself is sound (no flicker on Create/Restore, no tail after creation, the cold unlock paints retry first, the appearance pill gone, a restored account is not nudged). FAILS, two already root-caused from the cloud twin: **F-1** the backup nudge fires on an UNACCEPTED contact — `Friend.approved` DEFAULTS TO TRUE (Core `Friend.cs:196`) and every outgoing request adds the friend without passing it, so the #393 MAJOR-3 guard is dead code; the real predicate is `state == FriendState.Approved` · **F-2** hardware back exits the app from create/restore (C#'s `currentView` is not updating — instrument before guessing, then report the view from ONE place inside `show()`) · **F-3** restore = fatal exception + no network + empty lists, recovers on restart (WALLET PATH — get the exception text first; suspect the #393 `goHome()` deferral) · **F-4** the status bar stays dark after unlocking — #393 patched `closeOverlay` but the lock closes through `closeModalOverlay` (`SpixiContentPage.cs:647`) · **F-5** empty states pop in ~1s late everywhere (zero-gate suspect, triage first). ★ **LESSON: a smoke pin can PASS and prove nothing** — F-4's pin counted two call sites of the right function in the wrong method. **NEXT BATCH = AND-7 FULL BLEED (#396):** N73 answered the wrong question — Damir wants no strip, not a matching strip ("full bleed to the top", incl. the WALLET HERO). Already triaged: `MainActivity`'s InsetsListener pads the root top while the window already draws edge-to-edge; stop padding and feed the inset to the shells as a CSS var (Android `env()` is cutout-only). Measure on device first (#294). **Group chat answered** (Damir's question): non-contact members relay through the CREATOR by design — inherited from Ixian-Core (2026-05-17, before our 2026-06-16 fork), so nothing flows between two strangers while the creator's app is not running. **Damir's dials (#397):** the join CTA keeps the empty state AND gains a permanent **How to use** row (verified: `SettingsPage` has no `ixian:joinBot` handler → a shared static, the `BackupPage.backupAccount` precedent, no new verb) · **F-3 the restore crash is DEFERRED to the delete-account/delete-wallet batch** (one wallet lifecycle, done once). **NEXT = OVERNIGHT, UNATTENDED, CLOUD-ONLY (#398, budget mode lifted for that run):** both repos clone anonymously (`Rexoflex/Spixi` @ `redesign/frontend`, `ixian-platform/Ixian-Core` HEAD = `097341a`, cloned as a SIBLING); delivery by `SendUserFile` only. Batch A→F: the four live F5 fixes · the How-to-use join row · **full bleed AND-7 (his top ask, with the device measurement shipped as an on-screen probe)** · N63 locale tail + N70 + N44 · N64 ① scope · N71 only if the rest is green. 🟡 **NEXT SESSION: read `docs/handoff-2026-08-19e.md` FIRST; prompt in `docs/next-session-prompt.md`.**
- [x] **Cowork session 2026-08-22: #505 LOCK MODEL + #506 ①–④ + N86 QR BUILT (#507–#509; UNCOMMITTED; C# + components + shells + 12 locale drafts → FULL pipeline green in-session: bundle 275 exports · 18 shells · lint ✓ · pseudo 9/9 · verify-locales ALL CLEAN · cs-syntax 141 · smoke **2478 → 2564**, and the known pre-existers **4 → 3**). READ `docs/f5-checklist-2026-08-22-lock-qr.md`.** ★ **#505 (severity 1):** the desktop stops locking on window DEACTIVATION — MAUI raises OnSleep there on WinUI and a deactivated window is still fully visible, so "you clicked a browser" was being read as "you walked away"; **the signal was wrong, not the grace period**. `SDesktopIdle` (new, Windows-only) locks on **10 min input idle** (Damir's dial, `lockIdleMinutes` preference, clamped 1 min–24 h) — one signal that also covers system lock and, via a wall-clock leg, sleep/hibernate, which a tick counter cannot see. Raw `user32 GetLastInputInfo`, **no new NuGet** (#495: no toolchain here to verify a package reference). The **escape hatch is cause-agnostic** (the failing log is still owed): `sweepStrandedCover()` on window activation — unlocked-but-covered → uncover, latched-locked-with-no-lock → clear + re-present — and it LOGS what it finds so a recurrence names its own mechanism. ⚠ **Self-audit caught two of my own defects**: the sweep at the top of OnResume would have logged a FALSE diagnosis on every focus change, and it could have staged a SECOND lock inside the present-in-flight window. ⚠ **Two #496 SECURITY pins went red and were right to** — rewritten in place, never deleted, now asserting the property rather than the line shape. ★ **#506: two of four premises did not survive checking** — ① the truncated sounds are NOT the file descriptor (Android documents closing it after `setDataSource` as safe, and the sibling method closes it earlier still); the **player was being garbage-collected mid-playback** because its only reference was its own Completion handler — rooted now, iOS/Windows already did this; ③ wallet and apps were **never ungated** (all three run `createZeroGate`) — the gate WAS the delay, so both C# flushes gained a real `*Done` verb with the quiet window kept as the belt. ② long-press = the pressed **row** promoted above the scrim (2.01:1 under it vs 5.98:1 above; the stacking-context precondition is PINNED because its failure is silent) · ④ credits filled in 12 locale drafts. ★ **N86:** 12 px card padding dropped on both QR surfaces (white square ≈209→185 px) with the ISO quiet zone untouched and pinned, hub gains a "Show QR" reveal at full scan size — and **#149③ was never a code defect** (a stale 148 px expectation vs CSS that moved to 185 px on 2026-07-29), so the pre-exister count drops. `Config.maxLogCount` 1 → **5** with a `RELEASE BLOCKER — REDUCE TO 1 BEFORE LAUNCH` marker (Damir) + a pin. ⚠ **`git add Spixi/Platforms/Windows/SDesktopIdle.cs`** (new). 🟡 **OPEN for Damir:** the privacy-shield-on-deactivate posture dial · the FAILING-session `ixian.0.log` · W-3.1 (**no mechanism found — screenshot owed, not guessed**) · W-5.1 (already triaged as the #248/#250 WebView2 resize dial). ⚠ Pre-existing, not ours: two NUL-bearing legacy onboarding SVGs under `img/dark/onboarding/` (committed at `da72e812`; N76 retired that flow, so they are probably dead files).
- [x] **Cowork session 2026-08-22 (cont.): #503 ANDROID NotificationServiceExtension BUILT (#510; UNCOMMITTED, rides the #507–#509 drop; C#+manifest only → NO bundle/shell rebuild; smoke **2564 → 2580**, same 3 pre-existers; cs-syntax 142). READ `docs/f5-checklist-2026-08-22-lock-qr.md` ADDENDUM + `docs/ios-nse-spec.md`.** ★ Every API name was READ OUT OF THE SHIPPING BINDING, not recalled: nuget.org is blocked from the container, so the SDK repo was cloned, checked out at the pinned **6.1.9** tag, and `core-release.aar` disassembled with `javap` — `INotificationServiceExtension.onNotificationReceived(INotificationReceivedEvent)`, `getNotification()`, `preventDefault()`/`preventDefault(boolean)`, package mapped by the Core `Metadata.xml`. ★★ **And the bytecode paid for itself: `preventDefault()` is the WRONG call** — the no-arg form makes the SDK WAIT on the notification's display waiter, so a suppressing handler that never displays parks a coroutine until it times out; `preventDefault(true)` discards. Not in the docs. ★ The gate is now ONE shared `SPushService.decidePush` (`Suppress | PostOurs | ShowRaw`) used by both lanes, **idempotent on the OneSignal notification id** because the bytecode does not settle whether the foreground listener can also fire — rather than depend on being right about the ordering (the exact #503 error), a repeat is a no-op. `[Preserve]` because the manifest is the class's ONLY caller and a Release linker would otherwise be free to drop it; the `[Register]` name and the manifest value are pinned as **the same string, computed from both files**, because a drift there kills the lane silently. ⚠ `displayColdPush` is gone → 8 existing #495/NOTIF-5 pins went red and were RIGHT to; rewritten in place with a second reachability pin for the background lane. ⚠ **iOS NOT built, deliberately** — its extension is a separate SIGNED binary needing an extension bundle id, a provisioning profile and an App Group (three Apple-side prerequisites, and iOS push is already blocked on Apple), the SDK's own csproj warns a wrong `SupportedOSPlatformVersion` makes iOS **silently drop** mutable-content pushes, and `UNNotificationServiceExtension` can only MUTATE a notification, never cancel one — so a muted chat may still show a silent empty row and the real fix is server-side. Full work order + the decision Damir owes: `docs/ios-nse-spec.md`. ⚠ Session running total: **five** of my own pins were vacuous on the first pass, all found by mutation and none by reading.
- [x] **★ #507–#511 COMMITTED `0088a3e9` (2026-08-22) — 81 files, F5-passed on BOTH platforms, 25 pass · 1 fail · 8 n/a of 34, and the one fail was #268 working exactly as Damir ruled it. NEXT SESSION: read `docs/handoff-2026-08-23.md` FIRST; paste-ready prompt in `docs/next-session-prompt.md`.** ★ **#505 verified on hardware** — 1.1–1.4 all pass including 1.3, the 30-second Win+L that deliberately does NOT lock. ⚠ **#503 passed every symptom and stays 🟡**: A1.1–A1.4 pass on a killed app, but A1.5 — the `(service-extension)` log line — was never read, and **2.1 was a FALSE PASS last round for exactly that reason**. Ask for the notiflog; do not close it on a tick. ⚠ **W-4.6 did NOT reproduce** (2.2/2.3 n/a) — encouragement, not proof; it was intermittent, and the hatch stays armed. ★ The dev probe reads `wdone − wflush = 5 ms` against a 400 ms quiet window, so #506③'s half is measurably fixed; `wflush=11076`/`aflush=27490` are just when those tabs were opened and are NOT recorded as a C# delay. **NEXT = THE MENU BATCH**, all four calls Damir's: (a) the §5b **anchored dropdown on mobile for the message menu AND the chats row menu** — which fixes 4.1 *structurally*, because a menu that flips above the pressed message can never cover it; (b) the lift stays, the mobile scrim deepens; (c) **desktop keeps #268 — no wash** — retune `[data-dt-ctx-source]` instead; (d) the QR opens a **full bottom sheet** folding in the existing "What is this address?" surface. Riders: the dev HUD's 72 px rail offset, and **four `Logging.info` calls on the sound path** — ⚠ Damir reports **random sound effects on an IDLE Android app**, the leading candidate is `transactionVerified` firing with **no edge guard** beside an unconditional `updateStatus(…Final…)`, and **a sound that fires currently logs nothing**, so get the log before touching any trigger (#294). Damir also wants new UI SFX picks and asked to be **interviewed**. 🟡 Still his: the privacy-shield dial (asked twice), `ixian.0.log`, the W-3.1 screenshot, A3.1's build configuration, and the iOS §2 decision. ⚠ **My mistakes worth carrying:** five pins were vacuous on the first pass and only mutation found them · the 4.1 sheet-above-message ordering traded one occlusion for another · a date-only verdict filename **clobbered another batch's verdict** (recovered from git; filenames now carry the batch) · `EXIT=$?` after a pipe reported success for a `git add` the 45-second bridge limit had killed. ⚠ **Bridge:** `tar --overwrite`, stage git adds in chunks of ~20 and verify the staged count, `mv` stranded `*.lock` files to `_to_delete/`, `git push` does not work from the bridge, and never `git add -A` (CRLF churn on ~116 files).
- [x] **★ CANON AUDIT (#512, 2026-08-22) — Damir asked whether we are still following the .md files, and the honest answer was four gaps.** Followed: DECISIONS rows at decision time · verdict to disk (#459 ①) · verify-at-source (the pinned OneSignal AAR was disassembled with `javap`, not trusted to docs) · #294 honoured twice (W-3.1 and W-5.1 both refused for want of a mechanism) · mutate-before-believing (five vacuous pins caught). ★★ **NOT followed: the #46 adversarial loop never ran** — #507–#511 shipped on self-review plus mutation, the standard #46 calls insufficient, on a batch with TWO security-gate rows. Also missing: a `security-handover-gate.md` row (now written — and it immediately found that `SpixiNotificationServiceExtension` is **REACH INTRODUCED**: a cold push's `fa` now reaches our parsing code and an Intent extra before the SDK renders anything) · the ARCHITECTURE §4 push contract (now carries `clearPaymentActivityDone`/`clearAppsDone`) · and the **ASD-STE100 language rule**, which this session did not follow and which recent project docs do not either — 🟡 Damir to confirm whether it still stands. **`docs/opus-review-brief-507-511.md` is the work order; the loop RUNS IN A FRESH SESSION BEFORE the menu batch** (CLAUDE.md says "SEPARATE Opus session"; the author is the wrong person to adjudicate findings on his own security-gate code, and the menu batch would otherwise layer an anchored dropdown onto unreviewed z-order and lift work). Handoff and next-session prompt both gained the loop as **item 0**.
- [x] **SESSION B — L7 · L11 · THE #46 LOOP OVER SESSION A'S REPAIRS · THE WALK (2026-08-30/31; DECISIONS #657–#663; committed). NEXT SESSION: read `docs/handoff-2026-08-31.md` FIRST; paste-ready prompt in `docs/next-session-prompt.md`.** The session was planned as a review plus five rows. **The review was owed, so it ran first, and it took the session:** five rounds, **22 MAJORs, 228 mutations, no pin deleted** — **10 MAJORs in session A's repairs, 6 more inside the fixes for those, 6 more inside the fixes for THOSE.** Verdict: `docs/opus-review-verdict-session-b.md`. ★★ **THE HEADLINE:** two auditors with disjoint scopes independently found that *"was this group message delivered"* was answered by an **ENUMERATED LIST OF REACTION KEYS in four homes that drifted** — session A read `received`, round 1 added `seen`, round 2 added `fileReceived`, round 3 found `like` and `tip`. **The list was open-ended, so every reviewer found the next item on it.** Damir ruled it in one sentence — *"having delivered seems redundant"* — and the ruling made the fix **SMALLER than any of the patches**: the rule now lives in ONE C# home, `UIHelpers.anyOtherMemberHasMessage`, and **it names no reaction key** (#658). **SHIPPED:** **L7** removed as ruled · **L11** F1/F2/F3, F1 in three attempts · the `[RCPT]` probe removed · the member-context check folded into `cs-syntax-check`. **NOT SHIPPED, moved to session C: L6, L5, L10** — and ★ **L10 is NOT a one-line reorder**, the worklist said it was, and auditor C found a real ordering hazard at `SpixiContentPage.cs:322-326`. ★ **A RECORDS LESSON (#660): item 0 was based on a STALE ROW.** The prompt said the #507–#511 loop was still owed. It ran on 2026-08-22 and #515 records it CLEAN; only the verdict in §6 of its own brief was missing, so three handoffs copied "still owed" forward. **A verdict not written back into the brief that ordered it is a verdict nobody can find.** ★★ **DAMIR'S WALK (2026-08-31, Windows): 22 pass · 0 real failures · 2 n/a.** The two "fails" were passes with a design note, and both notes shipped in the same batch (#662): **A2** — hiding the ⊕ took the 36 px attach pill out of the composer row and the placeholder lost its optical gap, fixed with `.c-composer__field[data-no-attach]` driven from the SAME expression that hides the button · **B3** — the empty receipt row now says *"nobody has seen this yet"* (new key `noneSeenYet`, all 12 locales), and ⚠ **that reverses "no counts, no line" for ONE case only** — the other four empty returns stay silent because there we have no honest answer. ⚠⚠ **THE WINDOWS BUILD TRAP (#663), and it cost a walk: `dotnet build` for the Windows target does NOT stage the `MauiAsset` files beside `Spixi.exe`** — only the VS deploy step does. `localizeHtml` then logs an error and **returns without writing**, while `generatePage` still returns the URL to `ll_chat.html` in `Documents\Spixi\html\`, a user folder that survives every wipe. **So the app silently serves the previous build's shell and looks completely normal.** **BUILD WINDOWS WITH F5**, or copy `Resources\Raw\*` by hand; Android is unaffected. `dotnet build -t:Run` exits 9009 for this target. **Baseline now: bundle 301 · shells 18 · smoke BASELINE OK 3586 / the 3 known (#136 · M5 · B3) · locales ALL CLEAN 776 · cs-syntax 140+1 · Ixian-Core `097341a` verified clean** (its 170 modified files are CRLF churn). **SESSION C = L6 · L5 · L10 · L3 · L4 · L9, plus L13 and the four new walk rows L14 (tab flicker into Wallet) · L15 (no flags in the Windows language picker) · L16 (splash logo and light-mode ground, new blue #175595) · L17 (launcher icon — ★ Damir marked it safe to do early; he owes the new logo SVG).** ★ **THREE OF THE FOUR ARE VERIFY-FIRST.** ⚠ An earlier draft of L16 warned the splash *"cannot be theme-aware"*; **Damir corrected it and he was right** — he asked for a theme-aware splash and it shipped as **#534**. The real cause of the light-mode squircle is ONE MISSING LINE: `values-v31/styles.xml` declares no `windowSplashScreenAnimatedIcon`, so Android 12+ falls back to the LAUNCHER ICON and paints its background layer; `values-night-v31` supplies its own drawable and shows none. The logo size lives in the drawable's group transform, not in `BaseSize`. ★ **Two rules this session paid for: CHECK DECISIONS before accepting an "owed" row from any handoff · WHEN A REVIEWER FINDS THE SAME CLASS OF DEFECT TWICE, STOP PATCHING AND QUESTION THE DESIGN.** ⚠ Carried, must not be lost: the sticky `.c-money-cta` under the iOS keyboard · the iOS menu re-anchor that runs on the first resize only · the blind-group same-nick roster collapse (carried, not introduced) · BE rows CORE-4/CORE-5/CORE-6 and the membership question on `anyOtherMemberHasMessage` · the `[CDPERF]` probe STAYS until L10 is built. Consumed → `docs/archive/`: `handoff-2026-08-30.md`.

- [x] **SESSION H — THE SUBSCREEN SLIDES · THE SKELETON ROSTER · THE 760 COLUMN · THE A–H OPUS LOOP (2026-08-31, cloud container on a clean clone; DECISIONS #723–#730; UNCOMMITTED — commit is Damir's). NEXT SESSION: read `docs/handoff-2026-08-31b.md` FIRST; paste-ready prompt in `docs/next-session-prompt.md`.** Baseline verified in a clean clone and the record was wrong twice (#723: locales are 787 not 788 · the #710 pin was RED — Session G never regenerated icons.iife.js; registry now 90 with Damir's volume/bell-ringing/cloud-bolt, wired per #602). The whole prompt queue shipped: ① ONE shared in-shell slide at the NATIVE numbers (300/220, stylesheet decides desktop/reduced-motion) across Account sublevels · Contacts · Wallet Send/Receive · Launch create/restore, rendered on the built shells before rebuild (#725) · ② the skeleton roster — 40ms leading info paint, 24-row rAF batches, C# roster chunked in 24s under lock, 24 is MEASURED (#726) · ③ [PAINTDIAG] armed as a pinned SET — the L14 flashes get frame timestamps, no guessed fix (#727) · ④ `docs/url-preview-memo.md`, nothing built (#727) · ⑤b the 760 column, scroller-padding centred so the scrollbar stays at the window edge (#728) · plus Damir's mid-session asks: the composer pill's own token pair + the thin textarea scrollbar (#724). **★★ ⑥ THE OPUS ADVERSARIAL LOOP over #642–#722 + Session H → PASS (#729, verdict `docs/opus-review-verdict-session-h.md`): 3 MAJOR — (A) my own slide REINTRODUCED the L8 double-back class on THREE surfaces and the reviewer found the FOURTH (a second back inside the 220ms exit backgrounded the app; fixed: the overlay level counts a sliding-out cover, a second back aborts the slide) · (C×2) a stale Raw/html passed the suite green + the icons registries had no equality gate → `build-shells --check` is now a FATAL in-suite gate, registry equality pinned structurally, `extract-strings --check` is real, NUL/short-write gates on every shell write.** signSend peer-scoped on the two locked surfaces; gate sweep (`docs/security-gate-sweep-642-722.md`): zero new sinks/fetches A–H, the in-app privacy blurb rewritten honest THEN platform-scoped after the reviewer caught the oversell (#730, 🟡 Damir wording pass); [EXCERPTDIAG] capped; BE rows CORE-7/-7b. Closing: **bundle 317 · shells 18 · smoke 3892 / the 3 known (#136 · M5 · B3) · locales 787 ALL CLEAN · lint ✓ · pseudo 9/9 · cs-syntax 140 + 1 · mutations 8/8 + 4/4 + reviewer re-kills, zero survivors.** 🟡 Damir: FULL local run (generate-icons → bundle → shells → gates) → F5 `docs/f5-checklist-session-h.md` → commit ONE batch → the PAINTDIAG walk. ⚠ `git add` the new files (subscreen-slide.js/.css + the four new docs + the three SVGs).
- [x] **SESSION I — THE PREMIUM DENSITY PASS · LEGAL DOCS IN-APP · THE L14 HANDSHAKE · THE WALK (2026-09-02; DECISIONS #736–#747; committed f325d651).** 16 render sheets at the Motorola's measured 2.5 dpr, every dial picked, one token batch (brief §10); the full legal documents baked at build time (Terms live, Privacy HELD on two markers); the L14 cover handshake; [CDPERF] chat-open armed; the seed harness. Walk: 33 P · 7 F · 2 N/A (#747).
- [x] **SESSION J — THE SEVEN WALK FIXES · DAMIR'S EVENING RULINGS · THE LOGCAT MIRROR · SEED v2 (2026-09-02/03, cloud container on a clean clone; DECISIONS #748–#752; UNCOMMITTED — commit is Damir's, `docs/commit-message-session-j.txt`). NEXT SESSION: read `docs/handoff-2026-09-03.md` FIRST; paste-ready prompt in `docs/next-session-prompt.md`.** Baseline verified; smoke reads +1 WITH the Ixian-Core sibling (the M1 hold-out gate, #748 — record which). The seven walk fixes rendered on a rebuilt Playwright harness before their pins (#749): typed-card lift · ⊕ press = the 36 disc (a `background:` shorthand had reset `background-clip`) · Notifications flat · chats-row time 12 measured vs TG · the THIRD row height `--row-h-stacked` (56/60/64 sheet, his pick) · caret shape 2 + `CARET_WITHOUT_KEYBOARD` · ★ the tray ↔ keyboard flicker was focusComposer's blur/focus dance, not the absolute slot. Ten evening rulings built (#750): light canvas #E4EAF3 / #051C8E @6% / his 289° wash (the light dial closes) · notice white/midnight + elevation, no border · names Medium/Semibold · the selected tick on `--bubble-row-inset` · the money CTA on the bottom edge · input-on-card white · row titles Medium · typed-card buttons wrap · logo white. ★★ **THE NUMBERS (#751): nothing ever reached logcat because App.xaml.cs starts Logging with console_output=false** (now ON under SPIXI_DEV_COEXIST && ANDROID) and the shell's `[CDPERF] chat-shell` was `console.info` (the WebView drops INFO; now `warn`). The L10 fix waits for the stamps. Seed v2 (#752): light / heavy (10 × 1000 + 40 × 40), top-up, store write timed. Closing: **bundle 319 · shells 18 · smoke 4003 / 3 known (with sibling) · locales 786 · lint ✓ (6 dev, 2 sites) · pseudo 9/9 · cs 141+1 · three --checks ✓.** 🟡 Damir: gates → builds (`docs/f5-checklist-session-j.md`) → the walk sheet `docs/walk-session-j.html` → walk.log → commit ONE batch. Walked the same morning: 25/27, the two device rows measured and closed on numbers (#753–#758: the tray = the keyboard's height, `kbUp`, no L10 fix, tails pick D, dark canvas #0D1117). ⚠ `git add` the new files (walk-session-j.html · handoff-2026-09-03.md · commit-message-session-j.txt · docs/archive/handoff-2026-09-02-session-i.md · sheets/session-j/*); nine consumed docs moved to `docs/archive/` (git shows them as renames); `docs/handoff-2026-09-02.md` deleted. Closing smoke **4003** (with sibling).
- [x] **SESSION K — THE CHAT OPEN ON THE SHELL'S PAINT · THE LOCALIZED-DOCUMENT CACHE · WALK J2 · THE LOGGED ROWS · DARK WHITE-ON-BLUE (2026-09-03, cloud container on a clean clone; DECISIONS #759–#766; UNCOMMITTED — commit is Damir's, `docs/commit-message-session-k.txt`). NEXT SESSION: read `docs/handoff-2026-09-03b.md` FIRST; paste-ready prompt in `docs/next-session-prompt.md`.** Baseline verified, every number as recorded (#759). ★★ **The chat open (#760):** Damir: "takes half a second … must be faster … remove anything that doesn't introduce flash." From his own #754/#756 stamps: drain → present was ~130 ms while the shell painted 21–27 ms after drain — `presentPreload`'s flat 120 ms hold. Built: present on the shell's `ixian:painted` (revealDelayMs 0, 150 ms backstop, latched either order) · **`generatePage` ran the 640 KB `localizeHtml` line loop inside EVERY page constructor on the UI thread** (Windows re-wrote `ll_chat.html` to disk each open) — cached per (file, dictionary version; bumps on language load + addCustomString) · no FadeTo · the two missing stamps (tap → ctor · the shell's boot split). The remaining ~150 ms is the architecture (fresh WebView + parse per conversation); the route (warm chat WebView vs batch transport) = **"decide after the numbers"**. **Walk J2 12/1 (#761):** K1 keyboard → tray = a HOLD on his "go" (mirror of #721; the instant swap flashed keyboard + tray for the 100–300 ms the keyboard took to leave) · T1 avatar **32** at the composer edge (two tokens, `data-gutter` rows — ⚠ my first CSS insert closed `.c-bubble-row` early; the N36b built-shell pin caught it) · `[SCROLL]` jank probe. **Logged rows (#762):** received-sticker pill → inline-end · both nick-edit pencils pass `ariaLabel` at creation (the "home" warning was the WARM settings WebView) · Apps ⋯ = anchored dropdown, **no dim** (Damir on the render) · `[CDPERF] appnew` stamps (the #757 ② guess was not in the tree) · `[WV2]` stamps + WebView2 DefaultBackgroundColor in applyPageSurfaceColor · **group-avatar trace: the hash is NOT it** (uniform over 2000 synthetic addresses; parked on one device fact). **Dark (#763):** on-action inks white; action surface **primary-600** (his pick, AA) with darker states. **#764:** RAM 411 MB peak logged (meminfo split = the instrument, Mac/cable). Harness rebuilt to boot the REAL built shells in Chromium through the C# wire (handoff §3). Closing: **bundle 321 · shells 18 · smoke 4038 / the 3 known (with sibling) · locales 786 · lint ✓ · pseudo 9/9 · cs 141+1 · three --checks ✓ · mutations 12/12.** **Walk K 14 pass · 3 fail (#765): the A/B (J vs K, same phone) = NO regression, the half second = WebView create + parse, structural; K1 note fixed (tray re-pin), backstop 150 → 400; #766 his rule ("open as soon as possible"): the three form pages present at onload. COMMITTED by Damir 2026-09-03 (+ the #766 fix-up commit). NEXT SESSION ITEM 0 = the #46 Opus loop over Sessions I–K (`docs/opus-review-brief-sessions-i-k.md`) BEFORE any build.** ⚠ `git add` the new files (walk-session-k.html · handoff-2026-09-03b.md · f5-checklist-session-k.md · commit-message-session-k.txt · sheets/session-k/*); four consumed docs moved to `docs/archive/` (git shows them as renames).
- [x] **SESSION L — THE #46 ADVERSARIAL LOOP OVER SESSIONS I·J·K, CLEAN IN FIVE ROUNDS (2026-09-04, cloud container on a clean clone; DECISIONS #767–#773; UNCOMMITTED — commit is Damir's, `docs/commit-message-session-l.txt`). NEXT SESSION: read `docs/handoff-2026-09-04.md` FIRST; paste-ready prompt in `docs/next-session-prompt.md`.** Item 0 only — no build item was started, by design: the queue was gated on a CLEAN verdict and it took five rounds. 3 disjoint read-only auditors → 2 verifiers (which REFUTED four findings and ELEVATED one nobody had) → 3 fix agents on disjoint files → a central pin pass → **four** fresh break-my-verdict reviewers, each over the previous round's fixes. r1 NOT CLEAN (2) · r2 NOT CLEAN (5) · r3 NOT CLEAN (4) · r4 NOT CLEAN (1 prose) · **r5 CLEAN**. ★★ **Three MAJORs no self-review found.** **#768:** two of Damir's reports the same morning (Add app `ERR_FILE_NOT_FOUND`; a LIGHT Account pane under a dark system theme) were ONE cause, **measured on his machine** — `localizeHtml` returned void, `generatePage` gated freshness on `File.Exists` (true for an `ll_*.html` a PREVIOUS BUILD left in `Documents\Spixi\html`) and handed back the URL regardless; Session K's version map had cut the only diagnostic from once-per-open to once-per-process. Now: `bool` return, write-gated map, a degrade ladder ending in a page that NAMES the missing file, and `copyResources()` no longer hard-crashes the `App()` constructor. **#769:** his white-on-blue ruling reached a token **ten** surfaces read — mute label **1.14:1** — fixed by role, **no fill changed**. **#770:** a rotation persisted a keyboard height that fills the screen and latched `kbUp`; **the fix took three rounds and the first two were wrong** (a shape guard suppressed the `kbUp` clear as collateral, then could not see a portrait split-screen at all) — geometry cannot separate a keyboard from a window resize under `adjustResize`, so the discriminator is semantic. Also: the K1 hold's 32 px lurch on notched iPhones; the slide entry input-dead window (32 ms measured, not the 300 ms round 2 shipped by mistake); the anchored-menu listener leak; Session J's **missing** security-gate section filed retroactively. ★★ **Two rules promoted: #771** — a sweep over RAW TEXT is satisfied *or defeated* by the comment that explains it (a `[SCROLL]` set pin its own docblock satisfied: green on the regression, red on the sanctioned retirement) — every pin now declares `stripCode` or raw, and **a behavioural pin that stubs the function under test proves nothing**; **#772** — a comment stating an invariant the code does not enforce is a defect (nine, three written BY the fixes, one inside a pin, one inside the paragraph retiring rotted citations, which shipped a fifth). Closing: **bundle 321 · shells 18 · smoke BASELINE OK 4067 / the 3 known (with sibling) · locales 786 · lint ✓ · pseudo 9/9 · cs 141+1 · three --checks ✓ · Ixian-Core 097341a untouched.** Every pin added or repaired was killed by its own mutation; the final reviewer ran 15 across all rounds and owners, none passed. Residuals are #773 (incl. built `chat.html` **1,663 B** under the `#345` ceiling). ⚠ `git add` the new files (handoff-2026-09-04.md · f5-checklist-session-l.md · walk-artifact-session-l.html · commit-message-session-l.txt); three consumed docs moved to `docs/archive/` (git shows renames).
- [x] **SESSION N — THE LEGACY PURGE · THE PERF INSTRUMENTS · THE STRIP FORK (2026-09-05, cloud container on a clean clone; DECISIONS #789–#795; COMMITTED `aeee3bdc` + `056a59eb`; the checklist, walk artifact and commit message are in `docs/archive/`). NEXT SESSION: read `docs/handoff-2026-09-05d.md` FIRST (05c is archived); paste-ready prompt in `docs/next-session-prompt.md`.** Baseline verified exactly (4090/3 with the sibling). **Goal 1 (#789/#790):** the last four legacy documents + three C# pages + css/ libs/ fonts/ + js/* except `html5-qrcode.min.js` (★ live — scan injects it) + `hasLegacyPageChrome`/`rethemesByPush` and every reader + HomePage's three dead verb branches + the `SpixiThemeMode` carrier + ~60 unreferenced images DELETED, every route re-proved by exact reference search (two more grep lies found on the way: `spixiui-*.css` IS loaded by the legacy pages through the `*SL{SpixiThemeMode}` carrier — the transitive `fonts/` chain; the redesign embeds Sora, never Inter). The smoke suite gained the REACHABILITY GATE (images/ + img/ both directions, 18 docs viewport-fit, no legacy loader, js/ ≡ html5-qrcode, every shell runs native.js but the named bundle-less one). Raw/html 15 → ~7.7 MB. **Goal 2 (#791–#795), measure first:** the two investigations built as TEMPORARY `[CDPERF]` instruments (per-file parse split · one bridge round trip); the comment-strip fork settled as a RELEASE PACKAGING STEP with BOTH gates (`strip-release.mjs --check` · `smoke-packaged.mjs` — gate 2's first run found exactly the one pin whose premise the fork breaks); the SVGO doodle rendered on the real shell for his eye (canvas max delta 6/255), not landed; the pre-warm DESIGNED (`docs/prewarm-chat-spec.md`), not built — Tier 3 in his ranking, no compiler, the chats-list frame probe needs the phone. **#46 loop in-session: 2 auditors + a break-my-verdict reviewer → 4 MAJOR-class fixed** (csproj property evaluated in the body where `$(IntermediateOutputPath)` is EMPTY · the strip's silent truncation on an unterminated `/*` with both gates comparing against the same function · a ceiling raise that was not needed — the pin's unit is normalized chars, not raw bytes · a Windows argv trap: a dir ending in `\` before the closing quote). 10 mutations, all killed. Closing: **bundle 321 · shells 18 · smoke BASELINE OK 4125 / the 3 known (with sibling) · gate 2 4124 (without) · locales 786 · lint ✓ · pseudo 9/9 · cs-syntax 138 + 1 · three --checks ✓ · Ixian-Core 097341a untouched.** ⚠ The MSBuild leg of the strip target is UNVERIFIED here — Damir proves it with gate 1 against the DEPLOYED folder (checklist §2C). ⚠ `git add` the new files (strip-release.mjs · smoke-packaged.mjs · prewarm-chat-spec.md · sheets/session-n/ · the four session-n docs · the archived 05b handoff); the deletions are a `git rm` block in the handoff §2. **Same afternoon: the [CDPERF] capture (#796) and WALK N on Android 15 P · 0 F · 9 N/A, gate 1 verified against the Signed APK from the container; the group Damir could not delete on the way = #797 (Core throws when a leave notice has no route; `leaveGroup` ran the send before the removal with no try; ContactDetails cancelled its navigation only at the tail → Android loaded `ixian:leave` as a page) — C# only, smoke 4132.**
- [x] **SESSION O — THE #46 LOOP OVER SESSION M + SESSION N's C# AND #797 → CLEAN IN FIVE ROUNDS · CANCEL-FIRST ON ALL 19 HANDLERS · THE PRE-WARM's BEFORE INSTRUMENT (2026-09-05, cloud container on a clean clone; DECISIONS #798–#799; UNCOMMITTED — commit is Damir's, `docs/commit-message-session-o.txt`). NEXT SESSION: read `docs/handoff-2026-09-05e.md` FIRST; paste-ready prompt in `docs/next-session-prompt.md`.** Item 0 only, by design — the queue was gated on a CLEAN verdict. Baseline verified exactly (4132/3 with the sibling; the −1 without = the M1 `#448` pair). 3 auditors → 2 verifiers → 3 fixers → a fresh break-my-verdict reviewer per round: r1 NOT CLEAN (6) · r2 (5) · r3 (3) · r4 (2 prose) · **r5 CLEAN**. ★★ **Two MAJORs, neither in Session M's own delta.** (1) **The #797 rule held on ONE page** — nine `onNavigating` handlers cancelled only at the TAIL and six more kept a permissive `else`; a throw inside a branch leaves the navigation uncancelled and Android LOADS the verb as a page — on `LockPage` that URL is `ixian:unlock:<plaintext wallet password>`, on `LaunchPage` the create/restore passwords. All 19 now cancel after the decode and re-allow only `file:` (the grammar five pages already shipped); the pin is a WALK over every handler, no list (proven to catch a 20th page). ⚠ The one unverified leg — the #768 fallback ladder on WinUI/iOS — is walk row **A15** (Android immune in code). (2) **The dark Chat-appearance screen shipped an EMPTY CARD** (rendered on the built bundle: dark 2 cards, light 3 — `docs/sheets/session-o/`). Also: `leaveGroup` returns the truth at all four sites (a bot in a group = an honest error now, BE row) · `ixian:painted` gated on `hasGeneratedContent` · `LockPage`/`ScanPage`/`HomePage` `Contains`+`Split[1]` → anchored `Substring` · the strip's post-condition masker threw on CORRECT CSS (an apostrophe inside a comment) → fixed + an unquoted `url(` branch · `PATTERN_LEVELS` retired (786 → 784) · a theme flip closes an open Colour sheet · the settings `renderLayout` negative pin matched NOTHING on HEAD and the painted placement pins were positive-only against a LATCHED signal → repaired. ★★ **Four Session O pins were found vacuous by the NEXT reviewer** (`if \(` with a space · a 200-char window a `!` satisfied · an `ex.Message` ban `+ ex` walked past · a length bound called a charset gate) — #771 in its own loop, four times; and #798 cited a §Verdict that did not exist yet (#660 in reverse). ★ **The rule the loop paid for: a refusal is documented from the cases you did not think of, never from the author's list.** **#799:** the pre-warm's BEFORE instrument — `HomePage.onOverlayClosed` starts the `CdperfFrameProbe` (`chats-after-close`, 600 ms, Android) where `warm()` will be scheduled; the spare itself is NOT built (Tier 3, needs the phone). Closing: **bundle 320 · shells 18 · smoke BASELINE OK 4229 / the 3 known (with sibling) · gate 2 4228 · locales 784 · lint ✓ · pseudo 9/9 · cs-syntax 138 + 1 · three --checks ✓ · Ixian-Core 097341a untouched.** 🟡 Damir: pipeline → wipe obj/bin → F5 → `docs/f5-checklist-session-o.md` (A15 decides one clause; F1 = the eight `chats-after-close` lines) → commit ONE batch. ⚠ `git add` the new files (the brief · checklist · walk artifact · commit message · handoff 05e · `docs/sheets/session-o/`); 05d moved to `docs/archive/` (git shows a rename).
- [x] **SESSION P — THE PRE-WARMED CHAT WEBVIEW · THE BATCH TRANSPORT · A THIRTEEN-ROUND #46 LOOP (2026-09-06, cloud container on a clean clone; DECISIONS #800–#802; UNCOMMITTED — commit is Damir's, `docs/commit-message-session-p.txt`). NEXT SESSION: read `docs/handoff-2026-09-06.md` FIRST; paste-ready prompt in `docs/next-session-prompt.md`.** Baseline verified exactly (4229/3 with the sibling). Both levers built in ONE build (his call — the phone reads one number, the stamps stay separable). **#800 the pre-warm:** a `SingleChatPage` with NO friend is staged hidden in HomePage's grid and its shell booted while the user is still on the chats list; on tap `pushSpareChat` hands it the friend and the SAME overlay path presents the stage it already holds — #796's numbers say it moves WebView creation (72–79 ms) + parse (102–109 ms) of a ~315 ms median open OFF the critical path, and **every refusal falls back to today's construct-and-stage push**. **#801 the batch transport:** `loadMessages` collects the whole burst into ONE `UiBatch` and crosses the bridge once (`addMessages(json, "append")`), then `messagesDone` ends the shell's render burst BY SIGNAL instead of by its 250 ms timer; both transports stay live on both ends. ★★ **#802 THE LOOP: thirteen rounds, 306 mutations, zero survivors that did not become a pin — and it found SEVEN defects self-review did not.** A tap LOST to a navKey dedupe when `attach` threw · "one paint" NAMED by two pins and asserted by none · the chat TEXT SIZE baked into a document that boots before the tap · the shell's 250 ms timer spanning the batch BUILD (the wiped log painted, the reading anchor jumped to the bottom on load-more) · a LIVE message wiped by its own load burst and absent from the batch · a throw in `updateReactions` escaping before the pushes so **every history whose first row is a contact request opened EMPTY** · and, in the FINAL round, a pin built from a FILE LIST instead of a walk that let a `dropSpareChat` site put a **WALLET ADDRESS into `ixian.log`** on every conversation open. Everything else was pin coverage: one-token mutations that killed a lever silently with the whole block green. ★ **The rule that closed the loop is the rule that opened it (#798): a refusal, an enumeration or a sweep written from the author's list is not yet a pin.** ⚠ **Rounds 1–12 ran on the session's own model; only round 13 ran on OPUS — and it still found a MAJOR the other twelve walked past. Run the loop on Opus.** Closing: **bundle 320 · shells 18 · smoke BASELINE OK 4360 / the 3 known (with the sibling) · gate 1 OK · gate 2 OK 4359 · locales 784 ALL CLEAN · lint ✓ (6 dev) · pseudo 9/9 · cs-syntax 138 + 1 · three --checks ✓ · Ixian-Core 097341a untouched.** 🟡 **Damir: the gates → wipe `obj`/`bin` (C# changed) → F5 (Windows: F5, never `dotnet build` — #663) → `docs/f5-checklist-session-p.md`. ★ §3 IS THE SESSION'S POINT: the eight `chats-after-close` lines BEFORE and AFTER decide whether the warm stays where it is — if `drop=`/`max=` rise, the chats list is paying for the lever.** ⚠ `git add` the new files (checklist · walk artifact · commit message · handoff 09-06 · next-session prompt · the loop brief + verdict); 05e and the Session O checklist moved to `docs/archive/` (git shows renames).

- [x] **SESSION Q — THE ACCOUNT SUBLEVELS STOP BOOTING A WEBVIEW · THE RELEASE-READINESS INVENTORY (2026-09-06, cloud container on a clean clone; DECISIONS #803–#804; UNCOMMITTED — commit is Damir's, `docs/commit-message-session-q.txt`). NEXT SESSION: read `docs/release-readiness.md` FIRST, then `docs/handoff-2026-09-06b.md`; paste-ready prompt in `docs/next-session-prompt.md`.** Baseline verified exactly (4362/3 with the sibling). ★ **#803 measured the jank on Android and named the wrong suspect out of the running:** a BRAND-NEW account with zero chats, one minute of Account → Backup/Password/Download, read **12.19 % janky frames · 99th 48 ms** with the GPU idle at 11 ms; the CONTROL on the same profile — scrolling 50 conversations and opening three — read **4.09 %**. The chats list was never the problem. Three Account rows pushed a NEW C# page with its OWN WebView and paid a cold Chromium boot on the main thread. ★ **#804 is three deleted tokens:** `paneMode && ` came out of the `onDownloads` / `onBackup` / `onChangePassword` guards in `src/shells/settings.html`. The screens, the capabilities and the verbs all already existed and had shipped on desktop since #243 / #267 / #341, and `SettingsPage.onLoad` was pushing the caps to the phone the whole time — only the shell refused to use them. **Verified before it was built (#215), six questions, six answers in the tree** — the three screens rendered from `file://` in Chromium at 432×900 @2.5 with an Android UA, driven through the real `executeUiCommand` wire, beside the standalone shells they replace (`docs/sheets/session-q/`). The **#800 pre-warm was considered for these pages and REJECTED**: ~15 MB PSS per resident spare, so three spares would have been ~45 MB to make a boot the app still pays for happen earlier; this removes the boot. ⚠ **Desktop is NOT unchanged** — a window below the pane breakpoint has no `data-pane`, so it pushed the pages too and now renders them inline (walk rows C3b/C3c). ★★ **The #46 loop ran ON OPUS and found four MAJORs self-review had not, two of them inside pins I had mutation-tested myself:** PIN 2 located the caps DECLARATION and never the PUSH, so gating the push on `paneMode` would have killed the feature on every phone with the whole suite green — **and my FIRST repair survived the same mutation**, because it asserted POSITION and `if (paneMode) { … }` is also after that block (#798 twice in one loop; round 2 walks EVERY `if (paneMode)` and requires both statements outside all of them) · the block's error net filtered the NAVIGATION list for `ReferenceError`, which by construction it could never contain · and two prose claims read as a licence to delete live code ("exactly one caller" — the backup nudge is a second; "#797 closes that second leg" — it does not, only the branch's `try` keeps both plaintext passwords out of a shareable log). ⚠ **Two existing #341 pins went red at this batch's COMMENTS and were right to** (`{0,600}` character windows — #771 in a new costume); rewritten as slice-scoped property pins, and one rewrite was itself wrong. ★ **`docs/release-readiness.md` (NEW) answers "what is left": nothing architectural — verification, one BE engagement, and a freeze that has never been scheduled.** Its load-bearing correction: ⚠ **"wallet-send LAST" is a FOSSIL** — send shipped at #523, the legacy pages went at #640, and `composeSend` is pushed unconditionally (`HomePage.xaml.cs:1965`); the live constraint is the **#232/#523 review gate**, and that money delta is enabled in every build today. Also open and named there: the **introduced-vs-inherited sweep from `0e85a4b8` has NEVER run** · three rows sit unfixed in the gate's own "ours" column (MAJOR #3 · MAJOR #6(a) · `spixi.draft.*`) · `Config.cs:94` still reads `maxLogCount = 5` under its own RELEASE BLOCKER marker · the Privacy Policy is HELD on three markers · Sessions I → Q have never been on an iPhone. New: `docs/security-review-for-be-engineer.md` **MAJOR #8** (`HttpUtility.UrlDecode` turns a `+` in the wallet password into a space; INHERITED, and ⚠ the naive fix locks out anyone who already set a `+` password). Closing: **bundle 320 · shells 18 · smoke BASELINE OK 4377 / the 3 known (#136 · M5 · B3) with the sibling · gate 1 OK · gate 2 OK 4376 · locales 784 ALL CLEAN · lint ✓ (6 dev) · pseudo 9/9 · cs-syntax 138 + 1 · three --checks ✓ · 24 mutations, one survivor found and killed · Ixian-Core 097341a untouched.** 🟡 **Damir: the gates → wipe `obj`/`bin` → F5 (Windows: F5, never `dotnet build` — #663) → `docs/f5-checklist-session-q.md`. ★ §3 IS THE POINT: the same Account minute BEFORE and AFTER, with a battery temperature read at the start and end of every capture — end − start > 3 °C VOIDS a capture, and the two arms must start within 1 °C, because Session P's ladder was confounded by a phone that warmed 6 °C on the cable.** ⚠ `git add` the new files (release-readiness.md · handoff-2026-09-06b.md · f5-checklist-session-q.md · walk-artifact-session-q.html · commit-message-session-q.txt · opus-review-verdict-session-q.md · `docs/sheets/session-q/`); six consumed docs moved to `docs/archive/` (git shows renames).

- [x] **SESSION R — ★★ THE SECURITY HANDOVER SWEEP RAN, AND SIX ROUNDS OF THE #46 LOOP (2026-09-06/07, cloud container on a clean clone; DECISIONS #805–#806; UNCOMMITTED — commit is Damir's, `docs/commit-message-session-r.txt`). NEXT SESSION: read `docs/handoff-2026-09-07.md` FIRST; paste-ready prompt in `docs/next-session-prompt.md`.** Baseline verified exactly (4377/3 with the sibling). ★ **The gate `CLAUDE.md` has promised since 2026-08-15 finally ran.** Eight disjoint read-only auditors on Opus over the whole delta from `0e85a4b8`, each deriving its rows from the DIFF (#798), then two adversarial verifiers who refuted impact claims and moved four severities. **82 filed rows → 65 INTRODUCED**, censused in the final section of `docs/security-handover-gate.md`, one row per finding with a branch-or-method anchor (#773). **It did not come back clean.** ★ **Three auditors found the same headline from three directions: we widened the remote-media auto-fetch from two hosts to ALL of them** — the baseline auto-loaded an `<img>` only for an anchored `^https://<label>.(tenor|giphy).com/…`; the redesign took any host over `http` or `https`, on render, behind a gate that defaults ON and whose preference **nothing in the tree ever wrote**. ★ **Two more MAJORs nobody had filed:** a **ZIP-SLIP in account restore** (the `#565` heal-Windows-backups loop turns a backslash into a separator, and on Android/iOS/Mac a backslash is a legal filename character — and it runs **before `verifyWallet`**), and **iOS granting camera and microphone to mini-app content with no prompt** (the capture delegate read neither `origin` nor `frame`; the handler is registered app-wide). **The gate's own three "ours" rows are closed**: MAJOR #3 · MAJOR #6(a) · `spixi.draft.*` — the last one STRUCTURALLY, the mini-app WebView now has its own storage jar on iOS, so Android's `ClassId="miniapp"` marker is a two-platform contract. ⚠ **Windows and MacCatalyst are NOT partitioned**, written down with the one-line device test. **38 of 65 rows fixed; 27 open, each naming its owner.** All **116** BE-brief rows re-verified (26 changed state; sixteen stale rows the release doc had not named; nine INHERITED security rows filed). **★★ THE LOOP — SIX ROUNDS, EVERY ONE NOT CLEAN, and the pattern is the lesson: the pin guarding the link-spoof fix was rebuilt THREE TIMES and a fresh reviewer defeated every version, because each proved a NEGATIVE by matching ONE SPELLING** — r3 the keyword `if (` (one space), r4 the TYPE name `Browser`, r5 the METHOD name `OpenAsync`, and `SFileOperations.open` (which `SingleChatPage` already calls twice in the same file, and which on Windows is `Process.Start(UseShellExecute = true)`) walked straight past it with the suite green. **The fourth attempt changed the DESIGN**: every external link now opens through ONE gate, and the pin proves a **POSITIVE branch-scoped property** — inside each `ixian:openLink:` branch the only call is the gate — which killed reflection and a wrapper helper on its first run. **★★ And rounds 5–6 found the INSTRUMENT: `stripCode` removed BLOCK comments before LINE comments, so a `/*` inside a `//` comment blanked live code — 17 live lines across four files, invisible to EVERY negative sweep in the suite**, and a reproduction hid an OS-open primitive behind two ordinary comments with the whole suite green. Now a one-pass four-state tokenizer, pinned against three independent reference strippers. ⚠ **123 occurrences of the naive regex + 26 more local strippers remain — the next session's first list.** Also found: the peer-forget helper never ran on five (then six) removal paths because those C# handlers push nothing back; the census reported 16 rows OPEN that the batch had closed, because the doc pass ran BETWEEN the fix batches; and **nine of the loop's own pins were vacuous**, several already mutation-tested by their author. Closing: **bundle 321 · shells 18 · smoke BASELINE OK 4613 / the 3 known (#136 · M5 · B3) · locales 786 ALL CLEAN · lint ✓ · pseudo 9/9 · cs-syntax 138 + 1 · three --checks ✓ · Ixian-Core 097341a untouched.** ⚠ **CLEAN is NOT claimed** — r6's two MAJORs are fixed and their reproductions re-run RED, but no seventh round reviewed the fix. 🟡 **Damir: FULL pipeline → wipe `obj`/`bin` → build (Windows: F5, never `dotnet build` — #663) → F5 `docs/f5-checklist-session-r.md` → commit.** ⚠ `nuget.config` changed and cannot be validated without a restore — your first restore is the test, and it must exercise BOTH RocksDB references. ★ **NEW, and it is the next session's item 1: Damir reports Android KILLS the app after ~30 minutes** (511 MB max). The instrument is `adb shell dumpsys meminfo` (read the footer), the suspect is `SpixiContentPage.Dispose()` doing all its teardown inside `if (!Navigation.NavigationStack.Contains(this))`, and the free discriminator is `CHAT_SPARE_ENABLED = false`.

- [x] **SESSION S — THE THREE "STILL JANKY" ACCOUNT ROWS: THE PREMISE, MEASURED (2026-09-08, cloud container over the device bridge; DECISIONS #811; UNCOMMITTED — commit is Damir's, `docs/commit-message-session-s.txt`). NEXT SESSION: read `docs/handoff-2026-09-08.md` FIRST; paste-ready prompt in `docs/next-session-prompt.md`; walk sheet `docs/walk-artifact-session-s.html`.** Damir asked to fix the janky Account rows — Backup, Change password, Downloads. **Those three ARE #804**, so the session checked the premise before building, and the premise did not survive. ★ **Candidate 1 REFUTED AT THE REPO, four ways** — the source guards, the BUILT shell plus a green `build-shells --check`, `SettingsPage:184/:197` declaring AND pushing the caps outside the `if (paneMode)` block, and the one that could still have left the fix inert: `bridge.cap()` reads an `SPIXI_ENV` no shell sets, so the route lives or dies on `setCaps` mutating `bridge.capabilities` **in place** — proven by BOOTING the built shell and tapping the rows. ⚠ **Deployment is NOT settled and could not be**: no adb, no device on this bridge. Two facts point at a stale build — **#804 records Damir's own pre-fix words, *"backup and password stutter, how-to-use and about open just nicely"***, the same set he has now reported, and the Session Q §3 capture has never come back. The discriminator is one tap, and it is his own iOS walk row **L3**. ★ **MEASURED** on the built shell in Chromium at 432×900 @2.5 through the real `executeUiCommand` wire, five runs per screen: **How-to-use 5.1 ms of script · Backup 5.2 · Change password 5.3 · Downloads 29.0**, and Downloads scales — 6.6 at 0 files, 25.9 at 12, 58.9 at 100. **At zero files Downloads costs what the control costs; the whole term is the list** — `loadDownloads` sends one `addFile` marshal per file, each its own `EvaluateJavaScriptAsync` after a `File.GetCreationTime` stat, on the UI thread, fired at the START of the 300 ms slide (the #801/#298 shape). **The two rows named first are the two with no measurable cost.** **NOTHING BEHAVIOURAL WAS BUILT (#294)**; the Downloads batch is specified and gated on one fact nobody has — the file count. **What shipped is the instrument and the pin:** `[CDPERF] settings-sub` (`build` = the synchronous render · `paint` = tap → committed frame · `list` = the C# round trip as its own term) — ONE hook on a view CHANGE inside `renderLayout`, not a call in twelve setters (#798), `console.warn` never `.info` (#751) — **RUN in Chromium before it was pinned**; and **GATE 44**, the pin #810 asked for, which **EXECUTES** `strip-release.mjs` through a symlinked path (working guard → exit 2 + usage; the W-R6 guard → **exit 0 in silence**) plus a walk over every main-module guard under `scripts/`. ★★ **The pin's first draft convicted its own file** — the walk matched the raw token and clause (a)'s message named both tokens forty characters apart: **#771 in a third costume, the prose is the pin's input**. ★★ **And the instrument lied twice before it told the truth:** a "perfectly clean" four-screen result produced by **clicks that never opened anything** (headless Chromium defaults to `prefers-reduced-motion: reduce`) — *a measurement with no assertion that its subject changed is a pin with no mutation* — and a Downloads figure inflated by `btoa` run inside the page; the frame-gap column is **declared untrustworthy** (50.1 ms for Backup with a 640×640 PNG *and* with a 1×1 one, so the image hypothesis is **REFUTED**). **Nine mutations, zero survivors.** Closing: bundle **321** (untouched) · **18 shells**, settings rebuilt · `build-shells`/`extract-strings`/`build-legal-docs` `--check` ✓ · locales **786 ALL CLEAN** · lint ✓ (6 dev) · pseudo **9/9** · NUL sweep clean (⚠ `grep -c $'\0'` is VACUOUS — it matches every line; use `tr -dc '\000'`). ⚠ **`smoke-test.mjs` could NOT be run on this bridge** (>3 min per call, a backgrounded run dies with its call) and `cs-syntax-check` skipped (no tree-sitter) — **4621 → 4624 is PREDICTED, Damir's run is the measurement**. 🟡 **Damir: `node scripts/build-shells.mjs settings` → `node scripts/smoke-test.mjs` → build (no `obj`/`bin` wipe, no C# moved) → `docs/f5-checklist-session-s.md` — §1 is ONE TAP and it decides the session → commit.** ⚠ `git add` the four new docs. **Still owed and unchanged: the Android memory kill (511 MB, RELEASE build — `dumpsys meminfo`, the `SpixiContentPage.Dispose()` stack guard, `CHAT_SPARE_ENABLED = false` as the free discriminator) · a seventh #46 round over Session R's LOOPFIX8/9 · the 123 naive comment-strip regexes.**

- [x] **SESSION S, part 2 — M13: THE CODE HALF WAS ALREADY CLOSED, AND THE SWEEP THAT PROVED IT FOUND A DIFFERENT DEFECT IN EVERY SHELL (2026-09-08; DECISIONS #812; UNCOMMITTED with #811).** `release-readiness.md` §2 calls the i18n residual *"the most user-visible item on this page … ours and buildable now"*, from a Damir F5 of **2026-07-11**. It was not built against. **The instrument:** every BUILT shell booted under **`?lang=pseudo`** — the marker locale the project already ships, where a string that went through the dictionary renders `⟦key⟧` and anything in plain English **never did** — visible text plus `placeholder`/`aria-label`/`title`, one click deep, allow-list DERIVED (endonyms read out of `flags.js` at run time, #798) with its two scope carve-outs carrying their reasons; a second pass **feeds data** through the real `executeUiCommand` wire, because the surfaces named render nothing until C# pushes. ★ **VERDICT: no visible English left on those surfaces. M13's code half was closed by #269 on 2026-07-12 — the day AFTER the F5 that recorded the residual** — and the row was never updated (the #660 class). What remains is the **translator pass**, 15 (ru-ru) to 76 (it-it) still-English keys per locale — Damir's, not code. ⚠ **The near-miss:** a `?lang=de-de` cross-check flagged "Chats", "Apps", "Wallet", "Status" and **every one is a correct German translation identical to the English** — only the marker locale separates *never looked up* from *translated to the same word*; nothing from that pass is quoted. ★★ **What the sweep DID find, in every shell:** the static a11y labels in boot markup — 16 × `aria-label="Loading"`, the chat log's `"Messages"`, `"New chat"` — plus `createTopbar`'s `backLabel` and `createBottomNav`'s `ariaLabel`, which defaulted to the English **literal** and which almost no caller passes. **A German user's screen reader said "Back" and "Loading" while the dictionary held "Zurück" and "Wird geladen"**, on Settings, Downloads, Contributors, Add contact, Dev, Backup and every Account sublevel. Invisible to BOTH gates: `i18n-lint` sweeps DOM sinks not attributes, and `extract-strings` sweeps `strings.x || '…'`, which markup cannot be. **ONE mechanism, not eighteen edits:** the component defaults read the live dictionary through the `strings = getStrings()` param form **the extractor already sweeps** (`getStrings().back` would have been correct and still invisible); the markup labels carry `data-sl-aria` beside the English fallback, swapped by one boot walk per shell — they stay in the markup because a reader must have them on the FIRST paint, which is why they could never be `getStrings()` calls. ⚠ **`extract-strings` was taught the pair**, because a mechanism the sweep cannot see is a key that can never be translated — the exact class being closed. **786 → 788 keys** (`mainNav`, `messagesLog`), 0 fallback conflicts. ONE carve-out (`empty_detail.html`, no `window.SL` at all) **with its premise asserted, not skipped** — and its two English sentences were **REFUTED** as a leak: `*SL{}` carriers, present and translated in all 13 `lang/*.txt`. **GATE 45 = a WALK over all 18 shells** (every static aria-label is a carrier or carries a key · every shell with a key runs the walk that consumes it · both component defaults read the dictionary · the extractor still sweeps the pair). **Six mutations, zero survivors; 15 across the session, zero survivors.** Closing: bundle **321** · **18 shells** · `build-shells`/`extract-strings`/`build-legal-docs` `--check` ✓ · locales **788 ALL CLEAN** · lint ✓ · pseudo **9/9** · NUL sweep clean. ⚠ smoke still NOT runnable on this bridge — **4621 → 4625 predicted**. 🟡 **Damir: FULL pipeline (`extract-strings` → `build-locales` → `build-strings-iife` → `build-demo-bundle` → `build-shells`) → `smoke-test` → build → `docs/f5-checklist-session-s.md` §5 (TalkBack/VoiceOver under Deutsch — this batch changes accessible NAMES only; any visible text that moved is a regression) → commit.** ⚠ `docs/polish-roadmap.md`'s M13 row and `release-readiness.md` §2's M13 bullet are **STALE and were left alone** — rewriting another session's record without the walk to back it is how #660 happened; #812 is the correction.

- [x] **SESSION X — THE NEWEST BUBBLE UNDER THE COMPOSER (a `ReferenceError` swallowed for three sessions) · THE UNDECLARED-IDENTIFIER GATE · THE CHOOSER IN DARK · THE CHATS-ROW COLUMN · THE READ TICKS · THE CONTACT-DETAILS PROPOSAL (2026-09-17, over the device bridge — `device_bash` WORKED; DECISIONS #869–#877; UNCOMMITTED — commit is Damir's, `docs/commit-message-session-x.txt`). NEXT SESSION: read `docs/handoff-2026-09-17.md` FIRST; paste-ready prompt in `docs/next-session-prompt.md`.** §0/§1: icons green on disk, the W paste ran, suite = BASELINE OK / the 2 KNOWN, one commit not two, **S1 "P" but not timed → #864 still gated**. ★★ **#869:** Damir's screenshot — the last message under the pill in every chat. The #711 publish wrote `--composer-h` to `chatCanvasEl`, a const **Session U deleted with the "Live flow" renderer** (`93d8b808:698` → gone at `690b09c9`), inside `try { … } catch (_) {}` — never written, and **all five `--composer-h` pins green because each reads the source string**. Fixed (target from the slot, publish before the observer, the throw logged); the built-shell boot pin now reads the PROPERTY (HEAD red, fix green, 3 mutations killed). **The class gate: ESLint `no-undef` over every inline script of every source shell** (browser + ES globals + what the shell itself makes global) — named `chatCanvasEl` on `608e12b7`, zero noise on the other 17; refuses to pass without its parser (header: `npm i --no-save jsdom eslint globals` — ⚠ install all three together, `--no-save` of one PRUNES the rest). **#870** chooser (walk V 1.7/1.8/3.2): the card was neutral-800 on a neutral-700 sheet in dark (recessed), hairline the card's own tone, hover the card's own tone in BOTH themes → new `--surface-sheet-card/-hover/-pressed` pair (lifts in both modes, pinned as a property over resolved primitives), medallion tonal on hover; art ladder **png → glyph** exercised by a pin (⚠ an SVG-first rung shipped for one build and Damir's suite run FAILED it on the Session N reachability gate — I ran my pins, not the suite's gates; reverted) — 🟡 Damir owes a TRANSPARENT `add-contact.png`, same name. **#871** (mid-turn asks): the chats-row trailing column = two line boxes, always both tall — MEASURED 0.0 px time-to-name on the built shell; read ticks: `--icon-bubble-read` = `#8ff5b4` (4.52:1; success-300 was 3.06) — ⚠ a HEAVIER read tick shipped for one build and Damir REVERSED it the same day (**#877: one weight, colour is the only differentiator**; the C dial withdrawn); ⚠ my first contrast pin proved one declaration and claimed two — its own mutation caught it (#798). **#872**: bottom bleed = cheap but APP-WIDE (the AND-7 mirror: pad the IME leg only, publish `AndroidInsetBottom`, `--safe-bottom`, 31 sites/16 files; keyboard rows are the risk; Android 15 enforces it anyway) — read, not built (#294) · `docs/contact-details-premium-proposal.md` with a MOCK rendered over the real shell (monochrome glyphs, inset groups, destructive last, one accent; three forks, recommendation A·A·A — the discs are #147/#618's Account-hub grammar, so scope is his call) · #865 extends to PNG (`caBX` chunk on every committed sheet). Gates on the VM: bundle 317 · 18 shells · `build-shells --check` ✓ · `extract-strings --check` ✓ · lint ✓ · icons ✓ · NUL 0; **full smoke = Damir's run, predicted +9 / the 2 KNOWN** (a detached run dies with the call). ⚠ `git add` the new docs + `docs/sheets/session-x/`; `Remove-Item _to_delete`.
- [x] **AND-45 — THE TRANSPARENT NAVIGATION BAR UNDER THE COMPOSER, BUILT + THE OPUS LOOP ON IT (2026-09-18, over the device bridge; DECISIONS #883–#884; WALKED by Damir the same day **17 P · 0 F · 1 N/A** and COMMITTED `89c85b19`).** Damir reopened the #875-dropped bottom bleed ("let's do the transparent notch below the composer"). Built as #872 ① costed it — the mirror of AND-7/#401: `MainActivity` pads the root by the keyboard's height ABOVE the bar only (`ime − navBar`; #334/AND-16 untouched), the nav-bar inset travels as `*SL{AndroidInsetBottom}` + a `setInsetBottom` push into `--android-inset-bottom`, `base.css` `--safe-bottom = max(env, var)`, 28 use sites + 18 shell heads swapped, a mini-app keeps native padding, `NavigationBarContrastEnforced = false`. ★★ **The #46 loop (Opus) found FOUR MAJORs in the first cut — all fixed the same session:** my "effective 0 while the keyboard is up" value had to be PUSHED 1–3 frames after the root re-laid out = a one-bar composer JUMP on every keyboard open/close, and a mini-app captured that 0 → the value is now a keyboard-independent CONSTANT (root + shell = ime up / navBar down; chat.html adds the bar back onto the slot, Session J holds); the OS contrast SCRIM under a "transparent" bar (API 29+); the sticky Install bar losing its floor (my walk pin was ONE-SIDED — forbade `env()`, never required `--safe-bottom` → a positive property pin now); the gesture-nav estimate. 12 old `env()` pins re-based in both halves + Session J + B3 + the R2-1 harness; 23/23 mutations killed; `CHAT_KB_CEIL` 684 → 686 (delta stated). **The full suite in the container: BASELINE OK — 4727 / the 2 KNOWN on the exact tree landed** (hashes read back; built artifacts identical VM ↔ container; cs-syntax-check 138 clean). C# changed → F5 + Android deploy; device-walked 17/0/1 (A.14 n/a), `docs/archive/f5-checklist-and-45.md` holds the marks. **#884 = Damir's Session Z list from the Y walk, recorded with measurements:** sheets +padding (16 → 24/32, `overlay.css:39/68`) · the dark chooser card "random colour" = the grey/ink HUE BREAK at neutral-500/600 (palette call) · the hardcoded-colour sweep (64 CSS + 4 JS literals, verdicts per file) · "Groups you are both in" NOT all caps + translated (7/12 locales today; verify the device's language code) · the on-card hairline too strong in light (1.42 → a `--outline-on-card` pair) · the nickname pencil off-centres the name (contact details + hub) · locale drafts. LABEL: the code says `AND-45` — `AND-7b` was already #407's.
- [x] **SESSION Y — THE CONTACT-DETAILS PREMIUM PASS, BUILT · THE OPUS LOOP ON THE BATCH AND ON SESSION X · THE FULL SUITE IN THE CONTAINER (2026-09-17 evening, over the device bridge; DECISIONS #878–#882; UNCOMMITTED — commit is Damir's, `docs/commit-message-session-y.txt`). NEXT SESSION: read `docs/handoff-2026-09-17b.md` FIRST; paste-ready prompt in `docs/next-session-prompt.md`.** §0/§1: the X commit landed (`896958b4`), both `--check`s green, X walk all P, suite BASELINE OK / 2 KNOWN +9, **S1 still not timed → #864 still gated**. ★★ **#878 BUILT as decided** (#873/#875/#876, FE only, ★ #221 untouched): monochrome row glyphs (no discs on THIS screen; the hub keeps its) · inset-grouped cards (label outside, one card per group, radius 12, no elevation, `::after` hairlines) · ≤ FOUR tiles (Message · Call · Pay · Mute; Request out; `QA_MAX` log+trim) · Mute = a toggle TILE (label is the state) · the address UNDER THE NAME in ACTION INK when tappable (**#879**, V2 of four renders; bots carry it) · destructive rows one card, 1:1 last, rooms above the roster, red = a label role. Three token spellings moved on MEASUREMENTS (hairline -03: -01 is 800-on-800 in dark; a new `--surface-card-hover/-pressed` pair: `-raised` is the invisible 1.04/1.048 step); contrast COMPUTED in the suite (error 5.80/8.08 · glyph 6.44/9.09). Rendered both themes through the wire BEFORE pins; 42 properties, 19 mutations killed, 23 pins re-based in both halves. **#880 the Opus loop on the batch** (Damir: Opus reviews, Fable builds — pinned explicitly): 2 MAJOR only visible in a real cascade — my `::before` hairline collided with base.css's press layer (no line on tx rows AND the press fill squashed to 1 px) and the card's clip ate the focus ring (WCAG 2.4.7) — both fixed and re-rendered; aria-label/aria-pressed/comment numbers fixed; **Request is now unreachable from the directory — Damir's dial**. **#881 the Opus loop on Session X**: the gate's false REDs are LOUD as claimed (proved on 40+ fixtures + an AST diff, EXTRA = 0), but ONE silent FALSE GREEN — a `// <!--`…`// -->` pair in one script deleted the code between them before the lint — fixed with a comment scanner (outside `<script>` spans only), attribute `src`, seeded externals, file lines, per-shell vacuity and a FIXTURE pin; #877's tick clause was spelling-bound (6/8 survived) → the positive property (6/6 killed). ★★ **#882: THE FULL SMOKE SUITE RUNS IN THE CONTAINER** (tree tar'd across, 15 MB; ~5 min; bundle hash-identical to the VM's) — **BASELINE OK — 4714 / the 2 KNOWN on the final tree, a RUN not a prediction**; two traps with cures (`git status` leaves an undeletable `index.lock`; a post-reconnect "written" that was not on disk — re-read the hash). Four dials for Damir: card-row hover/pressed invisible on every card-hosted family · the hub's dark hairlines · `chat.html`'s dead #249 takeover inline (~30 KB, why `CHAT_KB_CEIL` moved 682 → 684) · Request from the directory. ⚠ `git add` the new docs + `docs/sheets/session-y/`; three moves into `docs/archive/`; `Remove-Item _to_delete`.
- [x] **SESSION Z — THE Y-WALK LIST BUILT · THE THREE #882 DIALS · FLAGS ON WINDOWS (L15b) · THE GLASS DIAL · THE OPUS LOOP (2026-09-18, container twin of the VM tree, landed over the device bridge; DECISIONS #885–#891; WALKED the same evening **15 P · 1 F** and COMMITTED `09f379b1`). NEXT SESSION: read `docs/handoff-2026-09-18b.md` FIRST, then DECISIONS #891; paste-ready prompt in `docs/next-session-prompt.md`.** §0/§1: AND-45 walked 17/0/1 and committed; S1 still not timed → #864 gated; his dials taken before any build (#885: sheets 24 · chooser = render both, he picks · `--ink-500/600` primitives · tokenise `system-notice.css` only · #882 (a)(c)(d) build · the flags brief "build it fully" · the Windows composer "too low"). **#886 = #884 ①–⑦ built in the order ④ ⑤ ⑥ ① ② ③ ⑦, each rendered both themes through the wire before its pins:** the card label in sentence case (+ `sharedGroupsTitle` for cn/id/it/ja/lt) · ONE hairline role `--outline-on-card` (light neutral-200 1.137 · dark neutral-600 1.187, measured; the hub's dark hairlines were 1.00) · the pencil's 44-wide ghost so the NAME centres on both screens · sheets 24 (the tray stays 16 — it fills a fixed slot) · ★ the dark chooser's "random colour" was a HUE BREAK (the ink ramp had no 500/600 → dark neutral-500/600 fell through to the GREY primitives) — `--ink-500 #253246` / `--ink-600 #202b3c`, every consumer re-measured · the notice card tokenised, every other literal `sanctioned:` and **GATE 64** (a declaration-joined walk over stripped CSS, with fixtures) as the property. **#887:** card-hosted hover/pressed on every family (a DERIVED pin: a control with no hover = fail) · the dead #249 takeover DELETED from chat.html (**−38 859 chars on every conversation open; `CHAT_KB_CEIL` 686 → 648, DOWN**) · Request back on the directory arm, Mute yields. ★★ **#888 L15b — flags on Windows, built:** a flags-only Twemoji subset (78 292 B, CC-BY 4.0, credited in-app + NOTICES) installed ONLY on an answered "no" from the canvas probe ('yes' AND 'unknown' refuse — a phone loads nothing), range U+1F1E6-1F1FF, PNG flags already painted upgraded in place; ★ **the CORS finding: a `@font-face` `url()` is REFUSED from a `file://` document (origin `null`)** → the payload is a generated classic `<script>` carrying a `data:` URL (`fonts/TwemojiCountryFlags.js`, `--check`-covered), 17 shells call `installFlagFont()`. Proved in Chromium from `file://`; **WebView2 = the walk's one unverified leg.** **#889:** Damir's glass question answered (cheap as ONE band — a compositor blur of the composer's rect, not per message) and built as an OPT-IN dial, default OFF, no toggle until he picks the A/B; the Windows composer foot 12 px. ★★ **#890 the loop on Opus:** "contrast unchanged" was FALSE (luminance) · the file:// font block (MAJOR) · the probe's INVERTED fail-safe · GATE 64's regex/walk-up holes · the welcome pill PNG · a VACUOUS derived pin · GATE 64 per-line blind · the unexecuted script path — all fixed, every pin mutation-killed; ★ and the SUITE's own last catch: my scratch runner swallowed jsdomErrors, so synthetic load/error events passed a pin the real suite failed on a real `file:` fetch (jsdom's interceptors never see `file:` URLs — probed) → the pin runs on the BUILT shell, jsdom fetching the shipped payload itself, and declares its one miss (`load(file, { expectErrors })`); 3/3 killed. Residuals recorded as dials (sheet-card hover 1.103 · member-sheet micro-label · Downloads/apps-grid screen hover · MINOR-6 boot-time probe · NIT-12). **Closing: bundle 1 289 624 B / 323 exports · 18 shells · built chat.html 662 061 · `build-shells --check` ✓ · `extract-strings --check` ✓ · lint ✓ · pseudo 9/9 · the FULL suite in the container: BASELINE OK — 4754 pass / the 2 KNOWN · every landed hash read back · no C# changed.** **WALK (#891): the flags PASSED in WebView2 (Z.1–Z.3 — the #888 route's unverified leg is closed); Z.5 FAIL — the glass band "looks ugly, a rectangle and cutoff" → the dial is OUT next session (one fade render for his eye, else nothing). ★ Three findings on the way, all verify-first: contact details → Message and a shared-group row both RETURN TO THE CONTACT LIST (one C# path: `popPageAsync()` then `HomePage.onChat()` — the `Chat page for … already open.` log line is the discriminator) · the CALL STRIP is INVISIBLE (A.11 passed this morning; `call.html` changed by ONE line since — the `installFlagFont()` call; one-line revert first).**
- [x] **SESSION AA — THE GLASS DIAL RETIRED · THE FADE RENDERED, NOT LANDED · THE THREE Z-WALK FINDINGS RULED OUT AT SOURCE (2026-09-18, over the device bridge; DECISIONS #892–#894; UNCOMMITTED — commit is Damir's, `docs/commit-message-session-aa.txt`). NEXT SESSION: read `docs/handoff-2026-09-18c.md` FIRST, then `docs/diagnostic-session-aa-findings.md` before touching calls or contact details.** §0/§1 verified on the real tree: three `--check` gates green, Session Z committed (`09f379b1` + the docs-only `e326f76b` carrying #891). **#892:** Damir walked the glass dial and rejected it (Z.5), so the rule + `@supports` fallback (composer.css), `--surface-composer-glass` in both modes (tokens.css) and `spixi.chat.glass` in ALL FOUR readers (chat.html) are deleted; the security-gate row is marked RETIRED, not removed (the pref never shipped a toggle — no device holds a value); `CHAT_KB_CEIL` 648 → **645**, delta stated (built chat.html 662 061 → 659 155, −2 906). ★★ **The retirement found a pin a grep could not: `grep 'spixi.chat.glass'` found the two pins that NAME the key (both re-based) and MISSED the one that only COUNTS — `Session P L1·12`, which never spells the key — it DERIVES the head script's key list and asserts `headKeys.length === 5`.** It went red in BOTH halves on the first full run, which is exactly its job. The #889 pin was INVERTED, not deleted (#835), because the property worth holding is a naming trap: `--surface-composer-glass` (retired) and `--surface-chat-glass` (LIVE — the day pill, GATE 48) differ by ONE WORD, so it asserts both directions. **7 mutation runs, 8 clause-kills, zero survivors.** **#893:** the one alternative Damir asked for, rendered not landed — a `mask-image` on the message scroller whose fade zone sits ABOVE the pill, so a bubble dissolves as it ARRIVES instead of being sliced mid-word (the first cut faded only UNDER the pill and was invisible, because the pill already hides that strip); the newest message is never dimmed (`#messages` already pads by `--composer-h`). Both themes on the real built shell through the live wire — `docs/sheets/session-aa/`. ⚠ The render ASSERTS ITS OWN SUBJECT before shooting (#811, and it bit again: a nine-message conversation never overflowed the viewport, so zero bubbles were under the pill and all four panels were identical — the guard aborted instead of producing a sheet that proved nothing). **#894:** ★★ **#891's own prescribed test was FALSIFIED before it could spend a build** — it called for a one-line revert of `installFlagFont()` in `call.html`, but that function returns immediately on any device that can paint a flag (`flags.js:222`), so on Android it is a no-op, and Damir's answer was "both blind, nothing at all". Also ruled out IN THE TREE: the built `call.html` boots CLEAN under the suite's own jsdom harness (`setCallUi` a reachable global); `CallPage` has a 1500 ms reveal backstop (`:493`); `callbar.css`/`call-overlay.css` untouched by Session Z; A.11's pass verified at its source. **What remains is the reading nobody had written down: a call showing no UI on either side is also what a call that never connected looks like.** For ①/② the pop was cleared (`popPageAsync` closes its OWN overlay), leaving `HomePage.onChat`'s two silent exits, told apart by ONE existing `Logging.warn`; ⚠ noted but NOT a verdict: `getChatPages()` carries a spare belt, `getChatPage` does not. **Nothing was built** — all three are settled by one log capture per device with lines that already ship. ★★ **#895 — THE #46 REVIEW ON OPUS: 1 MAJOR, 5 MINOR, and it caught me making the exact mistake I cited all session.** Residue, the derived pin, the numbers and stale comments came back explicitly clean. **The MAJOR was in the diagnostic: its table named `lockUp: …` as the exclusion firing, but that string exists ONLY in a `catch` — the exclusion ACTUALLY firing returned `null` in SILENCE** while the other three refusals in `ensureSurface` all name themselves, so a reader finds three reasons, sees none, and walks past the lock the table flagged two rows earlier — and that branch is the one a STRANDED lock takes, which is Damir's report verbatim. Fixed in the table AND in the code: **TWO `Logging` lines in `CallPage.xaml.cs`, no behaviour change — the only C# in this batch, so it DOES need a build.** ★★ The pin written for it WALKS every `return null` instead of listing the four I knew about, and immediately found a **FIFTH silent refusal** nobody had seen (`if (content == null)`) — #798 paying off inside the session that quoted it; an existing pin asserting ADJACENCY went red on the insertion and was re-based to the property. **MINORs:** my day-pill clause counted declarations FILE-WIDE while claiming "both themes" (the reviewer's counter-example PASSED it) — theme-scoped now on the file's own `withoutDarkBlocks`; the pin's stated reason for stripping comments was self-referential; "both verbs run the same two statements" is wrong for `ContactDetails:629`, whose first arm is pop-with-no-open = the reported symptom; my fix shape "agree with the plural" is a **no-op** (a blank spare already has `friend == null`); and **"the grep returned ONE site" was itself a wrong count** (five lines, two pins) — corrected everywhere, #660. **Suite in one container on this exact tree: BEFORE `4752` → AFTER `4753`, the same 2 KNOWN** (+1 = the new walk pin), `cs-syntax-check` 138 clean + 1 known gap (⚠ no `Ixian-Core` sibling there). ★ **MEASURED on Damir's machine the same day: `4755 / the 2 KNOWN` — Session Z's local 4754 +1, exactly this batch's one new pin, so the batch is clean.** ⚠ That also corrects MY reconciliation rather than either run: the container reads **2 below** this machine, not 1 (the M1 sibling gate is one; the second is another environment difference, not worth chasing). **Compare deltas within ONE environment; never compare an absolute count across two.** ★★ **#896/#897 — DAMIR'S DESKTOP REPORT, TRACED AND FIXED THE SAME DAY:** clicking through wallet transactions "weirdly fade in in the pane, briefly revealing the actual chat screen of some chat below". **The conversation really was still there** — `HomePage`'s `ixian:tab:` sweep closed the Account pane, the chat-info pane, the add-contact/add-app form pane and the tx detail **each by name**, and NOT the conversation, so a chat opened on Chats stayed live and pinned in the detail column across the whole app. ★ **The sweep was already making the argument** — `closeTxDetailOverlays` is in that branch because "the tx detail belongs to the wallet tab" (#263); by that rule the conversation belongs to the chats tab, and the list read like it was meant to be exhaustive. ★ **It is also the SAME DEFECT L6 fixed** (a pane that stopped hiding a pinned conversation revealed one the user never opened) — L6's negative pin *"a plain tab switch does NOT close the conversation"* was scoped that way only because no one had yet seen a tab switch reveal one, so it is **INVERTED with its reason** rather than quietly flipped, and re-sliced to `repaintOwnSystemBars` (its old `+2000` window no longer reached the branch end). `closeChatOverlaysForContacts` → `closeChatOverlays` (with a second caller the name would lie); 3 L6 pins re-based. ⚠ **The tab1 gate is HALF the property** — a tap on the tab you are on, and `home.html`'s Fix #8 boot echo, both arrive as `ixian:tab:tab1`; both halves mutation-killed separately. ★★ **#898 — AND THE FLASH ITSELF, WHICH WAS NEITHER CANDIDATE #896 NAMED.** Damir: "no fade no flash no nothing — each other you open just changes instantly". `WalletSentPage.onLoad` ended with **`webView.FadeTo(1, 150)`**: the page fading its OWN WebView in on every construction, translucent for 150 ms, so whatever was pinned under the detail column showed straight through. **One line in the PAGE, under two sessions of theory about the overlay machinery and compositors** — and #896's free discriminator would have split WebView2 first-paint from the 220 ms close fade, two mechanisms that were both wrong. Instant reveal now (the ctor's `Opacity = 0` stays; the shell's `bridge.painted()` already gates the present, so the ramp hid nothing). ★ And every tap built a NEW page and WebView — an open detail is now REUSED via `showTransaction`, which swaps the field and re-renders in ONE frame (#289 commits atomically). ⚠ The swap's risk is the latches, so that pin is DERIVED from the class's own field declarations with a carve-out — a field added later and not reset fails it, and the mutation proved it NAMES the culprit. ⚠ Pin ① failed on its own explanation before it was fixed (its negative sweeps for `FadeTo`; the docblock quotes `FadeTo(1, 150)`) — **the third time #771 appeared in one session**. ⚠ The trade is Damir's own, made twice: returning to Chats lands on an empty pane — if it bites, PARK instead of close (#315). Suite **4752 → 4756** across the session (#897 replaced one pin with one; #898 added three, all mutation-killed), `cs-syntax-check` 138 + 1. 🟡 **Damir: the paste in `docs/f5-checklist-session-aa.md` → walk AA.1–AA.13 (★ AA.3: the day pill's frost must SURVIVE · ★ AA.11: no fade/flash + instant swap · AA.12: the tab1 gate · AA.13: a PENDING tx still updates live) → pick on the fade → ONE commit.** ⚠ `git add` the new files (`docs/diagnostic-session-aa-findings.md` · `docs/f5-checklist-session-aa.md` · `docs/walk-artifact-session-aa.html` · `docs/handoff-2026-09-18c.md` · `docs/commit-message-session-aa.txt` · `docs/sheets/session-aa/`); `handoff-2026-09-18b.md` moved to `docs/archive/` (git shows a rename); `_to_delete/` is this session's scratch — gitignored, delete it. ⚠ The ASD-STE100 rule above is STILL unconfirmed (#512 flagged it; recent docs do not follow it) — one sentence from Damir settles it either way. ★★ **#899 — THE PRIVACY POLICY IS FINAL, and it had been since 08 September while TWO documents said otherwise.** Damir: "only thing was message retention before deletion, and we confirmed it." Verified: Session S (`9e099ee7`) removed **all three** markers `release-readiness.md` §1 still listed — the ⟨PLACEHOLDER⟩ in §4.3, its twin in the §11 table, and the #708 annotation; the confirmed answer (**undelivered messages discarded after 30 days**) is stated twice and consistently, and `claims-sweep.md` carries no `NEEDS DAMIR`. ⚠ That page's §0 said "both bake" while its §1 said HELD — one file, two opposite claims, eleven days (#660 again, in the document that answers "what is left before we ship"). ★ The one real gap was NOT the text: the header read `Last updated: 29 August` while the retention clause was written 08 September — the date a user relies on and a store reviewer checks. Corrected to the commit date, `Supersedes…` deliberately untouched (publication history this repo does not know). ⚠ Acceptance is NOT versioned (`LaunchPage.acceptedTerms` is a page-local bool) — no re-acceptance mechanism, logged not built. ⚠ **A lawyer still has not read it**; "nothing is held" and "counsel approved" are different statements. Rebaked 17 942 → 17 945 chars (+3 = the date string). ★ **#900 — WALKED: 12 P · 0 F · 1 N/A, ready to commit.** ⚠⚠ **TWO rows that look like closures are not.** **AA.7 DECLINES the fade** ("nothing for now, we dont touch it") → #893 RETIRES, the edge ships as it is. **AA.8 is N/A because finding ③ DID NOT REPRODUCE** — evidence of absence, not repair: nothing changed on the call path but two `Logging` lines, the mechanism is still unknown, and what the batch bought is that the branch which most plausibly explains it now NAMES ITSELF in `ixian.log`. The diagnostic stays armed. AA.9/AA.10 pass; 🟡 #897 removing the stale pinned conversation is a plausible cause but UNPROVEN (the log line was not captured) — `Chat page for … already open.` remains the discriminator if it returns. AA.11/AA.13 prove #898 (no fade, instant swap, and a PENDING tx still updating — no latch carried over); AA.12 accepted with his note. 🟡 **NEXT: the bubble gap + the tail — and the tail is NOT a token edit.** `--bubble-gap-inner: 1px → 2px` is a one-token REVERT (it was 2px at dial C) + one pin. But `--bubble-tail`/`-h` set only the BOX: the SHAPE is **four hard-coded `clip-path` values in absolute px**, drawn for a 9×13 box, LTR+RTL × sent+received, all pinned at `smoke-test.mjs:24303`. Shrink the tokens without redrawing all four and the tail is a 9×13 shape clipped inside a smaller box. **Render both directions in both themes before pinning.** **NEXT SESSION: read `docs/handoff-2026-09-19.md` FIRST.** Consumed → `docs/archive/`: `handoff-2026-09-18c.md`, `f5-checklist-session-aa.md` (with the verdict written in), `walk-artifact-session-aa.html`.
- [x] **SESSION AB — THE OWED #46 LOOP OVER #896–#900 (3 MAJOR, ALL FROM LAST SESSION'S OWN WORK) · THE BUBBLE GAP 2px + THE TAIL 7×11 · THE MENU RE-VERIFIED · THE PRIVACY POLICY CHECKED AGAINST THE CODE (2026-09-19, container twin of the VM tree, landed over the device bridge; DECISIONS #902–#906; UNCOMMITTED and UNWALKED — commit is Damir's, `docs/commit-message-session-ab.txt`). NEXT SESSION: read `docs/handoff-2026-09-19b.md` FIRST; paste-ready prompt in `docs/next-session-prompt.md`.** §0: Session AA was already committed (`6e059975` + `ed392326`); four `--check` gates green; suite BEFORE = `4756 / the 2 KNOWN` in the container. ★★ **The loop ran on Opus in THREE rounds (3 auditors → verify → fix → a fresh break-my-verdict reviewer → fix → r3 over the r2 fixes → fix) and found three MAJORs self-review had not, every one CREATED by #897/#898.** **#902:** #897 tied "the conversation belongs to the chats tab" to the `ixian:tab:` VERB, and a conversation can enter the detail column without one (contact details → Message via Account → Contacts, which sends no tab verb since L6; a notification tap) — one tx tap + one back then showed a chat on the wallet tab. `onTransaction(…, bool fromConversation = false)` closes it wherever it came from; ⚠ **the reviewer's own one-line fix was WRONG** — unconditional, it would have made a payment card's Details close the chat it was tapped in; the ONE exempt caller says so and the pin counts callers. Also a staging conversation/tx detail is `activePreload`, not an overlay, so the sweeps walked past it; the rating prompt and the `chats-after-close` probe (the #864 instrument) now fire only with the chats list on screen. **#903:** the reused tx pane kept "Show amounts" across transactions — with the wallet hidden, B opened FULLY UNMASKED with no eye (reproduced on the built #898 shell through the wire, then pinned there in both halves: the reveal drops on a txid CHANGE, never on the same tx's re-burst) · and the money card was now rendered from TWO threads: the body read the field three times, and ★ **`MainThread.BeginInvokeOnMainThread` runs INLINE on the main thread and POSTS off it**, so a tick's burst for A landed after the tap's inline burst for B. Fix = the shape `loadTransactions` already carries: `txLock`, the UI thread hands off to the pool and never takes the lock, one field read, a sequenced swap inside the lock; r2/r3 added `shownTransaction` (the explorer verb follows the CARD), `paneHosted` (the `getStagingPage() == this` proxy went false on the 4 s failsafe path = the flash again), the pushed page's pre-#898 fade restored (no stage, no paint gate there), and in the BASE class a volatile `pageLoaded` + a re-check after enqueue that POSTS the drain (a pool-side drainer is memory-safe and NOT order-safe). **#904:** `--bubble-gap-inner` 1 → 2 px (dial C's gap, D's corner stays); the tail 8×13 → **7×11** — the handoff's plan was checked against the tree and HELD: the tokens size the box, four absolute `clip-path()` values are the shape, all four redrawn from the master (the generator reproduces it byte for byte at 8×13). Rendered on the built shell, both directions × both themes × NOW/7×11/6×10, subject asserted before every shot (`docs/sheets/session-ab/`); the built shell then measured with nothing injected (box 8×11, offset −7, gaps 2/2/2). ★ The pin is the trap, DERIVED: every path's extent = (tail+1) × tail-h, absolute M/L/Q/Z only, sent mirrors received, RTL swaps. 🟡 7×11 vs 6×10 is Damir's eye. **#905:** the §3 "buildable now" menu re-verified — **iOS-43 ("the cheapest row on the list"), iOS-44, iOS-55, AND-35 and R7 were ALREADY BUILT**; records corrected with evidence; 7 of the 17 rows that list has carried were built when someone checked; the iOS three need an iPhone WALK (none since ~08-27), not a build. Nothing was built from the menu by design — the landscape round wants its own renders. **#906:** the privacy policy is final in FORM and wrong in two places about the APP — §4.8 describes the Mini Apps catalog while installed Mini Apps run in-app (`MiniAppPage:57-60`; the app itself says "Runs securely inside Spixi"), and §4 omits the sender-triggered GIF auto-load; PROPOSED text only (`docs/legal/proposed-amendments-2026-09-19.md`), the policy is untouched. ⚠ I wrote "the media pref has no writer" into that proposal from Session R's STATUS ROW and the tree said otherwise (Session R built the toggle) — caught before it left the session: **a status row records what a session FOUND, not what the tree IS.** ★★ **The pins were again where most findings were: 14 holes across three rounds, all in pins the author had mutation-tested** — a raw-text positive a comment satisfied (#771, a fourth time) · "derived" that matched one declaration form, whose REPAIR matched one formatting (#798 twice) · `==` read as an assignment · "inside the lock" as a brace COUNT · fixed character windows reaching into the next method · resets required "somewhere in the method" instead of inside the lock. ⚠ **And the harness lied once: 18 "kills" were one ENOENT** — its mutation root lacked a file the re-based pin reads; it now asserts a green baseline IN THAT ROOT first (#811 in the instrument). **69 mutations through the REAL pin text, one intended survivor** (tx-vs-chat close order — r2 showed it is not load-bearing and my comment had claimed it was, #772). Closing: bundle byte-identical (no component JS moved) · 18 shells, `chat.html`/`settings.html`/`wallet_sent.html`/`spixi.tokens.css` rebuilt · four `--check` gates ✓ · lint ✓ · `cs-syntax-check` 138 + 1 · **suite in the container on snapshot copies: `4756 → 4763 / the same 2 KNOWN`** (expect 4765 locally). ⚠⚠ **C# CHANGED IN FOUR FILES (`HomePage` · `WalletSentPage` · `SingleChatPage` · `SpixiContentPage`) AND NOTHING HERE COMPILES C# — Damir's F5 is the first compile; a build error is this session's bug.** 🟡 **Damir: the paste in `docs/f5-checklist-session-ab.md` → F5 (never `dotnet build`, #663) + Android → walk AB.1–AB.14 (★ AB.1 tx B masked · ★ AB.5 no chat under the tx detail · ★ AB.11 his tail pick · AB.9 is a measurement) → ONE commit.** ⚠ `git add` the new files (`docs/handoff-2026-09-19b.md` · `docs/f5-checklist-session-ab.md` · `docs/walk-artifact-session-ab.html` · `docs/commit-message-session-ab.txt` · `docs/legal/proposed-amendments-2026-09-19.md` · `docs/sheets/session-ab/`); `handoff-2026-09-19.md` moved to `docs/archive/` (git shows a rename); `_to_delete/` is scratch — delete it. Still LIVE: `docs/diagnostic-session-aa-findings.md` (AA.8 did not reproduce; nothing was fixed). ★★ **SAME EVENING — #907 (Damir, with a screenshot): after a multi-select delete a chat opened with five bubbles and "Show older messages" on EVERY open.** Read in Ixian-Core at `097341a`, not assumed: `Friend.deleteMessage` sets `fm.message = ""` — **a delete is a TOMBSTONE that never leaves storage** — and `loadMessages` counted tombstones toward its 50-row window. The window now counts VISIBLE rows (a widening read, `want + 1` — which also ends the pill that loaded nothing on a chat of exactly 50 — doubling, 8 passes, D-18's step over 100 inside); the "weird position" was a second defect the first exposed (the pill REPLACES the secure notice, whose auto margins were the only thing seating a short log: 680 px of empty canvas above the composer, measured; 4 px after). **An Opus read of that one method (r4) found 2 MAJOR in my first cut** — the predicate knew only `standard` rows while Core blanks ANY type, and the pass cap's VALUE was unpinned (`= 0` opened every conversation EMPTY, green) → `rendersNothing` with two arms, **tied by a pin to `insertMessage`'s own branches**. ⚠ **Not fixable in the app: a deleted CALL card comes back as "No answer"** (an empty call row is how Core stores a missed call) — walk AB.17, BE rows **CORE-9** (real deletion) + **CORE-10** (`handleMsgDelete` checks no authorship: a 1:1 peer can blank MY messages). **#909:** the delete investigation's STORAGE findings logged as cutoff items, nothing built — CORE-11 chat history unencrypted at rest (Damir: "a tough nut"; the row says why) · APP-1 Android backup exclusion (ours, small) · L8 status block (BE signs off) · order: CORE-9 → L8 → APP-1 → CORE-11 → disappearing messages. **#908:** self-destructing messages SCOPED on Damir's question, NOT built — the delete verb is the cheap 10 %; the timer, the carrier and real deletion are the feature, and two of the three are Core's. ★ **The "+2 on Damir's machine" is explained:** with `Ixian-Core` beside the repo the two M1 (#448) gates RUN — the container now has the sibling too (`/home/claude/Ixian-Core` @ `097341a`). **Final suite: 4756 → 4768 / the 2 KNOWN without the sibling, 4770 with it (= his number).** Walk is 17 rows now (AB.15–AB.17); built `chat.html` is 621 chars under the #345 ceiling. **#911 (2026-09-22): WALKED — 15 P · 0 F · 2 N** on Windows F5 + Android Debug; the four C# files compiled. AB.9: the draft SURVIVES the tab-sweep close (hypothesis closed, no flush). AB.11: 7×11 stays. AB.17 is a FINDING: call cards are not selectable BY DESIGN (`isMenuableRec`), so the deleted-call case is unreachable from the UI. **AB.5 NOT walked — open, heads the next walk.** **#912: the easy app-side fixes BUILT** — APP-1 Android (`backup_rules.xml` + `data_extraction_rules.xml`: Chats, MsgQueue, logs out of system backup; wallet/Acc/prefs still in) + iOS (`NSURLIsExcludedFromBackupKey`, UNCOMPILED until an iOS build) · the Windows html copy skips unchanged files (length + write time) · five `[STARTDIAG]` cold-start lines (the startup question answered by a number next launch, not a guess) · Release command in the checklist. Suite 4774 / the 2 KNOWN (4776 with the Core sibling). COMMITTED `bef1be21` (2026-09-22), the post-walk build compiled. **#913: the STARTUP NUMBERS are in** — relaunch: logger up +609 · node +700 · wallet +958 · root page +1244 · home shell loaded +2455 ms; the node boot is 91 ms, so the off-thread boot plan is OFF; the WebView leg (root page → home shell) is 1.21 s of 2.45 and survives a Release build — next: the dev-HUD `rdy` mark splits it. **#914: the three privacy wordings RULED by interview and BAKED** (honest-minimum Mini Apps · GIF auto-load + toggle pointer · deletion as a Retention row; Last updated 22 Sep; 19 420 chars; suite 4774 / 2 KNOWN) — counsel is the remaining step. AB.5 confirmed P → the AB walk is 16 · 0 · 1. NEXT SESSION: read `docs/handoff-2026-09-22.md` FIRST.
- [x] **SESSION AC — THE STARTUP LEVER MEASURED · THE LANDSCAPE ROUND · THE iOS PUSH EXTENSION WRITTEN · ROUTE B PREPARED · THREE OPUS REVIEWERS (2026-09-22, container twin of the VM tree, landed over the device bridge; DECISIONS #916–#926; UNCOMMITTED and UNWALKED — commit is Damir's, `docs/commit-message-session-ac.txt`). NEXT SESSION: read `docs/handoff-2026-09-22b.md` FIRST; paste-ready prompt in `docs/next-session-prompt.md`; the office iPhone day runs on `docs/walk-artifact-ios-office.html` (31 rows).** §0/§1: AB committed (`bef1be21` + `020ba24a`), four `--check` gates green, suite BEFORE `4774 / the 2 KNOWN` on a pristine snapshot with the Core sibling. **#916** = Damir's late addendum folded in (the road to TestFlight: office → freeze → sweep → strip [SHAPED 2026-09-23, #933: ONE branch, one cleanup, three comment tiers, the workshop docs to a private `spixi-workshop` repo, DECISIONS rewritten by area — NOT a merge-branch step] → gate re-run → merge → TestFlight; ⚠ the wrapper is BSD-2-Clause, not MIT). **#917 startup:** §2 said the WebView leg, not the node, and no blind change — measured on the built home shell at ×4: DCL and `load` 4 ms apart (the ready verb stays on `load`, #177); the one JS cost above the noise was the flag-font canvas probe on all 17 shells → `installFlagFontLater()` (after `load` + one frame on a phone, 300 ms belt for a hidden spare; SYNCHRONOUS on desktop — the text-flag paint-then-swap is the L15b flash — and on launch/settings). ⚠ **Measured twice:** the shipped shell's `load` moved −13/−20 ms median at ×4 (`perf-ab-load.txt`); the 440 → 375 first read was an instrumented copy and is corrected in the docblock. The phone's split still needs Damir's `rdy` mark + Release STARTDIAG + the Windows `copyResources` line. **#918 landscape:** AND-31/32/33/34 rendered NOW/AFTER at 915×412 both themes through the wire, subject asserted → ONE media block in `home.html` (`max-height: 500px` AND `orientation: landscape`, phone only — the reviewer's MAJOR: portrait with the keyboard up is under 500 px too), the sheet cap subtracting `--safe-top`/`--kb-inset` in BOTH `overlay.css` and `wallet-receive.css`, the chat-appearance preview floor (⚠ portrait is content-sized now = AC.11 dial; desktop 148 = AC.16); apps keeps its inner scroller (a sticky row inside a scrolling header cannot stick past its box). `chat.html` was 63 chars over the #345 ceiling → headroom 274. **#919 the iOS extension, UNCOMPILED:** `Spixi-PushService/SpixiPushGate.cs` + `NotificationService.cs` to Android `decidePush` parity (gate BEFORE OneSignal; Suppress = rewrite to EMPTY, never cancel; `Passive2`; `[SPUSH]` never prints the address), `Platforms/iOS/SPushPrefsShare.cs` writes the App Group store (lock, temp+move, backup-excluded, refused pre-unlock, cleared on wipe, nicks only when `senderName`), App Group in both Entitlements, `OneSignal_app_groups_key` in both Info.plist, the reference LIVE for ios, no fixed RID. ★ **The dial stated, not hidden:** the payload carries the SENDER only → a muted contact is silent in groups too, a muted GROUP is not silent (iO.11/iO.11b); per-chat = server-side. 16 mutations killed; `cs-syntax-check` walks the extension (182). **#920 route B:** 43 vendored `rocksdb-sharp @ f1cf0ba0` files for maccatalyst ONLY, the one Catalyst patch, LICENSE + `MANIFEST.sha256` (recomputed by the suite), csproj scoping, the README with the vtool line, ⚠ the nuget download line (the TFM no longer restores the package), Release = both slices `lipo`'d, and ⚠ that the July stamp NEVER reached `dlopen` — the first Mac run is that test; never before the iPhone walk. The checker gained the pointer-deref-of-a-cast gap + comment-masked matching (a `Status...Working on` in a comment had convicted a file). ★★ **#921 the loop, three Opus reviewers, TEN MAJOR-class findings, all fixed — then an r2 on Opus over the FIXES: 0 MAJOR · 5 MINOR (all fixed: the extension plist versions were inert · a CS8604 · the #918 pin counted ONE spelling → a walk over every @media · the isRunning check outside the lock + clear() before FriendList.clear() · the checker roots a list → derived from *.csproj) — and the first AFTER run RIGHTLY failed L15 (its data-desktop ban swept the scheduler; re-based to the detection body):** the height-only query (portrait+keyboard) · the desktop text-flag swap · the hidden spare never fires rAF · a pin missing the `loading` branch · `out NSObject` CS8600 · a fixed `ios-arm64` RID breaking simulator builds · the group-mute limit undocumented · OneSignal receipts needing the group key · the store overwritten EMPTY pre-unlock · built outside the lock · nicks written regardless · no backup exclusion / no clear on wipe · the README's "proven to LOAD" · the package no longer restored · Release universal · the csproj pin on RAW XML (an XML comment defeats a sweep — #771 in a fourth costume; now stripped + a walk over every live RocksDB reference incl. the dev's 0.0.42 whitelist) · a fixed 8-line banner count in pin ① (now DERIVED) · the sheet's "Copy results" still headed Session AB. ⚠ **C# changed in six app files + a NEW project + 43 vendored files and NOTHING here compiles any of it; Windows/Android compile none of the new code** — the office iPhone build is the first compile. ★ **#922 — THE ANDROID LANDSCAPE RAIL (Damir's question, then "do the fix on Android"):** Material's rail-for-medium-width vs the HIG's tab bar → gated on a NEW compile-time convention carrier `*SL{SpixiPlatform}` → `data-platform` (never a capability), toggled at RUNTIME by `attachLandscapeRail` on a DEVICE flag (desktop + iOS refuse) — ★ the reviewer's MAJOR-1 changed the design: a landscape phone is TWO-PANE (`HomePage` splits at ≥ 700 dp on every platform), so the home shell's viewport is a ~400 dp column that reads PORTRAIT and the first cut's media query would never have fired; NEW `landscape-runtime.js` reads `screen.orientation`, the #918 rules re-keyed on `:root[data-landscape]`, and Damir's answer to that dial is **#923** — a phone is ONE pane in every posture (`isPhoneDisplay()`: the display's short side < 600 dp at any width; tablets/desktop keep the split; the 600 pinned EQUAL to the shells' constant; 7 mutations killed; `HomePage.xaml.cs` = a fourth C# file, one EVERY platform compiles); the real new item = the SIDE INSETS nobody had published (`MainActivity` → `AndroidInsetLeft/Right` carriers + a `setInsetSides` push every head handles → `--safe-left/right`); rendered on the built shells with the carriers substituted like `generatePage` and the device emulated (rail 72 at x=0; 120 at the real pane geometry with a 48 left inset; iOS control keeps the bar; rotation round-trip rail/bar/rail/bar); 8 pins — every shell head EXECUTED in jsdom, the platform set DERIVED, a jsdom leg with the device stubbed — 17 mutations killed (⚠ three of the first cut only after the mutant root got `docs/legal` — the bundle had silently failed to rebuild, #906 again); `CHAT_KB_CEIL` 645 → 646 (+288 measured), `INDEX_KB_CEIL` 528 → 531, deltas stated; three MORE C# files, Android paths — Damir's Android build is their first compile; walk AC.19–AC.22. ★ **WALKED 2026-09-23: 17 P · 2 F · 1 N/A (#924)** — AC.19 passed (one pane + rail on the phone); the two fails were one cause, AND-7's OWN logged residual: the top inset was a boot carrier and a cutout phone's status bar is ~48 dp portrait / ~24 landscape → pushed LIVE now (`publishTopInset(dip, pushLive)`, latched, fenced); the apps banner returns in landscape as one line beside the search (rendered, the list keeps 276 px); AND-36 closed; re-walked → 19 P. **#925 the startup split, three ways:** Release Android 532 native / **1441 WebView leg** of 1973; Windows 491 / **1469** of 1960; `rdy` 380; a WARM WebView parses the same size in 81 ms → the first WebView's initialisation is the second, serialised after a half-second native boot; the lever is parallelism (#800's shape on the first WebView), a design step with BE, not built. ★★ **#926 the owed loop over #922–#924 (two Opus auditors + r2): 3 MAJOR** — `addCustomString` bumped the dictionary version on every call (four per keyboard round → the localized cache gone, the spare chat re-localizing on the UI thread) · `isPhoneDisplay` on WinUI read the MONITOR (a 1080p laptop at 200 % = a phone, single-pane everywhere) · content under a side-edge nav bar (chat, takeovers) — all fixed, nine MINORs fixed, residuals AND-48, r2 holds, 20 mutations killed; C# in six files. Closing: bundle rebuilt · 18 shells · four `--check` ✓ · lint ✓ · cs-syntax 183 + 3 · **suite AFTER BASELINE OK — 4817 pass / the 2 KNOWN (+43)** (container, with the sibling; compare the delta, #895). ⚠ `git add` the new files (handoff §7); `docs/handoff-2026-09-22.md` → `docs/archive/`; the VM-only `docs/addendum-2026-09-22-late.md` deleted at landing; `crash-logcat.txt` + `mem-*.txt` are TRACKED at the root — `git rm --cached` + gitignore (Damir); `scripts/_*.tmp.mjs` gitignored, never landed.
- [x] **2026-09-23 — THE OURS / HIS SPLIT (#927): Session AC + follow-up committed `b333142c`; Damir asked which cutover rows are really ours — about HALF of what was filed "waiting for BE" is app-side (a verb is ours; only Core's storage or wire is his), incl. C16. Work order for SESSION AD in `docs/handoff-2026-09-23.md` (four batches: the workaround-deleting rows · the ours blockers, security-gated · small verbs · medium) → then the Apple portal steps (#915) → the office (`walk-artifact-ios-office.html`) → the endgame (#916). NEXT SESSION: read `docs/handoff-2026-09-23.md` FIRST; prompt in `docs/next-session-prompt.md`.**
- [x] **SESSION AD — THE OURS ROWS: ELEVEN VERBS AND PUSHES, ONE MEDIA PURGE, FOUR `spixi.*` FAMILIES RETIRED · THREE OPUS LOOP ROUNDS (2026-09-23, container twin of the VM tree, landed over the device bridge; DECISIONS #928–#930; UNCOMMITTED and UNWALKED — commit is Damir's, `docs/commit-message-session-ad.txt`). NEXT SESSION: read `docs/handoff-2026-09-23b.md` FIRST; paste-ready prompt in `docs/next-session-prompt.md`; walk sheet `docs/walk-artifact-session-ad.html` (27 rows).** §0: HEAD `b333142c`, four `--check` gates green, suite BEFORE `4817 / the 2 KNOWN` on a pristine snapshot with the Core sibling. ★ **#928 — batches 1 → 3 → 4 of #927 as far as the verbs reach, ZERO Ixian-Core change, C# in 19 app files + `Spixi/Meta/SChatPrefs.cs` NEW.** Five shell workarounds DELETED because C# now pushes the fact: CH6's locale-carrier reverse map (+14 `*SL{}` carriers) → `addChat` carries the excerpt KIND · S11's `spixi.landtab` handshake → `ixian:landtab:`/`landOnTab` · #274's `spixi.settings.view` stash · C16's `spixi.exdel.*` hint (★ **premise REFUTED at Core**: `handleMsgDelete` persists the remote delete; the gap was the LOCAL delete's list row → `refreshChatRow` both paths) · S2's `spixi.backup.last` stamp → `setLastBackup` push, C# gates the nudge. **W11** — the payment response used to be DROPPED with no chat page open; stored + written before the gate now, the row found by the payer side's OWN rule, txid parsed before storing. C1/C2 `addPaymentRequest` +4 (kind · enum · fiat · `insufficient` — FALSE until a balance is `verified`, fee excluded) · S9 `ixian:dev` under `devMode` (DevPage presented by the IN-STACK HomePage) · CH4 favorites = an app PREFERENCE + the OS badge mute-aware at 3 sites · C6 `tip:<amount>` + total · C21 typist · CH8 reactor · CI2 · C4 `callActive` · CO3/CO4 · C17/CO1 `addContact` +2 + `ixian:undorequest` on ContactDetails (the relation's "pending" set) · A4 one 15 s deadline + `showUrlError(reason)` · i18n-C# · **CH3 the media purge** (`:media` suffix; READ-ONLY listing BEFORE via its own raw history parser — `getMessages` re-arms outgoing transfers; `purgeFiles` AFTER success, off-thread, spares shared paths, re-checks the root; `isInsideDownloadsRoot` fail-closed). Sweep: `[STARTDIAG]` retired · dead `getDetailContent` branch · #490's dead iOS body. NOT built, recorded: A3 · A5b · CO5 · CH3 mark-read · C10 (Core already refuses the duplicate id) · batch 4's CI6/C22/A8/FC1/C12/NT1(b)/PV1 NOT REACHED. OUT by order: L8 · L2/MAJOR #8 · CORE-10 (his) · L6 (own mini-session). ★★ **#929 THE LOOP, ON OPUS, THREE ROUNDS — 3 · 2 · 0 MAJOR, CLEAN NOT CLAIMED (no fourth reader over the r3 fixes):** r1 W11's lookup PEER-WRITABLE by id (any message with that id — a peer could rewrite a row it never requested, on disk) · S9's DevPage NEVER SHOWED (`PushModalAsync` from an overlay-presented page is a silent no-op) · C17 dead on a legacy Unknown-state contact; r2 CH3's other-contact walk ON the UI thread + a read failure deleting a shared file · the re-root accepting ANY recorded parent; r3 the re-root trusting a parent NAME whose LEAF the peer controls while S16 is open (`hasDotSegment` on the raw path) + **six one-token mutations left green in pins the author had mutation-tested** — W11's predicate matched each clause as a SUBSTRING (`x.localSender == false` and a trailing `|| true` both passed) → compared WHOLE against a canonical string · the txid validation guardable · CH3's owner skip matched inside `!other…` · the re-root gate matched un-negated · C17's success arm `status = "fail"` · C2's `anyVerified = true` — and **#574 ①'s pin went RED on this batch's own `// CH4` comment** (a 600-char RAW window, #771 in a fifth costume) → branch-sliced property. 13 r3 mutations through the real pin text, zero survivors; ~20 r1 pin findings incl. Session AD's own pins spelled FROM THE AUTHOR'S LIST (#798). **#930 dials:** the cancel-records-a-backup stamp · fee out of `insufficient` · a favorite survives a Core-side removal · the purge's orphan residual · `mutechat`'s token log is PRE-EXISTING (security-review NIT) · Core's `removeFriend` logs the address (NIT). Docs: gate section (Session AD) · security-review addenda (W11 fixed · two inherited NITs · CH3) · ARCHITECTURE §4 · be-cutover 22 rows → LANDED with anchors · release-readiness (W11/C16 moved, L6 named) · verdict §6 in the brief. Closing: **bundle rebuilt · 18 shells · four `--check` ✓ · lint ✓ · cs-syntax 184 + 3 · suite AFTER BASELINE OK — 4848 / the 2 KNOWN (+31)** (container, with the sibling; compare the delta, #895). ⚠⚠ **NOTHING HERE COMPILED ANY C# — 19 files + one NEW; Damir's F5 + Android Debug are the first compile; a build error is this session's bug.** 🟡 **Damir: FULL pipeline (`extract-strings` → locales → iife → bundle → shells → smoke) → wipe `obj`/`bin` → F5 (never `dotnet build`, #663) + Android → walk AD.0–AD.25 (★ AD.4 W11 with no chat open · AD.10 S9 from the pane · AD.19 C17 on a legacy-state contact · AD.22/AD.23 the purge — the ONLY rows that delete files; AD.0b = AC.3 still owed) → ONE commit.** ⚠ `git add` the new files (handoff §7); `docs/handoff-2026-09-23.md` → `docs/archive/` (git shows a rename); `scripts/_*.tmp.mjs` gitignored, never landed; `_to_delete/` is scratch. **Landed with it, from the parallel session's addendum (its file → `_to_delete/`): DECISIONS #931 (nine dials closed by interview — #82/W7/CI4/C14/C20 ledger rows updated; ASD-STE100 scoped to chat replies; S1 closed) · #932 (★ the Apple portal steps DONE — the office is next, no portal step remains) · #933 (the strip SHAPED: one branch, three tiers, workshop repo) · #934 (the invite-picker later note + the bot-room Delete BUG for the next FE round).**
- [x] **2026-09-23 evening — Session AD WALKED 22 · 2 · 3 and COMMITTED `a664d59f`; SESSION AE ORDERED (#937): the last FE fix round before the office — the Windows Debug freeze (diagnosis first) · the ⊕ attach sheet opening partially on Android · ★ NEW: messages fade BEHIND the composer the Telegram way (at the composer's level, not above — #892/#893 bracket it; render first, Damir picks) · the bot-room Delete gate (#934 b) · AD.13 + the re-walk rows. #936 = the dapp file-offer UX, later version. NEXT SESSION: read `docs/handoff-2026-09-23c.md` FIRST; prompt in `docs/next-session-prompt.md`.**
- [x] **2026-09-24 — SESSION AE LANDED + COMMITTED `c0711262` (from the delivery tarball, 61/61 SHA256 OK, rebuild byte-identical) and WALKED 13 · 3 · 3 (#945) · #944 BUILT the same day: the group/bot-room chat-list excerpt names its sender ("George: hi there") — C# `getFriendMessageHelper` `out excerptSender` (rooms only, never own/event/typing) + `resolveExcerptSender` (the bubble's own name order) as a 12th `addChat` arg; shell `excerptFor` shortens an address (#211); the component leads with the sender, ellipsizing name + colon outside. 10 pins, 4 C# mutations killed; UNWALKED. #945 logs the AE fails + Damir's new asks for SESSION AF: remove "Cancel request" from contact details/chat info (+ the `receiveData:1260` NRE → CORE-13 sibling) · member-sheet admin buttons (no `.c-member__actions` rule exists) · no hairlines inside a group (Account + bot roster) · duplicate-tip "Unknown error" · the reaction pill at the composer band (lowest). NEXT SESSION: read `docs/handoff-2026-09-24.md` FIRST; prompt in `docs/next-session-prompt.md`. The office day follows AF's walk (`docs/handoff-2026-09-23d.md` §3).**
- [x] **SESSION AF — THE OWED REVIEW (AE r4 CLEAN · #944 NOT CLEAN → 7 Opus rounds → CLEAN) · THE FOUR AE-WALK FIXES · A 10-ROUND LOOP OVER THE BUILD (2026-09-24, container twin of the PC tree, landed over the device bridge; DECISIONS #946–#961; UNCOMMITTED and UNWALKED — commit is Damir's, `docs/commit-message-session-af.txt`). NEXT SESSION: read `docs/handoff-2026-09-24b.md` FIRST; prompt in `docs/next-session-prompt.md` (the office day, after AF's walk).** ★ **#946:** #944 introduced an address leak — in a BLIND room `resolveExcerptSender` fell back to the member's address (the owner's device holds the REAL one) where the bubble says "Hidden member" → `""` under `Utils.hidesParticipants`; #944 had also never run the full suite (four CH6 pins RED → re-based by position). The renderLogNow order rule is now pinned by EXECUTING the built chat shell. **Build:** ③ #947 Cancel request removed from the pending profile + ContactDetails' `ixian:undorequest` deleted (CORE-13 sibling `receiveData:1260` logged) · ④ #948 the admin-button rule lived in chat-info.css, which chat.html does not load → member-sheet.css, stacked · ⑤ #949 no in-card hairlines (Account hub, roster) · ② #950 a second tip refused BEFORE any payment dialog, true message, 13 lang files · ① #951 rendered, Damir picks. **#952–#961:** ten Opus rounds over the build, 0 MAJOR, every finding a record or a pin's words; design calls #953/#959 (a pin claims only what text proves; the walk carries the render/behaviour). Suite (container, sibling): BEFORE 4868 + 4 red → **AFTER 4880 / the 2 KNOWN**; cs-syntax 184 + 3. C# changed: HomePage · SingleChatPage · ContactDetails + lang/*.txt. Walk sheet `docs/walk-artifact-session-af.html` (23 rows, also an Artifact). ⚠ A stale splice range makes every mutation look green — recompute and assert the pin printed.
