# F5 checklist — batch #383 (N12 restore-nudge · N40 connectivity)

**LANGUAGE RULE: ASD-STE100 Simplified Technical English.** See `CLAUDE.md`.

⚠ **C# changed** (`HomePage.xaml.cs` · `LaunchRestorePage.xaml.cs` ·
`LaunchCreatePage.xaml.cs`) → **wipe obj/bin before the build.**
Artifacts are already rebuilt in this batch (bundle 262 exports · 22 shells ·
smoke **1971 / the same 4**). Rebuild locally only to confirm.

```
Remove-Item -Recurse -Force .\Spixi\obj, .\Spixi\bin -ErrorAction SilentlyContinue
node scripts/build-demo-bundle.mjs      # components changed → bundle BEFORE shells
node scripts/build-shells.mjs
node scripts/smoke-test.mjs             # expect 1971 / the same 4
```

Only the legs this batch touched are listed.

---

## A · N12 — a restore must not ask for a backup

| # | Do | Expect |
|---|---|---|
| A1 | Restore an account from a backup file | After the wallet loads, onboarding opens **on "Join the community"**. The "One file protects everything" backup step is **not shown** |
| A2 | Finish onboarding (Join, or "Not now") | Normal home screen. No backup sheet appears in the following minutes |
| A3 | Open Account → the Backup row | The standing row is unchanged and still works — the nudge is skipped, the feature is not |
| A4 | **Create** a NEW account (not a restore) | Onboarding still opens on the **backup step** first, then Join. Nothing changed for created accounts |
| A5 | Start a restore, mistype the password, back out, then **create** an account | The created account behaves exactly as A4 — the abandoned restore leaves no mark (review MINOR-2) |

## B · N40 — "Connecting…" and the update notice

The prime suspect needs an update to be advertised. **Check first, from a browser:**
open `https://resources.ixian.io/spixi-update.txt`. If it serves a version above
`0.9.22`, B1 is testable today; if not, B1 needs a build with a lower version.

| # | Do | Expect |
|---|---|---|
| B1 | With an update advertised, turn the network off and wait ~5 s, on the Chats tab | **Both** show: the title becomes **"Connecting…"** AND the update banner stays. Before this batch the title never appeared |
| B2 | Tap the **✕** on the update banner | The banner collapses. It does **not** come back while the app runs |
| B3 | Restart the app (update still advertised) | The banner is **back** — the dismissal is per-session by design (your dial) |
| B4 | Turn the network back on | "Connecting…" clears within ~2 s. The banner is unaffected |
| B5 | Off/on again a few times | The title tracks the state every time. It must never stick in either position |
| B6 | Go offline, then leave the app running for well over an hour | "Connecting…" is still shown. This is the N40 symptom itself |
| B7 | Tab to Wallet and Apps while offline | Those titles show the connecting state too (#322, unchanged) |
| B8 | Keyboard/Tab across the Chats screen with **no** banner showing | Tab must **not** land on an invisible Dismiss control (review MINOR-1) |
| B9 | Switch the language to Deutsch while offline | "Connecting…" survives the re-bake (D-20/#357, must not regress) |

## C · No-regression spot checks

| # | Do | Expect |
|---|---|---|
| C1 | Open any chat while offline | The chat's own connecting state still shows in the topbar sub. Its banner is untouched by this batch |
| C2 | Open a chat, send a message | Normal. No money, no bridge verb changed in this batch |

---

**If B1 fails,** report which half is missing (title, banner, or both) and whether
the update banner is on screen — that difference names the remaining mechanism.
The ranked alternatives and the 5-step device protocol are in
`docs/n40-triage-connecting.md` §3–§4.
