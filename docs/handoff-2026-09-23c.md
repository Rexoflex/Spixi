# Handoff — 2026-09-23 (c) → Session AE: the FE fix round, then the office

**Read this first (#215).** Then `docs/handoff-2026-09-23b.md` (Session AD's own record: the loop's lessons in
§2 and §5b, the walk in §3 — do not skip §5b), then DECISIONS #928–#937. This file supersedes 09-23b as
the entry point; 09-23b goes to `docs/archive/` when this round's records are written.

## 0 · State of the tree you inherit

* Session AD is COMMITTED as `a664d59f` (94 files, the ours rows). Session AD was WALKED the same evening:
  **22 P · 2 F · 3 N/A (#935)** — Windows F5 + Android Debug, so its 19 C# files have compiled on both.
* Damir's post-walk docs (DECISIONS #935–#937, the AD commit message's walk line, 09-23b, this handoff,
  the prompt, `docs/sheets/session-ae/`) ride his next docs commit — if `git status` shows them
  uncommitted, that commit is item 0.
* Suite baseline: **4848 / the 2 KNOWN** in the container with the `Ixian-Core` sibling @ `097341a`
  (compare deltas in ONE environment, #895). Four `--check` gates green. `cs-syntax-check` 184 + 3 gaps.
* The iOS-only code (#912 ②, #919 the push extension, #920 Catalyst vendoring, #490's iOS line) has
  still never compiled — the office is its first compile. The Apple portal is DONE (#932); the two
  development profiles are in Damir's inbox.

## 1 · What this round is (Damir, 2026-09-23 evening)

The last fix round before the office. Everything below is ours; nothing new beyond it (his rule:
"if we keep adding every little thing, we never get to test mode"). In this order:

1. **★ THE FREEZE (#935) — a diagnosis before any code.** Windows Debug froze after remove-contact →
   the group-participant prompt → leave-group WITH "delete media". Get from Damir: the `ixian.log` tail
   (a `leavegroup: media purge deleted n of m` line means the purge finished and the freeze is
   elsewhere; no `leavegroup` line = it never returned) and the VS **Break All** UI-thread stack.
   Candidates in `HomePage.onLeaveGroupFor`, in run order: the SYNCHRONOUS `collectReceivedMedia`
   listing on the UI thread (every `.ixi` of the friend) · `SContacts.leaveGroup`'s fan-out send under
   Core's locks (#797 class) · `popPageAsync` on the removed group's pane. If the listing: list → op →
   purge as ONE background job, the op marshalled back. C#, ours, full loop.
2. **The ⊕ attach sheet opens only PARTIALLY on Android** (Damir: "the fix for sheet attach not opening
   fully on Android") — the tile row is cut at the viewport bottom, the second row hidden, until the
   keyboard has been opened once. INSTRUMENT first: log `--kb-inset` (and `--safe-top`, the viewport
   height) at sheet open; the suspect is the #918 cap `max-height: calc(100% - --safe-top - --kb-inset)`
   + `bottom: var(--kb-inset)` (`overlay.css:73-77`) reading a STALE `--kb-inset` before the first
   keyboard round (#770 class — a persisted or never-initialised inset). No CSS before the number.
   Shell-only. Walk: fresh install → open a chat → ⊕ before any keyboard → both tile rows visible.
3. **★ NEW — messages fade BEHIND the composer, the Telegram way** (Damir, with two Telegram
   screenshots: `docs/sheets/session-ae/reference-telegram-composer-fade-{light,dark}.png`). His words:
   *"an opacity screen for messages going behind the composer, same as Telegram has — the opacity drops
   at the SAME LEVEL as the composer, not above the composer."* Read the history before designing:
   #889/#892 the composer GLASS band was built and REJECTED ("ugly, a rectangle and cut off"); #893 a
   fade ABOVE the pill was rendered and DECLINED (AA.7). This is a third, different thing: the fade
   zone IS the composer's own band — a bubble is fully opaque until its top edge reaches the composer's
   top edge, then dissolves across the composer's height, so what is behind the composer reads as a
   dimmed continuation, not a hard cut and not a haze above it. Shape: a `mask-image` on the message
   scroller whose stops are DERIVED from `--composer-h` (opaque to the composer's top, to ~0 at its
   bottom), the composer's own background at partial alpha on the canvas so the dissolving bubble is
   faintly visible through it (a tint, NOT a blur — the blur is what #892 retired). Constraints:
   `#messages` already pads by `--composer-h`, so at rest the newest message is never under the composer
   — the effect shows only when scrolled up (assert that in the render, #811: a nine-message chat proved
   nothing in #893); the day pill is sticky above the band and must stay crisp; the keyboard round
   (`--kb-inset`) moves the band — the mask must follow; desktop gets the same (Telegram Desktop does);
   both themes; `chat.html` + `composer.css` only, no C#. RENDER FIRST on the built shell, a long
   conversation scrolled mid-way, both themes, NOW/AFTER, subject asserted; Damir picks from the sheet
   before it is pinned. Pin: the mask stops derived from the composer token, the composer alpha < 1,
   the pill unmasked.
4. **#934 (b) the bot-room Delete gate** — non-admins see Delete only on their OWN messages, admins on
   all; the select-mode bulk delete under the same predicate (`mode.isBot` + `mode.admin` are in the
   shell; `message-menu.js:132` adds Delete unconditionally). Shell-only. Walk: bot room as admin and as
   non-admin.
5. **AD.13 — a second tip refused**: ask Damir which device could not tip and the alert text BEFORE any
   code; the shell has no gate (`message-menu.js` offers Tip unconditionally, `chat.html` sums every
   `tip:` token), so the refusal is on the send side (C# `contextAction:tip`, the native confirm, or the
   wallet).
6. **Re-walk rows** on the same sheet: AD.19 (C17 on a legacy-state contact — Damir: "remind me"),
   AD.20 (A4's three reasons), AD.23 (the shared-file purge case).
7. NOT ours: **AD.14 = CORE-12** (group typing fan-out; the app half is built and waits).

One #46 loop on Opus over the round; mutation-kill every new pin; the pins around W11 / CH3 / C17 / C2
are EXACT-FORM (#929 r3) — a fix there goes red, re-base to the PROPERTY. One walk sheet
(`docs/walk-artifact-session-ae.html`), Damir walks once on Windows + Android.

## 2 · Then, unchanged

* **The office iPhone day** — `docs/walk-artifact-ios-office.html` (31 rows; the portal rows are DONE,
  #932): install the two profiles → wipe obj/bin → build for iPhone (the first compile of the extension
  and the iOS-only files) → the walk, with **iO.7 the mute test** as the one open question → optional
  route B (#920) last.
* **The endgame** (#916 as shaped by #933): L6 (its own mini-session, security-gated) → freeze
  (`maxLogCount 5 → 1`, retire the instruments) → sweep (`docs/audit-refactor-plan.md` phases 1–4) → the
  strip (ONE branch, three comment tiers, the workshop docs to a private `spixi-workshop` repo,
  DECISIONS rewritten by area, a `pre-strip` tag) → the introduced-vs-inherited gate re-run → merge →
  TestFlight (the extension's DISTRIBUTION profile at that point).
* Their own sessions: batch 4's medium rows (CI6 · C22 · A8 · FC1 · C12 · NT1(b) · PV1) · the Android
  memory kill · the translator pass · the comment-strip regexes.
* LATER VERSIONS (decided, not for v0.9.30): #934 (a) the invite-picker pointer · #936 the dapp
  file-offer UX (three options; Damir's open answer: how does the dapp use the file today?) · C14 link
  previews · C20 mini-app sessions · the wallet-password work (L8, L2) · CORE-10 and the Core rows.

## 3 · Open questions for Damir (asked in chat; answers go into the rows)

* The freeze: the `ixian.log` tail and the Break-All stack (item 1).
* AD.13: which device refused the second tip, and the alert's text (item 5).
* AD.19: a legacy-state contact to test C17 against (item 6).
* The fade: his pick from the render sheet before it is pinned (item 3).

## 4 · Still open, unchanged (do not re-derive)

AND-47 (Release logging `Arg_PlatformNotSupported`, verify-first in Core's `Logging.cs`) · AND-48
(side-inset residuals — render first) · the Android 30-minute memory kill · `docs/diagnostic-session-aa-
findings.md` (armed) · the 123 naive comment-strip regexes · the translator pass · counsel on the privacy
policy · S16 (BE) · the #930 dials · CORE-12 (new, group typing fan-out).

## 5 · The workshop

As 09-23b §5: `device_bash` works; `git --no-optional-locks status` only; the suite runs in the container
on a SNAPSHOT copy with the Core sibling (~5 min); land by tar → `_to_delete/` → `tar --overwrite` →
`sha256sum -c`; a file edited DIRECTLY on the VM is staged back before the next tar; **if another session
is running on the folder, land NEW files only** — this session's #935 (dapp UX) row was CLOBBERED by a
concurrent landing and had to be re-appended as #936 (a lesson, not a complaint: two sessions, one file).
Scratch runners `scripts/_*.tmp.mjs` are gitignored. Damir owes locally: `git rm --cached crash-logcat.txt
mem-*.txt` + gitignore; delete `_to_delete/`.

## 6 · Rules this round inherits

#215 · #294 · #660 · #663 · #771 · #798 · #811 · #882 ③ · #895 · #906 · #926 · #927 · #929 ·
★ #935: a walk row that fails is a question first (which device, what text, what stack) and a fix
  second — three of this round's items are questions to Damir before they are code ·
★ #892/#893 → item 3: a rejected dial is a boundary, not a veto — read what was rejected and why
  before building the neighbour.

## 7 · Files

NEW: this handoff · `docs/sheets/session-ae/reference-telegram-composer-fade-{light,dark}.png` ·
DECISIONS #936 (dapp UX, re-appended) · #937 (this round's order + the fade ask) · the prompt rewritten.
Consumed when this round's records are written → `docs/archive/`: `handoff-2026-09-23b.md`,
`f5-checklist-session-ad.md`, `walk-artifact-session-ad.html`, `commit-message-session-ad.txt`.
LIVE: `docs/walk-artifact-ios-office.html` · `docs/diagnostic-session-aa-findings.md`.
