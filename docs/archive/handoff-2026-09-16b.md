# Handoff — 2026-09-16 (b), after Session V

Records: `DECISIONS.md` **#862–#864** (527 rows). Walk sheet + the ONE paste:
`docs/f5-checklist-session-v.md`. Commit message: `docs/commit-message-session-v.txt`.
Read: `docs/contact-details-in-shell-read.md`.

#860 still holds: `device_bash` fails; stage / commit / list-dir work. Nothing in this session
was run on Damir's machine. Everything WAS run in the container on a bundle built there
(`build-demo-bundle` with the legal bake stubbed) — see "verified how" per item.

---

## What landed (uncommitted, on disk)

| file | what | verified how |
|---|---|---|
| `scripts/generate-icons.mjs` | `--check` · every spelling of ink → currentColor · warning reads every paint value | run in the container: stale tree → exit 1 "76 changed · 4 added"; after regen → green; hostile asset → still rejected before any write; gate-21 pins 6/6 |
| `scripts/smoke-test.mjs` | **GATE 63** (a–e) · **GATE 54 (a) re-based in both halves** + (a3)(a4)(a5) · **#345** INDEX_KB_CEIL 516 → 528 | GATE 63: 6 mutations / 6 kills. GATE 54: 8/8 on the fresh bundle, a0/a/a3/a4 red on the committed bundle |
| `src/components/contacts-shell.js` | `createAddContactSheet` (+ `import { createSheet }`) | 13/13 behavioural harness on the ESM sources; 15 source read-sites re-evaluated green |
| `src/styles/components/contacts-shell.css` | the sheet's content styles (tokens only) | — eyeball on device (walk §1) |
| `src/bridge/contacts-page.js` | the chooser in front of `mountAddContact`; `isOverlayOpen` re-entrancy; takeover close drops the sheet | same harness; handle-only guard mutation killed |
| `src/strings/draft/*.json` (12) | the five chooser strings, drafted | `build-locales --check` in the container: not flagged |
| `DECISIONS.md` | #862 #863 #864 | appended in the container, prefix byte-identical |
| `docs/…` | this file · `f5-checklist-session-v.md` · `contact-details-in-shell-read.md` · `commit-message-session-v.txt` · `next-session-prompt.md` (rewritten) | — |

**Done by Damir's pastes:** the deletes, the Figma PNG, the re-export, the regenerated registry, strings, locales, bundle and shells.

## Round 2 — what Damir's first run found, fixed on disk

* **GATE 51 (a)** red on the five chooser strings — no drafts. Drafted in all twelve `src/strings/draft/*.json` now (the gate's rule: no allow-list). Verified in the container: `build-locales --check` no longer names them.
* **#345** red — index.html 538 677 chars vs 516 KB. 3 276 is the chooser CSS, ~7 000 pre-existing (Session U growth, pin unrun since). Ceiling 516 → 528 with both deltas stated.
* **GATE 63 (e)** red only because `git rm` had not run. Step 1 again.
* The paste used `&&` and `git`, and Damir's PowerShell has neither — one cmdlet per line, `Remove-Item` / `Move-Item` for the tree changes.

## ★ Three things the measurement changed (#294) — read before touching the icons

1. **No SVG carries `stroke-width`.** Figma outlines the strokes. GATE 63 (c) derives the weight from the round-cap quarter-circle radius instead; the expected value is the mode of the set (1.50 today), never a literal.
2. **The re-export says `fill="black"`.** The old generator would have shipped 79 black-in-dark-mode glyphs silently; `checks-l` already ships that way. Fixed in the generator, pinned in GATE 63 (d). **If any glyph is black in dark mode after the paste, `generate-icons` did not run.**
3. **`apps.svg` / `messages.svg` are dead**, not shadowing — byte-identical to their `-filled` twins, skipped by the prefix rule.

## Open on Damir

* **The icons are CLOSED.** The frame at 11735:1277 is now the single source: Damir re-exported all 82 at 1.50 (the read-only flag on three files blocked the first export — `IsReadOnly = $false` fixed it), added `message-plus phone-end alert-small layout-grid` (+ `phone-done user-pentagon`, unused but harmless) to the frame, and the three dead files (`checks-l phone-done user-pentagon`) plus `apps.svg`/`messages.svg` are deleted. The second Figma export went to the wrong folder; the four were pulled by URL instead. ⚠ Confirm the final run said BASELINE OK before treating this as done — Session W asks for the line.
* **The walk results** (§5.1 / §5.5 of Session U's sheet) were never reported. Still the first thing to ask.
* **`CHAT_SPARE_ENABLED = false`** — still unrun, still the highest value per minute, and it now also decides whether the contact-details batch (#864) is a treatment or a nicety.
* **Contact details in the shell (#864)** — read done; it is a C# batch (9 verbs + 19 pushes re-targeted + 9 handover rows) with two forks (chat header keeps the page; desktop panes keep the page) and a takeover-subscreen shape, not a `c-sheet`. Needs Claude Code + F5. Damir's call after the spare experiment.

## Still waiting (unchanged from the 09-16 handoff)

Two removable items (#857/#858) · AND-40 · AND-36 / AND-39 · the freeze (#825) last · the BE cutover.

## Consumed — archive (in the paste)

`handoff-2026-09-09.md` · `handoff-2026-09-09b.md` · `f5-checklist-session-t.md` ·
`commit-message-session-t.txt` · `commit-message-session-s.txt` · `walk-artifact-session-t2.html`.
`handoff-2026-09-16.md` is superseded by this file once the paste has run.
