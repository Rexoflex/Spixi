# HANDOFF — after session A, 2026-08-30

⚠ **SUPERSEDED BY `docs/handoff-2026-08-31.md`.** Session B has run. Read the newer file
first. This one is kept because it holds the session A record, and because one of its rows
was wrong for three handoffs and is marked below rather than deleted.

Session A shipped **L1 · L2 · L8** plus a long device round with Damir. Decisions
**#642–#656**. He walked it on Android and Windows and signed it off:
*"all seems to be good."*

## The numbers

```
bundle 299 · shells 18 · smoke BASELINE OK 3496 / the 3 known (#136 · M5 · B3)
· locales CLEAN 775 · cs-syntax 140+1 · Ixian-Core 097341a, clean
```

⚠ **Four moved from the pre-session baseline** and all four are deliberate: cs-syntax
143→140 (three C# pages deleted), locales 772→775 (three copy keys), smoke 3402→3496,
bundle and shells unchanged.

## What actually happened, in one paragraph

The batch was gated green at 3453 pins and was **badly broken**. Damir asked whether the
#46 adversarial loop had run — it had not, the same omission #512 recorded on the previous
batch. He chose to run it before walking. It found **seven MAJORs**; the break-my-verdict
pass over the fixes found **two more inside the repairs**; his device round found four more
after that. The worst: contact-details **Pay and Request did nothing at all**, after
deleting the native screens they replaced.

★ **The loop is not optional, and this session is the evidence.** Read #646 and #647.

## The three pin shapes that failed, and what replaced them

* **A pin that reads a CALL SITE proves the call site** and says nothing about whether the
  callee runs. The batch now ships an **executing** pin that lifts the shipped guard and
  the shipped initial state out of the built file and runs them together.
* **A pin that asserts a PUSH proves the push** — two derivations were being computed for
  shell handlers that discard their arguments. Those are paired negatives now.
* **`cs-syntax-check` PARSES, it does not COMPILE (#593).** A `private` member nested
  inside a method body parsed clean and broke Damir's build. `/tmp/depthcheck.mjs`-style
  member-context checking is the gate that catches it — **fold it into cs-syntax-check.**

## Owed, and who owes it

| | |
|---|---|
| ~~**The #46 loop over #507–#511**~~ | ⚠ **SUPERSEDED 2026-08-30 — THIS ROW WAS WRONG, AND IT WAS WRONG IN THREE HANDOFFS.** The loop is NOT owed. **It ran on 2026-08-22, and DECISIONS #515 records it CLEAN after two rounds.** The artifacts are committed in `e1237928` and the fixes are in the tree — `popOwnModal` at `LockPage.xaml.cs:558`, the cascade-aware `rulesFor` helper. The only thing missing was the VERDICT in §6 of `docs/opus-review-brief-507-511.md`, which read "(append here)". Each handoff read that empty section, concluded the loop was owed, and copied the row forward. ★ **A verdict that is not written back into its own brief is a verdict nobody can find.** The verdict is written into §6 now. See DECISIONS **#660**. The original text follows, kept because this project marks superseded claims: *"STILL owed — `docs/opus-review-brief-507-511.md`, no verdict appended. Two batches deep now."* |
| ~~**The `[RCPT]` probe**~~ | ✅ **DONE in session B (#661).** Removed from `StreamProcessor`, and `SingleChatPage.getSelectedChannel()` went with it — the probe was its only caller. A negative sweep pins both names absent. |
| **The `[CDPERF]` probe** | keep until L10 re-measures, then remove (worklist). |
| **L12** — bot-room kick/ban | Damir's, needs an admin account. #637 is not done. |
| **CORE-1 · CORE-2 · CORE-3** | Ixian-Core, frozen. BE engineer. CORE-3 is new this session. |
| **The nine iOS rows** | office Mac, outside the three-session plan. ⚠ A1 (wallet password in cleartext Preferences) is a real launch blocker — no `SecureStorage` anywhere in the tree. |

## Damir's own list, reconciled into the queue

Everything he listed already had a row. Two changed shape:

* **L7 is RULED: remove it.** *"we decided to remove the mark as read from chat row menu,
  no need to force it."* That closes the dial that would have decided session B's size.
* **L9 changed scope.** He is no longer asking "where does a slide-in earn its place" but
  proposing one grammar: **sheets rise from the bottom, subscreens come from the right**,
  everywhere on mobile. He wants **fable** to take L9 together with **L3** (swipe-back),
  because a slide from the right and a swipe back to the right are one gesture read two
  ways. ⚠ He asked *"does it make sense"* — answer that before building. It does, with one
  caveat: a surface that is a PANE on desktop must not slide (#328).
* His ACCOUNT bullets are **L6** verbatim. Welcome/back and the dark launch sheets are
  **L4** and **L5**.

## What session B looks like now

**L6 · L7 · L5 · L11 · L10.** L7 shrank to a deletion. ⚠ #294 still applies to L6: it
carries three symptoms (rail, right pane, mobile flicker) that may or may not share a
cause — **measure the flicker before assuming**. L10 ends by removing the `[CDPERF]` probe.

Session C is **L3 + L9 + L4** and the flicker rows, with fable on the gesture work.
