# Handoff — 2026-08-12 (the fix session lands) — READ FIRST

Overnight (2026-08-11→12, cloud clone of `be7657a0`) the whole fix-session plate
was built + #46-looped. **Nothing is on the Mac yet** — the session lands it via
the bridge the moment the Mac opens. State: DECISIONS **#334** (the build) +
**#335** (the loop verdict) · smoke **BASELINE OK 1031 / the same 4** · lint ✓ ·
pseudo 9/9 · bundle 238 exports · 22 shells.

## Morning protocol (in order)

1. **Open the Mac** → the session lands every changed file (manifest below,
   hash-verified per file) → I re-run the generators ON the Mac
   (`build-locales` → `build-strings-iife` → `build-demo-bundle` →
   `build-shells` — bundle BEFORE shells) → you run `node scripts/smoke-test.mjs`
   locally → expect **BASELINE OK 1031/same-4** (the smoke summary is now
   baseline-honest: red FAILED = a REAL problem, always).
2. **Android deploy #1 (the probe build):** `rm -rf Spixi/obj/Debug/net10.0-android
   Spixi/bin/Debug/net10.0-android` → `dotnet build Spixi/Spixi.csproj -f
   net10.0-android -c Debug -t:Run` (C# changed — the wipe is LAW).
3. **FIRST: the kb-probe run (blocks the AND-20 lever).** Open any chat on the
   Samsung → a green KB-PROBE overlay sits at the top. (a) Tap the composer,
   wait for the keyboard, **screenshot**; (b) tap elsewhere/close the keyboard,
   **screenshot**; send both here. I design + build the AND-20 lever + AND-7
   edge-to-edge from the numbers while you F5 the rest.
4. **iOS build too** (C# changed): obj/bin wipe → plain build → `-t:Run`.
5. **F5 checklist** (§below). 6. I fold your findings + the lever into the batch
   → full pipeline re-run → **ONE commit** (message at the bottom).

## F5 checklist (Samsung unless marked)

**Chrome/visual:** status+nav bar strips = surface color, dark icons on light
theme (flip theme in Account → bars follow; flip the OS theme with app open →
bars follow too) · cold start = BLUE splash (not black) · a DisplayAlert (e.g.
delete-history confirm) = navy accent, no pink · Account open/close = less/no
white flash (report what remains — the Opacity-staging half is decision-gated
on your eyes) · notification for a chat message while backgrounded = white
Spixi silhouette on navy accent (not a blob) · have someone CALL you while
backgrounded = "Incoming call" notification (own channel, heads-up).
**Interactions:** tap search in chats, type nothing, scroll → header rides away
(AND-23); type a query → it pins · switch tabs → no stale highlight residue
(AND-18) · scan a contact QR → undistorted preview + decodes (AND-22) · locked
Spixi: send a file → NO unlock prompt on returning from the picker (AND-21) ·
first-ever call on a fresh install: mic prompt at RING time; Accept after
granting connects (AND-11) · bot group: send a contact request from a member
sheet → a "Request sent" row appears in the chats list + Requests chip
IMMEDIATELY (AND-17); a second request to the SAME person → "already exists"
alert, not silence · send a file to someone who doesn't accept → the bubble
shows **Cancel**; tap it → offer gone; peer-side row disappears live (reopen
resurrect = known C16, logged).
**Both platforms:** chat bubbles noticeably wider (82%/360) · contact details →
tap a real avatar photo → full-size viewer (gradient avatars: not tappable) ·
Account → sub-screens hide the bottom nav; hub shows it (iOS-59) · Account tab
→ chats unread badge visible on the nav (iOS-61) · open a chat with a file
transfer mid-list → lands AT the bottom (iOS-62) · type a draft, back out →
"Draft:" excerpt within ~2s (iOS-64) · Account → Language → pick Deutsch →
**NO flash anywhere on mobile**, picker stays put, whole Account re-renders
German; home is translated when you exit (iOS-58 v1; desktop: one pane flash =
logged residual) · chat appearance → pattern picker = 4 SWATCH TILES (iOS-60) ·
pending outgoing contact: waiting strip translated under sl-si (iOS-63) ·
create-account failure path: form re-enables + a real alert shows (L1/L5) ·
onboarding "Back up now" → share sheet opens, Account badge doesn't nag after
(L3/AND-5) · **iOS-54 repro (before any fix):** open a chat, airplane mode ON →
does the topbar sub show anything within ~4s? Report exactly what you see.
**AND-17 logcat leg (optional, verifies the fix):** `adb logcat -c` → send 2-3
member-sheet requests → rows should appear; if any DON'T, dump logcat here.

## NOT built tonight (by design — waiting on device data)

AND-20 keyboard lever + AND-7 edge-to-edge (probe numbers first, #294 LAW) ·
iOS-54 chat-leg fix (repro first) · Account connecting-state (rides iOS-54) ·
OS-theme-flip push conversion (rare path; follow-up) · full HOME live
re-localize (desktop-pane flash residual; do with you present).

## Security (tell the BE engineer)

#331 had re-opened MAJOR #4 on Android (mini-app WebView shares the renderer +
file:// partition) — **contained** tonight (DomStorage scoped off the
miniapp-tagged WebView, zero regression). Two NEW Android MAJORs **logged, not
fixed**: #8 mini-app file-XHR can read `wallet.ixi` · #9 unconditional
WebView permission auto-grant. All in `security-review-for-be-engineer.md`.

## Commit (after F5 + the lever round — ONE batch, GitHub Desktop)

```
Fix session (#334-#33x): pre-audit (MAJOR #4 Android containment + 2 new MAJORs
logged) + Android chrome/notifications round (bars/splash/icon/dialogs/flash
mask, per-type copy + call channel) + cheap-C# round (L1/L2/L5/L7, backup
hand-off, W9, iOS-59/61/62/63/64, AND-11/17/21/22/23) + live re-localize v1 +
swatch tiles + bubbles 82%/360 + avatar viewer + file-send cancel interim +
kb-probe (Android) + smoke baseline-honest; smoke 1031/same 4
```
(+ the AND-20 lever + F5 fixes appended at commit time.)

## Loop verdict (#335) — round-2 deltas folded in

The #46 loop found **1 MAJOR (AND-11 blank-ringing-cover in the denied-mic
state — fixed: the gate re-arms the ring re-render + main-thread requests)** +
9 MINOR (all fixed — see DECISIONS #335). F5 additions from the fixes:
- **Denied-mic path (Samsung):** deny the mic permanently (deny twice), then
  receive a call and tap Accept → the ring RE-APPEARS within ~2s (not a blank
  cover); Decline works. Damir dial owed: explain-copy + settings deep-link.
- **Cancel hygiene:** send a BIG file, have the peer accept → the Cancel next
  to your bubble disappears at the first tick and never comes back (even when
  new messages re-render the log).
- Smoke expectation is now **BASELINE OK 1038 / the same 4** (7 new pins).
⚠ Commit: the icon/splash assets are UNTRACKED new files (drawable-*/ic_stat_*,
values-v31/) — make sure GitHub Desktop includes them (missing them fails the
Android build loudly).
