# HANDOFF — SESSION L (2026-09-04): the definitive read for Session M

★★ **This file SUPERSEDES every earlier handoff** (Session K's is archived as
`docs/archive/handoff-2026-09-03b.md`). Read this whole file, then **DECISIONS #767–#773**, then
`docs/fable-build-brief-premium-density.md` §10 + §12 + §13 (the picks ledger — every value has
its reversal in the token comment). The Opus verdict for Sessions I–K is now written into the
brief that ordered it: `docs/opus-review-brief-sessions-i-k.md` (§Verdict). The three MAJOR
lessons from the Session H verdict still stand and are joined by two more, #771 and #772.

## 0 · State and the numbers

Session L did **ITEM 0 + THE WALK**. Item 0 was the #46 adversarial loop (five rounds to CLEAN);
no *build* item was started, by design — none may begin before a CLEAN verdict. Damir then walked
the batch on device and on Windows, so **Session M starts with the walk already in hand**, which
is unusual and valuable: the queue below is written against measurements, not guesses.
Damir has not committed yet (`docs/commit-message-session-l-short.txt`, or the long form).

In a clean clone, Linux container, `npm i jsdom tree-sitter tree-sitter-c-sharp`, **Ixian-Core
sibling at 097341a PRESENT**:

```
bundle 321 · shells 18 · smoke BASELINE OK 4067 / the 3 known (#136 · M5 · B3)  ← WITH the sibling
                        (say which, always — the M1 hold-out gate, #748, is one assertion fewer without it)
locales ALL CLEAN 786 · i18n-lint ✓ (6 dev exemptions, 2 sites) · pseudo 9/9
cs-syntax 141 clean + 1 known gap · extract-strings --check ✓ · build-shells --check ✓
build-legal-docs --check ✓ (terms baked · privacy HELD 🟡) · Ixian-Core 097341a untouched
```

**4040 → 4067 (+27).** No new export, no new string, no new C# file — bundle, shells, locales
and cs-syntax are unchanged in count. 37 files changed.

⚠ **Built `chat.html` is 674 177 B against the `#345` pin ceiling of 660 KB — 1 663 B of
headroom.** The ceiling lives only in the suite, so an overrun reddens a pin rather than failing
the build, but the next prose edit to that shell must budget a raise **in the same commit**, and
must price it the way the pin's docblock does (0.08 ms/KB, three measured in-tree points).
Do not raise it silently, and do not let the agent whose own prose crossed it raise it without
saying so. (DECISIONS #773 ④.)

## 1 · What Session L shipped

**The loop, and the three MAJORs it found.** Full account in the brief's verdict; the short form:

① **#768 — Windows: a missing asset folder rendered as a normal-looking STALE page.** Damir's
two reports that morning (Add app → `ERR_FILE_NOT_FOUND`; a LIGHT Account pane under a dark
system theme) were ONE cause, measured on his machine inside the hour. `localizeHtml` returned
`void`; `generatePage` gated freshness on `File.Exists`, which is true for an `ll_*.html` a
PREVIOUS BUILD left in `Documents\Spixi\html` — a folder that survives every wipe — and handed
back that URL regardless. Now: `bool` return (true only after flush + close, and it deletes a
file it truncated), the map gated on the WRITE with a `TryRemove` on failure, and a degrade
ladder ending in a built-in page that NAMES the missing file. `copyResources()` no longer
hard-crashes the `App()` constructor on the same folder.
② **#769 — the dark on-action white ink landed on ten surfaces that are not the blue button**
(mute label **1.14:1**). Seven role tokens; light unmoved; **no fill changed**.
③ **#770 — a rotation persisted a keyboard height that fills the screen** and latched `kbUp`.
The fix is semantic (`kbEditableFocused`), after two geometric attempts were broken by reviewers.

Plus: the K1 hold's 32 px lurch on notched iPhones; the slide entry's hit-testable-while-invisible
window (32 ms, measured, not the 300 ms round 2 shipped by mistake); the anchored-menu listener
leak; Session J's **missing** security-handover-gate section, filed retroactively.

## 1b · The walk (2026-09-04) — what it decided · full results in `docs/walk-session-l-results.md`

**13 P · 0 F on the sheet · one failure found in the log.** DECISIONS #776.

★★ **#768 confirmed fixed on device, and the alignment mystery is closed.** W0–W4 pass: the app
now STARTS with no `html\` folder and names the missing file instead of dying in the `App()`
constructor. **G1/G2 pass** — a grouped run lines up on a clean build — so the misaligned
continuation bubble of 2026-09-03 was **the stale asset set of #768**: one build, three faces
(Add app 404 · light Account pane · the hanging bubble). No component defect ever existed.

★★ **THE CHAT-OPEN ROUTE IS DECIDED (#764). It is the PARSE, not WebView creation.**
`nav` = `dcl` in **all six** opens (117/117 · 126/125 · 126/125 · 138/138 · 132/132 · 144/144),
so the in-document time is entirely parse and window load fires ~1 ms after DOMContentLoaded.
A ~320 ms warm open splits as:

```
60 ms  WebView creation   (onload 186 − nav 126)
125 ms document parse     ← the lever
30 ms  C# push + drain
74 ms  render → painted
30 ms  painted → present
```

**A retained warm chat WebView removes ~185 ms of ~320 ms** — creation AND parse, paid once
instead of per conversation. **Write the BE work order on this.** Zero `backstop t=` lines across
both logs: `ixian:painted` arrived on every open, so there is no #663 to chase.

★★ **RAM (#764) — the split names the lever, and it is mostly NOT ours.** Chats list **329 MB
PSS**, Seed 01 **368 MB** (Δ +39). At rest: Native 69.6 · Unknown 72.6 · Graphics 67.5 · Code
50.3 · System 55.9. Against WhatsApp/Telegram ≈177 MB peak we are **~150 MB over at rest, before
opening anything.** The FE lever (release the parked Account WebView + the tile budget) attacks
Graphics and might recover 15–25 MB — worth doing, **not sufficient**. The bulk is Native +
Unknown + Code, the .NET/Chromium side: **a BE row, now with a number instead of an adjective.**

Also confirmed: **#766** (`appnew onload t=75 → present t=75`, 0 dropped frames — the row
committed untested on his word) · **K1** on device (reveal on the real resize at 28 ms and 30 ms,
never the backstop) · **`[SCROLL]` needs no fix** (`drop=0`, `max=8–25 ms` after the first
page-in) · **L14** releases by cover, not backstop.

⚠ **One failure, found by the LOG and not by the sheet** — see the queue's item ②.

## 2 · Session M's queue, in order

★★ **The build queue is UNBLOCKED — the verdict is CLEAN.** Everything below is the Session K
queue, unchanged except that item 0 is done and ⓪ has moved up.

⓪ **THE BE WORK ORDER — write it first, it is the only item now blocked on nothing.** The
   numbers are in (§1b): the cost is the **parse**, so the ask is a **retained warm chat
   WebView** (creation + parse paid once, ~185 ms of ~320 ms), and the batch transport
   (`docs/chat-transport-spec.md`) remains the separate scroll-auto-load unlock. Add the **RAM
   row** with its measured split. #764 said "decide after the numbers" — the numbers exist.
① **#766's generalization.** The three FORM pages present at onload and are now MEASURED
   (`appnew onload t=75 → present t=75`). The DATA pages (chat-info · App details · tx detail ·
   Settings · Downloads) still carry the 120 ms hold — generalize the chat's `ixian:painted`
   grammar to them (present on the shell's own paint, per page), and give the chats-list flush a
   `[CDPERF] chats` pair (A1: ~60 `addChat` evals, one per row).
② **★ #770's THIRD TRIGGER — the split-screen backstop.** The walk sheet says FAILS: none and
   R4 was not run, but `walk2.log` holds the failure R4 exists to catch: at `ih=475`,
   `open kbUp=true` → **`reveal by=backstop`** 453 ms later, the grow arriving seven seconds
   after that. A small viewport produces no grow inside the window, so the tray waits out the
   backstop. The rotation cases ARE fixed (`kbUp=false`, non-hold, immediate); this one is not.
   ★ **An unscored row is not a passed row** — the sheet is true of what was tested, the log of
   what happened. (A `backstop t=` line on a chat OPEN would still be the #663 class — there
   were none.)
③ **RAM (#764) — MEASURED, see §1b.** The split names the lever — Graphics+WebView → release the
   parked Account WebView under memory pressure + the tile budget; Native → a BE row.
④ ~~`[SCROLL]` numbers~~ ✅ **MEASURED, NO FIX NEEDED** — `drop=0`, `max=8–25 ms` after the
   first page-in. Do not spend a session on a jank that the instrument says is not there.
⑤ **The group-avatar device fact** (the hash is NOT it — uniform over 2000 synthetic addresses):
   photo → find the writer under the group address (start at `FriendList.setAvatar` callers and
   `writeAvatar`); disc → the row is not handing the address, get two addresses from Contact
   details and run `identityIndex` on them.
⑥ ~~The continuation-bubble alignment~~ ✅ **CLOSED BY THE WALK (G1/G2 pass)** — it was #768's
   stale asset set, not a component defect. Historical note follows.
   ~~★ NEW — the continuation-bubble alignment~~ (Damir, 2026-09-03, screenshot): in a group, a
   grouped continuation row's bubble hangs ~40 px left of the bubble above it. **The component is
   not the suspect** — a jsdom probe of the shipped bundle shows `data-gutter` and the spacer
   present on `single`/`first`/`middle`/`last`, and the only way to lose them is
   `showAvatar: false`, which the shell computes as `(!isSent && mode.isMulti)` for every
   received row regardless of position. So source and symptom disagree. **First suspect: he was
   looking at the same STALE Windows asset set as #768** — one build, three faces. The decisive
   test costs nothing: rebuild with F5 and look again. If it survives a clean build, render it on
   the harness (§3) with a reaction on the row above, which is the one thing distinguishing his
   screen from the case T1 was built against.
⑦ **★ NEW — the chat-appearance restructure (#774, render round).** Four cards → three:
   Background absorbs Opacity ("None" = today's level 0), Canvas becomes a compact selector that
   is **absent in dark mode**. Spec: `docs/chat-appearance-restructure-spec.md`. ⚠ THREE RULINGS
   ARE OWED BEFORE BUILDING — does "line art" mean the shipping `doodles` (rename) or the RETIRED
   `lineart` (resurrect, against his own E1 ruling)? does `flow` stay? and for dark, hide the
   control or design a gradient (render both grounds and let him pick — do not answer it in
   prose). The `(style, level)` → one-value migration lands in all three pre-paint ladders (#690).
⑧ **★ NEW — apps list (#775, small):** default view → **grid**, and the list/grid toggle must
   **survive a restart** (`spixi.apps.layout`; today it is in-memory, `apps-shell.js:297`, a
   deferral nobody closed). Read at seed time, not after first paint (#690). The new key gets its
   security-gate row IN THE SAME BATCH — Session J's `spixi.kb.slot` did not, and it took a loop
   to notice. Spec: `docs/apps-layout-rows-spec.md`.
⑨ Walk fallout from his eye on the rest (every value has its reversal in the token comments).
⑩ The TG-order chat-info rebuild (render round — the harness boots the real shells).
⑪ Pending rulings: URL previews (`docs/url-preview-memo.md`) · privacy wording (#730) ·
   "X left the group" (#215 device check first).
⑫ iOS rows (Session G walk 15–27, 37) when he says Mac.
⑬ **Release hardening stays last** — retire [LANDTAB] · [EXCERPTDIAG] · [CDPERF] (chat + appnew
   + the boot/ctor stamps) · [SCROLL] · [KBTRAY] (five) · [WV2] · [WEBVIEW] (the
   `OnConsoleMessage` override) · the console mirror · `maxLogCount=5` · `SpixiDevCoexist`
   (csproj + SDevSeed.cs + the SettingsPage blocks + the About card) · the store keystore.
   ⚠ The `[SCROLL]` retirement is now SAFE to do as a set — its pin was vacuous and went red on
   the sanctioned retirement; it is fixed and mutation-checked in both directions (#771).

## 3 · The render harness (unchanged from Session K, still the best one)

`/tmp/j/render/harness.mjs` — Playwright (`playwright-core`, npm) over
`/opt/pw-browsers/chromium-*/chrome-linux/chrome`, `--allow-file-access-from-files`. It boots the
BUILT shell (`Spixi/Resources/Raw/html/<shell>.html`) from `file://` exactly as the device does,
432×900 @ 2.5, an Android UA (`isMobile`, `hasTouch`), Roboto forced via an init-script `<style>`
(apt `fonts-roboto`), theme via `colorScheme` + `data-theme`. A setup module drives the page
through `executeUiCommand(window[name], …base64 args)` — the same wire C# uses — so every render
is the real shell with real pushes. `page.evaluate` for measurements. PNG out; PIL for sheets.
Symlink `node_modules` into the harness dir. **Keep `page.on('pageerror')`** — a shell that throws
renders a spinner and looks like a layout bug.

## 4 · Rules and workflow (unchanged, load-bearing — plus two new ones)

Clean-clone gates in a Linux container (`npm i jsdom tree-sitter tree-sitter-c-sharp`;
Ixian-Core sibling at 097341a — and **SAY whether the sibling was present** when you record
smoke) · mutate in FULL tar copies, **never `cp -al`** (and rebuild bundle + shells inside the
copy when a component moves) · **bundle BEFORE shells, always** · measure the closing number
AFTER the last suite edit · render on the real shells before Damir rebuilds · **measure on device
before any fix (#215)**.

★ **NEW — #771: every pin declares `stripCode` or raw, explicitly.** A sweep over raw text is
satisfied — or defeated — by the comment that explains it. A pin whose subject IS the prose runs
on the `prose()` normaliser and asserts by **count-and-quote**, never by a literal absence.
**And: a behavioural pin that STUBS the function under test proves nothing.** `kbEditableFocused`
had a source pin and a stubbed harness, and a one-token mutation reverted two MAJOR fixes with the
suite fully green.

★ **NEW — #772: a comment stating an invariant the code does not enforce is a defect.** Nine this
loop, three of them written BY the fixes. `file:line` in a comment is a **searchable quoted
anchor** now: a number rots silently, a grep comes back empty.

★ **Marker grammar (#773 ⑤):** `rN RN-x` is **not unique per loop** — HEAD already carries six
`r2 R2-x` findings under this loop's numbers. Per-FILE uniqueness is the working rule; qualify
with the loop (`#46-K r2 R2-1`) where it matters.

## 5 · The one thing only Damir can do

**The walk.** The verdict unblocks the queue; it does not replace his eye or the device. Items
①–⑩ need the phone, and the chat-open capture is what decides the route for the remaining
~150 ms. `docs/f5-checklist-session-l.md` is the runsheet, and the clickable artifact
(`docs/walk-artifact-session-l.html`) carries P/F/N per row with copyable result blocks.

⚠ **Before anything else on Windows: build with F5 (the VS Deploy step is what stages
`Resources\Raw\**`), never a bare `dotnet build`.** #768 is fixed so that a missing folder now
says so instead of rendering a stale page — but the fix makes the failure *visible*, it does not
stage the assets for you. If Add app or the Account pane still look wrong after a clean F5, that
is a NEW finding and item ⑥ wants it.
