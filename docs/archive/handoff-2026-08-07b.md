# Handoff — 2026-08-07 №2 (the PLATE build) → next session

**Read this first.** Companion to (not a replacement of) `docs/handoff-2026-08-07.md`
(the plate list + carry-overs; its plate section is now BUILT) and
`docs/handoff-2026-08-07-cowork.md` (the parallel docs session — A7 done, Figma
mirroring retired, batch B dials locked). Built in the CLOUD Cowork session after
Damir's lid-close, from a GitHub clone verified == the Mac tip (`31003744`).

## What this session built (ALL UNCOMMITTED)

- **#314 — every B1 polish item (9/9, zero-C#).** Highlights the next reader needs:
  the toast bug was a MISSING STYLESHEET (settings.html never linked toast.css);
  the backup badge was a REFRESH gap, not read logic (iOS fires none of the three
  listeners on overlay pop → 2s guarded poll), and the SAME hole was fixed in
  landtab (now consumed on the `onSettingsClosed` push); the mention pill had TWO
  split modes (dot-less generic term · linkify-outer stealing URL-looking nick
  words) → mentions now split FIRST + URL-structural boundary class (accepted
  residual documented in message-bubble.js); iOS-47's wrong party was APPS;
  i18n gaps were missing DRAFTS — filled for all 7 locales (+102 strings).
- **#315 — Account as a peer tab (iOS-46 route (a)).** Park-don't-dispose +
  instant re-present, mobile/narrow only; hub back arrow REMOVED everywhere;
  `onRepresented` shell contract (exit-latch reset + dirty reconcile);
  reloadAllPages/onLowMemory drop the parked page; pageLoaded gate; first open
  stays cold (boot-preload REJECTED — it would drop user nav via the single
  staging slot). Desktop #245 pane untouched.
- **#46 loop: 2 adversarial rounds, CLEAN.** 4 MAJOR-class catches — frozen
  re-present (exit latch) · URL `@handle` theft · stale-theme park ·
  dirty-state reconcile — all fixed and mutation-pinned. r2's accepted residual
  (rare path-char mention splits) documented in-code.
- **#316–#318** — the parallel session's three decisions logged (numbering note
  in #316: they asked for "#314+", landed at #316+ — #314/#315 were already in
  code comments). **#319** = session close.
- Pipeline proof: baseline re-proven pre-work (953/same 4) → close **996 pass /
  the SAME 4 pre-existing** (#136 · #149③ · M5 · B3-shell); bundle 777 KB /
  238 exports · 22 shells · lint ✓ · pseudo 9/9.

## Damir's next Mac session (in order)

1. Delete the two scratch files at repo root: `_mention-repro.mjs`, `_mr2.mjs`.
2. **obj/bin WIPE → device build** (C# changed: SpixiContentPage · HomePage ·
   SettingsPage · UIHelpers · Node.cs) — recipe in the checklist.
3. **F5: `docs/f5-checklist-2026-08-07-plate.md`** (A = #314, B = #315 incl. the
   B6 nickname-revert and B7 theme-flip probes, C = regressions).
4. **ONE commit** (#314–#319 + docs) in GitHub Desktop — message in the checklist.

## Next build work (AFTER the plate lands — sequencing rule in the brief)

**Batch B per `docs/fable-build-brief-parity-b.md`**: B1 = R3 media cap flip
(ALL platforms, #317) · B2 = R4 ack-gated backup stamp + one-time clear
(small-C#, #318) · B3 = R7 share verb under the #301/#303 bare-address contract.
Same files as the plate (chat.html/home.html/HomePage/SettingsPage) — do not
interleave.

## Standing carry-overs (unchanged)

W1 es/ru broader pass · A10 on WINDOWS (decides part of batch B) · desktop scan
re-verify · Windows keyboard sanity · the 4 smoke pre-existers (own session) ·
B1/B2/B3 device-runsheet rows (MORE suspect post-#312) · real Terms/Privacy
documents (Damir) · iOS-41 (select-text dial) · iOS-43 (Launch clip) · iOS-44
(deferred) · iOS-18 (multiuser picker) · iOS-31 (sim first) · iOS-32 (heat,
measure on Release) · probe-noise retirement (KEEP the per-nav heal) · dev-entry
icon export (`adjustments-alt` stand-in, #314) · #315 later dials on record:
boot-preload (rejected this round) · parked content-process-death probe (F5 B9).

## Rules (unchanged)

Zero-C# unless evidence forces it (#215 — it rewrote a third of THIS brief) ·
★ #221 chat isolation · bundle BEFORE shells · smoke baseline now **996 / the
same 4** · #46 loop after every batch · DECISIONS #320+ (#302 stays retired) ·
`git --no-optional-locks`; commits = Damir in GitHub Desktop (#306) · obj/bin
wipe before device builds; verify the bundle after.
