> ⚠ **SUPERSEDED 2026-08-16 by `docs/handoff-2026-08-16c.md`.** D-18 is SOLVED and guarded (#354); AND-38 is built (#355); BE question 9 is filed. Archive this file with the next batch.

# Handoff — 2026-08-16b. READ FIRST.

**LANGUAGE RULE: ASD-STE100 Simplified Technical English.** See `CLAUDE.md`.

Supersedes `docs/handoff-2026-08-15c.md` for state. That file and
`docs/next-session-prompt.md` carry superseded banners; archive them with this batch.
⚠ The archive-name warning from 15c still applies: `docs/archive/handoff-2026-08-16.md`
(no `b`) is an OLD file from 2026-08-13. This file is `-16b` for exactly that reason.
Read the date in the header, never the file name.

---

## 1. State — verify it, do not trust it

```
git --no-optional-locks log --oneline -3
git --no-optional-locks status --porcelain
```

At session start the tree was CLEAN at `b2c6cc3e` (pushed). This session produced ONE
UNCOMMITTED batch on Damir's disk via tarballs `spixi-351.tar.gz` + the `spixi-353.tar.gz` delta: rows **#349–#353**.

| Item | Value |
|---|---|
| Smoke | **BASELINE OK — 1667 pass / the 4 KNOWN pre-existers (#136 · #149③ · M5 · B3)** — the baseline GREW from 1627 (+40 net assertions, all mutation-verified; the #353 dial retired 4 fix-B pins and added 2) |
| Compiler | ⚠ The #350 C# deletions were NEVER COMPILED (no .NET in the cloud). Deletion-only diff, zero balance delta. Damir's build is the first pass |
| Device | 🟡 F5 owed per `docs/f5-checklist-351.md` — Windows mouse pass + A52 touch pass |
| ★ Workflow rule (#352) | **Adversarial review runs on OPUS, always. Fable builds only.** Damir, mid-session, standing |

## 2. What landed this session

1. **#349 — the owed Android pass RAN, all green.** A9 · A7 · A5/I-8 on touch · reduced
   motion · the **Release PERF numbers: chat open = 234 ms cold / 212 ms warm on the A52.**
   Verdict per #294: no segment dominates, **no further chat-entry performance work**.
   Seven new findings AND-31…AND-37 (`docs/android-findings.md` — the landscape round).
   One open leg: the flick-scroll cancel check (now step 3.3 of the F5 checklist).
2. **#350 — `PerfTrace` DELETED.** The release blocker is closed. ⚠ The tarball cannot
   delete a file: Damir must run `Remove-Item "Spixi\Utils\PerfTrace.cs"` — it is in the
   checklist, step 1.
3. **#351 — D-16 BUILT (table A row 2).** The press fill completes then fades; full spec
   in the DECISIONS row. The #46 loop ran FIVE rounds (3 fable auditors → Opus
   break-my-verdict ×3 rounds) and found 8 MAJOR-class defects, 5 of them in my own
   fixes. All fixed, all pinned, every pin mutation-verified. The Downloads file row
   became the eighth press family in passing.
4. **#352 — the Opus review rule**, logged.

## 3. THE PLAN — table A continues (Damir's rule holds)

Work `docs/archive/`…no — the live table is §9A of the superseded 15c handoff; it
carries over unchanged except rows 1–2 are DONE. In order:

| Next | Item | Note |
|---|---|---|
| 0 | ★★ **D-18 — the load-more history bug** (§6) | **FIRST ACTION.** Verify-first; the suspects are listed in §6 |
| 1 | **I-2, chip outline only** — #353 removed the selected-row hover, so the ROW half of fix C is moot | One small token/outline pass + contrast check |
| 2 | **D-17** — Apps search-field flash | Read #340 C-MAJOR-1a/b/c first |
| 3 | **I-6** — digit grouping, **APP-GLOBAL** (Damir 2026-08-16) — every amount display | Display only; never touch the wire format (#77) |
| 4 | **D-5** — relation arg on addContact/addMember | C# here; = AND-30 |
| 5 | **D-9** — close delete-account on device | Throwaway account |
| 6 | **★ D1 reply-to** | Two-device id round-trip FIRST; the body marker is hostile under the gate |
| 7 | I-1 + welcome flicker · I-10/I-9 (measure first) · D-4/D-15 honest halves · D-1 part B · D-7 + I-11 (one locale round) · I-3 design round | Then the security sweep LAST |

Also queued: the AND-31…AND-37 **landscape round** (new, from #349) and AND-35's
chat-appearance renames dial (rides a locale round).

Table B (the eight BE questions) is UNCHANGED — parked, do not build against it.
**A5 and CI7 stay BLOCKED.**

## 4. How to work — unchanged, plus one rule

The cloud twin recipe, the build chain, the tarball delivery, the PowerShell rules: all
as in 15c §7 — they carry over verbatim. The one addition: **#352 — Opus for every
adversarial review pass.** And the standing set: verify plans against code · #294 ·
#215 (Ixian-Core is NOT in this repo) · smoke green at the NEW baseline **1667/4** ·
mutation-honest pins, proven · bundle before shells · Damir commits · DECISIONS rows at
decision time · the #46 loop on anything substantial · the security handover gate while
building. ★ Reply-to stays the one to watch under the gate.

## 5. Do not re-attempt — grew by one

All of 15c §8, plus: **a single fix-B hover rule carrying both colour and transition.**
The colour must stay unguarded and the transition guarded (split rule, Opus r3 finding 1)
— re-merging them re-ships a grey mid-press flash on selected rows.

## 6. F5 findings, 2026-08-16 (Damir, Windows + A52, during the #351 F5)

| ID | Finding | State |
|---|---|---|
| **D-18** ★★ CRITICAL | **"Show older messages" exhausts WITHOUT showing the full history.** A chat with 100+ messages: Damir pressed the pill until it disappeared and older messages were still missing. History: FLAGGED as **R1** in `docs/legacy-parity-audit.md` (history beyond the window unreachable) and the pill FIX was BUILT in parity batch A (#297–#299) — what he sees now is a defect in that chain or below it. Suspects, verify-first (#215): `friend.getMessages(channel, messagesToShow)` may return only Ixian-Core's in-memory tail (the on-disk history never loads) · `SingleChatPage.xaml.cs:1526` hides the pill when `messages.Count < messagesToShow` — an under-returning getMessages hides it EARLY · the Core `0.9.8k` bump `5643e5b` changed what messages STORE (the D-14 commit) · the shell burst path. ⚠ The #350/#351 batch did not touch this path (the loadMessages edits were mark/counter deletions) — but a stash A/B on the symptom is the honest attribution if doubt remains. **THE NEXT SESSION'S FIRST ACTION.** If the cause is Core, it joins table B as BE question 9 — but the investigation itself is table A |
| **AND-38** | Android: tapping the wallet BALANCE (hide/show toggle) flashes a pressed state "across the row". Damir's dial: **NO pressed state there — just swap the value.** The eye is `.c-wallet-hero__eye` (wallet-hero.js:42); find which pressable family the tap target matches and opt it out (a `data-nopress` opt-out honoured by pressable.js is the likely shape) | OPEN — small FE, next batch |
| **I-6 grows** | Digit grouping is **APP-GLOBAL** (Damir): every amount display, not the tip sheet alone. The #77 wire-format rule stands untouched | Table A row updated |
| Slowness scare | RESOLVED — the bare exe is fast; the VS-attached Debug run was the cost. The batch is innocent | Closed |
| **#353** | NO hover tint on a selected row (from the F5 video) — built, loop CLEAN, rides the #351 commit. I-2 shrinks to the chip outline only | Built |

| 2.3b residual | Windows: occasional tonal-hover still seen on a selected row AFTER #353. Suspect STALE Raw html in the build output (the incremental-build law, Windows leg). Re-check after a full obj/bin wipe before treating it as a defect � the rule is verified absent from source and built shells | OPEN � re-verify at D-18 session |
