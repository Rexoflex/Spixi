# HANDOFF — SESSION J (2026-09-02/03): the definitive read for Session K

★★ **This file SUPERSEDES every earlier handoff** (the Session I handoff is archived as `docs/archive/handoff-2026-09-02-session-i.md`).
Read this whole file, then `docs/fable-build-brief-premium-density.md` §10 (the picks ledger —
Session J's values are appended there as §12), then DECISIONS #748–#758. The Opus verdict
(`docs/opus-review-verdict-session-h.md`) still carries the three MAJOR lessons.

## 0 · State and the numbers

Damir commits the WHOLE Session J batch (code + docs + the walk sheet + the four fix-ups) as ONE
commit — the short message is `docs/commit-message-session-j.txt`. After it, the tree at the tip
must read:

```
bundle 319 · shells 18 · smoke BASELINE OK 4003 / the 3 known (#136 · M5 · B3)  ← WITH the Ixian-Core sibling
                        (4002 without it — the M1 hold-out gate, #748; record which)
locales ALL CLEAN 786 · i18n-lint ✓ (6 dev exemptions, 2 sites) · pseudo 9/9
cs-syntax 141 clean + 1 known gap · extract-strings --check ✓ · build-shells --check ✓
build-legal-docs --check ✓ (terms baked · privacy HELD 🟡) · Ixian-Core 097341a untouched
```

Why the numbers moved: smoke 3979 (with sibling) → 4003 (+24: the Session J block before the
summary — walk fixes, rulings, #753–#756 — and the console-mirror pin beside Session I ②) ·
i18n-lint dev exemptions 5 → 6 (the heavy seed row) · bundle, shells, locales, cs-syntax unchanged
(no new export, no new string, no new C# file). The C# (App.xaml.cs one `#if`, WebViewRenderer.cs
one override, SDevSeed.cs v2, SettingsPage.xaml.cs one verb) COMPILED on Damir's machine — Windows
Debug and Android Release both ran.

## 1 · What Session J shipped (built, rendered on the harness, pinned, WALKED 25/27)

① **The seven walk fixes (#749):** typed-card lift · the ⊕ press = the 36 disc (the mechanism was
the `background:` shorthand resetting `background-clip`) · Notifications flat inside its card · the
chats-row time 12/16, measured against TG · the Account THIRD row height `--row-h-stacked` = **60,
Damir's pick C** · the caret-without-keyboard dial — shape 2 did not blink either → **DROPPED**
(`CARET_WITHOUT_KEYBOARD = false`, his rule) · the tray ↔ keyboard flicker: see ⑥.

② **Damir's evening rulings (#750):** light canvas `#E4EAF3` · ink `#051C8E @ 6%` · his 289° wash
as the Gradient option (the light dial CLOSES, #744) · the secure notice white / midnight +
elevation-2, no border · chats-row names Medium / Semibold-unread · the selected-message tick on
the row's own inset, white, dark disc primary-500 · the money CTA rides the bottom edge · input on
a card = white · row titles Medium over regular subs · typed-card buttons never truncated (wrap) ·
the welcome/lock logo white. **Dark canvas (#758):** `#0D1117`, ink `#C6CFFD @ 4%`.

③ **The numbers, ALL IN (#751 · #754 · #756):** nothing reached logcat because Logging started
with `console_output: false` (now ON under `SPIXI_DEV_COEXIST && ANDROID`, tag `I/DOTNET`) and
this WebView forwards NO console level (the chrome client forwards the shells' console into
Logging as `[WEBVIEW]` in dev builds — never the mini-app's). Read: chat open onload → present
≈ 200 ms at 28 / 40 / **1000** messages (the window is 50 rows); shell paint 21–27 ms and it lands
BEFORE `present` — **the L10 shape does NOT apply, no chat-open fix**; 0–1 dropped frames per open;
`[L14] … released by cover`; seed heavy 11 600 messages at 0.24–0.34 ms/msg.

④ **The tray ↔ keyboard flicker, solved on numbers (#753 · #756):** the tray's height is the
keyboard's MEASURED height (`--kb-slot-h`, 346 on the Motorola, persisted); the remaining flicker
was keyboard → tray: `[KBTRAY] open … focused=false` — the ⊕ button takes focus before its click,
so `instant` read "no keyboard" and the tray rose. `kbUp` (the keyboard's own state) decides now.
🟡 **A5 re-walk on this build** — the one row still open from the sheet.

⑤ **Seed harness v2 (#752):** light (2–40) · heavy (Seed 01–10 × 1000 + 40 × 40), top-up in
place, the store write timed; the About status refreshes in place (#754).

⑥ **Tails — pick D (#756):** the group avatar rides the FIRST bubble, top-aligned beside a
WhatsApp-shaped tail (rounded tip, convex sweep); the 1-px seam at the join is closed.

⑦ **Small ones (#753 · #755):** the Windows recipe names the exe folder (the empty `$exe` left no
`html` beside the exe → ERR_FILE_NOT_FOUND, #663's second sighting) · the members search field is
white on its card · the Chat-appearance ground reads a stored Flat back and the preview shows it.

## 2 · Session K's queue, in order

① **A5 re-walk** (keyboard up → ⊕ → no rise; ⊕ → field → no dip) on the fix-up-4 build. PASS →
   retire `[KBTRAY]` + the `[CDPERF]` set together (each pin becomes its reversal). FAIL → the
   `[KBTRAY]` order in `walk.log` says where (grep `WEBVIEW`), and only then touch #721.
② **Logged with mechanisms, not yet built:**
   - the RECEIVED emoji-sticker's reaction pill covers its time chip — #746③ is direction-blind;
     received → the pill goes inline-end (#753 ③a).
   - five groups, one avatar colour — trace what `addChat` hands `createAvatar` for a group
     before touching the 12-anchor hash (#753 ③b).
   - the Apps ⋯ menu as an ANCHORED dropdown (the chats-row `.c-msgmenu` grammar), not a bottom
     sheet — "small tap area, Details and Uninstall close together" (#757 ①).
   - the Add-app screen stutters on open with 7 apps — MEASURE first (a `[CDPERF]`-shaped pair
     on apps.html), then #726's leading-paint + rAF-batch shape (#757 ②).
   - Windows/WebView2: the ½-s ghost of the previous page on Account ↔ Contacts (#754) and the
     white pane on chat-info open (#755) — one stamp pair, then the WebView2 background colour
     from C# (`DefaultBackgroundColor`) and the L14 handshake measured on WebView2.
   - the home shell logs `c-button: icon-only button requires ariaLabel` twice at boot — find it.
③ Walk fallout from his eye on the rest (every value has its reversal in the token comment).
④ The **TG-order chat-info rebuild** (render round) — the secure-notice redesign is DONE (#750).
⑤ Pending rulings as they arrive: URL previews (`docs/url-preview-memo.md`) · privacy wording
   (#730; the two markers in `privacy-policy.md` hold the full policy) · "X left the group"
   (#215 device check first).
⑥ iOS rows (Session G walk 15–27, 37) when he says Mac — the composer rows too.
⑦ **Release hardening** stays last — retire [LANDTAB] · [EXCERPTDIAG] · [CDPERF] · [KBTRAY] ·
   [WEBVIEW] (the OnConsoleMessage override) · the console mirror · `maxLogCount=5` ·
   `SpixiDevCoexist` (csproj + SDevSeed.cs + the SettingsPage blocks + the About card) · the store
   keystore — then the Opus adversarial loop over Sessions I–K (#46) and the security gate sweep.

## 3 · The render harness (rebuild it from this if the container is gone)

`/tmp/j/render/harness.mjs` — Playwright over `/opt/pw-browsers/chromium`: the BUILT shell's
`<head>` (every `<style data-src>` + tokens/base links, `*SL{…}` stripped) + `spixi.icons.js` +
`spixi.strings.js` (`window.SL = SpixiStrings.get(lang)`) + `spixi.bundle.js`, one `<style>` that
forces `--font-ui: Roboto` (apt `fonts-roboto`), viewport 432×900 @ 2.5, **an Android UA** (or the
shells stamp `data-desktop`), a setup script that mounts components via `window.Spixi.*`, a PNG
out. `layers.mjs` beside it counts compositor layers over CDP `LayerTree`. Sheets in
`docs/reference-screens/premium/sheets/session-j/`.

## 4 · Rules and workflow (unchanged, load-bearing)

Clean-clone gates in a Linux container (npm i jsdom tree-sitter tree-sitter-c-sharp; Ixian-Core
sibling at 097341a — and SAY whether the sibling was present when you record smoke) · mutate in
FULL tar copies, never cp -al · **bundle BEFORE shells, always** · measure the closing number
AFTER the last suite edit · render before Damir rebuilds; his eye rules every dial · commit is
Damir's, one batch, in GitHub Desktop; never `git add -A`; `git --no-optional-locks status` on
the mount · delivery from the container = tars extracted onto the mount (`tar --overwrite`),
`_to_delete_*` for anything the bridge cannot delete. Windows builds from PowerShell — the recipe
is in `docs/f5-checklist-session-j.md`, with the logcat capture that now works.
