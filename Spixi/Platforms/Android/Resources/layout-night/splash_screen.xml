<?xml version="1.0" encoding="utf-8"?>
<!-- ★ C5 (#534, #548): the PRE-Android-12 splash (windowBackground) under a DARK OS theme —
     the same layer-list as layout/splash_screen.xml on the near-black ground (#13171b =
     the shells' own dark instant background). The bitmap (@drawable/splash) is a WHITE
     lockup on transparency (verified: its opaque pixels are #fdfcfc), so it reads on
     this ground without a second asset. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
  <item>
    <shape>
      <gradient
      android:angle="90"
      android:startColor="#13171b"
      android:endColor="#13171b"
      android:type="linear"/>
    </shape>
  </item>
  <item>
    <bitmap
        android:src="@drawable/splash"
        android:tileMode="disabled"
        android:gravity="center"
        android:adjustViewBounds="true"
        android:scaleType="fitXY"
        />
  </item>
</layer-list>
