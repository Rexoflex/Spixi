# Handoff — 2026-08-15. READ FIRST.

Supersedes `docs/handoff-2026-08-14c.md` for state.

**LANGUAGE RULE: ASD-STE100 Simplified Technical English.** See `CLAUDE.md`.

## State — verify it, do not copy it

- HEAD carries two committed cleanup commits from 2026-08-14:
  `259cbd71` `.gitattributes` labelling, then the scratch removal.
  Check with `git --no-optional-locks log --oneline -3`.
- **UNCOMMITTED on Damir's disk: the #346 + #347 batch.** Built, smoke-green
  (**1540 pass / the 4 known pre-existers**), F5-tested on Windows and Android.
  It has NOT been committed because the F5 pass raised the findings below.
- `PerfTrace` is still in the build, deliberately. It has **no `#if DEBUG` guard**.

## ⚠ FIRST ACTION — settle W1 and W10 before touching any code

Every F5 item that failed is a change **inside a shell**. Every visual item that passed
comes from the **external** files added by #345. `obj/bin` was not wiped before the
Windows F5. That is the signature of stale packaged assets.

```powershell
Select-String "$env:USERPROFILE\Documents\Spixi\html\contact_new.html" -Pattern "max-width: 640px"
Select-String "$env:USERPROFILE\Documents\Spixi\html\chat.html" -Pattern "chat-boot__spinner \{"
```

No hits → wipe `obj/bin`, rebuild, retest W1 and W10 only. If they pass, the list drops
by two and we learn that a Windows F5 needs the same wipe discipline as iOS (#320).

## The work — `docs/f5-findings-2026-08-14.md` is the list

19 findings from Damir's Windows and Android pass, each triaged with a verdict and
file:line evidence. Do not re-derive them. Order agreed there:

0. **W15** launch shells (create/restore) content cap — same rule as #347.
1. **W14** delete account/wallet FREEZE, and always return to welcome. **Highest.**
2. **W2** the Account save control that does nothing, plus the auto-save decision.
3. **A9** landscape Account pane · **A7** FAB residue + "New chat" title.
4. **W4a** tx/chat selected colours · **W9** remove Account Share · **W11** 640 both ·
   **W8** tip in bot groups + address truncation.
5. **A5 / W4b** the centre-out fill. Its own unit.
6. **W1** chat-info treatment — measurement first (#294).

## Decisions Damir made, do not re-litigate

- **W11:** 640 for both add-contact and add-app. Change password is the reference.
- **W9:** remove **Account → Share address on DESKTOP ONLY**; it stays on mobile. It does
  NOT undo C7 — after #342 the desktop Share button's only behaviour was a clipboard
  fallback, duplicating the copy button beside it. Fix = do not pass `onShare` when
  `isDesktop`.
- **W4b/A5:** he does NOT want press feedback removed. He wants a centre-out fill
  replacing the current flat tint. Removing it and rebuilding later is the wrong order.
- **iOS is parked** — no device access. This phase is punch list plus Android and
  Windows.

## ⚠ Ixian-Core was updated on 2026-08-15 — rebuild before you judge anything

Core went `0.9.8j` → `0.9.8k` (three commits, two functional). It is a SHARED-SOURCE
import with no version pin, so this changes every platform build at once. **F5 chat send
and receive BEFORE starting a feature batch**, so a Core-caused regression is attributable.

The upstream sweep is in `docs/f5-findings-2026-08-14.md`: group rename and add-member are
still NOT in Core, the engineer's newer Spixi work is not in any public repo, and the
unpinned-Core issue is worth raising with him.

## Still owed from before

1. **Android Release `PERF` numbers.** Everything measured so far is Debug on a worn
   Galaxy A52. This decides whether ANY further performance work is worth doing.
   Keep `PerfTrace` until they exist, then delete the file and its 5 call sites.
2. **iOS** — not rebuilt since #341. iOS-67 (contacts picker dead to taps) needs one
   console line in the Safari inspector. Do not fix before it runs.
3. **D1 reply-to is NO LONGER BE-BLOCKED.** The BE engineer is right that it needs no
   Ixian-Core change, and the evidence is in the findings doc: message ids already cross
   the wire (reactions) and C# already resolves a message by hex id
   (`SingleChatPage.xaml.cs:958`). ★ Reference by ID and resolve locally — never embed the
   quoted TEXT, which would let a sender fabricate a quote. Two-device test first (#215).
4. **Punch list I1 and E3.** E3 needs a device log first: the failure is a swallowed
   exception (`BackupPage.xaml.cs:154-157`) and its identity is the whole blocker.

## Rules — unchanged

Simplified Technical English · zero-C# unless evidence (#215) · ★ #221 chat isolation ·
measure-first (#294) · **bundle BEFORE shells** · smoke stays green · every fix gets a
mutation-honest pin and you must PROVE it fails when reverted · commits = Damir ·
`git --no-optional-locks` · the mount FORBIDS unlink · the Windows tree is CRLF, so
`build-demo-bundle` FAILS there — build in a cloud twin and ship a tarball · full smoke
> 45 s, so run it in the twin.

## Do not re-attempt

Parking chat WebViews · instant chrome, a delayed spinner, or a fade on the chat log ·
a skeleton anywhere without a measurement · a virtual `https://appassets…` origin ·
"harmonizing" the password decode paths (it would lock out existing users — see
`docs/security-review-for-be-engineer.md` L2).
