# Handoff — 2026-09-08 (end of Session S)

Read this, then `DECISIONS.md` **#811**, then `docs/f5-checklist-session-s.md`.
The walk sheet is `docs/walk-artifact-session-s.html`.

⚠ The prompt for this session told you to read `docs/handoff-2026-09-08.md` first. **It did not
exist** — the previous session ended at `docs/handoff-2026-09-07.md` and the file was never
written. This is it, written at the end instead of the start.

---

## The ask, and why the session did not begin by fixing it

Damir: *"Fix the remaining janky opening screens … Account — Backup, Change password,
Downloads. That's what remained."*

**Those three ARE #804.** `paneMode && ` was deleted from exactly those three guards two days
earlier, and it is in the tree, in the built shell, and in the caps C# pushes to a phone. So
the session's first job was not a fix; it was deciding whether the build he judged had the fix.

---

## Item 0 — the baseline, and one thing could not be run

Every gate that fits inside this bridge came back green and matched the prompt:

| gate | result |
|---|---|
| `build-shells --check` | ✓ every generated artifact matches a fresh build |
| `extract-strings --check` | ✓ 786 keys, 0 fallback conflicts |
| `verify-locales` | ✓ ALL LOCALES CLEAN |
| `i18n-lint` | ✓ (6 dev-only exemptions) |
| `pseudo-locale-smoke` | 9/9 |
| `build-legal-docs --check` | ✓ (privacy still 🟡 HELD, by design) |

⚠ **`smoke-test.mjs` could NOT be run.** It needs more than the three minutes this bridge
allows per call, and a backgrounded run is killed with the call that started it — verified,
twice, with `setsid nohup` and with a 180 s foreground run. **The 4621 / 4619 figures in the
prompt are therefore UNVERIFIED here.** Damir's local run is the first full one.
⚠ `cs-syntax-check` also skipped — tree-sitter is not installed in this VM.
⚠ The `Ixian-Core` sibling is **not reachable**: only `…/Spixi` is a connected folder, so the
parent has no sibling to check out. Nothing this batch touched needs it.

---

## The verdict on the three candidates

### Candidate 1 — the fix is not live in the build being judged

**REFUTED AT THE REPO. Checked four ways, because three of them could each have been the
whole story:**

1. `src/shells/settings.html` — no `paneMode &&` in the `onBackup` / `onChangePassword` /
   `onDownloads` guards.
2. The **BUILT** `Spixi/Resources/Raw/html/settings.html` carries the same three
   `bridge.cap(…)` guards, and `build-shells --check` is green, so package ≡ source.
3. `SettingsPage.xaml.cs:184` declares the caps and `:197` pushes them, **both outside** the
   `if (paneMode)` block. A phone has always been told.
4. ★ The one that could still have left the fix inert: `bridge.cap()` reads
   `SPIXI_ENV.capabilities`, which **no shell sets**. The route works only because the
   shell's own `setCaps` handler mutates `bridge.capabilities` **in place**. It does — and
   the built shell was booted and the rows tapped to prove it rather than read.

**⚠ DEPLOYMENT IS NOT SETTLED, and this session could not settle it.** There is no adb and no
Android device on this bridge — `device_bash` runs in a Linux VM with one folder mounted.

**Two facts make the stale build the leading suspect:**

* DECISIONS **#804** records Damir's own PRE-fix words: *"backup and password stutter,
  how-to-use and about open just nicely."* The new report is that same set.
* `docs/f5-checklist-session-q.md` **§3** — the paired gfxinfo capture that was to decide
  whether #804 removed the 12.19 % — **has never come back.**

**The discriminator needs no tools and it is his own iOS walk row L3:** does Backup SLIDE in
with the Account's own top bar, or does the whole screen swap to a new page? It is row **A1**
of the walk sheet.

### Candidate 2 — the fix is live and something else is janky

**Measured.** The built shell was booted from `file://` in Chromium at 432×900 @2.5 with an
Android UA, driven through the real `executeUiCommand` wire (the #804 instrument), five runs
per screen, CDP `Performance.getMetrics` around the tap. Medians, ms of engine work in the
700 ms after the tap:

| screen | script | style | layout | nodes |
|---|---|---|---|---|
| **How to use** (Damir's "just nicely" control) | 5.1 | 3.7 | 3.5 | −127 |
| **Backup** | 5.2 | 4.3 | 3.3 | −119 |
| **Change password** | 5.3 | 4.2 | 1.6 | −143 |
| **Downloads** (12 files) | **29.0** | 7.0 | 5.8 | −3 |

And Downloads scales with the list:

| files | script | style | layout | task |
|---|---|---|---|---|
| 0 | 6.6 | 3.6 | 5.0 | 36.0 |
| 12 | 25.9 | 6.3 | 2.9 | 59.9 |
| 40 | 39.1 | 12.9 | 7.5 | 93.2 |
| 100 | 58.9 | 23.2 | 11.6 | 137.3 |

★ **At zero files Downloads costs what the control costs. The whole term is the list**, and
that matches the code: `SettingsPage.loadDownloads` sends `clearFiles` then **one `addFile`
marshal per file** — each its own `EvaluateJavaScriptAsync`, each after a
`File.GetCreationTime` stat — on the UI thread, and `buildScreen('downloads')` fires
`ixian:loadDownloads` at the **start of the 300 ms slide**. The #801/#298 shape exactly.

★ **Backup and Change password show no in-shell cost above the control.** The two rows Damir
named FIRST are the two with nothing measurable; the one with a real, sized, linear cost is
the one he named last.

### Candidate 3 — platform-specific

Untested. iOS passes L3 today, so nothing shared may be changed without re-walking there.

---

## What was built — and what deliberately was not

**Built (1): the instrument.** `[CDPERF] settings-sub` in `src/shells/settings.html`.

* `build` — the synchronous render, measured in the microtask after the click task.
* `paint` — tap → the committed frame (the double rAF `bridge.painted` already uses).
* `list` — Downloads only: tap → the settled list, so the C# round trip is its own term.

ONE hook, on a **view change** inside `renderLayout` — not a call in each of the twelve
`show*` setters (#798) — and `console.warn`, never `.info`, because the Android WebView drops
INFO and that is what cost Session J its numbers (#751).

**RUN before it was pinned:** all four sublevels tapped in Chromium on the built shell.
It stamps `build`+`paint` on every open, `list=169ms n=12` for Downloads, **nothing on the way
back to the hub, and nothing on a same-view re-render.**

**Built (2): GATE 44** — the pin #810 asked for, and it EXECUTES. See below.

**NOT built, deliberately (#294):**

* Nothing for Backup or Change password. No measurement named a mechanism.
* The Downloads **batch transport** — one `addFiles(json)` push with `clearFiles`/`addFile`
  kept live, the #801 both-transports pattern. Specified and ready. **Gated on one fact
  nobody has: how many files are in Damir's Downloads** (`n=` on the probe's `list` line). At
  zero it fixes nothing he can feel.
* The cheaper half of the same idea, recorded with it: **defer `ixian:loadDownloads` to the
  END of the slide** so the whole round trip lands outside the animation window. Two lines,
  no transport change. Also not shipped on a feel.

---

## ★ The instrument lied twice before it told the truth

Carry these — all three are the project's own recurring shapes.

1. **The first run reported How-to-use and Change password as perfectly clean** — zero style,
   zero layout, zero nodes, zero janky frames. The clicks **never opened anything**: headless
   Chromium defaults to `prefers-reduced-motion: reduce`, and Playwright's real mouse click
   did not reach those two rows. *A measurement with no assertion that its subject changed is
   the same defect as a pin with no mutation.* The harness now refuses to report a screen it
   cannot prove opened.
2. **The Downloads figure was inflated by `btoa` calls made inside the page.** C# does that
   encoding natively. Precomputed outside and re-measured, the number barely moved — which is
   itself the finding.
3. **The frame-gap column is declared untrustworthy.** `maxGap` came back at exactly 50.1 ms
   for Backup whether its illustration was the real 640×640 PNG or a 1×1 one. **The image
   hypothesis is REFUTED** and the gap is a headless artefact. Only engine-work columns are
   quoted anywhere in this batch.

---

## GATE 44 — a gate that cannot run must fail where the suite can see it

`strip-release.mjs`'s own docblock has said *"⚠ OWED: a pin"* since 2026-09-07. This is it.

* **(a) EXECUTES** the script through a **symlinked path** with no arguments: a working
  main-module guard prints its usage and exits 2; the W-R6 guard exits **0 in silence**. A
  direct invocation is the control. The link is a `'junction'` — Windows makes those without
  elevation, POSIX ignores the argument — and if it cannot be made **the pin FAILS and says
  why**, because an unrunnable gate is a red one, not a skipped one.
* **(b) WALKS** every `.mjs` under `scripts/` carrying a main-module guard and requires
  realpaths on both sides, with the premise asserted that the comment strip removed
  `strip-release`'s own docblock **quotation** of the broken form.

★ **The pin's first draft convicted THIS FILE.** The walk matched the raw token, and clause
(a)'s message named both tokens forty characters apart. **#771 in a third costume: the prose
is the pin's input, so the prose is part of the pin.** The predicate is proximity now, the
message names neither token, and the comment says so.

**Nine mutations, zero survivors:** the W-R6 guard reintroduced verbatim (both clauses red),
realpath on one side only (both red), the guard deleted so the CLI always runs ((a) green,
(b) red — the pin notices its own subject vanishing), and five over the probe including the
one that matters: **a source that has the probe and a BUILT shell that does not.**

---

## ★★ Then M13 — and the roadmap row was stale (#812)

`release-readiness.md` §2 calls the i18n residual "the most user-visible item on this page …
ours and buildable now", from a Damir F5 of **2026-07-11**. It was not built against.

**The instrument:** every BUILT shell booted under **`?lang=pseudo`** — the marker locale the
project already ships, where a string that went through the dictionary renders `⟦key⟧` and
anything in plain English **never did**. Visible text only, plus `placeholder` / `aria-label` /
`title`, one click deep. The allow-list is DERIVED (endonyms read out of `flags.js` at run time)
and its two scope carve-outs carry their reasons. A second pass **feeds data** through the real
`executeUiCommand` wire, because the surfaces Damir named render nothing until C# pushes.

**Verdict: no visible English left on those surfaces.** M13's code half was closed by **#269 on
2026-07-12 — the day after the F5 that recorded the residual** — and the row was never updated.
#269's own text is the residual list: *"the whole WALLET TAB rendered English under any locale …
+ hardcoded nav/topbar/FAB chrome threaded"*. What remains is the **translator pass**: 15
(ru-ru) to 76 (it-it) still-English keys per locale. Not code.

⚠ **The near-miss.** A `?lang=de-de` cross-check flagged "Chats", "Apps", "Wallet", "Status" —
and every one is a **correct German translation identical to the English**. Only the marker
locale separates *never looked up* from *translated to the same word*. Nothing from that pass is
quoted anywhere.

**What the sweep DID find, in every shell:** the static a11y labels in boot markup — 16 ×
`aria-label="Loading"`, the chat log's `"Messages"`, `"New chat"` — plus `createTopbar`'s
`backLabel` and `createBottomNav`'s `ariaLabel`, which defaulted to the English **literal** and
which almost no caller passes. **A German user's screen reader said "Back" and "Loading" while
the dictionary held "Zurück" and "Wird geladen"**, on Settings, Downloads, Contributors, Add
contact, Dev, Backup and every Account sublevel. Invisible to both gates: `i18n-lint` sweeps DOM
sinks, not attributes; `extract-strings` sweeps `strings.x || '…'`, which markup cannot be.

**One mechanism, not eighteen edits:** the two component defaults read the live dictionary via
the `strings = getStrings()` param form *the extractor already sweeps* (`getStrings().back`
would have been correct and still invisible); the markup labels carry `data-sl-aria` beside the
English fallback and one boot walk per shell swaps them. ⚠ **`extract-strings` was taught the
pair** — a mechanism the sweep cannot see is a key that can never be translated, which is the
class of defect being closed. 786 → **788 keys**, 0 fallback conflicts.

One carve-out, `empty_detail.html`, **with its premise asserted** rather than skipped. And its
two English sentences were **refuted** as a leak on the way: `*SL{}` carriers, present and
translated in all 13 `lang/*.txt` files — checked, not assumed.

**GATE 45** is a walk over all 18 shells. **Six mutations, zero survivors.**

## Numbers at close

bundle **321 exports** · **18 shells**, all rebuilt · `build-shells --check` ✓ · locales
**788 keys, ALL CLEAN** · i18n-lint ✓ (6 dev) · pseudo **9/9** ·
`extract-strings` / `build-shells` / `build-legal-docs` `--check` ✓ · NUL sweep clean on every
touched file (checked with `tr -dc '\000'`, with a control — ⚠ `grep -c $'\0'` is VACUOUS,
it matches every line) · **+4 smoke assertions expected (4621 → 4625)**, unverified here. **15 mutations across the session, zero survivors.**

---

## Files

New: `docs/handoff-2026-09-08.md` · `docs/f5-checklist-session-s.md` ·
`docs/walk-artifact-session-s.html` · `docs/commit-message-session-s.txt`.
Changed: all 17 `src/shells/*.html` except `empty_detail.html` · `src/components/topbar.js` ·
`src/components/bottomnav.js` · `scripts/extract-strings.mjs` · `scripts/smoke-test.mjs` ·
`src/strings/en-us.json` + the 12 locale files + `src/strings/draft/` · `src/demo/spixi.iife.js` ·
`src/demo/strings.iife.js` · `docs/i18n-strings.md` · all of `Spixi/Resources/Raw/html` (rebuilt) ·
`DECISIONS.md` (#811, #812) · `CLAUDE.md` · `docs/next-session-prompt.md`.

⚠ `git add` the four new docs. Never `git add -A` — CRLF churn on ~116 files. The commit is
Damir's, in GitHub Desktop. Plain `git status` strands a 0-byte `.git/index.lock` on this
mount; use `git --no-optional-locks`.

---

## Still owed, unchanged by this session

* **The memory kill** — Android kills the app after ~30 minutes, 511 MB max, on a RELEASE
  build. The instrument is `adb shell dumpsys meminfo com.ixilabs.spixi.dev` (read the
  FOOTER), the suspect is `SpixiContentPage.Dispose()` doing all its teardown inside
  `if (!Navigation.NavigationStack.Contains(this))`, and the free discriminator is
  `CHAT_SPARE_ENABLED = false`. **This is the only thing a user is losing work to.**
* A **seventh #46 round** over Session R's `LOOPFIX8` / `LOOPFIX9`. CLEAN was never claimed.
* The **123 naive comment-strip regexes and 26 local strippers** still in the suite (Session
  R's "next session's first list" — still not done).
* Release-readiness §3: ~~the i18n residual (M13)~~ — **its code half is closed, see #812; the
  translator pass remains and it is Damir's** — the Add contact / Add app regression, the
  `maxLogCount = 5` release blocker, the held Privacy Policy.
* ⚠ **`docs/polish-roadmap.md`'s M13 row and `docs/release-readiness.md` §2's M13 bullet are
  both STALE** and were left as they are, because rewriting another session's record without
  the walk to back it is how #660 happened. #812 is the correction; fold it in when M13 is
  formally closed.
