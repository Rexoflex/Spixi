# Handoff — 2026-09-17, after Session X

Records: `DECISIONS.md` **#869–#877** (540 rows). Paste + walk: `docs/f5-checklist-session-x.md`.
Commit message: `docs/commit-message-session-x.txt` — commit is Damir's, ONE batch, GitHub Desktop.
The next build is fully specified: `docs/contact-details-premium-proposal.md` (every fork picked).
Consumed → `docs/archive/`: `handoff-2026-09-16b.md` · `handoff-2026-09-16c.md` · the U/V/W checklists ·
the U/U2/V/W commit messages.

## What changed about the WORKSHOP

★ **`device_bash` WORKED all session** (node 22.23, git, npm on the VM) — #860 was a state, not a law;
check it first. Generators and per-pin harnesses ran on the real tree; the full suite still exceeds
one bridge call (180 s) and a detached `setsid nohup` run DIES with the call (zero lines) — the full
run is Damir's, its count predicted. Renders: stage the BUILT shells + the six co-located `spixi.*`
files into the container and boot them in Chromium through the C# wire (§3).

⚠ Two workshop lessons this session paid for: (1) `npm i --no-save <pkg>` PRUNES every other
`--no-save` package (no package.json protects them) — install `jsdom eslint globals` together;
(2) **run the suite's EXISTING gates on a change, not only your own pins** — the Session N
reachability gate failed Damir's run on my SVG-first art rung, which my harness never saw.

## §1 answers (Damir)

| ask | answer |
|---|---|
| suite line after the W paste | **BASELINE OK / the 2 KNOWN** — first clean baseline since 09-10 |
| paste step 1 (the zip) | ran — `--check` green on disk, no `<metadata>` stamps |
| the two commits | ONE (`e3b7ca17`, icons inside) — recorded |
| S1 | "P" = ran fine; **not timed** → #864 stays gated, the spare stays |
| the walks | V/W/U all reported: 17 P · 2 F · 1 N/A — the two F were the chooser in dark (#870) |
| X deploy | deployed the same day; the transparent `add-contact.png` is IN (X.9 clears on the next build) |

## What landed (uncommitted; every file re-read after write)

| file | what | verified how |
|---|---|---|
| `src/shells/chat.html` | #869: `--composer-h` target from the slot, `publish()` before the observer, the throw logged | built-shell boot pin reads the PROPERTY: HEAD red (`""`), fix green (`0px`); 3 mutations killed |
| `scripts/smoke-test.mjs` | +9 `ok()`: composer-h boot · undeclared-identifier gate + non-vacuity · art ladder (png → glyph + file-exists) · sheet-card property · chooser rule · row column · **ticks weight-free (#877, inverted)** · read contrast (every declaration) · header install line | each new-vs-committed in a harness; 15 mutations killed across the session; one of my pins was vacuous and rewritten (#871) |
| `src/styles/tokens.css` | `--surface-sheet-card/-hover/-pressed` both blocks · `--icon-bubble-read: #8ff5b4` both blocks | resolved-primitive lift pin · computed-contrast pin |
| `src/styles/components/contacts-shell.css` · `contacts-shell.js` | chooser cards on the pair, real hover + elevation-2, no hairline; art = `add-contact.png` → glyph (SVG rung reverted) | rendered light + dark (`sheets/session-x/chooser-after.png`); ladder exercised in jsdom |
| `src/styles/components/chatlist-item.css` | trailing column = two line boxes, always both tall | MEASURED 0.0 px time-to-name on 6 rows (`chats-row-after.png`) |
| `src/styles/components/message-bubble.css` | comment only after #877 | — |
| `src/demo/spixi.iife.js` · `Spixi/Resources/Raw/html/*` | rebuilt (bundle 317 · 18 shells) | `build-shells --check` ✓ · NUL 0 |
| `DECISIONS.md` #869–#877 · `CLAUDE.md` · `docs/security-handover-gate.md` (Session X) · `docs/security-review-for-be-engineer.md` (§1a CLOSED) · `docs/release-readiness.md` (#234 + Privacy re-synced) | — | — |
| `docs/contact-details-premium-proposal.md` + `docs/sheets/session-x/` (6 PNG) | the proposal, mock rendered; ⚠ each PNG carries a bridge `caBX` C2PA chunk (#865 extends to PNG) | — |

`_to_delete/` holds the session's `.tmp.mjs` harnesses + one temp built shell — `Remove-Item` in the paste.

## §2 · The decisions, one line each

#869 the composer regression + the class gate · #870 chooser tokens · #871 row column + read contrast ·
#872 bleed read (S1 untimed · one commit · bridge works · PNG stamping) · #873 group/bot destructive rows
ABOVE the roster · #874 two stale OPEN ledger rows closed (#234 → #458; Privacy bakes) · #875 the picks
(contact details ONLY · Mute tile · address under the name) + **bleed DROPPED** · #876 action row = max four
rounded-rect neutral tiles, Request out · #877 ticks ONE weight, colour only.

## §3 · The render harness (container)

`/tmp/x/render/harness.mjs` — `playwright-core` over `/opt/pw-browsers/chromium-1194`,
`--allow-file-access-from-files`, boots `/mnt/user-data/uploads/…/Raw/html/<shell>.html` at 432×900
@2.5 (Android UA, `isMobile`, `hasTouch`), theme via `spixi.appearance` in an init script;
`push(name, ...args)` = `executeUiCommand(window[name], ...base64)`. Grammar used:
home `clearChats · addChat(wallet, from, ts, avatar, online, excerpt, type['read'|'confirmed'|'pending'|''], unread, kind['group'|'bot'|'False'], mention) · clearChatsDone · setChatMuted`;
chat `onChatScreenReady · setNickname · setChatMode('0',…) · clearMessages · addMe/addThem(id, addr, nick, avatar, text, ts, sent, confirmed, read, paid, err) · addCall · onChatScreenLoaded`;
contact_details `setContext · setAddress · setNickname · showIndicator · showCallButton · setContactNotifications · setSharedGroups · clearRecentActivity · addPaymentActivity` (+ `setGroupInfo · clearMembers · addMember` for a group).
The home FAB needs `document.getElementById('fab').click()` (pressable.js swallows a synthetic tap).
The shells embed Sora/system-ui — renders are for geometry and colour, not type. The mock in
`details-mock.png` is injected CSS + a DOM reorder over the real shell (`run6.mjs`), not a build.

## §4 · Research brief (behind the proposal) — sources

Telegram iOS `PeerInfoHeaderNode.swift` / `PeerInfoHeaderButtonNode.swift` (avatar 100 pt; tile 40/11,
radius ≤16, tile = list block colour) · Signal-Android `IndividualSettingsScreen.kt` /
`ConversationHeader.kt` (avatar 80 dp; 5 ActionButtons 32 dp apart; Block/Report last after a
divider) · WhatsApp contact-info redesign (Android Police, Beebom, Smartprix) · Telegram blog Oct 2025
(user-chosen cover) · Apple HIG dark-mode base/elevated backgrounds · Material metrics & keylines ·
Muzli "7 tiny UI fixes" · "Red in UI design" (Bootcamp). Rules synthesised in the proposal §2.

## §5 · Open, in order

1. **Damir**: the X walk (checklist §2; ticks colour-only) · commit · **S1 timed**.
2. **The contact-details premium batch** — fully decided (#873/#875/#876), FE only, ★ #221 untouched.
   Render both themes through the wire before any pin; the #46 loop on Opus after.
3. **The #46 Opus loop over Session X** (#869–#877) — adversary the undeclared-identifier gate's
   `shellGlobals` heuristic (a false RED is loud, not silent — but prove it).
4. Then: #864 after S1 · the settings swatch mask (#866) · AND-40 · AND-36/39 · the freeze (#825) ·
   the BE cutover (walk him through `security-review-for-be-engineer.md` first — §1a is CLOSED now).
5. ~~Bottom bleed~~ — DROPPED (#875); the recipe stays in #872 ① if the dial ever reopens.

## §6 · Tester-readiness, as of tonight (from `docs/release-readiness.md`, re-synced)

The gate for a closed tester group is three things: the **#232/#523 money-path review** (or a one-line
cap gate on `composeSend`/`composeRequest` for the tester build) · **`maxLogCount` 5 → 1** ·
**retiring the probe sets** (they log; they do not cost battery — bounded from the code, #872-era read).
Plus an iOS walk if testers get iPhones (nothing since ~08-27 has been on one). Battery: Spixi Dev
read ~0.14 %/screen-min vs ~0.06–0.07 for the messengers on the same phone — Debug build, walk usage;
the only number that settles it is Release vs store Spixi, same phone, same 30-minute script.

## Rules that earned their place

#215 (a "closed" constraint was open; a "P" carried no number; a "needed" claim of mine was wrong) ·
#294 (the bleed was read, not built) · #660 (two OPEN ledger rows were long closed) · #771 · #798 (my
contrast pin proved one declaration and claimed two — its own mutation caught it) · ★ #869: a
`catch (_) {}` around a one-line write is a feature that silently never runs · ★ run the suite's
existing gates on your change, not only your own pins.
