# Handoff — 2026-08-17b. READ FIRST.

**LANGUAGE RULE: ASD-STE100 Simplified Technical English.** See `CLAUDE.md`.

Supersedes `docs/handoff-2026-08-16d.md` for state (banner added; archive it with the
next batch). This file is named **-17b** because `docs/archive/handoff-2026-08-17.md`
is an OLD file from 2026-08-13 — the archive slot is occupied. **Read dates in
headers, never file names.** This batch archived `handoff-2026-08-16c.md`.

★ **Planning doc:** `docs/master-worklist-2026-08-17.md` — Damir's two lists
reconciled into **N1–N45** + 9 rounds. Reference N-ids from now on.

---

## 1. State — verify it, do not trust it

```
git --no-optional-locks log --oneline -3
git --no-optional-locks status --porcelain
```

Damir commits the #356–#360 batch himself (title + body were handed to him — the
commit should be at HEAD when you read this; VERIFY). Both targets **COMPILED AND
RAN**: Android Release on the A52 and Windows Release from a clean (wiped) output —
the uncompiled-C# risk for #356–#360 is retired.

| Item | Value |
|---|---|
| Smoke | **BASELINE OK — 1703 pass / the 4 KNOWN pre-existers (#136 · #149③ · M5 · B3)** |
| F5 (2026-08-17, Windows + A52) | **PASS:** D-20 (both platforms) · I-2 · I-6 (incl. IME leg 27 + decimal leg 28) · 2.3b **CLOSED** (clean build, no tonal hover — the residual is retired) · **FAIL:** D-19 display dial + D-17 (see §2). Legs 29–30 (compact-balance, flick-cancel) untestable, still open |
| Loop record | In DECISIONS #356–#360. Note for humility: loop r2 MINOR-7 predicted the D-17 device failure exactly; the builder logged it as an acceptable trade and the device disagreed |

## 2. ★★ THE TWO REWORKS — Damir's dials are LOCKED, build in the fix session

### D-19b — NO "Hidden member" in bot rooms (`docs/f5-findings-2026-08-15.md` D-19b)

Damir: a bot group is ALWAYS nickname and/or address. The dial:

1. Label = nickname when known · truncated address when known · **NOTHING when
   neither** (legacy parity). Delete the "Hidden member" placeholder from every
   NON-BLIND surface (bubble label, copy path). Blind chats keep it.
2. **Reverse-resolve** the address for named-but-address-less rows: the roster
   (`friend.users`) still maps address → nick, so C# `insertMessage` (and/or the
   shell via `groupNicks`) looks up the nick and passes the address — **ONLY on an
   exact SINGLE-nick match**. A collision would hand out a copyable address / tip
   target for the wrong person: money-safety, so ambiguous = no address.
   This restores the member sheet, copy, and tip for every roster-known sender —
   the functionality Damir called "completely gone".
3. Keep from #356: no group-name impersonation · no group avatar on strangers'
   rows · nick-keyed bubble runs · the NRE guard · the empty-slot C# contract.
4. Retire/replace the affected pins honestly (placeholder pins die, reverse-resolve
   pins are born; count the delta).

### D-17b — the apps search bar is ALWAYS visible

The watchdog-vs-gate flash fired on BOTH platforms (first push lands later than the
window — the loop r2 MINOR-7 sequence, proven on device). Damir's dial supersedes
the whole mechanism: **the search + layout row never hides** — remove
`data-unknown`, the watchdog, AND the `data-empty` row-hiding (`setAppsHeaderEmpty`
keeps only whatever non-row duties it has; check before deleting). Nothing can
flicker if nothing ever hides. Retire the #359 pins honestly.

## 3. NEXT SESSION — the cheap batch (NO Damir input needed)

Every item below is an explicit Damir ask with the dial already given. Verify each
against code first (#215), pins + loop as always. If ANYTHING turns out to need a
dial — log it, do not build it.

| N-id | Item | Pointer |
|---|---|---|
| N5 | "Delete data" card responsive (truncates small screens; clips narrow desktop) | settings danger card CSS |
| N22 | Private-group topbar: member count under the name (bot parity) | topbar sub-line push, C# + shell |
| N24 | Desktop apps: highlight the list row whose details pane is open | apps list `data-selected`, chats-row precedent |
| N32 | Absolute-zero balances show `0.00` (IXI + fiat) | money.js dial + pin; #76 trim exception |
| N36 | Select mode: no pressed flash on select/deselect taps — selected color only | pressable opt-out in select mode |
| N38 | Hide the dead desktop Share buttons (wallet receive + account address row) | desktop-gate, `isDesktopPresentation` |
| N2a | Apps list⇄grid toggle uses a REAL grid glyph (not the apps icon) | apps-header.js `icon('apps')` |
| N3a | Em-dash sweep: replace — with simpler copy in ALL user-facing strings + locales | i18n-strings + verify-locales |
| N45 | Wire the saved PNG illustrations from `images/` (incl. `rate-me` on the rating nudge) — pick PNG vs SVG per asset by actual bytes | the untracked `images/*.png` on Damir's disk — they are NOT in the twin; stage what the build needs or work by name |
| N14a | Rating nudge uses the rate-me illustration (rides N45) | nudge component |

⚠ N45 caveat: the PNGs exist only on Damir's PC (untracked). If the session cannot
reach them, build the WIRING (paths + fallbacks) and let the files land at commit.

## 4. LATER-TODAY SESSION — the fix batch (Damir present)

D-19b + D-17b (§2), then F5 re-check of both + the two open legs (29–30) if the
wallet/chats state allows. After that the plan resumes: **D-5 → D-9 → D1** (16d §3),
then the master-worklist rounds (R1 identity first).

## 5. How to work — unchanged

Cloud twin recipe as in 15c §7 (archive) — clone, `npm install jsdom --no-save`,
bundle → shells → smoke; tarball delivery; PowerShell rules; the mount cannot
unlink (Move-Item works). Standing set: verify plans against code · #294 · #215 ·
smoke green at **1703/4** (the reworks will move it — state the new number) ·
mutation-honest pins, proven · Damir commits · DECISIONS rows at decision time ·
the #46 loop on anything substantial · **Opus for every adversarial pass (#352)** ·
the security handover gate while building. Ixian-Core reference: clone + checkout
`097341a` in the twin.

## 6. Do not re-attempt

All of 16d §6, plus:
- **Do not bring back ANY hide/show mechanism for the apps header row.** Damir's
  dial is always-visible. The flicker class dies by subtraction, not by a cleverer
  latch (two attempts have now failed: the pre-#359 gate hide and the #359 ghost).
- **Do not reverse-resolve a nick to an address on a MULTI-match.** One nick, many
  members is normal in a 10k room; a guessed address on a money path is worse than
  no address.

## 7. Open device items

| ID | Finding | State |
|---|---|---|
| D-19b | Bot-room label dial rework | 🔴 OWED — fix session (§2) |
| D-17b | Always-visible search bar | 🔴 OWED — fix session (§2) |
| 2.3b | Windows tonal hover | ✅ CLOSED (clean-build F5 2026-08-17) |
| D-20 / I-2 / I-6 | This batch | ✅ CONFIRMED on device (both platforms where applicable) |
| N40 | "Connecting…" missing on a LIVE document after long offline use | OPEN — triage (suspect: update-branch starvation, see #357 loop notes) |
| Compact-balance · flick-cancel | A52 legs | OPEN — untestable this round |
