<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
	<application android:allowBackup="true" android:supportsRtl="true">
		<provider
		   android:name="androidx.core.content.FileProvider"
		   android:authorities="${applicationId}.provider"
		   android:exported="false"
		   android:grantUriPermissions="true">
			<meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
		</provider>
		<!-- AND-13 (#334): accent for OneSignal SDK-rendered notifications (background
		     pushes bypass SPushService.showLocalNotification and its SetColor) —
		     ARGB hex, no '#', per OneSignal convention. FF3050BD = the brand accent
		     (logomark fill / tokens.css brand-600), same pick as builder.SetColor.
		     The small icon for that path is ic_stat_onesignal_default (now supplied
		     at all 5 densities). -->
		<meta-data android:name="com.onesignal.NotificationAccentColor.DEFAULT" android:value="FF3050BD" />
		<!-- ★★ #503: the ONLY hook OneSignal gives us for a BACKGROUND or killed-app push.
		     `WillDisplay` (#493) is a foreground lifecycle listener and never fired for the
		     case it was written for — the mute, the global master and the one-row-per-chat id
		     all belonged to code that never ran. The SDK looks this class up BY NAME and
		     instantiates it itself.
		     ⚠ The value must match `[Register]` on SpixiNotificationServiceExtension.cs
		     exactly. If they drift the SDK simply never finds the class — no error, no log,
		     the lane silently dead again. A smoke pin asserts the two agree. -->
		<meta-data android:name="com.onesignal.NotificationServiceExtension" android:value="com.ixilabs.spixi.SpixiNotificationServiceExtension" />
	</application>
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
	<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
	<uses-permission android:name="android.permission.WAKE_LOCK" />
	<uses-permission android:name="android.permission.CAMERA" />
	<uses-permission android:name="android.permission.RECORD_AUDIO" />
	<uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
	<uses-permission android:name="android.permission.USE_FINGERPRINT" />
	<uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

	<uses-feature android:name="android.hardware.audio.low_latency" android:required="false" />
	<uses-feature android:name="android.hardware.audio.pro" android:required="false" />
	
	<uses-permission android:name="android.permission.CAPTURE_AUDIO_OUTPUT" />
	<uses-permission android:name="android.permission.CAPTURE_SECURE_VIDEO_OUTPUT" />
	<uses-permission android:name="android.permission.CAPTURE_VIDEO_OUTPUT" />
	<uses-feature android:name="android.hardware.camera" android:required="false"/>
	<uses-feature android:name="android.hardware.camera.autofocus" android:required="false"/>
	<uses-feature android:name="android.hardware.microphone" android:required="false" />

</manifest>