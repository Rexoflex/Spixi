# Handoff — 2026-08-15c. READ FIRST.

**LANGUAGE RULE: ASD-STE100 Simplified Technical English.** See `CLAUDE.md`.

Supersedes `docs/archive/handoff-2026-08-15.md` for state.

**This is the ONLY handoff in `docs/`.** Everything consumed is in `docs/archive/`, with its
filename unchanged, so any older reference still resolves by name.

## ⚠ If you go digging in the archive, three file NAMES lie

`docs/archive/handoff-2026-08-16.md`, `-17.md` and `-18.md` are **OLDER** than this file.
All three were written on **2026-08-13**. Their names look newer and they are not, and the
"CONSUMED / this file is the live state" line at the top of each one is stale. **Read the
DATE IN THE HEADER, never the file name.**

The live chain that ends here:
`handoff-2026-08-14c` → `handoff-2026-08-15` → **this file**.

---

## 1. State — verify it, do not trust it

Run these two yourself before you plan anything:

```
git --no-optional-locks log --oneline -3
git --no-optional-locks status --porcelain
```

| Item | Value |
|---|---|
| Cloud twin base | `35c97dc` — *"#346 + #347 - the owed review loop, and the desktop add-contact pane"* |
| Damir's local HEAD at the START of the session | `f11286ae` ⚠ **not the same hash as the twin base. Confirm which commit his tree really holds.** |
| Batch **#348** | Applied to Damir's disk from a tarball. **NOT committed** at the time this was written. |
| Batch **#348b** | This batch. Ships as a second tarball on top of #348. |
| Smoke | **BASELINE OK — 1627 pass / the 4 KNOWN pre-existers (#136 · #149③ · M5 · B3)** |
| Locales | `verify-locales.mjs` exits 0. **ALL LOCALES CLEAN ✓** |
| Compiler | ⚠ **NEVER COMPILED in the cloud.** There is no .NET in the container and the install is blocked. Damir's build is the FIRST compiler pass over the C# in #348 and #348b. A brace and paren balance scan ran and gives (0, 0, 0) on all five modified files. That is not a substitute for a compiler. |

### The Windows tree is mixed CRLF

`scripts/build-demo-bundle.mjs` cannot parse a CRLF component, so it fails on Damir's PC.
**Build and test in a cloud twin.** See §7.

---

## 2. ✅ THE CORE GATE — PASSED

Ixian-Core moved `0.9.8j` → `0.9.8k` (`097341a`, plus `5643e5b` which changes what 1:1
messages STORE, and shipped under the message *"minor improvement"*).

Damir rebuilt the **clean** tree, with no batch applied, and did an F5 pass.
**1:1 and group send and receive were correct.** The gate passed.

The ordering did its job and repeats on every Core bump.

### ⚠ BUT THE GATE WAS NOT WIDE ENOUGH — see D-14

Later the same day, tipping a person in a BOT room failed every time. The cause is
`5643e5b`, in this very bump: it nulls the sender address for `FriendType.Normal`, and a
bot chat's friend IS `FriendType.Normal` with only the `bot` FLAG set. So `0.9.8k` silently
drops the sender address on every bot-room message.

**Send and receive stayed correct**, which is exactly why the gate passed. The loss is
invisible until something needs the SENDER.

★ **Add one step to the gate on the next bump: tip a person in a bot room.** More generally,
a gate that only tests the happy path of the thing that changed will keep missing this
class.

⚠ **`Spixi.csproj:304` still has NO version pin.** Core is a shared-source import from a
sibling folder. Damir's git correctly reported "no changes" while the compiled app changed
under him. This is on the BE engineer list.

---

## 3. What is on Damir's disk

### Batch #348 — the F5 batch (already applied, NOT committed)

W15 · W14 · W2 · A9 · A7 · W4a · W9 · W11 · W10 · W8 · A5 · W1. See DECISIONS **#348**.

### Batch #348b — the cheap-fix batch (this tarball)

| Item | What changed | Files |
|---|---|---|
| **I-8** | The press fill dial Damir asked for: the fill colour is the SELECTED colour (`--surface-action-tonal-default`), the fill is slower (`--duration-300` open), and the flat pressed tint is gone | `base.css` + 7 row components |
| **D-10 / I-7** | The tip sheet WAITS for a real answer. New `setTipResult` push, a `tipResult` capability gate, message-id correlation, a 12 s backstop timer, an inline failure body, and a success toast that restates the amount | `SingleChatPage.xaml.cs`, `chat.html` |
| **D-11** | Resolved BY DELETION. The alert that named the GROUP is gone, so the nick ladder written to fix it was dead code and was removed | `SingleChatPage.xaml.cs` |
| **D-9②** | A fatal start error can announce itself. `safeFatalAlert` logs first, then tries the alert inside a try/catch | `HomePage.xaml.cs`, `App.xaml.cs` |
| **I-5** | Only the Chats logotype keeps the accent colour. Every other screen title is `neutral-01` | `topbar.css`, `topbar.js` |

### Documents

- `docs/f5-findings-2026-08-15.md` — **the work list.** D-1…D-12 and I-1…I-9, each with
  file:line evidence, an owner (FE / C# here / BE-Core) and an effort. **Do not re-derive it.**
- `docs/archive/f5-checklist-348.md` — the device checklist for #348. **Consumed** — the F5 ran on 2026-08-15 and its results are in §4.
- `docs/security-handover-gate.md` — the introduced-vs-inherited rows for #348 and #348b.
- `DECISIONS.md` — rows #348 and #348b.

---

## 4. What the F5 pass found, and where each item stands

Damir tested on **Windows** on 2026-08-15. Android was **not** tested.

### Fixed and confirmed on device

| Item | Result |
|---|---|
| **W14** delete account | *"the actual function it worked flawlessly no hang"* |
| **W2** Account auto-save | Works. |
| **W8** tip in bot groups | Works. |
| **A5** press fill | Works, and Damir dialled it → **I-8**, built in #348b. |
| **W4a** tx row selected colour | Works. Damir: the tx row must behave the same as the chat row. It does. |
| The visual set | Works, except the items below. |

### Found during the pass, built in #348b

**D-10**, **D-11**, **I-7**, **I-8**, **I-5**. See §3.

### Found during the pass, NOT built

| ID | What | Why it is open |
|---|---|---|
| **D-9** ★★ | Delete account left the app UNBOOTABLE — a black screen on the next start, and Restore then said "account exists" with no way forward | The alert-safety half (**D-9②**) is built. The rest needs a device test. Damir recovered by moving `Documents\Spixi` aside and restoring the account. |
| **D-6** | Account tier: an account is WALLET + CONTACTS. The copy must say so, and "delete wallet" is probably redundant | **Damir is asking the engineer.** Do not build until he answers. |
| **D-7** | Restore copy | Needs a string in 13 locale files. |
| **D-12** | *"An unknown error occurred while sending your tip."* | Needs a C# string, and it needs BE item 5 (see below) to know what the error MEANS. |
| **D-1 · D-3 · D-4** | Avatars after restore · group lifecycle · kick does not stick | **BE / Ixian-Core.** |
| **D-10 residual** 🟡 | A throw AFTER `friend.addReaction` leaves a tip pill on a message whose sheet says the tip failed | Needs a remove-reaction API (BE item 5) and a 13-locale string. Logged in the findings under D-10 and in the code at the `catch (Exception tipEx)`. **INHERITED** — the sequence is legacy. |
| **D-14** ★★ | Bot-room messages lose the sender address, so tipping a person there always fails | **Ixian-Core `0.9.8k` REGRESSION**, commit `5643e5b`. One word fixes it: `&& !friend.bot`. **BE item 1.** Found at F5 on 2026-08-15, after the batch. Not ours |
| **D-15** | No "Leave" on BOT rooms; and a bot room wrongly offers "Remove contact" | FE, small, `chat-info.js:823` and `:831` — but ASK first whether `ixian:leave` works for a bot room. D-4 is the precedent for not promising what the app cannot keep |
| **D-13** | NRE at launch in `PendingMessageProcessor.removeMessage` | **Ixian-Core, pre-existing at `0.9.8j`.** First-chance only; the app continues. One null check. **BE item 8** |
| **D-16** ★ | The press fill does not finish on a fast click, and the row lands two ramp steps brighter — on the primary BUTTON colour | **OURS, FE only.** Found at F5 with a screen recording; frames 100 ms apart show the sweep frozen at 65% and then the full row two ramp steps brighter. **Damir's call: log it, commit #348b as it stands, build it next.** The current fill is still better than the flat tint it replaced, so waiting regresses nothing. It changes the ONE shared press mechanism, and #343 is the precedent — it needs the #46 loop and mutation pins |
| **D-17** | The Apps search field appears then disappears on the first visit with an empty list, and the rows jump | OURS, FE, small. Read the #340 C-MAJOR-1a/b/c latch rows first — same surface |
| **I-10** | Opening a new app pane feels slow | **MEASURE FIRST (#294).** One `PerfTrace` reading, the way #345 was measured. #340 BUG-2② is the prime suspect: it already took a 5-icon decode from 108 ms to 3.4 ms |
| **I-1** | Slide and fade the chat in | ★ **MOBILE ONLY** (Damir, 2026-08-15). On desktop the chat is a pinned pane in a grid column, not a pushed page. Gate on the PLATFORM, not the width and not `DeviceInfo.Idiom`. |
| **I-2 · I-3 · I-6 · I-9** | Chip outline · cover gradient · digit grouping in the tip amount · chat-info skeleton | Sized and evidenced in the findings. I-3 needs a design round. I-9 must be measured first (#294). |
| **Lock on background** | The lock is still overridden with Cancel on background and on back | Already logged before this session. |
| **Welcome flicker** | Welcome → Restore or Create flickers | Same lever as I-1 (the native page transition). Build them together, once. |

---

## 5. ⚠ OWED — the Android pass

**Damir could not test Android this session.** Three items in #348 and #348b have **never
run on a touch device or on Android**:

1. **A9** — Android landscape Account. The branch is gated on
   `DeviceInfo.Platform == WinUI || MacCatalyst`. `DeviceInfo.Idiom` was rejected because it
   is posture-dependent: a Surface with the keyboard detached reports Tablet, and Mac
   Catalyst reports Tablet. **This needs a real landscape check.**
2. **A7** — the FAB residue.
3. **A5 / I-8** — the centre-out press fill on a TOUCH device. Windows tested it with a
   mouse only. `:active` and `data-pressed` land together on Chromium, and touch has a
   different press-and-hold profile.

Also owed and still not measured: the **Android Release `PERF` numbers**. ★ `PerfTrace`
**must be deleted before any release** and it stays in the tree until those numbers exist.

---

## 6. What to put to the BE engineer

Five items, in this order. The full text with evidence is at the end of
`docs/f5-findings-2026-08-15.md`.

1. Does `SpixiBotActionCode.kickUser` do anything for a plain group with no bot server? (D-4)
2. Is there a post-restore resync hook behind `localStorage.accountRestored`? (D-1)
3. Group lifecycle: rename, re-avatar, add member, delete group. **None exist.** (D-3, CI7)
4. Pin the Core import. `Spixi.csproj:304`. (See §2.)
5. **Can a reaction be REMOVED?** `Friend.addReaction` is Core and shows no counterpart.
   This one answer unblocks BOTH the D-10 residual and D-12.

⚠ **A5 and CI7 stay BLOCKED.** Group rename and add-member are still not in Core.
**Do not plan around them.**

---

## 7. How to work on this repo

### The cloud twin

```
git clone --depth 1 https://github.com/Rexoflex/Spixi.git
cd Spixi
git fetch --depth 30 origin redesign/frontend && git checkout FETCH_HEAD
npm install jsdom --no-save
```

### The build chain

```
node scripts/build-demo-bundle.mjs
node scripts/build-shells.mjs
node scripts/smoke-test.mjs
```

Strings, when a string changed:

```
node scripts/extract-strings.mjs
node scripts/build-locales.mjs
node scripts/build-strings-iife.mjs
node scripts/verify-locales.mjs
```

**Rule: bundle BEFORE shells.**

### Shipping to Damir

Ship a **tarball** through `SendUserFile` + `device_commit_files`, then:

```
tar xzf spixi-348b.tar.gz
```

⚠ **Do NOT pass `--overwrite`.** Windows `tar.exe` is bsdtar. It rejects the flag and it
overwrites by default anyway.
⚠ **The mount CANNOT unlink.** Never plan a step that deletes a file on the mount.
⚠ PowerShell, not CMD. `Remove-Item -Recurse -Force`, not `rmdir /s /q`.

### The rules that govern this repo

- Verify a plan against the code before you build it.
- Measure before you optimise (**#294**).
- **Zero C# unless you have evidence** (**#215**). Ixian-Core is NOT in this repo.
- Smoke stays green at the stated baseline.
- **Every fix gets a mutation-honest pin, and you must PROVE it fails when reverted.**
- Bundle before shells.
- **Damir makes all commits.**
- Add a `DECISIONS.md` row for every significant decision, when you make it.
- Run the **#46 loop** on anything substantial — disjoint auditors, then a fresh reviewer
  told to break your verdict, and repeat until a round finds no product defect.

### ★ The security handover gate

**The redesign must introduce NOTHING.** Before the app goes to the BE engineer, an
introduced-vs-inherited sweep runs over the whole delta from the fork point `0e85a4b8`.

**One question per finding: does this exposure exist at the baseline?**
- **No** → we introduced it → **we fix it before handover.**
- **Yes** → legacy → he sees it untouched.

He must see only his own legacy issues, never ours. The sweep runs LAST, but **apply the
lens while you build.** Any batch that adds an `ixian:` verb, a `spixi.*` localStorage key,
a WebView setting, an `innerHTML`/`eval` sink, a network fetch, or a log line must ask the
question as it goes.

★ **Reply-to is the one to watch** — its body marker is a NEW parsing surface for
peer-controlled content.

---

## 8. Do not re-attempt

- **A spinner delay or a log fade to hide chat entry latency.** #343 tried it, it made chat
  entry WORSE on device, and it was reverted the same night. A DIRECTIONAL transition is a
  different mechanism (I-1) and is allowed — mobile only, and measured on the A52.
- **`--surface-interactive-selected` on a row surface.** Rejected in 2026-07. It measures
  1.027:1 in dark and is DARKER than the canvas.
- **`DeviceInfo.Idiom` to detect desktop.** It is posture-dependent. Use `DeviceInfo.Platform`.
- **Making chat match chat info by removing `webView.Opacity = 0`.** The chat spinner is
  invisible BY DESIGN.
- **Composing chat into a shared WebView.** #220 rejected it and #221 / SECURITY.md §1
  forbid it. Chat lives in its OWN WebView, isolated from the wallet.

---

## 9. The next batch — suggested order

★ **DAMIR, 2026-08-15: TIPPING AND GROUP BEHAVIOUR GET THEIR OWN SESSION.** His words:
*"tipping and group behavior needs its own separate session that we need to test out
everything."* Do not fold these into a mixed batch again. One session, on device, that walks
the whole surface: tip in a 1:1, in a private group, in a bot room, over balance, twice on
one message, and after a restore · leave, kick, ban, delete, add member, rename, re-avatar,
and the owner case for each · in a normal group, a bot room and a blind room.
**D-4 · D-10 residual · D-12 · D-14 · D-15 · CI7 all live in that surface**, and most of
them need the BE engineer to answer first. Book the BE answers BEFORE the session, or it
turns into another triage pass.

1. **Damir commits and pushes #348 + #348b.** Nothing else starts until the tree is clean.
2. ★ **D-16 — the press fill, FIRST of the visual work.** Damir chose to defer it rather than
   ship it unaudited. A (a ~250 ms floor on `data-pressed`, with the #346 cancel latch still
   winning) + B (ease the selected-row hover in at the fill duration). Then C — the
   `--surface-action-tonal-hover` ramp — **together with I-2**, because they are the same
   disease one step apart. **#46 loop and mutation pins required.**
3. **Android F5 pass** — A9, A7, A5/I-8, plus the Release `PERF` numbers. §5.
4. **D-9** — finish the delete-account story on device.
5. **Cheap and unblocked:** I-2 (chip outline), I-6 (digit grouping in the tip amount).
6. **D-6**, when the engineer answers.
7. **I-1 + the welcome flicker**, as ONE native transition treatment. Mobile only. Measure.
8. **D1 reply-to** — design is settled (see §10). Verify the two-device id round-trip FIRST.
9. **The security sweep**, LAST.

---

## 10. D1 — reply-to. The settled design.

No protocol change. The reference rides in the BODY as a message ID. The quote is resolved
LOCALLY and its text is never sent. Tapping the quote jumps to the referenced message.

★ **Verify the two-device id round-trip before you build anything.**

★ **Security:** the body marker is a new parsing surface for peer-controlled content. It is
the highest-risk item on the list under the handover gate. Treat every field of it as
hostile.
